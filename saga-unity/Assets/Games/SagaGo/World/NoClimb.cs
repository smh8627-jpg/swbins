using UnityEngine;

namespace Saga.Go.World
{
    /// <summary>
    /// PLAN.md 107 ② — 이 충돌체(또는 부모)에 붙어 있으면 `PlayerController` 가 기어오르지 않는다
    /// (지도 경계벽처럼 "막는 벽"이지 "오를 벽"이 아닌 것).
    /// </summary>
    public class NoClimb : MonoBehaviour
    {
    }
}
