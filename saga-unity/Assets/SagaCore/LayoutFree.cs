using UnityEngine;

namespace Saga.Core
{
    /// <summary>
    /// PLAN.md 110 ⑤c-2 — 배치 점검(`UiLayoutCheck`)이 겹침에서 빼는 **움직이는 표지**(지도 위 내 위치 화살표 등).
    /// 일부러 다른 글자 위에 얹히는 것에만 붙인다 — 고정 배치에 붙이면 진짜 겹침을 숨긴다.
    /// </summary>
    public class LayoutFree : MonoBehaviour { }
}
