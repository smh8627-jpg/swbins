# Unity generated / local-only — never commit (saga-unity/CLAUDE.md 참고)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]serSettings/
[Mm]emoryCaptures/
# PLAN.md 110 — 안드로이드 빌드(IL2CPP·Bee) 임시 폴더
.utmp/

# Visual Studio / Rider generated project files (재생성 가능)
.vs/
.idea/
*.csproj
*.unityproj
*.sln
*.slnx
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.pidb.meta
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Unity3D generated meta files that should not be ignored are already tracked;
# these are asset-store/package-cache leftovers only
/[Aa]ssets/[Aa]ssetStoreTools*
/[Aa]ssets/Plugins/Editor/JetBrains*

# Build artifacts
*.apk
*.aab
*.unitypackage
*.app
crashlytics-build.properties

# Mixamo(Adobe)에서 사람이 직접 받은 실사 캐릭터/애니메이션 — 약관상
# "원본 캐릭터·애니메이션을 독립 에셋으로 재배포" 금지라 이 공개 저장소엔
# 안 올린다(saga-forest가 같은 이유로 assets/models/people/realistic/를
# 막아 둔 것과 동일 원칙, PLAN.md 66-2장 "③ 캐릭터 에셋" 참고). 로컬엔
# 그대로 남아 게임은 정상 동작한다 — 다른 PC에서 다시 받는 절차는
# docs/ASSET_GUIDE.md에 적어 둔다.
Assets/Art/CharactersRealistic/

# Crash reports
sysinfo.txt

# OS
.DS_Store
Thumbs.db
