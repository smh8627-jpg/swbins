{
  "name": "deungyong-go-mobile",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "역사GO 모바일 앱 껍데기 (Capacitor) — 게임 파일을 그대로 담는다",
  "scripts": {
    "www": "node build-www.mjs",
    "sync": "node build-www.mjs && npx cap sync",
    "android": "node build-www.mjs && npx cap sync android",
    "apk": "node build-www.mjs && npx cap sync android && node build-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.1",
    "@capacitor/geolocation": "^6.1.0",
    "@capacitor/ios": "^6.2.1",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
