# 사가고 (saga-go)

> 시리즈 전체 인계(다섯 게임 표·지시 이력·함정·다음 할 일)는 **`../SAGA-HANDOFF.md`** 다.

**포켓몬GO 를 그대로 모방한 판.** 걷고(보급), 만난다(등용·포획).

## 사가 시리즈 — 완전히 별개인 다섯 프로젝트

| 게임 | 폴더 | 포트 | 원작 | 세이브 키 |
|---|---|---|---|---|
| **사가고** | `saga-go` | 8791 | 포켓몬GO | `deungyong-go/save/v1` |
| **사가블로** | `saga-dungeon` | 8792 | 디아블로 | `yeoksa-dungeon/save/v1` |
| **사가의숲** | `saga-forest` | 8793 | 동물의숲 | `yeoksa-village/save/v1` |
| **사가스토리** | `saga-story` | 8794 | 메이플스토리 | `yeoksa-side/save/v1` |
| **사가국지** | `saga-realm` | 8795 | 삼국지(코에이) | `saga-realm/save/v1` |

다섯 게임이 다 **원작 장르를 그대로 모방**한다.
그래서 원작에 없는 축은 각 게임에서 뺐다 — 이 게임(포켓몬고)에는 던전도 장비도
문답도 없다. 던전은 사가블로, 문답과 삼국지는 사가국지가 맡는다.

**공용 파일은 복사본이다** — `data.js`(인물 70·펫 41)·`sprite.js`·`core.js`·`hero.js` 가
다섯 프로젝트에 각각 한 벌씩 있다. 인물을 하나 추가하면 다섯 곳을 고쳐야 한다
(사용자가 그 값을 알고 고른 구조다). 세이브도 완전히 분리돼 있다.

> 이 폴더의 세이브 키는 `deungyong-go/save/v1` 로 **그대로 두었다** — 지금까지의
> 진행(도감·기록)이 거기에 있고, 키를 바꾸면 사라진다. 앱 패키지 id
> (`com.swbin.deungyonggo`)도 같은 이유로 그대로다.

## 실행

```
run.bat          오프라인 모드 — 파이썬 정적 서버 (http://127.0.0.1:8791)
run-online.bat   온라인 모드   — 노드 서버 + AI 사관 (http://127.0.0.1:8790)
run-phone.bat    폰에서 하기   — 같은 서버를 https·0.0.0.0 으로 (인증서 자동 생성)
build-pc.bat     집 PC 용 단독 실행판 만들기 → dist/사가GO.html (+ play.bat · 사용법.txt)
```

**집 PC 에서 하려면** `build-pc.bat` 을 한 번 돌려 `dist/` 를 통째로 옮긴 뒤
`play.bat` 을 더블클릭한다. css·js 를 전부 한 파일에 녹여 넣으므로 서버도 파이썬도
필요 없다. 세이브는 그 PC 의 브라우저에 남고, 실제 위치(GPS)는 파일로 열면 브라우저가
막으므로 키보드 이동 + 🤖 자동 순행으로 논다.

Python이 없으면 `index.html` 을 브라우저로 직접 열어도 동작한다.
온라인 모드는 `server/` 를 쓴다(→ `server/README.md`). **서버가 없어도 게임은 그대로 돌아간다.**

폰에 넣으려면 `mobile/` (→ `mobile/README.md`).

## 조작

| | |
|---|---|
| 이동 | `WASD` / 방향키 (`Shift` 달리기) |
| 조우 | 파란 원 안에 들어온 대상을 **클릭** |
| 3D 줌 | 휠 · 두 손가락(핀치) · `+`/`-` 로 카메라를 당기고 민다 (×0.7 ~ ×9, 기본 ×4).
          시점 버튼에 `3D ×4.0` 으로 지금 배율이 뜬다 |
| 등용 | 인물 기질에 맞는 어필을 3라운드 안에 골라 호감도 100 달성 (★4 이상은 기질이 숨겨져 있다) |
| 포획 | 흰 고리가 **초록 테** 안에 들어왔을 때 `Space` · 버튼 · **화면을 위로 밀기**
          (3D 무대가 꺼져 있으면 바늘이 **초록 구간**일 때 `Space`) |
| 만난다 | 화면 아래 근처 대상 카드의 **만난다** 버튼으로도 조우 |
| GPS | 우측 상단 `📡` → 실제 위치로 전환. 그 순간 지도가 현재 동네로 이동한다 |
| 지도 | `🗺️` 로 어두운 지도 / 밝은 지도 / 흰 지도 전환 |
| 시점 | `2D → 2.5D → 3D` 버튼 순환 (설정이 저장된다). 3D 렌더러가 켜져 있으면 **카메라 각도**가 바뀐다 |
| 등신 | `4등 → 2등 → 8등` 버튼 순환 — 캐릭터 비례 (설정이 저장된다) |
| 양식 | `🖌️ 전통 → 📗 그림책 → 🎴 만화` 순환 — 그림 양식 (설정이 저장된다) |
| 배속 | 게임 화면에는 버튼이 없다 — **`_admin.html` 의 균형 손잡이**에서 올린다(이동만 빨라지고 규칙은 그대로) |
| 자동 | `🤖` 자동 순행 켜기/끄기. 세부(등용·포획/승급/역참/성채/길조)는 🔮 사관 시트 |
| 역참 | 🏮 지도 위 고정 지점. 사거리 안에서 눌러 **들른다** — 등용서·사료·금, 이따금 천거장 |
| 천거 | 독의 ✉️ → 받은 천거장을 **행낭**에 넣는다. 넣은 뒤부터 걸은 거리를 센다 |
| 적도 | 🏴 이따금 역참이 **점거된다**. 그 동안은 보급이 없다 — 눌러 교전으로 물리치면 풀리고, 그 자리에 **암영(暗影)** 한 마리가 남는다 |
| 정화 | 암영은 도감에 바로 들지 않는다. 📖 도감 맨 위 **암영 칸**에서 ✨단사를 써서 **정화**해야 등록되고, 그 종의 영초를 얹어 준다 |
| 성채 | 🏯 다른 세력이 지킨다. 눌러서 **도전** — 동행이 차례로 겨뤄 셋을 밀어내면 점령 |
| 교전 | 성채·토벌에 붙으면 **실시간 전투**가 열린다. `Space` 속공 연타 · `F` 필살(기 만충) · `D` 회피(강타 예고에). 무대에서 **동행 선두와 적장이 마주 서서** 뛰어들고 맞고 피한다 |
| 연성 | 도감의 펫 칸을 눌러 상세로 → **연성**(영초🌿+단사✨으로 보정 올리기) · **승화**(영초를 많이 써서 상위 종) |
| 반려 | 같은 펫 상세에서 **반려로 세운다** — 곁을 걷는 한 마리. 걷기만 해도 그 종의 영초가 나온다 (🍖 사료를 먹이면 우애) |
| 시트 | 우하단 독(서당·도감·사관·기록) → 좁은 화면에서는 하단 시트로 바뀐다 |
| 상세 | 도감 칸을 **누르면** 인물 상세(레벨·승급·펫·열전·동행)가 열린다 |

## 구조

```
index.html
css/style.css
js/
  data.js         인물 · 펫 · 열전 데이터  ← 캐릭터 추가는 여기만 건드린다
  sprite.js       사람 · 짐승 · 건물을 코드로 그리는 렌더러 (등신 비례 포함)
  core.js         상태 · 저장 · 이벤트 버스 · 공적(功績)
  account.js      가입해서 저장 — 프로필마다 세이브 키가 갈린다
  hero.js         인물 레벨 · 승급 · 최종 능력치 · 부대 전투력  ← 힘은 여기 하나만 읽는다
  world.js        위치 공급자 · 실제 지도 타일 · 스폰 · 역참/성채 자리 · 시점(2D/2.5D/3D)
  world3d.js      3D 렌더러 — 지도 타일을 지면에 깔고, 사물을 절차적으로 세우고,
                  시각·천후를 보고 해를 띄우고, 카메라를 몬다. **판정에는 닿지 않는다**(그리기만)
  actor3d.js      3D 배우 — 사람·짐승·역참·성채를 **도형으로 조립한다**.
                  외형은 sprite.js 의 것을 읽는다(lookOf·beastFormOf·beastColorOf)
  vendor/three.iife.js  three.js r169 를 IIFE 한 덩이로 묶은 것 (build/three 에서 만든다)
  encounter.js    등용 / 포획 (자동 조우 autoResolve · 등용 처리 gainHero 도 여기)
  encounter3d.js  조우 무대 — 등용·포획을 3D 로 세운다. **판정은 encounter.js 것을
                  그대로 쓰고** 조준 고리·던지기·무대 카메라만 맡는다
  bag.js          행낭(行囊) — 원작의 가방 (소모품만. 장비는 없다)
  weather.js      천후(天候) — 원작의 날씨 부스트 (세 시간마다, 시각 해시)
  quest.js        사명(使命) — 원작의 필드 리서치 (인장 일곱이면 명사)
  raid.js         토벌(討伐) — 원작의 레이드 (성채에 45분)
  station.js      역참(驛站) — 원작의 포켓스탑
  rogue.js        적도(賊徒) — 원작의 로켓단. 역참 점거 · 암영 · 정화
                  (판정 층 at·fight·purify / 화면 층 open 으로 갈라 두었다)
  letter.js       천거장(薦擧狀) — 원작의 알
  fort.js         성채(城砦) — 원작의 체육관
  duel.js         교전(交戰) — 원작의 탭 전투 (판정 층과 화면 층이 갈려 있다)
  growth.js       연성(鍊成)·승화(昇華) — 원작의 사탕과 진화. 승화 사슬은 여기만 안다
  buddy.js        반려(伴侶) — 원작의 버디. 걷기와 영초를 잇는 자리 (수급은 growth 로만 넣는다)
  net.js          오프라인/온라인 모드 · 서버 통신 (여기만 서버를 안다)
  ai.js           사관(AI) 기능 · 천기(토큰 예산) · 길조 보정
  auto.js         자동 순행 — 목표 선택만 하고, 조작은 각 모듈의 공개 함수로 넣는다
  ui.js           프로필 · 재화 · 근처 대상 · 시트(천거/도감/사관/기록) · 상세
  game.js         부트 · 메인 루프 · 걷기 보급
  _expansion/     분리 보관 — 방치 전투·환생·방치 강화 (→ _expansion/README.md)
build/            build-single.mjs — PC 단독 실행판(dist/사가고.html) 빌더
dist/             단독 실행판 산출물 (build-pc.bat 이 만든다)
_test.html        자가진단 178항목 (브라우저로 열면 바로 결과가 나온다)
                  손잡이(어드민)를 읽지 않는다 — DG_NO_TUNE
_admin.html       어드민 — QA 운영판 (세이브 편집 · 균형 손잡이 · 시나리오 프리셋 ·
                  스냅샷 · 데이터 조각 · 백업). `#selftest` 로 도구 자체를 점검한다.
                  탭은 주소에 남는다(`#tune` · `#preset` …)
_demo.html        스크린샷용 데모 상태
                  (#map · #walk · #dex · #oracle · #log · #detail · #detail2 · #pet ·
                   #station(역참에 들른 화면) · #letters(천거장) · #fort · #fortmine ·
                   #duel(교전 한복판) · #dueltell(강타 예고) ·
                   #duelhit(내가 뛰어드는 순간) · #duelfoe(적이 달려드는 순간) —
                   동작은 0.24~0.46초짜리라 데모에서만 중간 프레임에 멈춰 세운다 ·
                   #petgrow(연성·승화) ·
                   #rogue(점거된 역참) · #roguewin(탈환 — 암영이 남은 참) ·
                   #dark(도감의 암영 칸) ·
                   #buddy(반려로 세우기 전 화면 — 세워 둔 쪽은 #pet 이 청룡이다) ·
                   #auto(지도에서 자동 순행) · #perf ·
                   해시에 **stat** 을 붙이면 3D 상태(타일·건물·배우·조명·프레임·픽셀)가
                   좌하단에 뜬다,
                   해시에 2d·3d·chibi·tall·anime·story 를 붙이면 시점·등신·양식도 바뀐다,
                   **noon·dusk·night** 은 3D 조명의 시각을 못박고(천후도 맑음으로 붙든다),
                   **meet** 은 조우 무대를 연다(짐승 — 조준 고리가 보인다),
                   **meethero** 는 등용 쪽, meetbeam 은 포획 빛기둥을 건다,
                   무대의 **움직이는 순간**은 데모에서만 한 곳에 세워 둔다(0.4초라 그냥은
                   못 잡는다 — 교전의 강타 예고와 같은 사정): throw(날아가는 중) ·
                   land(맞기 직전) · miss(놓쳐 물러섬) · nod(공감해 다가섬) · cold(미지근),
                   뒤에 -b 를 붙이면 시트를 끝까지 내려서 찍는다 — 예: #dex-b)
_gallery.html     스프라이트를 한 판에 늘어놓고 눈으로 보는 페이지
                  (?only=hero|pet&w=110&h=126&style=anime|story&prop=chibi|tall,
                   ?compare=1 은 같은 인물을 전통/그림책/만화 셋 나란히 놓는다)
_autoprobe.html   자동 순행이 실제로 판단하는지 보는 페이지 — 결과를 화면에 쓴다
                  (?sim=600 은 게임 루프를 직접 굴려 10분치 보고서를 찍는다.
                   헤드리스 --screenshot 에서는 rAF 가 거의 돌지 않아 움직임을 못 본다)
_swreset.html     서비스 워커 해제 + 앱 캐시 삭제 (?go=demo|game 이면 그 화면으로 이동)
                  "고쳤는데 안 바뀐다" 싶을 때. 세이브는 건드리지 않는다
_probe.html       스프라이트 하나를 카드 배경 없이 크게 그리는 판정용 페이지
manifest.json     PWA — 홈 화면 추가 · 전체화면 · 아이콘
sw.js             서비스 워커 — 앱 캐시 + 본 지도 타일 캐시 (오프라인 모드)
icons/            앱 아이콘 (登, 어두운 지도 바탕)
server/           온라인 모드 서버 — 정적 파일 + Claude 프록시 (API 키는 여기만)
mobile/           Capacitor 껍데기 — 안드로이드 APK · iOS Xcode 프로젝트
```

던전·문답·장비·경영은 **이 판에 없다.** 던전은 `saga-dungeon`, 문답과 경영은 `saga-realm`,
마을은 `saga-forest`, 사냥터는 `saga-story` 가 맡는다 — 여기는 걷고 만나는 축만이다.

세이브는 브라우저 `localStorage` (`deungyong-go/save/v1`) 에 저장된다.

## 확장할 때

### 캐릭터 추가
`js/data.js` 의 `HEROES` / `PETS` 배열에 항목 하나를 추가하면 끝이다.
도감·스폰·전투·부대 편성에 자동으로 반영된다. `id` 는 세이브 키이므로 한 번 정하면 바꾸지 않는다.

### 인물 성장 (구현됨)
능력치는 **세 층으로만** 쌓인다. 순서를 바꾸면 화면 수치와 전투력이 어긋난다.

```
기본치(data.js stats, 절대 안 변한다)
  × 성장 배율 (레벨 ×2.2%/Lv · 승급 ×6%/★)
  + 펫 보정 (배율을 타지 않는다)
= DG.hero.stats(heroId)      ← 전투력·설득·화면이 모두 이 함수만 읽는다
```

- 레벨은 최대 30. 등용·포획에 성공하면 **동행 전원**이 경험치를 나눠 받는다
- 승급은 최대 ★5. 같은 인물을 또 등용해 생긴 **중복분 + 금** 을 소모한다
  (중복은 도감 칸에 `+n` 으로 표시된다)
- 상세 화면의 능력치 바에 흰 세로선이 있는데, 그게 **타고난 기본치** 경계다

`js/hero.js` 하나만 보면 되고, 능력치를 직접 계산하는 코드를 새로 만들지 않는다.

### 역참 · 천거장 · 성채 (구현됨 — 원작의 세 축)

원작(포켓몬GO)에서 걷는 이유가 되는 세 가지를 같은 자리에 같은 역할로 옮겼다.

| 원작 | 이 판 | 파일 |
|---|---|---|
| 포켓스탑 (회전 · 5분) | **역참(驛站)** 🏮 들른다 · 5분 | `station.js` |
| 알 (2·5·10km · 부화기 3칸) | **천거장(薦擧狀)** ✉️ 2·5·10km · 행낭 3칸 | `letter.js` |
| 체육관 (팀 색 · 사기 · 코인) | **성채(城砦)** 🏯 세력 색 · 사기 · 공물 | `fort.js` |
| 레이드 (45분 · 패스 · 포획 기회) | **토벌(討伐)** ⚔️ 45분 · 격문 · 등용 기회 | `raid.js` |
| 필드 리서치 (과제 · 스탬프 7) | **사명(使命)** 📋 과제 · 인장 7 | `quest.js` |
| 가방 (볼·열매·향로·행운의 알) | **행낭(行囊)** 🎒 등용서·사료·별미·향·축문 | `bag.js` |
| 날씨 부스트 | **천후(天候)** 세 시간마다 · 여섯 종 | `weather.js` |
| 전투 (탭 · 빠른공격/차징/회피) | **교전(交戰)** ⚔️ 속공·필살·회피 · 60초 | `duel.js` |
| 사탕(종별) · 별사탕(공통) | **영초(靈草)** 🌿 종별 · **단사(丹砂)** ✨ 공통 | `growth.js` |
| 강화(파워업) · 진화 | **연성(鍊成)** 20단 · **승화(昇華)** 여섯 사슬 | `growth.js` |
| 버디 (함께 걷기 · 하트 · 베스트) | **반려(伴侶)** 🐾 등급별 거리 · 우애 넷 | `buddy.js` |

자리는 셋 다 **좌표 해시**라 같은 땅은 늘 같다(`world.js`). 사라지지도 배회하지도 않는다.

```
행낭    상한 250. 담기는 것은 전부 소모품이다 — **장비는 되살리지 않는다**
        (원작에 없는 축이라 다섯 판으로 가를 때 saga-dungeon 으로 보냈다).
        별미 2분 포획+25%p · 향 15분 붐빔 · 축문 30분 경험치 두 배
천후    세 시간마다 바뀐다. 비엔 짐승이, 바람엔 사람이, 안개엔 신수가 많고,
        눈에는 인적이 드문 대신 귀한 것이 온다. 맑음은 무력, 흐림은 지력 기질을 민다.
        실제 기상 정보를 받지 않는다 — **서버 없이도 같아야 하므로** 시각 해시다
사명    역참에서 받는다(최대 셋). 채우면 보상과 **인장**, 인장은 하루 한 개.
        일곱이면 명사(★5)가 코앞에 선다
토벌    성채에 적장이 45분간 든다(시간 칸 해시라 어느 기기에서나 같다).
        격문 한 장을 쓰고 **교전에서** 기세를 다 깎으면 그 자리에서 등용 기회.
        자동 순행은 교전을 열지 않고 열 합으로 즉시 판정한다(예전 규칙 그대로)
교전    **무대**에서 동행 선두와 적장이 마주 선다. 속공에 뛰어들고, 맞으면 번쩍이며
        밀리고, 예고에 맞춰 피하면 옆으로 샌다 — 동작은 전부 CSS 애니메이션이라
        **판정 층과 무관하다**(헤드리스에서 안 돌아도 결과는 같다).
        속공(연타·기가 찬다) · 필살(기 만충에 한 방) · 회피(강타 예고에만 뜻이 있다).
        60초 안에 기세를 다 깎아야 한다. 사기가 먼저 다하면 패퇴.
        토벌은 **낸 피해가 곧 판정**이고, 성채는 그 성과가 배율(0.65~1.45)로 얹힌다
연성    잡을 때마다 그 종의 **영초** 셋과 등급만큼의 **단사**가 들어온다(원작의 사탕·별사탕).
        둘을 써서 20단까지 올리고, 한 단마다 장착 보정이 6% 오른다.
        **인물의 승급과는 다른 축이다** — 인물은 도감의 중복분을 직접 태우고(hero.js),
        펫은 자원을 모아 쓴다. 도감의 마리수는 기록이니 연성·승화로 줄지 않는다
3D      화면은 **three.js 로 그린다**(`world3d.js` · `actor3d.js`). 지도 타일을 지면에
        텍스처로 깔고, 지도에 없는 높이는 `terrainAt` 격자로 **절차적으로 세운다**.
        **3D 는 밝은 지도(voyager)를 쓴다** — 2D 의 어두운 지도(dark_all)는 UI 뒤
        배경이라 어두워도 됐지만, 3D 에서는 그 지도가 지면 전체가 되어 화면이 새까맣다.
        **CARTO 는 voyager 만 `rastertiles/` 아래 둔다**(dark_all·light_all 은 루트).
        루트로 부르면 404 라 3D 지면이 통째로 빈 종이색이 된다 — 2026-08-26 까지
        그 상태였다. 3D 에서는 **늘 @2x 타일**을 쓴다: 타일 한 장이 242m 로 펼쳐져
        1m 가 한 픽셀이라 낮은 카메라 앞에서 지도가 뭉개진다.
        지도가 깔린 자리에서는 **절차적 물을 세우지 않는다** — 물은 높이가 아니라
        색이고 지도가 이미 칠한다. 세우면 지도에 없는 곳에 호수가 떠서 어긋난다.
        WebGL 이 없거나 켜다 실패하면 **조용히 2D 로 돌아간다**. 끄려면 어드민의
        손잡이 `world.render3d` 를 0 으로 둔다.

        **2단계에서 네 가지가 더 들어갔다.**
        · **배우가 입체다** — 사람·짐승·역참·성채를 `actor3d.js` 가 도형으로 조립한다.
          외형은 `sprite.js` 의 것을 그대로 읽으므로(갓·도포·망토·무기 · 짐승의 형태와
          무늬) 지도 위의 인물과 도감 초상이 같은 사람으로 보인다. **3.5등신**이다 —
          실사 비례면 지도에서 머리가 점이 되어 누군지 안 보인다. 걸으면 다리가
          엇갈리고 진행 방향으로 돈다(방향은 판정에 없으니 화면 층이 프레임 차이로 만든다).
          되돌리려면 손잡이 `world3d.mesh` 를 0 으로 — 1단계의 빌보드로 돌아간다
        · **시각과 천후를 따라 해가 뜨고 진다**(`lightingAt`). 새벽·낮·노을·밤에
          해의 높이·색·세기, 하늘색, 안개 거리, **지도 타일에 곱하는 물감**이 함께 바뀐다
          (지도 이미지는 늘 한낮 그림이라 안 곱하면 밤에 땅만 대낮이다).
          밤에는 길가 등롱에 불이 들어오고 플레이어를 따라다니는 작은 불 하나가 켜진다 —
          안 켜면 배우가 실루엣으로만 남는다. 비·눈·안개는 해를 가리고 시야를 좁힌다.
          늘 한낮으로 두려면 손잡이 `world3d.dayNight` 를 0 으로
        · **건물 밀도**(`propPlan`) — 격자마다 **도심도**를 뽑아 번화한 칸은 높은 집이
          여덟 채까지 들어차고 변두리는 낮은 기와집 두엇에 나무가 섞인다. 길가에는
          등롱, 빈 들에는 풀·바위가 선다(아무것도 없는 칸은 구멍처럼 보인다).
          잔 사물은 **가까운 절반 안에서만** 세운다. 배수는 손잡이 `world3d.density`
        · **조우 연출** — 조우 창이 열리면 카메라가 **나와 대상 사이**로 돌아 둘을 한
          화면에 담고, 창이 닫히면 놓아 준다. 잡히면 그 자리에 빛기둥이 서고, 사라지는
          대상은 떠오르며 흩어진다(곧바로 지우면 점멸하듯 없어진다)
        · **줌** — 원작만 한 배율(아바타가 화면의 1/8)로 열면 **야생 대상이 한 마리도
          안 보인다.** 이 판의 대상은 70~320m 밖에 생기는데(SPAWN_RADIUS) 3D 카메라는
          27m 앞을 보기 때문이다. 그래서 휠·핀치·`+`/`-` 로 당기고(×0.7 ~ ×9),
          기본을 **×4** 로 둔다. 멀리 물러날수록 더 내려다보고(높이는 배율의 1.12제곱),
          **×4 부터는 그림자를 끈다**(그 높이에서는 몇 픽셀도 안 되는데 그림자 맵이
          가장 비싸다). 배율은 세이브에 남고 시점 버튼이 `3D ×4.0` 으로 늘 보여 준다.
          **판정에는 안 닿는다** — 스폰 거리도 조우 사거리도 그대로다
        · **먼 대상은 표식처럼** — 멀리 있는 배우를 원근 그대로 두면 점이 된다.
          45m 밖부터 거리의 0.85제곱만큼 키워(최대 4배) 화면에서 거의 같은 크기로
          남긴다. 나에게도 같은 보정을 준다 — 안 그러면 크게 당겼을 때 나만 점이 된다

조우 무대  **3단계**(`encounter3d.js` · `css/encounter3d.css`). 원작에서 가장 오래 보는
        화면이 조우인데, 지도가 3D 가 된 뒤에도 그 창만 2D 카드로 남아 있었다.
        이제 대상을 누르면 **그 자리에 상대가 서서 이쪽을 본다** — 카메라가 상대 앞으로
        가고(거리는 상대 키에 비례한다), 조우 카드는 화면 아래로 내려앉고,
        지도 UI(근처 목록·독)는 걷힌다.
        · **조준 고리** — 여태 막대 위를 오가던 바늘을 **원의 반지름으로 옮겼다**.
          흰 고리가 좁아졌다 넓어지고, 초록 테 사이에 들어왔을 때 던지면 적중이다.
          **막대에서 맞는 값은 원에서도 맞는다** — 같은 값을 다른 모양으로 잰 것뿐이라
          판정은 한 줄도 안 바뀐다(자가진단이 201칸을 훑어 확인한다)
        · **던지기** — 화면을 위로 밀거나(원작의 그 조작) 버튼·스페이스. 셋 다
          `encounter.throwNow()` 라는 **같은 문**으로 들어간다. 사료가 포물선으로
          날아가 맞은 뒤에 결과 창이 뜬다 — 그 시각은 **타이머가 잡는다**(rAF 가 거의
          돌지 않는 자리에서도 창이 떠야 한다). 판정 자체는 던진 순간에 이미 끝나 있다
        · **등용** — 어필을 고르면 상대가 다가오거나(공감) 물러선다(미지근)
        끄려면 손잡이 `world3d.stage3d` 를 0 으로 — 예전 카드 화면으로 돌아온다.
        3D 가 아예 없으면(2D 시점·WebGL 없음·자가진단) 이 파일은 통째로 잠든다
손잡이  규칙 상수(보급 주기·반려 거리·연성 값·교전 상수·걸음 배속)를 **세이브가 아니라
        따로 사는 칸**(`deungyong-go/tune`)에서 잡는다. 어드민이 쓰고, 게임은 켜질 때
        한 번 읽는다(걸음 배속만 매번 읽어 곧바로 듣는다).
        **자가진단과 데모는 읽지 않는다**(`DG_NO_TUNE`) — 손잡이를 잡아 둔 기기에서
        판정과 스크린샷이 흔들리면 안 되기 때문이다. 잡혀 있으면 게임 상단에
        🎛️ 배지가 뜬다
반려    포획한 종 **하나**를 곁에 세운다. 등급마다 정해진 거리(★1 1km ~ ★5 5km)를
        걸을 때마다 그 종의 영초가 나온다 — 걷는 이유가 천거장 말고 하나 더 생긴다.
        함께 걸은 거리(+ 먹인 사료 한 줌 = 300m)로 우애가 깊어진다:
        면식 → 친교(2km) → 지기(10km) → 막역(30km). 깊어질 때마다 단사가 들어오고,
        한 몫에 나오는 영초도 는다(1 → 4). **막역이면 장착 보정이 10% 오른다 —
        다만 곁에 세워 둔 동안에만이다**(원작의 베스트 버디도 그렇다).
        우애는 **종별로 남아** 반려를 갈아도 지워지지 않는다. 사료는 포획에도 드는
        물건이라, 먹이는 만큼 잡을 밑천이 줄어든다.
        **인물의 동행(party)과는 다른 말이다** — 동행은 함께 싸우는 사람 다섯,
        반려는 곁을 걷는 짐승 하나다
승화    영초를 많이 써서 상위 종이 된다. **근거가 뚜렷한 여섯 사슬만** 잇는다 —
        잉어→청룡(용문) · 호랑이→백호 · 학→주작 · 곰→불가사리 ·
        불꼬리→화룡 · 뜀잉어→이무기. 연성 단계는 승계된다.
        사슬은 `growth.js` 에만 있다 — `data.js` 에 칸을 만들면 다섯 판이 나눠 든
        같은 도감의 동일성이 깨진다
역참    구역마다 둘. 들르면 등용서·사료·금·경험치 50, 22% 로 천거장.
        레벨을 조금 탄다 — 올라갈수록 같은 역참에서 더 받는다
천거장  행낭(3칸)에 넣은 뒤부터 거리를 센다. 받아만 둔 것은 걸어도 줄지 않는다.
        멀수록 높은 등급이 온다 — 10km 는 ★4~5. 나오는 것은 사람뿐이다
성채    구역의 4할. 그 등급의 인물을 셋 이상 가진 세력이 지킨다.
        동행이 차례로 겨뤄 수비 셋을 밀어내면 열두 시간 내 것이 되고, 시간당 40금
        (하루 480 상한). 패퇴하면 수비가 십 분 경계한다
```

- **세력이 먼저가 아니라 등급이 먼저다.** 세력을 고르고 등급을 맞추면 보(堡) 를 조조가
  지키는 일이 생긴다(그 세력에 낮은 등급이 없어 조건이 버려진다)
- 등용의 처리는 손·자동·천거장이 **`encounter.gainHero()` 하나**를 쓴다.
  금 수급을 붙일 때 두 곳을 고쳐야 했던 중복을 그때 없앴다
- 이 판의 `core.hash2` 는 **0~0.5 만** 돌려준다(지형 문턱값이 그 범위에 맞춰져 있어
  고치지 않기로 한 값이다). 자리를 고를 때는 **두 배로 펴서** 쓴다 — 안 그러면
  역참·성채가 구역의 왼쪽 위 사분면에만 선다. 진단이 그것을 못박는다

### 자동 순행 (구현됨)
손으로 걸을 수 없는 자리에서도 게임이 돌아가게 하는 층이다. 원칙이 하나뿐이다 —
**규칙을 새로 만들지 않는다.** `auto.js` 는 "무엇을 목표로 삼을지" 만 고르고,
조작은 원래 있던 공개 함수로 넣는다.

```
지도   world.walkTo()            손으로 탭하는 것과 같은 길
       encounter.autoResolve()   미니게임 확률을 그대로 굴린다
역참   station.autoVisit()       화면 없이 들르기만 한다
천거   letter.autoFill()         빈 행낭을 먼 것부터 채운다
성채   fort.autoAct()            이길 만하면 도전하고, 내 것이면 공물을 걷는다
```

- 목표 점수는 `희귀도 × 12 + (도감에 없으면 40) − 거리 × 0.14`.
  밑천(등용서·사료·명성)이 없는 대상은 후보에서 빼고, 후보가 없으면 **순행**한다
  (걸으면 250m 마다 보급이 들어오므로 그게 회복 수단이다)
- 밑천이 떨어지면 순행 대신 **채워진 역참**으로 걸어간다. 지나는 길에도 줍는다
- 성채는 **이길 가망이 있을 때만** 부딪힌다(부대 합이 수비 합의 75% 이상).
  패퇴는 십 분을 잃기 때문이다
- 천거장이 열릴 때 자동 중이면 결과 화면 대신 토스트로 알린다 —
  모달이 뜨면 자동이 조우를 못 하고 그 자리에 선다
- **일부러 자동화하지 않은 것**: 실제 위치(GPS)로는 대신 걷지 않는다.
  자동을 켜면 키보드 이동으로 바꾼다 — 위치 공급자를 흉내 내는 것보다 정직하다
- 화면을 보고 있는 동안에만 돈다(`requestAnimationFrame`). 방치 수익을 새로 만들지 않으려고
  일부러 이렇게 뒀다

### 그림 양식 (구현됨 · 3종)
`classic` 전통 삽화 · `story` 그림책(도감) · `anime` 일본 만화.

classic·anime 는 **도형을 다르게 그려서** 만든다(얼굴·머리·명암만 갈아끼우고 골격은 공유).
`story` 는 **다 그린 다음 픽셀에서 한 번 훑어** 만든다 — `sprite.js` `storyize()`:

```
1) 채도를 낮추고 살짝 밝힌 뒤 색을 계단(24)으로 끊는다   → 플랫 채색
2) 색이 꺾이는 곳·실루엣을 찾아 얇은 갈색 선을 얹는다     → 선화
```

선화·플랫 채색은 도형마다 손볼 성질이 아니라 그림 전체에 걸리는 성질이라,
이 방식이 인물 70 · 짐승 41 을 한 번에 맞추는 유일한 길이다.
비용은 캐시 미스 때 한 번만 든다(스탬프·초상 모두 캐시된다).

밟은 함정 세 가지 — 되돌리면 그대로 재발한다:

- **배경까지 훑으면** 세력 문양·산의 윤곽을 따라 그려서 인물을 가로지르는 네모가 생긴다
  → 카드에서는 인물을 딴 캔버스에 그려 **그것만** 훑고, 배경 선은 직접 긋는다
- **전통풍 얼굴 그대로 훑으면** 3/4 각도의 작은 눈이 계단·선에 먹혀 뭉갠 자국이 된다
  → 이 양식만 정면의 크고 단순한 이목구비(`storyFace`)로 그린다.
  이목구비는 투구 테를 피해 머리 중심보다 **아래**에 둔다
- **작은 지도 스탬프에 안쪽 선까지 그으면** 20~30px 안에서 형태가 뭉개져 뼈만 남는다
  → 스탬프는 `inner:false` 로 실루엣만, 그것도 어두운 지도에 묻히지 않게 **밝은 테**로 두른다

### PC 단독 실행판 (구현됨)
`build-pc.bat` → `build/build-single.mjs` → `dist/사가GO.html`.
`index.html` 이 부르는 순서를 그대로 읽어 css·js 를 인라인한다(순서를 두 곳에 적지 않는다).

- `String.replace` 의 치환값은 **반드시 함수로** 넘긴다. 문자열로 넘기면 코드 안의 `$'`
  (사관 시트의 달러 표시)이 특수 패턴으로 먹혀 그 자리에 문서 뒷부분이 끼어든다 —
  실제로 그렇게 깨져서 단독판이 `SyntaxError` 로 죽었다
- `play.bat` 내용은 **ASCII 만** 쓴다(한글 cmd 가 UTF-8 한글을 오파싱한다).
  파일 이름이 한글이라 경로를 못 적으니 폴더의 html 을 훑어서 연다
- file:// 에서 되는 것 / 안 되는 것: 세이브(localStorage)·지도 타일·초상화는 되고,
  서비스 워커와 GPS 는 안 된다(보안 컨텍스트가 아니다)

### 오프라인 / 온라인 (구현됨)
**오프라인이 기본이다.** 세이브도 계산도 전부 이 기기 안에서 끝난다.
온라인 모드는 거기에 **딱 하나**를 더한다 — AI 사관.
그래서 서버가 없거나 꺼져 있어도 게임은 아무 것도 잃지 않는다.

```
오프라인   localStorage + 서비스 워커(앱 캐시 · 본 지도 타일 캐시)
온라인     위의 전부 + /dg-ai/* 프록시가 살아 있을 때만 사관 기능
```

모드는 **사관(🔮) 시트**에서 바꾼다. 온라인으로 켤 때 서버에 닿지 못하면 그대로 오프라인으로 남는다.

#### 사관 — 토큰 사용량이 자원이다
AI 를 부를 때마다 **실제 토큰 사용량만큼 '천기(天機)'가 깎인다.**
천기는 서버가 잡는 하루 지출 한도(`DG_DAILY_CAP`, 기본 $0.5)이고,
게임 화면의 숫자는 표시용이다 — 한도는 서버가 판단한다.

| | 하는 일 | 게임에 남는 것 |
|---|---|---|
| ⚖️ 군략 | 지금 형편을 보고 다음 할 일 셋 | 읽을 것 (기록에 남는다) |
| 💬 대화 | 부대 인물에게 말을 건다 (열전·기질로 답한다) | 읽을 것 |
| 🔮 천기 | 앞길을 점친다 | 길조면 20분간 실제 보정 하나 |

**AI 가 게임 수치를 건드리는 자리는 길조 보정 하나뿐이다.** 나머지는 전부 읽을 것이다 —
AI 응답이 규칙을 바꾸면 밸런스를 손댈 수 없게 된다.

API 키는 **서버에만** 둔다. 브라우저에서 Anthropic 을 직접 부르면 키가 그 자리에서
공개되므로, `net.js` 는 우리 서버만 부른다. 자세한 건 `server/README.md`.

### 손가락 조작 (구현됨)
- **지도** — 빈 땅을 누르면 그쪽으로 걸어간다. 멀리 있는 대상을 눌러도 그쪽으로 걷는다.
  GPS 모드에서는 무시한다(실제로 걸어야 움직인다)
- **역참·성채** — 지도에서 그대로 누른다. 멀면 그쪽으로 걸어간다
- 입력이 늘어도 걸음 애니메이션은 안 고쳤다. **위치 변화만 보고** 만들기 때문이다

### 앱 (안드로이드 · 아이폰)
`mobile/` 에 Capacitor 껍데기가 있다. 게임 코드는 그대로 쓴다.

- **안드로이드** — 이 PC 에서 바로 APK 가 나온다: `cd mobile && npm run apk` (약 4.4MB).
  `JAVA_HOME` 이 JDK 8 을 가리켜도 빌드 스크립트가 JDK 17 을 골라 쓴다
- **아이폰** — Xcode 프로젝트까지 만들어 두었지만 **바이너리는 Mac 에서만** 만들 수 있다
  (Xcode·CocoaPods 는 macOS 전용, 실기기 설치는 Apple 개발자 계정 서명 필요).
  Mac 없이 지금 쓰려면 `run-phone.bat` → 사파리에서 열고 **공유 → 홈 화면에 추가**(PWA)
- **아이폰은 https 가 사실상 필수다.** 사파리가 안전하지 않은 출처에서 `navigator.geolocation`
  을 막기 때문에 http 로 열면 GPS 산책이 안 된다. 서비스 워커(오프라인 캐시)도 https 전용.
  그래서 `server/make-cert.mjs` 가 자체 서명 인증서를 만들고(SAN 에 이 PC IP·호스트명),
  서버는 인증서가 있으면 자동으로 https 로 뜬다. 폰에는 뿌리 CA 를 한 번 설치·신뢰시킨다
  (→ `server/README.md`). 네이티브 앱은 출처가 `https://localhost` 라 이 문제가 없다

### 업무 연동 (나중)
게임은 **공적(功績)** 이라는 단일 통로로만 성장한다.

```
[게임 행동: 등용·포획·역참·점령]  ──┐
                                   ├──> DG.core.gainFeat(n, '출처') ──> 칭호
[업무: Jira 종결·커밋·QC 통과]    ──┘
```

업무를 붙일 때는 어댑터가 `DG.core.gainFeat(점수, 'Jira 종결')` 을 호출하기만 하면 되고,
게임 쪽 코드는 한 줄도 고치지 않는다.

### 지도와 GPS
지도는 CARTO 베이스맵 타일(zoom 17)을 직접 그린다. 타일을 못 받는 환경이면
자동으로 프로시저럴 지형으로 폴백하므로 오프라인에서도 게임은 돌아간다.

- `DG.world.useKeyboard()` — PC 프로토타입 (기본, 판교 일대에서 시작)
- `DG.world.useGeo()` — 실제 위치. **첫 좌표를 받는 순간 그 지점을 원점으로 잡아** 지도가 현재 동네로 이동한다.
- `DG.world.cycleMapStyle()` — 지도 스타일 전환

월드 좌표를 **미터 단위**로 다루고 그릴 때만 픽셀로 환산하므로,
GPS로 바꿔도 조우 거리·구역 크기 계산이 그대로 통한다. (구역 한 변 576m, 조우 거리 46m)

기본 시작 위치는 `js/world.js` 의 `DEFAULT_ORIGIN` 에서 바꾼다.

### 스프라이트 (구현됨)
이미지 파일 없이 **캔버스 도형으로 형상을 그린다**(`js/sprite.js`). 벡터라 2.5D 원근에 맞춰
확대·축소해도 깨지지 않고, 관절 각도로 걷는 동작이 나온다.

```
human(ctx, o)     머리·몸통·팔·다리 + 투구/모자 + 무기. 걸음 위상으로 팔다리가 흔들린다
beast(ctx, o)     네발 / 조류 / 용 / 거북 / 물고기 / 두꺼비 / 도깨비
building(ctx, o)  한옥 기와 실루엣. 집무전·병영·성벽·저잣거리·서고·마구간·둔전·대장간·봉수대·사당
portrait(kind, ref, size)  오프스크린으로 한 번 그려 data URL 로 캐시 (도감·목록의 <img> 용)
```

- 인물 옷·갑옷·어깨 보호대 색은 **세력색**(`data.js` 의 `FACTIONS`)을 따른다. 촉은 녹색, 위는 청색 …
- 사람은 얼굴(눈·눈썹·코·입) · 머리카락 · 손 · 신발 · 찰갑 비늘 · 흉갑 · 허리 술 ·
  도포 소매와 흰 동정 · 치마 주름과 고름까지 그린다. **크기가 작으면 잔 디테일을 건너뛴다**
  (`fine`/`superFine` 게이트 — 작게 그릴 때 다 그리면 오히려 지저분해진다)
- 서 있을 때도 다리를 벌린다(`stance`). 안 그러면 두 다리가 겹쳐 죽마처럼 보인다
- 짐승은 형태(form) 외에 **무늬(`BEAST_PATTERN`)** 로 구분된다 —
  줄무늬(호랑이·백호) · 반점(판다) · 흰 점(사슴) · 반달(곰) · 송곳니(멧돼지) ·
  여러 꼬리(구미호) · 갈기(해태·불가사리) 등
- `shade()` 는 hex 와 `rgb()` 를 모두 받는다. 반환값이 `rgb()` 문자열이라
  색을 두 번 겹쳐 깎을 때 다시 들어오기 때문이다
- 등급 ★5 인물은 윤곽에 옅은 금빛이 돈다
- `portraitCard(kind, ref, w, h)` — 상세 화면용 큰 초상. 세력색 배경 + 큰 문양 + 등급 테두리
- 외형은 인물마다 일일이 적지 않는다. 특징이 뚜렷한 인물만 `LOOKS` 에 적고
  나머지는 **기질·등급에서 규칙으로** 만든다 (무인 → 창·투구·갑옷 / 지략가 → 두루마기·관모 …)
- 투구 종류: 갑주(gapju) · 투구 · 왕관 · 갓 · 관모 · 승려 · 비녀 · 댕기 · 상투 · 꿩깃(여포)
- 무기 종류: 청룡언월도 · 방천화극 · 창 · 검 · 도끼 · 활 · 부채 · 두루마기 · 붓 · 지팡이 · 방망이
- 적도 같은 엔진으로 그린다 (`data-enemy.js` 의 `kind`/`look`/`form`/`color`)

성능 주의: 헤드리스 브라우저의 소프트웨어 렌더링에서는 스프라이트가 느리다.
자가진단 페이지는 `window.DG_NO_DRAW = true` 로 캔버스 렌더를 건너뛴다.

### 걷기 애니메이션 (구현됨)
`world.js` 가 **위치 변화만 보고** 걸음 상태를 만든다. 그래서 키보드 이동이든 실제 GPS든
같은 코드로 걷는다 (`updatePlayerMotion`).

- 플레이어: 걸음 위상에 따른 상하 바운스 · 좌우 기울임 · 눌림(squash), 진행 방향 화살표,
  일정 거리(6m)마다 좌우 번갈아 찍히는 **발자국**(7초간 남고 지면에 그려져 원근을 그대로 받는다)
- 아이콘은 **부대 선두 인물**의 모습이 된다 (부대가 비면 🧭)
- 야생 인물·펫: 생성 위치를 중심으로 반경 15m 안을 2.6m/s 로 배회한다 (`wanderSpawns`).
  목적지에 닿으면 0.5~3초 쉬고 다음 목적지를 고르며, 걸을 때는 종종걸음·서 있을 때는 숨쉬는 흔들림.
- 조우 범위에 들어오면 **멈춰서 플레이어를 바라보며 폴짝 뛴다**.

### 2.5D 렌더 (구현됨)
캔버스를 두 장 쓴다.

```
#map-ground : 지도 타일 · 구역 격자 · 조우 반경   ← CSS perspective + rotateX 로 눕는다
#map        : 인물 · 펫 · 이름표                  ← 정면(빌보드). 눕히면 못 읽으므로 따로 그린다
```

빌보드의 화면 위치는 CSS 와 똑같은 원근 식을 JS 로 다시 계산해 맞춘다.

```
s = P / (P - v·sinθ)            원근 배율
X = u·s,  Y = v·cosθ·s          지면 로컬 → 화면
v = Y·P / (P·cosθ + Y·sinθ)     화면 → 지면 로컬 (클릭 판정)
```

- `DG.world.toggleTilt()` — 2D ↔ 2.5D. 끄면 두 면이 같은 평면이 되어 투영이 항등이 된다.
- 각도·원근은 `world.js` 의 `TILT_DEG`(44) · `PERSP`(1200) 로 조절한다.
- 지면 캔버스는 화면보다 크게 잡고(위쪽이 멀어져 축소되므로) 부족한 위쪽은 안개로 덮어 지평선을 만든다.
- 오브젝트는 먼 것부터 그려 겹침이 자연스럽게 되고, 크기도 원근 배율에 따라 커지고 작아진다.

시점 3모드(2D·2.5D·3D)는 각도·원근·앵커만 다르고 이 두 캔버스 구조는 그대로다.

## 자가진단

```
_test.html 을 브라우저로 열면 178항목 판정 결과가 표시된다.
```
데이터 무결성(인물 70·펫 41), 본편/확장 경계와 경영·던전·문답 부재 확인, 스폰,
등용/포획(명성·동행 경험치·금), 걷기 보급(주기·리셋 보호),
**행낭**(상한 · 역참이 못 넣음 · 쓰는 물건의 지속·이어붙임 · 축문·향의 실제 효과),
**천후**(세 시간 주기와 결정성 · 보정이 core.effect 로 흘러듦 · 비/바람의 편향 · 눈의 인적),
**사명**(셋 상한·중복 없음 · 진행 · 거두기 · 하루 한 인장 · 일곱이면 명사 · 역참 지급),
**토벌**(45분 · 같은 칸이면 같은 적장 · 격문 없으면 막힘 · 한 판 한 번 · 자동의 승산 판단),
**교전**(속공 쿨 · 기 만충에만 필살 · 강타는 예고 뒤 · 예고 없는 회피는 헛동작 ·
  사기 소진과 시간 만료 · 성과 계산과 열 덩이 접기 · 낸 피해가 토벌 판정이 되는지 ·
  성채에 배율이 실제로 얹히는지),
**연성·승화**(첫 포획에 영초가 정확히 셋인지 — 도감 등록과 겹치지 않는지 ·
  옛 세이브 환산이 한 번만 도는지 · 값이 올라갈수록 오르는지 · 상한 ·
  연성이 인물 능력치에 실제로 닿는지 · 사슬 여섯의 등급이 오르는지 ·
  승계와 도감 마리수 불변),
**걸음 배속·손잡이**(이동만 곱하는지 · 손잡이가 세이브와 다른 칸에 살고 진단이 남기지 않는지),
**교전 무대**(둘이 마주 서는지 · 친 쪽에 동작이 붙는지 — 이 항목만 화면 층을 본다),
**반려**(포획한 종만 세워지는지 · 등급별 거리마다 영초가 나오는지 · 곁을 걷는 종에만 들어오는지 ·
  세우지 않으면 걸어도 안 나오는지 · 기준점 리셋 보호 · 우애가 깊어질 때 단사 ·
  사료는 우애만 올리는지 · 깊을수록 영초가 느는지 · 장착 보정이 **세워 둔 동안에만** 붙는지 ·
  갈아타도 종별 우애가 남는지 · 저장 왕복 · 상세 화면),
**역참**(자리의 결정성 · 구역에 고르게 서는지 · 보급 · 쉬는 시간 · 세이브 정리 · 자동 연동),
**천거장**(가방 상한 · 행낭에 넣어야 세는 규칙 · 진행 · 열림 · 등급 · 역참 지급 · 자동 채우기),
**성채**(드문 정도 · 세력·수비대의 결정성 · 도전과 경계 · 공물과 하루 상한 · 사기 소진 · 자동 판단),
저장 왕복, UI 4시트 렌더, 좌표 왕복,
원근 투영 왕복(2.5D·3D)·먼 곳 축소·2D 항등·시점 순환·3D 앵커,
**3D 렌더러**(그리기는 안 켜고 **값을 내는 함수만** 굴린다 — 한낮이 한밤보다 밝은지 ·
  시각이 아침·낮·저녁·밤을 가르는지 · 밤에 지도가 어두워지고 등롱이 켜지는지 ·
  천후가 해와 시야를 바꾸는지 · 해가 동에서 떠 서로 지는지 · 같은 시각이면 같은 값인지 ·
  격자 계획의 결정성 · 마을이 들판보다 빽빽한지 · 도심에 높은 집이 서는지 ·
  길에 등롱이 서는지 · 물·산·숲이 다른 것을 세우는지 · 밀도 손잡이가 듣고 되돌아오는지 ·
  관우의 투구·언월도·망토 · 도포 인물에 갑주 어깨가 안 붙는지 · ★5 만 금테인지 ·
  유럽 인물이 갓을 안 쓰는지 · 짐승 여덟 형태의 뼈대가 다른지 · 구미호의 꼬리 아홉과
  신수의 광배 · 조립한 부품 수 · 걸으면 다리가 엇갈리는지 · 조우 때 카메라가 대상 쪽인지 ·
  시점을 눕힐수록 낮아지는지 · **값을 내도 세이브가 안 바뀌는지**),
**조우 무대**(3D 가 없으면 잠들고 예전 길로 가는지 · 바늘이 나아갈수록 고리가 좁아지는지 ·
  **초록 테가 막대의 적중 구간과 201칸 모두 일치하는지** · 던진 것이 포물선인지 ·
  무대가 없으면 결과가 곧바로 뜨는지 · 조준 값을 읽어도 판정이 안 바뀌는지),
배회·걸음·발자국·탭 이동·스틱 입력,
스탬프 캐시 재사용/재생성/상한, 등신 비례 전환과 캐시 초기화,
인물 레벨·승급·펫 보정·기본치 불변, 상세 화면 렌더(획득/미획득/펫)·열전 누락,
모드 기본값·오프라인 차단·온라인 판정·길조 보정 만료·상태 요약 크기·사관 화면 렌더,
그림 양식 3종(전통/그림책/만화) 전환과 결정성,
자동 순행(켜기 저장·순행 거리 누적·사거리 조우·자동 승급),
런타임 에러를 확인한다.

무작위는 씨앗(mulberry32, 20260824)으로 고정돼 있고, **천후도 `weather.force('clear')` 로
붙들어 둔다**(세 시간마다 바뀌면 판정이 시각에 끌려다닌다). 세 번 돌려 판정이 같아야 한다.
로그의 시각과 상단 지갑 HUD 한 줄은 씨앗과 무관하게 흔들린다(렌더 타이밍).

**주의** — 헤드리스 `--screenshot` / `--dump-dom` 에서는 `requestAnimationFrame` 이
거의 돌지 않는다(6초에 두 컷 정도). 그래서 메인 루프가 굴러야 보이는 것(자동 순행의
이동)은 스크린샷으로 판정할 수 없다 — `_test.html` 처럼 `update(dt)` 를
직접 돌리거나 `_autoprobe.html` 로 상태를 화면에 써서 본다.

헤드리스로 한 번에 보려면:

```
chrome --headless=new --virtual-time-budget=25000 --dump-dom http://127.0.0.1:8791/_test.html
```
## 가입해서 저장 (account.js)

첫 실행 때 **이름**을 물어보고, 그 이름으로 진행이 저장된다. 서버는 없다 —
이 브라우저 안에서만 도는 가입이고 비밀번호도 없다.

```
세이브 키   <게임>/save/<프로필id>      ← account.js 가 정해 준다
프로필 목록 <게임>/save/accounts
옛 세이브   <게임>/save/v1             ← 첫 가입 때 '이어받기' 로 옮긴다
```

- 게임 코드는 `game.js` 의 `boot()` 에서 **`DG.account.gate(start)`** 한 줄만 부른다.
  gate 가 프로필을 정해 `core.setSaveKey()` 를 부른 뒤 게임을 켠다
- 상단 **👤** 는 account.js 가 직접 끼워 넣는다 (index.html 을 고칠 필요가 없다).
  이름 바꾸기 · 다른 이름으로 새 판 · 삭제
- 이름은 **게임마다 따로**다 (다섯 프로젝트가 별개이므로)
- 자가진단은 `window.DG_NO_ACCOUNT = true` 로 가입 화면을 건너뛰고 `…/save/test` 를 쓴다

## PC 에서 하기

```
build-pc.bat  →  dist/<게임이름>.html (한 파일) · play.bat · 사용법.txt
```

css·js 를 전부 한 파일에 녹여 넣으므로 서버도 파이썬도 필요 없다. 다섯 판을 한 번에
묶으려면 `tools/saga-pc/build-all.bat` — `saga-pc/dist/` 에 다섯 html + 런처(index.html)가 나온다.

