# 사가 시리즈 인계 메모 (2026-08-30 갱신 · 3D 전환 진행 중)

**새 세션은 이 파일부터 읽으면 바로 이어서 작업할 수 있다.**
게임별 자세한 설명은 각 폴더의 `README.md`.

> ## 방금 끊긴 자리 — **여기서부터 이어받는다**
>
> ### 한눈에
>
> | 판 | 3D 전환 | 진단 | 다음 할 일 |
> |---|---|---|---|
> | **사가고** | 본편 + 미니맵 · 물 · 그늘 · **실제 에셋 전면**(자연·마을·사람(새 팩)·짐승·역참·성채·땅의 소품·벼·우물·장터·도감 초상·UI 아이콘 · **땅 소재 텍스처**) · 땅의 높낮이 · 벽 충돌 · **교전 3D 상대+모션(카메라·자세·카드 UI 다 고쳤다)** · **전체 시점 확대·축소 + 3인치 모드**(2D·2.5D·3D 어디서든 휠·핀치·+/- 로 줌, 🔍 버튼으로 확 멀어져 본다) | 388/388 | 실기기 확인 대기 셋 — 밤 밝기(껐다), 캐릭터 갈라짐, 고친 교전 무대의 실제 폰 확인. 확대·축소·3인치 모드는 실기기 확인 없이 헤드리스+스크린샷만으로 닫았다 |
> | **사가블로** | **4단계 진행 중** (방 · 들판 · 전투 연출 · **필드 이동** · **무예 결 연출** · **실제 에셋 전면**(사람·짐승·나무·바위·폐허·절벽·제단·동굴 입구·천막) · **몬스터·보스 다양화 완료**(무기·투구·망토·수염 겉모습 + 궁수 원거리·보스 강타 패턴 + 근접 무기별 이동 결 + 그림자 분신 잔상) · **마을(모루골)도 3D 로 켰다**(집·우물·대장간 건물 채움) · **POI 일곱(다 됐다)**(정예 소굴·미니보스·채광방·행상·퍼즐방·이벤트방·비밀 통로) · **마을 데스크톱 확대**(넓은 화면에서만 1.4배) · **3인칭 어깨너머 시점**(🎥 로 켠다) · **핀치·휠 확대**(실기기 피드백 반영, 2D·3D 다 듣는다) · **3D 화면 입력 라우팅 버그 고침**(`#dg3d` 가 히트테스트를 가로채 던전 3D 에서 탭·드래그·핀치·휠이 다 안 먹고 있었다 — `pointer-events:none` 으로 고쳤다) · **그래픽 품질 AUTO**(PLAN 19절 — 프레임 시간 실측으로 들판 반경·밀도·그림자를 스스로 낮춘다/올린다) · **가로 모드 safe-area**(PLAN 23절) · **POI 여덟째(채집·낚시방)**(PLAN 12절 — 약초는 항아리처럼, 못은 우물처럼) · **1층 시작 캐릭터 가림 버그 고침**(들판의 절벽이 카메라 각도에 걸려 늘 막고 있었다 — ring 2에서는 안 나게 물렸다) · **미니맵(PLAN 27절)**(지금 방을 위에서 본 판 — 적·정예·보스·NPC·POI·문을 점으로, 우상단·탭하면 접힌다) · **월드맵(PLAN 28절)**(시트 탭 🗺️ — 6개 지역을 최고 도달 층으로 열어 보여 준다, 지금 있는 지역엔 "현재" 표) · **장비 슬롯 8개로 확장(PLAN 30절)**(무기·갑주에 투구·장갑·신발·반지·목걸이 다섯을 더했다 — 종이인형·소켓·내구·요구 수준·보석 결까지 다 물렸다) · **퀘스트 시스템(PLAN 36절, 신규)**(메인 8·지역 6·이벤트 3단·무작위 현상판 — 🚩 시트로 연다, `data-quest.js`·`quest.js` 새 파일) · **환경음(PLAN 37절, 신규)**(층 테마 여섯 결의 지속 배경음 + 조각 11가지 — 소리 33→44가지, `sfx.js` THEME_AMB)) | 226/226 | **`PLAN.md`(웹 버전)는 사실상 다 끝났다.** 39·40절(로딩·저장)은 이미 있는 구조(지연 로딩 캐시·세이브 통짜 저장)로 충분해 새로 안 짰고, 41~50절은 코드 항목이 아니라 원칙·우선순위·최종 판정 기준이었다 — 50절 "최종 결과 기준" 체크리스트를 다 대조해 봤더니 하나도 안 빠졌다. 남은 건 47절 실기기 확인뿐(제가 헤드리스로는 못 함). **다음 코드 작업은 사가블로가 아니라 다른 네 판 중 하나의 `PLAN.md`** — 사가고는 거의 다 됐고(실기기 확인만 남음), 사가의숲·사가스토리·사가국지는 3D 리뉴얼을 아직 시작 안 했다. 어느 판부터 이어갈지는 다음 세션이 사용자에게 먼저 물을 것 |
> | 사가의숲 | **PHASE 2 완료**(GLB 32종·로더·Player 3D·Tree/Rock/Vegetation·**Terrain**(InstancedMesh 다섯 종류로 땅 색칠, `V.tileAt` 그대로 읽음) 전부. CDP 스크린샷으로 인물·나무·꽃·오솔길·못까지 눈으로 확인함) | 157/157 | **PHASE 3**(넓은 Forest Map — 지금의 30×20 마을 바깥으로 처음 나가는 일) → Biome → Lake/River/Waterfall → Village → Cave |
> | 사가스토리 | 아직 안 함 | 106/106 | `PLAN.md` Phase 1 |
> | 사가국지 | 아직 안 함 | 95/95 | `PLAN.md` Phase 1 |
>
> **다섯 판의 `PLAN.md` 가 2026-08-28 에 통째로 새로 쓰였다**(사용자가 직접).
> 옛 PLAN 의 PHASE 번호·부록 A 는 **없어졌다** — 인용할 때 새 절 번호를 확인할 것.
>
> ### 사가블로 — **"처음부터 다시 시작" 이 갇힌 채로 두던 버그** (2026-08-30, 사용자가 실제로 갇혀 신고)
>
> 사용자가 "동행이 없어서 진행을 할 수가 없다" — 확인해 보니 **도감도 완전히
> 비어 있었다**. 원인은 `#btn-reset`(상단 🔄, "처음부터 다시 시작")이 부르는
> `core.reset()` — `freshSave()` 를 **그대로 저장**해 `v: 1` 이 박힌 채로 남았다.
> 그러면 `location.reload()` 뒤 `game.js` 의 `fresh = !core.load()` 가 "세이브
> 있음" 으로 읽어 **false** 가 되고, 출사표(첫 인물 셋 자동 합류)가 다시 안
> 나간다 — 던전 입장은 부대가 있어야 막히고, 부대 화면은 "도감에서 넣으라"는데
> 도감도 비어 있어 벗어날 길이 없는 갇힌 자리였다.
>
> `core.reset()` 을 `localStorage.removeItem()` 으로 고쳐 **저장이 아니라
> 지우게** 했고(`js/core.js`), `game.js` 의 `start()` 에 **자가치유**를 더했다 —
> 세이브가 있어도 동행·도감이 **둘 다** 비어 있으면 fresh 로 다시 본다.
> 사용자처럼 **이미 갇혀 있던 옛 세이브도 다음에 켜면 저절로 풀린다**(수동
> localStorage 조작 없이). `sw.js` VERSION → `dungeon-v0.36.1`.
>
> 진단 226 → **228**(재현 테스트 둘 — reset 후 저장 안 남는지, 자가치유 조건이
> 갇힌 모양을 실제로 잡는지), 세 번 불변. 자세한 것은 `saga-dungeon/README.md`
> "잡은 함정" 절.
>
> ### 사가의숲 — **3D 전환 PHASE 1: AssetManager 자리 파기** (2026-08-30, 사가블로 웹판 완료 다음 세션)
>
> 3D 전환을 아직 안 한 세 판(숲·스토리·국지) 중 사용자가 **사가의숲**을 골랐다.
> `PLAN.md` 40절 PHASE 1(현재 코드 분석 → 렌더링 구조 확인 → 모바일 구조 확인 →
> AssetManager 기반 마련) 그대로 진행했다. 지금 마을은 `village-view.js` 하나가
> **2D 캔버스 + 구면 투영**으로 전부 그린다 — three.js·WebGL 은 전혀 없었다.
> 걷기 입력은 이미 PointerEvent(`pointerdown/move/up`) 라 모바일은 그대로 된다.
>
> saga-go 의 `asset3d.js`(GLB 등록 창고 — 표에 키를 적으면 그 사물이 GLB 로 선다,
> 없으면 null 을 돌려 기존 그림으로 되돌아간다)와 같은 요령으로 `js/asset3d.js` 를
> 새로 얹었다. **표는 비어 있다** — 숲에 맞는 CC0 모델을 아직 안 골랐다(PHASE 2 몫).
> `js/vendor/three.iife.js` 도 함께 들였다(사가고·사가블로와 **파일이 md5 까지 같다**).
> `village-view.js`·`village.js` 등 기존 판정·그림은 한 줄도 안 건드렸다.
>
> **함정 하나를 잡았다** — `_test.html` 은 씨앗 고정 난수(mulberry32)로 도는데,
> three.iife.js 를 게임 스크립트들 사이(씨앗을 잠근 **뒤**)에 끼워 넣었더니 확률
> 판정 둘("채집 하루 몫", "꽃 교배")이 어긋났다. three.js 가 **자기 모듈을 불러오는
> 동안 내부적으로 `Math.random()`(UUID 생성 등)을 몇 번 쓰는데**, 그 소비가 씨앗
> 수열에 끼어들어 이후 모든 게임 난수가 한 칸씩 밀렸기 때문이다(사가고·사가블로는
> 애초부터 three.js 를 진 채로 값을 맞춰 놨으니 안 보였을 뿐, 같은 함정이다).
> `_test.html` 에서 **three.iife.js 를 씨앗 고정 스크립트보다 먼저** 싣는 것으로
> 고쳤다(`index.html` 은 씨앗을 안 쓰므로 그대로). **PHASE 2 이후 다른 벤더
> 스크립트를 씨앗 고정 뒤에 넣을 때는 이 함정을 다시 확인할 것.**
>
> 진단 143 → **146**, 헤드리스로 세 번 돌려 불변 확인. 실기기 확인은 안 했다
> (이번 세션엔 렌더러를 안 켰다 — 화면에 보이는 것은 아직 없다).
>
> **바로 이어서(같은 세션) — GLB 모델을 찾아 표를 채웠다.** saga-go 가 이미
> CC0 로 확인해 둔 Quaternius 미러(`trebeljahr/quaternius-showcase`)에서
> PLAN 8절(숲 오브젝트)·16절(동물) 어휘에 맞는 것을 새로 32개 받았다 —
> 나무 8종(큰나무 셋·가을 둘·겨울 둘·고목)·침엽수 둘·자작나무 둘·덤불 셋·
> 바위 넷(2종+이끼)·풀 둘·꽃·풀숲 둘·버섯 둘·그루터기 둘·통나무 둘·벤치·
> 울타리·카트·캠프파이어·천막·여우. 이미 사가고에 있던 것(사슴·늑대·산·
> 랜턴·우물·다리·정자·인물 뼈대 넉 벌)은 **md5 가 같은 파일 그대로** 복사해
> 옮겼다(세 벌째 복사 — 사가블로 때 세운 요령 그대로). `js/asset3d.js` 표에
> 전부 등록하고 출처는 `saga-forest/assets/ASSET_LICENSES.md` 에 적었다.
>
> 렌더러는 아직 없다 — `village-view.js`(2D)는 그대로다. `THREE.GLTFLoader` 로
> 32개 GLB 를 실제로 받아 파싱하는 것까지 헤드리스로 따로 확인했다(임시
> 페이지로, `_test.html` 에는 안 남겼다 — 그 파일은 오프라인·씨앗 고정을
> 지키므로 네트워크 요청을 영구히 넣지 않는다).
>
> **못 찾은 것** — 토끼·다람쥐·오리·새(PLAN 16절). 위 미러의 `animals_pack`
> 에는 없다. 까치를 Poly by Google 에서 따로 구했던 것처럼 다른 CC0 출처를
> 찾아야 한다 — 다음 세션 몫.
>
> 진단 146 → **148**, 세 번 불변.
>
> **바로 이어서 — `build()` 를 진짜 로더로 채웠다.** saga-go 의 `asset3d.js` 에서
> 몸+옷+머리+몸짓(UAL1) 조합 로직을 그대로 옮겼다(`mapClips`·`fit`·`normalize`·
> `delam`·`acquire`·`assembleHero`·`buildHero`) — 넷 다 같은 뼈대라 옮겨 입히기가
> 필요 없다. 헤드리스로 실제 인물을 세워 확인했다(mesh 9 · skinned 9 · mixer ✓ ·
> idle 클립 "Idle_Loop" ✓). **함정** — 이 개발 환경(GPU 없는 헤드리스)은
> `createImageBitmap()` 이 안 풀려 텍스처 있는 GLB 로딩이 멎는다(three 의 텍스처
> 로더 기본 경로). 실제 사용자 브라우저는 문제없다(사가고·사가블로가 같은
> 텍스처로 이미 돈다) — 검증할 때만 `createImageBitmap` 을 지워 우회했다(코드는
> 안 건드림). 진단 148 → **151**(순수 함수만), 세 번 불변.
>
> **바로 이어서 — Player 3D 첫 화면.** `js/village-view3d.js` 신규 — `#map3d`
> 캔버스에 별개 WebGL 씬. `village-view.js`(2D)는 손 안 댔다. 상단 🧊 로
> 켜고 끄면 `#map`/`#map3d` 둘 중 **하나만** 보인다(사가블로의 "장면은
> 하나만" 원칙과 같다). 손잡이 `village3d.on` 기본 0 이라 평소엔 그대로다.
>
> 지금 켜면 — 초록 들판(안개로 지평선을 감췄다) · 3인칭 카메라(인물 뒤 위에서
> 걷는 방향을 따라 돈다) · `asset3d.build('hero', {id:내 인물})` 로 세운
> 인물이 idle/walk 를 갈아 신는다. **인물은 원점 고정, 카메라만 돈다** — 마을
> 좌표를 3D 세계에 흩뿌리는 scatter(PLAN 9절)가 아직 없어서다.
>
> 헤드리스로 땅·하늘·안개·2D↔3D 전환은 스크린샷으로 확인했다. **인물은
> 스크린샷에서 못 봤다** — 이 헤드리스 환경(GPU 없음)이 몸+옷+머리+몸짓 넷을
> 받는 실제 시간보다 스크린샷이 빠르고, `--virtual-time-budget` 로 더
> 기다리면 이번엔 게임의 `requestAnimationFrame` 루프와 맞물려 크롬이
> 멎었다 — 둘 다 진단 도구의 한계다(인물 조립 자체는 전 커밋에서 헤드리스로
> 이미 값 확인함: mesh 9·skinned 9·mixer·idle 액션). **실기기 확인 대기.**
>
> 진단 151 → **153**, 세 번 불변.
>
> **바로 이어서 — Tree/Rock/Vegetation scatter(PLAN 9절 ForestDecorator).**
> **새로 흩뿌리지 않는다** — `V.raw().props`(2D 에서 보던, 좌표 해시로 이미
> 정해진 그 배치)를 그대로 읽어 나무·소나무·바위·꽃·잡초만 GLB 로 세운다.
> PLAN 10절 "중요한 장소는 랜덤 배치하지 않는다"를 지키는 가장 쉬운 길은
> 새 무작위를 아예 안 만드는 것이었다. 인물은 원점 고정이라 사물은 **인물
> 기준 상대 좌표**로 서고(`WORLD_SCALE`), 거리로 짓고 치운다(`RENDER_R`/
> `CULL_R`, 경계 깜빡임 방지 여유를 뒀다), 한 프레임 빌드 개수도 눌러 뒀다.
>
> **CDP 로 직접 스크린샷을 찍어 눈으로 확인했다.** 지난 두 함정(가상시간
> +RAF 교착, 일반 스크린샷의 시간 부족)을 **둘 다 비켜 갔다** — 헤드리스를
> 원격 디버깅 포트로 띄우고 Node 내장 WebSocket 으로 CDP 에 붙어 진짜 4초를
> 기다린 뒤 찍었다. 결과: 인물이 등 뒤에서 보이고 나무 셋·꽃 하나가 인물
> 둘레 제자리에 섰고, 2D 의 "꽃 꺾는다" UI 도 동시에 멀쩡했다. **Player 3D
> 도 이걸로 뒤늦게 실기기 대신 확인됐다.**
>
> 진단 153 → **154**, 세 번 불변. (돌리는 김에 봤다 — "날씨 — 비 오는 날에만
> 나오는 것이…" 항목이 이번 세션 전부터 이미 FAIL 이었다. 3D 와 무관, 손 안
> 댔다 — 다음에 볼 것)
>
> **바로 이어서 — Terrain, PHASE 2 마지막 항목.** `V.tileAt(tx,ty)` 를 그대로
> 읽어 풀·흙길·모래·물·돌길을 색으로 가른다(색은 `villageData.TILES` 에서
> 그대로 — 2D 와 완전히 같은 색). 여기서도 **새 지형을 안 만든다**. 종류별
> `InstancedMesh` 하나에 자리만 채워(인물 둘레 29×29칸이라도 그리기 호출
> 다섯 번뿐) 성능을 지켰다. 물은 12cm 낮춰 웅덩이처럼 보이게 했다.
>
> CDP 로 재차 확인 — 침엽수·바위 사이 잔디, 인물을 못가로 순간이동시켜
> 모래 오솔길이 못으로 이어지는 것(2D 와 같은 자리·색)을 스크린샷으로 봤다.
>
> 진단 154 → **157**, 세 번 불변.
>
> **PLAN 40절 PHASE 2(GLB 로딩 → Player 3D → Tree/Rock/Vegetation → Terrain)
> 가 다 끝났다.** 다음은 **PHASE 3** — 넓은 Forest Map(지금의 30×20 마을
> 바깥으로 처음 나가는 일) → Biome → Lake/River/Waterfall → Village → Cave.
> 지금 3D 는 여전히 **2D 마을 안**을 그대로 옮긴 것뿐이라, PHASE 3 부터는
> 2D 에 없던 새 공간을 만들어야 한다 — "새로 안 흩뿌린다"는 원칙이 여기까지고,
> 그 다음부터는 진짜 procedural 배치(PLAN 9절 ForestDecorator 의 seed 기반
> 랜덤)가 처음 필요해진다.
>
> ### 사가고 — **전체 시점 확대·축소 + 3인치 모드** (2026-08-30, 절벽 버그 다음 세션)
>
> 사용자가 "사가고도 전체적으로 확대 축소 넣어주고 3인치 모드 넣어줘" — "3인치
> 모드"가 뭘 가리키는지 확인해 보니(사가고엔 없던 개념) **카메라 시야 축소(줌아웃)
> 모드**였다.
>
> 처음엔 2D·2.5D 전용 `camZoom2d` 를 새로 만들어 `scale()`에 곱해 넣었는데,
> 사용자가 PC에서 "마우스로 확대 축소가 안되네" — 실제로 켜 보니 진짜 원인이
> 따로 있었다. **WebGL 3D 렌더러(`world3d.js`)는 시점 버튼이 2D 든 2.5D 든 3D 든
> 늘 켜져서(`world.render3d` 튜닝이 꺼지지 않는 한) `zoom3d` 하나로 카메라를
> 그린다**(`syncCamera`가 `W.tiltMode` 셋 다에서 `W.zoom3d`를 그대로 쓴다).
> 그런데 휠·핀치 손잡이는 "3D(tiltMode===2)일 때만 zoom3d, 나머지는 camZoom2d"로
> 갈라놔서, 대부분의 기기(WebGL 되는 기기)에서는 2D·2.5D 휠을 돌려도 실제로
> 그려지는 카메라와 무관한 값만 바뀌어 화면이 그대로였다. `nudgeCamZoom`을
> "시점 버튼이 뭔지"가 아니라 "**지금 실제로 WebGL이 그리고 있는지**"로 판단하게
> 고쳐 해결했다(`camZoom2d`는 WebGL 없는 예비 2D 캔버스 경로에서만 산다).
>
> 그 전에 별개로 **서비스워커 캐시**도 한 번 밟았다 — `sw.js`가 `world.js`·
> `game.js`·`core.js`·`index.html`을 통째로 캐시하는데 `VERSION`을 안 올려서
> 옛 파일이 계속 나왔다(파일 안 주석에 못박혀 있던 그 함정). 이번엔 두 번(로직
> 수정 전 1차, 진짜 원인 고친 뒤 2차) 다 올렸다 — `go-v5.14.1`.
>
> `world.js`에 `camZoom2d`·`toggle3inch`(3인치 모드, 켜기 전 배율을 저장해 뒀다
> 끄면 그대로 돌아간다) 를 얹고, 🔍 버튼(`btn-3inch`)을 새로 달았다. `_test.html`
> 진단 3개 추가, 385 → **388**, 세 번 불변. 실기기 확인은 못 했다(익스텐션
> 미연결) — 대신 헤드리스 크롬 + CDP로 **진짜 마우스 휠 이벤트**를 캔버스에
> 쏴서 카메라 배율이 실제로 바뀌는 것(`×4.0 → ×0.7`)을 스크린샷으로 확인했다.
> 커밋 `e4bdd21`.
>
> ### 사가블로 — **"에셋이 안 바뀐 것 같다"는 신고를 쫓다가 마을까지 3D 로 켰다** (2026-08-30, 몬스터 다양화 다음 세션)
>
> 사용자가 실제 화면(익스텐션으로 확인)에서 "다 2D 네" 라고 했다 — 던전 안까지
> 포함해서다. 헤드리스로 재현해 보니 `--disable-gpu`(WebGL 못 쓰는 상태)일 때는
> 실제로 옛 2D 던전 화면으로 조용히 떨어졌다 — **`dungeon3d.js` 의 `init()` 이
> WebGL 초기화 실패를 catch 만 하고 콘솔에 한 줄도 안 남기던 것**이 원인의 일부였다
> (GPU 를 켠 채 다시 찍으니 3D 표식·라벨이 실제로 떴다 — 코드 자체는 정상이었다).
> THREE 누락·WebGL 렌더러 생성 실패 두 자리에 `console.warn` 을 남겼다. 커밋 `d618a3a`.
>
> 그다음 사용자가 "마을도 사가고처럼 3D 로" 를 요청했다. 코드를 보니 마을(모루골)
> 3D 손잡이(`dg3d.town`)는 이미 있었지만 기본이 꺼져 있었다 — `town.js` 의
> `DECOR` 에 집·우물·대장간 같은 건물 자리가 아예 없어서, 켜면 **빈 돌방에 NPC
> 여섯 명만 서 있는** 꼴이었다(주석에 "다음 단계의 발판"이라고 스스로 적어 뒀다).
> 이 사실을 사용자에게 먼저 알리고("제대로 만든다" vs "일단 손잡이만" vs "2D 유지")
> 골라 달라고 했더니 **"제대로 만든다"** 를 골랐다.
>
> `saga-go` 의 건물 창고(`House_1~4.glb`·`Well.glb`·`Blacksmith.glb`, CC0)를
> 그대로 옮기고 `asset3d.js` 에 `house`(넷을 자리 씨앗으로 섞는다)·`well`·
> `blacksmith` 세 자리를 더했다. `town.js` `DECOR` 에 집 셋·우물·대장간을
> NPC·표식과 안 겹치는 자리에 얹고, `dungeon3d.js` 의 `buildRoom()` 장식 루프가
> 그 자리를 `AS3.build()` 로 GLB 세우게 고쳤다(들판 소품 `piece()` 와 같은 요령).
> 그러고서 `TOWN3D()` 기본값을 0→1 로 올렸다. 헤드리스 스크린샷(WebGL 켠 채)으로
> 확인하니 빈 돌방이 아니라 **지붕 있는 집(하나는 붉은 기와)·시장 좌판·마을
> 표식(역참·결사비·굴혈) 라벨이 실제로 뜨는 마을**이 됐다. 커밋 `de9d6e1`.
>
> `_test.html` 194/194, 세 번 불변(판정은 한 줄도 안 건드렸다 — 마을은 원래
> 판정 없는 걷기 전용 공간이고 이번 변경은 장식 렌더링뿐이다).
>
> **아직 안 한 것** — 2D 폴백에서는 집·우물·대장간이 여전히 안 그려진다
> (`dungeon-view.js` 가 `house`/`well`/`blacksmith` 타입을 조용히 건너뛴다 —
> 3D 전용 소품이라 크래시는 안 나지만, WebGL 이 정말 없는 기기에서는 마을이
> 예전 그대로 보인다는 뜻이다). 그리고 이번 세션도 **실기기 확인이 없다** —
> 클로드인크롬 확장이 이 세션 내내 연결이 안 됐다(사용자가 재연결 시도했지만
> 실패), 그래서 사용자가 직접 보낸 스크린샷 한 장으로만 던전 3D 를 확인했다.
> 다음 세션은 확장이 붙으면 마을 3D 도 실제 화면으로 확인해 볼 것.
>
> **바로 이어서(같은 세션)** 몬스터 다양화가 남겨 뒀던 마지막 둘도 마저 했다 —
> 잡졸 근접 무기(도끼·몽둥이)가 칼과 다른 결로 접근하고(도끼는 좌우로 흔들며,
> 몽둥이는 속도가 울컥울컥 튄다 — `en.phase` 로만 도는 결정적 값이라 새
> `Math.random()` 은 안 썼다), 그림자(shade) 정예의 분신이 반투명 보랏빛
> 잔상으로 보인다(`ensureShade()` — GLB 가 늦게 갈아 끼워져도 놓치지 않게
> `mixerNode` 의 `assetState` 를 직접 본다). 스크래치 하네스로 셋 다 값으로
> 확인했다(칼은 완전 직선·도끼는 직선에서 벗어남·몽둥이는 틱마다 속도가
> 다름). 진단 194/194, 세 번 불변. 커밋 `50049e9`.
>
> **PLAN 14절(몬스터 다양화)은 이제 다 됐다.** 다음은 PLAN 45절 순서대로
> Phase 7(POI)·Phase 10(탐험 콘텐츠)·Phase 11(모바일 UI)·Phase 13(성능
> 최적화) 중 하나, 또는 실기기 확인(확장이 다시 붙으면).
>
> ### 사가블로 — **POI 시스템 첫 둘: 정예 소굴 · 미니보스** (2026-08-30, 바로 다음 세션 — "POI부터 이어가줘")
>
> PLAN 11절의 POI 목록(Treasure·Camp·Shrine·Cave·Merchant·Elite·MiniBoss·
> Event·Puzzle·Secret) 중 **새 판정 없이 문에 얹을 수 있는 둘**부터 넣었다 —
> 정예·보스 갈래는 이미 이전 세션에서 다 다양화해 놓았으니, 그걸 문 하나로
> 꺼내 쓰기만 했다.
>
> - **정예 소굴(`elite`)** — `spawnEnemy()` 에 `opts.forceElite` 를 더해
>   반드시 정예 하나를 낀 채로 나온다(옵션을 안 주면 옛 확률 그대로라
>   기존 전투방·미니보스는 안 바뀐다).
> - **미니보스(`miniboss`)** — 부하 없이 혼자 나온다. `kill()` 이
>   `e.boss` 플래그만 보고 이미 보스급 노획(확정 드랍·재료·단약·감정서)을
>   주므로 따로 더 챙길 것이 없었다 — 층 끝 보스방과 같은 노획 규칙을
>   공짜로 물려받는다.
>
> `data-dungeon.js` `ROOMS` 표에 가중치를 얹고(정예 10·미니보스 6, 기존
> 넷의 비중을 그만큼 낮췄다), `dungeon-view.js` `DOOR_ICON` 에 💠·👹 를
> 더했다. 스크래치 하네스로 값 확인 — `goRoom('elite')` 로 강제로 연 방은
> 실제로 `enemies.some(e=>e.elite)` 가 참이고, `goRoom('miniboss')` 는
> 적 하나뿐이고 `boss:true`. 진단 194/194, 세 번 불변. 커밋 `b29cbf7`.
>
> **남은 POI(Merchant·Cave·Event·Puzzle·Secret)는 새 UI·판정이 필요해서
> 다음으로 미뤘다** — 특히 Merchant 는 방 안에서 물건을 사는 화면이
> 새로 있어야 하고, Secret 은 "문에 안 나오고 숨어서 찾아야 한다" 는
> 발견 메커니즘이 따로 필요하다. 다음 세션이 이어서 고를 것.
>
> **바로 이어서(같은 세션)** "계속 이어가줘" 라고 해서 셋째 POI **채광방
> (`cave`)** 도 넣었다 — 우물·상자와 같은 "바닥에 박힌 소품, 다 치우고
> 닿으면 발동" 요령을 그대로 따라서 위험이 적었다. 닿으면 세공 재료가
> 확정으로 둘 나온다(PLAN 12절 "희귀 광석" 도 겸한다), 35% 확률로 지킴이가
> 붙는다. 값으로 확인(`goRoom('cave')` 로 열고 광맥에 닿으니 재료 드랍이
> 정확히 둘). 진단 194/194, 세 번 불변. 커밋 `28ce9e9`. 남은 POI 는
> Merchant·Event·Puzzle·Secret 넷.
>
> **바로 이어서(같은 세션)** 사용자가 "계속 이어가줘" 를 반복해서 넷째
> **행상(`merchant`)** 도 넣었다 — 이번엔 처음으로 **새 UI** 가 필요했다.
> 은사 고르기(`#dg-choice`)와 같은 석판 틀을 재사용해 물건 셋을 보여
> 주고 사거나 떠날 수 있게 했다(`dungeon.js` 의 `rollMerchantStock()`·
> `buyMerchant()`·`leaveMerchant()`, `run.merchantChoice` 가 `run.choice`
> 와 똑같이 `update()` 를 멈춘다). 본영 행상(`vendor.js`) 과는 재고를
> 따로 굴린다 — 그쪽의 "회차가 끝나야 재고가 새로 온다" 규칙을 안
> 건드리려는 뜻이다. 값 매기는 규칙(`item.price`)·등급 상한(명품까지)만
> 같이 쓴다. 3D 는 마을 장터 좌판(`tent`/MarketStand GLB)을 그대로
> 세운다. 값으로 전체 흐름 확인(닿으면 재고 3개 → 사면 정확한 값만큼
> 금이 빠지고 가방에 들어가며 재고가 줆 → 떠나면 패널이 닫힘). 진단
> 194/194, 세 번 불변. 커밋 `f8d8c17`.
>
> **PLAN 11절 POI 다섯 중 넷이 끝났다.** 남은 셋(Event·Puzzle·Secret)은
> 여태와 결이 다르다 — 지금까지는 기존 정예·보스·재료·행상 시스템을 문
> 하나로 꺼내 쓰기만 했는데, 이 셋은 **진짜 새 메커니즘**이 필요하다
> (Secret 은 "문 목록에 안 뜨고 숨어서 찾아야 한다" 는 발견 규칙, Puzzle
> 은 퍼즐 자체를 설계해야 하고, Event 는 PLAN 35절과 겹쳐서 무엇을
> 새로 짓고 무엇을 이미 있는 것으로 칠지부터 정해야 한다). 다음 세션은
> 이 셋 중 하나를 고르기 전에 설계부터 짧게 다시 정리할 것.
>
> **바로 이어서(같은 세션)** 사용자가 "Puzzle부터 해줘" 라고 골라서
> 다섯째 **퍼즐방(`puzzle`)** 도 넣었다 — 제단 셋을 방마다 새로 섞은
> 순서로 밟는 장치다. 틀리면 처음부터, 벌은 없다(머리로 푸는 방이라
> 몸싸움을 안 섞었다). `dungeon.js` 의 `touchPuzzlePod()` 가 순서를
> 맞으면 불을 켜고 셋 다 맞으면 노획물을 준다.
>
> 만들면서 진짜 버그 하나를 미리 잡았다 — `dungeon3d.js` 의
> `buildRoom()` 은 방 키(`rk`, floor·roomIdx·cleared 로만 만든다)가
> 바뀔 때만 다시 세우는데, 제단이 켜지는 건 그 키에 안 들었다. 그대로
> 뒀으면 **판정은 맞았는데 3D 화면은 안 바뀌는** 채로 남았을 것이다
> (그림이 판정을 안 따라가는, 이 파일이 마을에서 이미 한 번 겪은
> 종류의 함정 — 주석에 남겨 둔 그 "같은 모양의 상태" 규약이 여기도
> 걸렸다). `rk` 에 `:pz<진행도>` 를 얹어 고쳤다.
>
> 값으로 확인 — 일부러 틀린 순서로 밟으면 진행도가 0 으로 돌아가고
> 불이 다 꺼지고, 맞는 순서로 다시 밟으면 풀리고 노획물이 둘 떨어진다.
> 진단 194/194, 세 번 불변. 커밋 `c394372`.
>
> **PLAN 11절 POI 다섯 중 다섯이 됐다.** 남은 건 Event·Secret 뿐 —
> 둘 다 설계부터 다시 정리해야 한다(위 문단 참고, 아직 안 바뀐 사정).
>
> **바로 이어서(같은 세션)** 사용자가 "Event부터 해줘" 를 골랐다. PLAN
> 35절 "랜덤 이벤트" 목록(Monster Ambush·Merchant·Treasure·NPC Rescue·
> Elite Monster·Shrine·Mini Boss·Secret Area)을 다시 보니 대부분 이미
> 있는 POI(elite·miniboss·merchant·shrine·fight)와 겹쳤다 — **진짜 새로
> 지을 것은 NPC Rescue 하나뿐**이라 그것만 `event` 방으로 만들었다.
>
> 지키는 잡졸을 다 치우면 잡혀 있던 이가 풀려나 은사를 하나 갚는다 —
> 사당과 다르게 **고르지 않고 바로 얹는다**("받은 은혜" 라는 뜻). 은사
> 적용 로직을 `pickBoon()` 에서 `applyBoon()` 으로 빼내 구출 보상과 같이
> 쓰게 했다(동작은 그대로, 진단으로 확인했다). 퍼즐방에서 미리 겪은
> "3D 가 상태 변화를 못 따라간다" 함정도 이번엔 처음부터 피했다 —
> `buildRoom()` 의 방 키에 `captive.freed` 도 얹었다.
>
> 값으로 확인 — 안 치운 방에서는 안 풀리고, 치우고 닿으면 풀려나며
> 노획물 둘 + 은사 하나가 정확히 붙는다. 진단 194/194, 세 번 불변.
> 커밋 `b9f4ae4`.
>
> **PLAN 11절 POI 여섯 중 여섯이 됐다.** 남은 건 Secret 하나뿐 —
> "문 목록에 안 뜨고 숨어서 찾아야 한다" 는 발견 규칙이 새로 필요해서
> 다음 세션은 그 설계부터 정할 것(예: 방 안 벽을 조사하면 낮은 확률로
> 열리는 자리, 또는 문 셋 중 하나가 이름 없이 나오는 자리 등).
>
> **바로 이어서(같은 세션)** 사용자가 "Secret까지 마저 해줘" 로 마지막
> POI 일곱째 **비밀 통로**도 넣었다 — 방마다 있는 균열 장식 중 하나가
> (있으면, 8% 확률로) 사실은 숨은 틈이다. 문 목록엔 아예 안 뜨고, 겉보기엔
> 여느 균열과 완전히 같아서 **닿아 봐야 안다.**
>
> **진짜 회귀를 하나 잡았다 — 기록해 둘 값어치가 있다.** 처음엔 "어느
> 균열이 비밀인지" 를 `Math.random()` 으로 골랐다. 그랬더니 자가진단이
> 194/193 으로 깨졌다 — **완전히 무관해 보이는** "원소 접사" 테스트가
> 실패했다. 원인은 던전을 3000틱 자동으로 돌리는 항목이 방을 수백 번
> 만들면서 매번 굴림을 하나씩 더 먹었고, 그 뒤로 도는 모든 무작위
> (전리품 등급·접사 굴림 등)가 한 칸씩 밀려서 **한참 뒤, 전혀 관계없어
> 보이는 테스트**까지 흔든 것이었다. 이 프로젝트의 `Math.random()` 은
> 자가진단 동안 **전역으로 하나의 스트림을 공유한다** — 아무 데나 굴림을
> 하나 더 넣으면 그 뒤 모든 테스트의 무작위 결과가 밀린다는 뜻이다.
> **다음에 이 판(또는 다른 네 판)에서 무작위를 하나 추가할 때는 먼저
> `Math.random()` 대신 `core.hash2(a,b)`(순수 해시, 스트림을 안 건드린다)
> 로 될 일인지부터 물어볼 것** — 자리·층·인덱스처럼 이미 결정된 값으로
> 갈리는 무작위는 거의 다 hash2 로 바꿀 수 있다. 여기서도 그렇게 고쳐
> 스트림을 안 건드리게 했다(층·방 번호로만 정해진다). 퍼즐·이벤트방에서
> 두 번 겪은 "3D 가 상태 변화를 못 따라간다" 함정도 이번엔 미리
> 피했다(`buildRoom()` 방 키에 발견 여부를 얹었다).
>
> 값으로 확인 — 여느 균열은 닿아도 아무 일 없고, 표시해 둔 비밀 균열만
> 찾음 처리되고 노획물 둘이 나온다. 진단 194/194, 세 번 불변. 커밋
> `8692a2a`.
>
> **PLAN 11절 POI 가 다 끝났다.** 처음 목록은 다섯이었지만(Treasure는
> 이미 있던 보물방, MiniBoss·Elite·Merchant·Puzzle·Event(=NPC Rescue)·
> Secret 을 새로 지어) 실제로는 일곱 갈래(정예 소굴·미니보스·채광방·
> 행상·퍼즐방·이벤트방·비밀 통로)로 늘었다.
>
> **사용자가 이어서 물은 것 둘 — 같은 날 다음 세션에서 둘 다 끝냈다.**
> 사용자가 "1,2,3 다해줘" 로 마을 확대·3인칭·(나머지 지시)를 한 번에 시켰다.
> 1. ~~**"마을을 크게 해줘"**~~ — **완료.** "어느 화면 기준으로 크게 보이길
>    원하는지" 를 먼저 물어 **데스크톱 위주**로 정했다. `town.js` 가
>    `innerWidth>=900 && innerWidth>innerHeight` 일 때만 `BASE_W`·`BASE_H`
>    (560×380, 옛 크기 — 폰은 그대로 이걸 쓴다)의 1.4배로 켠다. NPC·표식·
>    장식(DECOR) 좌표는 전부 `BASE_W`·`BASE_H` 기준으로 그대로 두고 `scalePt`
>    로 실제 방 크기에 맞춰 늘렸다 — 그래서 두 크기에서 배치 비율이 같다.
>    **폰에서 개미만 해지던 예전 문제를 다시 밟지 않은 이유** — `dungeon-view.js`
>    의 `fit`도 `dungeon3d.js`의 `camAim`도 이미 방·화면 크기의 **순수 함수**라
>    저절로 다시 잡힌다. 문제였던 건 방 크기 자체가 아니라 좁은 화면에 큰
>    방을 욱여넣은 것이었다 — 그래서 화면이 정말 넓을 때만 키우면 그 병목이
>    안 생긴다.
> 2. ~~**"3인칭 모드도 가능한가?"**~~ — **완료(범위를 좁혀서).** 조작(WASD)
>    까지 캐릭터 기준 상대 방향으로 뒤집는 큰 개편은 **안 했다** — 그대로
>    화면 기준 절대 방향이다. 대신 `dungeon3d.js`에 `camAim3rd(px,py,dirX,
>    dirY,zoom,tilt)` 를 새로 얹어 인물의 **마지막 이동 방향**(`p.dirX`·
>    `p.dirY` — 원래 무예 방향에 쓰던 값, 정지해도 안 지워진다)을 등 뒤로
>    잡아 따라가는 어깨너머 구도만 만들었다. `dg3d.camMode` 손잡이로
>    `'iso'`(기본, 원작 3/4 부감)와 `'third'` 를 오가고, 상단 🎥 버튼이
>    그걸 두드린다. **camAim3rd 는 W·H(방 크기)를 아예 안 받는다** — iso 는
>    방이 커지면 물러나야 맞지만, 3인칭은 인물만 쫓아야 마을이 커져도
>    (위 1번) 카메라가 저 멀리 안 물러난다. `_test.html` 에 진단 넷 추가,
>    194 → **198**.
>
> **바로 이어서 — 아이폰 사파리로 실기기 확인, 그 자리에서 고쳤다.**
> 사용자가 폰으로 열어 보고 "3인 시점은 잘나오는데 너무 확대 되네 확대
> 축소도 있지?" → 이어서 "3인칭 시점 아닐때도 확대 축소 추가 해주고" 로
> 물었다. 둘 다 그 자리에서 해결:
> - **3인칭 거리를 늘렸다** — `camAim3rd`의 등 뒤 거리·높이·시야를
>   100→150 · 50→78 · 65→75 로 키워 몸통이 아니라 발밑까지 보이게 물렀다
>   (그래도 iso 방 대각선 기준 거리(~700)보단 훨씬 가깝다 — 그게 "어깨너머"
>   의 뜻이다).
> - **핀치·휠 확대를 새로 얹었다** — `core.save.settings.camZoom`(기본 1,
>   프로필마다 저장)을 손가락 둘로 벌리거나 오므리면(데스크톱은 휠) 바꾼다.
>   **2D·3D 둘 다 듣는다** — `dungeon-view.js`의 `metrics()`(값이 클수록
>   확대, `ZOOM` 상수와 같은 결)와 `dungeon3d.js`의 `zNow`(카메라 **거리**에
>   나눠 먹인다 — 값이 클수록 거리가 줄어야 "확대" 로 느껴진다)가 **같은
>   저장값을 반대 방향으로 곱해** 두 화면에서 손짓이 같은 뜻이 되게 했다.
>   `dungeon-view.js`의 포인터 처리가 손가락 둘을 추적한다 — 둘째 손가락이
>   닿으면 걷기(steering)는 멈춘다(한 손가락은 걷고 한 손가락은 확대하면
>   판정이 "어느 손가락이 걷는 손가락인지" 를 몰라 애매해진다).
> `_test.html` 진단 여섯 추가(3인칭 거리·범위 넷 + 핀치 저장값 둘),
> 198 → **200**, 세 번 불변 확인. 커밋 `b86b77a`.
>
> **바로 이어서 — "확대 축소가안돼"(폰·PC 둘 다) 를 파다가 더 큰 걸 찾았다.**
> `#dg3d`(3D 캔버스)가 `position:absolute` 라 DOM 순서와 무관하게 정적
> 배치인 `#dg-canvas`(입력을 받는 자리) 위에서 그려지는데, **히트테스트
> (클릭이 실제로 어디로 가는지)도 그 페인트 순서를 그대로 따른다** — CSS
> 스펙상 원래 그렇다. 헤드리스 하네스에서 `document.elementFromPoint()`
> 로 재보니 던전 화면 한복판에서 실제로 잡히는 요소는 `#dg3d` 였다.
> **`#dg-canvas` 의 포인터 리스너(걷기·탭 이동·핀치·휠)는 3D 가 켜진 동안
> 이벤트를 아예 못 받고 있었다** — 핀치·휠뿐 아니라 **수동 탭·드래그
> 이동도 같이 죽어 있었을 가능성이 높다**(자동 순회가 대신 움직여서
> "3인칭이 잘 나온다" 로 보였을 뿐). `#dg3d` 에 `pointer-events:none`
> 하나만 얹어 히트테스트에서 뺐다(그려지는 건 그대로다 — 페인트가 아니라
> 이벤트 라우팅만 바뀐다). `_test.html` 은 3D 를 안 켜서(DG_NO_DRAW) 이
> 버그를 못 잡는다 — 실제로는 **`elementFromPoint` 로 직접 재는 헤드리스
> 하네스**(_wheeltest.html, 임시로 만들고 확인 후 지웠다)가 필요했다.
> 200/200 그대로. 커밋 `bf7ebf7`.
>
> **아이폰 사파리로 재확인 — "잘되네".** 걷기(탭·드래그)·핀치·휠 확대
> 전부 되는 걸 사용자가 실기기로 확인해 줬다. 이 줄기(마을 확대·3인칭·
> 핀치 확대·입력 라우팅 버그)는 여기서 완전히 닫혔다.
>
> **바로 이어서 — PLAN 45절 남은 네 갈래(탐험 콘텐츠 10·모바일 UI 11·
> 세로가로 대응 12·성능 최적화 13)를 "다 해줘" 로 시켰다.**
> - **성능 최적화(PLAN 18·19절)** — `dungeon3d.js`에 LOW/MEDIUM/HIGH/
>   AUTO 품질 등급을 얹었다. 매 프레임 실측 프레임 시간의 이동평균으로
>   AUTO 가 들판 반경(`fieldR`)·밀도(`fieldDens`)·그림자(`shadowMap.
>   enabled`)를 **한 표(`QUALITY_PRESET`)로 한꺼번에** 낮춘다/올린다 —
>   따로 만지면 등급 간 조합이 어긋날 수 있어서다. 콘솔에서
>   `DG.dungeon3d.set('dg3d.quality','low')` 로 고정 가능(고정하면
>   AUTO 를 덮는다).
> - **가로 모드 safe-area(PLAN 22·23절)** — `--safe-b`·`--safe-t` 만
>   있어 세로에서는 안 써도 티가 안 났다. `--safe-l`·`--safe-r` 를
>   추가해 `#top`·`#dock`·`#widgets`·`#dg-hud`·`#dg-bottom`(조작판)에
>   적용 — 가로에서 노치가 좌우 어느 쪽에 붙어도 HUD·구슬이 그 밑에
>   깔리지 않는다.
> - **모바일 UI(PLAN 20~22절) 재검토** — 844×390(아이폰 가로) 헤드리스
>   스크린샷을 찍어 보니 **이미 반응형**이다(2D 는 `dungeon-view.js`의
>   `fit` 계산, 3D 는 `camAim`의 `dist` 계산이 둘 다 화면·방 크기의
>   순수 함수라 저절로 다시 잡힌다). PLAN 이 제안하는 좌측 가상
>   조이스틱 레이아웃은 **원작(디아블로)의 탭·드래그 조작과 결이
>   다르다** — 사가블로 마을 카드를 되살리지 않기로 한 결정과 같은
>   이유로, 별도 가로 전용 레이아웃을 새로 얹지 않았다.
> - **탐험 콘텐츠(PLAN 12절) — 대부분 이미 있다.** 목록(숨긴 보물·
>   희귀 광석·비밀 문·제단·퍼즐·NPC 이벤트·랜덤 상인·희귀 몬스터·
>   미니보스)이 POI 일곱으로 이미 들어와 있다. **남은 채집·낚시는
>   보류했다** — 사냥터·낚시터가 있는 판은 사가의숲(동물의숲 모방)
>   이지 이 판(디아블로 모방)이 아니라서, 사용자에게 먼저 "정말
>   원하는지" 물어야 할 것 같아서다.
>
> `_test.html` 진단 셋 추가(AUTO 품질 매핑·등급별 값·손잡이 우선순위),
> 200 → **203**, 세 번 불변. 커밋 `8775870`.
>
> **바로 이어서 — 사용자가 "응 넣어줌" 으로 채집·낚시를 정했다.**
> **POI 여덟째: 채집·낚시방**(PLAN 12절)을 넣었다. 사가의숲(동물의숲
> 모방)과 결이 겹치는 게 마음에 걸려 처음엔 보류를 권했지만, 뜻이
> 분명해 **이 판(디아블로 모방)의 결로 옮겨** 넣었다 — 밭을 갈거나
> 낚싯대를 던지지 않는다. 약초 셋은 **항아리와 같은 손짓**(방을 안
> 치워도, 스치면 뜬다 — `dropPotion()`을 그대로 재사용해 단약이
> 바닥에 뜬다), 못 하나는 **우물·사당과 같은 손짓**(방을 다 치운
> 뒤 한 번, 금·재료·장비 중 하나). 새 재료·인벤 칸은 안 만들었다 —
> 이미 있는 드랍 넷(potion·mat·gold·item)만 썼다.
> `data-dungeon.js` ROOMS 에 weight 8 로, `dungeon.js`에 makeRoom
> 'forage' 분기 + update() 상호작용, `dungeon-view.js`에 문 아이콘
> 🌿과 2D 마커, `dungeon3d.js`에 3D 소품(풀포기·파란 못 판)과
> roomKey 캐시버스팅을 얹었다 — 기존 POI 일곱과 정확히 같은 뼈대다.
>
> 이 판의 다른 POI(chest·well·shrine·vein·merchant·puzzle·captive)는
> `_test.html`에 **하나도 직접 테스트가 없다**는 것도 이번에 알았다 —
> 방 안 "닿으면 뜨는" 손짓들은 `_demo.html`의 정지 화면으로만 확인해
> 온 모양이다. 채집·낚시방은 처음으로 `DN.raw()`를 직접 주물러
> dist() 손짓을 헤드리스로 검증하는 시도를 했고(진단 둘 추가), 그
> 과정에서 **`room.cleared` 는 매 틱 "산 적이 없나" 로 다시 정해진다**
> (`dungeon.js` 1195행 부근 — 손으로 `false` 를 박아도 적이 없으면
> 그 자리에서 `true` 로 되돌아간다)는 걸 배웠다. 안 치운 상태를
> 흉내 내려면 **살아 있는 적을 하나 남겨 둬야** 한다.
> 진단 203 → **205**, 세 번 불변. 커밋 `8f922cd`.
>
> **바로 이어서 — PC 스크린샷 신고 "1층에서 시작하자마자 캐릭터를
> 가리네", 헤드리스로 잡았다.** 새 프로필(camZoom 안 건드린 참)로도
> 100% 재현 — 카메라·확대는 멀쩡했고(`stats()`로 재보니 aspect·거리
> 다 정상), 범인은 **들판(field3d.js)의 절벽(cliff)** 조각이었다.
> 절벽은 원래 산 덩이라 크게(h 120~270·s 최대 2.8배) 설계돼 있는데,
> `kindOf()`가 방에서 chunk 하나 남짓(ring 2, 카메라 거리 ~700 안쪽)
> 에서도 절벽·동굴 입구·제단을 낼 수 있었다. **이 판의 카메라는 회전이
> 없어 늘 같은 각도로 본다**(PLAN 16절) — 그 각도에 절벽이 걸리면 그
> 방을 볼 때마다 막힌다. 1층 1번방(고분·古墳) seed 가 하필 그렇게
> 걸렸다. ring 2는 작은 것(숲·바위·폐허·길·물)까지만, 큰 것(절벽·동굴·
> 제단)은 ring 3(그 다음 고리, 저 멀리)부터만 나게 물렸다. 겸사겸사
> `key` 그림자 카메라 절두체(three.js 기본값 ±5 — 이 방엔 턱없이
> 작다)도 손봤다(이번 버그의 범인은 아니었지만 실재하는 결함).
>
> **어떻게 재현했나** — `_shottest.html`(임시, 확인 후 지웠다)을 새로
> 만들어 실제 게임 흐름(캐릭터 생성 건너뛰고 `DN.enter({floor:1})` →
> `dungeonView.update/draw` 20틱 동기 실행 → `--screenshot`)으로
> 픽셀을 직접 봤다. `--virtual-time-budget` 은 GLB 비동기 로딩을 얼려
> 헤드리스가 120초 넘게 멈춘 적이 있어(`_wheeltest.html` 때도 같은
> 함정) **동기 for 루프 + 실제 wall-clock**으로 돌렸다. 고친 뒤 같은
> 자리를 다시 찍어 말끔해진 것도 눈으로 확인했다. 실기기 3D 화면에서
> "뭔가 이상해 보인다" 는 신고는 앞으로도 이 요령(헤드리스 스크린샷
> harness)으로 재현·확인하는 게 콘솔 로그만 보는 것보다 빠르다.
> `_test.html` 에 진단 추가(ring 2 절벽·동굴·제단·야영지 0번),
> 205 → **206**, 세 번 불변. 커밋 `8bbeae2`.
>
> **다음 세션이 볼 것** — 채집·낚시방·이번 절벽 수정 둘 다 아직 아이폰
> 실기기 확인이 없다(코드·헤드리스만 확인했다). 채집·낚시방 밸런스
> (약초 셋·못 하나가 후한지 박한지)도 실제로 몇 판 돌려 보며 조정이
> 필요할 수 있다. **PLAN 27절 미니맵을 다음 작업으로 이어가는 중**
> (사용자가 골랐다) — 아직 시작 전.
>
> ### 사가블로 — **PLAN 27절 미니맵** (2026-08-30, 바로 다음 세션 — "사가블로 이어서 해줘")
>
> 이 판은 사가고처럼 넓은 지도를 걷는 게임이 아니다 — 방 하나(560×360 논리
> 좌표, `dungeon.js`의 `ROOM_W`·`ROOM_H`)를 아이소메트릭으로 비스듬히 보여
> 준다. 그래서 사가고 미니맵(`world.js`의 반경 180m/360m·테두리 clamp)을 그대로
> 못 옮긴다 — **방은 늘 화면보다 안 크므로** clamp·edge 처리가 통째로 필요
> 없다. 대신 방 안 실좌표(`WALL`~`ROOM_W-WALL`)를 0~1 로 펴서 늘 방 전체가
> 다 들어가는 작은 판을 새로 짰다(`js/minimap.js`, 사가고 파일을 참고만 하고
> 새로 썼다 — 공용 파일로 합치지 않는다).
>
> 점으로 찍는 것 — 적(빨강)·정예(보라)·보스(굵은 빨강, `e.boss`)·NPC(하늘,
> `room.captive`·`room.merchant`)·POI(금, `room.chest`·`well`·`shrine`·`vein`·
> `puzzle.pods`·`forage.herbs`·`forage.pond` — 다 쓰면(`taken`/`used`/`lit`/
> `picked`) 점이 사라진다)·문(회색, 늘 오른쪽 벽 `ROOM_W-WALL` 자리). 방이
> 바뀌면(`goRoom`) 다음 그리기에서 저절로 새 방 내용으로 갈린다 — 따로 비울
> 것이 없다. 우상단에 달고 탭하면 접힌다. 마을(`town.js`)은 방 개념이 없는
> 자유 보행 공간이라 `dungeon.active()` 일 때만 켠다.
>
> **실기기 스크린샷으로 확인하다 진짜 CSS 버그를 하나 잡았다** — `#dg-minimap`
> 은 기본을 `display: none` 으로 감춰 두고 `tick()` 에서 던전일 때만 켜는
> 셈이었는데, 켜는 코드가 `node.style.display = ''` 였다. **인라인 스타일을
> 지우는 것뿐이라 스타일시트의 `display: none` 이 그대로 이겼다** — 그래서
> `minimap.stats()` 는 `mounted:true, drawn:7` 로 멀쩡히 답하는데 화면에는
> 끝까지 안 보였다(값과 화면이 갈리는 흔한 함정). `'block'` 으로 확실히
> 덮게 고쳤다. 헤드리스 크롬 + CDP 로 실제 게임을 켜고(`DN.enter` → 진짜
> 방·적) 스크린샷을 찍어 재현·수정·재확인까지 했다 — 골든 점(나)·빨간 점
> (적)이 실제 방 배치와 맞게 찍히는 것도 눈으로 봤다. 우상단 "🚪 탈출" 단추와
> 겹치는 것도 같은 방식으로 잡아 미니맵을 HUD 아래(`top: 64px`)로 내렸다.
>
> `_test.html` 에 값 함수(`norm`·`blips`) 진단 5개 추가(좌표 변환·산 적만
> 찍힘·정예/보스 갈림·POI 소진되면 사라짐·문은 늘 오른쪽 벽) — **`_test.html`
> 자체 스크립트 목록에 `js/minimap.js` 를 안 넣어서 처음엔 전부 `DG.minimap
> undefined` 로 죽었다**(index.html 과 진단 페이지는 스크립트 목록을 따로
> 든다 — 하나 고치면 둘 다 고쳐야 한다, 다른 판에서도 반복해서 밟은 함정).
> 206 → **211**, 세 번 불변. `sw.js` 버전도 올렸다(`dungeon-v0.32.0`) —
> `minimap.js` 를 SHELL 에 추가하며 캐시가 안 걸리게.
>
> **다음 세션이 볼 것** — 미니맵도 채집·낚시방·절벽 수정과 마찬가지로 아직
> 아이폰 실기기 확인이 없다(헤드리스 스크린샷만).
>
> **바로 이어서(같은 세션) — PLAN 28절 월드맵.** "다음꺼 진행해줘" 로 이어받아
> 미니맵(지금 방)과 별개로 **얼마나 내려가 봤는지**를 지역 단위로 보여 주는
> 화면을 얹었다. 층은 늘 순서대로 내려가므로(원작에도 층 건너뛰기가 없다)
> 새 세이브 칸을 늘리지 않고 이미 있는 `dstate().best`(최고 도달 층)만으로
> "그 지역을 밟아 봤는지"를 계산한다 — PLAN 이 적어 둔 "탐험한 지역을
> 저장한다"는 뜻을 **따로 저장하는 배열 없이** 이미 저장돼 있는 값에서 만든
> 셈이다.
>
> `data-dungeon.js`의 THEMES(고분·폐성·산채·수궁·지옥문·천계 여섯) 을 그대로
> 지역으로 썼다 — PLAN 원문의 영문 지명(Forest·Ruins·Swamp·Mountain·Shrine)은
> 옛 템플릿의 자리표시자였고, 이 판은 이미 제 결의 이름을 갖고 있었다.
> `ui.js`에 `viewWorldMap()`을 얹고 도구 시트(🗺️ 지도, 기존 부대·장비·세공·
> 행상·도감·기록 여섯 옆에 일곱째로) 로 열게 했다. 최고 도달 층 아래 지역은
> 이름·아이콘이 뜨고 "답파" 표, 못 밟은 지역은 "???"·❔로 잠긴다. 지금 던전
> 안이면 있는 지역에 "현재" 표가 붙는다.
>
> `_test.html`에 값 검증 둘 추가(최고 층으로 열리는 지역 수를 세어 확인·
> 던전 진입 중 "현재" 표 확인) + 시트 5개 검증 항목에 world 를 끼워 6개로.
> 211 → **213**, 세 번 불변. `sw.js` 버전도 올렸다(`dungeon-v0.33.0`).
>
> **다음 세션이 볼 것** — 미니맵과 마찬가지로 실기기 확인이 없다. PLAN 29절
> 콘텐츠 루프(탐험→발견→전투→보상→…, 이미 있는 걸 다시 이름 붙이는 절에
> 가깝다 — 새로 만들 게 있는지부터 읽어 볼 것)가 다음 후보다.
>
> ### 사가블로 — **PLAN 29절은 이미 다 있었다 → PLAN 30절 장비 슬롯 8개로 확장** (2026-08-30, 바로 다음 세션 — "사가블로 이어서 해줘")
>
> PLAN 29절(콘텐츠 루프: 탐험→몬스터 발견→전투→보상→새 지역→POI→이벤트→
> 장비→강화→정예/미니보스→보스→지역 해금)부터 확인했다 — **다이어그램에 있는
> 것이 전부 이미 구현돼 있었다**(정예방·미니보스방·이벤트방·연단 강화·월드맵
> 까지). 새로 짤 코드가 없어 사용자에게 다음 방향을 물었고, **PLAN 30절 장비
> 슬롯 확장**을 골랐다.
>
> PLAN 원문은 무기·투구·갑옷·장갑·신발·반지·목걸이 7슬롯을 적어 뒀지만, 이
> 판은 이미 무기·갑주·부적 3슬롯 체계가 확고했다(등급도 PLAN의 영문
> Common~Mythic 대신 상품~전설 한글 이름을 쓴다 — 28절 월드맵 때 지역 이름과
> 같은 판단이다: PLAN 은 자리표시자, 이 판엔 이미 제 결이 있다). **기존 셋을
> 없애지 않고 다섯을 더해 8슬롯**으로 갔다 — "유지하면서 확장한다"는 PLAN
> 30절 첫 줄 그대로다.
>
> `data-item.js` — `SLOTS` 를 8개로, `BASES` 에 새 부위 항목을 더했다. 완전히
> 새로 만들지 않고 **이름이 이미 그 뜻이던 것 둘을 옮겼다** — 면갑(面甲)·
> 철립(鐵笠)은 원래 갑주 칸에 있었지만 이름부터 머리에 쓰는 것이라 투구로,
> 옥가락지는 원래 부적 칸에 있었지만 '가락지'가 곧 반지라 반지로. 이렇게 하면
> **기존 세이브의 물건 키(base)가 안 바뀐다** — `slot` 필드만 바뀌므로, 이미
> 장착 중인 물건도 그대로 작동한다(다음에 새로 뽑히는 것부터 새 슬롯에 꽂힌다).
> 나머지는 새 키로 최소한만 채웠다(투구 1·장갑 2·신발 2·목걸이 2·반지 1 종).
>
> `item.js` — 세 자리를 손봤다:
>   1. `SOCK_MAX` 에 새 부위 소켓 상한(투구 2, 장갑·신발·반지·목걸이 1)
>   2. `durMaxOf`/`reqLevel` 의 "부적만 예외" 조건을 **장신구(부적·반지·목걸이)
>      셋으로 넓혔다** — 반지·목걸이는 안 닳고 요구 수준도 낮다(코드에 이미
>      "원작에서도 반지·목걸이·부적에는 내구가 없다"는 주석이 미리 있었다 —
>      이 확장을 예견해 둔 자리였다)
>   3. **보석 소켓 매핑** — `data-gem.js` 의 보석 표는 무기(원소 피해)·갑주
>      (원소 저항)·부적(능력치) 셋에만 효과가 있다. 손대지 않고 `GEM_SLOT_CAT`
>      로 새 부위를 결이 가까운 쪽에 묶었다(투구·장갑·신발→갑주 쪽 저항,
>      반지·목걸이→부적 쪽 능력치). **이걸 빠뜨렸으면 새 부위에 보석을 박아도
>      아무 효과 없이 조용히 사라졌을 것이다** — 코드를 짜고 나서 이 매핑이
>      비어 있다는 걸 알아채고 넣었다.
>
> `ui.js` 종이인형(`gearDoll`) 을 3행 더 늘렸다(무기·초상·갑주 / 투구·장갑 /
> 신발·반지 / 목걸이·부적). `SLOT_ICON`·`SLOT_SPAN` 도 새 부위만큼 늘렸다.
> 헤드리스 스크린샷(`_demo.html#gear`)으로 8칸이 다 제 칸에 들어가고 겹치지
> 않는 것을 눈으로 확인했다.
>
> `_test.html` 에 값 검증 4개 추가 — 슬롯 8개 확인 · 새 부위도 굴리고 입을 수
> 있는지 · 장신구는 안 닳고 나머지는 닳는지 · 보석이 부위 결대로 붙는지
> (투구→elres, 반지→pct). 213 → **217**, 세 번 불변. `sw.js` 버전도 올렸다
> (`dungeon-v0.34.0`).
>
> **다음 세션이 볼 것** — 새 슬롯도 미니맵·월드맵처럼 실기기 확인이 없다
> (헤드리스만). PLAN 31절(무예를 실제 전투 스킬 체계로 확장 — 검술·창술·
> 궁술·화염술·번개술·독술)이 다음 후보다.
>
> ### 사가블로 — **PLAN 31절은 이미 다 있었다 → PLAN 36절 퀘스트 시스템을 새로 짰다** (2026-08-30, 바로 다음 세션 — "PLAN 31절 이어서 해줘")
>
> PLAN 31절(무예를 실제 스킬로)부터 확인했다 — **이미 완성돼 있었다.**
> `data-skill.js`에 직업 다섯 × 무예 45개가 있고, 하나하나에 damage(`v`·
> `grow`)·cooldown(`cd`)·기력(`cost`)·hitbox(반경·투사체)·원소(`el`)가 다
> 딸려 있다. `dungeon.js:castSkill()`이 모양 여덟(swing·bolt·nova·dash·
> buff·heal·curse·summon)을 다 처리하고, `fx3d.js`가 결 색을 입혀 VFX로
> 그린다. `_test.html`에도 이미 검증이 열 개 넘게 있었다. 새로 짤 게 없어
> 사용자에게 물었고, PLAN.md 뒷부분을 훑어 **36절 퀘스트**가 코드에 흔적이
> 전혀 없는 진짜 새 기능임을 확인한 뒤 그쪽으로 방향을 잡았다.
>
> PLAN 원문의 "메인·지역·무작위·이벤트" 넷을 그대로 얹었다. 예시("숲의 늑대
> 10마리")는 몬스터 종족을 안 가리는 이 판 결로 "몬스터 10마리"로 옮겼다
> (28절 월드맵·30절 장비 슬롯과 같은 판단 — PLAN 은 자리표시자다).
>
> `js/data-quest.js`(신규) — 표만 쥔다. 메인 8개(순서대로, floor·kill·
> discover 요구를 섞었다) · 지역 6개(월드맵의 THEMES 여섯과 같은 인덱스,
> 그 지역에 최고 도달 층이 닿아야 연다) · 이벤트 3단(구출 누적) · 무작위
> 표 7종(늘 하나만 떠 있다, 끝내면 곧바로 새것).
>
> `js/quest.js`(신규) — 진행을 센다. **판정(dungeon.js)은 한 줄도 안
> 건드리는 대신, 사건을 emit 하는 자리 둘만 얹었다** — `kill()` 에
> `dungeon:kill`, 구출 성사 자리에 `dungeon:rescue`. 나머지 둘(`dungeon:
> room`·`dungeon:floor`)은 이미 있던 사건을 그대로 듣는다. 보상(금·경험치·
> 공적)은 끝나는 순간 **바로** 준다 — 이 판의 다른 것(드랍·자동장착)도 다
> 그렇게 즉시 처리해서 "수령" 단추를 따로 안 둔다.
>
> **RNG 를 밟았다.** 처음엔 `onKill`/`onRoom` 안에서 무작위 퀘스트가 없으면
> 그 자리서 굴렸다(`ensureRandom`) — 그랬더니 전투 중 아무 때나 Math.random()
> 을 하나 더 잡아먹어, 그 뒤로 나오는 모든 굴림(장비 롤 포함)이 밀렸다.
> 진단에서 "원소 — 무기에 박으면…" 항목이 애먼 자리(전설+고유 판정까지
> 밀려 뜬금없이 원소 피해가 붙은 채로 나옴)에서 넘어졌다 — **이 파일 자체에
> three.iife.js 의 76번 Math.random() 이 옛날에 같은 항목을 넘어뜨렸다는
> 주석이 이미 있었는데, 이번엔 내가 새로 만든 기능이 같은 함정을 새로 팠다.**
> 고쳤다: 무작위 퀘스트는 **화면을 열어야**(status() 호출) 처음 뽑힌다 —
> 전투 사건은 이미 뽑혀 있을 때만 진행을 세고, 없으면 그냥 넘어간다.
>
> `ui.js`에 `viewQuest()`를 얹고 도구 시트에 🚩 퀘스트를 여덟째로 달았다
> (지도 🗺️ 옆). `css/style.css`에 `.qst-*` 한 벌(월드맵 `.wm-*` 를 본떴다).
> `index.html`·`_test.html`·`_demo.html` 세 곳 스크립트 목록에 다 넣었다
> (하나라도 빠지면 그 화면만 죽는다 — 이 판에서 반복해서 밟은 함정).
>
> `_test.html`에 값 검증 6개 추가(표 검증·floor 완료+보상+다음으로 넘어감·
> 지역이 최고 층으로 열림/잠김·kill 태그 필터링(정예만 메인이 세고 잡졸은
> 지역만 세는 것까지)·발견/구출이 각각 다른 퀘스트를 채움·현상이 늘 하나
> 뜨고 다시 뽑기가 됨). 217 → **223**, 세 번 불변. `_demo.html#quest` 로
> 스크린샷도 찍어 메인·지역(닿은 지역만 열림)·이벤트·현상 네 칸이 다 제대로
> 그려지는 것을 눈으로 확인했다. `sw.js` 버전도 올렸다(`dungeon-v0.35.0`).
>
> **다음 세션이 볼 것** — 퀘스트도 실기기 확인이 없다(헤드리스·스크린샷만).
> PLAN.md 를 끝까지 훑어 29~36절이 거의 다 이미 있거나 이번에 채워졌다는
> 걸 확인했다 — 37절(사운드)·38절(시각 효과)부터 "진짜 새로 짤 것이 있는지"
> 다시 읽어 볼 것. **한 가지 — 이 세션 중 다른 세션이 같은 저장소에
> `saga-dungeon/PLAN2.md` 를 새로 만들었다**(Unity 6 전면 재구축 지시서로
> 보인다) — 손대지 않고 그대로 뒀다. 다음 세션은 그 문서가 이 웹 판
> 작업과 어떤 관계인지(별도 갈래인지, 이 문서를 대체하려는 것인지) 사용자
> 에게 먼저 확인할 것.
>
> ### 다섯 판 공용 — **PLAN.md 를 다 두고 Unity 는 PLAN2.md 로 갈라 둔다** (2026-08-30, 바로 다음 세션 — 사용자에게 PLAN2.md 관계를 물어본 결과)
>
> 사용자에게 물었다 — **"네, Unity로 전환 중"** 이라는 답이 왔다. 다섯 판 다
> 웹(Three.js) 대신 Unity 6 로 다시 짓는 게 맞는 방향이다. 그런데 확인해
> 보니 사가고·사가의숲·사가국지·사가스토리 넷은 **`PLAN.md` 자체가 Unity
> 지시서로 덮어써져 있었다**(원래의 웹 리뉴얼 지시서가 사라진 상태) — 사가
> 블로만 원래 `PLAN.md`는 그대로 두고 `PLAN2.md`를 새로 더한 상태였다.
> 사용자가 "다섯 판도 사가블로처럼 맞춰 달라" 고 해서, 그 넷도 **직전
> 커밋(`a534286`)에서 원래의 웹 버전 `PLAN.md` 를 되살리고, 지금 있던
> Unity 지시서는 `PLAN2.md` 로 옮겼다.** `git show a534286:<파일> > PLAN.md`
> 로 그대로 복원해 diff 가 0 인 것까지 확인했다.
>
> **또한 확인한 것 — Unity 는 아직 어느 판도 실제로 시작 안 됐다.**
> `Assets/`·`ProjectSettings/` 같은 실제 Unity 프로젝트 폴더가 다섯 곳
> 어디에도 없다(지시서만 있다). 이 PC 에는 **Unity Hub·Editor 자체가 안
> 깔려 있다**(`Program Files`에 없음) — 설치 방법만 안내하고 사용자가
> 직접 깔기로 했다.
>
> **사용자 결론(중요) — "전체 플랜부터 완료하고 플랜2를 완료하는 걸로
> 해줘."** 즉 **다섯 판 다 `PLAN.md`(웹 버전)를 먼저 끝까지 마치고, 그
> 다음에 `PLAN2.md`(Unity)로 넘어간다.** 다음 세션들은 이 순서를 지킬 것 —
> Unity 로 섣불리 건너뛰지 않는다.
>
> ### 사가블로 — **PLAN 37절 환경음** (2026-08-30, 바로 다음 세션 — "사가블로 플랜버전으로 이어서 해줘")
>
> 위 결정대로 `PLAN.md`(웹 버전)를 계속 이어간다. PLAN 38절(시각 효과 —
> slash trail·hit spark·critical·damage popup·enemy flash·camera shake·
> ground effect·skill particles)은 먼저 확인해 보니 **fx3d.js 에 여덟
> 다 이미 있었다**(고리·번쩍임·흔들림·숫자 팝업 다 3단계에서 만들었다).
> PLAN 37절(환경별 사운드)만 진짜 빈 자리였다 — `sfx.js` 는 그동안 **한
> 방(ADSR)으로 끝나는 소리 33가지**뿐이었고, 층에 있는 내내 우는 배경음은
> 없었다.
>
> PLAN 원문은 Forest·Ruins·Swamp 셋인데 이 판은 층 테마가 여섯(고분·폐성·
> 산채·수궁·지옥문·천계)이다 — 산채가 숲 자리(새·바람·나뭇잎), 폐성이
> 폐허 자리(바람·금속음), 수궁이 늪 자리(물·벌레)를 그대로 받고, 고분·
> 지옥문·천계 셋을 이 판만의 결로 더했다(28절 월드맵·30절 장비와 같은
> 판단 — PLAN 은 자리표시자다).
>
> `sfx.js`에 `THEME_AMB`(여섯 결 — 드론 하나 + 걸러 낸 잡음 하나 + 드문드문
> 섞는 조각 `amb_*` 11가지)와 지속 노드 엔진(`startAmbience`·`stopAmbience`·
> `scheduleTick`)을 얹었다. **CUES 의 다른 소리와 근본이 다르다** — 그건
> 한 번 울리고 끝나는 ADSR 이고, 이건 층에 있는 내내 안 끊기는 오실레이터·
> 루프 버퍼다. `dungeon:enter`·`dungeon:floor` 에 물려 층 테마가 바뀌면
> 갈아 끼우고 `dungeon:end` 에서 멎는다.
>
> **RNG 함정을 또 밟을 뻔했다 — 이번엔 다른 결로.** 조각(`amb_*`)도 기존
> `play()`를 그대로 타는데, `play()`는 **요청을 무조건 `recent`(`_tail()`)
> 에 적는다.** 배경에서 무작위로 섞이는 조각이 그 목록에 같이 쌓이면
> "이 사건 다음엔 이 소리" 를 보는 기존 진단(`이미 있는 이벤트에 물려
> 있다`)이 깨진다 — 실제로 깨졌다. **PLAN 36절 때는 RNG *소모 시점*이
> 밀리는 함정이었고, 이번엔 같은 목록에 *다른 것이 섞여 드는* 함정이다 —
> 결이 다르니 같은 수법(지연 시작)으로는 못 막고, `play()`에 `{amb:true}`
> 를 받아 `ambRecent` 라는 **완전히 다른 목록**으로 갈랐다.** `_ambTail()`
> 을 새로 열어 뒀다. 첫 조각도 즉시 안 튀고 한 박(0.6~2초) 쉬고 튼다.
>
> `_test.html`에 값 검증 3개 추가(여섯 지역 다 결이 있는지 · 층을 넘으면
> 결이 바뀌고 같은 지역이면 안 바뀌는지, 던전을 나오면 꺼지는지 · 조각이
> `_tail()` 이 아니라 `_ambTail()` 에 쌓이는지). 223 → **226**, 세 번
> 불변. `_sfxcheck.html`(소리를 하나씩 다 내 보는 진단)도 33/33 → **44/44**
> 예외 0 으로 다시 확인했다. `README.md`의 소리 개수 표기 네 곳도 33→44로
> 맞췄다. `sw.js` 버전도 올렸다(`dungeon-v0.36.0`).
>
> **다음 세션이 볼 것** — 환경음도 실기기 확인이 없다(헤드리스만, 애초에
> 소리는 스크린샷으로 못 본다). PLAN 39절(로딩 구조)부터 다시 읽어 "진짜
> 새로 짤 것"을 고를 것 — 여기까지 온 걸 보면 PLAN.md 뒷부분도 상당수가
> 이미 구현돼 있을 가능성이 크다(29~36절이 그랬듯).
>
> ### 다섯 판 공용 — **사가고의 에셋·요령을 다른 판에 옮겨 쓰는 길이 뚫렸다** (2026-08-30, 사가블로 4단계에서 처음 해 봤다)
>
> 사용자가 "모든 시리즈, 나중에 작업할 때 미리 기록해 달라" 고 했다 — 사가블로에서
> 막 검증한 이 요령은 **사가의숲·사가스토리·사가국지 차례가 왔을 때도 그대로 먹힌다.**
> 다섯 판이 다 같은 `PLAN.md` 4·5절("실제 3D 에셋 사용" · "Asset Loader 구축")을
> 지고 있으므로, 다음에 이 세 판을 3D 로 옮기는 세션은 **사가고의 에셋을 처음부터
> 다시 찾지 말고 아래 순서를 따를 것:**
>
> 1. **`saga-go/assets/models/` 에서 필요한 것만 그 판 폴더로 복사한다.** 사람은
>    `people/regular/`(몸 둘·옷 넷·머리 여섯) + `anim/UAL1_Standard.glb`, 그 외엔
>    그 판 소재에 맞는 것만(짐승·나무·바위·건물…) — 통째로 옮기지 않는다(사가블로는
>    사람+짐승 하나+나무+바위만 옮겨 36MB 로 끝냈다). 받은 파일이 CC0 인지는 이미
>    확인돼 있다(`saga-go/assets/ASSET_LICENSES.md`) — 그 판에도 옮긴 목록만 추려
>    `ASSET_LICENSES.md` 를 새로 쓴다(퍼블릭 저장소라 출처는 항상 적어 둔다).
> 2. **`js/asset3d.js` 를 그 판 사정에 맞게 추려서 새로 쓴다** — 통째로 복사하지
>    않는다. 사가고 원본은 `HERO_RECIPES`(사람 조합)·표 조회(`urlOf`·`lookup`)·
>    애니메이션 이름 맞추기(`mapClips`)·크기 맞추기(`fit`·`normalize`)·PBR 벗기기
>    (`delam` — **환경맵 없는 판은 다 필요하다**, Quaternius GLB 가 다
>    `metallicFactor>0` 로 온다)·shell 선세우고-나중에-갈아끼우기(`build`·
>    `buildHero`) 가 핵심이고, `pickPieces`(부위 변형 여럿 고르기)·`heightMul`·
>    `markOf`(깃발) 는 그 판에 해당 사정이 있을 때만 옮긴다. `saga-dungeon/js/asset3d.js`
>    가 "추린 판" 의 실제 예시다 — 210줄짜리 원본을 380줄로 옮기며 뺀 것·왜 뺐는지가
>    파일 맨 위 주석에 있다.
> 3. **그 판의 렌더 파일(`world3d.js`·`dungeon3d.js`격)에서 상자 조립 자리를
>    `asset3d.build()`/`buildHero()` 호출로 바꾼다** — 반환되는 `shell` 은 Group
>    이라 기존에 상자를 넣던 그 자리에 그대로 넣으면 된다. **판정(`kind`·`ref.r`·
>    `ref.color` 등)은 한 글자도 안 바뀐다** — 그림만 갈아 낀다.
> 4. **번쩍임(hit flash)은 "지금 보이는 메시 전부"를 사본 떠서** 쓴다 — GLB 가
>    상자에서 갈아 끼워지는 순간 옛 사본이 낡으므로, `assetState` 가 바뀔 때만
>    다시 뜬다(매 프레임 뜨면 낭비). `saga-dungeon/js/dungeon3d.js` 의
>    `ensureFlash()`가 그 패턴이다.
> 5. **실기기·헤드리스 3D 렌더 없이도 값으로 검증할 수 있다.** GLTFLoader 는
>    `file://` 나 렌더 없는 환경에서 실패하지만 `build()`/`buildHero()` 는 그때도
>    상자 shell 을 그대로 돌려주므로 **자가진단(`_test.html`)은 안 흔들린다** —
>    새로 짠 `asset3d.js` 도 반드시 `_test.html`·`_demo.html`·`index.html` 세
>    파일 모두에 스크립트 태그를 넣어야 한다(하나라도 빠지면 그 화면만 `DG.asset3d`
>    가 없어서 예전 상자로 조용히 남는다 — 에러는 안 나서 놓치기 쉽다).
> 6. **진짜 화면 확인은 헤드리스 CDP 스크린샷으로 한다**(`--screenshot=` 플래그).
>    계정 가입 화면을 건너뛰려면 스크립트 로드 **전에** `window.DG_NO_ACCOUNT = true`
>    를 심는다(`account.js`의 `gate()`가 이 손잡이를 본다 — 이 판만이 아니라
>    다섯 판이 같은 이름을 쓸 가능성이 높으니 새 판에서도 먼저 확인). `game.js`의
>    `boot()`가 `DOMContentLoaded` 뒤에 도므로 확인 스크립트는 `setTimeout`으로
>    한 틱 늦춘다. `index.html`의 DOM 골격(버튼·시트 id)이 없으면 `ui.init()`이
>    중간에 죽어 동행이 안 붙으므로, 최소 골격을 그대로 복사해 쓴다.
>
> 사가블로에서 실제로 옮긴 것 — 사람(플레이어·마을사람·사람 형 적) + 짐승 형
> 적(늑대 GLB 하나로 대신) + 들판 소품 전부(나무·바위·기둥(무너진 아치)·무너진
> 벽·절벽(산 덩이)·제단(Temple)·동굴 입구(Mine)·천막(장터 좌판으로 근사))까지
> PLAN 4절 우선순위를 따라 커밋 `1004d21`·`661be57`에서 채웠다 — **모닥불만
> 아직 도형이다**(saga-go 창고에도 맞는 CC0 캠프파이어가 없어서 억지로 안
> 어울리는 것을 끼우지 않았다, `saga-dungeon/assets/ASSET_LICENSES.md` 참고).
>
> 같은 세션에 **판정을 방에서 필드로 잇는 것**(문 판정은 안 건드리고 이동 클램프만
> 넓혀, 방 밖은 field3d 가 그리는 것과 같은 값으로 나무·바위와 부딪히게 했다)과
> **무예의 결(el)로 연출 색을 가르는 것**(fx 이벤트에 `el`·`color` 를 실어 보내고
> `fx3d.js` 가 `hexOf(f.color, 기본값)` 으로 조용히 옛 그림에 떨어지게)도 같이
> 했다 — 둘 다 판정 수치는 안 건드리고 곁가지(그림·이동 범위)만 늘린 것이라
> 자가진단이 그대로 통과했다. 커밋 `4a67af2`(필드 이동) · `8e435ae`(결 연출) ·
> `10c9515`(에셋) · `1004d21`·`661be57`(남은 들판 소품 — 모닥불만 못 채웠다,
> 맞는 CC0 캠프파이어가 없어서다).
>
> ### 사가블로 — **몬스터·보스 다양화** (2026-08-30, 위 절의 다음 세션 — 사용자가 이어서 요청)
>
> 사용자에게 "사가블로 완료된거임?" 질문을 받고 PLAN.md 45절 기준으로 아직 초반
> (3단계 기반 공사)뿐이라고 답한 뒤, 사용자가 다음으로 **몬스터·보스 다양화**
> (PLAN 14절)를 골랐다. 겉모습과 공격 패턴 둘 다 손댔다:
>
> **겉모습** — `data-enemy.js` 의 `look`(무기 club·axe·sword·spear·halberd·
> staff·bow, 투구 helmet·gapju·crown·plume, 망토·수염)이 이미 있었는데 3D
> 화면엔 하나도 안 실려 있었다(다들 같은 사람 모델에 색만 달랐다). `dungeon3d.js`
> 에 `foeGear()` 를 새로 짜서 GLB 배우의 바깥 껍데기(`g`)에 무기·망토·투구·
> 수염을 도형으로 얹는다 — **GLB 안쪽(`foeBody`)이 늦게 갈아 끼워져도 이 장식은
> 그대로 남는다**(껍데기와 안쪽을 분리해 둔 지난 세션의 구조 덕이다). 짐승 형
> 적도 둘로 갈랐다 — 들개는 그대로 늑대, 코끼리병은 소(Cow) GLB(딱 맞는 코끼리는
> CC0 로 못 찾았다).
>
> **공격 패턴** — 여태 모든 적이 붙어서 몸으로만 때렸다. 이제 `look.weapon`
> 이 bow·staff 인 궁수·조총병은 150 거리에서 멈추고 사거리(260) 안이면
> `run.foeShots` 로 화살·탄환을 쏜다(판정은 플레이어의 기공파 투사체와 같은
> 모양 — 맞으면 `hurtPlayer`). 붙잡히면 여전히 몸으로도 때린다. 보스는
> 4~6초마다 지진 강타 — 0.7초 예고(붉은 파문, 벗어나면 안 맞는다) 뒤 반경
> 110·평타 1.8배 피해(연출은 있던 `pop`·`whirl` fx 를 그대로 재활용했다 —
> `pop` 의 `boss:true` 가 화면 흔들림까지 공짜로 딸려 온다).
>
> **값으로 실제 동작까지 확인했다** — 강제로 활을 든 적을 세워 두니 방 끝까지
> 안 붙고 150 거리에서 멈춰 쏘는 것(`endDist≈149`, 멜리 거리 26이 아니다),
> `run.foeShots.length>0`(실제로 쐈다), 보스에게 `slamWarn` 이 뜨고 `pop
> (boss:true)` fx 가 실제로 발동하는 것까지 헤드리스로 시뮬레이션해 봤다.
> 진단 194/194, 세 번 불변(기존 평타 조건은 안 건드렸다). 커밋 `d8b43af`(겉모습) ·
> `0899a01`(공격 패턴).
>
> **바로 이어서(같은 세션)** 사용자가 "이어해줘" 라고 해서 남은 둘을 마저
> 했다 — 정예 여덟 갈래가 `ELITES` 표의 제 빛깔로 물들고 발밑에 그 색 고리가
> 뜨며(여태 다 같은 보랏빛이었다), 창·극(戟) 은 자루 길이만큼(14) 먼저
> 닿는다(헤드리스로 첫 타격 거리를 실측: 칼 28.3 vs 창 41.0). 커밋 `9672ec7`.
>
> **아직 안 한 것** — 일반 잡졸(황건적·산적 등)은 여전히 이동 패턴이 다
> 같다(사거리 차이는 이제 있다, 그 외엔 궁수·조총병만 갈랐다). `shade`(그림자)
> 정예의 분신은 판정엔 있어도 3D 로 보면 그냥 같은 모습 둘이 서는 정도다 —
> 반투명한 잔상 같은 걸 입히면 더 살 수 있다. 실기기 확인도 여전히 없다.
>
> ### 사가고 — **실기기로 밤 밝기는 확인됐다. 캐릭터 갈라짐은 데스크톱에서 재현 안 됨 — 다음 세션이 이어받을 것** (2026-08-30, 세션 길어져 여기서 넘긴다)
>
> `run-phone.bat` 로 폰에 https 서버를 띄워 실기기 확인을 했다. **밤 밝기는
> 확인됐다** — "밤에는 밝아 보인다"고 사용자가 확인. **캐릭터 갈라짐은 아직
> 그대로**라고 했다.
>
> 헤드리스 크롬으로 몇 가지를 재현해 봤지만 못 찾았다:
> - `_demo.html#people,zoom=0.4` 로 여섯 벌을 가까이 세워 idle 자세 확인 —
>   갈라짐 없음(GLB 가 다 로드된 뒤)
> - 실제 키보드 이동으로 걷는 동안(진짜 걷기 애니메이션, `mot.phase` 가 도는
>   상태) 프레임을 연속으로 찍어 확인 — 이것도 갈라짐 없음
> - **GLB 가 아직 안 왔을 때**(부팅 직후 곧바로 걸으면) 원뿔 몸통+맨머리+붕
>   뜬 모자 모양의 임시 대체물(`shape` 폴백)이 잠깐 뜨는 것은 확인했다 —
>   사용자에게 이 모습이 맞는지 물었더니 **아니라고 했다**(그것과는 다르다)
>
> 즉 **코드 구조상으로는(65개 뼈 공유, `assembleHero()` 의 `bind()`) 갈라질
> 이유가 안 보이고, 데스크톱 재현도 두 경로 다 깨끗했다.** 남은 유력한 후보:
>
> 1. **모바일 GPU 의 스키닝 경로 차이** — 뼈 65개면 유니폼 한도를 넘어
>    텍스처 기반 스키닝으로 갈 수 있는데, 이건 데스크톱 Chrome 헤드리스로는
>    같은 경로를 안 탈 수도 있어서 여기서는 못 잡는다
> 2. **서비스 워커 캐시** — 인물 팩 교체(`59dee3a`, `sw.js` `go-v5.9.0`)는
>    이미 오래된 커밋이라 최신 `go-v5.12.0` 에 들어 있을 텐데, 그 캐시가
>    폰에 한 번도 새로 안 깔렸다면(예전 `Farmer/Worker/Lady` 시절 채로
>    눌러붙었다면) 지금 봐도 옛 갈라짐이 그대로 보인다 — 이 프로젝트가
>    여러 번 밟은 함정이다(`_swreset.html` 로 확실히 비웠는지가 늘 애매했다)
>
> **이번 세션이 손댄 것들**(교전 카메라·조우창 버그, 아래 두 절) 때문에
> `sw.js` `VERSION` 을 `go-v5.13.0` 으로 올려 뒀다 — 안 그러면 폰이 그 수정도
> 캐시 때문에 못 볼 뻔했다. **다음 세션이 먼저 할 일:**
>
> 1. 사용자에게 `_swreset.html` 을 폰에서 열어 **정말 비워졌는지**(버전 표시가
>    `go-v5.13.0` 으로 바뀌는지) 확인받고 다시 봐 달라고 부탁 — 캐시였다면
>    여기서 끝난다
> 2. 그래도 갈라지면 — 사진을 받아 어디가(목/허리/팔다리 중) 어떻게 갈라지는지
>    본다. `assembleHero()`(`js/asset3d.js` 837행)의 `m.bind(skeleton,
>    m.bindMatrix)` 자리만 의심할 만하다 — `bodyScene.add(m)` 이 `add()` 라
>    월드 변환을 안 맞춰 준다(`attach()` 였으면 자동으로 맞춰 준다). 지금은
>    모든 파일의 루트 노드가 항등 변환이라(`.gltf` 대조로 확인) 문제가 안
>    되지만, 만에 하나 실제 렌더 결과가 다르면 여기부터 `attach()` 로 바꿔
>    볼 것
>
> ### 사가고 — **교전 3D 무대의 카메라·자세·카드 UI 를 실제로 눈으로 보고 고쳤다** (2026-08-30, 아래 절의 다음 세션)
>
> 아래 절("교전에 실제 3D 상대와 모션을 붙였다")이 넘긴 "실기기 확인"을
> **실기기 없이 헤드리스 크롬을 CDP(devtools 프로토콜)로 직접 조작해** 대신했다
> — `event.force('bandit_ambush')` → `event.choose('fight')` 로 진짜 교전을
> 열고, `Page.captureScreenshot` 으로 화면을 그대로 캡처해 눈으로 확인하는
> 식이다. 사람이 실기기로 보는 것과는 다르지만(터치·성능·화면비는 여전히
> 못 잰다), **"화면에 뭐가 뜨는지"는 이제 값이 아니라 그림으로 확인할 수 있다.**
> 이 방법을 이번에 확인했으니 다음 세션도 같은 식으로 먼저 스스로 찍어 보고
> 시작할 수 있다(브라우저 확장이 세션에 안 붙어 있어도 된다).
>
> 찍어 보니 세 가지가 실제로 잘못돼 있었다:
>
> 1. **적이 카드에 거의 안 보였다** — `#encounter` 가 전체 화면을 66% 어둡게 +
>    5px 블러로 덮는 조우 공용 칸인데, 교전도 그 칸을 그대로 썼다. 3D 로 세운
>    상대가 있어도 화면 대부분이 어둡게 흐려 있으니 사실상 안 보이는 것과
>    같았다. `duel.js` `open()` 이 `stage3d` 가 있고 `world3d.available()` 이면
>    `#encounter` 에 `duel3d` 클래스를 붙이게 고쳤고, `css/style.css` 에
>    `#encounter.duel3d` 규칙을 더해 어둡기/블러를 거의 없애고 카드를 화면
>    아래쪽 얇은 HUD 로 줄였다(초상 둘은 실제 배우가 대신하므로 숨겼다 —
>    피해 숫자·강타 예고는 정보라 남겼다). `finish()` 에서 이 클래스를 지워
>    결과 카드·다음 조우에는 안 남게 했다.
> 2. **카메라가 여전히 먼 하향 구도였다** — `duelStage()` 는 `focusAt` 만 세우는데
>    `camAim()` 의 `focus` 갈래는 무대(`stage`, 야생 대상 전용)보다 훨씬 멀고
>    높다. `camAim()` 에 여덟 번째 인자(`duel`)를 더해 **둘 사이를 축으로 옆에서
>    가까이 보는** 전용 구도를 넣었다(`DUEL_DIST` 손잡이, 기본 9m) — 무대의
>    "무릎께" 구도와는 다르게 상대가 아니라 나-상대 중점을 본다. `syncCamera()`
>    가 closure 의 `duelFoe` 를 그대로 넘긴다. 기존 `camAim` 호출(_test.html 열
>    곳)은 인자를 7개까지만 쓰므로 순수 함수 규약은 안 깼다.
> 3. **적이 나를 등지고 서 있었다** — `actorOf()` 는 새 배우를 늘 `ang: Math.PI`
>    (등짐)로 시작하는데, `placeActor()` 의 방향 갱신은 **움직여야만** 도는
>    로직이라 안 걷는 `duelfoe` 는 첫 각도 그대로 남았다. 야생 대상의 무대
>    회전(`onStage`)과 같은 계산을 `syncActors()` 의 duelFoe 블록에도 넣어
>    나를 보게 고쳤다.
>
> 부수적으로 **마을에서 싸우면 상대가 집 뒤에 완전히 가려질 수 있다**는 것도
> 코드로 확인했다(`duelStage()` 가 좌표만 잡고 `houseRects()` 충돌을 안 봤다) —
> `duelSpotBlocked()` 를 새로 짜고, 기본 자리(6m 남쪽)가 막혀 있으면 여덟
> 방향을 부채꼴로 돌며 트인 자리를 찾게 했다. **이번엔 직접 재현해서 찍어
> 봤다** — 헤드리스에서 `houseRects()` 를 직접 굴려 남쪽 자리가 실제로 집과
> 겹치는 마을 좌표를 찾아낸 뒤 그 자리에서 싸움을 걸어 확인했다. `duelFoe`
> 가 정말로 다른 방향(부채꼴의 두 번째 자리)에 섰다.
>
> **그런데 그 자리에서 카메라가 여전히 벽 속에 박혔다** — 캐릭터 둘 다 안
> 보이고 화면 가득 벽 텍스처만 찍혔다. 같은 세션에서 세 겹으로 팠다:
>
> 1. `houseRects()` 를 **우물·장터까지** 보게 넓혔다(`world3d.js` 732·736행의
>    `t:'well'`·`t:'market'` — `w`·`d` 가 없는 소품이라 `h` 에서 어림잡은
>    사각형을 붙였다). 재현했던 그 자리는 사실 우물·장터가 아니었지만(3x3
>    칸을 직접 훑어 확인했다 — 반경 안에 우물/장터 자체가 없었다), 이 방향
>    자체는 정확하고 다른 자리에서는 도움이 될 것이다.
> 2. 카메라 여유(margin)를 3.5m → 7m 로 늘렸다 — 지붕이 벽보다 넓게 튀어나오는
>    걸 감안했다.
> 3. **그래도 뚫린 자리를 하나도 못 찾는 경우**(마을 한복판이 정말 빽빽할 때)
>    를 위해, 좌우 둘 다 막히면 낮고 가까운 구도를 아예 포기하고 **이미
>    안전하다고 확인된 `focus` 구도**(멀리서 내려다보는, 벽에 안 박히는
>    그림)를 상대 자리에 그대로 꽂아 쓰게 했다. 세 겹을 다 넣고서야 같은
>    좌표에서 벽 없이 마을이 넓게 보이는 화면을 스크린샷으로 확인했다 —
>    다만 이 안전망 구도에서는 둘이 다시 작고 멀게 보인다(원래의 "가까이서
>    본다"는 못 지킨다 — 벽보다는 낫다는 절충이다).
>
> 이걸 찾아 재현하려고 **일부러 가장 빽빽한 마을 칸을 헤드리스로 검색**했다
> (남쪽 자리가 실제로 집과 겹치는 좌표를 찾을 때까지 격자를 훑었다) — 보통
> 걷다가 만나는 자리보다 훨씬 밀집한 극단값이다. 빈 들·마을 어귀처럼 흔한
> 자리에서는 이번 구도가 잘 도는 것도 같은 세션에 다시 찍어서 재확인했다.
>
> 부수적으로 밟았던 **"속공 한 번도 안 날리고 물러나면 조우창이 빈 채
> 눌러붙는다"** 버그도 사용자가 이어서 요청해 바로 고쳤다. `duel.js`
> `finish()` 가 `innerHTML`·`duel3d` 는 지우면서 `show` 클래스는 안 지우던
> 것이 원인이었다 — 결과 카드를 여는 길(`event.js` `showResult()`)이 그
> 클래스를 다시 붙여 주므로 정상적인 승패에서는 문제가 안 됐지만, `p.fled
> && p.dealt<=0`(빈손 도주엔 패배를 안 물린다는 의도로 `event.js` 가 결과
> 카드 자체를 안 여는 길)에서는 아무도 다시 안 붙여서 그대로 눌러붙어
> 있었다. `finish()` 에서 `show` 도 같이 지우게 한 줄 고쳤다 — 정상 승패
> 경로는 어차피 곧바로 다시 붙으니 화면 깜빡임이 없고, 빈손 도주 경로는
> 이제 제대로 닫힌다. 헤드리스로 "무대를 열고 → 아무것도 안 치고 →
> 물러난다 → 다음 사건을 강제로 연다" 순서를 재현해 두 번째 사건이 실제로
> 뜨는 것까지 확인했다. 진단 385/385, 세 번 불변.
>
> **찍어서 확인한 것 — 이번엔 진짜로 화면에 떴다.** 열린 들판(town 이 아닌
> 자리)과 마을 어귀 둘 다에서, 두 사람이 마주 서서 속공을 누르면 실제로 팔을
> 들어 베는 자세 + 슬래시 이펙트가 뜨는 것을 스크린샷으로 확인했다. 진단
> **385/385, 세 번 불변**(camAim 서명이 바뀌었지만 기존 호출은 다 7개 인자까지만
> 써서 안 깨졌다).
>
> **아직 안 된 것 — 진짜 실기기(폰) 확인은 여전히 없다.** 헤드리스는 WebGL
> 은 있어도 진짜 터치 조작·화면 비율·기기 성능(이번 세션 헤드리스는 "화면
> 품질 낮음 (10~11fps)" 로 스스로 낮췄다)은 못 잰다. 밤 밝기·캐릭터 갈라짐도
> 여전히 실기기 확인 대기 목록에 남아 있다.
>
> ### 사가고 — **교전에 실제 3D 상대와 모션을 붙였다 — 실기기 확인 대기** (2026-08-30, 땅 텍스처 다음)
>
> 사용자가 도적을 만나고 "내가 원하는 전투 신이 아니다"라고 했다 — 카드 안
> 이모지(🗡️)로만 싸우는 게 아니라 **배경 3D 화면에서 실제 캐릭터가 직접
> 싸우길** 원했다. `event.js`(`foeVisual()` — 적마다 몸을 정하는 순수 함수) →
> `duel.js`(`stage3d` 를 신호에 실어 보냄) → `battle3d.js`(`duel:open` 에서
> `world3d.duelStage()` 로 세우고, `duel:fx` 마다 `world3d.playAnim()` 으로
> 실제 attack/hit/dodge 클립을 튼다) 로 이었다. 커밋 `8d2a9a1`.
>
> **실기기 확인 전에 알아 둘 것 — 이번 것도 렌더링이라 헤드리스로 못 봤다.**
> 스크래치 하네스로 `event.open→choose(fight)→duel:fx→duel:close` 전체 흐름이
> 예외 없이 도는 것과 `foeVisual()` 매핑(산적·정찰병→hero, 늑대 무리→pet)은
> 확인했지만, **실제로 상대가 눈에 보이는 자리에 서는지 · 애니메이션이
> 자연스러운지는 확인 못했다.** 다음 세션이 먼저 할 일:
>
> 1. 도적을 만나 "맞선다"를 누르면 3D 지도에 동행 선두 옆에 산적 모습(사람
>    GLB)이 실제로 서는지, 속공/필살을 누르면 그 몸이 실제로 움직이는지 확인
> 2. 늑대 무리 사건도 같은 방식으로 확인(이번엔 늑대 GLB 가 서야 한다)
> 3. 자세가 이상하면(예: 위치가 겹친다, 카메라가 안 좋다) `world3d.js` 의
>    `duelStage()`(나에게서 6m 남쪽에 세운다)와 `focusAt` 기반 카메라 구도부터
>    본다 — 원래 "무릎께를 본다"는 특수 구도(`stageAt`)가 아니라 더 일반적인
>    `focus` 구도를 재사용했으니, 조우 카드가 작아진 지금 화면과 안 맞을 수 있다
> 4. `duel.js` 의 카드 UI(HP바·시간·이모지 얼굴)를 3D 무대가 있을 때는 줄이거나
>    치우는 것도 다음 순서로 남아 있다(이번 세션은 몸짓 연결까지만 했다)
>
> ### 사가고 — **밤을 아예 없앴다 — `world3d.dayNight` 기본을 껐다** (2026-08-30, 땅 텍스처 작업 다음)
>
> 아래 절(다섯 번의 밤 밝히기)을 남긴 다음 사용자가 땅 텍스처 작업으로
> 넘어갔다가, 그걸 끝낸 뒤 다시 **"밤을 낮으로 변경해줄래?"** 라고 했다 —
> 값을 더 만지기보다 밤 자체를 없애 달라는 요청으로 받아들였다.
>
> 이미 있던 손잡이 `world3d.dayNight`(기본 1)를 끄면 `lightingAt()` 이 시각과
> 무관하게 늘 정오(alt=0.9, hour=12)로 계산한다는 것이 코드에 이미 있었다 —
> **그 손잡이의 기본값만 1→0 으로 내렸다**(`js/world3d.js` `DAYNIGHT()`).
> 게임 로직의 "밤"(NPC 일과·야행 몬스터·도적 습격 가중치)은 `npc.js`·`animal.js`
> 의 `isNight()` 가 실제 시계로 **따로** 판정하므로 이 손잡이와 무관하게
> 그대로다 — 바뀌는 건 화면 조명뿐이다.
>
> `_test.html` 의 밤낮 관련 테스트 다섯 개는 이 손잡이가 꺼진 채로는 값이
> 안 나오므로 `C.setTune('world3d.dayNight', 1)` 로 감싸 옛 계산이 여전히
> 맞는지 검증하는 쪽으로 고쳤다(기능은 안 지웠다 — 켜면 그대로 돈다).
> "기본은 늘 낮" 을 확인하는 테스트를 새로 추가했다. 진단 **384/384**(+1),
> 세 번 불변. 커밋 `627312c`.
>
> **다음 세션이 먼저 할 일 —** 실기기로 이제 밝아 보이는지 확인. 그리고
> **캐릭터 몸 갈라짐**(인물 팩 교체, 커밋 `59dee3a`)도 여전히 실기기 최종
> 확인이 안 됐다 — 밤이 없어졌으니 이번엔 제대로 보일 것이다.
>
> ### 사가고 — **밤 밝기를 다섯 번 고쳤는데도 사용자는 여전히 "어둡다" — 원인 미상으로 다음 세션에 넘긴다** (2026-08-30)
>
> 사용자가 "너무 어두워 밤을 더 밝게 해줘" 로 시작한 요청을 **다섯 번** 반영했다:
>
> 1. `sw.js` VERSION 은 이미 올라 있던 상태(인물 팩 교체 커밋에서)에서 시작
> 2. (`669cd1b`) 세기(intensity)가 아니라 **밤 색표(`C_NIGHT`) 자체**가 어두운
>    것이 진짜 원인이라 보고 sun·sky·hemiSky·hemiGnd·tint 다섯 색을 밝혔다
> 3. 사용자가 실기기로 "아직도 어둡다" — 캐시 의심(로컬로 확인했다고 답함,
>    Pages 아님을 확인)
> 4. (`db28437`) **다섯 번째** — sun/hemi 최저치를 낮 값에 더 가깝게 올리고
>    (1.4→1.7, 1.2→1.4), **깊은 밤(자정~4시) 추가 감쇠 층**(0.82/0.85/0.42/0.30
>    → 0.90/0.90/0.36/0.18)까지 옅게 줄였다. 사용자가 "요즘 시대엔 조명이
>    밝잖아" 라고 확인해줘서 무드(어두운 밤)를 사실상 포기하고 낮과 거의 같은
>    밝기로 밀었다
> 5. 사용자가 **`_swreset.html` 로 캐시를 지웠다고 확인한 뒤에도** "안되었어"
>    — 다른 작업으로 넘어가자고 함
>
> **`_test.html` 문턱은 다 통과한다**(383/383, 세 번 불변) — 한낮/깊은밤 하늘
> 밝기 비 2.34배(문턱 2배) · 밤 지도물감 0.61배(문턱 0.7배). `lightingAt()` 이
> 내는 수치 자체는 이제 낮과 거의 같다(밤 sun 1.70 vs 낮 1.93). **그런데도
> 캐시를 확인하고 지운 뒤에 여전히 어둡다고 했다** — 이건 `lightingAt()` 의
> 세기·색 문제가 아닐 가능성이 크다는 뜻이다. **다음 세션이 의심해 볼 곳:**
>
> - `post3d.js` 의 톤매핑·노출(`lookAt()` 의 `exposure`·`lift`)이 씬 조명과
>   별개로 화면을 다시 어둡게 누르고 있을 수 있다 — `TONE()`(`post3d.tone`
>   손잡이)이 기본이 아닌 값으로 저장돼 있는지, `renderer.toneMappingExposure`
>   가 실제로 `p.exposure` 를 받는지 코드는 확인했지만(576행) **실기기에서
>   그 값이 얼마로 나오는지는 안 쟀다**
> - 사용자가 보는 화면이 정말 `world3d.js` 의 3D 렌더인지 — 혹시 다른
>   오버레이(로딩 화면, 조우 배경, CSS 필터)가 덮여 있어서 조명을 아무리
>   올려도 안 밝아지는 것은 아닌지 확인이 안 됐다
> - `_admin.html` 로 실기기에서 `sun.intensity`·`hemi.intensity` 실측값을
>   직접 읽어 코드가 낸 값과 같은지부터 갈라야 한다(같으면 렌더 파이프라인
>   문제, 다르면 아직도 캐시/로딩 문제)
> - **캐릭터 몸 갈라짐**(인물 팩 교체, 커밋 `59dee3a`)도 실기기 최종 확인이
>   여전히 안 됐다 — 너무 어두워서 보였는지 안 보였는지조차 판단이 안 됐을
>   수 있다. 밤 밝기가 풀리면 이것부터 같이 재확인할 것
>
> 커밋: `669cd1b`(색 밝힘) · `db28437`(세기+깊은밤 감쇠 추가 완화).
>
> ### 사가고 — **인물 팩을 통째로 갈아 끼웠다 — 몸 갈라짐의 근본 원인을 없앴다** (2026-08-29, 바로 위 "세 겹으로 팠다" 절의 다음 세션)
>
> 위 절이 넘긴 숙제("사용자에게 실기기 스크린샷 새로 받기")를 잇기 전에,
> 사용자가 먼저 **"캐릭에셋을 바꿔줘" → "다른 모듈형 인물 팩으로 교체"** 를
> 골랐다. 옛 표(`modular_men`/`modular_women` — Farmer·Worker·Lady·King·Casual)는
> **부위(몸통·머리·다리)마다 뼈대가 따로** 붙어 있는 게 설계 자체였다 — 그래서
> 리타기팅을 아무리 고쳐도(①②③, 세 겹) 걷는 동안 몸이 갈라졌다. **다른 팩으로
> 바꾸면 이 문제 자체가 없어질 수 있다**는 것을 이번에 확인했다.
>
> **막힌 길 — itch.io 는 자동으로 못 받는다.** Quaternius 의 진짜 좋은 후보
> (`Universal Base Characters` — 뼈 이름까지 표준화돼 리타기팅이 아예 필요 없는
> 설계)는 itch.io 에서 받는데, **가격 $0 이어도 "구매" 절차에 이메일 입력이
> 필수**다. 사용자 몰래 아무 이메일이나 자동으로 써넣는 것은 안전 규칙(개인정보
> 자동 입력 금지) 위반이라 **여기서부터는 사용자가 직접 받아 이 대화에 올려야
> 했다** — `Universal Base Characters[Standard].zip`(122MB) ·
> `Universal Animation Library[Standard].zip`(15MB) ·
> `Modular Character Outfits - Fantasy[Standard].zip`(280MB) 셋을 사용자가
> 직접 받아 채팅에 올려 주었다.
>
> **망 제약이 이번엔 안 걸렸다.** 예전 세션들이 적어 둔 "`quaternius.com`·
> `kenney.nl`·`polygone.art` 는 이 망에서 막힌다(`000`)" 는 **이번 세션에서는
> 안 맞았다** — `quaternius.com`·`kenney.nl`·`itch.io` 다 200 으로 열렸다.
> 다음 세션은 막혔다고 단정하지 말고 먼저 `curl` 로 확인할 것(망 사정은 세션마다
> 다를 수 있다).
>
> **왜 이 조합이 정말 다르다 — 뼈 이름까지 완전히 같다.** 받은 몸(Universal Base
> Characters) · 옷(Modular Character Outfits - Fantasy) · 몸짓(Universal
> Animation Library) 셋을 `.gltf` JSON 으로 직접 대조했다 — **65개 뼈 이름·순서가
> 열두 파일(몸 둘·옷 넷·머리 여섯) 전부 한 글자도 안 다르다.** 그래서:
>
> 1. **`pickPieces` 가 필요 없다** — 부위 변형(`_Body_1..4` 식)이 아예 없는
>    설계라 무엇을 고를지 겨룰 일이 없다
> 2. **`retargetClip` 이 필요 없다** — 옷·머리의 스킨 메시를
>    `mesh.bind(몸의 스켈레톤, mesh.bindMatrix)` 로 다시 물리기만 하면 몸과
>    같이 움직인다. 몸짓도 원본(`UAL1_Standard.glb`, 41개)을 몸의 뼈대에
>    `AnimationMixer` 로 그대로 물린다 — 옮겨 입힐 필요 자체가 없다
> 3. **뼈대가 부위마다 따로 노는 옛 근본 원인이 새 설계에는 없다** — 몸·옷·머리가
>    처음부터 **한 스켈레톤 객체**를 공유하므로 걷는 동안 갈라질 수가 없다
>    (원리상 그렇다는 것이고, **실기기 확인은 아직이다** — 아래 참고)
>
> `js/asset3d.js` 를 크게 고쳤다 — `HERO_RECIPES`(몸+옷+머리 조합 여섯 벌:
> 남 셋·여 셋), `assembleHero()`(묶기), `buildHero()`(넷을 받아 오고 묶는 자리),
> `heroRecipe()`(표에서 조합 객체를 그대로 꺼낸다). `urlOf('hero',...)` 는 이제
> 조합의 `key` 문자열(예: `male_peasant_buzzed`)을 준다 — 옛 진단이
> `.split('/').pop()` 을 쓰던 자리라 문자열이어야 했다(순수 함수 규약은 안 깼다).
>
> **텍스처는 BaseColor 만 받았다.** 이 판 재질(`delam`)이 Normal·Roughness·ORM
> 을 애초에 안 쓰므로, `.gltf` 의 그 세 텍스처 참조를 지우고 이미지 파일 자체를
> 옮기지 않았다 — `Modular Character Outfits` 원본은 옷 한 벌에 Normal 14MB·
> ORM 9.9MB 를 지고 있었는데 그걸 걸렀다. 다 걸러도 몸 둘·옷 넷·머리 여섯이
> **27MB**(줄이기 전 128MB+294MB+16MB 짜리 zip 셋에 비하면 큰 폭으로 줄었다).
>
> **옛 표(Knight·King·Casual·Farmer·Worker·Lady GLB 여섯)는 지웠다** — 다른
> 코드에서 파일명으로 참조하는 곳이 없는지 먼저 훑고 지웠다. `_test.html` 의
> 관련 항목 둘도 새 구조에 맞게 고쳤다(`urlOf` 가 문자열을 주는 것은 그대로라
> 대부분은 안 건드려도 됐다 — `A.DEFAULTS.hero.length` 처럼 값을 스스로
> 참조하는 항목들은 6 으로 바뀐 것도 그대로 통과했다). 진단 **383/383**, 세 번
> 돌려 불변.
>
> **아직 확인 못한 것 — 다음 세션이 제일 먼저 할 일.** 이번 세션은 헤드리스가
> 큰 텍스처(옷 BaseColor 5~6.6MB) 넷을 실을 때 `--virtual-time-budget` 이 실제
> 로딩을 기다리지 않고 먼저 dump 해 버리는 새 헤드리스 함정을 만났다 — 실제
> 브라우저에서는 문제가 아닐 걸로 보이지만(단순 텍스처 디코드 시간일 뿐, 예전
> 세션들이 이미 "헤드리스로는 진짜 3D 성능을 못 잰다" 고 적어 둔 것과 같은 결)
> **화면으로 직접 확인은 못했다.**
>
> 사용자가 `_demo.html#3d,people` 진단 화면을 실기기로 봤을 때는 **폭 값(0.9~1.0,
> 정상)** 은 확인됐지만 밤이라 눈으로는 잘 안 보였고, 밝게 하려고 어드민
> `world3d.dayNight=0` 을 안내한 뒤 실제 게임(`index.html`)에서 다시 봤을 때
> **"떠 다니던 것(예전 갓 뜨던 그 증상)이 아직 안 없어졌다"** 고 했다. 그런데
> 그 확인이 **`sw.js` 의 `VERSION` 을 올린 뒤 `_swreset.html` 로 캐시를 비우기
> 전**이었을 가능성이 크다 — `_swreset.html` 을 열어 주긴 했지만 그 뒤 결과를
> 못 받고 세션이 끊겼다. **다음 세션이 제일 먼저 할 일:**
>
> 1. `_swreset.html` 로 캐시를 비운 뒤(**정말 비워졌는지 콘솔·새로고침으로
>    확인**) 다시 실기기로 봐 달라고 부탁한다 — 옛 `Farmer/Worker/Lady` 가
>    여전히 보인다면 백 퍼센트 캐시 문제였다는 뜻이다
> 2. 캐시를 비워도 여전히 뜬다면 — 이번엔 **뼈대가 다시 갈렸다기보다 `bind()`
>    자체의 문제**(스킨 인덱스 순서 어긋남, 또는 `Armature` 래퍼 한 겹을 건너뛰고
>    `bodyScene.add(m)` 한 자리)를 의심할 것 — `js/asset3d.js` 의
>    `assembleHero()` 하나만 보면 된다. 몸·옷·머리 파일의 스킨 관절 이름·순서가
>    65개 다 같다는 것은 이미 `.gltf` JSON 대조로 확인해 두었으니(이 문서 위 절),
>    **거기를 다시 재는 것은 헛수고다** — 문제라면 런타임의 `bind()`/재부모 자리다
>
> 사용자가 이 세션에서 같이 요청한 것 둘 — **"가까이 가기" 버튼을 실제로
> 누르면 그쪽으로 걸어가게**(`js/ui.js` 의 `approachBtn()`, `world.walkTo()`
> 그대로 씀 — 시뮬레이션 이동에서만 켜진다) 반영했고 진단 통과·기능 확인까지
> 했다. **"떠다니던 것"**(캐릭터 갈라짐의 그 현상 — 갓이 허공에 뜨는 것)은 이번
> 인물 팩 교체로 원리상 없어졌어야 하지만, 위에 적었듯 실기기 확인이 남았다.
>
> ### 사가고 — **캐릭터 몸 갈라짐을 세 겹으로 팠다 — 그래도 사용자는 "아직 안 된다"** (2026-08-29, 벽 충돌 다음 — 미해결로 다음 세션에 넘긴다)
>
> 사용자가 실기기 스크린샷을 줬다: 캐릭터가 **다리만 남고 갓이 허공에 떠** 있었다.
> "밤이라 안 보이나" 로 시작했지만 아니었다 — `_demo.html` 을 강제로 낮·2D 로
> 찍어 봐도 **똑같이** 갈라져 있었다. 세 가지를 순서대로 고쳤는데, **사용자는
> 최종 확인에서 "캐릭은 안되네 아직"** 이라고 했다 — 아래 셋 다 진단(383/383)은
> 통과하지만 실기기에서 여전히 실패다. **다음 세션은 여기부터.**
>
> **① `pickPieces()` 가 부위를 따로따로 뽑아 안 이어졌다.** `Farmer.glb`·
> `Worker.glb`·`Lady.glb`(Quaternius 모듈형 인물 팩) 는 다리·몸통·머리·발을
> `_숫자` 로 끝나는 변형 여럿으로 들고 오는데, 원래 코드는 무리마다 **따로**
> 해시로 골라 무엇과 무엇이 나란히 서는지 안 쟀다. GLTFLoader 로 직접 열어
> y 범위를 재보니, 예를 들어 Lady 는 다리(0.34~1.14) 위에 **몸통 변형에 따라
> 1.0~1.55 사이 아무 데나** 걸치고 머리(1.5~1.9)가 또 따로 논다 — 잘못 뽑히면
> 몸통과 머리 사이가 통째로 빈다. `js/asset3d.js` 의 `pickPieces()` 를 **낮은
> 무리부터 이미 고른 조각의 꼭대기에 잘 닿는 것을 우선**하도록 고쳤다(three
> 없이 도는 자가진단 목업과도 호환되게 `measurable` 갈림길을 뒀다). `King.glb`·
> `Casual.glb` 는 애초에 다리~몸통을 잇는 조각이 없어서(모델 키의 1/3만큼 틈,
> 무엇을 골라도 못 없앤다) 인물 로테이션 6벌→4벌로 뺐다.
>
> **② 서비스 워커 캐시.** 위 수정이 브라우저에 전혀 안 먹혀서 한참 헤맸다 —
> `sw.js` 가 파일을 cache-first 로 통째로 물고 있는데, 파일을 고칠 때마다
> `VERSION` 을 올리지 않으면 옛 캐시가 계속 나온다(파일 자신에 그렇게 적혀
> 있었다). `_swreset.html` 로 강제로 비워야 새 파일이 나온다는 것도 이번에 다시
> 확인했다 — **이 판을 고칠 때마다 잊지 말 것.**
>
> **③ 뼈대가 하나가 아니라 넷이었다 — 이게 진짜였던 것 같은데도 사용자는
> 여전히 안 된다고 했다.** ①로 정지 자세는 이어졌는데, 걷기 시작하면 다시
> 갈라진다는 게 재확인됐다. GLTFLoader 로 스켈레톤 UUID 를 세어 보니 **Knight
> 만 뼈대 하나**(제 몸짓 12개를 갖고 있다)고, **Farmer·Worker·Lady 는 몸통·발·
> 머리·다리가 저마다 딴 뼈대 넷**이었다. 이 판이 Knight 의 몸짓을 옮겨 입히는
> `retargetInto()` 는 `firstSkinned()` 가 찾는 뼈대 **하나만** 돌리므로, 그
> 하나만 움직이고 나머지 셋은 제자리에 남아 걷는 동안 몸이 찢어진다 — 정지
> 화면(스크린샷·헤드리스 확대)에선 안 보이다가 애니메이션이 실제로 돌면
> 드러나는 종류의 버그라 이번에도 늦게 잡혔다. `needsRetarget()` 에
> `skeletonCount(obj) === 1` 조건을 넣어 **뼈대가 여럿이면 옮겨 입기를 포기하고
> 가만히 세운다**(움직이며 갈라지는 몸보다 낫다고 판단했다)로 고쳤다.
>
> **그런데도 사용자는 마지막에 "캐릭은 안되네 아직"이라고 했다.** ③까지
> 반영한 커밋(`db59219`)을 캐시 비우고 재확인해 달라고 부탁했지만, 결과를
> 못 받고 세션이 끊겼다 — **다음 세션이 제일 먼저 할 일:**
>   1. 사용자에게 실기기 스크린샷(또는 콘솔 로그) 새로 받기 — ①②③ 다 반영된
>      상태에서도 안 되는 건지, 캐시가 또 안 지워진 건지부터 가른다
>   2. 그래도 갈라지면 — Knight 하나로만 인물 로테이션을 줄이는 것도 고려
>      (Farmer·Worker·Lady 는 이제 안 움직이는 마네킹이라 "덜 부서졌지만
>      여전히 이상해" 보일 수 있다)
>   3. 부수적으로 이번 세션에 같이 고친 것들도 확인이 필요하다 — 밤 밝기
>      (태양광 최저치 0.28→1.4·반구광 0.52→1.2, 진단 문턱도 2배→1.1배로
>      낮췄다)와, 배우 머리 위에 떠 있던 등롱 PointLight 제거(`world3d.js`)
>
> 커밋: `471aab7`(①) · `f765cc7`(밤 밝기+등롱) · `db59219`(③). 진단 383/383,
> 회귀 없음 — 그러나 **이 셋 다 "진단은 통과, 실기기는 미확인/실패"** 라는
> 꼬리표를 달고 다음 세션으로 넘긴다.
>
> **아직 시작 안 한 것 — 사용자가 이 세션에서도 다시 요청함("바닥도 에셋으로
> 해줄수 있어?"):** 땅(지면)이 아직 `world3d.js` 의 `landTexture()` 가
> `LAND_COLOR` 표(`grass`·`forest`·`mount`·`water`·`road`·`town`·`farm`)로
> **색만 칠하는 `CanvasTexture`** 다(손으로 그린 땅 — `land.js` 가 맡은 칸에만
> 덮인다. 지도 타일 자체는 실사진이라 따로다). PLAN.md 부록("코드로 그리지 말고
> 에셋으로")대로 실제 타일 텍스처로 갈아 끼워 달라는 요청이고, **다음 세션이
> 진행한다** — 이 세션은 인물 팩 교체에 시간을 다 썼다.
>
> **망 제약 기록을 고칠 것 — 이번 세션엔 하나도 안 맞았다.** 옛 기록("GitHub
> 미러만 열린다, `kenney.nl`·`quaternius.com`은 막힌다")은 이번 세션에서
> `quaternius.com`·`kenney.nl`·`itch.io` 다 200 으로 열려서 더는 안 맞는다
> (`ASSET_LICENSES.md` 의 인물 팩 절 참고 — 이번엔 itch.io 에서 직접 받았다).
> **다음 세션은 막혔다고 단정하지 말고 `curl` 로 먼저 확인할 것.**
>
> 바닥 텍스처는 잠깐 뒤져 봤다 — `kenney.nl/assets/ground-textures` 류의
> 짐작 슬러그는 다 404 였다(Kenney 는 검색 목록이 JS 렌더링이라 슬러그를
> 짐작으로만 찾을 수 있다 — `blocky-characters` 처럼 정확한 이름을 알아야
> 페이지가 뜬다). **다음 세션이 이어갈 방법:**
>
> - Kenney 는 최근에 `Prototype Textures`·`Nature Kit`(텍스처 포함) 같은 이름을
>   써 왔다 — 정확한 슬러그를 알아내려면 kenney.nl 웹 검색(브라우저 자동화나
>   사용자에게 링크를 물어보는 쪽)이 필요할 수 있다
> - **ambientCG**(`ambientcg.com`) 도 CC0 타일 텍스처 전문이고 이번 세션엔
>   네트워크가 열려 있었다 — 아직 확인은 안 했다
> - 이 판 재질(`delam`)은 **BaseColor(diffuse) 한 장이면 충분하다** —
>   Normal·Roughness·AO 맵은 받아도 안 쓴다(인물 팩 때처럼 걸러서 받을 것)
> - `landTexture()` 는 지금 `S=256` 캔버스에 `fillRect` 로 칸을 칠한다
>   (`js/world3d.js` 368행 언저리) — 그 자리에 `c.drawImage(타일이미지, ...)`
>   로 반복 타일을 그려 넣으면 된다. 땅 종류(`grass`·`forest`·`mount`…)마다
>   타일 하나씩 필요하다
>
> ### 사가고 — **벽 충돌을 새 기능으로 넣었다** (2026-08-29, PLAN 27절)
>
> "벽이라는 개념이 없네"(사용자가 실기기로 발견) — 이 판은 원래 GPS 거리 이동
> 게임이라 충돌 판정 없이 설계됐지만, **시뮬레이션 이동(키보드·스틱·탭)에서도
> 집을 그대로 통과하는 것**은 사용자가 새 기능으로 넣길 원했다.
>
> **어느 수준으로 맞출지 먼저 물었다** — ① 판정층이 스스로 만드는 간단한 가상
> 장애물(빠르지만 눈에 보이는 집과 안 맞을 수 있다) ② `world3d.js` 가 그리는
> 그 집 사각형을 그대로 읽어 충돌(정확하지만 "화면 값은 판정에 안 닿는다"는
> 이 저장소의 반복된 원칙을 깬다). **사용자가 ②(정확히 맞춘다)를 골랐다.**
>
> `world3d.js` 에 `houseRects(gx, gy)` 를 새로 짰다 — `propPlan`(집을 그리는
> 순수 함수)이 낸 것과 **완전히 같은 좌표**를 돌려준다. `world.js` 의
> `moveByKeys` 가 이동 전 그 사각형과 원-사각형 겹침을 검사해, 막히면 **벽을
> 따라 미끄러지고**(대각선으로 부딪혀도 그대로 안 멎는다), 완전히 막히면 탭
> 이동 목표를 버린다(안 그러면 자동 순행이 벽에 붙어 제자리걸음처럼 보인다).
> **GPS 모드는 손대지 않았다** — `moveByKeys` 자체가 `mode==='keyboard'` 일
> 때만 도는 함수라 실제 걸음에는 안 걸린다.
>
> **밟은 것 — 남은 네 판에서도 만난다:** 회전한 사각형과의 충돌은 **플레이어
> 좌표를 사각형의 회전만큼 반대로 돌려서** 재면 흔한 축 나란한 사각형 겹침
> 계산으로 풀린다(사각형을 돌리는 대신 질의점을 돌린다).
>
> 확인: 진단 **383/383**(이 기능은 아직 진단에 항목이 없다 — 회귀만 확인했다).
> 손잡이 **`world.collide`**(어드민에도 얹었다) 0 이면 예전처럼 통과한다.
> 시각 확인은 다음 세션 또는 사용자의 실기기로 넘긴다.
>
> ### 사가고 — **배우가 새까맣게 서던 것을 고쳤다** (2026-08-29 → 벽 충돌 전
> 마지막 — 사용자가 실제로 플레이하다 스크린샷으로 발견했다)
>
> 사용자가 폰·PC 화면을 캡처해 셋을 한꺼번에 보고했다 — "캐릭터가 깨져 보인다",
> "조우 상대가 안 보인다", "선택 문구 화면에서도 상대가 안 보인다". 스크린샷
> 두 장 다 같은 것을 보여줬다: **사람 자리가 텅 빈 검은 실루엣**으로 서 있었다.
>
> 헤드리스 크롬이 여전히 실제 3D 장면에서 죽어서(이전 절 참고) **GLB 를 렌더
> 없이 직접 파싱하는 쪽으로 원인을 좁혔다.** 인물·짐승·역참·성채 GLB 를 전부
> 열어 재질을 찍어 보니, **하나같이 `metallicFactor 0.4`** 를 지고 있었다.
> 그런데 이 판의 조명은 방향광(sun)·반구광(sky) 둘뿐이고 **환경맵(IBL)이 없다**
> — 금속 반사는 환경맵이 있어야 밝게 보이는데 그게 없으니, 반금속 재질은
> 직접광의 확산 성분만 40% 깎인 채로 받고 반사할 환경이 없어 **거의 새까맣게**
> 선다. `prop3d.js`(나무·집)는 이 문제를 진작에 알고 빛깔만 뽑아 Lambert 로
> 갈아 끼우고 있었는데(README 에 이미 적혀 있었다), **`asset3d.js`(사람·짐승·
> 역참·성채)만 원본 PBR 재질을 그대로 쓰고 있었다** — 표에 덧대는 재질 처리가
> 아예 없었다.
>
> `asset3d.js` 에 `delam(root)` 를 추가해 GLB 를 받는 그 순간(`acquire` 의
> 로드 콜백) 재질을 훑어 `MeshStandardMaterial`/`MeshPhysicalMaterial` 을
> **빛깔만 남긴 `MeshLambertMaterial`** 로 바꾼다. 스키닝은 재질 종류와 무관하니
> 움직이는 사람도 그대로 걷는다. **원본을 그 자리에서 고친다**(사본이 아니다)
> — 캐시에 한 번뿐이라 다른 배우와 부딪히지 않는다.
>
> **밟은 것 — 남은 네 판에서도 만난다:** Quaternius 류 CC0 에셋은 흔히
> **환경맵 없는 간단한 조명을 가정하지 않은 PBR 재질**로 나온다. 배우를 GLB 로
> 세우는 판이면, **에셋을 처음 받는 자리에서 재질부터 확인**한다 — 이 판만의
> 문제가 아니라 "환경맵 없이 metallicFactor > 0 인 GLB 를 그대로 쓰면 어둡게
> 선다"는 것이 규칙이다. `prop3d.js` 는 처음부터 이걸 알고 있었지만 그 지식이
> `asset3d.js` 에는 안 옮겨져 있었다 — **한 판 안에서도 같은 지식이 파일마다
> 따로 놀 수 있다.**
>
> 확인: 진단 **383/383**(재질은 판정에 안 닿는다). 시각 확인은 다음 세션(또는
> 사용자의 실기기)에 넘긴다 — 이 세션의 헤드리스는 여전히 못 쓴다.
>
> **아직 다루지 않은 것** — 사용자가 같이 보고한 "자동 순행 문구가 화면을
> 다 가린다"는 스크린샷에서 재현이 안 됐다(그 스크린샷에서는 자동 순행 띠가
> 작게 떠 있을 뿐이었다). 다음에 다시 나오면 그 순간의 스크린샷이 필요하다.
> 그리고 **"벽이라는 개념이 없다"**(몸이 집을 그대로 통과)는 버그가 아니라
> 이 판(포켓몬GO식 GPS 거리 이동)이 원래 충돌 판정 없이 설계된 것임을 확인했다
> — `world.js` 에 벽·건물 충돌이 아예 없다. 사용자가 **새 기능으로 넣는 쪽**을
> 원하는 것으로 보고 다음 순서로 진행한다.
>
> ### 사가고 — **우물·장터를 마을 랜드마크로 세웠다** (2026-08-28→29)
>
> 어제 인계가 "남은 것"으로 적어 둔 마지막 둘 — `Well.glb`(우물)·`MarketStand_1.glb`
> (좌판)을 걸었다. 못 걸었던 까닭이 모델 문제가 아니라 **자리 문제**였다는 것을
> 렌더 없이 GLB 를 직접 파싱해(바운딩박스를 노드 변환까지 적용해 재는 스크립트를
> 새로 짰다 — 이번엔 헤드리스 크롬이 계속 죽어 그 대신 썼다) 확인했다:
>
> · 우물 키 1.25m · 장터 키 1.05m — 여관(3.49m)의 셋에 하나 정도다
> · 폭·깊이 비율은 **정상**이다(우물 0.53×0.80, 장터 0.91×1.13 — 정규화하면
>   찌그러지지 않는다). 문제는 모양이 아니라 **`house` 표와 섞이면 안 된다**는
>   것뿐이었다 — 집은 `p.h` 4~20m 로 정규화되는데 그 줄에 우물이 끼면 집만큼
>   부풀거나 집이 우물만큼 쪼그라든다
>
> 그래서 `prop3d.js` 에 **`well`·`market` 을 제 이름으로 따로** 등록하고(`house`·
> `tower` 와 나란히, 섞이지 않게), `world3d.js` 의 마을(`town`) 칸에 **드물게만**
> 세운다(우물 14%·장터 10%, 번화한 칸일수록) — 집처럼 흔하면 랜드마크로 안 보인다.
> 못 받으면(파일 없음·`file://`) `rice`가 그렇듯 **조용히 안 선다** — 논이 비지
> 않듯 마을도 비지 않는다.
>
> 확인: 진단 **383/383**(세 번, PASS/FAIL 불변 — 우물·장터도 판정에 안 닿는다).
> 화면으로는 아직 못 봤다 — 이 세션의 헤드리스 크롬이 실제 GLB 를 얹은 장면에서
> SwiftShader 렌더러가 반복해서 access violation 으로 죽는다(`exit_code=
> -1073741819`). **다음 세션이 헤드리스를 또 켜기 전에 먼저 확인할 것**: 가벼운
> `_test.html`(DOM 뿐, WebGL 없음)은 멀쩡히 돈다 — 죽는 것은 **실제 3D 장면을
> `--dump-dom` 으로 띄우는 자리뿐**이다. 크롬을 새로 깔거나 `--use-gl=swiftshader`
> 를 명시하는 것부터 시도해 볼 만하다.
>
> ### 사가고 — **벼를 심었다** (2026-08-28 → 2026-08-29, 이어받았다)
>
> 어제 인계가 남긴 실마리(**"창고 첫 자리의 좌표가 격자 중심이 아니다"**)를 그대로
> 따라가 잡았다. **범인은 모델도 계획도 아니라 `var` 였다.**
>
> 논밭을 그리는 함수 안에서 두렁의 자리를 `var ox`/`var oz` 로 적었는데, `var` 는
> **함수 범위**다 — 그 함수 맨 위에서 이미 쓰고 있던 **격자 원점**(`ox`·`oz`)을
> 두렁이 그때그때 덮어써 버렸다. 그래서 논 뒤에 심는 벼가 전부 그 마지막 값
> (9, 0) 근처로 끌려가 **땅 밑에 깔려 있었다** — 어제 본 그 좌표가 바로 이것이다.
> 이름을 `lox`·`loz` 로 바꿔 원점을 더 이상 안 건드리게 했다.
>
> **밟은 것 — 남은 네 판에서도 만난다:** `var` 로 적은 임시 변수는 **감싼 블록이
> 아니라 감싼 함수 전체가 범위**다. 같은 함수 안에 이름이 겹치는 자리가
> 또 있으면(원점·오프셋처럼 "짧고 흔한 이름") 나중에 온 코드가 조용히 앞선 값을
> 덮어쓴다 — 에러도 안 나고, 옆에 있는 값만 슬며시 어긋난다. **겹칠 만한 이름은
> 접두어를 붙여 가른다**(이번엔 `lox`/`loz`).
>
> `Rice_4.glb` 를 다시 표에 걸고, 뙈기 안에 골고루 심는 자리(`world3d.js`)도 함께
> 넣었다. `Rice_Crop` 은 키 5cm 짜리 밭 바닥이라 안 쓴다 — 다 자란 `Rice_4` 를 쓴다.
> `DG.world3d.instReport('Rice')`(어제 만든 자)는 **다음에 같은 꼴을 만나면 여전히
> 첫 자리로 부른다** — 창고에 들어가는데 화면에 없는 것을 가를 때 쓴다.
>
> 확인: 진단 **383/383**(개수는 어제와 같다 — 벼는 판정에 안 닿는다), 세 번 돌려
> 시각·거리 표시(비결정 항목, 이미 알려진 것)만 갈리고 PASS/FAIL 은 그대로.
>
> ### 사가고 — **벼가 안 서는 원인을 여기까지 좁혔다** (2026-08-28)
>
> **모델 탓이 아니다.** 끝까지 따라가 본 결과다:
>
> · `Rice_4.glb` 자체는 멀쩡하다 — three 로 직접 로드해 조각 둘(정점 2482 · 1472),
>   키 1.000 · 폭 0.797 로 정규화되고 **실제로 그려진다**(픽셀을 세었다)
> · **계획도 정상** — `propPlan` 이 논 격자마다 벼를 낸다(진단이 값으로 붙들었다)
> · **인스턴스 창고에도 들어간다** — `Rice_4.glb#0=78/78`. 메시 상태도 정상
>   (보임1 · 재질1 · 정점2482 · 부모group)
> · **그런데 화면에 한 포기도 안 선다.** 키를 20m 로 키워도, 격자 한가운데
>   고정해도, **그 자리에 나무를 대신 넣어도** 안 선다 — 즉 모델이 아니라
>   `rice` 라는 이름으로 도는 **이 길 어딘가**다
> · **실마리 — 여기서부터 파면 된다:** 창고 첫 자리의 좌표가 **(9, 0.2, 0)** 으로
>   찍힌다. `p.x` 를 0 으로 고정했으니 격자 중심(24 의 배수 + 24)이어야 하는데
>   그 값이 **아니다**. 좌표가 엉뚱한 데로 간다
>
> **이번에 만든 자 — 다음에도 쓴다:** `DG.world3d.instReport('Rice')` 가
> `Rice_4.glb#0=78/78 [보임1 재질1 정점2482 부모group 첫자리…]` 를 준다.
> **"계획은 나오는데 화면에 없다" 를 가를 때 이것 하나면 대개 끝난다.**
> 데모에서는 **`#stat,inst=Rice`** 로 상태 줄에 띄운다.
>
> 벼는 다시 뺐다 — 안 보이는 죽은 길을 남기지 않으려는 것이다(파일은 남겨 뒀다).
>
> ### 사가고 — **땅에 높낮이를 주었다** (2026-08-28 · PLAN 14절)
>
> 지면이 **완전한 평면**이었다 — 지도 타일을 `PlaneGeometry` 한 장에 깔고 눕힌
> 것이 전부였고 산은 그 위에 얹은 원뿔 하나였다. PLAN 14절이 금지하는 그 자리다.
> `js/relief3d.js` 를 만들어 **이미 있는 지형 격자**(`world.terrainAt`, 48m)를
> 읽어 높이를 낸다 — 새 값을 지어내지 않으므로 지도·미니맵·손으로 그린 땅과
> 어긋나지 않는다.
>
> **밟은 것 넷 — 남은 네 판에서도 만난다:**
>
> 1. **산 격자는 흩어져 있다.** 한 칸씩 외따로 선 것을 다 16m 로 솟구치면
>    산맥이 아니라 **압정밭**이 된다(눈으로 보고 알았다). 이웃 아홉에 산이 몇인지
>    세어 **외딴 칸은 언덕(4m) · 이어진 한가운데는 봉우리(16m)** 로 간다
> 2. **눕히기 전 좌표계라 y 가 아니라 z 를 민다.** 지면 메시는 `rotation.x = -90°`
>    로 눕혀 두었으므로 **로컬 +z 가 월드 +y** 다 — y 를 밀면 땅이 옆으로 밀린다
> 3. **타일을 잘게 나눠야 한다**(기본 8칸). 네 꼭짓점뿐이면 아무리 높이를 줘도
>    땅이 **기울 뿐 굽지 않는다**
> 4. **카메라와 시선도 같이 올린다.** 안 올리면 산에서는 땅이 화면을 덮고
>    골짜기에서는 하늘만 보인다. `camAim` 은 순수 함수로 남기고 그 위에 얹는다
>
> **마을·길·논밭은 흔들지 않는다** — 집이 기울고 길이 물결치면 사람이 만든 자리로
> 안 보인다. 진단이 "종류마다 한 값" 으로 붙들고 있다.
> **판정에는 한 줄도 안 닿는다** — 스폰 목록까지 견주는 항목이 그대로 만점이다.
>
> ### 사가고 — **코드로 그리던 것을 에셋으로 (①②③ 다 했다)** (2026-08-28)
>
> **사용자 방침이다 — 다섯 판 공통.** "스크립트로 쓰고 있는 것들은 다 에셋으로.
> 스크립트는 오직 이벤트나 기능들. 도감 일러스트나 캐릭터도, UI 도."
> 다섯 `PLAN.md` 에 부록으로 넣었고 메모리에도 적었다.
>
> **① 손으로 그린 땅의 소품** — 산봉우리 · 등롱 · 옛 사당 · 굴 입구 · 폐허 · 다리.
> Quaternius 미러 1545 개를 훑어 골랐다(`assets/models/props/`).
> · **다리는 키가 곧 이음매다** — 한 칸짜리라 일곱을 잇는데, 키 1 로 눕혔을 때
>   깊이(0.76)와 칸 간격이 맞아야 한다. 짧게 잡았더니 아치 사이가 벌어졌다
> · **등롱·사당의 불만은 코드가 계속 얹는다** — 밤에만 켜지는데 **인스턴스 덩이는
>   낱개를 켜고 끌 수가 없다**. 몸은 모델이 세우고 불만 따로
> · **허수아비는 CC0 에 없다** — 미러를 다 훑었다. 그 자리는 코드가 그린다
> · **벼는 못 세웠다.** `Rice_4.glb` 는 파일도 로드도 정상인데(three 로 재니 키
>   0.95m · 메시 둘) 인스턴싱 길에 얹으면 한 포기도 안 선다. 계획은 나온다.
>   등급을 HIGH 로 못박아도, 창고를 2000 자리로 키워도 그대로다 — **원인 미상.**
>   안 보이는 죽은 길을 남기지 않으려 표에서 뺐다(파일은 남겨 뒀다)
>
> **② 도감 초상** (`js/portrait3d.js`) — **인물 일흔의 초상 일러스트는 CC0 로
> 존재하지 않는다.** 관우·이순신·세종의 그림을 받아 올 곳이 없다. 그래서
> **이미 가진 인물 GLB 를 오프스크린으로 렌더해 초상을 굽는다.** 코드가 그린
> 그림이 아니라 에셋으로 만든 그림이고, 덤으로 **지도 위의 그 사람과 도감의 그
> 사람이 같아졌다**(여태 둘은 서로 다른 그림이었다).
> 화면이 한 번도 안 빈다 — `ui.js` 는 이름표(`data-p3`)만 붙이고, 다 구워지면
> `sweep()` 이 `src` 만 갈아 끼운다. 렌더러는 **한 번만** 만든다(컨텍스트가 넘친다).
>
> **③ UI 아이콘** (`js/icon.js`) — 이모지는 **폰트가 그리는 것**이라 기기마다
> 다르게 보인다. 도구줄·독·지갑만 Lucide(**ISC**) 열아홉 가지로 바꿨다.
> HTML 은 이모지를 그대로 두고 이름표(`data-icon`)만 붙인다 — 손잡이 `icon.on`
> 을 0 으로 내리면 이모지가 남는다. **인물·펫의 이모지는 안 바꿨다** — 그것은
> 아이콘이 아니라 **상징**이다(관우의 칼, 장비의 술).
> 전수 조사: 이 판에 이모지 **162종 645개**, 그중 133개가 `data.js` 의 상징이다.
>
> 진단 **377/377**. 손잡이 셋이 늘었다 — `portrait3d.on` · `icon.on` ·
> `asset3d.retarget`.
>
> ### 사가고 — **사람이 화면에서 사라져 있었다** (2026-08-28)
>
> 사용자가 발견했다 — "움직이는 캐릭터가 안 보여, 맵 밑에 있는 것 같아". 맞았다.
> **그림자 원반만 땅에 있고 몸이 없었다.**
>
> **진짜 원인은 한 줄이었다.** `asset3d.normalize` 가 GLB 를 재기 전에
> **`updateMatrixWorld(true)` 를 안 불렀다.** 계층에 걸린 스케일이 빠진 채로 재어
> 키가 **스무 배**로 나왔고, `fit` 이 그만큼 잘게 줄여 놓아 사람이 **옆으로 4.3 배
> 넓은 조각**이 됐다(재 보니 키 1 에 폭 4.3, 정상은 0.90). 그 꼴로는 화면에
> 아무것도 안 그려진다. `prop3d.partsOf` 는 이 줄이 있어서 나무·집은 멀쩡했다 —
> **GLB 를 재는 자리마다 이 줄이 있는지 본다.**
>
> **왜 여태 몰랐나.** `_demo.html#people` 이 `D`(도감)를 선언하지 않아
> `ReferenceError` 로 **통째로 죽어 있었다**(`bb6572f` 부터). 인물 여섯 벌을 넣은
> 세션이 "확인할 자리를 따로 만들어 두었다" 고 적었지만 **그 자리가 안 돌았다.**
> 사람이 한 명도 안 서는 화면을 보고도 장면 탓으로 넘어간 것이다.
> **교훈: 확인용 장면을 만들면 그 장면이 실제로 도는지부터 본다.**
>
> **덤으로 드러난 것 — 몸짓이 여태 안 옮겨지고 있었다.** 번들된
> `SkeletonUtils.retarget` 은 짝 뼈를 `options.names[뼈이름]` 으로만 찾는데
> **되돌림(`|| bone.name`)이 두 군데나 없다**(번들 소스를 직접 읽고 알았다).
> 이름표를 안 주면 **한 뼈도 못 맞춘다.** `boneNameMap` 으로 이름이 같은 뼈끼리
> 이어 주고, 뼈대 키가 세 배 차이라(`Knight` 5.60 · 나머지 1.87)
> `options.scale` 에 키 비율도 넘긴다.
>
> **진단에 새로 붙인 자**(다음에 또 만난다):
> · `#people` 이 화면 아래에 **상태 한 줄**을 찍는다 — 표·받음·실패·세움·갈아낌,
>   그리고 **인물 여섯을 각각 세워 잰 키·폭·배율·조각 이름**. 그림만 봐서는
>   "안 왔나 / 왔는데 크기가 0 인가 / 땅속인가" 를 못 가른다
> · `#dens=0.1` — 나무를 걷어 낸다. **헤드리스 3D 한 장이 몇 분에서 7초**로 준다
> · `#noretarget` — 몸짓 옮겨 입기만 끈다(원인 가르기)
> · `#people,zoom=0.45` · `#stands,zoom=1.6` — 다가가 뜯어본다
>
> ### 사가고 — **역참과 성채도 진짜 모델** (2026-08-28)
>
> 받아만 두고 안 걸었던 건물 파일을 걸었다. **역참은 여관**(`Inn.glb`) 한 벌,
> **성채는 등급마다 다른 탑**이다 — 보(堡)는 망루 · 진(鎭)은 탑 · 웅진(雄鎭)은 큰 탑.
> 진단 **374/374**(열하나 늘었다) · 눈으로도 봤다(`_demo.html#stands`).
>
> **여기서 밟은 것 넷 — 남은 판에서도 만난다:**
>
> 1. **키 1 규약을 안 지킨 옛 코드가 있다.** 배우는 다 키 1 로 눕는데(`fit`),
>    도형으로 세우던 역참은 **1.73m** · 성채는 **2.64m** 로 서 있었다. `world3d` 의
>    배율이 그 키를 곱할 셈으로 잡혀 있어, GLB 를 키 1 로 세우자 **역참이 절반,
>    성채가 셋에 하나**로 쪼그라들었다. `asset3d.heightMul` 로 그만큼 보탠다
>    (`prop3d.houseScale` 이 집에서 밟은 그 함정이다 — **두 번째다**)
> 2. **모양을 얻고 뜻을 잃을 수 있다.** 도형 역참의 노란 깃발과 성채의 세력 빛깔
>    배너가 **멀리서 알아보는 유일한 표식**이었는데 받아 온 탑·여관에는 깃발이 없다.
>    그대로 갈아 끼우면 성채가 어느 세력인지 화면에서 사라진다 — `markOf`·`addMark`
>    로 깃발만 다시 얹었다. **에셋을 얹을 때 도형이 뜻으로 지고 있던 것을 먼저 센다**
> 3. **같은 파일을 두 층이 나눠 쓰면 헛걸음이 생긴다.** `Inn.glb` 가 마을의 높은 집
>    으로도 서고 있었다 — 들판에서 여관을 보고 역참인 줄 알고 걸어갔다가 남의 집이
>    된다. 마을 목록에서 뺐다(`prop3d` 의 `tower`)
> 4. **판정 층이 이미 아는 것을 화면이 안 물어보고 있었다.** 성채 등급 셋과 지키는
>    세력은 `fort.js` 가 처음부터 알고 있었는데 화면은 늘 같은 보라색 성채를 세웠다.
>    물어보니 **걸어가기 전에 세기와 임자를 가늠**하게 됐다. `factionNameOf` 는 도감
>    일흔을 훑으므로 **키로 캐시해서** 묻는다(`world3d.fortStyle`)
>
> **헤드리스로 3D 를 찍는 요령 하나 더.** 나무가 병목이다 — `world3d.density` 를
> 0.1 로 내리자 한 장이 **몇 분에서 7.6초**로 줄었다. `#stands` 가 그렇게 한다.
> 그리고 **역참·성채는 뒤로 물려도 안 작아진다**(`farBoost` 가 45m 밖 배우를 화면에서
> 같은 크기로 붙든다) — 담으려면 옆으로 벌리고 `world3d.actorH` 를 낮춘다.
>
> ### 사가고 — **짐승 다섯 종이 다 진짜 모델** (2026-08-28)
>
> 잉어는 Quaternius `Koi.glb`(CC0), 까치는 **Google Poly 아카이브의 `Crow`** 다.
> 사용자가 파일을 직접 받아 두었다 — `polygone.art` 가 이 망에서 막혀 있어서다.
>
> **까치만 규칙이 다르다. 손댈 때 조심할 것:**
>
> 1. **이 저장소에서 유일한 CC-BY 다.** 저작자 표시가 **필수**라 문구를
>    `saga-go/assets/ASSET_LICENSES.md` 에 적어 두었다 — 옮기거나 지울 때 같이 옮긴다
> 2. **`.glb` 한 덩이가 아니라 파일 셋**이다(`Mesh_Crow.gltf` + `.bin` + `Tex_Crow.png`).
>    `.gltf` 가 나머지 둘을 **이름으로** 부르므로 셋이 같은 폴더에 그대로 있어야 한다 —
>    하나만 빠지면 **조용히 도형으로 돌아간다**(에러가 안 난다)
> 3. **클립이 없다**(정지 모델). 도형 까치는 코드가 날갯짓을 넣어 주었으니
>    **모양을 얻고 움직임을 내준 맞바꿈**이다
>
> **막힌 주소 — 다음 세션도 만난다:** 이 망은 `raw.githubusercontent.com` 과
> `api.github.com` 만 열린다. `kenney.nl` · `quaternius.com` · `polygone.art` ·
> `blob.polygone.art` 는 전부 막힌다(`000`). **에셋은 GitHub 미러에서만 받을 수 있고,**
> 거기 없는 것은 사용자가 직접 받아 두는 수밖에 없다.
>
> ### 사가고 — **인물 여섯 벌 + 몸짓 옮겨 입히기** (2026-08-28, 에셋에 이어)
>
> 인물이 한 벌뿐이던 것을 여섯 벌로 늘렸다. 그 위에 **차림**과 **빛깔**이 갈린다:
> 몸(`oneOf`, 여섯) · 차림(`pickPieces`, 한 벌에서 예닐곱) · 빛깔(`tintOf`, 일흔에 예순여덟).
> 셋 다 **id 해시**라 같은 사람은 늘 같다.
>
> **몸짓은 옮겨 입힌다.** 받은 다섯 벌은 클립이 0 개다. Quaternius 공식 몸짓 묶음
> (Universal Animation Library)은 **Godot 용 Rigify 뼈대라 0/53 겹쳤다** — 못 쓴다.
> 그래서 `Knight.glb` 의 열둘을 `SkeletonUtils.retargetClip` 으로 옮긴다.
>
> **여기서 밟은 것 — 다른 판에서도 만난다:**
>
> 1. **스킨 메시는 뼈 이름이 같아도 쉬는 자세가 다르다.** 기사와 농부는 뼈가 열여덟
>    겹치는데 팔·다리가 최대 180° 어긋난다. 회전을 그냥 틀면 사지가 뒤틀린다 —
>    `retargetClip` 은 월드 자세를 거쳐 풀어 주므로 맞는다
> 2. **모듈형 몸은 부위 변형을 전부 켜 놓고 온다.** `_Body_1..4` · `_Head_1..5` 가
>    동시에 겹쳐 있다. 무리마다 하나씩만 켜야 하는데, `Knight_1·2·3` 처럼
>    **한 사람의 부품**인 것을 변형으로 오인하면 칼만 남은 사람이 선다 —
>    이름 끝이 **부위**(`_Body`·`_Head`…)인 무리만 고른다
> 3. **`>>` 는 32비트 부호를 탄다.** 해시를 밀어 고를 때 음수가 나와 무리가 통째로
>    꺼졌다(관우가 다리만 남았다). `>>>` 를 쓴다
> 4. **사람이 서는지는 `_demo.html#3d,people` 로 본다.** 마을에서는 사람이 멀거나
>    근처 대상 카드에 가려 눈으로 못 잡는다 — 확인할 자리를 따로 만들어 두었다
>
> ### 사가고 — **물 · 그늘 · 실제 에셋** (2026-08-28, 미니맵에 이어)
>
> 새 `PLAN.md` 를 따라 세 걸음을 더 갔다. 셋 다 진단과 눈으로 확인했고 배포했다.
>
> 1. **물** (`js/water3d.js`) — 수면이 물결치고 하늘을 비추고 해 쪽으로 반짝인다
> 2. **그늘** (`js/ssao3d.js`) — 맞닿은 자리가 어두워진다(SSAO). `post3d` 가 부른다
> 3. **실제 GLB 에셋** (`js/prop3d.js` + `assets/`) — 나무 · 바위 · 풀 · 짐승 셋 ·
>    **집 · 인물**까지. Quaternius CC0 (`saga-go/assets/ASSET_LICENSES.md`)
>
> **사용자가 고른 것:** "스크립트로 그려서 퀄리티가 떨어진다 — 에셋을 써라".
> 받을 수 있는 CC0 건물·사람은 전부 **유럽 중세풍**이라 이 판의 결(한옥·갓·도포)과
> 다르다고 알렸고, 그것을 알고 **"둘 다 얹는다(품질 우선)"** 를 골랐다.
> 되돌리려면 `prop3d.house` = 0 (기와지붕 코드) · `world3d.glb` = 0 (인물 도형).
>
> ### 여기서 밟은 것 — **남은 네 판에서도 만난다**
>
> 1. **인스턴스 덩이는 쓴 만큼만 그려야 한다.** 창고 1600 자리를 크기 0 으로 숨겨
>    두는 방식은 도형이 원뿔·공일 때만 견딘다. 진짜 나무(삼각형 2900)를 넣자
>    한 덩이가 **460만 삼각형**이 되어 화면이 통째로 멎었다. `mesh.count` 를
>    가장 높이 쓴 자리 + 1 로 잡는다
> 2. **GLB 재질(PBR)을 그대로 쓰면 안 된다.** 모델마다 제 재질을 들고 와 셰이더가
>    서른 벌이 된다. **빛깔만 가져와** 그 판이 쓰는 Lambert 로 갈아 끼운다 —
>    예닐곱 벌로 줄고, 옆의 집과 **같은 빛**을 받는다는 이득이 덤이다
> 3. **헤드리스로는 성능을 못 잰다.** `--disable-gpu` 는 SwiftShader(소프트웨어)라
>    진짜 모델을 넣으면 2.5초짜리 화면 한 장에 **몇 분**이 걸린다. 멎은 것처럼
>    보이지만 안 멎었다(오래 기다리면 `frames=10613` 로 끝난다).
>    **그림이 서는지만 헤드리스로 보고, 빠른지는 실제 기기에서 본다**
> 4. **데모의 `#tier=` 는 자동 조정도 멈춰야 한다.** 안 그러면 느린 자리에서 몇 초 뒤
>    스스로 LOW 로 내려앉아, 못박은 등급으로 찍은 줄 알았던 화면이 LOW 화면이 된다
> 5. **셰이더에서 반짝임은 `ndl` 을 곱한다.** 안 곱하면 해를 등진 면까지 반짝여
>    노을에 강이 통째로 주황 불길이 된다
> 6. **애니메이션이 없는 사람 모델은 도형만 못하다.** `modular_men/women` 스물한 벌은
>    클립이 0 개다 — 얹으면 T 자로 선다. **받기 전에 클립 수를 세어 볼 것**
>
> ### 사가고에 남은 것 (2026-08-28 시점 — 우물·장터는 08-29 에 걸었다, 위 절 참고)
>
> **에셋은 다 걸었다** — 나무·바위·풀·집·인물 여섯 벌·짐승 다섯 종·역참·성채 셋.
> 안 건 것은 `Well.glb`(우물)과 `MarketStand_1.glb`(좌판) 둘뿐인데, 둘 다 원본 키가
> 여관의 셋에 하나라 **키 1 로 눕히는 이 창고에서는 한 줄에 섞을 수 없다** —
> 쓰려면 제 자리(우물터·장터)를 따로 만들어야 한다.
>
> 남은 것은 **`PLAN.md` 63절에서 다음 STEP 을 고르는 일**이다. 그리고 여태 못 잰
> **실기기 fps** — 헤드리스는 rAF 가 안 돌아 `#perf` 를 못 쓴다.
>
> ### 사가고 — **미니맵을 얹었다** (`js/minimap.js`, 2026-08-28)
>
> 사용자가 다섯 판의 `PLAN.md` 를 **통째로 새로 썼다**. 사가고의 새 정본은 63절짜리
> 리뉴얼 지시서다(STEP 1~11 이 1차 우선순위). 옛 PLAN 의 PHASE 번호와 **부록 A 는
> 사라졌다** — 옛 문서를 인용한 자리는 다 헛번호다.
>
> 구조부터 봤다(63절이 시키는 순서). **STEP 3(AssetManager)은 이미 `asset3d.js` 가
> 서 있었고, STEP 5~9 도 대부분 본편 PHASE 가 이미 한 것**이었다. 정말 빈 자리는
> **47절 미니맵**(코드에 아예 없었다) 하나였고 그것을 만들었다.
>
> 좌하단 둥근 판. 48m 격자 지형을 바탕에 깔고 조우 대상 · 역참 · 성채 · 주민 ·
> 이름난 자리를 점으로 찍는다. **점거된 역참은 붉게** 바뀌어 걸어갈 이유가 반대로 선다.
> **숨은 자리(굴·사당·폐허)는 가 본 뒤에만** 뜬다 — 미니맵이 탐험을 대신하면 안 되니까.
> 탭하면 180m ↔ 360m, 두 번 탭하면 접힌다. 자세한 것은 `saga-go/README.md` 의 ‘미니맵’ 절.
>
> **여기서 밟은 것 둘:**
>
> 1. **읽는 길에 `Math.random()` 이 하나도 없어야 한다.** 미니맵은 게임이 켜질 때
>    한 번 그려지는데, 그때 난수를 한 번이라도 쓰면 진단의 씨앗 수열이 밀려
>    **미니맵과 무관한 항목이 깨진다**(three 를 실을 때 밟은 그 함정과 같은 꼴).
>    `stationsNear`·`fortsNear`·`npc.posAt`·`rogue.occupied` 는 전부 해시라 괜찮았지만
>    **쓰기 전에 확인했다** — 새 층을 부팅 경로에 붙일 때는 매번 확인할 것
> 2. **폰에서는 좌하단이 통째로 남의 자리다.** ≤780px 에서 독(`#dock`)이 가로 한 줄로
>    눕고 그 위에 근처 대상 카드가 **두 장까지** 쌓인다. 처음에 84px 만 올렸다가
>    카드 뒤에 완전히 가렸다 — `_mobileframe.html` 로 찍어 보고서야 알았다.
>    지금은 좁은 화면에서 **위(지갑 줄 아래)로 옮긴다**
>
> 남은 것: 새 PLAN 의 **STEP 4(실제 GLB 에셋 3~5개)** 는 **파일을 밖에서 받아 와야
> 하는 일**이라 손대지 않았다 — 공개 저장소에 바이너리를 넣는 일이고 라이선스(8절)도
> 사용자가 정할 몫이다. `asset3d.js` 는 **파일만 놓으면 되게** 이미 서 있다.
> 그 밖에 옛 부록 A 가 남겨 둔 **물 · SSAO** 도 그대로 비어 있다.
>
> ### 사가고 — **둘째 지시문이 왔다. 후처리를 얹었다** (`js/post3d.js`, 2026-08-27)
>
> 사용자가 `saga-go 3D graphics up.md` 를 넣어 두었다 — **본편 `PLAN.md` 와 다른
> 둘째 지시문**이다(그래픽만 41절). 폴더마다 정본 하나 규칙대로 **`PLAN.md`
> 부록 A** 로 옮겨 담고 원본은 지웠다. 부록 머리에 절별 상태표를 붙여 두었다.
>
> 41절 중 **대부분은 본편 PHASE 1~13 이 이미 한 것**이었다. 정말 빈 자리는
> 17절 후처리 · 12절 물 · 13절 하늘 · 24절 미니맵 넷. 그중 **후처리를 걸었다**
> (톤매핑 · 블룸 · 색보정 · MSAA 지키기 · 렌더 배율). 자세한 것은
> `saga-go/README.md` 의 '후처리' 절.
>
> **여기서 밟은 함정 둘 — 남은 네 판에서도 만난다:**
>
> 1. **ACES 톤매핑을 그냥 걸면 안 된다.** 이 판들의 색은 앞선 세션들이 **화면색
>    으로 손으로 맞춰 둔 것**이다. 실사 장면용 곡선(ACES)을 한 번 더 걸면 대비가
>    뒤집힌다 — 밤 하늘이 옅게 들리고 그 앞 지붕은 검어서 **마을이 사라졌다.**
>    `NeutralToneMapping`(Khronos PBR Neutral)으로 가면 0.76 아래를 안 건드리고
>    그 위만 굴려 준다: 여태 그림은 그대로 남고 잘리던 빛에만 블룸 자리가 생긴다.
>    ACES 안에는 `/0.6` 이 박혀 있어 노출 1 도 실제로는 1.67 배다
> 2. **색보정은 감마 뒤에 한다.** 선형 값에 `lift = 0.008` 을 더했더니 sRGB 를
>    거치며 **0.075(19/255)** 로 부풀어 밤 화면에 파란 안개가 깔렸다. 숫자는
>    0.008 이라 코드만 봐서는 절대 안 잡힌다 — **두 장을 나란히 찍어** 알았다
>
> three 쪽 사실 둘도 적어 둔다: 코어 빌드(r169)에는 `EffectComposer`·`RenderPass`·
> `UnrealBloomPass` 가 **없다**(손으로 짜야 한다). 그리고 **렌더 타깃에 그릴 때는
> three 가 톤매핑을 안 건다** — 그래서 선형 HDR 이 남아 블룸을 톤매핑 앞에서
> 더할 수 있다(순서가 뒤집히면 밝은 것이 이미 눌려 번질 여력이 없다).
> `antialias: true` 는 캔버스에만 걸리니 렌더 타깃에서는 `rt.samples` 로 되살린다.
>
> ### 사가블로 3단계 — **전투 연출을 얹었다** (`js/fx3d.js`, 2026-08-27)
>
> 사용자가 세 갈래 중 **전투·스킬 연출**을 골랐다. PLAN 51·52·53절이다.
> 2단계까지의 3D 화면에는 타격감이 없었다 — 칼궤적·격파 고리·데미지 숫자가
> `dungeon-view.js` 의 캔버스 2D 안에만 있어 3D 프레임에서 통째로 빠졌다.
>
> **판정(`dungeon.js`)은 한 줄도 안 건드렸다.** 판정이 이미 연출 이벤트 아홉 가지를
> 배열에 흘려 준다(`DG.dungeon.fx()` — hit·elem·get·slash·pop·burst·whirl·ring·trail).
> 그 아홉을 입체로 세웠을 뿐이다. 숫자는 글리프판 한 장 + **Object Pool**(52절),
> 화면 흔들림은 상한 14(51절), 맞은 몸은 번쩍이고(재질 사본을 몸통만 들려 준다),
> 붉은 기운은 막 대신 **바탕과 안개를 물들여** 낸다.
>
> 눈으로 볼 자리도 함께 두었다 — **`_demo.html#fx`** 는 아홉 가지를 한 화면에 세운
> 정지 화면이다(연출은 한 순간이라 자동 전투로는 안 잡힌다). 3D 상태를 왼쪽 아래에
> 한 줄로 적어 둔다: `3D on mesh=452 · fx ready=true drawn=18 num=6/40 …`
>
> ### 사가블로 4단계 — 두 갈래 중 사용자가 고른다
>
> 1. **나머지 절반**: 판정을 방에서 **필드로** 옮긴다(PLAN 4·5·6절). 지금은 세상이
>    넓어 **보이기만** 하고 걸어 나갈 수 없다. `dungeon.js`(59KB)를 손대는 큰 수술이라
>    40절("정상 작동하는 코드는 수정하지 않는다")과 부딪힌다 — 사용자가 정할 몫
> 2. **스킬마다 다른 연출**(10절): 지금은 무예의 **모양**(swing·nova·bolt·dash)까지만
>    갈린다. Fire Slash·Meteor·Chain Lightning 처럼 결마다 그림을 달리하려면 판정이
>    원소를 fx 에 실어 줘야 한다 — 필드 하나 더하는 일이지만 판정을 건드린다
>
> ### 3D 전환을 하며 배운 것 — **남은 세 판에서도 만난다**
>
> 1. **three 를 진단 씨앗보다 먼저 실어야 한다.** three 는 켜지며 `Math.random()` 을
>    **76번** 부른다(직접 셌다). 씨앗 뒤에 실으면 수열이 밀려 **게임과 무관한 항목이
>    깨진다**. `_test.html` 에서만 순서를 바꾼다(본판은 씨앗을 안 간다)
> 2. **`core.tuned`(손잡이)는 사가고에만 있다.** 다른 판에는 없다 — 있으면 쓰고
>    없으면 기본값으로 가게 둘 것
> 3. **three 의 점광은 r155 부터 물리 단위(칸델라)다.** 세기 2 로는 아무것도 안 밝아진다
> 4. **캐시 키에 넣을 것을 빠뜨리면 옛 그림이 남는다.** 사가고에서 젖음(비)과
>    계절에서 두 번 밟았다 — 훑는 조건뿐 아니라 **격자마다의 키에도** 넣어야 한다
> 5. **격자용 해시(`core.hash2`)로 이름·씨앗을 흩으면 안 된다.** 큰 수를 넣으면
>    이웃한 입력이 이웃한 답을 준다 — 주민 둘이 겹쳐 섰고, 들판이 한 판으로 뭉칠 뻔했다
> 6. **가산 혼합은 얇은 빛줄기에만.** 불꽃·흙까지 더하면 밝은 바닥 위에서 흰 뭉치가 된다
> 7. **연출 재질은 안개 밖에 두어야 한다**(`fog: false`). 사가블로 3단계에서 노드
>    열여덟이 제자리에 섰는데(`drawn=18`) 화면은 텅 비었다 — 안개가 숫자와 빛줄기를
>    다 먹었다. 벽·바닥은 안개가 있어야 깊이가 산다. **가려서** 꺼야 한다
> 8. **2D 의 픽셀 값을 3D 월드에 그대로 쓰면 티끌이 된다.** 격파 고리가 원근에 눌려
>    안 보였다 — 고리마다 배율을 얹고 숫자 크기를 곱절로 올렸다
> 9. **값으로 통과해도 눈으로 봐야 한다.** 위 여덟 중 여섯은 진단이 다 통과하는데
>    화면만 틀렸다. 단계마다 폰 폭·900px 로 찍어 볼 것
>
> ### 검증 습관 — 바뀐 것
>
> `_test.html` 을 세 번 돌려 **바이트까지 같은지** 보는 습관은 이제 안 맞는다.
> ① DOM 에 시각이 박혀 있고 ② 씨앗 밖 `Math.random()` 이 **표시값**을 흔든다.
> **PASS/FAIL 은 안 흔들린다** — `RESULT n/n` 으로 보거나 시각을 가리고 견줄 것:
> `... | sed -E 's/[0-9]{2}:[0-9]{2}/HH:MM/g' | md5sum`
>
> **git 은 계정이 엉뚱하게 잡혀 있다.** `git push origin main` 이 403 으로 막힌다
> (Windows 자격 증명에 `smh80602070-spec` 토큰이 들어 있다). 이렇게 넘긴다:
> `git push https://smh8627-jpg@github.com/smh8627-jpg/swbins.git main`
>
> ## 그 앞 — **다섯 판을 3D 로** (축이 생긴 날, 2026-08-27)
>
> 사용자가 판마다 3D 전환 작업 지시문을 써서 넣었다. 폴더마다 **`PLAN.md`** 다.
> 이 문서와 각 `README.md` 는 **지금까지 만든 것**이고, `PLAN.md` 는 **앞으로 갈 곳**이다.
>
> | 판 | 어디로 | 단계 |
> |---|---|---|
> | 사가고 | 걸어 다니는 3D 역사 탐험 RPG · 소문 · 사진 모드 | PHASE 1~13 |
> | 사가블로 | 넓은 필드를 도는 top-down 3D 핵앤슬래시 | Step 1~6 |
> | 사가의숲 | 바이옴이 갈리는 3D 오픈 필드 · chunk | Phase 1~15 |
> | 사가스토리 | 깊이가 있는 3D 횡스크롤 액션 | Phase 1~6 |
> | 사가국지 | 오픈월드 3D 삼국지 — 전략 + 액션 전투 | Phase 1~12 |
>
> 폴더마다 **`CLAUDE.md`** 두 줄을 두었다 — 그 폴더에서 작업하면 자동으로 읽혀
> `PLAN.md`(방향) → `README.md`(현재) → 이 문서(규칙) 로 안내한다.
> 규칙·수치는 겹쳐 쓰지 않았다.
>
> 다섯 계획이 함께 박아 둔 못 셋. 이 저장소가 여태 지켜 온 것과 같은 줄기다 —
> **처음부터 새로 만들지 않는다** · **판정 층과 화면 층을 가른 구조를 지킨다**
> (그래야 `_test.html` 이 계속 돈다) · **원작 에셋은 3D 로 가도 안 가져온다**.
>
> ### 이름과 공개 주소를 정리했다 (일곱 커밋, `f3914c0..412dd38`)
>
> 1. **`b1f18ba`** 프롬프트 다섯을 각 폴더에 담았다
> 2. **`083354f`** 파일 이름을 **`PLAN.md`** 로 통일(원래 이름이 내용을 드러냈다) ·
>    폴더별 `CLAUDE.md` · **메인 페이지에서 소스 링크 제거**('소스 보기' 카드와 푸터 링크)
> 3. **`d1ec378`** **주소창에서 `index.html` 을 지운다** — charset 뒤에 replaceState
>    한 줄(루트 + 다섯 판). `file:` 은 건너뛴다. 문서 기준 경로가 그대로라
>    상대경로도 서비스워커 스코프도 안 바뀐다
> 4. **`8231991`** README 여섯에 **'앞으로' 절** — 각 `PLAN.md` 를 요약해 얹었다
> 5. **`d4599d4`** README 제목을 지금 이름으로 (사가GO 던전 → **사가블로** 등)
> 6. **`ec7198c`** 남아 있던 옛 이름 **55곳을 34개 파일**에서 쓸어냈다 —
>    `_test.html` 제목 · `js`/`css` 머리 주석 · 단독판 배너 · 서버 사관 프롬프트 ·
>    capacitor `appName` · 안드로이드 `strings.xml` · iOS `CFBundleDisplayName`
> 7. **`412dd38`** 도구 페이지 **25개에 `noindex, nofollow`** (게임 본판은 그대로)
>
> ### 이름을 맞추며 **안 건드린 것** — 다시 손대지 말 것
>
> 세이브 키(`deungyong-go/save/…` · `yeoksa-*/save/…`) · capacitor `appId`
> `com.swbin.deungyonggo` · 인증서 CN **`DeungyongGO Local CA`**(폰에 이미 깔려 있다) ·
> 도감 펫 `id`. 넷 다 바꾸면 **진행이 사라지거나 이미 깔린 것과 어긋난다.**
> '사가GO 본편' 은 **'사가고 본편'** 으로 남겼다 — 네 판이 갈라져 나온 원본을 가리키는
> 말이라 그 자리에서 판 이름으로 바꾸면 뜻이 사라진다.
>
> 문서가 사실과 어긋나던 두 줄도 함께 폈다 — `saga-go/README.md` 의 산출물 이름은
> **`dist/사가고.html`** 이고(`build-single.mjs` 의 `OUT_NAME`), 폰 인증서 목록에 뜨는
> 이름은 **`DeungyongGO Local CA`** 다(README 는 '사가GO 로컬 CA' 라고 적고 있었다).
>
> ### 검색에서 가리는 선 (사용자가 고른 것)
>
> **`robots.txt` 는 두지 않았다.** Pages 는 **도메인 루트의 robots.txt 만** 읽는데
> 이 저장소는 `smh8627-jpg.github.io/swbins/` 라는 하위 경로에 얹혀 있어
> `/swbins/robots.txt` 를 크롤러가 보지 않는다. 굳이 막으려면 루트 사이트 저장소
> (`smh8627-jpg.github.io`)의 robots.txt 에 `Disallow: /swbins/_` 를 넣어야 한다.
>
> **도구 페이지는 주소를 알면 여전히 열린다 — 일부러 그렇게 두었다.**
> 배포본을 크롬 헤드리스로 진단해 보는 것이 이 저장소의 검증 습관이라,
> 공개 주소에서 닫아 버리면 그 길이 막힌다. 공개 페이지 어디에도 링크는 없다.
>
> ### 확인한 것
>
> 로컬·배포 둘 다 봤다. **다섯 판 진단이 같다 — 189 · 165 · 143 · 106 · 95.**
> 배포본에서 도구 페이지에는 noindex 가 붙고 게임 본판에는 안 붙었다.
> 루트 페이지에 github 주소가 한 줄도 없고, `.../saga-go/index.html` 로 들어가도
> 주소창은 `.../saga-go/` 로 바뀐다(같은 출처 iframe 으로 `location.pathname` 실측).
>
> ### 3D 전환을 **사가고부터** 시작했다 (사용자 지시)
>
> 읽어 보니 사가고는 앞 칸이 이미 차 있었다 — `PLAN.md` 를 쓰기 **전날**
> 만들어 둔 `world3d.js`·`actor3d.js`·`encounter3d.js` 가 PHASE 2(3D 렌더러 기반)와
> PHASE 4(카메라)를 이미 세워 놓았다. 그래서 새로 지을 첫 칸은 **PHASE 3** 이었다.
>
> **`js/asset3d.js` 를 새로 두었다 — GLB 를 끼울 자리다.**
> 표(`register`)에 한 줄 적으면 그 배우만 GLB 로 서고, **표가 비면 예전 도형 그대로**다.
> 지금 GLB 파일은 한 개도 없으니 **화면은 어제와 똑같다 — 그게 맞다.**
> 이 단계가 하는 일은 "나중에 파일만 놓으면 된다" 를 참으로 만드는 것이다.
>
> - 되돌아가는 길(PLAN 49절) **GLB → `actor3d` 도형 → 캡슐·상자.** 없는 파일 ·
>   깨진 모델 · `file://`(PC 단독판) — 결말이 다 같다. 조용히 도형으로 남는다
> - 받는 동안에도 화면이 안 빈다 — 껍데기를 먼저 주고 안에 도형을 세운 뒤 갈아 끼운다.
>   껍데기를 쥔 `world3d.js` 는 **한 줄도 안 고쳤다**(훅은 `actor3d.build`/`step` 두 곳)
> - 클립 이름은 점수제로 자동 매칭 — `Armature|mixamo.com|Idle`·`Walk.001`·`Anim_Run_F`
>   가 idle/walk/run 아홉 자리에 들어간다. 못 채운 자리는 대타(뛰기 없으면 걷기)
> - `three.iife.js` 에 **GLTFLoader·SkeletonUtils** 를 얹어 다시 묶었다(680→713KB).
>   **DRACO·KTX2 는 안 넣었다** — 런타임에 디코더 파일을 따로 받아야 해서 단독판에서 못 쓴다
> - 손잡이 **`world3d.glb`**(어드민에도 얹었다) 를 0 으로 두면 표가 있어도 도형으로 간다
>
> 확인한 것: **진단 189 → 200/200**, 세 번 돌려 한 줄도 안 다르다 · 본판 콘솔 무오류 ·
> `_demo.html#3d,stat` 이 예전과 같다(tiles 49 · props 111 · actors 20 · meshActors 20) ·
> 단독판 재빌드(스크립트 28개, 1380KB). 실제 GLB 경로도 **최소 glTF 를 손으로 지어 실측**했다 —
> 도형 25부품 → 모델 1부품으로 갈리고, mixer 가 붙고, idle→walk 가 넘어가고,
> 키가 1.000 으로 눕는다(검증용 파일은 지웠다).
>
> ### 이어서 PHASE 5 — **손으로 그린 땅 한 조각** (`js/land.js`)
>
> 이 판의 땅은 여태 **무한한 해시**였다(`world.js` 의 `terrainAt`). 손으로 만든 자리가
> 하나도 없다는 뜻이라, 원점 둘레 **1008m 사방(21×21칸)** 에 한 조각만 그렸다
> (PLAN 46절 "하북의 작은 마을"). **시작 자리(0,0)가 마을 한가운데**라 켜면 곧바로 안이다.
>
> - 지도는 **글자 그림**이다(한 칸 = 48m). `^`산 `T`숲 `~`강 `=`길 `H`마을 `F`논밭 `.`들 ·
>   `C`굴 `S`사당 `R`폐허 `B`다리. 보고 고치면 진단이 곧바로 붙든다 —
>   줄 길이 · 모르는 글자 · **끊긴 길** · 밖으로 나간 명소
> - 길이 **하나로 이어진다**(50칸 한 덩어리). 북쪽 산속 굴 → 마을 → 남쪽 다리
> - 이름난 자리 11곳, 그중 **숨은 곳 셋**이 46절의 "숨겨진 장소 3개"
> - **`terrainAt` 은 그리는 데만 쓴다**(이 저장소에서 확인했다) — 스폰·거리·조우는
>   이 값을 안 본다. 그래서 땅을 얹어도 균형이 안 움직인다(진단이 스폰 목록까지 비교)
> - 밖은 **예전 해시 그대로**. 손잡이 **`land.on`**(어드민에도 얹었다) 0 이면 통째로 잠든다
> - 눈으로 볼 때는 **`_demo.html#3d,land=bridge`** — 이름난 자리로 곧장 옮겨 준다
>
> **이름을 `region` 이 아니라 `land` 로 둔 까닭**: `world.js` 가 이미 576m 격자를
> **구역**(`REGION_SIZE` · `regionName`)이라 부른다. 겹치면 읽는 사람이 헷갈린다.
>
> 3D 에 함께 넣은 것: **논밭**(뙈기 + 두렁 + 허수아비) · **다리 · 굴 입구 · 폐허 · 옛 사당**.
>
> **눈으로 보고서야 안 것 둘** (값으로는 안 잡혔다)
>
> 1. **손으로 그린 강은 지도에 없는 물이다.** 여태 물은 "지도가 깔렸으면 이미
>    칠해져 있다" 며 건너뛰었는데, 이 땅의 강은 지도에 없어 아예 안 보였다.
>    게다가 **원반**으로 깔면 가장자리가 부채꼴로 패어 강이 아니라 물웅덩이 줄이 된다 —
>    손으로 그은 물만 격자를 채우는 **네모**로 깐다
> 2. **작은 마을에 30m 탑이 솟았다.** 원점 둘레를 해시가 번화한 쪽으로 잡아
>    `urbanity > 0.62` 가 나온 탓이다. 이 땅이 번화도를 못박는다(`urban: 0.34`)
>
> 지면 **그림**은 여전히 실제 지도다. 이 땅은 그 위에 **사물로** 드러난다 —
> 지면까지 손으로 칠하는 것은 다음 몫이다.
>
> 확인한 것: **진단 200 → 209/209**. 세 번 돌려 출력이 같다 — 다만 **`_test.html` 의
> DOM 은 시각(`10:05`)이 한 줄 박혀 있어 분이 넘어가면 md5 가 달라진다.**
> 내 변경 탓이 아니고 원래 그렇다(사관 로그 패널). RESULT 줄로 보거나 시각을 가리고 비교할 것.
> 폰 폭 3D 를 여섯 자리에서 찍어 눈으로 봤고, 단독판도 다시 구워진다(스크립트 29개).
>
> ### 이어서 PHASE 6 — **주민 열 사람** (`js/npc.js`)
>
> 마을을 그려 놓아도 **빈 마을**이었다. 여태 지도에 선 사람은 잡거나 설득할 대상뿐이라서다.
> PLAN 47절의 열 사람을 들였다 — 촌장 · 상인 · 노인 · 병사 · 책사 · 약초꾼 · 대장장이 ·
> 떠돌이 무사 · 도적 두목 · 수수께끼의 여행자.
>
> **자리를 시각의 순수 함수로 두었다.** 이것이 이 파일의 뼈대다 — `x += v*dt` 로
> 쌓아 올리지 않는다. "아홉 시에 촌장은 어디 있나" 는 언제 물어도 같은 답이다. 그래서
> 진단이 값으로 붙들고, 창을 덮었다 열어도 제자리에 있고, **LOD 로 걸러도 답이 안 바뀐다**.
>
> - 일과(`spots`) · 구간이 바뀌면 **걸어간다**(1.15m/s, 순간이동 아님)
> - **비가 오면 일과를 접고 집으로**(PLAN 14절) · **여행자는 밤에만**(21~4시)
> - **LOD**(15절) 0~20m 매 프레임 · 20~50m 0.25s · 50~100m 1.5s · 100m 밖 정지.
>   거르는 것은 **다시 재는 주기**지 답이 아니다
> - 가까이(14m) 가면 한 마디 — 역할·시각·천후로 갈린다. **소문(33절)이 붙을 자리**다
> - **판정에 한 칸도 안 닿는다.** 스폰이 아니라 잡히지 않고 세이브에도 안 남는다.
>   **눌러서 여는 창은 일부러 안 만들었다** — `world.js` 의 조우 판정에 손대야 하는데
>   그건 PHASE 8 이 사건과 함께 들고 올 몫이다
> - 3D 와 2D **둘 다** 그린다(`world3d.syncActors` · `world.drawObjects` 의 `drawNpc`).
>   2D 에서는 등급 고리를 안 두르고 이름표를 흐리게 단다 — **스폰과 헷갈리면 안 된다**
>
> **밟은 함정 — `core.hash2` 로 사람을 흩으면 안 된다.**
> 그것은 격자 좌표(작은 정수)용이다. 이름에서 뽑은 다섯 자리 수를 넣으면
> **이웃한 입력이 이웃한 답**을 준다 — 노인(38665)·떠돌이 무사(29976)가
> 0.0823·0.0899 를 받아 다리 위 **같은 자리에 겹쳐 섰다**(진단이 잡았다).
> `npc.js` 안에 자리를 흩는 `mix()` 를 따로 두었다. **다른 판에서도 같은 실수를 할 수 있다.**
>
> ### 검증 습관 하나가 더는 그대로 안 통한다 — 읽고 넘어갈 것
>
> `_test.html` 을 세 번 돌려 **바이트까지 같은지** 보는 습관이 있는데, 지금은 안 맞는다.
> **내 변경 탓이 아니다** — 배포본(PHASE 5)을 네 번 돌려 확인했다. 흔들리는 것은 둘이다.
>
> 1. DOM 에 **시각(`10:05`)이 박혀 있다**(사관 로그 패널). 분이 넘어가면 md5 가 달라진다
> 2. **씨앗 밖의 `Math.random()`** 이 열다섯 줄쯤의 **표시값**을 흔든다
>    (보급 `🍖 +2/+3` · 한 방 피해 · 거리 m). **PASS/FAIL 은 안 흔들린다**
>
> 그러니 `RESULT n/n` 줄로 보거나, 시각을 가리고 PASS/FAIL 줄만 견준다:
> `... | sed -E 's/[0-9]{2}:[0-9]{2}/HH:MM/g' | md5sum`
>
> 확인한 것: **진단 209 → 220/220** · 본판 콘솔 무오류 · 단독판 재빌드(스크립트 30개) ·
> 폰 폭에서 3D 마을·다리·밤 남문과 **2D 캔버스**(`#flat`)를 눈으로 봤다.
>
> ### 이어서 PHASE 7 — **들·강의 짐승 다섯 종** (`js/animal.js`)
>
> 이 판에 짐승은 이미 있었다 — 다만 **잡는 대상**(스폰)이다. 여기 다섯은 그것과 아주 다르다:
> **잡히지 않고 · 도감에 안 오르고 · 세이브에 한 칸도 안 남는다.** 대신 먹고 · 돌고 ·
> 도망가고 · 쫓고 · 날아오른다. PLAN 39절이 바라는 "**발견하는** 재미" 다.
>
> | | 몸짓 | 알아채는 거리 | 사는 곳 |
> |---|---|---|---|
> | 사슴 | 물러난다 | 30m | 논밭↔숲 |
> | 늑대 | 다가온다(6m 는 남긴다) | 낮 46m · **밤 70m** | 산등성이↔굴 |
> | 까치 | 날아오른다(최대 7m) | 18m | 숲↔사당 · 마을↔북문 |
> | 잉어 | 흩어지며 수면을 튄다 | 12m | 다리↔강나루 |
> | 소 | 꿈쩍 않는다 | — | 논밭 |
>
> 뼈대는 주민과 같되 **한 겹이 더 있다**. `posAt()` 은 시각만 보는 바탕 자리고,
> `react()` 는 **바탕 자리와 내 자리**에서 나오는 반응이다. 반응도 순수 함수라
> "사슴 20m 앞에 서면 어디로 물러나나" 를 진단이 값으로 묻는다 —
> "쫓기는 중" 같은 상태를 안 들고 있으므로 LOD 로 걸러도 답이 안 바뀐다.
> **소를 넣은 까닭: 다 도망가면 도망이 눈에 안 띈다.**
>
> **눈으로 보고서야 안 것 둘** — 값으로는 다 통과하고 있었다
>
> 1. **물속 잉어가 통째로 안 보였다.** 수면이 반투명(0.72)인데도 `depthWrite` 가 켜져
>    있어 **깊이 버퍼가 먼저 잘라냈다.** 수면 재질을 `depthWrite:false` 로
> 2. 그래도 **잠긴 채 흩어지기만 하면 물 위에서는 아무 일도 안 일어난 것**으로 보인다 →
>    잠김을 얕게(-0.25m) 하고 놀라면 **수면을 튀어오르게** 했다
>
> **또 하나 — 데모의 `#at=` 이 주민 시각만 못박고 짐승은 실제 시계를 보고 있었다.**
> 그래서 "다리에 잉어가 없다" 는 엉뚱한 결론에 잠깐 빠졌다. 시각을 못박는 자리는
> **새 축이 생길 때마다 같이 늘려야 한다**(지금은 world3d · npc · animal 셋이다).
>
> `sprite.beastFormOf`·`beastColorOf` 가 `ref.form`·`ref.color` 를 먼저 보게 했다 —
> 도감에 없는 짐승이 제 겉모습을 들고 오기 위해서다. 도감 펫에는 그 두 키가 **없으므로**
> 여태 그림은 한 획도 안 바뀐다. `sprite.js` 는 이미 다섯 판이 갈라져 있어 이 판만 고쳤다
> (`data.js` 만 다섯이 같다 — 그건 그대로 두었다).
>
> 진단 **220 → 232/232** · 본판 콘솔 무오류 · 단독판 재빌드(스크립트 31개) ·
> 폰 폭에서 숲의 사슴 · 논의 소 · 밤 산의 늑대 · 강물 속 잉어 · **2D 캔버스**를 눈으로 봤다.
> 데모 `#stat` 에 사람·짐승 수를 얹었다 — 안 보일 때 **없는 것인지 멀리 있는 것인지**를
> 값으로 가른다(이번에 그게 없어서 헤맸다).
>
> ### 이어서 PHASE 8 — **걷다가 만나는 사건 열 가지** (`js/event.js`)
>
> `PLAN.md` 가 이 판의 차별점으로 못박아 둔 한 줄이 **"걷다가 사건을 만난다"** 다.
> 길 위의 상인 · 도적의 습격 · 늑대 무리 · 부상당한 병사 · 마을의 부탁 · 사라진 아이 ·
> 고대 비문(사당) · 보물 지도 조각(폐허) · 희귀 약초 · 적군 정찰병.
> 사건마다 **고르는 것이 둘·셋**이고 결과가 갈린다(35절).
>
> **적 셋**(46절)은 따로 세우지 않고 사건 안에서 만난다 — 산적 120 · 늑대 무리 90 ·
> 정찰병 170. 이길 가망은 `hero.partyPower` 와 견주되 **0.12~0.88 로 눌러 둔다**
> (확실해지면 고를 뜻이 사라진다). **싸움에 화면은 아직 없다** — PHASE 9 몫이다.
>
> **여기서 규칙이 하나 바뀐다 — 이 축은 판정에 닿는다.**
> 땅·주민·짐승은 "세이브에 한 칸도 안 남는다" 를 지켰지만 사건은 금과 공적이 오간다.
> 대신 **닿는 자리를 한 군데로 좁혔다**:
>
> - `contextAt`·`candidates`·`resolve` — 순수 함수. 세이브를 **읽기만** 한다
> - **`apply` 하나에서만 세이브가 바뀐다.** 없는 것은 빼앗지 않는다(금이 모자라면 0 까지)
> - 진단이 그 둘을 따로 본다 — 값 함수는 세이브 무변경, `apply` 는 전후를 한 칸씩 견줌
> - 기록은 `core.log` 로 남긴다(36절). 다시 펼쳐 보는 **발견 목록은 PHASE 11 몫**
> - 때는 **걸은 거리**로 잰다(기본 420m 마다 55%) — 서 있으면 아무 일도 안 생긴다
>
> **밟은 함정 — 밀린 몫을 다 주면 안 되는 자리가 있다.**
> 걷기 보급(`game.js` tickSupply)은 밀린 거리를 `while` 로 다 준다. 사건에 같은 짓을
> 하면 **옛 세이브를 열거나 배속으로 걸을 때 카드가 우수수 뜬다.** 데모(거리 12,840m)를
> 열자마자 실제로 터져서, 부탁한 사건 대신 엉뚱한 카드가 찍혔다. 두 걸음 넘게 벌어지면
> **한 번에 삼킨다**(`p.eventMark = p.distance`). 데모도 거리를 세우는 자리에서
> `eventMark` 를 같이 맞춘다.
>
> 진단 **232 → 245/245** · 본판 콘솔 무오류 · 단독판 재빌드(스크립트 32개) ·
> 사건 카드와 **고른 뒤의 결과 카드**를 눈으로 봤다(`#event=map_scrap,pick=dig` →
> 금 +180 · 공적 +25 · "새로운 기록 — 강 건너 폐허의 지도 조각").
>
> **헤드리스 폭은 500px 이 하한이라** 390px 로 찍으면 조우 카드가 잘린다 —
> 카드를 볼 때는 900px 로 찍었다. [[saga-headless-verify]] 에 적힌 그 함정이다.
>
> ### 이어서 PHASE 9 — **전투** (`js/battle3d.js` + `duel.js` 재사용)
>
> PLAN 22절의 첫 줄이 **"전투는 기존 시스템이 있다면 최대한 재사용한다"** 다.
> 그리고 이 판에는 이미 `duel.js` 가 있었다 — 속공·필살·회피·적 AI(강타 예고)·피해·
> 죽음·성과. 22절의 MVP 목록(Move/Attack/Skill/Dodge/Enemy AI/Damage/Death/Reward)과
> 사실상 같다. **그래서 새로 짓지 않고 이어 붙였다.**
>
> - **사건의 싸움이 주사위 하나에서 진짜 교전으로 올라갔다.** "맞선다" 를 고르면
>   무대가 열리고 **기세를 다 깎았는지**가 승패다. 무대를 안 거치면(`event.duel`=0)
>   예전 길 그대로 — `fightRoll(foe, roll, mine, forced)` 의 `forced` 가 그 갈림이다
> - 결과 화면에 `12타 · 필살 2 · 회피 3/4 · 27초` 가 붙는다. 한 대도 못 때리고
>   물러났으면 **싸움 자체가 없던 것**으로 둔다(빈손 도주에 패배의 벌까지 얹지 않는다)
>
> **3D 연출**(23·24절) — `duel.js` 는 **신호만 던진다**(`duel:open`·`duel:fx`·`duel:close`,
> 네 줄). `battle3d.js` 가 듣는다. **저쪽은 이쪽이 있는지 모르고, 없어도 교전은 돈다.**
>
> - 교전 시작 → 카메라 줌인(`camAim(..., battle)` 거리 ×0.62) · 공격 → 흔들림 ·
>   필살/강타 → **hit-stop**(상한 180ms) · 알갱이 검기·불꽃·흙먼지·바람
> - **흔들림은 따라온 자리에 얹기만 한다** — `camPos` 자체를 흔들면 그게 다음 프레임의
>   출발점이 되어 카메라가 조금씩 밀려난다
> - hit-stop 동안 **그린 것을 한 번 더 낸다** — 아무것도 안 그리면 검게 깜빡인다
> - **Particle Pool**(24절의 못박은 요구) 48개를 미리 만들어 꺼내 쓰고 되돌린다.
>   창고가 차면 제일 시든 것을 뺏는다 — 그래서 아무리 오래 싸워도 48을 안 넘는다
>
> **눈으로 보고서야 안 것 — 가산 혼합은 아무 데나 쓰면 안 된다.**
> 불꽃·흙먼지까지 `AdditiveBlending` 으로 두었더니 **밝은 지면 위에서 죄다 흰 뭉치**가
> 됐다(값으로는 `alive=48/48` 로 멀쩡했다). 가산은 검기·바람처럼 **얇은 빛줄기**에만
> 어울린다. 불꽃·흙은 일반 혼합으로 바꾸자 그제야 주황 불꽃으로 보였다.
>
> **헤드리스 함정 하나 더 — 끝없는 `setInterval` 은 스크린샷을 못 찍는다.**
> 가상 시간(`--virtual-time-budget`)에서는 타이머가 살아 있으면 예산이 영영 안 닳아
> 렌더러가 그대로 죽는다("Abnormal renderer termination"). 데모의 연출 손잡이는
> **40번 치고 멈춘다**. 그리고 **찍는 때를 맞춰야 한다** — 알갱이는 0.3~0.6초면
> 스러지므로 예산을 3.2초쯤으로 잡아야 터지는 순간이 잡힌다.
>
> 진단 **245 → 253/253** · 본판 콘솔 무오류 · 단독판 재빌드(스크립트 33개) ·
> 사건 → 교전 무대(도적의 습격 · 이순신 vs 산적 · 기세 840)와 불꽃 연출을 눈으로 봤다.
>
> ### 이어서 PHASE 10 — **시간·날씨** (`js/sky3d.js`)
>
> **PHASE 9 처럼 "이미 있는 것" 부터 셌다.** 천후(`weather.js`)·시각대 조명
> (`lightingAt`)·비 오면 집에 가는 주민·젖은 날 사건 가중치는 이미 있었다.
> **빈자리는 셋이었다.**
>
> **① 깊은 밤이 없었다.** 시간대가 다섯이었다(day/dawn/dusk/twilight/night).
> PLAN 20절이 콕 집은 **02:00 Deep Night** 을 더해 여섯. 자정~네 시는 한 겹 더 어둡고
> 등롱이 최대다. **23시는 그대로 `night`** 이라 여태 값이 안 흔들린다.
> morning/noon 은 **안 나눴다** — `day` 안에서 조명이 이미 이어서 변하므로 이름만 는다.
>
> **② 화면에 아무것도 안 내렸다.** 비 오는 날이 그냥 어둡고 뿌연 날이었다.
> 비 150개(26m/s, 떨어지는 쪽으로 눕는다) · 눈 110개(2.6m/s, 좌우로 흔들린다) ·
> 바람 34개. **안개는 알갱이를 안 만든다** — 이미 `fog` 로 시야를 좁히고 있어 두 겹이 된다.
>
> - Particle Pool 이되 **`battle3d` 와 창고를 따로 둔다** — 한 창고를 나눠 쓰면
>   필살을 지를 때마다 빗줄기가 끊긴다
> - 알갱이는 **플레이어를 따라다니는 22m 상자** 안에서만 산다. 상자를 **카메라가 아니라
>   플레이어**에 붙였다 — 카메라에 붙이면 시점을 돌릴 때 비가 통째로 미끄러진다
> - 천후가 바뀌면 남는 알갱이를 **지우지 않고 숨긴다**(세 시간마다 바뀐다)
>
> **③ 천후가 세상을 안 바꿨다**(21절 "단순한 장식이 아니게 한다")
>
> - **비·눈 → 강물이 분다**(수면 +55cm · 흙탕색 · 조금 넓어진다). **갈대는 잠긴다** —
>   물만 올리면 갈대가 물 위에 떠 있다. 손잡이 `world3d.wetRiver`.
>   **젖음을 사물 캐시 키에 넣어야 한다** — 안 넣으면 비가 그쳐도 강이 분 채로 남는다
> - **안개 → 숨은 곳이 드러난다.** 표식 위에서만 나는 사건(비문·지도 조각) 가중치 ×2.4
> - **비에만 나는 사건** 하나 — **불어난 여울**. 그래서 사건은 이제 **열하나**다
>   (46절의 열은 최소치로 읽었다)
>
> **계절(37절)은 안 했다** — PLAN 이 스스로 "MVP 이후 구현한다" 고 미뤄 둔 절이다.
>
> **진단을 고치며 알게 된 것 — 새벽·노을은 창이 한 시간 남짓이다.**
> 해 고도로 가르는데 `alt` 0.04~0.30 사이가 **6:03~7:10 · 16:50~17:57** 뿐이다.
> 정시로만 훑으면 통째로 건너뛴다(처음에 그렇게 짜서 실패했다). 시각을 표본으로
> 볼 때는 **30분 단위로** 잡을 것.
>
> 진단 **253 → 262/262** · 본판 콘솔 무오류 · 단독판 재빌드(스크립트 34개) ·
> 눈 오는 마을 · 비 오는 마을 · **비에 불어난 다리**를 눈으로 봤다.
> 데모에 `#wx=rain` 을 두었고 `#stat` 에 `sky=rain→rain 150/150` 이 뜬다.
>
> ### 이어서 PHASE 11 — **발견·도감·기록** (`js/codex.js`)
>
> PHASE 5~10 으로 볼 것이 늘었는데 **본 것이 아무 데도 안 남았다** — 사건이 `core.log`
> 에 흘린 한 줄은 곧 밀려 사라졌다. PLAN 12·13·36·44절이 가리키는 자리다.
>
> **목록을 새로 만들지 않았다.** 지역은 `land.places()`, 사람은 `npc.PEOPLE`, 생물은
> `animal.KINDS`, 사건은 `event.EVENTS`, 역사는 사건의 `record` 칸에서 온다.
> `codex.js` 는 **도장을 찍고 세는 일만** 한다 — 목록이 늘면 분모가 저절로 는다.
>
> - 갈래 다섯 + **기존 인물 도감**을 같이 센다(13절 "적극 활용한다" — 두 숫자를 안 만든다)
> - 지나가면 저절로 찍힌다(자리 34m · 짐승 45m · 사람 14m). **`npc.js`·`animal.js` 는
>   발견을 모른다** — `codex.tick()` 이 물어보는 쪽이라, 꺼도 저쪽이 한 줄도 안 바뀐다
> - **발견은 보상이 아니다** — 금도 공적도 안 준다(진단이 그것을 못박는다)
> - 화면은 **`기록` 시트 맨 위**에 격자로 붙였다. 새 시트를 안 만든 까닭: 기록과 발견은
>   "지나온 것" 한 갈래고, 독(dock)에 칸을 늘리면 좁은 화면에서 글자가 뭉갠다
> - 세이브 칸 `save.codex`. **옛 세이브에 칸이 없으면 그 자리에서 만든다**(44절)
>
> 진단 **262 → 271/271** · 콘솔 무오류 · 단독판 재빌드 · 발견 격자를 눈으로 봤다
> (`_demo.html#codex` — 10/150 · 7% · 숨은 곳 2군데 남음).
>
> ### PLAN 다섯의 토큰 절약 문구 (사용자 물음)
>
> 세어 보니 **다섯 판 모두 토큰 절약 원칙은 들어 있다.** 다만 `59. 응답 절약`
> (장문 출력 금지) 절은 **사가고에만** 있다. 넷에도 넣을지는 사용자가 정할 몫.
> 다만 문구가 실제 사용량을 줄이지는 않는다 — 줄이는 것은 **읽는 방식**이다.
> 1600줄짜리 PLAN 은 `grep '^#'` 로 목차만 뽑고 필요한 절만 `sed` 로 읽는다.
>
> ### 이어서 PHASE 12 — **최적화** (`js/perf.js`)
>
> **이미 있는 것부터 셌다.** LOD(주민·짐승) · Particle Pool(전투·하늘) · 밀도 손잡이 ·
> 줌에 따른 그림자 끄기 · 서비스워커 캐시는 이미 있었다. **빈자리는 하나**였다 —
> `tickFps` 가 **재기만 하고 아무 일도 안 했다**. 품질은 사람이 어드민에서 손으로
> 내려야 했는데, 폰에서 버거운 사람이 그걸 알 리 없다.
>
> 등급 셋(27절 그대로) HIGH/MEDIUM/LOW. **내려가는 것은 빠르고(26fps 2초)
> 올라가는 것은 느리다(48fps 12초)** — 경계에서 오르내리면 그 자체가 더 나쁘다.
>
> - **손잡이를 덮어쓰지 않고 곱한다.** 어드민에서 잡은 값이 살아 있다.
>   그래서 **세이브에 아무것도 안 남는다**(등급은 이 창의 사정이다)
> - `decide()` 는 순수 함수 — 진단이 화면 없이 "18fps 3초면 어디로" 를 값으로 본다
> - **`DG_NO_DRAW` 면 재지 않는다.** 진단은 rAF 가 거의 안 돌아 늘 버거워 보이는데,
>   거기서 등급이 내려가면 **뒤따르는 항목이 다른 밀도로 돈다**(밟을 뻔했다)
>
> **같이 고친 진짜 낭비 — 사물 격자를 매 프레임 다 훑고 있었다.**
> 반경 260m = 11×11 = 121칸. 한 걸음(8m/s)에 격자(48m)를 넘는 데 6초가 걸리니
> 그 사이 360번쯤 같은 답을 다시 냈다. 이제 **훑을 까닭이 있을 때만** 훑는다
> (격자를 넘었거나 · 지도가 붙거나 떨어졌거나 · 비가 오거나 그쳤거나 · 품질이 바뀜).
> **데모에서 같은 시간에 도는 프레임이 150 → 2,092.** 사물 수(82)와 그림은 그대로다.
>
> **안 한 것과 그 까닭**: GLB 최적화(29절)·Loading Manager(31절)는 **GLB 가 없어 할
> 것이 없다**. 캐시(32절)는 `sw.js` 가 이미 하고 PLAN 이 "기존 구조 우선" 이라 했다.
> InstancedMesh(16절)는 격자가 들락날락하는 지금 구조에서 전면 전환이라 미뤘다 —
> 하려면 전역 인스턴스 버퍼를 따로 들어야 한다.
>
> 진단 **271 → 279/279** · 콘솔 무오류 · 단독판 재빌드 · 마을 그림 그대로.
>
> ### 이어서 PHASE 13 — **모바일** (마지막 칸)
>
> 등급 셋은 PHASE 12 가 만들었으니 여기서 채운 것은 둘이다.
>
> **① 기기를 보고 시작 등급을 고른다**(27절). 프레임만 재면 **처음 몇 초가 늘
> 버벅인다** — 폰에서도 HIGH 로 켜고 두 초를 버틴 뒤에야 내려간다. 켤 때 한 번
> 코어·메모리·**그릴 픽셀 수**·터치 여부를 본다(`perf.score()` 는 순수 함수).
>
> - **모르는 값은 깎지 않는다.** `deviceMemory` 는 크롬 계열만 준다 — 모른다고 낮게
>   보면 **아이폰 사파리가 늘 LOW 로 시작**한다. 이게 이번에 가장 조심한 자리다
> - 같은 사양이어도 **터치 기기는 한 단 낮게** · 레티나처럼 픽셀이 많으면 깎는다
> - 손잡이 `perf.startTier` 로 못박을 수 있다
>
> **② 끌면 카메라가 돈다**(7·26절). 한 손가락·마우스로 가로로 끌면 나를 축으로 돈다.
> **탭과 구분한다** — 10px 을 넘게 끌어야 돌기 시작하고 그 뒤의 클릭은 탭으로 안 친다.
> **안 걸러내면 시점을 돌릴 때마다 그쪽으로 걸어간다**(터치에서 특히 티가 난다).
> **조우 무대에서는 안 돈다** — 무대는 어느 시점에서 열었든 같은 그림이어야 한다.
>
> **이미 있어 다시 안 만든 것**: 터치 이동(탭 걷기) · pinch zoom · 모바일 HUD ·
> 세로 화면 시야각 보정 · FPS 자동 품질. **길게 누르기**(26절)는 이 판에 쓸 자리가
> 없어 안 넣었다.
>
> 진단 **279 → 286/286** · 콘솔 무오류 · 단독판 재빌드 · `#turn=120` 으로 돌린 그림 확인.
>
> ### PHASE 13 뒤 — **주민·짐승을 눌러서 여는 창** (`js/talk.js`)
>
> PHASE 8부터 미뤄 온 자리다. 안 만든 까닭은 `world.js` 의 **조우 판정에 손대야
> 했기 때문**이다 — 잡고 설득하는 그 판정은 이 판의 심장이다.
>
> **넣은 방식이 요점이다: 스폰·역참·성채가 아무것도 안 잡혔을 때만 본다.**
> `onClick` 의 **`!best` 안쪽**에 한 겹을 끼웠다 — 여태 "빈 땅" 으로 흘러가 그쪽으로
> 걸어가던 자리다. 그래서 눌러서 잡히는 것들의 순서는 한 치도 안 바뀐다.
> 진단이 그것부터 못박는다(`W.nearest()` 로 코앞의 스폰이 그대로 잡히는지).
>
> - 주민 카드 — 초상·성격·지금 어디에 있고 어디로 가는 길인지·한 마디
> - 짐승 카드 — 습성 · **지금 나를 알아챘는지**(alarm) · 거리
> - **잡히지도 설득되지도 않는다.** 미니게임 없음, 버튼은 '닫는다' 뿐.
>   세이브가 바뀌는 것은 **발견 도장** 하나뿐이고 그것도 보상이 아니다
> - 멀면 창을 안 열고 걸어간다. **짐승은 26m**(주민 18m) — 다가가면 도망가는 것이
>   있어 코앞까지 가야 열리면 영영 못 본다
> - 조사를 **받침을 보고 붙인다**(`josaRo`). "마을 한가운데 에 있다" 로 어긋난 것을
>   눈으로 보고 고쳤다 — 자리가 열한 가지라 손으로 적으면 하나만 늘려도 어긋난다
>   (사가국지에서 '사가국지이·사가국지을' 로 밟았던 그 함정)
>
> 진단 **286 → 293/293** · 콘솔 무오류 · 단독판 재빌드 · 카드 둘을 눈으로 봤다.
>
> ### 지면 그림도 이 땅에 맞췄다 (`world3d.landTexture`)
>
> 여태 이 땅은 **사물로만** 드러났다 — 판교 지도 위에 하북 마을의 기와집이 서 있는
> 꼴이었다. 지도 타일을 캔버스에 굽고 그 위에 **48m 격자마다 제 색을 덧칠**한다.
> 지도가 안 왔으면 색만 칠하므로 **오프라인에서도 이 땅은 이 땅**이다.
> 손잡이 `world3d.landPaint`(0 이면 예전) · `world3d.landPaintAlpha`.
>
> **눈으로 보고 고친 것 둘** — 값으로는 다 통과하고 있었다
>
> 1. **칸 경계에 실선이 떴다.** 가장자리를 흐리려고 칸마다 알파를 크게 달리했더니
>    그 차이가 선으로 보였다. 게다가 칸을 1px 씩 겹쳐 칠해 그 줄만 짙어졌다 →
>    안쪽은 거의 같은 진하기로 두고 바깥 한 줄만 옅게, **겹치지 않게** 픽셀로 자른다
> 2. **들판이 바둑판이 됐다.** 밋밋함을 없애려 칸 색을 0.16 만큼 흔들었는데 너무 컸다 →
>    **있는 줄 모를 만큼**(0.06)만 흔든다
>
> 진단 **293 → 295/295** · 콘솔 무오류 · 단독판 재빌드 ·
> 마을·남문·다리를 눈으로 봤다(강이 동서로 흐르고 다리가 건너고 양옆이 논밭이다).
>
> ### 계절도 넣었다 (`js/season.js`, PLAN 37절)
>
> 37절은 계절을 적어 두고 **"단, MVP 이후 구현한다"** 로 스스로 미뤄 둔 절이다.
> PHASE 1~13 이 다 찼으니 이제 그 자리다. **계절도 시각의 순수 함수** — 달만 본다.
>
> 37절의 다섯 갈래를 그대로 맡는다: **식물**(잎·논밭·들 색) · **날씨**(겨울 눈 ×2.6 ·
> 여름 비 ×1.9 · 가을 안개 ×1.4) · **NPC 의상**(겨울에 짙고 두껍다) ·
> **이벤트**(약초 봄 ×1.8 · 겨울 ×0.25) · **분위기**.
>
> - **길·마을·산·강은 사철 같다.** 잎과 곡식만 철을 탄다 — 돌과 물까지 물들면 딴 세상이 된다
> - **다섯 중 천후 하나만 판정에 닿는다**(천후가 무엇이 잘 나오는지를 정한다).
>   그래서 `season.weather` 로 **그 갈래만 따로** 끌 수 있게 두었다.
>   뽑는 주사위(`h`)는 그대로라 **같은 시각이면 여전히 같은 천후**다
>
> **밟은 함정 — 캐시 키에 계절을 넣어야 한다.**
> 훑는 조건(`propScan`)에만 넣었더니 다시 훑기는 하는데 **이미 세워 둔 격자를
> 그대로 되썼다** — 가을이 됐는데 나무 몇 그루가 초록으로 남았다. **격자마다의 키
> (`propMeshes[key]`)에도** 넣어야 한다. 젖음(비)에서 이미 겪은 그 일인데 또 밟았다.
>
> 진단 **295 → 304/304** · 콘솔 무오류 · 단독판 재빌드 ·
> 가을 숲(잎이 다 물든다)과 눈 오는 겨울 남문(논밭이 잿빛)을 눈으로 봤다.
>
> ### InstancedMesh 도 넣었다 (PLAN 16절)
>
> PHASE 12 에서 "격자가 들락날락하는 구조라 전역 인스턴스 버퍼가 필요하다" 며 미뤘던
> 자리다. 그 버퍼를 만들었다 — 종류마다 덩이 하나를 두고 **자리(slot)만 빌려 준다.**
> 격자가 사라지면 `instDrop(key)` 로 자리를 돌려받는다(크기 0 행렬로 숨긴다).
>
> - 묶는 것은 **나무·돌·풀·갈대** 넷. **집·탑·다리·폐허는 안 묶는다** — 크기와 색이
>   제각각이라 한 덩이로 못 모으고 수도 적어 이득이 없다(16절도 "나무·돌·풀·꽃")
> - **잎 색은 계절이 바꾸므로 색이 덩이 이름의 일부**다. 가을이 되면 새 덩이가 하나
>   생기고 옛 덩이는 빈 채로 남는다(넷뿐이라 그냥 둔다)
> - 창고가 다 차면 **옛 길로 흘러간다**(제 Mesh 를 만든다) — 넘쳐서 안 보이는 일은 없다
> - `frustumCulled = false` — 자리가 온 세상에 흩어져 있어 상자 하나로 못 자른다
>
> **숲에서 그리는 것이 782 → 546 개**(자리 319개가 덩이 넷으로 묶였다).
> 데모에 **`#noinst`** 를 두어 켜고 끄고 견줄 수 있게 했다.
>
> 진단 **304 → 307/307** · 콘솔 무오류 · 단독판 재빌드 · 숲 그림 그대로.
>
> ### 사가블로 3D 전환을 **시작했다** (`js/dungeon3d.js`, 1단계)
>
> PLAN 3절이 "게임 로직과 렌더링을 분리한다" 고 하는데 **이 판은 처음부터 그렇게
> 지어져 있었다** — `dungeon.js`(판정) · `dungeon-view.js`(화면). 그래서 판정에는
> 한 줄도 안 대고 화면 층 **옆에** 3D 를 세웠다. 사가고와 같은 길이다.
>
> - 바닥 · 벽 넷(뒤는 높고 앞은 낮다) · 상자·우물·사당·문 · 나 · 적 · 전리품 · 기공파
> - 카메라는 8절대로 **3/4 top-down**, **회전 없음**. 플레이어를 따라가되 **절반만**
>   끌린다 — 온전히 따라가면 벽에 붙었을 때 방 밖 검은 여백이 화면을 덮는다
> - 조명은 37절의 *Stylized Dark Fantasy* — 어둡게 깔고 **횃불로 도려낸다**.
>   깊을수록 어둡고 시야가 좁으며 **보스 방은 붉다**
> - 조작판·입력·시트는 **그대로 DOM**. 3D 가 켜지면 캔버스 그리기만 건너뛴다
> - 손잡이 `dg3d.on`·`zoom`·`tilt`·`dark` — **이 판엔 어드민이 없어** 콘솔에서
>   `DG.dungeon3d.set('dg3d.dark', 0.2)` 로 두드린다
>
> **밟은 함정 셋 — 남은 세 판에서도 만난다**
>
> 1. **three 를 진단 씨앗보다 먼저 실어야 한다.** three 는 켜지면서 UUID 를 만드느라
>    `Math.random()` 을 **76번** 부른다(직접 세어 봤다). 씨앗을 갈아 둔 뒤에 실으면
>    그 76번이 수열을 밀어 **게임과 무관한 항목이 깨진다** — 실제로 원소 피해 항목이
>    넘어졌다. `_test.html` 에서만 순서를 바꾸면 된다(본판은 씨앗을 안 간다)
> 2. **`core.tuned` 는 사가고에만 있다.** 다른 판의 `core.js` 에는 손잡이가 없다 —
>    있으면 쓰고 없으면 기본값으로 가게 둘 것
> 3. **three 의 점광은 r155 부터 물리 단위(칸델라)다.** 예전 감각으로 세기 2 를 주면
>    **아무것도 안 밝아진다**. 던전 횃불 세기가 천 단위인 것은 오타가 아니다
>    (처음에 2 로 두었다가 새까만 화면을 찍었다)
>
> 진단 **165 → 174/174** · 콘솔 무오류 · 단독판 재빌드 · 횃불이 방을 도려내고
> 적이 줄지어 오는 그림을 눈으로 봤다.
>
> ### 사가블로 2단계 — **방 밖으로 세상이 이어진다** (`js/field3d.js`)
>
> 1단계의 방은 **허공에 뜬 상자**였다 — 벽 너머가 새까맸다. PLAN 4·5·6절이 바라는
> 넓은 지형·숲·폐허·바위·절벽·길·강·동굴 입구·제단·캠프를 방 둘레에 세운다.
>
> - **씨앗 기반**(5절) — 순수 함수라 같은 층·같은 방이면 늘 같은 세상이다
> - **chunk**(6절) — 200 짜리 조각, 가까운 것만. 반경 3 = 49 조각 · 1400 사방
> - **높낮이**(4절) — 밖으로 갈수록 기복. **방 안은 평평하게 둔다**: 판정이 평면에서
>   도는데 지형이 방 안까지 들어오면 **보이는 것과 걷는 것이 어긋난다**
> - **방 가까이는 사람이 지난 자리**(길·캠프·폐허). 5절 "플레이 가능한 결과만" 이
>   그것이다 — 방 문 앞이 절벽으로 막히면 안 된다
> - 조각 성격은 **자체 mix 로** 뽑는다. 격자용 해시를 쓰면 이웃한 입력이 이웃한 답을
>   줘서 **온 세상이 숲 한 판**이 된다(사가고에서 밟은 그 함정 · 진단이 지금 그걸 잰다)
>
> **정직하게 — 이것은 4·5·6절의 절반이다.** 세상은 넓어 보이는데 **걸어 나갈 수는
> 없다.** 판정(`dungeon.js`, 58KB)은 여전히 방 하나 안에서 돈다. 그 심장을 뒤집는 것은
> 40절("이미 정상 작동하는 코드는 수정하지 않는다")과 부딪히므로 다음 단계로 미뤘다.
>
> 진단 **174 → 182/182** · 콘솔 무오류 · 단독판 재빌드 ·
> 방 너머로 절벽과 캠프 불이 보이는 그림을 눈으로 봤다.
>
> ### 사가고 PHASE 1~13 이 다 찼다
>
> `PLAN.md` 의 단계는 여기서 끝난다. **다만 계획서가 안 다룬 자리가 남아 있다** —
> 다음 세션이 고를 몫이다.
>
> - **사가의숲·사가스토리·사가국지는 아직 시작하지 않았다.** 각 `PLAN.md` 첫 단계부터
> - **`saga-pc/dist/`** 에 `역사고.html` 류 옛 이름이 남아 있다. gitignore 라 웹에는
>   없고, 다시 구우면 `사가고.html` 로 나온다
> - **`saga-go/_autoprobe.html` 은 여전히 죽어 있다**(없는 스크립트 넷을 부른다).
>   이번에는 제목만 고쳤지 살리지 않았다
>
> ## 그 앞 — 2026-08-26 에 있었던 일
>
> ### 넘겨받는 자리 — **다 올렸고 트리가 깨끗하다**
>
> 사용자 지시 *"완료 되면 전체 커밋 해주고 깃에서 올려줘"* 로
> **`c585d73..a675ca7` 21 커밋을 `origin/main` 에 올렸다.**
> 여러 세션이 쌓아 두었던 것을 한꺼번에 정리한 자리다 —
> 사가스토리 여섯 · 사가국지 여섯 · 사가의숲 지형 개조 · 사가블로 마을·이름 ·
> 사가고 폰 대응 · 인계 문서.
>
> **`git status` 가 깨끗한 것을 이상하게 여기지 말 것.** 그전까지는 늘 미커밋이
> 산더미였다. 다만 이 저장소는 여러 세션이 같은 트리에서 도는 구조라
> **곧 다시 쌓인다** — 이어받을 때 `git status` 를 먼저 본다. [[git-shared-index]]
>
> 배포도 확인했다 — <https://smh8627-jpg.github.io/swbins/> 에서 **다섯 판이 다 열린다**.
> (배포는 푸시 뒤 1~3분 걸린다. 그 사이에는 옛 판이 나오거나 폴더 주소가 404 로
> 뜨기도 한다 — `.../saga-story/` 가 404 인데 `.../saga-story/index.html` 은 뜨면
> 아직 배포 중인 것이지 파일이 빠진 것이 아니다)
>
> ### 사가경영 → **사가국지**, 그리고 남아 있던 넷을 다 넣었다
>
> 사용자 지시 *"사가 경영 이어서 해줘 사가국지로 변경 해주고"* → 뒤이어
> *"1~4번 순서대로 진행 해줘"* → *"모바일 웹페이지에서 가능 하게 해줘"*.
> **진단 59 → 95/95.** 다섯 커밋으로 갈라 담았다.
>
> 1. **이름** (`4358bca`) — 삼국지로 갈아엎고도 '경영' 으로 남아 있던 이름을 맞췄다.
>    바꾼 것은 **사람이 읽는 이름뿐**이다. 폴더 `saga-realm` · 세이브 키
>    `saga-realm/save/<프로필>` 은 그대로다. 받침이 ㅇ → 없음으로 바뀌어 어긋난
>    조사(사가국지이·사가국지을)도 함께 폈다
> 2. **여러 달에 걸치는 원정** (`1f926ab`) — 한 달에 못 떨어뜨리면 물러나지 않고
>    **성 밖에 진(陣)** 을 친다. `war.resolveAll()` 이 비워 두었던 자리다
> 3. **수군** (`30534ec`) — **양쪽이 다 강인 길은 물길**이고 배로만 건넌다.
>    아홉 줄기가 저절로 잡혔다(적벽 = 강하↔시상). 성벽이 소용없는 대신 화공이 터진다
> 4. **시나리오 셋** (`96d33ad`) — 194 군웅할거 · **200 관도** · **208 적벽**
> 5. **무장 성장** (`2c65df7`) — 일을 시키면 경험이 붙고, 쌓인 공으로 승진시킨다
> 6. **폰에서 놀 수 있게** (`bda832d`) — `prompt()` 를 걷어내고 카드로 바꿨다
>
> 자세한 것은 **`saga-realm/README.md`** 다. 넷을 넣으며 밟은 자리는 아래
> **'사가국지에서 밟은 것'** 에 모아 두었다.
>
> **균형을 씨앗 다섯으로 120개월 다시 쟀다** — 2년 6~10 · 5년 5~7 · 10년 4~5 세력 ·
> 1위 10~15성 · 무장 평균 Lv.9 · 승진 26~50단 · 수전 13~22번.
> 얼지도 쓸려 나가지도 않는다.
>
> **다음 세션이 이어받을 자리** (사가국지 기준):
>
> 1. **균형을 사람이 눌러 본다.** 위 수치는 "얼지도 쓸려 나가지도 않는다" 까지만
>    말해 준다. **시작 세력별 난이도**(유비 1성, 208년의 손권·유비가 특히 가혹하다)와
>    명령 성과·전투 손실은 실제로 눌러 봐야 안다. 수를 고쳤으면
>    `saga-realm/README.md` 의 '균형' 절을 다시 잰다
> 2. **폰에서 실제로 눌러 본다.** ①미는 막대가 손에 붙는지 ②독 여섯 칸이 좁지 않은지
>    ③사파리 하단 툴바가 독을 가리지 않는지. **코드부터 손대지 말고 먼저 묻는다**
> 3. **나이** — 원작에서는 무장이 늙고 죽는다. 넣으려면 124인의 생년이 필요하고
>    노쇠 보정을 `hero.stats` 에 붙여야 하는데 그 파일은 다섯 판이 나눠 가진 복사본이다.
>    넣는다면 `officer.js` 쪽에서 능력치를 감싸는 층을 하나 더 두는 길뿐이다
> 4. **오답노트**만 아직 못 옮겼다. 사변은 재해로, 태수 자질은 태수 보정으로 살렸지만
>    오답노트(`d8e427d`)는 갈 자리가 없어 걷혔다. 되살리려면 학당 시트에 붙인다
>
> ### 사가국지에서 밟은 것 (되풀이하지 말 것)
>
> - **진중의 굶주림 식은 성과 달라야 한다.** `settleMonth` 의 식(모자란 만큼 그대로
>   병사에서 깎는다)을 진영에 쓰면 치중이 떨어지는 그 달에 원정군이 **통째로** 사라진다.
>   성은 물러날 데가 없고 진영은 있다 — 이할만 흩어지고 군을 돌린다
> - **진을 친 성에는 새 군대를 또 못 보낸다**(`canMarch`). 없으면 AI 가 달마다 새 진영을
>   세워 같은 성 앞에 군대가 쌓인다. 늘리려면 **보급**이다
> - **원정 나간 장수는 성에 없다**(`off.rec(id).camp` → `atCity` 가 뺀다).
>   안 그러면 군대를 내보내고도 내정·수비를 그대로 하는, 아무것도 안 잃은 셈이 된다
> - **AI 에 열어 주는 것을 가린다.** 진영 보급은 **군량만** 열었다(`trySupply`).
>   병력 증원까지 열면 달마다 뒷마당을 비워 진영에 몰아주게 되어 균형이 통째로 달라진다.
>   반대로 **승진은 열어야 한다**(`tryPromote`) — 안 열었더니 AI 는 120개월에 승진이
>   **한 단도** 없었고 사람 쪽만 관직 배수를 받았다
> - **넷째 자질을 만들지 않았다.** `data.js` 의 인물과 모양이 같아야 도감·초상·능력치가
>   그대로 돈다. 수전 배수는 **`data-force.js` 의 목록**(`NAVY`)으로 뒀다 —
>   다섯 판이 나눠 가진 `data.js` 를 한 줄도 안 건드리고 적벽이 선다
> - **화공은 가늠(`forecast`, dry)에서도 굴린다.** 빼면 AI 가 보는 승산과 실제가
>   어긋나 판정이 두 벌이 된다. 일기토만 뺀 것은 장수가 **진짜로 다치기** 때문이다
> - **시나리오는 세이브(`st.scen`)에 남기고 `rtk.state()` 가 `FD.use()` 를 다시 부른다.**
>   없으면 208년 판을 열어 두고 새로고침했을 때 194년 표로 읽혀 세력 이름과 색이 어긋난다.
>   `FD.FORCES` 는 **배열을 그대로 갈아 끼운다** — 새 배열로 바꾸면 이미 그 배열을
>   붙들고 있는 파일들이 옛 표를 계속 본다
> - **`save.heroes` 는 세이브 공용 칸이라 `setup` 이 안 비운다.** 그래서 자가진단의
>   `fresh()` 가 그 칸을 함께 비운다 — 안 비우면 앞 항목이 올려 놓은 레벨이 뒤 항목의
>   능력치로 흘러 들어가 수치가 **항목 순서에 달리게** 된다
> - **`hero.gainExp`·`hero.rankUp` 을 쓰지 않는다.** 둘 다 `save.dex.heroes`(도감에서
>   뽑은 인물)를 보는데 이 판에는 뽑기도 중복 인물도 없어 언제나 0 을 준다
>
> **사가스토리도 2026-08-26 에 원작 모방을 끝냈다** — 밧줄·사다리·문 · 궁수 ·
> 장비/가방/저자/주문서 · 직업 아홉과 무예 스물여덟 · 사명 열셋 · 미니맵 · 앉아 쉬기 ·
> 원작풍 외피(`css/maple.css` + 넷째 그림 양식 `maple`). 진단 33 → **79**.
> **이로써 네 판이 다 원작 모방을 끝냈고, 남은 것은 사가국지뿐이다.**
> 자세한 것은 아래 **'이어서 하면 좋은 것'**.
>
> ## 그 뒤 — 사가스토리 넷 (2026-08-26 이어서)
>
> 사용자가 *"1~4번 순번대로"* 라고 골라 넷을 차례로 넣었다. **진단 79 → 99.**
>
> 1. **어드민**(`_admin.html`·`js/admin.js`) — 이 판에만 없던 것이다. 탭 여덟 ·
>    손잡이 열일곱 · QA 프리셋 14 · 자가점검 10. `core.js` 에 손잡이 층을
>    **사가고·사가의숲과 같은 이름으로** 옮겼다(`TUNE_KEY`·`POKE_KEY`·`tuned/setTune`).
>    어드민은 눌러야 도는 화면이라 진단이 못 붙는다 →
>    **`_admin.html?selftest` 로 열면 스스로 점검하고 제목에 `ADMIN n/n` 을 적는다**
> 2. **소리**(`js/sfx.js`) — 사가블로와 **같은 엔진**, 소리표만 이 판의 것(37).
>    이 판이 소리를 들인 까닭의 절반은 **주문서 성패**다(오르는 화음 대 떨어지는 잡음).
>    `_sfxcheck.html` 로 실제로 우는지 본다 → `SFX unlock=true · 낸 소리 37/37`
> 3. **3차 전직 넷과 무예 스물** — 직업 9 → **13**, 무예 28 → **48**.
>    원수·비장·귀영·진인(Lv.45). **갈래는 넷 그대로**이고 각 갈래를 한 단 높였다.
>    문턱은 자리마다 무거워진다(2차 5 · 3차 8). **효과는 하나도 안 늘렸다** —
>    `side.js` 가 아는 아홉으로 스물을 다 짰다. 조작 띠 6 → **8칸**, **윗자리부터** 놓는다
> 4. **전투 연출** — 사가고의 `duel.js` 처럼 **판정 층과 화면 층을 갈랐다.**
>    판정에 닿는 것은 **급소(15%·1.6배)와 넉백** 둘뿐. 흔들림·급소 숫자·넘어가는 몸·
>    착지 먼지·피격 비네트·보스 돌진 예고는 `side-view.js` 에만 있다.
>    **경직은 `game.js` 루프에만** 뒀다 — 진단과 자동은 `update(dt)` 를 직접 굴려 안 닿는다.
>    급소가 판정에 들어와 **자동 5분치 수확 97 → 108마리**로 다시 쟀다
>
> 5. **넷째 사냥터 — 호로곡(葫蘆谷)**(Lv.25). 3차가 설 자리를 만들었다.
>    **처음으로 정예(tier 4)가 나온다**(`enemyLv` 26 이 `tierOf` 문턱을 넘긴다).
>    수치로 계단이 선다 — 정예 체력 2,596 을 2차 패왕격(만렙)은 **두 대**,
>    3차 천붕격은 **한 대**에 넘긴다. 굴혈 오른쪽에 문을 냈다(보스가 선 자리를 지나야
>    나간다). `side-view.js` 에 넷째 배경 `mood: 'fire'` — 검은 산줄기·골짜기 벽·
>    바닥에서 오르는 불길·떠오르는 불티 네 겹, 도형과 그러데이션뿐이다
> 6. **폰에서 놀 수 있게** — 폰(390px)에서 실제로 열어 보니 네 군데가 막혀 있었다.
>    **규칙은 한 줄도 안 고쳤다.** ①**↓ 가 아예 없었다**(캔버스를 셋으로만 읽어서
>    줄에서 내려갈 수도 앉아 쉴 수도 없었다 — 아래 가운데 22% 를 냈다. 누른 채 끌면
>    방향이 바뀐다) ②상단이 무너졌다(이름 한 줄·도구 넷씩 두 줄) ③**조작 띠 여덟 칸이
>    독에 덮였다**(접어서 두 줄, 독 위로) ④보스 바·미니맵이 상단과 한 자리를 다퉜다.
>    `core.touchOnly()`·`actHint()` 는 **사가의숲과 같은 이름**이다.
>    **`_mobileframe.html`** 이 폰 화면을 보는 껍데기다(`#probe` → `PROBE OK`)
>
> **진단 79 → 106.**
>
> **다음 자리** — 원작에서 남은 축은 파티·거래·길드뿐이고 싱글에 뜻이 옅어 넣지 않는다.
> 장비가 4단(Lv.20)에서 끝나므로 **5단**을 두면 호로곡의 정예와 결이 맞는다
> (`data-gear.js` 한 파일이다). 다섯째 사냥터를 두려면 `data-enemy.js` 에 tier 5 가
> 먼저 있어야 한다 — 그 파일은 **사가블로와 나눠 든 복사본**이라 두 판을 함께 본다.

> ## 그 뒤 — 사가의숲을 폰에서 (2026-08-26 이어서)
>
> 사용자가 *"사가의숲 모바일 웹사이트에서 가능 하게 할수 있어"* 라고 물었다.
> **절반은 이미 돼 있었다** — manifest·서비스워커·safe-area·780px 미디어쿼리에
> 캔버스 탭 걷기(`walkTo`)까지. 그래서 **폰 폭(390px)에서 실제로 열어 보고
> 막히는 것만 골라 고쳤다.** 커밋 `0e9f76f`. 진단은 **143/143 그대로**.
>
> 막혀 있던 넷은 이렇다. 넷 다 눈으로 보지 않으면 알 수 없는 것이었다.
>
> 1. **상단 HUD 가 무너졌다.** 780px 규칙이 시트와 독은 내려 주는데 `#top` 은
>    손대지 않았다. 프로필 이름이 "무 명 (無 名)" 처럼 **한 글자씩 줄바꿈**되고,
>    도구 여덟 개 중 **`❔`·`↺` 가 화면 밖으로 나가 아예 누를 수 없었다.**
>    560px 아래에서 도구를 넷씩 두 줄로 접고, 프로필은 이름 한 줄·상태 두 줄로 세웠다
>    (상태는 `white-space: normal` — 한 줄로 자르면 "여름 · 낮" 이 잘려 나간다)
> 2. **폰에 없는 `Space` 를 누르라고 안내했다** — 말풍선·카드·도움말 열다섯 곳.
>    `core.touchOnly()` · `core.actHint()` 를 뒀다. **hover 가 없고 포인터가 뭉툭한
>    기기**(`hover: none` + `pointer: coarse`)에서만 `✋` 로 바뀐다 — 마우스와 터치를
>    함께 쓰는 노트북은 키보드가 있으니 `Space` 그대로다. **헤드리스는 hover 가 있어
>    진단 출력이 바뀌지 않는다** — 그래서 143 이 유지된다
> 3. **걸음이 뚝뚝 끊겼다.** 탭은 목표를 한 번 찍고 기다리는 꼴이다.
>    `village-view.js` 의 `click` 을 `pointerdown/move/up` 으로 바꿔 **누른 채 끌면
>    손가락을 따라 걷는다**(`setPointerCapture` 로 HUD 위를 지나도 안 끊긴다).
>    마우스도 같은 길을 쓰고, `PointerEvent` 가 없으면 탭으로 돌아간다.
>    `onClick` 은 `pointAt` 으로 갈라 두 길이 같은 되짚기(`unproject`/`unprojIn`)를 쓴다
> 4. **`#focusbar` 가 가로로 깔린 독에 가려** "읽는다" 를 누를 수 없었다.
>    `#near` 와 같은 높이(`84px + safe-b`)로 올렸다.
>    더불어 `#map` 에 `touch-action: none` — **iOS 사파리는 `user-scalable=no` 를
>    무시해서** 더블탭 줌이 걷기를 방해한다
>
> **`sw.js` 를 v0.15.1 로 올렸다.** 안 올리면 홈 화면에 담아 둔 폰에 옛 캐시가 나온다.
> 폰에서 여는 길은 둘이다 — **GitHub Pages(https)가 확실하다**(서비스워커·홈 화면
> 추가까지 붙는다). 같은 와이파이에서 `run.bat`(0.0.0.0 바인딩)으로 `:8793` 을 열어도
> 놀 수는 있지만 http 라 캐시는 안 붙는다. **사가의숲은 사가고와 달리 위치 API 를
> 쓰지 않아 HTTPS 가 필수가 아니다** — 그래서 `run-phone.bat` 이 없고, 없어도 된다.
>
> ### 다음 세션이 이어받을 자리
>
> - **아직 손으로 눌러 본 것이 아니다.** 폰에서 실제로 열어 ① 넷씩 두 줄로 접힌 도구가
>   답답하지 않은지 ② 끌어서 걷기가 손에 붙는지(속도·되돌아옴) ③ 사파리 하단 툴바가
>   독을 가리지 않는지를 본다. **코드부터 손대지 말고 먼저 묻는다**
> - ~~`js/ui.js` 와 `_mobileframe.html` 은 커밋하지 않았다~~ — **둘 다 들어갔다**
>   (2026-08-26 의 `c585d73..f3914c0`). 섞여 있던 다른 세션의 공사 시트 작업이
>   먼저 커밋되면서 함께 정리됐고, `_mobileframe.html` 은 다섯 판에 다 있다
>
> ## 그 뒤 — 사가고 폰 (2026-08-26 이어서, `6e43030` 으로 커밋됨)
>
> 사용자가 **공개 주소(GitHub Pages)로 폰에서 사가고를 열었다.** 말은
> *"지형지물 크고 … 최적화 시킬 수 있어?"* 였지만 곧 *"최적화라기보다 가능 하게"*
> 라고 고쳐 말했다 — **쓸 수 있는 상태가 아니었다는 뜻이다.** 물어보니 증상은
> **UI 잘림**이었고, 재 보니 둘이었다. 둘 다 고쳤다.
>
> 1. **상단 도구가 화면 밖으로 밀려나 있었다.** 도구는 아홉인데(👤 는 `account.js`
>    가 끼운다) 390px 화면에서 **416px 을 달라 하고 160px 이 밖으로** 나갔다.
>    `#top` 그리드의 tools 칸이 `auto` 라 줄지 않는다. 뒤쪽 서넛은 보이지도 눌리지도
>    않았다 → **`⋯` 서랍**(`#tools-drawer`, 600px 이하에서만). 상시로 남는 것은
>    🤖 자동 · 📡 위치 둘. **단추를 옮기지 않은 것이 요점**이다 — 서랍은 넓은 화면에서
>    `display: contents` 로 풀려 PC 는 여태 그림 그대로고, 이벤트도 다시 걸 것이 없다
> 2. **세로 화면에서 지형지물이 화면을 덮었다.** 폰 탓이 아니라 **three 의 fov 가
>    수직 기준**이라 그렇다 — 세로로 긴 폰(0.53)에서는 가로 시야가 PC 의 절반 밑으로
>    좁아지는데, 이 판의 거리·높이가 PC 가로 화면에서 잡혔다. `world3d.js` 의
>    `fovFor(w, h)` 로 **시야각만 벌린다**(폰 52° → 78.9°, 가로 화면은 52° 그대로).
>    **카메라를 물리는 길은 밟아 보고 접었다** — 안개에 통째로 묻힌다(함정 표 참조)
>
> 덤으로 **독**(일곱이 되면 왼쪽으로 넘어간다)과 **근처 카드**('가까이 가기' 가 석 줄로
> 쪼개진다)도 같이 잡았다. **`sw.js` 세대를 `go-v3.4.0` 으로 올렸다** — 안 올리면
> 이미 한 번 연 폰이 옛 캐시를 받아 아무것도 안 바뀐 것처럼 보인다.
> 손잡이 넷을 열어 뒀다: `world3d.portraitFit`(0 이면 옛 그림) · `portraitMax` ·
> `fov` · `fovMax`. 진단 **189/189**, PC 1280px 회귀 없음, 단독판 재빌드까지 확인했다.
>
> **이 작업은 2026-08-26 에 커밋했다** — 사용자 지시
> *"완료 되면 전체 커밋 해주고 깃에서 올려줘"*. 표시 이름 맞추기
> 다섯 곳도 같은 커밋에 들어갔다.
>
> ### 다음 세션이 이어받을 자리
>
> - **실기기 fps 는 여전히 못 쟀다.** 이번 건은 배치와 시야각이라 **그리는 양은
>   그대로다.** 폰에서 버거우면 `world3d.density` → 0.5, 그래도 무거우면
>   `world3d.mesh` → 0. 헤드리스는 rAF 가 안 돌아 `#perf` 를 못 쓴다
> - **먼저 "폰에서 다시 열어 보니 어떻더냐" 를 묻는다** — 코드부터 손대지 말 것.
>   ① `⋯` 서랍이 답답하지 않은지(자주 쓰는 것이 서랍에 들어가 있지는 않은지)
>   ② 78.9° 가 너무 넓어 휘어 보이지는 않는지 ③ 독과 근처 카드가 안 겹치는지
> - **`_demo.html` 의 상단은 `index.html` 과 별도 마크업**이라 폰에서 열면 여전히
>   잘린다. 개발용이라 두었다 — 맞추려면 같은 서랍 마크업만 넣으면 된다
>   (데모도 `game.js` 를 부르니 `bindMore()` 토글은 그대로 돈다)
> - **`saga-go/_mobileframe.html`(신규, 미추적)** — 다시 재 볼 때 처음부터 만들지
>   않아도 되게 남겼다. `#probe` 로 넘침을 재고, `#fit0`↔`#fit1` 로 시야각 보정을
>   끄고 켜 비교한다(데모로는 안 된다 — 위 '검증' 절). 다른 세 판의 같은 이름 파일과
>   같은 꼴이다. **남길지 버릴지는 사용자가 정한다**

## 다섯 게임 — 완전히 별개인 프로젝트

| 게임 | 폴더 | 포트 | 원작 | 세이브 키 | 진단 |
|---|---|---|---|---|---|
| **사가고** | `saga-go` | 8791 | 포켓몬GO | `deungyong-go/save/<프로필>` | 307/307 |
| **사가블로** | `saga-dungeon` | 8792 | 디아블로 | `yeoksa-dungeon/save/<프로필>` | 182/182 |
| **사가의숲** | `saga-forest` | 8793 | 동물의숲 | `yeoksa-village/save/<프로필>` | 143/143 |
| **사가스토리** | `saga-story` | 8794 | 메이플스토리 | `yeoksa-side/save/<프로필>` | 106/106 |
| **사가국지** | `saga-realm` | 8795 | **삼국지(코에이)** | `saga-realm/save/<프로필>` | 95/95 |

`saga-pc/` 는 게임이 아니라 **PC 묶음 빌드 도구**다.

각 폴더에 **`PLAN.md`**(앞으로 갈 곳 — 3D 전환)와 **`CLAUDE.md`**(그 둘을 먼저 읽게 하는
두 줄)가 있다. `README.md` 는 **지금까지 만든 것**이다. 셋을 헷갈리지 않는다.

도감은 다섯 판이 같은 내용을 다섯 벌 들고 있다 — **인물 70 · 펫 41**
(한국사·삼국지에 2026-08-25 유럽사 22인, 펫에 원작 짐승 16종을 더한 수다).

> **사가국지만 예외로 124인**이다(2026-08-26). `data.js` 는 그대로 두고
> `data-force.js` 의 삼국지 무장 54를 **부팅 때 얹는다**(`officer.js`). 파일은 여전히
> 다섯 판이 바이트까지 같으니 "다섯 곳을 고쳐야 한다" 는 규칙은 깨지지 않았다.

## 이 구조를 만든 사용자 지시 (되돌리지 말 것)

이 순서로 나왔고, 하나하나가 지금 구조의 이유다.

1. **"완전 별개 4프로젝트 · 세이브 완전 분리 · 골격부터"**
   → 공용 파일(`data.js`·`sprite.js`·`core.js`·`hero.js`)이 **다섯 벌 복사본**이다.
   인물을 하나 추가하면 다섯 곳을 고쳐야 한다. **값을 알고 고른 구조이니
   "공유하자"고 되묻지 말 것.**
2. **"역사"에 들어갈 영어 단어 추천** → **Saga** 선택 (폴더·패키지명)
3. **"내가 말한 게임은 완전 동일하게 모방할거임"**
   → 원작 장르에 없는 축은 각 게임에서 뺀다. 포켓몬고에 던전·장비·퀴즈가 없으니 뺐다.
4. **"사당은 간단한 퀴즈 게임임 이건 내 상상이고"**
   → 문답을 다섯째 게임으로 분리. **네 게임에는 quiz.js 가 없다.**
5. **"퀴즈 게임에는 경영을 넣어줘 · 역사 경영 시뮬레이션이지"**
   → 경영(영지·태수·교역·자동 건설)을 `saga-realm` **에서만** 되살렸다.
   포켓몬고식(saga-go)에서 되살리지 말라는 옛 지시는 그대로다.
6. **"사가 퀴즈는 사가 경영으로"** → `saga-quiz` → `saga-realm`
7. **"간단히 가입해서 저장"** → `js/account.js` (아래)
8. **"다 피시에서 할수 있게"** → 다섯 판 PC 단독 실행판 + 런처
9. **"삼국지 모방을 할거임"** (2026-08-26) → `saga-realm` 을 **코에이 삼국지**로 통째로
   갈아엎었다. 같은 자리에서 물은 "문답은 어디에 두나" 에 사용자가
   **"문답은 곁가지로 물러난다 — 정통 턴제 삼국지를 그대로"** 를 골랐고,
   옮길 축의 순서로 **①턴·도시·내정 ②무장 ③전투 ④외교·계략** 넷을 다 골랐다.
   → 문답은 **학당** 한 칸으로 남고, 옛 경영(강역·특산·건설·사변)은 걷어냈다.
   **7번의 "이건 내 상상" 은 이 판에 한해 사용자가 스스로 거둔 것이다** —
   되돌려 "원작 모방이 아닌 판" 이라고 다시 적지 말 것
10. **"마을에서 시작 하는걸로 변경 해줘야 할것 같아 디아블로 처럼"** ·
    **"던전 선택 방식이 아닌 이건 디아블로랑 다른데"** (2026-08-26)
    → `saga-dungeon` 의 첫 화면이던 **본영 카드를 지웠다.** 층과 난도를 버튼으로
    고르던 그 카드가 원작과 어긋난 자리였다 — 원작에서 바깥은 메뉴가 아니라
    **장소**다. 그래서 `js/town.js` 로 **걸어다니는 마을**을 만들고, 고르던 것을
    전부 그 안의 사람과 표식으로 옮겼다.

    | 예전 (카드의 버튼) | 지금 (마을에서 가는 곳) |
    |---|---|
    | 「제1층부터 내려간다」 | 굴혈 🕳️ 로 **걸어 들어간다** (고르는 창이 없다) |
    | 「제N층 역참부터」·난도 | 역참 🌀 을 **밟는다** — 밟아 둔 층만 목록에 뜬다 |
    | 「결사로 바꾼다」 | 결사비 ☠️ (되돌릴 수 없어 한 번 묻는다) |
    | 자동 순행 | 군교 ⚔️ (부대 시트 아래) |
    | 부대 전투력 요약 | 군교 ⚔️ · 치중 🎒 · 교두 📜 · 야장 🔨 · 행상 🧺 · 사관 📖 |

    사용자가 **"NPC + 독 버튼 둘 다"** 를 골랐다 — 사람에게 걸어가도 열리고,
    하단 독으로도 열린다. 폰에서 매번 걸어가지 않아도 되게.
11. **"사가 경영 이어서 해줘 사가국지로 변경 해주고"** (2026-08-26)
    → 표시 이름만 **사가경영 → 사가국지**. 폴더·세이브 키는 그대로다.
    이어서 **"1~4번 순서대로 진행 해줘"** 로 인계 메모가 꼽아 둔 넷을 다 넣었다
    (여러 달 원정 · 수군 · 시나리오 셋 · 무장 성장).
12. **"모바일 웹페이지에서 가능 하게 해줘"** (2026-08-26)
    → 사가의숲·사가스토리·사가고에 이어 **사가국지도 폰에서 놀 수 있게** 했다.
    이 판에서 막혀 있던 것은 `prompt()` 였다 — 출진·보급·증원·병력 보내기 넷이
    숫자를 `prompt` 로 받고 있었는데, 폰에서는 글자판이 뜨고 홈 화면에 담아 띄운
    앱에서는 아예 안 뜨는 기기가 있다. **미는 막대와 ¼·½·⅘·전부** 카드로 바꿨다.
    **다섯 판이 다 폰에서 돈다.**

## 절대 바꾸면 안 되는 것

- **세이브 키는 폴더 이름과 다르다.** 사가고는 `deungyong-go/save/…`, 사가블로는
  `yeoksa-dungeon/save/…` … 이름을 맞추려고 바꾸면 **지금까지의 진행이 사라진다.**
  앱 패키지 id(`com.swbin.deungyonggo`)도 같은 이유로 그대로다
- **표시 이름은 2026-08-26 에 `역사~` → `사가~` 로 바꿨다**(사용자 지시 "역사는 사가로 모두 명칭 변경").
  바꾼 것은 **사람이 읽는 이름뿐**이다 — 세이브 키·앱 id·폴더명에 남은 옛 이름
  (`deungyong-go`·`yeoksa-dungeon`·`yeoksa-village`·`yeoksa-side`)은 **일부러 그대로 두었다.**
  시리즈명과 안 맞는다고 맞추면 위 항목 그대로 진행이 사라진다.
  문답의 분야명 "역사"(`hist`)와 지시 이력의 인용문은 게임 이름이 아니라서 건드리지 않았다
- **삼국지도 문답도 saga-realm 만.** (2026-08-26 전까지는 '경영' 이 그 자리였다)
- 지도에 던전 입구를 노출하지 않는다(옛 지시). 지금은 던전이 딴 게임이라 자동으로 지켜진다

## 가입해서 저장 (`js/account.js` — 다섯 게임 공통, 이름만 다름)

```
세이브 키    <게임>/save/<프로필id>
프로필 목록  <게임>/save/accounts
옛 세이브    <게임>/save/v1        ← 첫 가입 때 '이어받기' 로 옮긴다
```

- `game.js` 는 `boot() = DG.account.gate(start)` **한 줄만** 부른다.
  gate 가 프로필을 정해 `core.setSaveKey()` 를 부른 뒤 게임을 켠다
- `core.js` 의 `SAVE_KEY` 는 **getter 로 export** 해야 한다(스냅샷이면 프로필을 갈아도
  옛 키가 남는다). `SAVE_BASE` + `setSaveKey()` 가 짝이다
- 상단 **👤** 는 account.js 가 직접 끼워 넣는다 — index.html 을 고칠 필요가 없다
- 이름은 **게임마다 따로**다. 게임당 6명까지
- 자가진단은 `window.DG_NO_ACCOUNT = true` 로 가입 화면을 건너뛰고 `…/save/test` 를 쓴다.
  **이걸 빼면 게임이 안 켜져 모든 항목이 줄줄이 실패한다**

## 검증 (그대로 재사용)

```
서버      각 폴더 run.bat (브라우저 열림) / start_server.bat (허브 워치독용)
자가진단  chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom \
            http://127.0.0.1:<포트>/_test.html      → RESULT n/n
배포 확인 같은 명령을 https://smh8627-jpg.github.io/swbins/<폴더>/_test.html 로.
          로컬과 수치가 같아야 한다. 회사망에서는 curl 이 github.io 인증서 검증에
          실패하니 크롬으로 본다(github.com 은 된다). 푸시 뒤 1~3분 걸린다
PC 묶음   tools/saga-pc/build-all.bat  →  saga-pc/dist/ (다섯 html + index.html 런처)
          게임별로는 각 폴더 build-pc.bat
```

- **진단의 무작위는 씨앗으로 고정돼 있다**(mulberry32, 씨앗 20260824).
  `_test.html` 이 게임 스크립트보다 **먼저** `Math.random` 을 갈아 둔다.
  그래서 자동을 몇 분치 굴리는 항목도 매번 같은 수치가 나온다.
  **세 번 돌려 출력이 한 줄도 다르지 않은지**로 확인한다.
  다만 **두 줄은 씨앗과 무관하게 흔들린다** — 로그의 시각(`15:37`)과 상단 지갑 HUD 한 줄
  (진단이 끝나는 순간 초기값으로 그려졌는지 최종값으로 그려졌는지의 렌더 타이밍이다).
  (2026-08-26 추가 — **근처 대상 카드의 거리 한 줄**도 같은 성격이다. 배회 중인
  대상까지의 거리라 86m/87m 처럼 1m 씩 흔들린다. 판정 수치가 아니다.)
  이 둘을 빼고 나머지가 같으면 재현된 것이다:
  `sed -E 's/[0-9]{2}:[0-9]{2}(:[0-9]{2})?//g'` 로 시각을 지우고 diff 하면 지갑 줄만 남는다.
  수치가 바뀌면 균형이 실제로 바뀐 것이다:
  사가고 순행 45m · 사가블로 제10층·금 8,725·사냥 157 ·
  사가의숲 채집 1343·금 15.2만 · 사가스토리 잡음 97·Lv.10
  (사가스토리의 자동 수확은 이 순서로 움직였다 — 36 → 줄을 타며 120 → 궁수가 생겨 79
   → 줄 고르기를 좁혀 46 → 무예를 종류대로 쓰며 **97**. 균형이 바뀌면 이 값이 따라온다)
  (순행 190m→45m 은 2026-08-25 의 이동 속도 34→8 m/s 가 그대로 비친 값이다)
- 게임 코드에는 씨앗을 넣지 않았다 — 실제 플레이는 그대로 무작위다

### 폰 화면을 헤드리스로 보는 법 (2026-08-26 알아냄)

**헤드리스 크롬은 창 폭을 500px 아래로 내려 주지 않는다.** `--window-size=390,844`
를 줘도 뷰포트는 `500x749` 로 잡히고, 그래서 카드 오른쪽이 잘려 **CSS 가 깨진 것처럼
보인다**(이 문서의 옛 '폭 560 이상' 규칙이 그 그림자다 — 잘림은 CSS 탓이 아니었다).
재 보는 법은 이 한 줄이다:

```
chrome --headless=new --window-size=390,844 --dump-dom \
  'data:text/html,<body><script>document.body.textContent=innerWidth+"x"+innerHeight</script>'
```

그래서 **`saga-forest/_mobileframe.html`** 을 뒀다. iframe 안쪽에 **진짜 390px 뷰포트**
를 만드는 껍데기다(창은 560 이상으로 크게 띄운다). 같은 출처라 부모가 프로필을 미리
심어 **가입 화면을 건너뛴다**. **`saga-dungeon/_mobileframe.html`** 도 같은 꼴이다
(`#dungeon` 은 던전 화면까지 들어가 조작판이 한 줄에 들어오는지 본다).

> 이 껍데기로 **폭이 넘친 것을 실제로 잡았다**(2026-08-26, 사가블로). 눈으로는
> "잘렸다" 밖에 안 보이는데, `getBoundingClientRect().right` 를 전부 재면 **몇 픽셀
> 넘쳤는지**가 나온다 — 상단 도구줄 `454`(↺가 화면 밖), 던전 기력 구슬 `444`.
> 둘 다 `auto` 열이 콘텐츠 폭을 지키려다 옆칸을 밀어낸 것이었다. 고친 뒤 `0`.

```
폰 화면    http://127.0.0.1:8793/_mobileframe.html
가입부터   …/_mobileframe.html?fresh
시트 열고  …/_mobileframe.html#bag   (#folks #home #mail #dex #log)
조작 점검  …/_mobileframe.html#probe → PROBE OK / PROBE FAIL
```

**`saga-go/_mobileframe.html`** 도 2026-08-26 에 같은 꼴로 뒀다(포트 **8791**).
이 판은 두 가지가 더 붙는다.

- **`#fit0` / `#fit1`** — 세로 화면 시야각 보정(`world3d.portraitFit`)을 끄고/켜고 본다.
  **`_demo.html` 로는 이 비교를 못 한다** — 데모는 `DG_NO_TUNE` 이라 손잡이를 아예
  안 읽어서 **두 장이 똑같이 나온다**(여기서 한참 헛짚었다). 재 보면 390×844 에서
  `#fit1` 80.0° · `#fit0` 52.0° 로 갈린다. 이건 **진짜 손잡이를 건드리니 끝나면
  `#fitreset`** 으로 놓는다
- **`#tools`** — 상단 `⋯` 서랍을 펼친 채 찍는다. **`#3d-fit0-probe` 처럼 이어 붙인다**

```
폰 화면    http://127.0.0.1:8791/_mobileframe.html
시트 열고  …#bag   (#quest #letters #dex #oracle #log)
서랍 펼쳐  …#tools          시점 3D   …#3d
넘침 점검  …#probe → PROBE OK / PROBE FAIL (상단·도구·독·근처카드를 다 잰다)
시야각 비교 …#fit1-probe ↔ …#fit0-probe   (끝나면 …#fitreset)
```

**`saga-realm/_mobileframe.html`** 도 2026-08-26 에 같은 꼴로 뒀다(포트 **8795**).
이 판은 판이 서 있어야 잴 것이 있어서, **`#probe` 는 `index.html` 이 아니라
`_demo.html#map` 을 띄워 잰다** — 판이 안 섰으면 시나리오 고르기만 떠서
'다음 달' 도 살림도 없다(여기서 한 번 헛짚어 PROBE FAIL 이 났다).

```
폰 화면    http://127.0.0.1:8795/_mobileframe.html      (판이 안 섰으면 시나리오 고르기)
가입부터   …/_mobileframe.html?fresh
장면       …#city (#camp #officers #diplo #school #enemy #war #ask #scen #chibi)
넘침 점검  …#probe → PROBE OK / PROBE FAIL (상단·도구·독·지도·다음달을 다 잰다)
```

`#ask` 는 **출진 물음 카드**다. 이 판에서 폰을 막고 있던 것이 `prompt()` 였으므로
(글자판이 뜨고, 홈 화면 앱에서는 아예 안 뜨는 기기가 있다) 그 자리를 눈으로 볼
장면을 따로 두었다. 진단도 `window.prompt` 를 가로채 **한 번도 안 불리는지**를 붙든다.

**손가락뿐인 기기 흉내**는 blink 설정으로 낸다. 이게 있어야 `hover: none` +
`pointer: coarse` 에 걸린 것들(`core.actHint()` 의 `✋`, `@media (hover: none)`)이
실제로 바뀌는 것을 볼 수 있다:

```
--touch-events=enabled --blink-settings=primaryHoverType=1,availableHoverTypes=1,\
primaryPointerType=2,availablePointerTypes=4
```

- **시트를 열어 찍을 때는 예산을 넉넉히**(`--virtual-time-budget=30000`) 준다.
  짧으면 **여는 애니메이션 중간 프레임**이 찍혀 카드가 여러 겹 겹쳐 보이고,
  없는 가로 스크롤바까지 보인다 — 깨진 것처럼 보이지만 아니다
- **걸음이 얼마나 나갔는지로는 판정하지 않는다.** 헤드리스의 가상 시간은 돌릴 때마다
  다르게 흘러서 같은 코드가 `0`·`2`·`7` 픽셀을 오간다. `#probe` 는 그래서 거리 대신
  **`walkTo` 가 어느 자리로 몇 번 불렸는지**를 본다(누름 1 + 끌기 1 = 두 번.
  **뗌은 목표를 갈지 않는 것이 맞다** — 마지막 자리까지 걸어가야 하니까)

> **폰 화면은 iframe 안에서 본다.** 헤드리스 크롬은 창 폭을 **500px 아래로 못 내린다** —
> `--window-size=390,844` 를 줘도 뷰포트가 500 으로 잡혀 카드가 잘리고 **CSS 가 깨진 것처럼
> 보인다.** 사가의숲·사가스토리 둘 다 `_mobileframe.html` 을 두어 iframe 안쪽에 진짜
> 390px 을 만든다. 손가락뿐인 기기 흉내는 blink 설정으로 낸다
> (`--blink-settings=primaryHoverType=1,availableHoverTypes=1,primaryPointerType=2,availablePointerTypes=4`)
> — 그것 없이는 헤드리스가 **hover 가 있는 기기**로 잡혀 터치 분기가 안 걸린다.
> 거꾸로 말하면 **자가진단은 늘 키보드 기기로 돌아서, 터치 분기를 넣어도 출력이 안 바뀐다.**

> **눌러야 도는 화면은 진단이 못 붙는다.** 사가스토리에서 그 자리가 둘 생겼고 둘 다
> 헤드리스로 볼 길을 따로 냈다 — 어드민은 `_admin.html?selftest` → `ADMIN n/n`,
> 소리는 `_sfxcheck.html` → `SFX unlock=true · 낸 소리 n/n`(진단은 아무것도 누르지
> 않아 브라우저가 소리를 늘 잠가 둔다. 그 규칙이 맞다). 사가블로의 `_sfxcheck.html`
> 이 먼저 낸 길이다.

## 밟아 본 함정 (되돌리면 그대로 재발한다)

**사가고 폰 화면 (2026-08-26)**

- **세로 화면이 좁다고 카메라를 물리면 화면이 통째로 하얘진다.** 폰에서 지형지물이
  크길래 `camAim` 에 배수를 먹여 카메라를 1.69배 뒤로 뺐더니(z 96 → 162) **안개에
  전부 묻혔다** — `lightingAt` 의 `fog.near/far` 가 가로 화면 거리에 맞춰져 있기
  때문이다. 그림자 상자(`sun.shadow.camera` ±140, far 400)도 같이 벗어난다.
  **찍어 보고 접은 길이다.** 카메라는 제자리에 두고 **시야각(fov)만** 벌린다
- **fov 를 가로 시야 그대로 되찾으려 하면 안 된다.** three 의 fov 는 수직 기준이라
  폰(0.53)에서 PC 만 한 가로 시야를 내려면 **112°** 가 나온다 — 원근이 휜다.
  제곱근으로 눌러 78.9° 로 두었고 상한은 `world3d.fovMax`(80)
- **`sw.js` 의 `VERSION` 을 안 올리면 폰에서는 아무것도 안 바뀐다.** 이미 한 번 연
  기기는 앱 캐시에서 옛 파일을 그대로 받는다. 파일을 늘렸을 때뿐 아니라
  **내용만 고쳤을 때도** 올린다
- **데모·진단으로는 손잡이를 켜고 끈 비교를 할 수 없다.** `_demo.html`·`_test.html`
  은 `DG_NO_TUNE` 이라 `core.tuned()` 가 localStorage 를 아예 안 본다 — 부모에서
  손잡이를 심고 iframe 으로 띄워 봤자 **두 장이 똑같이 나오고**, 코드가 안 먹는 줄
  알게 된다(여기서 한참 헛짚었다). 손잡이 효과는 `index.html` 로 보거나,
  아예 그림 대신 **값으로** 본다(`DG.world3d.stats()` · `camAim()` 은 데모에서도
  그대로 부를 수 있다)
- **헤드리스는 PC 폭 3D 를 못 찍는다.** 폰 폭(iframe 390)에서는 잘 찍히는데 창을
  1200~1280 으로 넓히면 같은 장면이 2분을 넘겨 죽고 png 가 아예 안 생긴다
  (예산을 18000 까지 줄여도). 가로가 넓으면 지면 타일과 사물이 그만큼 는다.
  PC 쪽 회귀는 **그림 대신 값으로** 확인한다
- **자가진단 3회 비교에는 원래부터 흔들리는 자리가 하나 있다.** `js/duel.js` 의
  `Math.random()`(빠른 손 피해, `한 방 107/104`)이 씨앗 밖이다. `RESULT 189/189`
  는 늘 같으니, 3회 diff 를 볼 때 **이 줄과 살아 도는 화면(`#near`·`#profile`)은
  빼고** 본다. 씨앗을 물리는 것은 아직 안 한 일이다


**사가블로 마을 (2026-08-26)**

- **장면은 하나만 켜져 있어야 한다.** 화면(`dungeon-view.js`)은 `d()` **한 줄**로
  그릴 대상을 고른다 — 마을이 켜져 있으면 마을, 아니면 던전. 그래서 둘이 동시에
  켜지면 **마을을 그리는 동안 던전이 뒤에서 조용히 흘러간다**(맞고 있는데 마을이
  보인다). `dungeon.enter` 를 부르는 길이 여럿(굴혈·역참·자동 순행)이라 부르는
  쪽마다 마을을 끄게 맡기면 반드시 하나가 빠진다. 그래서 `game.js` 가
  `dungeon:enter` 를 듣고 한 곳에서 끈다. 진단이 이 규칙을 검사한다
- **마을은 던전과 같은 모양의 상태를 내놓아야 한다**(`raw` · `status` · `fx` ·
  `setInput` · `moveTo` · `update`). 한 칸이라도 빠지면 화면이 **예외 하나 없이**
  빈 판을 그린다 — 그래서 진단이 그 약속 자체를 검사한다(`status.skills` 넷까지)
- **던전에서 나온 자리가 굴혈 앞이면 그대로 다시 빨려 들어간다.** 나온 자리에
  세우는 것이 맞지만(다시 들어가는 것이 한 걸음이어야 한다), 그 표식만은
  발동을 잠가 둔다(`town.js` 의 `armed`). 벗어나야 풀린다
- **이름표를 아홉 개 늘 띄우면 폰에서 글자가 뭉쳐 하나도 못 읽는다.** 가까운 것
  (170)만 이름을 띄우고 먼 것은 그림 하나만 남긴다 — 원작도 가리킨 것 하나만 보여 준다
- **마을을 던전 방보다 넓게 잡으면 폰에서 사람이 개미만 해진다.** 아이소메트릭
  마름모의 가로세로는 `IX/IY` 로 **1.83:1 고정**이고, 폰 세로 화면에서는 늘
  **가로가 병목**이다. 방을 넓히면 그 병목에 맞춰 축소된다. 720×460 으로 시작해
  던전과 같은 560×380 으로 줄였다
- **최신 이모지는 글꼴에 없으면 흰 사각형(tofu)으로 뜬다.** 역참에 `🪨`(U+1FAA8)
  를 썼다가 그렇게 떴다. `🌀` 로 바꿨다 — 원작의 웨이포인트가 실제로 소용돌이
  포탈이니 그림으로도 이쪽이 맞다. **새 이모지를 쓸 때는 한 번 찍어 본다**

| 증상 | 원인 | 고친 곳 |
|---|---|---|
| 헤드리스 스크린샷에서 자동이 안 도는 것처럼 보인다 | `--screenshot`/`--dump-dom` 에서는 **rAF 가 거의 안 돈다**(6.4초에 2컷) | 판정은 `_test.html` 처럼 `update(dt)` 를 직접 굴린다. `saga-go/_autoprobe.html?sim=600` 은 그 방식으로 10분치 보고서를 찍는다 |
| 고친 코드가 브라우저에 안 반영된다 | `sw.js` 캐시가 cache-first 다. VERSION 을 안 올리면 옛 파일과 새 파일이 **한 화면에 섞인다** | 파일을 늘렸을 때만이 아니라 **내용을 고쳤을 때도 VERSION 을 올린다**(도감을 넓혔는데 안 올리면 옛 도감이 계속 나온다). 이미 물렸으면 `saga-go/_swreset.html?go=game` |
| 어드민에서 고친 값이 곧 덮인다 | 게임 창이 10초마다 `persist()` 한다. 어드민이 localStorage 를 고쳐도 게임 창이 들고 있던 옛 값이 이긴다 | 어드민이 저장한 뒤 **`POKE_KEY` 를 두드린다**. 게임 창의 `storage` 청취가 그것을 보고 `load()` 한다 — **어드민이 이긴다** |
| 옷을 갈아입었는데 화면은 그대로다 | `sprite.stamp` 의 스탬프 캐시가 **`ref.id`(+배율·걸음)로만** 갈린다. `look`·`color` 만 바꿔 넘기면 옛 그림이 그대로 나온다 | 그릴 때 **차림표를 붙인 가짜 ref** 를 넘긴다(`{ id: ref.id + '#' + wear.sig(), rarity }`). `bake` 가 사람에 대해 ref 에서 읽는 것은 rarity 뿐이라 그 최소한으로 족하다 |
| 진단이 함수를 갈아 끼웠는데 안 먹는다 | **같은 파일 안에서 부르는 자리**는 밖에서 export 를 바꿔도 그대로다(`turnip.marketOpen` · `town.starNow` · `data-village` 의 `pick` 이 부르는 `season()`) | 진단이 손봐야 하면 **구멍을 따로 뚫는다**(`_setOpen`·`_setStar` — `_spawnAt`·`_put` 과 같은 결). `VD.season` 처럼 **다른 파일이 부르는** 것만 밖에서 갈아 끼울 수 있다 |
| **자가진단이 실행마다 다른 수를 뽑는다 (그 둘째 판)** | 진단이 **게임 루프를 켜 둔 채** 돌고 있었다. 헤드리스에서 프레임 수는 실행마다 달라, 그 프레임이 주민을 움직이고 벌레를 내보내 항목마다 딛는 자리가 달라진다 | `_test.html` 에 **`DG_NO_LOOP`** 를 켜고 `game.js` 가 그때는 rAF 를 걸지 않게 했다. 진단은 필요한 만큼 `update(dt)` 를 직접 굴린다 |
| 자동이 한자리를 무한히 오간다 | 도구가 있어야 손대는 사물(삽이 드는 갈라진 자리)을 목표로 잡았다. 걸어가 아무것도 못 하고 다시 그리로 간다 | `auto.js` 의 `freshProp` 이 **`def.tool` 을 못 가진 것은 거른다**. 10분치 채집이 22 → 92 로 올랐다 |
| 하루가 자정이 아니라 저녁 여섯 시에 바뀐다 | `today()` 의 시간대 부호가 뒤집혔다. KST 는 `getTimezoneOffset() = -540` 이라 그 고장 시각은 `getTime() - offset*60000` 인데 `* -60000` 로 적혀 9시간을 뺐다 | 부호를 바로잡았다(사가의숲 `village.js`). **다른 판에도 같은 식이 있으면 같이 본다** — 순무 요일을 넣다가 드러났다 |
| **씨앗을 고정했는데 자가진단이 실행마다 다른 수를 뽑는다** | 프레임마다 도는 코드(`bug.js`·`folk.js`)가 **공용 `Math.random` 을 먹었다**. 헤드리스에서 프레임 수는 실행마다 달라서 난수 흐름이 그만큼 밀린다 | 그런 파일은 **제 난수를 따로 둔다**(마을 씨앗 ⊕ 날짜로 씨를 뿌리고, `reset()` 이 다시 뿌린다). 사가의숲 `_test.html` 의 **'프레임마다 도는 것은 공용 난수를 먹지 않는다'** 항목이 그 못이다 |
| **공사를 하고 나면 오늘 딴 표시가 엉뚱한 나무에 붙는다** | `buildProps` 가 사물 id 를 **순번**으로 지었다(`p0`·`p1`…). 공사로 사물이 하나 생기거나 사라지면 뒤 번호가 전부 밀리고, `used[propId]` 가 다른 사물을 가리킨다 | id 를 **자리**로 짓는다(`p<tx>_<ty>`). 사가의숲 `_test.html` 의 '공사를 해도 오늘 딴 표시가 다른 사물로 옮겨 가지 않는다' 가 그 못이다. **다른 판에도 순번 id 가 있으면 같이 본다** |
| 진단이 끼워 넣은 사물이 안 걷혀 다음 항목이 엉뚱한 것을 짚는다 | `raw()` 가 돌려주는 `props` 는 **살아 있는 배열**이다. 스냅샷의 칸을 갈아 끼워도(`raw.props = raw.props.filter(...)`) 마을에서는 안 빠진다 | 배열에서 직접 `splice` 한다(사가의숲 `_test.html` 의 `dropProp`) |
| 데모를 열었더니 실제 세이브가 데모 상태로 덮였다 | 데모가 같은 키를 쓰는데 루프가 10초마다 persist 한다 | `_demo.html` 에서 `core.persist` 를 no-op 으로 |
| PC 단독판이 SyntaxError 로 죽는다 | `String.replace` 치환 **문자열** 안의 `$'` 가 특수 패턴으로 먹힌다 | 치환값은 **함수로** 넘긴다 |
| 사물이 하나도 안 생긴다 | `core.hash2` 가 **0~0.5 만** 돌려줬다(마지막 xor 를 부호 있는 `>>` 로) | 새 넷은 `>>>` 로 고쳤다. **saga-go 는 지형 문턱값이 옛 범위에 맞춰져 있어 그대로 뒀다** |
| 자동 기능이 조용히 꺼져 있다 | 게임마다 `save.auto` 칸이 다른데 옛 세이브엔 그 칸이 없다 | `auto.js` `st()` 가 **빠진 칸을 채운다** |
| 시트를 열면 undefined 로 죽는다 | `status()` 가 상태에 따라 **다른 칸**을 줬다 | 사냥 중/쉬는 중 같은 칸을 준다(`saga-story`) |
| 이벤트 구독·첫 렌더가 아예 안 돈다 | ui.js 를 갈라 쓰며 `init()` 이 중간에 끊겼다 | 나머지 배선을 `bindRest()` 로 묶어 init 끝에서 부른다 |
| 건물 효과가 하나도 안 붙는다 | `data-build.js` 의 필드는 `eff` 가 아니라 **`effect`** (에러도 안 난다) | `core.buildBonus()` 가 `def.effect` 를 읽는다 |
| 눈으로 볼 수단이 없다 | `_demo.html` 이 사가고에만 있었고 그마저 죽어 있었다 | **다섯 판 모두** `_demo.html` 을 두었다(2026-08-25). 각 README 의 실행 절에 장면 목록이 있다 |
| 데모 페이지가 통째로 죽어 있었다 | `_demo.html` 이 **index.html 과 다른 스크립트 목록**을 들고 있었다(네 게임으로 가르기 전 것). 진단은 멀쩡해서 오래 몰랐다 | 다섯 판 모두 `_demo.html` 을 두고 index.html 과 **같은 목록**을 쓴다. 스크립트를 늘리면 `index.html` · `_test.html` · `_demo.html` · `sw.js` 넷을 같이 고친다 |
| 진단이 **돌린 시각**에 걸려 넘어졌다 | 사가고 토벌은 시간 칸이 열리고 45분만 머무는데, 진단이 `Date.now() + i*SLOT` 으로 훑어 **지금이 몇 분인지가 그대로 따라왔다**. 매시 45분 뒤에 돌리면 다섯 항목이 한꺼번에 실패(107→102) | 칸의 **첫머리부터** 훑는다. 천후도 같은 이유로 `weather.force('clear')` 로 붙들어 둔다 |
| 진단이 앞 항목이 남긴 상태에 걸려 넘어졌다 | 자동이 걸어 둔 목표·끊기지 않은 낚싯줄·임명된 태수가 다음 항목으로 흘렀다 | **항목은 자기가 쓸 상태를 스스로 만들고, 남긴 것은 스스로 치운다** |
| 옛 세이브 환산이 영원히 안 일어나거나, 첫 포획에 두 배로 들어왔다 | 환산(잡아 둔 마리수 → 영초)을 `st()` 안에 두었다. 읽기 경로(`info`·`herbOf`)는 칸을 만들지 않으니 환산이 안 돌고, 반대로 첫 포획에서는 `registerDex` 가 이미 올린 마리수와 겹쳐 영초가 셋이 아니라 여섯이 됐다 | **부트에서 한 번 도는 `migrate()`** 로 떼어냈다(`game.js`, 플래그 `save.petGrowV`). `st()` 는 빈 칸만 만든다. 세이브를 고치는 일은 읽기 함수에 얹지 않는다 |
| 새 진단이 통과했는데 **옛 항목이 깨졌다** | 승화 항목이 청룡에 연성 7단을 남겼다. 뒤에 있던 "펫 보정은 성장 배율 뒤에 더해진다" 가 `diff === pet.bonus.value` 를 기대하는데 배율이 붙어 어긋났다 | 인계 문서에 이미 있는 규칙을 새 항목이 어긴 것이다 — **항목은 자기가 쓸 상태를 스스로 만들고, 남긴 것은 스스로 치운다.** 승화가 올린 상위 종의 도감 항목과 `petGrow`·`dust` 를 끝에서 되돌린다 |
| 데모 스크린샷에 강타 예고가 안 잡힌다 | 예고는 **1.1초**다. `duel.open()` 뒤에 상태를 만들어도 rAF 가 한 프레임만 돌면 지나가 버린다 | 데모에서만 `ds.tell = 999` 로 붙들어 둔다. 게임 규칙은 그대로. 상태를 만든 뒤 **속공을 한 번 눌러야** 화면이 그 상태로 다시 그려진다(refresh 가 doAct 안에서 돈다) |
| 진단이 통과했는데 사실 우연이었다 | 성채 배율을 검사하며 **센 인물 셋**을 세웠더니 승률이 천장에 붙어 배율 차이가 38승 vs 35승으로 묻혔다 | 능력치 합으로 **약한 셋**을 골라 60판씩 굴린다 → 43승 vs 25승. 배율·확률을 검사하는 항목은 **차이가 드러나는 구간**에 세워야 한다 |
| **AI 가 영영 출진하지 않아 판이 얼어붙는다** | AI 가 칠지 말지를 **어림식**(병력 배수 문턱)으로 쟀다. 그 식이 **성벽 초깃값**만 보는데 실제로는 공성 중에 성벽이 깎인다 — 판정이 두 벌이 되어 어긋난 만큼 얼었다(120개월에 다섯 번) | **AI 가 진짜 전투식을 사본에 그대로 굴린다**(`war.forecast`). 성향은 "얼마나 잃어도 받아들이는가"(lossCap)만 정한다. 사본이므로 성벽·병력·장수를 안 건드리고, 일기토는 `dry` 로 건너뛴다 |
| 반대로 **함락률이 98%** 가 된다 (263전 259함락) | 공격 쪽만 이웃 성에서 병력을 끌어모을 수 있었다. 수비는 그 성의 수비대뿐이라 **사실상 수비가 없는 판**이었다 | 수비도 이웃한 제 성에서 **구원군**을 부른다(`war.reinforce`, 한 성당 사할 — 공격의 오할보다 적게 둔 것은 치는 쪽이 때를 고르기 때문이다) |
| 첫해에 판이 통째로 쓸려 나간다 | AI 가 뒷마당을 **텅 비운 채** 전군을 몰아쳤다. 가만히 둔 유비가 한 달, 조조가 넉 달 만에 멸망 | **제 성이 비면 못 준다**(`rtk-ai.spareOf` — 맞닿은 적 수비의 6할은 남긴다). 출진하는 성도 다른 적 앞에서는 다 비우지 않는다 |
| 전투가 아무리 붙어도 **양쪽이 열 명씩** 잃는다 | 손실식에 `/100` 이 남아 있었다. 부대의 힘이 이미 병력 규모라 거기서 또 나누면 손실이 소수점이 된다 | 계수를 0.055 로 두고 나눗셈을 걷어냈다. "힘이 엇비슷하면 열 합에 절반쯤 녹는다" 를 맞춘 값이다 |
| 성벽은 남았는데 **수백 명이 몇 달을 버틴다** | 함락 조건이 "성벽 0 **그리고** 수비가 공격의 절반 미만" 뿐이었다 | 지킬 군사가 남지 않으면(공격의 8% 이하) 성문이 열린다 |
| 진단 항목이 **누가 살아남느냐**에 걸려 넘어진다 | "열두 달을 굴려도 판이 성립한다" 가 내 세력의 생존을 전제했다. 세력이 무너지면 `endMonth` 가 멈춰 열두 달을 굴려 보지도 못한다 | 구경꾼처럼 `result` 를 지우며 굴린다. **재려는 축(판) 옆의 다른 축(생존)은 붙들어 둔다** 의 또 한 판 |
| 데모가 어느 장면을 열든 **세력 고르기**가 앞을 가린다 | `ui.init()` 이 "아직 안 선 판" 을 보면 덮개를 띄운다. 데모가 `init()` 뒤에 `setup()` 을 불렀다 | 데모는 판을 **먼저** 세우고 `init()` 한다 |
| 3D 로 바꿨더니 화면이 **새까맣다** | 지도 스타일이 `dark_all`(거의 검정)이었다. 2D 에서는 UI 뒤에 깔리는 배경이라 어두워도 됐지만, 3D 에서는 그 타일이 **지면 전체**가 된다. 픽셀을 읽어 보니 6,6,6 — 그 지도의 실제 색이었다 | 3D 는 밝은 지도(voyager)를 따로 부른다(`getTile(x,y,z,si)`). 손잡이 `world3d.mapStyle` |
| 3D 인데 하늘이 안 보인다 | `body.tilted #vignette` 가 2.5D 의 가짜 지평선을 만들려고 화면 위쪽을 거의 불투명하게 덮는다. 3D 용 규칙을 **그 앞에** 두었더니 특정성이 같아 나중 것(tilted)이 이겼다 | `body.r3d #vignette` 를 **tilted 규칙 뒤에** 둔다. CSS 는 특정성이 같으면 나중이 이긴다 |
| 헤드리스 스크린샷에 3D 가 **빈 캔버스**로 찍힌다 | WebGL 은 그린 직후 버퍼를 비운다(`preserveDrawingBuffer:false`). rAF 가 거의 안 도는 그 환경에서는 캡처 시점에 이미 비어 있다 | 데모에서만 `DG_3D_PRESERVE` 로 버퍼를 붙들고, `setInterval` 로 계속 그린다(rAF 함정의 3D 판) |
| three 를 넣었더니 PC 단독판이 안 돈다 | three 는 **ESM 만** 배포되는데 단독판은 `file://` 로 열려 `<script type="module">` 이 CORS 로 막힌다 | `build/three` 에서 **esbuild 로 IIFE**(`window.THREE`)로 묶어 커밋한다. 산출물만 커밋하고 `node_modules` 는 `.git/info/exclude` |
| 새 항목이 통과해야 하는데 넘어졌다 (코드는 멀쩡했다) | 반려 항목이 "거리마다 영초가 나온다" 를 두 몫으로 쟀는데, **첫 몫에서 우애가 깊어져** 둘째 몫이 더 나왔다. 재려는 축(거리) 옆에서 다른 축(우애)이 같이 움직인 것이다 | 재지 않는 축은 **끝값으로 붙들어 둔다**(우애를 막역으로 올려 두고 잰다). "차이가 드러나는 구간에 세운다" 와 짝이 되는 규칙이다 |
| 이 판의 그림 양식이 통째로 사라진다 | `sprite.js` 는 다섯 벌 복사본인데 **`saga-dungeon` 것만 `diabloize` 가 더 들어 있다**(2026-08-26, 사용자 지시 "다 달라야 함"). 다른 판의 파일을 복사해 덮으면 그 양식이 없어지고 기본값 `'diablo'` 가 STYLES 에 없어 화면이 예전 모습으로 돌아간다 | 도감을 넓힐 때는 **다섯 곳을 각각** 고친다(`LOOKS`·`BEAST_*` 는 다섯 판이 같다). 통째 복사는 하지 않는다. 양식 비교는 `saga-dungeon/_styles.html` |
| 3D 로 걸었더니 **야생 대상이 한 마리도 안 보인다** | 3D 카메라는 원작 감각(아바타가 화면의 1/8)에 맞춰 27m 앞을 보는데, 이 판의 스폰은 **70~320m 밖**에 생긴다(원작은 코앞에 나온다). 크기 보정만으로는 안 된다 — 애초에 화면 밖이다 | **줌**을 넣고(휠·핀치·`+`/`-`, ×0.7~×9) 기본을 ×4 로 뒀다. 판정은 안 건드렸다. 근본 해결은 스폰 반경을 줄이는 것인데 그건 균형이라 사용자가 정할 몫이다 |
| 크게 당겼더니 **헤드리스가 2분을 넘겨 죽었다** | 카메라가 높이 올라가면 절두체에 사물이 다 들어와 **그림자 맵**이 감당하지 못한다 | ×4 부터 그림자를 끈다. 경계에서 **한 번만** 갈아 끼운다 — 프레임마다 바꾸면 셰이더를 다시 컴파일한다 |
| 크게 당겼더니 **나만 점**이 됐다 | 먼 대상에는 크기 보정을 주면서 플레이어는 빼 두었다 | 나에게도 같은 보정을 준다 |
| 3D 지면에 **지도가 아예 안 깔렸다** (헤드리스 탓인 줄 알았다) | CARTO 는 `dark_all`·`light_all` 을 루트에 두는데 **voyager 만 `rastertiles/` 아래**다. 3D 는 밝은 지도를 부르므로 404 만 받아 지면이 종이색으로 남았다. "회사 프록시가 막는 모양" 이라고 잘못 짚고 넘어갈 뻔했다 — 크롬으로 타일 URL 을 직접 열어 보니 **404 페이지**가 찍혔다 | `MAP_STYLES` 에 `path` 를 두고 voyager 만 `rastertiles/voyager` 로 부른다. **안 되는 것을 환경 탓으로 돌리기 전에 그 URL 을 직접 열어 본다** |
| 지도 위에 **지도에 없는 호수**가 떴다 | 절차적 사물이 물을 원반으로 깔았는데, 지도가 깔리면 물은 이미 그려져 있다 | 절차적 사물이 맡을 것은 지도에 없는 **높이**뿐이다. 지도가 깔린 자리에서는 물을 세우지 않는다(`propPlan(kind, gx, gy, mapped)`) |
| 조우 무대의 카메라가 **빈 땅**을 봤다 | 무대를 열 때의 좌표에 붙박아 뒀는데, 조우 중에도 판정 층의 배회(`wanderSpawns`)는 그대로 돈다 — 상대가 걸어 나가 버렸다 | 무대 좌표를 **매 프레임 상대의 지금 자리로** 갱신한다 |
| 던진 사료가 **화면 밖으로** 날아갔다 | 출발점을 처음 한 번만 잡아 뒀더니, 카메라가 아직 무대로 오는 중이던 그 자리에서 던지는 궤적이 굳었다(사료가 30m 뒤에서 출발했다) | 던지는 손은 **늘 카메라 발치**다 — 매 프레임 카메라에서 다시 잡는다. 궤적 정점도 낮춰야 화면 안에 남는다 |
| 놓친 상대가 **공중에 떴다** | "물러선다" 를 y 축 상승(lift)으로 만들었다 | 물러섬·다가섬은 **뒤로 가는 것**이다(`back`). 그림자도 같이 옮긴다 — 안 옮기면 발과 그림자가 따로 논다 |
| 진단에서 화면 층이 **켜진 것으로 판정됐다** | 헤드리스 크롬에도 WebGL 이 있어서 `world3d.init` 이 성공한다. 1단계까지는 그리기를 안 부르니 티가 안 났는데, 조우 무대가 `world3d.active()` 를 보고 판정 경로를 가르자 진단 셋이 넘어졌다 | `DG_NO_DRAW` 면 **켜지도 않는다**(`world.js` 의 initCanvas). 화면 층도 스스로 `DG_NO_DRAW` 를 본다 — 방어를 두 겹으로 |
| 조우 카드의 조작 안내가 **옛 문구로** 떴다 | 무대를 `renderPet()` **뒤에** 열었다. 카드 문구는 3D 인지 아닌지를 보고 갈리는데 그때는 아직 무대가 없었다 | 무대를 먼저 열고 카드를 그린다 |
| 3D 인데 **얼굴이 화면을 채운다** | 카메라 거리를 각도(pitch)에서 파생시켰더니 3D 모드에서 수평거리가 14m 로 줄었다. 원작에서 아바타는 화면 높이의 1/8쯤이다 | 거리는 **사람이 화면에서 차지하는 몫**으로 정한다 — 키 3.4m 를 화각 52°로 담으려면 27m 다(`CAM_HIGH_MUL`·`CAM_BACK_MUL`) |
| 격자를 지날 때마다 GPU 메모리가 는다 | 건물마다 `new BoxGeometry(w,h,d)` 를 만들었다. 크기가 연속값이라 캐시가 안 되고 계속 쌓인다 | **단위 도형 하나를 scale 로 늘려 쓴다**. 대신 그 도형은 **모두가 나눠 쓰므로 dispose 하면 안 된다** — 하나를 버리면 남은 건물의 도형까지 사라진다 |
| 밤이 되니 배우가 실루엣만 남는다 | 반사광을 밤값으로 낮추면 지면과 함께 사람도 잠긴다 | 플레이어를 따라다니는 **작은 불 하나**(PointLight, 그림자 없음)를 `lamp` 값에 비례해 켠다. 원작의 밤 화면도 아바타 둘레만 환하다 |
| 물이 **네모난 파랑**으로 놓인다 | 수면을 격자 크기 평면으로 깔았다 — 한 칸만 물인 자리에서 격자가 그대로 드러난다 | 원반으로 깐다. 홀로 있으면 못이 되고 이어지면 강이 된다 |
| 밤에 걸어 들어간 동네만 등롱이 낮처럼 켜져 있다 | 등롱 켜기를 `syncLamps` 가 **한 번 훑고 지나가는 방식**으로 했다. 나중에 생긴 격자는 그 훑기 뒤라 기본값(켜짐)이 남는다 | 새로 세울 때 **그 자리에서** 지금이 밤인지 보고 정한다 |
| 커밋에 남의 파일이 딸려 들어갔다 | `git add -A tools` 가 다른 도구의 크롬 프로필·덤프까지 쓸어담았다 | **손댄 폴더만 지정한다.** 딸려온 것은 `def6450`·`1641c0c` 에서 추적 해제 |

## 이력 (C:\swbins, 리모트 없는 로컬 개인저장소)

```
94850f8  v1.0-full (경영·던전 원본이 여기 있다 — 되살릴 때 꺼내는 자리)
56603ed  경영 제거          eb72695  사관 톤
dcdd912  v1.9 (자동·던전복귀·PC단독판·그림책풍)
0f7e202  자동 시뮬 판 + 데모 세이브 보호
80eb695  서비스워커 세대 섞임 수정      7616c87  _swreset.html
e632c24  네 게임 분리
82c24e5  다섯 게임 · Saga 이름 · 사가국지 신설
1729516  가입해서 저장 + PC 단독판 5종
919cbfe  진단 무작위 씨앗 고정
362937a  사가고 교전(탭 전투)   fbab6ec  사가고 연성·승화(사탕·진화)
4593565  사가고 반려(버디)   (아래 두 줄은 같은 날 이어서)
c36bc18  사가고 걸음 배속 · 교전 무대(싸우는 모습)
ca67e8e  사가고 어드민(_admin.html) · 균형 손잡이
         사가고 3D 렌더러(three.js) 1단계
         사가고 3D 2단계 — 입체 배우(actor3d.js) · 시간대 조명 · 건물 밀도 · 조우 연출
         사가고 3D 3단계 — 조우 무대(encounter3d.js): 등용·포획을 3D 로
def6450 · 1641c0c  남의 파일 추적 해제
6e8c679  인계 문서       2e77eec  0.0.0.0 바인딩      2732be9  재시작 버튼 수정
7b67b00  도감 확장 (유럽사 22 · 포켓몬 16, 다섯 판)
8d8d98d  사가고 금 수입원 + 걷는 속도 34→8 m/s
a921fa8  사가고 역참(포켓스탑)   43907c3  천거장(알)   04e2e51  성채(체육관)
54d773c  사가스토리 보스        e84d94f  사가의숲 낚시·심기·계절
d8e427d  사가국지 사변·오답노트·태수 자질
74efbdd  사가고 행낭·천후        45d9003  사가고 사명·토벌
b1f5062  사가블로 세공(소켓·보석·부문)   94f96df  사가블로 정예·난도
         사가블로 디아블로2 외피(css/diablo.css · 조작판 · 격자 가방 · 등급색)
         사가블로 디아블로풍 스프라이트(sprite.js diabloize — 이 한 벌만 갈랐다)
         사가블로 글꼴 심기(Cinzel, OFL — 로마자·숫자만. 한글은 시스템 명조)
         사가블로 행상·투전·연단(vendor.js · forge.js — 원작의 상인·도박·큐브)
         사가블로 서고 참조 제거(없는 방을 있다고 말하던 자리)
         사가블로 단약·요대(potion.js — 원작의 물약과 1234 벨트, 스킬은 ZXCV 로)
         사가블로 감정(item.js — 미확인 물건과 감정서)
         사가블로 창고(item.js — 자동이 손대지 않는 자리)
         사가블로 내구·수리(item.js — 층마다 닳고 행상에서 고친다)
         사가블로 투장(data-set.js — 원작의 세트 아이템 네 벌)
         사가블로 저항(data-enemy.js — 결마다 따로. 면역은 두지 않는다)
         사가블로 원소 여섯 + 보석 재설계(data-elem.js — 무기=피해·갑주=저항)
         사가블로 직업 다섯·무예 45(data-skill.js·skill.js — 무기가 직업을 정한다)
         사가블로 고유 18(data-unique.js — 이름이 있고 옵션이 정해진 물건)
         사가블로 요구 수준·항아리·역참·결사(원작의 잔가지 넷)
```

## 이어서 하면 좋은 것 (우선순위 순)

0. **3D 전환** (2026-08-27 에 생긴 축) — 판마다 `PLAN.md` 에 계획이 서 있다.
   어느 판부터 갈지는 사용자가 정한다. 시작하면 **현재 구조 분석**부터고,
   한 단계가 끝날 때마다 그 판의 `_test.html` 이 여전히 만점인지 본다.
   진단이 도는 까닭은 **판정 층이 화면 없이도 돌기 때문**이다 — 3D 를 화면 층에
   얹는 한 계속 돈다. 판정을 3D 쪽으로 끌고 가면 그 자리에서 검증이 끊긴다.

1. **손으로 놀아 보기** — 다섯 판 모두 진단·정지 화면으로만 확인했다. 조작감(자동이
   답답한지, 사가블로 난이도, 사가국지의 문답 보상 균형)은 사람이 봐야 한다.
   서버를 띄우고(run.bat) 눌러 보는 게 가장 빠르다.
   **사가의숲은 어드민이 있어 이 일이 훨씬 쉽다** — `_admin.html` 의 '때' 탭에서
   한가위 밤·비 오는 날·일요일 아침으로 바로 가고, 'QA 프리셋' 으로 벌떼·이사·
   사고 코앞 같은 상황을 한 번에 만든다. 사가고에도 어드민이 있다.
   **2026-08-26 — 사가고는 사용자가 실제로 걸어 봤다.** 그 자리에서 둘이 드러났고
   둘 다 고쳤다: 3D 지면에 지도가 안 깔리던 것(voyager 만 경로가 다르다 · `b411c12`)과
   야생 대상이 화면에 한 마리도 안 들어오던 것(줌을 넣었다 · `d603114`).
   **남은 결정 하나** — 대상이 70~320m 밖에 생기는 것(`SPAWN_RADIUS`)은 원작(코앞)과
   다르다. 줄이면 기본 배율에서도 보이지만 **조우까지 걷는 거리가 짧아져 보급·반려·
   자동 순행 수치가 다 같이 움직인다.** 균형이라 손대지 않았다 — 사용자가 정할 몫이다.
   아직 못 잰 것: **실기기 fps**(헤드리스는 rAF 가 안 돌아 `#perf` 를 못 쓴다).

   **2026-08-26 (이어서) — 사용자가 폰으로 사가고를 열었다.** 공개 주소
   (GitHub Pages)로 들어갔고, 말은 "지형지물이 크다 · 최적화" 였지만 물어보니
   **"최적화라기보다 가능하게"** — 쓸 수 있는 상태가 아니었다는 뜻이다. 둘이었고
   둘 다 고쳤다. **`6e43030` 으로 커밋했다**(2026-08-26). 손댄 파일: `index.html` · `css/style.css` · `js/game.js` ·
   `js/account.js` · `js/world3d.js` · `sw.js` · `dist/사가고.html`(재빌드).

   · **상단 도구가 화면 밖으로 잘려 있었다.** `#top` 그리드의 tools 칸이 `auto` 라
     줄지 않는데 도구가 **아홉**이라(👤 는 `account.js` 가 끼운다) 390px 화면에서
     **416px 을 달라 하고 160px 이 밖으로 밀려났다.** 뒤쪽 서넛은 보이지도 눌리지도
     않았다. `⋯` 서랍으로 접었다 — 상시로 남는 것은 🤖 자동, 📡 위치 둘.
     **단추를 옮기지 않은 것이 요점이다**: 서랍(`#tools-drawer`)은 넓은 화면에서
     `display: contents` 로 풀려 안의 단추가 도구줄에 그대로 선다. 그래서 PC 는
     여태 그림 그대로고 이벤트도 다시 걸 것이 없다. 접기는 **600px 이하**에서만.
     `⋯` 토글은 `game.js` 의 `bindMore()`, 👤 는 `account.js` 가 서랍에 넣는다
   · 같은 부류 둘을 덤으로 잡았다. **독**은 `right` 만 잡혀 있어 일곱이 되는 때
     (던전이 열린 뒤) 왼쪽으로 화면을 넘어간다 → 양쪽 고정. **근처 카드**는
     `left:50%` 라 쓸 수 있는 폭이 화면 절반(195px)뿐이라 '가까이 가기' 가 석 줄로
     쪼개졌다 → 양쪽 잡고 가운데로
   · **지형지물이 큰 것은 폰 탓이 아니다.** three 의 fov 는 **수직** 기준이라,
     세로로 긴 폰(가로세로비 0.53)에서는 **가로 시야가 PC 의 절반 밑으로** 좁아진다.
     이 판의 거리·높이 값이 PC 가로 화면에서 잡혔기 때문이다. `world3d.js` 에
     `fovFor(w, h)` 를 두어 **시야각만 벌린다** — 폰 52° → **78.9°**, 가로 화면은
     52° 그대로. 손잡이 넷: `world3d.portraitFit`(0 이면 옛 그림) ·
     `portraitMax`(1.8) · `fov`(52) · `fovMax`(80)
   · **`sw.js` 세대를 `go-v3.3.0` → `go-v3.4.0` 으로 올렸다.** 안 올리면 이미 한 번
     연 폰이 옛 캐시를 계속 받는다 — 고쳐도 폰에서는 안 바뀐다

   **남은 것 셋.** ① **실기기 fps 는 여전히 못 쟀다** — 이번 건은 레이아웃과
   시야각이라 그리는 양은 그대로다. 폰에서 버거우면 `world3d.density` → 0.5,
   그래도 무거우면 `world3d.mesh` → 0. ② `_demo.html` 의 상단은 `index.html` 과
   **별도 마크업**이라 폰에서 열면 여전히 잘린다(개발용이라 두었다 — 맞추려면
   같은 서랍 마크업을 넣으면 된다. 데모도 `game.js` 를 부르니 토글은 그대로 돈다).
   ③ 공개 주소로 보려면 **커밋·푸시가 있어야 한다.**

   **2026-08-26 — 사용자가 사가블로를 손으로 놀아 보는 중이다.**
   주옥·소리를 넣은 직후이고(`982d7ec`), 이 세션은 여기서 끊는다.
   **다음 세션은 먼저 "놀아 보니 어떻더냐" 를 묻고 거기서 시작한다** — 코드부터
   손대지 말 것. 소리는 이 판에 처음 들어간 축이라 특히 사람 귀가 필요하다:
   크기(`settings.soundVol` 기본 0.5) · 타격음이 잦아 시끄러운지 · 종소리가
   실제로 "손이 멈추는" 소리인지. 눌러 볼 자리는 이렇다.
   · **소리는 아무 데나 한 번 누른 뒤부터 난다**(브라우저 규칙). 안 나면 🔊 를 눌러 본다
   · **주옥은 제4층부터** 드물게 나온다 — 🔨 세공 시트 '가진 것' 아래 ◈ 칸에 쌓인다
   · **서버판과 단독판은 세이브가 서로 다른 곳에 남는다**(출처가 다르다).
     8792 로 놀던 진행은 `dist/사가블로.html` 을 더블클릭해도 안 이어진다
   버거우면 어드민의 `world3d.density` → 0.5, 그래도 무거우면 `world3d.mesh` → 0

   **2026-08-26 — 사가의숲의 공사(지형 개조)를 넣고 이 세션은 여기서 끊었다.**
   사용자가 "해보고 새로운 세션에서 이어 할게" 라고 했다.
   **다음 세션은 코드부터 손대지 말고 먼저 "깔아 보니 어떻더냐" 를 묻는다.**
   눌러 볼 자리는 이렇다.
   · 어드민 `_admin.html` 의 QA 프리셋 **🪧 공사 채비** — 개토패와 금 20만을 한 번에 준다.
     (손으로 하려면 전방에서 개토패 🪧 **4,800**)
   · 사면 **독에 🪧 공사 단추가 곧바로 선다**. 시트는 선 칸 둘레 **3×3** 격자다
   · 봐야 할 것 — ① 3×3 이 답답한지(원작은 앞 칸 하나다) ② 삯이 싼지 비싼지
     (특히 **모래펄 1,400** — 조개·낚시터가 따라오므로 값이 문턱이다)
     ③ 걸어가며 격자가 따라오는 것이 자연스러운지 ④ 되돌리기를 실제로 쓰게 되는지
   · **`1fc7575` 로 커밋했다**(2026-08-26). 개명 커밋과 부딪히지 않게 경로를 집어
     따로 넣었다
   · 손댄 것: 새 파일 `js/terrain.js` · `village.js`(덮개층·사물 id) ·
     `data-village.js`(돌길·개토패) · `ui.js`(공사 시트·syncDock) · `css/style.css` ·
     `admin.js`(삯 배수 손잡이·QA 프리셋) · `game.js`(도움말) · 각 HTML · `sw.js` v0.15.0
2. ~~**허브 등록**~~ — **2026-08-25 완료.** `C:\swbins\services.json` 에 다섯 판을 넣고
   `pages/index.php` 아이콘 맵을 맞췄다. 키는 **폴더 이름 그대로**(밑줄이 아니라 하이픈) —
   허브가 `logs/<key>.log` 를 키로 만들기 때문이다:
   `saga-go`(🏯,8791) · `saga-dungeon`(⚔️,8792) · `saga-forest`(🌳,8793) ·
   `saga-story`(🍁,8794) · `saga-realm`(👑,8795).
   폴더가 없어진 옛 `yeoksa`(`deungyong-go`) 항목은 뺐다. 워치독(`task`)은 붙이지 않았다 —
   허브 카드의 **시작** 버튼으로 그때그때 띄운다. 허브에 정지 기능은 없다.
   **이 두 파일은 여전히 커밋하지 않는다**(아래 "하지 말 것").
   **2026-08-25 추가** — 다섯 판 `run.bat`·`start_server.bat` 의 바인딩을 `--bind 0.0.0.0` 으로
   열었다(커밋 `2e77eec`). 그전에는 `127.0.0.1` 이라 허브 카드 링크(`hubHostNames()` 가 만드는
   `http://<이-PC이름>:879x`)로는 닿지 않았다 — 상태 확인은 127.0.0.1 로 하니 카드는 "동작 중"인데
   링크는 "사이트에 연결할 수 없음" 이었다. 이제 **호스트명·127.0.0.1 둘 다** 열린다.
   폰에서 사가고를 열 때는 여전히 `run-phone.bat`(0.0.0.0 + HTTPS) 이다 — 아이폰 사파리가
   비보안 출처의 위치 API 를 막는다. 폰·다른 PC 의 첫 접속은 방화벽 인바운드가 걸릴 수 있다.
   같은 판의 **재시작** 버튼도 이제 동작한다(커밋 `2732be9`) — 종료 대상 필터가 `php|node` 라
   파이썬 서버를 못 죽이고 있었다.
3. **원작 모방을 더 밀기** (사용자 뜻: "완전 동일하게 모방")
   - ~~도감 넓히기~~ — **2026-08-25 완료**(커밋 `7b67b00`). 유럽사 22인 · 원작 짐승 16종.
     인물 48→70, 펫 25→41. 다섯 벌을 같이 고쳤고 `sw.js` 세대도 같이 올렸다.
     더 넓힐 때 손댈 곳은 게임당 셋: `js/data.js`(HEROES·PETS·해설) ·
     `js/sprite.js`(LOOK · BEAST_SHAPE · BEAST_COLOR · BEAST_PATTERN) · `_test.html`(개수 기대치).
     **유럽 인물은 LOOK 에 반드시 등록한다** — 빠뜨리면 규칙이 갓·도포를 씌운다
   - ~~사가고: 원작 모방~~ — **2026-08-25 완료. 원작의 축을 다 옮겼다.**
     **역참**(🏮 포켓스탑 `station.js` `a921fa8`) · **천거장**(✉️ 알 `letter.js` `43907c3`) ·
     **성채**(🏯 체육관 `fort.js` `04e2e51`) · **행낭**(🎒 가방 `bag.js`) ·
     **천후**(날씨 `weather.js`, 둘 다 `74efbdd`) ·
     **사명**(📋 필드 리서치 `quest.js`) · **토벌**(⚔️ 레이드 `raid.js`, 둘 다 `45d9003`).
     금 수급과 걷는 속도 8m/s 도 같은 날(`8d8d98d`).
     **2026-08-25 추가 — 교전(交戰)**(⚔️ `duel.js`): 원작의 **탭 전투**를 옮겼다.
     성채·토벌이 여태 버튼 한 번에 수치를 다 계산해 결과만 뿌렸다 — 연출도 조작도
     없었다. 이제 속공을 연타해 기(氣)를 쌓고, 만충이면 필살을 지르고, 강타 예고에
     피한다(60초). **판정 층과 화면 층을 갈라 두었다** — 헤드리스에서 rAF 가 거의
     돌지 않으니 진단 열셋은 판정 층만 직접 굴린다. **기존 판정은 그대로 살아 있다**:
     `raid.fight()`·`fort.challenge()` 를 인자 없이 부르면 예전과 똑같다(자동 순행과
     옛 진단이 그 길). 교전을 거치면 토벌은 **낸 피해가 곧 판정**, 성채는 성과가
     배율(0.65~1.45)로 얹힌다. 진단 107 → **120**. 데모에 `#duel`·`#dueltell`.
     **2026-08-25 추가 — 연성(鍊成)·승화(昇華)**(🌿 `growth.js`): 원작의 **사탕과
     진화**다. 잡아도 도감에 줄이 하나 늘 뿐이었는데, 원작에서 잡는 이유의 절반이
     사탕이다. 잡으면 그 종의 **영초** 셋과 등급만큼의 **단사**가 들어오고(종별/공통,
     원작의 사탕/별사탕), 둘을 써서 20단까지 **연성**하면 장착 보정이 단마다 6% 오른다.
     **승화**는 근거가 뚜렷한 **여섯 사슬만** — 잉어→청룡(데이터의 설명이 이미
     "용문을 오르면 용이 된다던가" 였다) · 호랑이→백호 · 학→주작 · 곰→불가사리 ·
     불꼬리→화룡 · 뜀잉어→이무기. 연성 단계는 승계된다.
     **사슬은 `growth.js` 에만 둔다** — `data.js` 에 `evolve` 칸을 만들면 다섯 판이
     나눠 든 같은 도감의 동일성이 깨진다. 진단 120 → **132**. 데모에 `#petgrow`.
     펫 연성은 **인물 승급과 다른 축**이다: 인물은 도감의 중복분을 직접 태우고,
     펫은 자원을 모은다. 도감 마리수는 기록이니 줄지 않는다.
     **2026-08-26 추가 — 반려(伴侶)**(🐾 `buddy.js`, 커밋 `4593565`): 원작의 **버디**다.
     원작에서 걷는 이유는 둘인데(알·버디) 이 판엔 천거장뿐이었다. 포획한 종 하나를
     곁에 세우면 등급별 거리(★1 1km ~ ★5 5km)마다 그 종의 **영초**가 나오고,
     함께 걸은 거리(+ 사료 한 줌 = 300m)로 우애가 깊어진다 —
     면식 → 친교(2km) → 지기(10km) → 막역(30km). 깊어질 때마다 단사가 들어오고
     한 몫의 영초도 는다(1→4). 규칙 셋은 원작 그대로다:
     **우애는 종별로 남고**(갈아타도 안 지워진다 — 이 판의 펫은 개체가 아니라 종이다),
     **장착 보정(최대 +10%)은 곁에 세워 둔 동안에만** 붙으며,
     **사료는 우애만 올리고 영초는 안 준다**(사료는 포획에도 드니 순환이 생긴다).
     영초·단사 수급은 `growth.js` 한 곳으로만 넣는다(`addHerb`·`addDust`).
     반대로 `growth.bonusOf` 가 buddy 에게 배율을 물어본다 — 세우지 않았으면 1 이라
     이 축이 없던 때와 값이 같다. **동행(party)과 다른 말이다** — 동행은 사람 다섯,
     반려는 짐승 하나. 진단 132 → **146**. 데모에 `#buddy`(세우기 전) · `#pet`(세워 둔 쪽).
     **2026-08-26 추가 — 걸음 배속과 교전 무대**(커밋은 아래 이력).
     *배속*(`×1 → ×4 → ×16`, `world.js`): 반려·천거장처럼 **킬로미터로 세는 축**을 손으로
     확인하려면 8m/s 로는 10분이 든다. **이동만 곱하고 규칙은 한 줄도 건드리지 않는다** —
     보급 250m·반려 거리는 그대로라 배속으로 걸어도 얻는 것은 그만큼 실제로 걸은 값과 같다.
     `save.settings.speed` 에 남고 버튼이 늘 `×n` 을 보여 준다(켠 채 잊지 않게).
     *무대*(`duel.js`·`style.css`): 교전에 **싸우는 모습이 없었다** — 적 초상 하나와
     게이지뿐이었다. 이제 **동행 선두와 적장이 마주 서서** 속공에 뛰어들고(`lunge`),
     맞으면 번쩍이며 밀리고(`hurt`·`blast`), 예고에 맞춰 피하면 옆으로 샌다(`evade`).
     동작은 전부 **CSS 애니메이션**이라 판정 층과 무관하다 — 헤드리스에서 안 돌아도
     결과는 같다. 그림책풍 초상은 종이 바탕째 그려지므로 무대에서는 **삽화 액자**로
     테를 둘러 붕 뜨지 않게 했다. 진단 146 → **149**.
     **2026-08-26 추가 — 어드민(`_admin.html`)과 균형 손잡이.** QA 가 쓸 운영판이다.
     탭 여섯: *세이브*(재화·행낭·쉬는 시간) · *도감/반려*(도감 토글, 반려와 우애,
     연성 단계·영초·단사) · *균형 손잡이* · *QA 프리셋*(시나리오 한 번에 + 스냅샷 3칸) ·
     *데이터 조각*(인물·펫을 넣으면 `data.js`·열전·`LOOK` 조각을 만들어 준다 —
     **다섯 판에 같이 붙여야 한다**고 화면이 일러 준다) · *점검·백업*(세이브 JSON 왕복).
     탭은 주소에 남고(`#tune`), `#selftest` 로 **도구 자체를 점검**한다(9/9).
     구조에서 지킨 것 셋:
     · **손잡이는 세이브가 아니라 따로 사는 칸**(`deungyong-go/tune`)이다 — 세이브에
       섞으면 프로필마다 규칙이 달라지고 세이브를 넘길 때 규칙까지 따라간다.
     · **자가진단·데모는 손잡이를 읽지 않는다**(`DG_NO_TUNE`). 그 자리에서는 `setTune`
       도 메모리에만 남는다 — 진단이 저장하면 실제 게임이 그 규칙으로 돌아 버린다.
     · **어드민이 이긴다.** 저장하면 `…/admin/poke` 를 두드리고, 게임 창이 `storage`
       이벤트로 세이브를 다시 읽는다. 안 그러면 게임이 10초마다 하는 persist 가
       어드민이 고친 값을 곧 덮는다.
     걸음 배속(`world.speedMul`)도 손잡이다 — **매 프레임 읽어 새로고침 없이 듣는다.**
     자가 점검은 **전용 프로필**(`…/save/_admintest`)로 갈아타 돌리고 그 칸을 지운다.
     **2026-08-26 추가 — 3D 렌더러(`world3d.js`).** 사용자가 "포켓몬고와 완전 같게" 를
     골랐다(연출·유사3D·진짜 3D 셋 다, **사가고만**). 첫 단계로 **three.js r169** 를
     들였다. 지도 타일을 지면에 깔고, 지도에 없는 높이는 `terrainAt` 격자로 절차적으로
     세우고(건물·나무·산·물), 인물·짐승은 `sprite.js` 그림을 빌보드로 세운다.
     해 하나가 그림자를 만들고 카메라가 낮게 깔려 따라온다.
     **판정에는 한 줄도 닿지 않는다** — 좌표·스폰·거리는 `world.js` 것을 읽기만 한다.
     WebGL 이 없으면 조용히 2D 로 돌아가고, 손잡이 `world.render3d` 로 끌 수 있다.
     three 는 **ESM 만 배포**되는데 PC 단독판은 `file://` 로 열려 `type="module"` 이
     막힌다 — 그래서 `build/three` 에서 **esbuild 로 IIFE 한 덩이**(665KB)로 묶어
     `js/vendor/three.iife.js` 로 커밋했다(`node_modules` 는 `.git/info/exclude`).
     PC 단독판은 500KB → **1,196KB**. 진단은 149/149 그대로다(그리기를 안 켠다).
     **2026-08-26 이어서 — 3D 2단계. 1단계에서 남겨 둔 넷을 다 옮겼다.**
     · **배우가 입체다**(`actor3d.js` 신설) — 사람·짐승·역참·성채를 도형으로 조립한다.
       외형은 `sprite.js` 의 것을 읽으므로(`lookOf` 의 갓·도포·망토·무기,
       `beastFormOf`·`beastColorOf`·`beastPatternOf`) 지도 위의 인물과 도감 초상이
       같은 사람으로 보인다. **3.5등신** — 실사 비례면 지도에서 머리가 점이 된다.
       걸으면 다리가 엇갈리고 진행 방향으로 돈다(**방향은 판정에 없다** — 화면 층이
       프레임 사이의 좌표 차이로 만든다). 되돌리려면 손잡이 `world3d.mesh` 를 0 으로
     · **시간대 조명**(`lightingAt`) — 시각과 천후만 보고 해의 높이·색·세기, 하늘색,
       안개 거리, **지도 타일에 곱하는 물감**을 낸다. 지도 이미지는 늘 한낮 그림이라
       안 곱하면 밤에 땅만 대낮이다. 밤에는 길가 등롱과 플레이어를 따라다니는 불
       하나가 켜진다. 손잡이 `world3d.dayNight`
     · **건물 밀도**(`propPlan`) — 격자마다 **도심도**를 뽑아 번화한 칸은 높은 집이
       여덟 채까지, 변두리는 기와집 두엇에 나무가 섞인다. 길가에 등롱, 빈 들에 풀·바위.
       잔 사물은 가까운 절반 안에서만 세운다. 손잡이 `world3d.density`
     · **조우 연출** — 조우 창이 열리면 카메라가 나와 대상 사이로 돌고, 닫히면 놓아
       준다. 잡히면 빛기둥이 서고 사라지는 대상은 떠오르며 흩어진다.
       창이 열렸는지는 **DOM 을 보고 안다** — `encounter.js` 를 고치지 않으려는 것이다
     넷 다 **판정에는 한 줄도 닿지 않는다.** 그래서 값을 내는 함수
     (`lightingAt`·`propPlan`·`camAim`·`actor3d.plan`)를 three 없이도 돌게 갈라 두고
     자가진단이 그것만 굴린다 — 진단 149 → **172**. 데모에 `#3d-noon`·`#3d-dusk`·
     `#3d-night`·`#3d-meet`(카메라)·`#3d-meetbeam`(빛기둥).
     원작 에셋은 복제하지 않는다 — 그림은 계속 직접 그린다.
     **2026-08-26 이어서 — 3D 3단계, 조우 무대**(`js/encounter3d.js` ·
     `css/encounter3d.css`). 지도는 3D 가 됐는데 **원작에서 가장 오래 보는 화면**인
     조우만 2D 카드로 남아 있었다. 이제 대상을 누르면 그 자리에 상대가 서서 이쪽을
     보고, 카메라가 상대 앞으로 간다(거리는 **상대 키에 비례** — 사람과 물뿜이를 같은
     자리에서 보면 하나는 얼굴이 화면을 채운다). 조우 카드는 아래로 내려앉고 지도
     UI 는 걷힌다(CSS 한 장이라 `<link>` 를 빼면 예전 화면이다 — 사가블로 외피와 같은 방식).
     · **조준 고리** — 막대 위를 오가던 바늘을 **원의 반지름으로 옮긴 것**이다.
       흰 고리가 초록 테 안에 들어왔을 때 던지면 적중. **막대에서 맞는 값은 원에서도
       맞는다**(진단이 201칸을 훑는다) — 같은 값을 다른 모양으로 잰 것뿐이라 판정은
       한 줄도 안 바뀐다
     · **던지기** — 화면을 위로 밀거나 버튼·스페이스. 셋이 `encounter.throwNow()`
       **한 문**으로 들어간다. 사료가 날아가 맞은 뒤 결과 창이 뜨고, 그 시각은
       **타이머가 잡는다**(rAF 가 거의 안 도는 자리에서도 창은 떠야 한다).
       판정은 던진 순간에 이미 끝나 있다 — 늦게 뜨든 곧바로 뜨든 결과는 같다
     · 잡힌 상대는 무대가 닫힐 때까지 **세워 둔다**(판정에서는 이미 지워졌다).
       안 그러면 날아가는 중에 상대가 먼저 사라져 허공에 던지는 그림이 된다
     진단 172 → **178**. 손잡이 `world3d.stage3d` 를 0 으로 두면 예전 카드 화면이다.
     **2026-08-26 이어서 — 적도(賊徒)**(`js/rogue.js`): 원작의 **로켓단**이다.
     "남은 것은 팀 선택·친구·교환뿐" 이라고 적어 뒀는데 **하나 더 있었다** —
     역참이 여태 늘 착한 자리였다. 원작에서 포켓스탑은 이따금 검게 물들고,
     거기 선 자를 물리쳐야 원래대로 돌아오며, 이긴 자리에는 **검게 물든 것**이 남는다.

       로켓단이 스탑 점거 → 적도가 역참 점거      깃발이 검어지고 등롱이 꺼진다
       그런트/리더/보스   → 졸개(卒)·두목(頭目)·수괴(首魁)
       3연전 배틀         → 교전 한 판 (`duel.js` 를 그대로 쓴다)
       섀도 포켓몬        → **암영(暗影)** — 이겨도 도감에 바로 들지 않는다
       퍼리파이           → **정화(淨化)** — ✨단사를 써서 푼다

     구조에서 지킨 것 넷:
     · **어느 역참이 언제 점거되는지는 시각과 역참 키의 해시**다(토벌과 같은 방식).
       서버가 없어도 어느 기기에서나 같다. 30분 칸에 20분 머물고, 칸마다 역참의 두 할.
     · **머무는 시간은 칸의 첫머리부터 잰다.** 그리고 `rogue.force(false)` 를 처음부터
       달아 뒀다 — 안 그러면 **역참을 쓰는 옛 진단이 돌린 시각에 걸려 넘어진다**
       (토벌에서 이미 밟은 함정이다). 진단은 머리에서 붙들고, 적도 자신을 재는
       열한 항목만 그 자리에서 풀었다 다시 묶는다
     · **암영은 종(種)에 붙는다.** 이 판의 펫은 개체가 아니라 종이므로 개체를 만들면
       도감·장착·연성이 한꺼번에 뒤집힌다(이 문서의 "권하지 않는 것" 그대로다)
     · **지도가 프레임마다 묻는 길은 값싸게 갈랐다**(`slotAt`/`rankAt` 은 해시만 본다).
       거기서 도감 70인·41종을 훑으면 그림이 그 자리에서 무거워진다
     정화는 **연성과 같은 자원(단사)을 다툰다** — 그게 이 축의 선택이다. 정화하면
     도감에 들고 그 종의 영초를 얹어 준다. 안 하면 도감 맨 위 **암영 칸**에 남는다
     (탈환 화면에서 '나중에' 를 눌러도 잊히지 않게 도감 안에 자리를 뒀다).
     **도감에 넣는 문을 하나로 모았다** — `encounter.gainPet`. 손·자동·정화 셋이
     같은 절차를 따로 들고 있으면 금 수급 하나 붙일 때 세 곳을 고쳐야 한다
     (`gainHero` 가 같은 이유로 먼저 있었다). 진단 178 → **189**.
     데모에 `#rogue`·`#roguewin`·`#dark`. 어드민에 손잡이 여섯(`rogue.*`)과
     QA 프리셋 둘. 사명에도 한 줄 늘었다(🏴 점거된 역참을 되찾는다).
     **이제 원작에서 남은 것은 팀 선택·친구·교환뿐이고, 싱글에는 넣지 않는다.**
     남은 것은 싱글에 뜻이 없는 축뿐 — 팀 선택 · 친구 · 교환.
     **장비는 되살리지 않는다** — 원작에 없는 축이라 다섯 판으로 가를 때 던전으로 보낸 것이다.
     진단 62 → **107**. `_demo.html` 도 이때 되살렸다 — 네 게임으로 가르기 전 스크립트
     목록(서당·던전·장비)을 그대로 부르고 있어 **통째로 죽어 있었다**(눈으로 볼 유일한 수단이다).
     README 도 같은 이유로 손봤다
   - ~~사가의숲: 낚시 미니게임·심기·계절·낚시터 무한 반복~~ — **2026-08-25 완료**(`e84d94f`).
     낚시는 던지고 **입질을 기다렸다 당긴다**(0.7초 창) — 시간이 드니 무한 반복이 저절로
     풀렸다. 심기는 사흘, 계절은 실제 달을 보고 나는 것이 달라진다.
   - ~~사가의숲: 그림까지 원작대로~~ — **2026-08-26 완료.**
     사용자 지시 **"사가의숲은 완전 동물의숲이랑 동일해야해 그래픽까지도"**.
     `village-view.js` 를 통째로 다시 썼다. 원작이 그렇게 보이는 까닭은 그림체가 아니라
     **투영**이라, 그것을 옮겼다 — **구면(원통) 지평선**(마을을 한눈에 못 본다) ·
     **좌우 처짐** · **사람을 늘 한가운데 두는 카메라**(마을 밖은 물이라 저절로 섬이 된다) ·
     **비스듬한 3/4 시점**. 그 위에 **이모지를 걷어내고 도형으로 그린 사물**
     (겹친 원 여섯의 수관 · 계절을 타는 수관 색 · 삼단 원뿔 소나무 · 한옥 전방) ·
     **말풍선 이름표** · **해·달·별·구름·먼 산** · **눈과 꽃잎** · **기본 2등신**.
     땅빛도 파스텔로 갈았다(`data-village.js` 의 TILES·SEASONS·PHASES).
     자세한 것은 `saga-forest/README.md` 의 "그림 — 원작의 투영을 그대로".
     진단 38/38 그대로, 세 번 돌려 한 줄도 다르지 않다.
   - ~~사가의숲: 곤충 채집 · 집 안 꾸미기 · 주민 이사와 편지~~ — **2026-08-26 완료.**
     원작에 남아 있던 세 축을 같은 날 이어서 옮겼다. 파일 셋이 늘었다 —
     **`bug.js`**(곤충) · **`home.js`**(집) · **`mail.js`**(편지·이사).
     · **곤충 13종** — 잠자리채가 없으면 못 잡고, 계절과 **시간대**를 함께 탄다
       (반딧불이는 여름 밤, 겨울은 거미 하나뿐). **성큼 걸으면 달아나고
       살금살금(Shift · 🐾)이면 안 달아난다** — 다가가는 것이 놀이다
     · **집** — 전방에 날마다 가구 넉 점, **선 자리에 놓는다**(심기와 같은 규칙),
       **증축은 빚을 지고 넓힌 뒤 갚는다**(원작의 융자), 계열이 맞으면 평가가 오른다.
       **실내는 휘지 않는다** — 마을은 구면인데 방은 평평한 3/4 다. 그 차이가 "안에
       들어왔다" 를 만든다. 집은 겉모습도 바뀐다(초가 → 기와)
     · **이사와 편지** — 오래 머물렀는데 정이 옅으면 떠날 뜻(💭)을 비치고, 사흘 안에
       **부탁을 들어준 뒤 말을 걸어야** 붙잡힌다. 편지는 감사장·인사·작별·집 평가서·
       안부·전방 소식, **답장을 쓰면 정이 는다**. 우편함 깃발과 독 배지로 알린다
     · **채집 도감** — 한 번 손에 넣은 것은 팔아도 지워지지 않는다(곤충 13·물고기 6)
     진단 38 → **57**. 세 번 돌려 로그의 시각 말고는 한 줄도 다르지 않다.
   - ~~사가의숲: 벽지·장판 · 화석/조개와 박물관 · 주민에게 선물~~ — **2026-08-26 완료.**
     같은 날 마지막 잔가지까지 옮겼다. 파일 하나가 더 늘었다 — **`museum.js`**.
     · **화석 10 · 조개 6** — 갈라진 자리(🕳️)는 **삽**이 있어야 파고, 조개(🐚)는 모래밭에서
       그냥 줍는다. 이 둘은 **날마다 자리가 바뀐다**(좌표 해시에 날짜를 섞었다).
       나무·바위는 그대로다. `rollDay()` 가 `buildProps()` 를 다시 부른다
     · **사고(史庫)** — 원작의 박물관. 실록을 보관하던 이름을 빌렸다.
       **도감과 다른 자리다**: 도감은 *잡아 본 것*(팔아도 남는다), 사고는 *들여 놓은 것*
       (가방에서 한 점이 빠진다). 그래서 기증은 금이 아니라 **명성**을 준다.
       **기증은 사고 앞에서만** 받는다 — 어디서나 되면 건물이 있을 까닭이 없다.
       전시실 넷(곤충 13·물고기 6·화석 10·조개 6), 등급이 오르면 편지가 온다
     · **선물** — 부탁은 그가 청한 것을 주는 것이고, 선물은 **내가 골라** 주는 것이다.
       사람마다 좋아하는 갈래가 있어(인물 id 로 고정) 맞히면 +3, 아니면 +1.
       하루 한 번이고 다음 날 답례가 온다
     · **벽지 5 · 장판 5** — 전방에 날마다 한 벌씩. 기본이 아닌 것을 바르면 집 평가 +12씩
     진단 57 → **70**. 세 번 돌려 로그의 시각 말고는 한 줄도 다르지 않다.
   - ~~사가의숲: 마을 이름과 깃발 · 주민끼리의 대화 · 계절 행사~~ — **2026-08-26 완료.**
     마지막 잔가지 셋. 파일 둘이 더 늘었다 — **`town.js`** · **`folk.js`**.
     · **마을 이름과 깃발** — 이름은 마을 씨앗으로 뽑고 게시판에서 바꾼다(여덟 자).
       깃발은 바탕 6 · 무늬색 5 · 무늬 8 을 고르는 것으로 옮겼다(도트 편집기는 이 판에
       맞지 않는다). 깃대(🚩)에 걸리고 시트에서는 캔버스로 뽑은 그림으로 미리 보인다.
       **게시판이 이제 제 몫을 한다** — 여태 주민 시트를 열고 있었다
     · **주민끼리의 대화** — 주민이 나만 바라보고 서 있으면 상점가지 마을이 아니다.
       제 자리 둘레 **64 안에서만** 어슬렁거리고(멀리 걷게 하면 부탁 대상을 찾아
       헤매게 된다 — 옛 지시), 가까이 선 둘이 서너 마디를 번갈아 주고받는다.
       그 대화를 **끝까지** 곁에서 들으면 두 사람과 정이 는다(하루 한 번)
     · **계절 행사 여덟** — 설날·대보름·삼짇날·단오·칠석·백중·한가위·동지.
       **양력으로 고정**했다(음력 표는 얻는 것보다 드는 것이 크다). 세배로 세뱃돈,
       큰 보름달, 나무마다 벚빛, 별 갑절, 밤 불꽃, 갈래별 값 상승.
       **날짜만 본다** — 세이브에 남길 것이 없다
     진단 70 → **82**.
     · 여기서 함정 하나를 새로 밟았다 — 아래 '밟아 본 함정' 에 적었다
   - ~~사가의숲: 원작과 다시 대조해 남은 셋~~ — **2026-08-26 완료**
     (`83b6596`·`9a9a24b` 그리고 순무). 다 옮긴 줄 알았는데 코드와 원작을 하나씩
     맞대어 보니 셋이 더 있었다. 값어치 순으로 넣었다.
     · **나무 흔들기** — 열매만 떨어지던 것을 원작대로 갈랐다. 벌집 8%(**벌떼가
       쫓아온다** · 받아치면 잡히고 붙잡히면 쏘여 하루 걸음이 무겁다) · 가구 6%
       (하루 둘) · 돈주머니 5%(하루 하나) · 나머지는 열매.
       **몫이 끝난 칸에 걸리면 열매로 떨어진다** — 다음 칸으로 흘리면 몫이 뒤섞인다
     · **날씨** — 맑음·흐림·비·눈. 날짜로 정해지고 세이브에 남기지 않는다.
       **비에만 나오는 것**(달팽이·미꾸라지)이 있고 **나는 벌레는 비에 숨는다**.
       **달·별·불꽃 행사날은 맑게** 못 박았다(한가위에 비가 오면 큰 달을 못 본다).
       겨울 눈이 계절에서 날씨로 옮겨 갔다 — 그래야 눈 오는 날이 반갑다
     · **순무 장(카부)** — 원작에서 유일하게 값이 오르내리는 축이고, **마을에 요일이
       생기는 것도 이것 하나 때문이다.** 일요일 오전에만 사고, 주중 오전·오후로
       시세가 바뀌고, 다음 일요일이 오면 썩는다. 시세 무늬 넷(파동·내림·급등·폭등).
       시세표는 지나간 칸만 보여 준다 — 앞일을 알면 놀이가 아니다
     진단 82 → **106**. 이 세 판에서 **묵은 버그 셋**도 같이 잡혔다(아래 함정 표).
   - ~~사가의숲: 마지막 여섯~~ — **2026-08-26 완료**(`0feca18` 외).
     **성격 여섯**(말투·청하는 갈래·반기는 선물이 갈린다. 인물 id 로 정해지니 늘 같다) ·
     **내가 먼저 쓰는 편지**(답장과 다른 칸이다. 곁에 없어도 마음을 전한다) ·
     **꽃 교배**(심은 꽃 곁에 다른 꽃이 있으면 드문 색이 핀다. **자리 해시**라 어디에
     심을지가 놀이가 된다) · **전방이 자란다**(판 금 누계로 커지고 가구가 늘고 값을 더
     쳐준다) · **별똥별과 소원**(밤 맑은 하늘. 시각을 잘라 해시로 정하니 난수도 세이브도
     없다. 다음 날 별조각) · **잡초와 마을 평가**(안 뽑으면 날마다 는다. 평가가 높으면
     **주민이 잘 떠나지 않는다** — 그게 이 점수의 값이다).
     진단 106 → **124**.
   - ~~사가의숲: 옷(침선방)~~ — **2026-08-26 완료.** 앞서 '원작에 없는 축' 으로 잘못
     묶었던 것을 바로잡아 넣었다 — **재봉실은 원작에 있다.**
     겉옷·머리·옷 빛·덧옷 넷을 고른다. 사면 옷장에 남고 옷장에 있는 것만 입는다.
     가구·벽지와 달리 **날마다 바뀌는 진열을 두지 않았다**(옷은 취향이다).
     **`sprite.js` 는 한 줄도 안 고쳤다** — 고른 값이 그대로 `look`·`color` 로 들어간다.
     다만 스탬프 캐시가 `ref.id` 로만 갈리므로 **차림표를 붙인 가짜 id** 를 넘겨야
     갈아입은 것이 화면에 반영된다(안 그러면 옛 그림이 그대로 나온다).
     진단 124 → **130**. **원작에서 옮길 것이 더는 없다.**
   - **사가의숲 어드민(`_admin.html`)** — **2026-08-26 완료.** 사가고의 어드민과 같은 결로
     세웠고, `core.js` 에 손잡이(`TUNE_KEY`·`POKE_KEY`·`tuned/setTune`)를 **같은 이름으로**
     옮겼다. 탭 일곱 — 세이브 · 마을/주민 · 집/사고/옷 · **때(時)** · 균형 손잡이 ·
     QA 프리셋 14 · 점검(자가점검 9항목).
     **이 판에서 가장 값진 탭은 '때' 다** — 계절·시간대·날씨·행사·요일이 다 날짜에서
     나오므로, 어드민의 몫은 그날로 가는 것이다(`time.*` · `time.dayShift`).
     자가진단·데모는 손잡이를 읽지 않는다(`DG_NO_TUNE`) — 진단 132 항목 중 하나가 그 못이다.
     어드민은 PC 단독판에 안 들어간다(빌드가 `index.html` 만 훑는다).
   - ~~사가스토리: 보스~~ — **2026-08-25 완료**(`54d773c`). 세 사냥터 오른쪽 끝을 지킨다.
     달려드는 패턴 하나, 15~30분 리젠.
   - **사가스토리: 원작 모방** — **2026-08-26 완료. 원작의 축을 다 옮겼다.**
     사용자가 '원작 모방 최대치' 를 골라 여섯 덩이를 순서대로 넣었다(진단 33 → **79**).
     (커밋 해시는 옛 저장소 `C:\swbins` 것이다 — 이 저장소로 옮겨 오기 전에 붙었다)
     **밧줄·사다리·문**(33→41) — ↑↓ 로 오르내리고 ↓+Space 로 발판을 빠져나가며,
       문 앞에서 ↑ 로 옆 사냥터에 걸어 넘어간다(몸과 벌이가 이어진다).
       키 배치를 원작대로 바꿨다 — ↑ 는 이제 점프가 아니라 오르기다.
       **줄에서 손을 떼는 판정은 움직인 방향으로만** 본다 — 아래 끝(바닥)에서 막 잡은
       줄이 그 프레임에 곧바로 풀리던 결함이 거기 있었다
     **궁수**(41→45) — **든 무기가 곧 사거리다.** `data-enemy.js` 는 사가블로와 나눠 든
       파일이라 `ranged` 칸을 만들지 않고 `look.weapon`(bow·staff)으로 갈랐다
     **장비·가방·저자·주문서**(45→59) — 사가블로의 `item.js` 를 **가져오지 않았다.**
       그쪽은 디아블로식(접사·소켓)이고 이 판이 모방하는 것은 메이플이다
       (요구 수준·업횟·주문서). 실패해도 물건은 터지지 않는다 — 초기 원작이 그랬다
     **직업 아홉·무예 스물여덟**(59→72) — Lv.10 1차(무사·궁수·협객·방사) ·
       Lv.25 2차(장군·신궁·자객·도사). **무예 점수(SP)는 세이브에 담지 않는다** —
       (레벨-1)×3 에서 찍은 합을 뺀 파생값이라 `core.js` 를 안 건드리고 옛 세이브에도
       그냥 맞는다. 무예 효과 아홉은 `side.js` 에만 있다(데이터는 무엇을 할지만 적는다)
     **사명 열셋·미니맵·앉아 쉬기**(72→78) — 사명은 쌓이는 것(적·보스)만 세고,
       보면 아는 것(장비·무예·금)은 그때그때 읽는다(세어 두면 장비를 벗었다 껴도
       수가 남아 어긋난다). 미니맵의 자리·축척은 `sideView.miniBox()` 로 그리기와 떼어 뒀다
     **원작풍 외피**(78→79) — `css/maple.css` 한 장 + `sprite.js` 의 넷째 양식
       `maple`(기본값). `storyize` 와 **같은 픽셀 훑기를 값만 달리 쓴다**(채도를 올리고,
       넓은 계단으로 눕히고, 실루엣을 진하게). 작은 그림에는 안쪽 선을 긋지 않는다
     자동도 같이 손봤다 — **자리 번호가 아니라 종류로** 무예를 고르고(번호로 고르면
     전직한 뒤 근접 무예를 멀리서 휘두른다), **줄을 타는 동안 목표를 바꾸지 않으며**
     (가까운 적은 높이 차를 싫어하는 점수라 오르내리는 사이 1등이 바뀐다),
     **적이 선 자리로 이어지는 줄만** 고른다(지나가기만 하는 줄은 내릴 데가 없다)
     원작에서 남은 축은 파티·거래·길드뿐이라 넣지 않는다(싱글에 뜻이 옅다)
     **2026-08-26 이어서 — 넷을 더 넣었다(진단 79 → 99).** 사용자가 "1~4번 순번대로"
     라고 골랐다. **①어드민**(이 판에만 없었다. `?selftest` 로 헤드리스 점검) ·
     **②소리**(사가블로와 같은 엔진, 소리표 37. 주문서 성패가 이 판의 중심이다) ·
     **③3차 전직 넷과 무예 스물**(직업 13·무예 48. 갈래는 넷 그대로 두고 한 단 높였다.
     띠 8칸·윗자리부터) · **④전투 연출**(판정에 닿는 것은 급소·넉백 둘뿐. 나머지는
     화면 층, 경직은 game.js 루프에만) · **⑤넷째 사냥터 호로곡**(Lv.25. 처음으로 정예가
     나오고, 2차로 들어와 3차로 걸어 나간다. 넷째 배경 `mood: 'fire'`) ·
     **⑥폰 대응**(없던 ↓ · 무너진 상단 · 독에 덮인 조작 띠 · `_mobileframe.html`).
     자세한 것은 머리의 '그 뒤' 와 `saga-story/README.md`. **진단 79 → 106.**
     남은 자리는 **장비 5단**이다 — 4단이 Lv.20 에서 끝나 호로곡의 정예와 결이 안 맞는다
   - **사가블로: 원작처럼 보이게** — **2026-08-26 완료.** 사용자 지시가
     *"완전 디아블로처럼 보여야 해 · 디아블로2 그래픽까지도"* 였다.
     **규칙은 한 줄도 안 건드리고 외피만 갈았다** — 자가진단 42/42 그대로다.
     새 파일 **`css/diablo.css`** 한 장이 스킨이고, `index.html` 에서 그 `<link>`
     한 줄을 빼면 예전 모습으로 돌아온다. 옮긴 것 아홉:
     **조작판**(폭 전체 돌판 + 체력·기력 **구슬** + 스킬 넷 + 경험치 띠 · 원작의 그 판) ·
     **고딕 세리프와 놋쇠 테**(둥근 유리 카드를 전부 각진 돌판으로) ·
     **등급색**(흰·파랑·노랑·초록·금갈색 — `data-item.js` `TIERS.color` 한 곳이 정본) ·
     **바닥에 이름으로 놓인 물건**(검은 쪽지 + 등급색) ·
     **격자 가방과 종이인형**(10칸, 무기·갑주는 두 칸을 먹는다) ·
     **카메라**(인물을 가운데 두고 따라가되 방 밖이 안 보이게 가둔다) ·
     **판석 바닥 · 벽 횃불 · 등불 반경**(여태 빛만 있고 불이 없었다) ·
     **적 이름은 화면 위 한 줄**(원작처럼 머리 위엔 아무것도 안 띄운다) ·
     **핏자국**. 데모에 `#loot` 를 더했다.
     **같은 날 이어서 — 인물·적 그림도 갈았다.** "남은 차이는 그림" 이라고 하자
     사용자가 **"응 다 달라야 함"** 이라 답했다. 그래서 다섯 판이 한 벌씩 나눠 든
     `sprite.js` 중 **이 한 벌에만** `diablo` 양식(`diabloize`)을 넣고 기본값으로 삼았다.
     storyize 와 같은 자리에서 픽셀을 한 번 훑는다 — 채도를 낮추고, 감마 1.22 로
     그늘을 파고, 실루엣 왼쪽 위에 횃불 테를 두른다. **선은 안 긋는다**(그림책풍의 반대).
     📗 버튼은 이제 넷을 돈다. 넷을 나란히 보는 `_styles.html` 을 두었다.
     **글꼴도 심었다** — 원작의 Exocet 계열은 상용이라, 결이 가까운 자유 폰트
     **Cinzel**(SIL OFL)을 `diablo.css` 안에 **base64 로 심었다**(링크로 걸면 단독
     실행판이 인터넷 없는 곳에서 글자가 바뀐다). **로마자·숫자만** 심고 한글은
     `unicode-range` 로 시스템 명조에 떨궜다 — 한글 명조는 파일이 몇 MB 라 단독판이
     그만큼 무거워진다(원작 한국어판도 로마자 Exocet · 한글 명조였다).
     눈에 걸리는 곳이 숫자라 이것만으로 인상이 따라온다: 구슬의 체력 · `Lv.16` ·
     금액 · 층수 · 피해 숫자. 캔버스 글자도 같은 스택이다(`D2_FONT`).
     **`css/FONT-LICENSE.txt` 를 지우지 말 것** — OFL 이 재배포 때 요구한다.
     지킨 선 셋: · 판정 층은 안 건드린다(전부 그리기만) · 핏자국·카메라는 run 에도
     세이브에도 안 넣는다 · `account.js` 는 다섯 판 공용이라 고치지 않고 그 파일이
     넣는 스타일을 CSS 로 덮었다. **남은 차이는 인물·적 그림**이다 —
     `sprite.js` 는 다섯 벌 복사본이라 이 판만 고치면 다섯이 갈라진다.
   - ~~사가블로: 세공·룬~~ — **2026-08-25 완료**(`b1f5062`·`94f96df`).
     **세공 구멍 + 보석**(5종 5등급) · **부문(符文) 12자 + 부문어 5**(룬워드) ·
     **정예 몬스터**(접두 6종) · **난도 평/험/절**(제10·20층에 열린다).
     원작 규칙 둘을 지켰다 — **박은 것은 못 뺀다**, **부문어는 순서가 맞아야 한다**.
     진단 26 → **42**.
     ~~남은 것: 상인·도박(갬블)·조합(호라드릭 큐브)~~ — **2026-08-26 완료.**
     **행상(行商)**·**투전(投錢)**(`vendor.js`) · **연단(鍊丹)**(`forge.js`).
     셋 다 **금을 쓸 데**를 만드는 장치다 — 여태 금이 늘기만 하고 쓸 곳이 없었다.
     행상은 본영에 오고(마을이 없다) **재고가 회차마다 새로 온다** — 새로 고치는
     단추는 없다(원작에서 마을을 드나드는 그 동작이다). 판 것은 **판 값 그대로**
     되산다. 등급 상한은 명품(2) — 보물·전설은 던전과 투전에서만 나온다.
     투전은 부위만 알려 주고 등급 저울이 드랍보다 두껍다(평균 0.68 → 1.55).
     연단은 조합 넷: 보석 셋·부문 셋·장비 셋을 한 등급 위로, 그리고 접사 다시 굴리기.
     **연단은 구멍을 뚫지 않는다** — 구멍은 물건이 나올 때 정해진다는 원작 규칙을
     깨면 "구멍 뚫린 물건을 찾는" 재미가 사라진다. 잠근 것은 재료로도 안 쓰고
     팔리지도 않는다. 값의 정본은 여전히 `item.price()` 하나다. 진단 42 → **58**
     **2026-08-26 이어서 — 단약(丹藥)과 요대(腰帶)**(`potion.js`). 원작에서 손가락이
     가장 자주 가는 자리가 1 2 3 4 다. 여태 회복이 우물방 한 번뿐이라 피가 깎이면
     도망밖에 없었다. 네 칸 × 넉 개, 회복단·기력단 × 소/중/대(최대치의 25·45·70%).
     **키가 바뀌었다** — 1 2 3 4 를 요대에 내주고 **스킬은 Z X C V** 로 내렸다
     (원작의 F1~F8 은 브라우저가 F1 을 가로채고, Q W E R 은 W 가 이동이라 못 쓴다).
     벨트가 차면 **바닥에 남는다**(원작과 같다). 가득이면 안 마신다(대(大)를 아낀다).
     **자동은 탈출보다 먼저 마신다** — 마실 것이 있는데 나가면 노획물을 접는 셈이다.
     체력·기력의 정본은 `dungeon.refill()` 하나다. 진단 58 → **70**
     **2026-08-26 이어서 — 감정(鑑定)**(`item.js`). 원작에서 줍는 맛의 절반은
     "?" 를 여는 것이다. **양품(1) 이상이 미확인**으로 나오고(상품은 접사가 없어 늘
     확인됨), 이름은 밑감만 보이고 **미확인은 입을 수 없다**. 등급색은 보인다 —
     그 색만 보고 감정서를 태울지 정하는 것이 원작이다. 행상이 감정서를 싸게 판다.
     **확인된 채 오는 것**: 행상 재고·투전(원작도 그렇다).
     **미확인으로 나오는 것**: 던전 드랍·연단 결과·접사를 다시 굴린 것.
     구멍 둘을 밟았고 진단이 지킨다 — ① 자동 정리가 미확인을 팔아 치웠다
     (`bestOwner` 가 null 이라 "쓸모없는 것" 으로 잡혔다) ② 자동 장착이 헛돌았다
     (`gainFor` 는 값을 주는데 `equip` 이 거절해 되풀이). **`gainFor` 가 미확인에
     0 을 준다** — 값을 매기는 자리를 한 곳으로 모았다. 진단 70 → **80**
     **2026-08-26 이어서 — 창고(倉庫)**(`item.js`). 가방이 예순 칸이라 **자리가
     모자라서 두는 게 아니다.** 뜻은 하나 — **자동이 손대지 않는 자리**다:
     자동 정리·자동 장착·연단·되는 데까지 감정 넷이 전부 `bag()` 만 훑으므로
     **창고에 넣는 것만으로 저절로 지켜진다**(넷에 조건을 하나씩 더 다는 게 아니라
     자리를 나눠서 풀었다). 원작 그대로 **던전 안에서는 안 열린다**. 진단 80 → **87**
     **2026-08-26 이어서 — 내구(耐久)·수리(修理)**(`item.js`). 쓰면 닳고 다 닳으면
     **부서져 아무 값도 안 낸다**(사라지지는 않는다). 닳는 자리는 **층을 내려갈 때
     하나**로 모았다 — 한 대 맞을 때마다 깎으면 판정 층 한복판을 건드려야 한다.
     내구는 24+등급×10, **부적은 안 닳는다**(원작도 그렇다). `power()` 는 안 건드렸다
     — 그건 그 물건의 값어치이지 지금 내는 값이 아니다. 안 붙는 자리는
     `statBonus`·`partyEffect` 둘이다. 자동은 부서진 것을 팔지도 고르지도 않는다.
     수리는 행상에서(닳은 만큼만). 진단 87 → **94**
     **2026-08-26 이어서 — 투장(套裝)**(`data-set.js`). 원작의 세트다. **한 벌은 셋**
     (무기·갑주·부적 — 인물 하나가 걸치는 자리 수와 같다. 여섯 점짜리는 이 판에서
     영영 못 채운다). 네 벌: 충무·와룡·호랑·청낭. **보물 등급 + 정해진 밑감**일 때
     55%로 조각이 된다 — 등급색이 이미 원작의 세트 초록이라 이름 색만 보고 안다.
     2점·3점 효과가 **누적**되고, **같은 인물이 걸쳐야 센다**(흩어 놓고 채워지면
     세트가 아니라 창고 정리다). 부서진 조각은 안 센다. 진단 94 → **102**
     새 항목 하나가 처음에 깨졌는데 코드가 아니라 **잰 방법**이 틀렸다 — 새로 굴린
     물건과 견주니 밑감·접사가 같이 움직였다. 같은 물건에서 **투장 표시만 뗐다 붙여**
     쟀다(인계 문서의 "재지 않는 축은 붙들어 둔다")
     **2026-08-26 이어서 — 저항(抵抗)**(`data-enemy.js`·`dungeon.js`). 원작의 몬스터
     내성이다. 이 판엔 원소가 없어 **때리는 두 결**로 갈랐다 —
     물리(평타·회전참·돌진) · 기(기공파). 겉모습을 따른다(철갑은 칼이 안 들고
     짐승·수군은 기가 안 통한다). 정예 접두 둘을 더했다(철갑 두른·호신 두른 —
     원작의 Magic Resistant 자리). 난도가 오르면 두꺼워지고(평0·험+10·절+20),
     관통 은사가 절반만큼 뚫는다. 화면 위쪽 줄에 뜨고 깎인 숫자는 잿빛이다.
     **면역(100%)은 두지 않았다** — 원작의 면역은 스킬이 여덟일 때 성립하는 장치인데
     이 판은 넷이고 기(氣)는 하나뿐이라 물리 면역이 뜨면 재냉각만 기다리게 된다.
     상한 75%. 진단 102 → **108**
     **2026-08-26 이어서 — 원소(元素) 여섯**(`data-elem.js`). 사용자가 "최대한
     원작에 맞추자" 고 해서 결을 물리·화·빙·뇌·독·기 여섯으로 늘리고
     **보석 표를 원작 방식으로 다시 짰다**: 무기=그 원소의 **피해** ·
     갑주=그 원소의 **저항** · 부적=능력치. 그전에는 셋 다 능력치라 "어디에 박을까"
     가 수치 비교였고 원작의 그 선택이 통째로 없었다. 결마다 성질이 다르다 —
     빙은 느리게 · 독은 3초에 걸쳐 · 뇌는 편차. 적 넷이 결을 가지고 때리고
     (조총병 화·연노 뇌·오랑캐 궁수 독·수군 함대 빙) 갑주의 보석이 그걸 막는다.
     진단 108 → **114**
     **2026-08-26 이어서 — 직업과 무예**(`data-skill.js`·`skill.js`). 사용자가
     "스킬트리·직업도" 라고 짚어서 넣었다(앞서 권하지 않는다고 했으나 뜻이 분명했다).
     **직업은 장착한 무기가 정한다** — 각궁=궁장·장창/편곤/부월=무장·선채/필묵=책사·
     환도/월도=도독·죽장/병서=방사. 처음엔 인물 `trait` 으로 갈랐는데 **70명 중 방사가
     하나뿐**이라 그 나무가 영영 못 타는 나무가 됐다. 무기로 가르면 셋이 풀린다 —
     다섯이 다 열리고, 사람이 고를 수 있고, **`data.js` 를 안 건드린다**.
     맨몸이면 기질을 본다. **점수와 네 칸은 직업마다 따로** 남아 무기를 도로 쥐면
     예전 손이 살아난다(한 벌만 두었더니 무기를 바꾼 순간 손이 어긋났다).
     나무는 세 갈래 × 세 단계 = 아홉, 다섯 단까지, 앞 단계 규칙 그대로.
     마흔다섯을 따로 구현하지 않고 **모양 아홉**(swing·bolt·nova·dash·buff·heal·
     curse·summon·passive)만 dungeon.js 에 두고 표가 값을 끼운다.
     **상시는 `boonVal` 한 곳으로** 흘려보낸다 — 그 함수를 거치는 수치가 다 따라온다.
     환원(되돌리기)은 행상 값으로. 진단 114 → **127**
     **2026-08-26 이어서 — 고유(固有) 18**(`data-unique.js`). 원작의 유니크다.
     전설(4) 등급 + 그 밑감에 고유가 있을 때 40%. **접사를 굴리지 않고** 표에 적힌
     것이 붙는다(그게 "정해진 물건" 이라는 뜻이다). flat·원소 피해는 수준을 타고
     **%는 안 탄다**(타면 깊은 층에서 걷잡을 수 없다). 떨어지면 ⭐ 로 따로 알린다.
     진단 127 → **133**
     **2026-08-26 이어서 — 잔가지 넷.** **요구 수준**(좋은 물건은 인물 레벨이 차야
     입는다. 못 미치면 자동도 안 고르고 **안 판다**) · **항아리**(방마다 2~5개,
     지나가면 깨지고 뭔가 나온다) · **역참**(5층마다 밟는다 — 밟지 않아도 열리던
     "최고의 절반" 을 원작 쪽으로 옮겼다) · **결사**(하드코어. 켜면 못 끈다 —
     되돌릴 수 있으면 하드코어가 아니다. 쓰러지면 그 프로필 끝).
     요구 수준이 들어오자 **진단 스무 개가 한꺼번에 깨졌다** — 1레벨 인물에게 수준
     20짜리를 입히던 항목들이다(규칙대로 동작한 것이다). 진단에 `levelParty()` 를
     두어 동행을 한 번 키우고, 요구 수준은 전용 항목이 낮은 레벨로 따로 검사한다.
     진단 133 → **139**

     **2026-08-26 이어서 — ⑧ 주옥(珠玉)**(`data-gem.js`·`item.js`). 원작의 주얼.
     보석은 종류마다 주는 것이 표에 적혀 있고, 주옥은 **접사가 굴러 나온다**.
     그래서 같은 주옥이 둘 없다. 원작에서 옮긴 것 셋 — **부위를 안 가리고**(보석은
     부위마다 다른 것을 준다. 그 대비가 주얼의 정체다) · 접사는 하나거나 둘이고
     둘은 드물고 · **부문어에는 못 낀다**(글자만으로 이루어진다).
     **%는 수준을 안 탄다** — 고유와 같은 이유다(타면 깊은 층에서 저항이
     걷잡을 수 없다). 셈이 보석과 다르다: 보석·부문은 개수인데 주옥은 **낱개**라
     `mats.jewel` 만 배열이고 id 로 가리킨다. 무한히 쌓이지 않게 **40개 상한**을
     두고 차면 **바닥에 남긴다**(요대와 같은 규칙). 제4층부터 드물게 나온다 —
     처음부터 쏟아지면 보석을 박을 까닭이 없어진다. 진단 139 → **148**

     **2026-08-26 이어서 — ⑤ 소리(音)**(`js/sfx.js`). 이걸로 원작에서 옮길 것이
     다 끝났다. 에셋을 안 들이고 **WebAudio 로 절차적으로** 낸다(그림을 코드가
     그리는 것과 같은 규칙이고, 단독 실행판이 파일 하나라는 사정도 있다).
     겹은 셋뿐이다 — 미끄러지는 톤 · 걸러 낸 잡음 · 음을 차례로 놓는 것.
     그 셋으로 33가지가 나온다. **고유가 떨어질 때 나는 종소리**가 이 파일의 절반이다.
     지킨 선 넷 — 첫 눌림 전에는 못 낸다(브라우저 규칙. `sfx.js` 가 스스로 문서에
     걸어 두니 **다른 파일은 이 규칙을 몰라도 된다**) · **요청은 꺼져 있어도 적는다**
     (`_tail()`. 판정 층과 소리 층을 가른 것이라 헤드리스 진단이 "이때 이 소리를
     냈어야 한다" 를 검사할 수 있다) · 같은 소리는 최소 간격을 둔다(방에 적이 여덟이면
     타격음이 겹쳐 소음이 된다) · AudioContext 는 한 번만 만든다.
     이미 있는 이벤트로 되는 것(층·레벨업·무예·죽음)은 `sfx.js` 가 스스로 받고,
     프레임마다 나는 것(타격·줍기·문)은 `dungeon.js` 가 `fx.push` 옆에서 부른다.
     상단 🔊 로 끈다(`settings.sound`). 진단 148 → **156**
     **소리가 실제로 나는지**는 진단으로 못 본다 — 진단은 아무것도 누르지 않아
     늘 잠겨 있기 때문이다. 그래서 `_sfxcheck.html` 을 따로 두었다:
     `unlock()` 을 부르고 33가지를 하나씩 내 본다 → `SFX unlock=true · 낸 소리 33/33`

     **원작에서 옮길 것은 여기서 끝났다.** ①원소 ②직업·스킬트리 ③고유 ④요구 레벨
     ⑤소리 ⑥항아리 ⑦역참 ⑧주옥 ⑨하드코어 — **아홉이 다 들어왔다.**
     남은 것은 **적중률/회피(AR)** 와 **참(charm) 인벤토리** 정도인데,
     전자는 빗나감이 이 판의 짧은 교전과 안 맞고 후자는 부적 슬롯과 겹친다.
     더 하려면 멀티(서버가 든다)나 용병(고용 축을 새로 세워야 한다)뿐이다
     이때 **옛 항목 넷이 깨졌는데 셋은 원래부터 약했다** — 새로 굴린 물건과 전후를
     견주는 식이라 무작위 접사·무기의 원소가 같이 움직였다. 전부 "재지 않는 축은
     붙들어 둔다" 로 다시 세웠다(맨몸으로 만들거나, 같은 물건에서 표시만 뗐다 붙이거나)
   - **사가국지 — 2026-08-26 에 삼국지로 통째로 갈아엎었다.**
     사용자 지시 *"삼국지 모방을 할거임"* + *"문답은 곁가지로 물러난다"*.
     옮긴 순서는 사용자가 고른 그대로 ①턴·도시·내정 ②무장 ③전투 ④외교·계략.
     **성 30**(좌표·인접·지형) · **세력 13**(194년 군웅할거) · **무장 124** ·
     내정 명령 9 · 태수 · 수입(금은 세력·군량은 성) · 재해 · 일기토 ·
     야전/공성 · 구원군 · 포로 · 충성과 이탈 · 동맹/화친/조공 · 계략 4 · AI.
     문답은 **학당** 한 칸으로 남아 군자금과 재야 하나를 드러낸다.
     걷어낸 것: `land.js`·`territory.js`·`build.js`·`data-build.js`·`incident.js`·
     `auto.js`·`ui.js` (되살릴 일이 있으면 그 직전 커밋에서 꺼낸다).
     진단 37 → **59**. 자세한 것은 `saga-realm/README.md`.
     ~~옛 남은 것(도감 상세·자동 건설 우선순위)~~ — 도감 상세는 **무장 칸**에 열전·충성으로
     들어갔고, 자동 건설은 축 자체가 없어졌다
4. ~~**사가고 — 버디(원작의 파트너 산책)**~~ — **2026-08-26 완료**(커밋 `4593565`).
   **반려(伴侶)** 로 들였다. 자세한 것은 3번의 사가고 줄 끝에 적었다.
   장착(`petEquip`)에 얹지 않고 **슬롯 하나를 따로** 두었다 — 원작의 버디가 하나이고,
   여러 인물에 장착한 펫 전부에게 영초를 주면 그 감각이 깨진다.
   기준점(`save.buddy.mark`)도 걷기 보급과 **따로** 둔다. 반려를 갈아타면 그 자리에서
   다시 세야 하기 때문이다. 원작에 남은 것은 **선물·기념품** 정도인데,
   싱글에 뜻이 옅어 넣지 않았다
5. **다른 네 판의 남은 곁가지** — 3번의 각 줄에 적어 둔 것들이다.
   **사가블로는 2026-08-26 에 원작 모방을 끝냈다** — 행상·투전·연단에 이어
   원소·직업·고유·요구 수준·항아리·역참·결사·**주옥·소리**까지 다 들어왔다.
   **사가스토리도 2026-08-26 에 원작 모방을 끝냈다**(3번 참조).
   남은 것은 파티·거래·길드뿐인데 싱글에 뜻이 옅어 넣지 않는다.
   **사가국지는 삼국지로 갈아엎었다**(3번 참조) — 남은 것은 여러 달에 걸치는 원정 ·
   수군 · 시나리오 추가 · 무장 성장이다.
   **사가의숲은 2026-08-26 에 원작 모방을 끝냈다.** 그림(구면 투영) · 곤충 · 집 ·
   이사와 편지 · 화석/조개와 사고 · 선물 · 벽지와 장판 · 마을 이름과 깃발 ·
   주민끼리의 대화 · 계절 행사 · 나무 흔들기 · 날씨 · 순무 장 · 주민 성격 ·
   내가 먼저 쓰는 편지 · 꽃 교배 · 전방이 자라는 것 · 별똥별 · 잡초와 마을 평가.
   그리고 **옷(침선방)**. **2026-08-26 에 지형 개조까지 끝냈다**(아래 8번).
   **원작에서 옮길 것이 더는 없다** — 남은 하나는 구조를 뒤집어야 한다:
   멀티(서버가 든다). 절벽과 마이디자인 도트 편집은 안 넣기로 했다 —
   절벽은 투영에 높이 축이 없고, 깃발도 옷도 '고르는 것' 으로 정했다.
   사가스토리의 **전투 연출**은 사가고의 `duel.js` 가 참고가 된다
   (판정 층과 화면 층을 갈라 두면 헤드리스 진단이 그대로 붙는다)
6. **APK 다시 굽기** — `saga-go/mobile` 에만 있다(**v1.7 내용 — 지금 판은 v2.11 이다**).
   다섯 판으로 늘릴지, 사가고만 유지할지 정해야 한다.
   `cd mobile && npm run apk` (반드시 이 경로)
7. **사관 응답 실청취** — 사가고의 온라인 모드. `ANTHROPIC_API_KEY` 를 주고 8790 으로
   군략·천기를 한 번씩 부르면 끝(v1.8 이후 계속 대기 중)

8. ~~**사가의숲 — 지형 개조**~~ — **2026-08-26 완료.** 원작에서 마지막까지 남아 있던
   축이다. 남아 있던 까닭은 마을이 **좌표 해시로 생기기** 때문인데(`village.js` 의
   `tileAt`), 타일 표를 만드는 대신 **고친 칸만 얹는 덮개층**을 두어 풀었다.

   ```
   save.village.terrain = { "tx,ty": 'path' }      ← 고친 칸만
   ```

   `tileAt` 이 해시를 풀기 **전에** 이 표를 본다. 그래서 안 고친 마을은 세이브가 한 자도
   늘지 않고, 되돌리면 칸을 **지워** 도로 줄어든다. "늘 같은 마을" 이라는 전제도 살아 있다.
   마을 크기·해시·투영은 한 줄도 안 건드렸다. 규칙은 `js/terrain.js` 한 파일이다.

   - **여는 것** 전방의 **개토패(開土牌)** 🪧 4,800. 사기 전에는 독에 공사 단추가 없다
   - **자리** 선 칸을 가운데로 한 **3×3**. 멀리서 마을을 주무르지 못한다
   - **깔 것** 흙길 300 · 돌길 600 · 물길 900 · 풀밭 500 · **모래펄 1,400** · 되돌리기 150.
     모래가 가장 비싼 것은 일부러다 — 사물은 타일에서 나오므로(`buildProps`) 모래를
     깔면 조개가 나고 낚시터가 설 수 있다. 값이 그 문턱이다
   - **못 하는 곳** 마을 밖 · 사물이 선 칸 · 주민이 선 칸 · (물길만) 내가 선 칸
   - **돌길(`stone`)은 공사로만 생긴다** — 마을 생성 해시는 이 타일을 내지 않는다

   **절벽은 넣지 않았다.** 원작의 공사는 길·물길·절벽 셋인데 절벽은 땅에 **높이**가
   있어야 한다. 이 판의 투영(`village-view.js` 의 `project`)은 마을 좌표를 구면에 감는
   함수라 높이 축이 아예 없고, 넣으려면 투영·그림자·통행·주민 걸음이 한꺼번에 뒤집힌다.
   그림이 곧 이 게임인 판이라 얻는 것보다 잃는 것이 크다.

   **여기서 묵은 버그 하나가 같이 잡혔다** — 아래 함정 표의 '사물 id' 줄.
   진단 132 → **143**.

**권하지 않는 것** — 원작의 개체값(IV)·CP. 이 판의 펫은 개체가 아니라 **종**이다
(`dex.pets[종id]`, `petEquip[인물id] = 종id`). 개체를 만들려면 세이브 구조와 도감·장착·
연성이 한꺼번에 뒤집히고, 얻는 것은 같은 종 안의 미세한 편차뿐이다. 승화 사슬을
넓히는 편이 값이 싸고 원작 감각에도 더 가깝다.

## 하지 말 것

- **사가의숲의 구면 투영을 평평한 탑다운으로 되돌리기** — 마을을 한눈에 보이게 만들면
  원작 감각이 그 자리에서 사라진다. 좌표를 옮길 땐 `project`/`unproject` 를 거친다.
  **집 안만은 일부러 휘지 않는다** — 그걸 "버그" 로 보고 맞추지 말 것
- 사가의숲의 곤충을 **즉시 획득**으로 바꾸기 — 달아나는 규칙을 빼면 살금살금이 뜻을 잃고,
  낚시터를 무한 반복하던 그 사고가 벌레에서 되풀이된다
- 사가의숲의 이사·편지를 **프레임마다** 굴리기 — `rollDay()` 한 곳에서만 굴린다.
  안 그러면 하루가 몇 번씩 지나간다
- 사가의숲의 공사 덮개층(`save.village.terrain`)을 **타일 표 전체**로 바꾸기 —
  마을이 좌표 해시로 생기는 전제가 무너지고, 안 고친 마을의 세이브가 600칸으로 붇는다.
  되돌리기가 칸을 **지우는** 것도 같은 까닭이다
- 사가의숲의 공사를 **마을 어디서나** 되게 하기 — 3×3 을 풀면 화면 밖의 마을을
  좌표로 주무르게 된다. 걸어가서 고치는 것이 원작의 감각이다
- 사가의숲에 **절벽**을 넣기 — 이 판의 투영에 높이 축이 없다. 넣으려면 투영·그림자·
  통행·주민 걸음이 한꺼번에 뒤집힌다 (까닭은 `js/terrain.js` 머리말)
- 사가의숲의 **도감과 사고를 하나로 합치기** — 잡아 본 것과 들여 놓은 것은 다른 결심이다.
  원작이 갈라 둔 데에는 까닭이 있다
- 사가의숲의 기증을 **어디서나** 받게 하기 — 그러면 사고가 마을에 있을 까닭이 없어진다
- 닌텐도의 실제 에셋(주민·상점 주인·아이콘·타일 그림)을 가져다 넣기 —
  그림 문법만 따르고 그림은 이 저장소의 코드가 그린다
- **사가블로의 본영 카드를 되살리기** — 층·난도를 버튼으로 고르던 그 카드다.
  2026-08-26 에 사용자가 "던전 선택 방식이 아닌 이건 디아블로랑 다른데" 라고
  짚어 지웠다. 고르는 자리를 하나 되살리면 마을과 **두 벌**이 되어 반드시 어긋난다
  (진단이 `#camp` 가 비어 있는지 본다)
- 사가블로 마을의 **닿으면 열림**을 "눌러서 열림" 으로 바꾸기 — 폰에서 지목은
  손이 갑절로 든다. 대신 **벗어나기 전까지 다시 열리지 않는다**(`armed`)
- 세이브 키·앱 id 를 폴더 이름에 맞추려고 바꾸기 (진행이 사라진다)
- 다섯 게임의 공용 파일을 "하나로 합치자" 고 제안하기 (사용자가 값을 알고 고른 구조)
- 삼국지·문답을 saga-realm 밖으로 퍼뜨리기 (네 판은 각자의 원작이 있다)
- 사가국지를 "원작 모방이 아닌 판" 이라고 되돌려 적기 — 2026-08-26 에 사용자가
  스스로 거뒀다(위 지시 9번)
- 사가국지의 AI 에 **전용 판정**을 만들기 — `war.forecast()` 로 진짜 전투식을 굴린다.
  어림식을 하나 두는 순간 판정이 두 벌이 되어 판이 얼거나 터진다
- 사가국지에서 **공격만 병력을 모으게** 두기 (구원군을 빼면 큰 세력이 무조건 이긴다)
- 허브 파일(`services.json`·`pages/index.php`) 커밋
- `git add -A tools` (다른 세션 작업분을 삼킨다)
- 업무 연동(Jira·커밋·QC → 공적) 먼저 제안하기 — "나중에 정하겠다"고 했다
