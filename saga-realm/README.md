# 사가국지 (saga-realm)

> 시리즈 전체 인계(다섯 게임 표·지시 이력·함정·다음 할 일)는 **`../SAGA-HANDOFF.md`** 다.

**삼국지 — 턴제 전략.** 다스리고, 꾀고, 친다. 판은 셋 — **194년 군웅할거 · 200년 관도 · 208년 적벽**.
2026-08-26 에 사용자 지시 *"삼국지 모방을 할거임"* 으로 판을 통째로 갈아엎었다.
그 전까지 이 판은 **문답 + 경영(상상)** 이었고, 문답은 이제 **학당**이라는 곁가지로 물러났다.

**장기 지향점**: 삼국지(중국) 안에 갇히지 않고 **전세계 영토를 점령하는** 판으로
넓힌다 — 그래서 국토 지도가 턴제 고정판이 아니라 병사·부대가 실시간으로 오가며
계속 넓어지는 **오픈월드형 지도**를 목표로 한다. **첫 확장지 한국은
2026-09-04에 지도에 들어갔다** — 성 일곱(양평·국내성·낙랑·대방·위례성·
금성·김해)이 주인 없이 시작해 정복 대상이 된다(수비병·수비 무장 포함,
기존 출진·전투 로직 그대로 재사용). **부대를 실시간(여러 달)으로 옮기는
"원정" 명령도 2026-09-04에 끝났다**(내 땅·빈 땅만 거쳐 먼 성까지 육로로
이동, 도착하면 기존 출진에 그대로 붙는다). **둘째 확장지 일본열도는
2026-09-09에 지도에 들어갔다** — 성 일곱(대마도·일기도·축자·일향·출운·
길비·야마토)이 같은 결로 주인 없이 시작한다. 한국과 다른 점은 **바다** —
김해→대마도→일기도→축자, 축자→출운 두 자리는 물길이라 배 없이는
못 건넌다. **셋째 확장지 교주(영남·베트남 북부)도 2026-09-09에 같은 날
이어서 들어갔다** — 성 일곱(남해·창오·울림·합포·교지·구진·일남, 실제
한대 교주 칠군 이름)이 장사(長沙) 한 곳을 관문 삼아 주인 없이 시작한다.
대륙과 붙어 있어 일본과 달리 대부분 육로다(합포↔교지만 연안 항로).
**넷째 확장지 서역(하서주랑 너머 실크로드)도 같은 날 이어서 들어갔다** —
성 일곱(돈황·누란·언기·구자·우전·소륵·대완)이 무위(武威)를 관문 삼아
주인 없이 시작한다. 이번엔 **서쪽**이라 지도 원점을 음수 쪽으로 옮겨야
했다(기존 성 좌표는 안 건드림). **다섯째 확장지 남중(강주 남쪽 산지·
밀림)도 2026-09-09에 이어서 들어갔다** — 성 일곱(주제·건녕·월수·장가·
운남·영창·흥고)이 강주(江州)를 관문 삼아 주인 없이 시작한다. 관문 성
주제를 강주처럼 `land:'river'`로 둬서 노수(瀘水)를 건너야 드는 땅이라는
고사를 물길로 옮겼다. 자세한 것은 `PLAN.md`의 해당 절.

**전투 화면은 2026-09-03에 "실시간처럼 재생"하도록 바뀌었고, 같은 날 이어서
개입형까지 됐다** — 플레이어가 손수 출진을 누른 싸움은 합마다 끊겨
⚔️ 돌격·🛡️ 수비·➡️ 정공법·↩️ 퇴각 중 하나를 고를 수 있다(`war.marchInteractive`).
개입 없이 흘러가면(항상 정공법) 예전 `fight()`를 한 번에 돌린 것과 자릿수까지
같은 값이 나온다 — 판정 수식 자체는 그대로고, 명령이 곱하는 배율만 새로
얹었다. AI가 치는 싸움·가늠(forecast)·진영의 달마다 재개는 여전히 `fight()`를
그대로 쓴다.

```
run.bat          →  http://127.0.0.1:8795
start_server.bat →  같은 것, 브라우저를 열지 않는다 (허브 워치독용)
_admin.html      →  QA 운영판 — 시나리오·성·무장·전쟁·외교·손잡이·프리셋
                    (`?selftest` 로 열면 헤드리스로도 확인된다 → ADMIN n/n)
_test.html       →  자가진단 95항목
_mobileframe.html→  폰 화면(390px) 확인용 껍데기
_demo.html       →  스크린샷용 장면 (#scen · #pick · #map · #chibi · #grown · #city · #enemy · #war · #camp · #officers · #diplo · #school)
```

## 앞으로 — 3D 로 옮긴다 (`PLAN.md`)

이 폴더의 **`PLAN.md`** 가 앞으로의 방향이다. 이 README 의 나머지는 **지금까지 만든 것**이니,
손대기 전에 둘을 같이 본다.

가는 곳은 **오픈월드형 3D 삼국지 전략 RPG** 다. 지금의 턴제 경영을 버리는 것이 아니라,
그 위에 넓은 월드맵과 액션 전투를 붙인다.

- **반드시 재사용**: 게임 데이터 · 무장 · 성 · 진영 · 외교 · 기록 · 학당 ·
  저장/불러오기 · 기존 UI 로직 · 게임 상태
- **새로 만드는 것**: 3D 렌더링 계층 · 월드 · 카메라 · 캐릭터 · 건물 · 지형 ·
  전투 · 월드 이벤트 · 새 HUD
- 여덟 축: 탐험✅ · 랜덤 사건✅ · 무장 수집✅ · 장비✅ · 진형✅ · 외교✅ · 역사 분기✅ · 보스전✅
  (2026-09-09에 넷, 2026-09-10에 나머지 넷이 끝나 여덟이 다 붙었다 — 아래
  Phase 9·"장비"·"진형" 절과 PLAN.md "여섯째~여덟째 확장 지역" 절 참고)

**Phase 1~12** 로 잘라 간다 — 엔진 연결 → 카메라 → 플레이어 → 월드 → GLB →
성·도시 → 전투 → 부대 지휘 → 랜덤 사건 → 외교·AI → 경제 → 모바일.

> 경영·문답은 이 판의 것이다. 3D 로 가도 다른 판으로 퍼뜨리지 않는다.

### 지금까지 — Phase 1(엔진 연결) · Phase 2 일부(카메라) · Phase 4 일부(월드)

2026-09-01 `js/realm3d.js`·`js/asset3d.js` 신설. svg 평면 지도(`#realm`)는 그대로
두고, 🧊 버튼으로 **국토 지도 3D**(`#realm3d`)를 켠다 — 성 서른 곳을 성벽 값에 따라
세 등급 탑(GLB, Quaternius CC0)으로 세우고, 세력 색으로 물들이고 깃발을 꽂는다.
길은 인접 관계·물길(`data-city.js`)을 그대로 읽어 색을 가른다(같은 편은 밝게,
물길은 파랗게). 산지 성 둘레엔 봉우리를, 나머지엔 나무·바위를 몇 개 흩는다.
카메라는 드래그로 돌리고 휠/핀치로 당기는 **궤도 카메라** — 아직 지상을 걷는
플레이어는 없다(이 판은 턴제 지도 화면이라 Phase 3 "플레이어"는 다르게 풀어야
한다). 성을 탭하면 2D 와 똑같이 `ui.openCity()` 가 열린다 — 판정은 손대지 않았다.

**2026-09-10 — 동양풍 CC0 탑 채택.** 2026-09-09에 받아 둔 후보
(`BellStructure.glb`, CC0, 자세한 것은 `assets/ASSET_LICENSES.md`)를
예외적으로 허용된 헤드리스 스크린샷 한 번으로 확인해 1등급 성채(`city:t1`)
기본값으로 뒤집었다(`core.tuned('asset3d.asianTower', 1)`) — 붉은 기둥
문/종틀 형태로 서양풍 Watchtower와 실루엣이 뚜렷이 다르다. 2·3등급 후보로
점찍었던 `Portal.glb`·`MainAltar.glb`는 실제로 확인해 보니 병풍·제단
상자(실내 소품)라 탑이 아니어서 채택하지 않았다 — 2·3등급은 당분간
서양풍 그대로. **다음 몫**: 실기기 확인(아직 안 함) · 2·3등급 동양풍 탑은
이 팩의 조각 조합(`Column.glb`+`RoomRoof.glb` 등)이나 다른 팩을 새로
찾아야 한다.
(~~성 안으로 들어가는 3D 도시 화면(Phase 6)~~ — `js/city3d.js`로 이미
완료, 이 줄은 그 뒤로 안 지워지고 남아 있던 것)

**2026-09-10 — 도감 초상 외형 20종 추가(MPFB2 실사, `saga-go`에서 복사).**
`js/asset3d.js`의 `HERO_RECIPES`(QRPG 6종)에 MPFB 몸 20종을 더했다 —
이 판은 인물이 안 걸으므로(`portrait3d.js` 초상 굽기 전용) `anim`을
UAL1로 리타깃하지 않고 몸 파일 자신(클립 0개)으로 줘서 mixer 없이 정지
자세로만 세운다. 자세한 사정·용량은 `assets/ASSET_LICENSES.md` 참고.
투구 5종(scholar·gat·hairpin·monk·braid)은 재확인 결과 여전히 대응 CC0가
없어 맨머리로 남긴다 — 무기와 달리 잘못 아무 걸 씌우면 더 어색해진다.

**Phase 9(랜덤 사건)는 2026-09-09에 이어서 끝났다** — 원정(`war.js`
`journeys[]`)이 가는 길에(도착하는 달은 뺀다) 손잡이 확률(`war.journeyEventChance`,
기본 16%)로 매복·도적떼·보물·마을의 환대·길 잃음·유물 발견 여섯 종
(`JOURNEY_EVENTS`) 중 하나를 만난다 — `rtk.js`의 재해(`DISASTERS`) 표와
같은 결. 병력·진영 금고·사기·남은 달 정도만 살짝 흔들 뿐 **새 전투 판정은
안 만든다** — 국경 도착 전투는 여전히 `fight()` 한 곳뿐이다. 진 시트의
원정 카드에 마지막 사건이 배지로 뜬다(`ui-rtk.js` `journeyCard`).

**"장비"(여덟 축 중 하나)도 같은 세션에서 이어서 얹었다 — 다른 판을
참고했다.** `saga-dungeon`의 무거운 ARPG 장비 시스템을 옮기지 않고,
그 판이 이미 지키는 **계약**만 빌렸다 — 다섯 판 공유 파일 `hero.js`는
애초부터 `global.DG.item.statBonus(id)` 라는 장비 자리를 열어 두고 있었다
(모듈이 없으면 0). `js/data-item.js`를 새로 얹어 원작 코에이 삼국지의
"보물" 여덟 종(무쇠검·준마·보갑·병서·나침반·옥패·인수·전고)을 그 자리에
꽂았다. 장비창은 없다(v1) — 원정 사건 "유물 발견"이 원정 간 장수 하나에게
곧바로 씌운다. `officer.js`의 능력치 함수는 안 건드렸다 — hero.js 한
곳만 거치므로 화면·판정이 갈라질 일이 없다.

검증 — `_test.html`에 사건 표 구조·손잡이 100% 강제 시 실제로 매달
붙는지(병력 1 밑으로 안 감·금 음수 없음)·"유물 발견"으로 실제 스탯이
오르는지를 새 항목 셋으로 추가(106→109).

**"진형"(여덟 축 중 하나)도 이어서 얹었다.** 원작 코에이 삼국지의 진형을
가볍게 옮기되 **고르는 화면은 안 만들었다** — 데려가는 장수의 능력치가
문턱(75)을 넘으면 자동으로 그 진형이 붙는다(추행진=무력·학익진=지력·
방원진=통솔). `war.js`의 `armyPower()` 한 곳에서만 배율을 곱해, march·
journey·camp·forecast 어디서 부르든 화면(전투 로그)과 판정이 저절로
같이 움직인다. 출진·원정 사전 화면에도 지금 데려갈 장수로 진형이 서는지
미리 보여준다.

검증 — `_test.html`에 표 구조·자동 발동·전투력 배율을 새 항목 넷으로
추가(109→113, 이 세션은 node·chrome이 없어 직접 못 돌려 봄).

## 사가 시리즈 — 완전히 별개인 다섯 프로젝트

| 게임 | 폴더 | 포트 | 원작 | 세이브 키 |
|---|---|---|---|---|
| **사가고** | `saga-go` | 8791 | 포켓몬GO | `deungyong-go/save/v1` |
| **사가블로** | `saga-dungeon` | 8792 | 디아블로 | `yeoksa-dungeon/save/v1` |
| **사가의숲** | `saga-forest` | 8793 | 동물의숲 | `yeoksa-village/save/v1` |
| **사가스토리** | `saga-story` | 8794 | 메이플스토리 | `yeoksa-side/save/v1` |
| **사가국지** | `saga-realm` | 8795 | **삼국지(코에이)** | `saga-realm/save/v1` |

**세이브 키는 갈아엎으면서도 안 바꿨다.** 바꾸면 지금까지의 진행이 통째로 사라진다.
옛 세이브의 문답·지식은 그대로 살아 있고, 삼국지 판은 `save.rtk` 로 **따로 붙는다**.

## 놀이 순환

```
달(月)    무장 한 사람이 한 달에 명령 하나. 다 쓰면 ▶ 다음 달
다스린다  개간·상업·기술·치안·축성·징병·훈련·조선·수색·등용   (성마다)
꾄다      외교로 시간을 사고(동맹·화친·조공), 계략으로 깎는다(이간·유언비어·매수·화계)
친다      맞닿은 성으로만 출진. 일기토 → 야전이나 공성 → 함락
원정      한 달에 못 떨어뜨리면 물러나지 않고 **성 밖에 진**. 달마다 한 번씩 더 친다
수전      강끼리 이어진 **물길**은 배로만 건넌다. 성벽이 소용없고 화공이 터진다
기른다    일을 시키면 **경험**이 붙고, 쌓인 **공**으로 **승진**시킨다
곁가지    학당(문답) — 군자금과, 학식이 차면 재야 하나가 드러난다
```

- **금(金)** 은 세력 금고 하나. **군량(糧)** 은 **성마다 따로** — 6·10월에 거둔다.
  코에이가 이 둘을 갈라 둔 데에는 까닭이 있다. 합치면 "군량이 있는 곳으로 싸우러 간다" 는
  판단이 통째로 사라진다
- 승리는 **성 전부**(지금은 쉰여덟 — 중국 서른 · 한국 일곱 · 일본 일곱 · 교주 일곱 · 서역 일곱), 패배는 **성을 다 잃는 것**

## 여러 달에 걸치는 원정 (진영)

한 달 안에 성을 못 떨어뜨렸다고 군을 되돌리면 **공성이라는 것이 판에서 사라진다**.
그래서 이 판의 군대는 물러나지 않고 성 밖에 **진(陣)** 을 친다.

```
치중(輜重)  출진할 때 들고 나가는 군량이 곧 진영의 밥이다 — 두 달치
한 달       진영마다 fight() 를 한 번 더 굴린다 (march 와 **같은 함수**다)
사기        달마다 0.94 씩 깎인다. 0.60 아래로 내려가면 군이 지쳐 물러난다
굶주림      치중이 한 달치를 못 채우면 이할이 흩어지고 물러난다
포위 효과   에워싸인 성은 **수확을 못 거두고 인구도 늘지 않는다**
```

- **진중의 굶주림 식은 성과 다르다.** 성은 모자란 만큼을 그대로 병사에서 깎지만
  (`settleMonth`), 진영에 그 식을 쓰면 치중이 떨어지는 그 달에 원정군이 통째로
  사라진다. 성은 물러날 데가 없고 진영은 있다
- **진을 친 성에는 새 군대를 또 못 보낸다**(`canMarch`). 늘리려면 **보급**이다.
  이 단서가 없으면 AI 가 달마다 새 진영을 세워 같은 성 앞에 군대가 쌓인다
- **원정 나간 장수는 성에 없다**(`off.rec(id).camp`). `atCity` 가 빼므로 내정도
  못 하고 수비에도 안 선다. 봉급은 그대로 나간다(`ofForce` 는 센다)
- **AI 는 군량만 댄다**(`rtkAI.trySupply`). 병력 증원까지 AI 에 열어 주면 달마다
  뒷마당을 비워 진영에 몰아주게 되어 균형이 통째로 달라진다. 증원은 사람 손잡이다
- 돌아갈 나라가 사라진 원정군은 그 자리에서 **사로잡힌다** — 그냥 지우면
  판에서 사람이 조용히 사라져 등용할 거리가 준다

120개월을 굴리면 진영이 28~47번 서고, 그중 6~15번이 포위 끝의 함락으로 이어진다
(전체 함락의 이할 남짓). 가장 긴 포위는 서너 달이다.

## 시장 (금↔군량 환전, 2026-09-09)

상업(comm)은 그동안 "달마다 금이 는다" 하나뿐이었다. 금은 남는데 군량이
모자라거나(혹은 그 반대여도) 손쓸 길이 없던 것을 성마다 붙는 **시장**으로 풀었다.

```
환율   🍚1 = 🪙(0.35~0.7), 상업(comm)이 클수록 후하다(comm 700에서 최고)
판다   군량 → 금 (rtk.trade(cityId, 'sell', 양))
산다   금 → 군량 (rtk.trade(cityId, 'buy', 양))
```

- **명령이 아니다** — `war.transfer()`처럼 물류라 `officer.done`을 안 쓴다.
  그 달에 몇 번이든 쓸 수 있다
- AI(`rtkAI.tryTrade`)도 쓴다 — 군량이 위태로우면 사서 메우고, 금이 모자란데
  군량이 썩어날 만큼 넉넉하면 판다. 새 AI 전용 판정을 만들지 않고 사람이
  쓰는 `rtk.trade()`를 그대로 부른다(한 세력이 한 달에 한 성만)
- 성 시트에 💰판다·🌾산다 두 버튼으로 뜬다

## 무장 성장 (경험과 승진)

능력치는 `hero.stats(id)` **한 곳**에서 나온다. 성장이 하는 일은 그 함수가 읽는
`save.heroes[id]` 의 `lv`·`rank` 를 올려 주는 것뿐이다 — 성장한 무장이 실제로 더 세게
싸우는 것은 `war.armyPower` 가 같은 `off.stats()` 를 읽기 때문이지,
전투에 따로 붙인 보정이 아니다.

```
경험  내정 명령 +4 · 출진 +12 · 진을 치고 한 달 +8 · 함락 +20 · 일기토 승 +15 · 태수 +2
관직  무관 → 교위 → 중랑장 → 장군 → 대장군 → 도독 (다섯 단)
승진  공(feats) 20+20n 과 금 300+300n. 능력치가 오르고 **충성이 +12**
```

- **`hero.gainExp` 를 쓰지 않는다.** 그쪽은 `save.dex.heroes[id]`(도감에서 뽑은 인물)만
  올려 주는데 이 판에는 뽑기도 도감 획득도 없어 언제나 0 을 준다. 같은 까닭으로
  승급도 `hero.rankUp`(중복 인물 소모)이 아니라 **공**으로 한다 — 삼국지에 중복 인물은 없다
- **AI 도 승진시킨다**(`rtkAI.tryPromote`). 고르는 눈은 하나뿐이다 — 올릴 수 있는 사람
  가운데 **충성이 가장 낮은 사람**. 원작에서 관직이 하는 일이 그것이다.
  이 줄이 없으면 AI 는 120개월에 승진이 한 단도 없고 사람 쪽만 관직 배수를 받는다
  (실제로 그렇게 재 보았다 — 승진 0단이 나왔다)
- 성장은 **일부러 느리다.** 120개월을 굴리면 무장이 평균 Lv.9 (×1.18) 언저리에 선다.
  후하게 주면 늦게 시작한 세력이 영영 못 따라잡는다
- `save.heroes` 는 **세이브 공용 칸**이라 `setup` 이 안 비운다(옛 문답 시절의 레벨이
  그대로 이어진다). 그래서 자가진단의 `fresh()` 는 그 칸을 **함께 비운다** —
  안 비우면 앞 항목이 올려 놓은 레벨이 뒤 항목의 능력치로 흘러 들어간다

## 시나리오 셋

같은 서른 성이지만 **누가 어디를 쥐고 있는지**가 다르다. `data-force.js` 에 표를
하나 더 두면 판이 하나 는다 — 그 밖에 고칠 곳이 없다.

```
194 군웅할거  세력 13. 열세 깃발이 한꺼번에 섰다 (지금까지의 판)
200 관도      세력 8.  하북의 원소와 중원의 조조. 여포도 원술도 이미 없다
208 적벽      세력 6.  조조가 19성을 쥐고 강을 내려온다. 손권과 유비는 동맹으로 시작한다
```

- **어느 표에도 안 적힌 무장은 저절로 재야가 된다**(`rtk.scatterFree`). 200년 표에
  여포·이각·원술을 안 적으면 그들이 재야로 흩어진다 — 죽은 사람을 판에서 지우는 대신
  남겨 두는 것이 이 판의 결이다(지우면 등용할 거리가 그만큼 준다)
- 시나리오의 `pacts` 가 **시작 맹약**을 놓는다. 적벽의 손·유 동맹이 그것이다.
  그것 없이는 적벽이 아니다
- **세이브에 `scen` 이 남는다.** `rtk.state()` 가 그걸 보고 `FD.use()` 를 다시 부른다 —
  이 한 줄이 없으면 208년 판을 열어 두고 새로고침했을 때 194년 표로 읽혀
  세력 이름과 색이 통째로 어긋난다
- `FD.FORCES` 는 **배열을 그대로 갈아 끼운다**(새 배열로 바꾸지 않는다). 다른 파일이
  이미 그 배열을 붙들고 있어서, 참조를 바꾸면 그쪽이 옛 표를 계속 본다
- 200·208년의 유장은 보라(`#9a6b9a`)다. 초록(유비)과 붙어 지도에서 헷갈렸고,
  그 판에는 이각이 없어 그 색이 비어 있다

## 수군 (물길 · 배 · 수전)

**양쪽이 다 강(river)인 길이 물길이다.** 목록을 따로 두지 않았다 — 지형 한 글자만
고치면 물길이 따라 움직인다. 지금 아홉 줄기가 난다.

```
황하  복양↔하비
회수  하비↔수춘 · 수춘↔건업 · 수춘↔시상
한수  양양↔강릉 · 양양↔강하
장강  강릉↔강하 · 강하↔시상(적벽) · 시상↔건업
```

```
배      성마다 따로. 강을 낀 성만 갖는다(처음 60척, 상한 300척)
조선    🛶 새 내정 명령. 강을 낀 성에서만 쓴다
싣기    배 한 척에 100명. 물길 출진은 배에 타는 만큼만 간다
수전    성벽이 곱해지지 않고, 공성추도 없다. 대신 성벽 뒤에 숨지도 못한다
화공    지력이 60을 넘는 사람이 합마다 건다. 터지면 상대 병력 18% · 배 25%
수전 배수  data-force.js 의 `NAVY` 목록 — 이순신 ×1.4 · 주유 ×1.35 · 황개/감녕/채모 ×1.3
```

- **넷째 자질을 만들지 않았다.** `data.js` 의 인물과 모양이 같아야 도감·초상·능력치가
  그대로 돌기 때문이다. "물에서 더 잘 싸운다" 는 **`data-force.js` 의 목록**으로 뒀다 —
  다섯 판이 나눠 가진 `data.js` 를 한 줄도 안 건드리고 적벽이 선다
- **화공은 가늠(`forecast`, dry)에서도 굴린다.** 여기서 빼면 AI 가 보는 승산과 실제가
  어긋난다. 일기토만 뺀 것은 장수가 **진짜로 다치기** 때문이다
- 강을 낀 성에 배를 처음부터 60척 준다. 없이 시작하면 강동이 첫해 내내 아무 데도
  못 가고 판이 그대로 언다
- AI 는 물길 앞에서 배에 맞춰 병력을 줄이고, 배가 모자라면 그 길을 아예 안 고른다.
  **AI 전용 판정이 아니다** — 사람을 막는 `canMarch` 가 AI 도 그대로 막는다

시상(손책)에서 강하(유표)로 2만을 건너 보내면, **주유·황개를 데려갔을 때와
주태만 데려갔을 때의 손실이 두 배 넘게 벌어진다.**

## 무장 240 — 명부를 어떻게 늘렸나

```
data.js        인물 105 (삼국지 22 · 한국사 26 · 일본사 20 · 세계사 37)  ← 다섯 판이 **바이트까지 같은 복사본**
data-force.js  무장 135 (삼국지 군주 13을 포함한 54 + 한국·일본·교주·서역·남중·천축·막북·임읍·균열 9×9) ← 이 판만의 것
```

**2026-09-10 — 아홉째(균열)는 사람이 아니라 괴물이다.** 앞 여덟과 자리·
판정은 같은 결(`FUTURE_OFFICERS` 9인·`FUTURE_GARRISON`)이지만 처음으로
실제 지명이 아니고, 수비 무장 9인 전부 `monster` 필드(CC0 3D 모델 경로)를
문다 — 자세한 것은 `PLAN.md` "26-4. 방향 정정" 절.

**2026-09-10 — data.js 쪽 105는 다른 세션(HEROES 가명화 후속)이 한국사·유럽사만 있던
것을 일본사·세계사로 늘린 결과다.** 이 수는 앞으로도 더 늘 수 있어 코드·진단 어디서도
못 박지 않는다(`officer.js`·`_test.html`은 `DG.data.heroes.length` 를 그때그때 잰다).

`data.js` 는 손대지 않는다. **부팅 때 `officer.js` 가 126인을 `DG.data.heroes` 에 얹는다** —
`data.find` 가 배열을 그때그때 훑는 구조라 얹기만 하면 `hero.stats`·`sprite.portrait`·도감이
그대로 돈다. 다섯 판을 맞출 일이 생기지 않는다.

**삼국지 사람이 아닌 인물(한국사·일본사·세계사 83 + 아홉 확장 지역 9×9=81,
그중 균열의 9는 사람이 아니라 괴물)은 재야(在野)** 다. 성에 흩어져 있고,
**수색해야 보이고 등용해야 온다.** 이순신이 손권 밑에서 수군을 맡는
판이다. 재야는 이름 해시로 **결정적으로** 흩는다(`CD.CITIES.length`로
나눠 성을 정한다) — 무작위로 흩으면 진단이 실행마다 다른 성을 짚는다. **성을 늘리면
이 나눗셈이 바뀌어 누가 어느 성에 숨는지도 통째로 바뀐다** — 확장 지역
수비 무장은 `seedNeutral()`이 scatterFree() 뒤에 곧바로 제자리로 되돌리지만, 특정 성을
못 박아 둔 자가진단 항목은 성을 늘릴 때마다 다시 깨질 수 있다(실제로 일본 지역 확장
때, 그리고 2026-09-10 균열 확장 때(성 수 93) 하필 cao의 시작 성 셋 모두가 비어 또
깨졌다 — `_test.html`의 `ensureHiddenAtMine()`으로 산포 운에 안 기대게 고쳤다).

## 파일

```
js/data-city.js   성 58 곳 — 좌표(지도)·인접(길)·지형·**물길**    ← 성을 늘릴 땐 여기만
js/data-force.js  **시나리오 셋** + 삼국지 무장 54 + 한국·일본·교주·서역 수비 36 + 수전 배수(NAVY) ← 배치·성향도 여기
js/officer.js     명부 합치기 · 소속/배치 · 충성 · 인연 · **성장(경험·승진)**
js/rtk.js         연·월 · 내정 명령 9 · 태수 · 수입/수확/재해 · 학당 · 승패
js/war.js         출진 · 일기토 · 야전/공성/**수전** · 구원군 · 함락 · forecast(가늠)
                  **진영** — encamp/resolveCamp/supply/withdraw (여러 달 원정)
                  **수군** — 배 싣기 · 화공(fireRoll) · 배 침몰(sinkShips)
js/diplo.js       우호 · 동맹/화친/조공 · 계략 4 · 이탈
js/rtk-ai.js      다른 열두 세력의 한 달                          ← 사람이 쓰는 함수만 부른다
js/ui-rtk.js      지도(svg) · 성 · 무장 · 진영 · 외교 · 학당 · 기록 · 전황
js/quiz.js        학당(문답) — 상금은 세력 금고로 들어간다
js/core.js        상태·저장·이벤트 버스
js/game.js        부트 — **rAF 루프가 없다**(턴제라 시간은 사람이 누를 때만 흐른다)
```

**2026-08-26 에 걷어낸 것** — `land.js`(강역) · `territory.js`(특산·태수) · `build.js` ·
`data-build.js` · `incident.js`(사변) · `auto.js` · `ui.js`.
그 자리는 성(城)의 농업·상업·기술·치안이 맡는다. 되살릴 일이 있으면 그 직전 커밋에서 꺼낸다.

## 가입해서 저장 (account.js)

첫 실행 때 **이름**을 물어보고, 그 이름으로 진행이 저장된다. 서버는 없다.

```
세이브 키   <게임>/save/<프로필id>      ← account.js 가 정해 준다
프로필 목록 <게임>/save/accounts
옛 세이브   <게임>/save/v1             ← 첫 가입 때 '이어받기' 로 옮긴다
```

- `game.js` 의 `boot()` 은 **`DG.account.gate(start)`** 한 줄만 부른다
- 상단 **👤** 는 account.js 가 직접 끼워 넣는다 (index.html 을 고칠 필요가 없다)
- 자가진단은 `window.DG_NO_ACCOUNT = true` 로 가입 화면을 건너뛴다

## 자가진단

```
chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom \
  http://127.0.0.1:8795/_test.html        → RESULT 128/128
```

**무작위는 씨앗(mulberry32, 20260824)으로 고정돼 있다.** 세 번 돌려 **한 줄도 다르지 않은지**로
확인한다. 이 판은 턴제라 **rAF 함정이 아예 없다** — 헤드리스에서 프레임이 몇 번 돌든
`endMonth()` 를 부른 횟수만이 시간을 옮긴다.

## 폰에서

**절반은 처음부터 돼 있었다** — manifest·서비스워커·safe-area·780px 미디어쿼리.
폰 폭(390px)에서 실제로 열어 보고 **막히는 것만** 골라 고쳤다.

```
_mobileframe.html          폰 화면(390px)을 헤드리스에서 보는 껍데기
_mobileframe.html#probe    상단·독·지도·다음달이 화면 안에 있는지 재서 PROBE OK
_mobileframe.html#camp     그 장면을 폰 폭으로 (…#city #officers #ask #scen …)
_mobileframe.html?fresh    가입 화면부터
```

헤드리스 크롬은 창 폭을 **500px 아래로 안 내려 준다**. 그래서 iframe 안쪽에
진짜 390px 뷰포트를 만든다(사가의숲·사가블로 것과 같은 꼴이다).

고친 것은 넷이다.

1. **`prompt()` 를 걷어냈다.** 출진·보급·증원·병력 보내기 넷이 `prompt` 로 숫자를
   받고 있었다. 폰에서 `prompt` 는 숫자 키패드가 아니라 글자판을 띄우고,
   홈 화면에 담아 띄운 앱(standalone)에서는 아예 안 뜨는 기기가 있다.
   **미는 막대와 ¼·½·⅘·전부** 를 가진 카드로 바꿨다(`ui.askNumber`)
2. **지도의 성이 손가락에 안 닿았다.** 성 점은 화면에서 8px 남짓이다.
   보이지 않는 큰 원(`.rhit`, r=4.2)을 하나 깔았다 — 그림은 그대로, 손만 커진다
3. **상단이 두 줄로 접히는데 지도가 그대로였다.** 780px 아래에서 `#realm` 을
   148px 로 내렸다. 안 내리면 북쪽 성들이 상단 유리판 밑으로 들어가 못 눌린다.
   `.p-sub` 는 한 줄로 — "수입 388/월" 의 '월' 이 떨어져 나갔다
4. **`touch-action: manipulation`** — iOS 사파리는 `user-scalable=no` 를 무시해서
   더블탭 줌이 성 누르기를 방해한다

폰에서 여는 길은 둘이다. **GitHub Pages(https)가 확실하다** — 서비스워커와
홈 화면 추가가 거기서만 붙는다. 같은 와이파이에서 `run.bat`(0.0.0.0 바인딩)으로
`:8795` 를 열어도 놀 수는 있지만 http 라 캐시가 안 붙는다.
**이 판은 위치 API 를 쓰지 않아 HTTPS 가 필수는 아니다** — 그래서 `run-phone.bat` 이 없다.

## 손볼 때

- **판정을 두 벌 두지 않는다.** AI 가 칠지 말지는 `war.forecast()` — **진짜 전투식을 사본에
  그대로 굴린다**. 어림식(병력 배수 문턱)으로 재던 때는 성벽 초깃값만 보고 판단해
  120개월에 다섯 번밖에 안 싸웠다
- **공격만 병력을 모으게 두지 않는다.** 수비도 이웃에서 구원군(`war.reinforce`, 사할)을 부른다.
  없이 굴렸더니 263전 259함락 — 사실상 수비가 없는 판이었다
- **뒷마당이 비면 못 준다**(`rtk-ai.spareOf`). 이 단서가 없으면 AI 가 전군을 몰아쳐
  첫해에 판이 쓸려 나간다(가만히 둔 유비가 한 달, 조조가 넉 달 만에 멸망했다)
- 성을 늘리면 `data-city.js` 만 고친다. **없는 성을 가리키는 링크는 조용히 버린다**
  (`CD._dropped` 로 볼 수 있다) — 지도가 통째로 안 그려지는 사고를 막으려는 것
- 스크립트를 늘리면 `index.html` · `_test.html` · `_demo.html` · `sw.js` **넷을 같이** 고친다.
  `sw.js` 의 VERSION 도 올린다 (안 올리면 옛 코드가 섞인다)

## 균형 — 지금 어디쯤인가

씨앗 다섯을 바꿔 120개월을 구경꾼으로 굴린 결과다. 수를 고쳤으면 이걸 다시 잰다.

```
2년   13 → 6~10 세력     (약소 세력이 정리된다)
5년   5~7 세력
10년  4~5 세력 · 1위가 서른 중 10~15 성
120개월에 수전 13~22번 · 화공 1~17번 · 판 위의 배 490~660척
무장은 평균 Lv.9 (최고 Lv.12~14) · 승진 26~50단
```

이 수치는 **194년 판**을 잰 것이다. 200년은 세력이 여덟이라 더 빨리 정리되고,
208년은 조조가 19성으로 시작하므로 **균형을 재는 판이 아니다** — 기울어진 것이 그 판의 뜻이다.

(2026-08-26 에 **여러 달 원정**과 **수군**을 넣고 다시 쟀다. 2년째가 덜 정리되는 것은
물길이 배 없이는 막히기 때문이고, 1위 성 수의 폭이 넓어진 것은 포위와 수전이 성패를
크게 가르기 때문이다 — 얼지도 쓸려 나가지도 않는다는 것은 그대로다)

전투는 **×1 무승부 · ×2 성벽을 크게 깎음 · ×3 이상 함락** 언저리다.
**균형은 사람이 봐야 한다** — 이 수치는 "얼지도 쓸려 나가지도 않는다" 는 것까지만 말해 준다.

## 소리(音) — 절차적 생성 (2026-09-10)

**다섯 판 중 이 판만 여태 무음이었다.** `saga-dungeon/js/sfx.js`(33종 절차 음)를
그대로 본으로 삼아 `js/sfx.js`를 새로 얹었다 — WebAudio 로 그 자리에서 합성하고
(에셋 없음), 첫 입력 전에는 안 나고, 꺼져 있어도 요청은 `_tail()`에 남는다.

**이 판만의 차이 — 폴링이 없다.** 사가의숲(계절·유성·발소리를 값으로 폴링)과
달리 이 판은 턴제라 시간이 흐르는 계기가 "다음 달" 버튼뿐이고, 일어나는 일은
전부 `core.emit(...)`으로 이미 이름 붙어 나온다. 그래서 **판정 파일(`war.js`·
`rtk.js`·`officer.js`·`diplo.js`·`ai.js`)은 한 줄도 안 건드렸다** — 전부 이미 있는
이벤트 구독(`rtk:battle`·`rtk:grew`·`rtk:promote`·`rtk:end`·`rtk:discover`·
`rtk:fallen`·`rtk:history`·`rtk:month`·`rtk:journey`·`rtk:journeyEvent`·
`rtk:camp`·`quiz:answered`)과, `toast`의 머리 이모지(🔮 천기·🤝 동맹/합류·
💰 배신/귀순·🔍 재야 발견·🌻 풍년·🌵🌊🦠🦗 재해)로만 갈래를 가른다.

**"내 세력" 문턱.** `rtk:grew`·`rtk:promote`는 AI 세력의 무장에도 매달 똑같이
일어난다 — 안 거르면 세력이 열 개 안팎인 판에서 "다음 달" 한 번에 종소리가
수십 번 겹친다. `officer.js`가 이미 화면 로그를 내 세력으로만 거르는 그 판단을
그대로 베껴 `off.rec(id).force === rtk.me()` 로 조용히 걸렀다. `rtk:battle`도
내가 치거나 맞은 싸움만 듣는다(공격·수비 둘 다 — 대신 관전만 하는 AI끼리의
싸움은 지도 반대편 소식이라 안 울린다).

상단 더보기(⋯) 안 🔊 로 끈다(`settings.sound`). 소리 24종을 하나씩 들어볼 수
있는 `_sfxcheck.html`도 같이 뒀다(사가의숲과 같은 패턴). **소리가 실제로 나는지는
자가진단으로 못 본다** — 자가진단은 아무것도 안 눌러 늘 잠겨 있기 때문이다.
`_test.html` RESULT 116→**128**(12개 보탬, 3회 동일), `_admin.html?selftest`
ADMIN 12/12 그대로. `sw.js` → `realm-v1.23.0`.

## ⚙️ 설정 시트 (2026-09-10)

시리즈 다음 갈래 표(`../SAGA-HANDOFF.md`)의 "⚙️ 설정 화면을 사가고·사가의숲·사가국지로
확장" — 다만 이 판은 다른 네 판과 상단 UI 결이 달라(독(dock)이 성·무장·진영·외교·
학당·기록 여섯뿐이고, "더보기" 개념은 상단 `#top-more` 팝오버가 이미 맡고 있다)
**그 시트 구조를 그대로 복제하지 않고**, 이미 있던 상단 더보기의 🔊 단추 옆에
**⚙️ 설정**을 새로 넣어 기존 시트(성·무장 등이 쓰는 `#sheet`)를 그대로 빌려 쓰게 했다.

- **효과음 on/off + 음량** — 상단의 🔊 단추(빠른 켬/끔)는 그대로 두고, ⚙️ 설정 시트에
  음량 슬라이더를 새로 얹었다(`js/sfx.js`의 `enabled/volume/setEnabled/setVolume`은
  2026-09-10 사운드 신설 때부터 이미 있었다 — 이번에 화면만 붙였다)
- **그래픽 품질은 안 넣었다** — `realm3d.js`에 등급표 자체가 없다(성벽 tier·건물
  단계는 시각적 등급이지 렌더 부하 손잡이가 아니다). 없는 것을 있는 척 만들지
  않았다 — 다른 판처럼 손잡이를 새로 설계하려면 범위가 이 갈래 밖이다
- 설정 시트는 세력을 고르기 전에도 열린다(`renderSheet()`의 "시작함" 문턱을
  `settings` 만 건너뛴다) — 소리는 시작 화면에서도 끄고 싶을 수 있어서다.
  나머지 시트(성·무장 등)는 국토 자체가 없어 그 문턱이 그대로 맞다

자가진단 128/128(3회 동일, 회귀 없음). **실기기 확인 전**(9번 항목에 합류).

## 지도 조이스틱 · 내 땅으로 · 실시간 전투 현황판 (2026-09-10)

"조이스틱이 필요할 것 같고 내가 어느 땅을 소유한지 모르겠어, 실시간 전투
사항도 모르겠어" 신고 — 세계가 삼국지 한 판을 넘어 일곱 지역까지 늘면서 2D·3D
지도 둘 다 늘 전 세계를 한 화면에 욱여넣어 성이 점으로 뭉개져 있던 게 원인이다
(자세한 것은 `PLAN.md` "26-2. 지도 조이스틱·내 땅 표시·실시간 전투 현황판").

- 좌하단 조이스틱(`#rjoy`, saga-go `#gjoy`와 같은 결)으로 2D(`viewBox` 이동·
  확대)·3D(궤도 중심 `pivotX/pivotZ`) 지도를 다 옮길 수 있다
- 🏠 "내 땅으로" — 내 성 무게중심으로 지도를 옮기고 당긴다. 세력을 고른
  직후에도 자동으로 불러 **처음부터 내 땅이 보이게** 했다. 지도 밑 범례의
  내 이름을 탭해도 같다
- ➕➖ 확대·축소 버튼
- 전투 화면에 성 시트가 쓰던 막대(`.rstat`)를 그대로 재사용해 아군·적군
  병력·성벽을 숫자로 보여준다 — 개입형 실시간 전투는 합마다 갱신, AI가
  친 싸움도 시작 대 끝 숫자를 보여준다

war.js 판정·battle3d.js 디오라마·realm3d.js 회전/확대는 한 줄도 안
건드렸다 — 보는 창을 옮기고 이미 있는 값을 숫자로 마저 보여줬을 뿐이다.
자가진단 128/128(3회 동일, 회귀 없음)·`_admin.html?selftest` ADMIN 12/12.
**실기기 확인 전**(9번 항목에 합류).

## 일기토를 실제 캐릭터 대결로 + 맵 드래그(2026-09-10, 같은 날 이어서)

"맵이동이 편해야 하고" · "전투 하는 모션이 삼국지랑 동일했으면 해(리얼타임
액션)" 피드백을 받아 더 갔다(자세한 것은 `PLAN.md` "26-2절 이어서").

- **지도 드래그 팬** — 조이스틱에 더해 한 손가락으로 지도를 직접 밀어서도
  옮길 수 있다(구글지도처럼). 성 탭과 안 부딪히게 살짝 움직이면 탭, 크게
  끌면 이동으로 가른다.
- **일기토(장수 대결)가 깃발 둘 → 실제 QRPG 캐릭터 둘의 실시간 대결로.**
  무장 초상이 쓰던 Quaternius 몸에는 원래 걷기·공격·사망 클립이 있었지만
  `asset3d.js`의 로딩 코드가 그 클립을 한 번도 못 읽어 오던 버그를 고치고
  (사가블로가 이미 검증한 클립-이름 매칭·재생 로직을 그대로 옮겨 왔다),
  `war.js`가 붙여 주는 공격/수비 쪽 장수 id로 실제 모델 둘을 세워 세력
  색을 입혔다. 합마다 맞는 쪽이 공격, 맞은 쪽이 피격 동작을 실시간으로
  주고받고, 끝나면 이긴 쪽은 대기 자세·다친 쪽은 쓰러진 자세로 정리된다.
  병력 무리(수십 명)는 여전히 깃발 다발 그대로다 — 어수선해진다는 원래
  판단은 유지하고 **일기토(단둘의 대결)만** 바꿨다.

war.js의 판정 수식은 그대로다(장수 id 이름표만 더했다). 자가진단
128/128(3회 동일, 회귀 없음)·ADMIN 12/12. **실기기 확인 전** — 특히 QRPG
몸의 기본 정면 방향은 감으로 정해서, 등을 지고 서 보이면 코드 한 곳
(`battle3d.js` `setupDuel()`의 `rotation.y` 부호)만 뒤집으면 된다.

## 핀치 확대·관성 스크롤·일기토 카메라 컷(2026-09-10, 같은 날 세 번째)

"응 계속 추가해 실기는 나중에 한번에" — 실기기 확인을 미루고 같은 두
갈래를 한 단계씩 더 밀었다(자세한 것은 `PLAN.md` "26-2절 세 번째").

- **핀치로 지도 확대·축소** — 드래그 팬에 두 손가락 오므리기를 더했다.
  이미 있던 `zoomMapBy()`(➕➖ 버튼과 같은 것)를 그대로 부른다.
- **손을 뗀 뒤에도 살짝 미끄러진다(관성)** — 빠르게 끌다 놓으면 속도를
  이어받아 매 프레임 10%씩 줄며 계속 밀리다 멎는다.
- **일기토 카메라 컷** — 합을 주고받는 동안만 카메라가 두 장수 가까이
  붙고, 라운드(무리) 전투가 시작하면 다시 넓은 그림으로 돌아간다(뚝 끊기지
  않게 매 프레임 살살 뒤쫓는다). 합마다 부딪히는 소리도 그 타이밍에 맞춰
  같이 낸다.

war.js 판정·`realm3d.js`의 회전/확대·`sfx.js`의 이벤트 구독 자체는 안
건드렸다. 자가진단 128/128(3회 동일, 회귀 없음)·ADMIN 12/12. **실기기
확인 전**(사용자 지시대로 이번엔 미루고 다음에 한 번에 확인하기로 함).

## 시작 카메라를 내 땅 크기에 맞춰 당겼다(2026-09-10, 같은 날 네 번째)

"시작시 너무 멀리서 시작해 이동이 되면 멀리서 볼 필요가 있나?" — 맞는
말이다. "내 땅으로"(`centerOnMine()`)가 예전엔 고정 배율로만 당겨서 세력을
고르자마자 봐도 세계 절반이 같이 보였다(3D는 더 심해서 세계 전체가 들어오는
먼 거리 그대로였다). 이제 **내 성들의 실제 좌표 범위를 재서 그게 화면에
꽉 차는 배율**을 스스로 구한다(2D·3D 다). 조이스틱·드래그·핀치·관성까지
갖췄으니, 더 보고 싶으면 손으로 나가면 된다는 판단이다.

자가진단 128/128(3회 동일, 회귀 없음)·ADMIN 12/12. **실기기 확인 전** —
새 기본 배율이 딱 맞는지는 실제로 세력을 골라 봐야 안다.

## 병력 무리(집단전)도 부딪히는 느낌으로(2026-09-10, 같은 날 다섯 번째)

일기토(단둘)는 실제 캐릭터로 바꿨지만 무리(수십 명)는 여전히 깃발
다발이었다 — "30명을 실제 모델로 세우면 오히려 어수선하다"는 원래 판단은
그대로 두고, 대신 **라운드가 부딪히는 순간**을 세 가지로 더 또렷하게 냈다:
두 무리가 서로에게로 살짝 밀렸다 당겨지고, 카메라가 살짝 훅 당겼다 풀리고,
부딪히는 소리(`sfx.js`의 새 `round_clash`)가 난다. 개입형 실시간 전투
(플레이어가 손수 출진하는 그 싸움)에서도 합마다 똑같이 느껴진다.

war.js 판정은 그대로다. 자가진단 128/128(3회 동일, 회귀 없음)·ADMIN 12/12.
**실기기 확인 전** — 밀리는 폭·카메라 훅 크기가 적당한지는 실제로 봐야 안다.

## 전황 요약도 실시간으로, 일기토 예고장, 피격 번쩍임(2026-09-10, 여섯 번째)

지금까지 쌓인 걸 스스로 점검해 빈 곳 셋을 더 채웠다.

- **전황 요약 화면(AI 가 친 싸움 등)도 이제 합·라운드마다 숫자가 바뀐다** —
  화면(3D 디오라마)은 이미 실시간으로 갈아 끼우고 있었는데 HUD 숫자만
  "시작 대 끝"에 멈춰 있던 어긋남을 고쳤다. 개입형 실시간 전투와 같은
  함수로 갱신한다.
- **일기토 예고장** — 결투 전 "OOO ⚔️ OOO"를 초상 곁들여 보여준다.
- **맞은 쪽이 붉게 번쩍인다** — 사가블로가 이미 쓰던 재질-복제 요령을
  그대로 옮겼다.

war.js 판정·재생 타이밍 자체는 안 건드렸다 — 이미 있던 타이밍에 화면을
하나씩 더 얹었다. 자가진단 128/128(3회 동일, 회귀 없음)·ADMIN 12/12.
**실기기 확인 전**(계속 미루기로 함).

## 성벽이 무너지는 순간을 키웠다(2026-09-10, 일곱 번째)

공성전용 새 소품(사다리·공성추)은 이 저장소에 그런 CC0 자산이 없어 이번엔
안 넣었다(새로 받으려면 자산 조사·라이선스 확인이 따로 필요해 이 갈래
범위 밖이다) — 대신 **성벽이 실제로 한 칸 무너지는 그 프레임**의 반응을
키웠다. 카메라가 실제로 흔들리고(`wallShake`, 라운드 충격의 거리-당김보다
드물고 큰 사건이라 이번엔 진짜 지터를 줬다), 전용 소리(`wall_break`)도 낸다.

war.js 판정은 그대로다. 자가진단 128/128(3회 동일, 회귀 없음)·ADMIN 12/12.
**실기기 확인 전** — 흔들림 세기가 적당한지는 실제로 봐야 안다.

## ⚙️ 전투 화면 흔들림 켬/끔(2026-09-10, 여덟 번째)

성벽 붕괴 카메라 흔들림은 지금까지 넣은 것 중 유일하게 "화면이 진짜로
떨리는" 연출이라, 민감한 사람을 위해 ⚙️ 설정 시트에 켬/끔을 더했다(효과음
켬/끔·음량 옆). 부드러운 것(라운드 충격 거리-당김, 일기토 근접 컷)은 안
가리고 진짜 지터만 끈다.

## 관성 스크롤 버그 고침(2026-09-10, 아홉 번째)

새 기능 대신 이 세션에서 늘린 코드를 스스로 감사했다 — `_test.html`은
war.js·rtk.js 판정만 재서 지도 조작·battle3d 연출은 그 범위 밖이다.

**찾은 것**: 관성 스크롤(`startMapMomentum()`)이 실제로는 안 끊겼다. 손을
떼서 관성이 돌기 시작한 뒤 곧바로 다시 손을 대면, 이미 예약된 애니메이션
프레임이 계속 돌아 **새 드래그와 관성이 동시에 지도를 미는** 상태가 됐다.
매 프레임 자기 상태를 다시 확인하도록 한 줄 고쳤다.

자가진단 128/128(3회 동일) — 고친 버그 자체가 자가진단 범위 밖이라 수치는
그대로다.

## 나이 · 노쇠 · 죽음 (2026-09-10)

"다음에 채울 것"에 남아 있던 마지막 항목이다 — 사용자에게 진행 여부를
먼저 물어(생년 데이터·죽음까지 넣는 것은 밸런스에 미치는 영향이 커서)
**"진행 — 노쇠+사망까지"** 로 답을 받아 시작했다(자세한 것은 `PLAN.md`
"26-3. 나이·노쇠·죽음").

명부 231인 다수가 이 판이 지어낸 가명 인물이라 실제 생년을 조사하는 대신
id 문자열을 해시해 **결정적** 생년을 만든다(130~178년, 재야 산포와 같은
결). `hero.js`(다섯 판 공유 파일)는 한 줄도 안 건드렸다 — `officer.js`의
`stats()` 한 곳에서만 노쇠 배율을 곱한다. 60세부터 능력치가 서서히 줄고
(90세에서 0.55배 바닥), 65세부터는 달마다 자연사할 확률이 생긴다(연
60%가 상한, `rtk.js` `settleMonth()`가 재해·이간과 같은 자리에서 부른다).
**군주는 늙지도 죽지도 않는다** — 승계 체계가 없어 세력이 끝나 버리는
것을 막으려는 의도적 범위 축소다. 죽으면 `dead` 플래그만 세우고(세이브에서
안 지운다), 태수 자리를 비우고, 세 로스터 함수(`atCity`/`freeAt`/
`ofForce`)에서 걸러진다. 무장 시트에 나이·"노쇠" 배지를 보여준다.
`_admin.html`에 `rtk.agingDeathMul` 손잡이(기본 1, 0이면 안 죽는다)를 얹었다.

자가진단 132/132(3회 동일, 새 항목 넷 + 회귀 없음)·ADMIN 12/12. **실기기
확인 전** — 60/65/90세 문턱과 연 60% 상한은 감으로 잡은 값이다.

## 모션 버그 고침 + 지휘관 인물 추가 (2026-09-10, 같은 날 이어서)

사용자가 모션 자연스러움과 인물 추가를 다시 짚어 코드를 훑었다(자세한
것은 `PLAN.md` "26-2절 이어서").

- **죽은 장수가 되살아났다 다시 쓰러지는 버그를 고쳤다.** `asset3d.js`의
  `play()`가 애니메이션 loop 모드를 정한 적이 없어 공격·피격·죽음
  동작이 three.js 기본값(반복)으로 돌았다 — 클립이 끝나자마자 처음부터
  다시 재생됐다는 뜻이다. 한 번만 재생하고 마지막 자세에서 멈춰야 할
  다섯 슬롯(공격·피격·죽음·회피·상호작용)만 `LoopOnce`+
  `clampWhenFinished`로 바꿨다.
- **일기토가 안 붙는 싸움(대다수 — 65%는 애초에 안 붙는다)에도 이제
  지휘관 둘이 실제 캐릭터로 선다.** `war.js`가 이미 계산해 두던 양쪽
  우두머리 장수를 `leadA`/`leadD`로 보고서에 얹었고, `battle3d.js`가
  일기토가 없으면 그 둘로 실제 QRPG 모델을 세운다(idle만, 공격·피격
  없음 — 새 판정이 아니다).

`_test.html`이 3D 파일(`battle3d.js`/`asset3d.js`)을 안 불러오는
자가진단이라 애니메이션 수정은 헤드리스로 못 재고, `leadA`/`leadD`만
새 항목으로 확인(133/133, 3회 동일, 회귀 없음)·ADMIN 12/12. **실기기
확인 전**(일기토 패배 장면·지휘관 등장을 눈으로 봐야 한다).

## 방향 정정 — 삼국지 완전 모방이 아니다 + 아홉째 지역 균열(2026-09-10)

사용자가 방향을 다시 짚었다 — "너무 삼국지처럼 안 해도 돼, 현대 미래
과거 다 있어 / 완전 퓨전이야 / 괴물도 나오고 / 다 짬뽕이야 / 난 완전
모방이 아니야, 모방보다 더 좋아야 해 / 제목도 나중에 완성되면 바꿀
거야". 여덟 지역 확장 내내 전부 고대·역사 지명 오마주로만 갔던 게
이 판 애초의 지향점("시대 제한 없이 지구 전체를 정복")과 어긋나 있었다.
자세한 것과 다음 세션을 위한 메모는 `PLAN.md` "26-4. 방향 정정" 절과
세션 메모리(`saga_realm_fusion_vision.md`) 참고.

이 정정을 코드로 바로 증명하려고 **아홉째 확장 지역 — 균열(龜裂)**을
붙였다. 야마토(일본) 너머, 시간이 뒤섞인 자리라는 가상의 지역 — 처음으로
실제 지명이 아니고, 수비 무장 9인이 사람이 아니라 **괴물**이다. 자리·
판정 구조(성 일곱·`force:null`·`garrison`·`FUTURE_OFFICERS`·
`FUTURE_GARRISON`)는 앞 여덟 지역과 완전히 같다. 몬스터는 `saga-dungeon`
이 이미 검증해 둔 CC0 3D 모델(Quaternius Ultimate Monsters Bundle) 9종을
그대로 복사해 왔고(약 1MB), `asset3d.js`의 `heroRecipe()`에 한 줄만
더해(`ref.monster`가 있으면 그 경로를 몸+몸짓으로 그대로 준다) 새 조립
코드 없이 기존 QRPG 파이프라인을 그대로 태웠다. 2D 지도 viewBox 폭을
동쪽으로 다시 넓혔고(225→270), 3D 는 손 안 댔다(지면이 이미 넉넉하다).

**진단에서 걸린 것** — 성 수가 93 으로 늘면서 "재야 결정적 산포" 해시가
하필 cao(시작 세력)의 성 셋 전부를 비껴가 관련 항목 셋이 한꺼번에
깨졌다. `_test.html`에 `ensureHiddenAtMine()` 헬퍼를 두어 산포 운에
안 기대고 재야 하나를 확실히 내 세력 성으로 옮기도록 고쳤다.

검증 — `_test.html` **133/133**(헤드리스 크롬 3회 동일, 회귀 없음)·
`_admin.html?selftest` ADMIN 12/12. **실기기 확인 전** — 균열 지역
전투에서 몬스터 3D 모델이 실제로 뜨는지는 `_test.html`이 3D 파일을 안
실어 헤드리스로 못 봤다.

## 다음에 채울 것

- ~~**여러 달에 걸치는 원정**~~ — **2026-08-26 완료.** 위 '진영' 절을 보라
- ~~**수군**~~ — **2026-08-26 완료.** 위 '수군' 절을 보라
- ~~**시나리오**~~ — **2026-08-26 완료.** 위 '시나리오 셋' 절을 보라
- ~~**무장 성장**~~ — **2026-08-26 완료.** 위 '무장 성장' 절을 보라
- ~~**나이**~~ — **2026-09-10 완료.** 위 '나이·노쇠·죽음' 절을 보라

## 어드민을 들였다 (2026-08-31)

**이 판에는 여태 어드민이 없었다** — 사가고·사가의숲·사가스토리·사가블로에는 진작 있던 것이다.
`_admin.html` + `js/admin.js` 를 새로 넣고, 그 넷과 같은 이름의 손잡이 층
(`core.tuned`·`setTune`·`POKE_KEY`)을 처음으로 `js/core.js` 에 얹었다.

이 판은 **경영·전략**이라 사가스토리·사가블로의 "사냥터·장비" 짜임을 그대로 못 썼다.
탭 일곱 — 세이브(프로필·연·월·시나리오를 새로 세우기·순위·진행 비우기) · 성(城)(서른 성의
소속·내정·살림을 표로 고친다) · 무장(내 세력·재야·포로 — 충성·승진·등용을 곧장 다룬다) ·
전쟁·외교(진(陣) 목록·철수, 세력 간 동맹·화친) · 균형 손잡이 · QA 프리셋(9개) · 점검·백업.

**`war.capture` 를 부르지 않는다.** 그 함수는 전투 보고서(atk/def)를 요구하고 수비 무장을
달아나게·사로잡게 만드는 부작용이 커서, 어드민에서 사본만 만들어 부르면 오히려 세이브가
어긋난다. 성을 강제로 넘길 때는 소속·치안·태수만 **직접** 고쳐 "막 빼앗은 성" 의 상태를
흉내낸다 — `rtk.js`·`war.js`·`officer.js`·`diplo.js` 같은 판정 코드는 손잡이 여섯 곳
(무장 봉급·군량 소모·상업/농업 배수·태수 보정 상한·재해 확률 — `rtk.js`)을 상수 대신
`core.tuned(...)` 로 읽게 바꾼 것 외엔 한 줄도 건드리지 않았다.

이 판은 **턴제**라 그 손잡이들은 모듈이 뜰 때 한 번만 읽는다 — 어드민에서 바꾼 뒤
게임 창을 새로고침해야 듣는다(다른 판의 물리 손잡이 몇몇과 같은 결이다).

QA 프리셋 아홉은 전부 **결정적**이다(무작위로 성·무장을 흩지 않는다) — 🏁갓 시작한 판 ·
⚔️관도 직전 · 🔥적벽(시나리오가 스스로 손·유 동맹을 건다) · 🏰1위 세력(12성) · 💀패배 직전(1성) ·
👑천하통일 직전 · 💰재정·공적 넉넉 · 🕵️재야 인재 발견 · ⛺원정 진행 중.

`_test.html` 에는 `DG_NO_TUNE = true` 를 추가해 자가진단이 손잡이 값을 읽지 않게 잠갔다.

검증 — `_admin.html?selftest` 헤드리스 → **ADMIN 12/12**(프리셋 9개를 실제로 눌러
예외가 안 나는지까지 포함). `_test.html` 은 손대지 않은 채로 **RESULT 95/95**,
두 번 돌려 완전히 동일(회귀 없음). `index.html` 도 콘솔 무오류로 뜬다.
`sw.js` VERSION → `realm-v1.6.1-phone`.

**폰 폭(390×844)으로도 확인했다.** iframe 뷰포트를 실제 폰 크기로 고정해 두고
탭 7개 전환·손잡이 반영·**QA 프리셋 9개 전부**를 자동으로 눌러 봤다 — 전부
정상, 콘솔 에러 0. **성(城) 표는 열이 열넷이라 폰 폭에 다 안 들어간다** —
`.tablewrap { overflow-x:auto }` 로 감싸 두어 좌우로 스크롤하면 나머지 열이
보인다(잘림이 아니라 의도된 스크롤). GitHub Pages 배포본(`…/swbins/saga-realm/
_admin.html?selftest`)에서도 **ADMIN 12/12** 그대로 확인했다.
