# PC 단독 실행판 산출물 — build-pc.bat 이 언제든 다시 만든다
dist/

# Mixamo 원본 파생물 — 재배포(공개 저장소 커밋) 금지 약관이라 여기 안 올린다.
# 자세한 사정·재현 방법은 assets/ASSET_LICENSES.md 참고. 로컬 기기엔 그대로 둬도
# 게임이 그 파일을 찾아 쓴다 — 파일이 없으면 asset3d.build() 가 cb(null) 로 불러
# 그 사물만 조용히 안 세워진다(다른 사물엔 영향 없다, `asset3d.js` 머리말 참고).
assets/models/people/realistic/
assets/models/anim/mixamo_realistic.glb
