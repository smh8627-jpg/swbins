# PROJECT_STATE — saga-godot 현재 상태 (2026-09-16 재편)

**이 파일은 현재 상태만 담고 세션이 끝나면 덮어쓴다. 상한 15KB.** 세션 기록·경위·판단 이유는 `docs/HISTORY.md` 에 날짜 항목으로 append 한다(항목당 15줄 이내). 규칙: `PLAN.md` 33장 규칙 10(재정의)·104장, `../SAGA-DESIGN.md` §9.
2026-09-16 이전에 이 파일에 쌓였던 6882행은 `docs/HISTORY.md` 첫 절에 그대로 있다.

## 완료 요약 — 다섯 게임 × 진척

| 게임 | 대표 씬 | VS 승인(100단계) | 51장 확장 진척(끝난 것) |
|---|---|---|---|
| GO | `games/saga_go/world/TestVillage.tscn` | **승인** 2026-09-11(사용자 실기 "재미도 있음") | 사건 7+밤 2+굴·여울·폭포 · 역사 인물 조우 3(마을 2·폐허 1, 설득 3라운드) · 도감 5갈래+pet(신수 11 배치) · 동물 6 · 날씨·계절·TimeOfDay · 역참·사진 모드·소문 · 지역 3(마을 11×11·포구 9×9·폐허 7×7, REGIONS 레지스트리) · 저장/버전 · **101-4 ①일과판+마무리 카드**(목표판 3줄·저장 시 세션 요약 카드) · **101-2 ②승급 3택**(부대 레벨업마다 공/수/보 3택+거절) · **101-2 ③패배 비용·회수**(도적전 패배 시 경험치 15%(상한 15) 그 사건에 남고 10분 안에 그 사건을 다시 이기면 회수) · **101-2 ④사당 시련**(파도 4(3+소보스), 하루 3회, 클리어 시 미보유 인물 조우 새로 열림) |
| DUNGEON | `games/saga_dungeon/world/TestRoom.tscn` | **승인** 2026-09-12(⑯~⑱ 실기 확인) | 방 6(보스 2)·방 종류 5(상자·우물·정예 소굴·미니보스·채광)·엘리트 8 · 은사·장비 등급+접사·소켓·룬워드·세트·내구·행상·투전·연단·원소 6·인물 등용·결사·보스층 · 직업 5·무예 9모양×6갈래×3단 완주·갑주 등 부위 6 |
| FOREST | `games/saga_forest/world/TestVillageForest.tscn` | **승인** 2026-09-12 | 채집·낚시·주민 5+부탁·곤충/화석/조개+박물관·순무 시세·벽지/장판·꽃 교배(깊이+경제)·계절행사 8·옷·바이옴 지형 · 가구·집 증축 · 퓨전 몬스터 4(숲도깨비·바위도깨비·버섯정령·꽃정령)+바이옴별 2~4종째. 구면 투영 유지 |
| STORY | `games/saga_story/world/SinyaField.tscn`(세계 첫 자리)·`HeodoField.tscn`(전직)·`TestField.tscn` | 게이트 **미기록**(실기 몰아서) | 사냥터 9 완주 · 무예·MP·사다리·채집·보스·배경 · 장비 10부위 tier1~4·고유·주문서 · 원거리 적 · 업적·상점·사명 20/20+반복/일일 6 · 전직 4단(tier1~4 무예 완주)·SP 투자·칭호 라벨 · NPC_TALK 밀도 |
| REALM | `games/saga_realm/world/TestCity.tscn` | 게이트 **미기록** | 성 107 전부 · 명령 7·전임 · 월드맵(드래그 궤도, 좌표 수정) · 전쟁·외교(조공·화친)·정복 편입·충성·계략·이간·매수 · 문답 260·서고 · 인구·재해·성벽·승진 5단·정복 후 관리 · 타 세력 AI(creed)·멸망 판정·경제 AI · 시나리오 3(기본·200 관도·208 적벽)+새 게임 UI · 3D 몬스터 실루엣 |

**공통(saga_core)**: `data/characters.gd`(인물 105, id 불변)·`data/pets.gd`(신수 11) · `ui/toast.gd`·`ui/virtual_joystick.gd`·`ui/goal_board.gd`+`ui/session_card.gd`(101-4, 목표판 3줄·마무리 카드, 지금은 GO 만 붙임)·`ui/duel_hud.gd`(101-2 ④에서 bandit_encounter.gd 전투 막대·버튼을 뽑음) · `world/world_curve_material.gd`+`shaders/curved_*`(FOREST 구면) · `world/density_report.gd`(104-5, 발견 밀도) · `shaders/cel_toon.gdshader`+`cel_shader_apply.gd`(66-2, 잠정 적용). 렌더러 Forward+/Mobile 이중(66-1, `env_pc.tres`·`env_mobile.tres`). PC 실행 파일 다섯 판 1회 빌드(2026-09-13). 스크립트 321·씬 23.

## 현재 작업

- **2026-09-16, 한 세션에서 104장 Phase 0(①~⑤) 뒤 101-4/101-2 GO 후보 ①②③④까지 이어서 완료**("사가고돗 이어해줘 묻지말고" 세 번, 상세 경위는 HISTORY 날짜 항목들):
  - **104장 Phase 0**: `tools/godot_regress.sh`(5대표씬×3회 md5+error/warn 0+`.import` 되돌림) · STORY·REALM 세이브 `_migrate` 보강 · `ChoicePrompt` 클로저 30곳 재검사(안전) · `saga_core/world/density_report.gd`(발견 밀도: GO 마을 62.5%·포구 61.2%·폐허 80.0% 빈 격자, FOREST 마을 0.0%는 기준 자체가 무의미 → PLAN 105 Q-f).
  - **101-4 ①일과판+마무리 카드**: `saga_core/ui/goal_board.gd`+`session_card.gd`(다섯 판 공용, 지금은 GO 만) — 목표판 3줄("이번 주"는 정직하게 "—")·저장 시 세션 요약 카드.
  - **101-2 ②승급 3택**: `games/saga_go/data/perks.gd`(풀 12, 공/수/보 축). 웹은 인물별 rank up, 이 판은 부대 레벨업에 붙였다. 거절 보상은 경험치(재화 없음).
  - **101-2 ③패배 비용·회수**(표준 F): `games/saga_go/data/drop_state.gd`(autoload) — 도적전 패배 시 경험치 15%(상한 15, 동시 3개)를 그 사건에 남기고 10분 안에 그 사건을 다시 이기면 회수. 웹의 "같은 자리로 돌아와 회수"를 "그 사건을 다시 이겨서 회수"로 좁혔다.
  - **101-2 ④사당 시련**(표준 A E F): `games/saga_go/world/shrine_trial.gd` 신설, `landmarks_builder.gd` `_add_shrine()`이 제단 자리에 심는다. 판정 층은 `duel_rules.gd` 그대로 재사용(파도 4개, 90/120/170/230 세기, 이전 파도의 남은 시간을 다음 파도 시작 시간으로 넘겨 전체 180초 예산을 `DuelRules`의 자체 시간 판정이 지키게 함 — 별도 전체 타이머 안 둠). 하루 3회(실시간 날짜 기준), 실패 시 재입장 10분, 클리어 시 경험치 60(웹 "공적 60" 상당)과 **아직 안 배치된 인물 중 하나를 새로 `hero_encounter.gd`로 인스턴스화**해 그 골격(설득 3라운드)을 그대로 연다 — 마을 고정 조우 둘(현책·해장)·폐허 하나(결사)와 안 겹치게 제외, rarity 3~4 우선. `saga_core/ui/duel_hud.gd`로 bandit_encounter.gd의 전투 막대·버튼을 뽑아 공용화(리팩터, md5 불변으로 무해함 확인).
  - 네 후보 전부 헤드리스 자가진단(임시, 커밋 전 지움)으로 실제 동작 확인 — ④는 `_duel` 필드를 직접 조작해 파도 4개를 프레임 없이 빠르게 이겨 끝까지 흐름(클리어→인물 스폰, 일일 횟수 차감)을 직접 봤다. 헤드리스 3회 회귀 통과(GO md5는 매번 바뀜 — 새 로직이 로그를 바꾸는 게 당연, 3회 동일·error/warn 0으로 확인).
- 그 앞(이전 세션, 2026-09-16): GO 폐허에 3번째 역사 인물 조우, 포구 콘텐츠 5호(고래뼈), 96·97 재감사(고칠 것 없음).

## 다음 작업 (우선순위 — 상세는 PLAN 해당 장)

1. **실기 확인 몰아서**(아래 "실기 확인 대기") — 사용자 몫. 결과는 HISTORY 에 날짜 항목으로. 이번 세션의 목표판·마무리 카드·승급 3택·패배 비용·사당 시련 화면도 여기 추가됨.
2. **PLAN 101-2 GO ⑤봉수대**: REGIONS 지도 해제(탑에 오르면 지도가 열린다) — 101-2 순서대로 다음.
3. **PLAN 102장 그래픽 1차**: WorldEnvironment 재설정(AgX·SSAO·Glow·Adjustments·Fog) → 툰 톤 사람 승인 → 아웃라인.
4. **PLAN 105장 열린 질문** 답 받기(완성판 트랙·캐릭터 스타일·후보 폴더 삭제·SDFGI/LightmapGI·FOREST 발견 밀도 기준).

## 알려진 오류

- 코드 오류: 없음(헤드리스 회귀 오류 0). `ChoicePrompt` 클로저 null 크래시는 2026-09-16 고침.
- 그래픽 결함(오류 아님): 셀셰이더에서 흰 알베도 + rim + `env_pc.tres` glow 가 겹쳐 하얗게 날아감 — 톤 미확정(102장).
- 문서: `saga-unity/`와 그래픽 목표가 갈라섰다(이쪽 카툰, 그쪽 사실적) — 개념 공유 안 함.

## 테스트 상태

- 헤드리스: `--headless --editor --quit` 임포트 오류 0 · 다섯 대표 씬 `--quit-after 3~8 --verbose` 오류·경고 0 · GO 3회 로그 md5 동일(2026-09-16).
- GUI: 2026-09-15 다섯 대표 씬 스크린샷 정상 렌더(포구는 세이브 위치 복원 때문에 못 봄). 2026-09-12 이전 실기 목록은 전부 해소.
- PC 빌드: 2026-09-13 다섯 판 1회 성공.

## 실기 확인 대기 (항목명만 — 상세는 `docs/HISTORY.md` 해당 날짜)

- **GO**: 포구 전체(늙은 어부·갈매기·게·표류물·조각배·고래뼈) · 폐허(지형·갈림길·옛 유물·"결사" 설득 3라운드) · 신수 11 조우 · 역참·사진 모드·소문 · 목표판 3줄(화면 위치·겹침 여부) · 저장 시 마무리 카드(문구·닫기) · 레벨업 승급 3택 카드(문구·손맛, 실제로 여러 번 레벨업해 카드 다양성 체감) · 도적전에서 실제로 져서 짐 드랍 문구 확인 후 10분 안에 다시 이겨 회수 체감 · 사당 시련 실기 확인(입장 프롬프트·파도 4개 손맛·클리어 후 새 인물 조우·하루 3회 제한 체감)
- **DUNGEON**: 방 종류 5 · 엘리트 8 · 방 6/보스 2 동선 · 갑주 등 부위 6 슬롯 UI · 활성 무예 45종 손맛
- **FOREST**: 바이옴별 2~4종째 몬스터 · 집 증축 · 꽃 교배 경제 · 벽지·장판 신규
- **STORY**: NPC_TALK 밀도 · 칭호 라벨 · 장비 tier2~4·고유·주문서 · 원거리 적 · 업적·상점·사명 화면
- **REALM**: 문답 260 · 시나리오 선택 UI · 월드맵 좌표(수정 후) · 3D 몬스터 실루엣 · AI 되받아침·멸망 판정
- **공통**: 셀셰이더 톤(rim/glow) 최종 승인 · Forward+ 실제 화질 · Mobile 프로파일 실기기
