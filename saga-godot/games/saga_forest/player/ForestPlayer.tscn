[gd_scene load_steps=5 format=3]

[ext_resource type="Script" path="res://games/saga_go/player/player.gd" id="1"]
[ext_resource type="Script" path="res://games/saga_dungeon/player/dungeon_camera_rig.gd" id="2"]
[ext_resource type="PackedScene" path="res://assets/characters_vroid/saga_forest_avatar_01.glb" id="3"]

[sub_resource type="CapsuleShape3D" id="CapsuleShape1"]
radius = 0.45
height = 1.7

[node name="Player" type="CharacterBody3D" groups=["player"]]
script = ExtResource("1")

[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
shape = SubResource("CapsuleShape1")
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.85, 0)

[node name="Visual" parent="." instance=ExtResource("3")]
## VERTICAL_SLICE_FOREST.md 3절 — 곡률(1절)은 정점 셰이더라 카메라와
## 무관하지만, 플레이어 자신은 곡률 중심 그 자체라(delta≈0) 안 굽는다.
## 2026-09-19⑮ — 사용자가 만든 VRoid 아바타(saga_forest_avatar_01.vrm)로
## 교체(105 Q3 결정 a, GO Player.tscn과 같은 결). headless 스크립트로
## 실측 키(1.643m)를 1.7m 표준(105 Q-h)에 맞춰 역산한 스케일(×1.0344).
## 얼굴은 GO와 같은 이유(7서피스 데칼)로 Blender Selected-to-Active
## 베이크가 필요해 `tools/asset-forge/vroid_face_bake_project.py`로
## `generated/saga_forest_avatar_01_Face_Baked.png`를 새로 구웠다
## (cel_shader_apply.gd가 GLB 경로로 캐릭터별 베이크를 찾아 쓴다).
transform = Transform3D(1.0344, 0, 0, 0, 1.0344, 0, 0, 0, 1.0344, 0, 0, 0)

[node name="CameraRig" type="Node3D" parent="."]
script = ExtResource("2")
## VERTICAL_SLICE_FOREST.md 3절 결정 — pitch_deg=62.0(DUNGEON 55°보다 더
## 위에서), spring_length=14.0(DUNGEON 12m보다 조금 멂). 새 스크립트를
## 안 짜고 DUNGEON의 dungeon_camera_rig.gd를 그대로 크로스게임 재사용한다
## (그 스크립트는 DUNGEON 전용 로직이 없는 순수 범용 컴포넌트 — 3절 근거).
## 방·집 치수는 원래부터 사람 스케일이라(105 Q-h) 거리·피치는 그대로 둔다.
pitch_deg = 62.0
spring_length = 14.0
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.85, 0)

[node name="SpringArm3D" type="SpringArm3D" parent="CameraRig"]
spring_length = 14.0
collision_mask = 1

[node name="Camera3D" type="Camera3D" parent="CameraRig/SpringArm3D"]
current = true
