# 사가스토리 (saga-story)

> 시리즈 전체 인계(다섯 게임 표·지시 이력·함정·다음 할 일)는 **`../SAGA-HANDOFF.md`** 다.

**메이플스토리식 2D 사이드스크롤.** 뛰고, 썰고, 오른다.

**장기 지향점**: 맵과 맵 사이 로딩 없이 걸어서 이어지는 오픈월드 사이드스크롤을
목표로 한다.

```
run.bat          →  http://127.0.0.1:8794
start_server.bat →  같은 것, 브라우저를 열지 않는다 (허브 워치독용)
_test.html       →  자가진단 79항목
_demo.html       →  스크린샷용 데모 상태 (#boss · #hunt · #camp · #field · #dex · #log)
```

## 앞으로 — 3D 로 옮긴다 (`PLAN.md`)

이 폴더의 **`PLAN.md`** 가 앞으로의 방향이다. 이 README 의 나머지는 **지금까지 만든 것**이니,
손대기 전에 둘을 같이 본다.

가는 곳은 **3D 횡스크롤 액션 RPG** 다. 2D 를 3D 로 갈아 끼우는 것이 아니라, 옆으로 걷는
놀이에 **공간의 깊이**를 넣는 쪽이다 — Background / Midground / Foreground 레이어와,
실제로 놀이가 되는 Gameplay Depth.

**Phase 1~6** 으로 잘라 간다 — 현재 분석·3D 기반 → 몬스터·기본 공격 → 스킬·콤보 →
맵 확장·플랫폼 → 성장(레벨) → 콘텐츠(NPC).

계획 맨 앞에 **토큰 절약**이 규칙으로 박혀 있다 — 파일을 통째로 읽지 말고 필요한 곳만
찾아 읽기, 고친 곳만 짧게 보고하기. 큰 작업이라 그렇다.

## 네 게임 중 하나

사가 시리즈는 2026-08-24 부터 **완전히 별개인 다섯 프로젝트**다.
다섯 게임이 다 원작 장르를 그대로 모방한다.

| 게임 | 폴더 | 포트 | 원작 | 세이브 키 |
|---|---|---|---|---|
| **사가고** | `saga-go` | 8791 | 포켓몬GO | `deungyong-go/save/v1` |
| **사가블로** | `saga-dungeon` | 8792 | 디아블로 | `yeoksa-dungeon/save/v1` |
| **사가의숲** | `saga-forest` | 8793 | 동물의숲 | `yeoksa-village/save/v1` |
| **사가스토리** | `saga-story` | 8794 | 메이플스토리 | `yeoksa-side/save/v1` |
| **사가국지** | `saga-realm` | 8795 | 삼국지(코에이) | `saga-realm/save/v1` |

**공용 파일은 복사본이다** — `data.js`(인물 105) · `sprite.js` · `core.js` · `hero.js` 가
다섯 프로젝트에 각각 한 벌씩 있다. 문답은 사가국지만 갖는다.
사용자가 그렇게 고른 구조다(완전 독립). **인물을 하나 추가하면 다섯 곳을 고쳐야 한다.**

**2026-09-10 — HEROES에 일본사(20명) 신설 + 유럽사를 세계사로 개명하고
비유럽 세계 각지 인물(15명) 보강**, 인물 70→105. 다섯 판 `data.js`·
`sprite.js`를 같이 고쳤다(md5 일치 확인). 새 인물의 외형(복장)은
`sprite.js`의 `LOOK` 표를 그대로 확장해 자동으로 따라온다.
세이브도 완전히 분리돼 있어 도감·성장이 서로 오가지 않는다.

**2026-09-10 — 3D 몸 외형 후보 20종(MPFB2 실사, `saga-go`에서 복사)을
`js/asset3d.js`에 선언**, 같은 날 뒤이어 saga-go의 리타깃 코드
(`retargetInto`·`firstSkinned`·`boneNameMap`·`sceneHeight`)를 옮겨 오고
**표 기본(`DEFAULTS.hero`)에 실제로 얹었다.** 이 판은 `side-view3d.js`가
`asset3d.buildHero()`를 **실제 플레이 화면**에서 부르므로(사가의숲과
달리 죽은 자리가 아니다), 몸마다 다른 `heightPx`(58·75.4·110.2 등)로
불려도 리타깃 캐시가 뒤틀리지 않게 **정규화 전 원본 몸을 기준으로 재고
굽는다**(코드 주석 참고 — 사가의숲과 다른 점). 자가진단 134/134(3회
동일). **실기기 확인은 아직**(20종이 실제 플레이 화면에서 걷는 모습을
직접 볼 것). 자세한 사정은 `assets/ASSET_LICENSES.md` 참고.

## 가입해서 저장 (account.js)

첫 실행 때 **이름**을 물어보고, 그 이름으로 진행이 저장된다. 서버는 없다 —
이 브라우저 안에서만 도는 가입이고 비밀번호도 없다.

```
세이브 키   <게임>/save/<프로필id>      ← account.js 가 정해 준다
프로필 목록 <게임>/save/accounts
옛 세이브   <게임>/save/v1             ← 첫 가입 때 '이어받기' 로 옮긴다
```

- 게임 코드는 `game.js` 의 `boot()` 에서 **`DG.account.gate(start)`** 한 줄만 부른다.
  gate 가 프로필을 정해 `core.setSaveKey()` 를 부른 뒤 게임을 켠다
- 상단 **👤** 는 account.js 가 직접 끼워 넣는다 (index.html 을 고칠 필요가 없다).
  이름 바꾸기 · 다른 이름으로 새 판 · 삭제
- 이름은 **게임마다 따로**다 (다섯 프로젝트가 별개이므로)
- 자가진단은 `window.DG_NO_ACCOUNT = true` 로 가입 화면을 건너뛰고 `…/save/test` 를 쓴다

## PC 에서 하기

```
build-pc.bat  →  dist/<게임이름>.html (한 파일) · play.bat · 사용법.txt
```

css·js 를 전부 한 파일에 녹여 넣으므로 서버도 파이썬도 필요 없다. 다섯 판을 한 번에
묶으려면 `tools/saga-pc/build-all.bat` — `saga-pc/dist/` 에 다섯 html + 런처(index.html)가 나온다.

## 자가진단

```
chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom   http://127.0.0.1:PORT/_test.html        → RESULT n/n
```

**무작위는 씨앗으로 고정돼 있다.** `_test.html` 이 게임 스크립트를 불러오기 **전에**
`Math.random` 을 mulberry32(씨앗 20260824)로 갈아 둔다. 그래서 자동 순행을 몇 분치
굴려 보는 항목도 **매번 같은 수치**가 나온다 — 전에는 그 항목들이 드물게 실패했다
(같은 코드로 24/25 → 25/25). 게임 코드는 손대지 않았으니 실제 플레이는 그대로 무작위다.

세 번 돌려 결과가 **한 줄도 다르지 않은지**로 확인한다. 시각 기반 값(프로필 id 등)은
결과 문구에 넣지 않는다 — 넣으면 매번 달라져서 "무엇이 바뀌었나" 를 눈으로 못 쫓는다.

**어드민은 따로 본다.** 눌러야 도는 화면이라 `_test.html` 이 붙지 못한다:

```
chrome --headless=new --disable-gpu --dump-dom "http://127.0.0.1:PORT/_admin.html?selftest"   → ADMIN n/n
```

**소리가 실제로 나는지도 따로 본다.** 진단은 아무것도 누르지 않아 브라우저가 소리를
늘 잠가 두기 때문이다(그 규칙이 맞다). `_sfxcheck.html` 이 `unlock()` 을 스스로 부른다:

```
chrome --headless=new --disable-gpu --dump-dom http://127.0.0.1:PORT/_sfxcheck.html   → SFX unlock=true · 낸 소리 n/n
```

## 손볼 때

- `core.hash2` 는 **부호 없는 시프트**로 고쳐 뒀다(옛 식은 0~0.5 만 돌려줘서, 문턱값을
  0.9 로 잡으면 아무 것도 생기지 않았다). `deungyong-go` 에는 아직 옛 식이 남아 있다
- 파일을 늘리거나 고쳤으면 **`sw.js` 의 VERSION 도 같이 올릴 것.** 안 올리면 브라우저가
  옛 파일을 물고 있어 한 화면에 세대가 섞인다(그래서 실제로 죽은 적 있다)
- 자동 설정(`save.auto`)은 `auto.js` 의 `st()` 가 **빠진 칸을 채워 준다**
- 헤드리스 크롬(`--screenshot`/`--dump-dom`)에서는 `requestAnimationFrame` 이 거의 돌지
  않는다. 움직임은 스크린샷으로 판정할 수 없으니 `_test.html` 처럼 `update(dt)` 를
  직접 굴려서 본다

## 원작처럼 보이게 (2026-08-26, 2026-09-10에 기본값 해제)

**규칙은 한 줄도 안 건드렸다.** 외피는 새 파일 `css/maple.css` 한 장이고,
`index.html` 에서 그 `<link>` 한 줄을 빼면 예전 모습으로 돌아온다
(사가블로의 `css/diablo.css` 와 같은 방식이다).

**2026-09-10 — 사용자 요청으로 `index.html`에서 그 `<link>` 를 뺐다**(사가고 UI와
테마·독·상단 도구줄 구조를 맞추려는 것). 파일 자체와 아래 내용은 그대로 남아 있고,
`_test.html`·`_demo.html` 은 여전히 이 외피를 걸고 돈다 — 되살리려면 index.html에
`<link rel="stylesheet" href="css/maple.css">` 한 줄만 다시 넣으면 된다.

옮긴 것 여섯 —
**밝은 창**(어두운 유리 → 흰 종이 + 굵은 남색 테) ·
**볼록한 단추** · **체력 빨강 · 기력 파랑 두 줄** ·
**미니맵**(왼쪽 위 밝은 창) ·
**뒷배경**(하늘 맵은 둥근 언덕 두 겹 + 구름 · 숲은 둥근 나무 두 겹 · 굴은 종유석) ·
**데미지 숫자**(굵고 크게, 검은 테를 둘러 위로 튄다. 100 이 넘으면 더 크고 금빛).

`sprite.js` 에는 **넷째 양식 `maple` 을 넣고 기본값으로 삼았다**(📗 단추가 넷을 돈다).
그림책풍(`storyize`)과 **같은 픽셀 훑기를 값만 달리 쓴다** — 채도를 올리고, 색을
넓은 계단으로 눕히고, 실루엣을 진하게 두른다. 작은 그림에는 안쪽 선을 긋지 않는다
(그으면 형태가 선에 먹혀 검은 덩어리가 된다).
넥슨의 실제 에셋은 쓰지 않는다 — **그림 문법만** 옮기고 그림은 이 저장소의 코드가 그린다.

## 이 게임만의 것

- **내 몸은 앞에 세운 인물** — 그 인물의 능력치가 체력·공격력이 된다(`side.power`).
  도감에서 다른 인물을 앞에 세우거나 승급하면 세진다
- **물리** — 중력 1900 · 점프 760 · 달리기 270. 발판은 **위에서만** 밟힌다
  (아래에서 올라갈 때는 통과한다 — 사이드스크롤의 기본 감각)
- **줄과 문** — 밧줄·사다리는 **↑↓ 로 오르내리고**(줄 위에서 Space 는 손 떼기),
  ↓+Space 로 밟고 선 발판을 빠져나간다. 사냥터 끝의 **문 앞에서 ↑** 를 누르면
  옆 사냥터로 걸어 넘어간다 — 체력·기력·주운 금은 그대로 이어진다.
  좌표는 `data-side.js` 의 `ropes`(`[x, 위, 아래, 종류]`) · `portals`(`[x, 갈 곳]`)
- **직업과 무예** — 처음엔 **무명**(연참·횡소·기탄·기합 넷 고정).
  **Lv.10 에 1차 전직**(무사·궁수·협객·방사), **Lv.25 에 2차**(장군·신궁·자객·도사),
  **Lv.45 에 3차**(원수·비장·귀영·진인 — 2026-08-26),
  **Lv.70 에 4차**(전신·궁성·명왕·천존 — 2026-09-10, 갈래의 끝).
  되돌릴 수 없다. 윗자리로 오르려면 **아랫자리 무예 하나를** 2차는 5, 3차는 8,
  **4차는 10(만렙)** 이상 익혀야 한다. **갈래는 넷 그대로**다 — 다섯으로 늘리면
  인물 능력치와 무기가 따라가야 한다. 대신 각 갈래를 두 단 더 높였다.
  직업 **17** · 무예 **64**
- **무예 점수(SP)** — 레벨마다 3점. **세이브에 담지 않는다** — 총점 (레벨-1)×3 에서
  찍은 레벨의 합을 뺀 것이 남은 점수다(파생값이라 옛 세이브에도 그냥 맞는다).
  **찍은 무예만 조작 띠(1~8)에 놓인다** — 원작 그대로.
  띠는 여덟 칸이고 **윗자리 무예부터** 놓는다(4차까지 열리면 한 갈래가 열다섯이라
  아래에서부터 채우면 새로 얻은 무예가 영영 자리를 못 잡는다 — 4차 넷 + 3차 넷이
  이미 여덟을 정확히 채우므로 띠를 더 늘릴 필요는 없다)
- **무예의 효과 아홉** — `melee`(앞) · `aoe`(주위) · `bolt`(관통) · `arrow`(첫 하나) ·
  `volley`(여러 발) · `dash`(밀고 나가며) · `rain`(앞쪽에 쏟기) · `buff` · `heal`.
  데이터(`data-job.js`)는 무엇을 할지만 적고 **어떻게 하는지는 `side.js` 에만** 있다.
  **3차·4차 서른여섯을 넣으며 효과는 하나도 안 늘렸다** — 늘리면 규칙을 두 곳에
  적게 된다(진단 '무예 예순넷은 side.js 가 아는 효과 아홉만 쓴다')
- **사냥터 4곳** — 허창 들판(Lv.1) → 오림 숲(Lv.5) → 한중 굴혈(Lv.12) →
  **호로곡(葫蘆谷)(Lv.25)**. 적은 던전 게임과 같은 `data-enemy.js` 를 쓴다
- **멀리서 쏘는 적** — **든 무기가 곧 사거리다.** 활(`bow`)·조총(`staff`)을 든 적은
  사거리(360·420)에 들면 **멈춰서 쏜다**. `data-enemy.js` 는 던전 게임과 나눠 든 같은
  파일이라 거기에 `ranged` 칸을 만들지 않았다 — 규칙은 `data-side.js` 의 `rangedOf()`
- **장비** — 부위 일곱(무기·투구·갑옷·전군·전화·수갑·망토) × 네 단(Lv.1·5·12·20).
  낀 것이 **인물의 힘 위에 얹힌다**(`side.power()` 가 `gear.bonus()` 를 더한다).
  방어는 맞는 값을 깎되 **6할까지만** 막는다 — 넘게 두면 후반에 아무것도 안 아파진다
- **주문서** — 원작의 그 물건. 100%·60%·10% 셋. **실패해도 물건은 터지지 않고**
  업횟(`left`)만 닳는다. 초기 원작도 그랬다
- **가방 24칸 · 저자(상점)** — 차면 못 줍는다(그 답답함이 규칙이다). 파는 값은 산 값의 3할
- **사명(使命)** — 원작의 퀘스트. 열셋(되받는 것 셋 포함). **받은 뒤부터** 센다.
  장비·무예·금처럼 **보면 아는 것은 세어 두지 않고 그때그때 읽는다** —
  세어 두면 장비를 벗었다 껴도 수가 남아 어긋난다. 사냥터 시트 안에 있다
- **앉아 쉬기** — ↓ 를 누른 채 가만히 있으면 앉아 체력 2%/초 · 기력 5.5%/초를 채운다.
  곁에 적이 오면 일어선다 (원작에서 의자에 앉던 그 자리다)
- **미니맵** — 왼쪽 위. 자리·축척(`sideView.miniBox`)은 **그리기와 떼어 둬** 진단이 붙는다
- **탕약🧪** — Q 로 마신다(체력 45%). 잡을 때 가끔 떨어진다
- **쓰러지면** 그 판에서 주운 금의 **절반만** 남는다 (사냥터는 그대로)
- **자동 사냥(🤖)** — 가까운 적으로 달려가 베고, 적이 한 층 위/아래면 **줄을 타고**,
  체력 35% 밑이면 탕약, 18% 밑이면 나온다. 줄을 쓰게 되면서 5분치 수확이
  **36 → 120마리**로 올랐다. 그 뒤 궁수(**79**)·줄 고르기 좁히기(**46**)를 거쳐
  그 뒤 자동이 띠에 놓인 무예를 **종류대로**(근접/원거리/돌진/북돋움/치유) 쓰게 해
  **97마리**가 되었고(자리 번호로 고르면 전직한 뒤 근접 무예를 멀리서 휘두른다),
  **급소가 들어와 지금은 108마리**다 — 15%×1.6배가 그대로 수확에 얹혔다
  (진단 '자동 — 5분치를 굴리면 실제로 잡힌다')

## 폰에서 하기 (2026-08-26)

폰(390px)에서 실제로 열어 보니 네 군데가 막혀 있었다. **규칙은 한 줄도 안 고쳤다** —
막힌 것은 손이 닿는 자리였다.

- **↓ 가 아예 없었다.** 캔버스를 위/아래좌/아래우 셋으로만 읽어서, 폰에서는
  **줄을 타고 내려갈 수도, 앉아 쉴 수도, 발판을 빠져나갈 수도 없었다** — 규칙은 다
  있는데 손이 닿지 않는 자리였다. 아래쪽 **가운데 22%** 를 ↓ 로 냈다.
  좁게 잡은 것은 걷다가 잘못 눌리지 않게 하려는 것이다

  ```
  ┌─────────────────────┐
  │   위 45%  ↑ / 점프   │   줄·문 앞이면 오르기·들어가기, 아니면 점프
  ├──────┬───────┬──────┤
  │  ←   │   ↓   │  →   │   가운데 22% 가 ↓ 다
  └──────┴───────┴──────┘
  ```

- **누른 채 끌면** 손가락을 따라 방향이 바뀐다(`setPointerCapture` 로 HUD 위를 지나도
  안 끊긴다). 끌어서 위로 올라가는 것은 점프로 읽지 않는다 — 걷다가 뛰어 버린다
- **상단이 무너졌다.** 프로필 이름이 한 글자씩 줄바꿈되고 도구 여섯 중 둘이 화면 밖으로
  나갔다. 560px 아래에서 이름은 한 줄로 자르고 도구는 **넷씩 두 줄**로 접는다
- **조작 띠 여덟 칸이 독에 덮였다**(3차 전직으로 여섯에서 여덟이 되었다).
  폰에서는 **접어서 두 줄**로 두고 독보다 위로 올린다
- **보스 바와 미니맵이 상단 UI 와 한 자리를 다퉜다.** 좁은 폭에서 상단이 두 줄로
  접히므로 미니맵은 196px, 보스 바는 254px 로 내렸다 — 폰에서 **무엇이 달려드는지**를
  못 읽던 자리다
- **폰에 없는 키를 누르라고 안내했다.** `core.touchOnly()`·`actHint()`·`upHint()`·
  `downHint()` 를 두어(사가의숲과 **같은 이름**) 손가락뿐인 기기에서는 누를 자리를 적는다.
  **키보드가 있는 기기는 예전 문구 그대로다** — 그래서 진단 출력도 안 바뀐다

`_mobileframe.html` 이 폰 화면을 보는 껍데기다. **헤드리스 크롬은 창 폭을 500px 아래로
못 내리므로**, iframe 안쪽에 진짜 390px 뷰포트를 만든다(사가의숲과 같은 꼴).

```
chrome --headless=new --disable-gpu --window-size=700,900 \
  --touch-events=enabled --blink-settings=primaryHoverType=1,availableHoverTypes=1,\
primaryPointerType=2,availablePointerTypes=4 \
  --dump-dom "http://127.0.0.1:PORT/_mobileframe.html#probe"        → PROBE OK
```

`#probe` 는 실제로 이벤트를 쏘아 **걷기·점프·내려가기가 듣는지**와 **단추가 화면 안에
있는지**를 함께 본다. `#hunt`·`#gorge`·`#bag` 같은 해시로 그 화면을 찍어 볼 수도 있다.
손가락뿐인 기기 흉내는 위의 `--blink-settings` 가 낸다 — 그것 없이는 헤드리스가
**hover 가 있는 기기**로 잡혀 터치 분기가 안 걸린다.

## 넷째 사냥터 — 호로곡(葫蘆谷) (2026-08-26)

3차 전직이 Lv.45 인데 마지막 사냥터가 Lv.12 에 열려 있었다. 무예를 스물 얹어 놓고
**쓸 데가 없었다** — 그 간극을 메우려고 넣었다. 2차 전직과 같은 Lv.25 에 열리고,
**2차로 들어와 3차로 걸어 나가는 곳**이다.

- **처음으로 정예(tier 4)가 나온다** — `enemyLv` 26 이 `data-enemy.js` 의 `tierOf` 문턱
  (26~)을 넘긴다. 철갑 중장병 · 근위 기병 · 연노 사수 · 수군 함대 · 흑기병 다섯 종이고
  보스는 **적국 대장군**(40분 리젠 · 체력 20배 · 공격 2.8배)
- **수치로 선 계단** — 정예 체력 2,596. 2차 패왕격(만렙)이 1,576 이라 **두 대**,
  3차 천붕격이 3,568 이라 **한 대**다. 2차로도 들어갈 수는 있고, 3차면 수월하다
- **굴혈 오른쪽 문**으로 걸어 넘어간다 — **보스가 선 자리를 지나야 나간다**(원작의 그 배치).
  돌아오는 문도 냈다(한쪽만 내면 갇힌다)
- **지형이 가장 험하다** — 발판 여덟에 줄 여덟, 폭 3,400. 바닥에 오래 서 있을 자리를
  주지 않는 것이 이 사냥터의 성격이다
- **불타는 골짜기**(`mood: 'fire'`) — `side-view.js` 의 넷째 배경이다. 겹 넷:
  멀리 선 검은 산줄기 · 골짜기 벽 · 바닥에서 오르는 불길 · 떠오르는 불티.
  **에셋은 없다**(도형과 그러데이션만) — 불길은 시각으로 흔들리지만 **판정에는 닿지 않는다**

**사냥터를 더 늘릴 때 같이 봐야 하는 것 둘** — ① `enemyLv` 가 적 풀의 tier 를 고른다
② `mood` 는 `side-view.js` 의 `drawBackdrop` 이 아는 이름이어야 한다(모르는 이름을 주면
하늘만 있고 뒤가 빈다). 그 둘만 맞으면 사냥터 시트·미니맵·어드민·자동은 표를 훑으므로
따라온다. 진단에 그 둘을 지키는 못을 박아 두었다.

`_demo.html#gorge` 로 본다.

## 전투 연출 (2026-08-26)

사가고의 `duel.js` 가 판정 층과 화면 층을 가른 그 자리를 이 판에도 세웠다.
**판정에 닿는 것은 둘뿐이고, 나머지는 화면 층에만 있다.**

**판정 층**(`side.js`) —
· **급소(急所)** 원작의 크리티컬. 15% 로 터지고 1.6배 아프다. 같은 무예를 같은 적에게
  써도 수치가 갈려야 손이 계속 간다. 어드민에서 확률·배수를 잡는다
· **넉백** 맞은 적이 뒤로 밀린다. 위치만 바뀌고 피해는 그대로다 — 판정을 흔들지 않으면서
  "때렸다" 는 감각을 주는 가장 싼 값이다. **보스는 밀리지 않는다**(밀리면 달려드는
  패턴이 뜻을 잃는다)

**화면 층**(`side-view.js`) — 여기 있는 것은 통째로 걷어도 판정이 한 자도 안 바뀐다 ·
**화면 흔들림**(side.js 는 `'shake'` 한 줄만 남기고 세기도 위상도 화면이 정한다) ·
**급소 숫자**(더 크고 붉은 금빛에 `!` 가 붙고 한 박자 부풀었다 가라앉는다) ·
**넘어가는 몸**(죽은 적은 목록에서 빠지고 모습만 남는다 — 남겨 두면 그 자리가 계속
맞고 부딪친다) · **착지 먼지**(세게 떨어졌을 때만) · **피격 비네트** ·
**보스 돌진 예고**('달려든다!' — 소리만으로는 못 듣는 사람이 있다)

**경직(硬直)**은 `game.js` 의 루프에만 있다. 급소가 터지면 0.055초 동안 시간이 1/8 로
느려진다 — 원작에서 큰 타격에 잠깐 멈추던 그 감각이고, 손맛의 절반이 여기서 나온다.
**자가진단과 자동 사냥은 `update(dt)` 를 직접 굴리므로 경직에 닿지 않는다.**

밟은 자리 하나 — **데미지 숫자가 겹쳐 한 덩어리로 읽혔다**(주위를 쓰는 무예로 셋을
치니 "24"·"42"·"35" 가 `244235` 로 보였다). 같은 순간에 가까이서 난 숫자를 세어
**층으로 쌓아 올려** 풀었다. 세는 값이 fx 목록의 순서라 늘 같은 자리에 선다.

`_demo.html#crit` 이 이 연출을 한 장면에 세운다 — 정지 화면으로 볼 유일한 수단이다.

## 마을 사람끼리의 잡담 · 고유(固有) 장비 — 2026-09-10

**시리즈 다음 갈래 표(`../SAGA-HANDOFF.md`)의 "사가스토리에 NPC 잡담 / '고유' 아이템
이식"** — 사가의숲 `js/folk.js`(주민끼리의 대화)와 사가블로 `js/data-unique.js`(원작의
이름 있는 전설급 장비) 패턴을 이 판의 사정에 맞게 옮겼다.

**NPC 잡담** (`js/side.js` · `data-side.js` `NPC_CHAT`) — 이 판의 마을 사람(`NPC_TALK`)은
사가의숲 주민과 달리 **제자리에 선 표지판**이라 실제로 걸어서 마주치는 장면을 그대로
옮기지 않았다. 대신 **마을(`town:true`)에 머무는 동안 이따금 둘 사이에 오간 말 한 토막을
전해 듣는다** — 평균 16초마다 굴려 40% 확률로, 마을에 있는 아무 둘을 골라 `NPC_CHAT`
화제표에서 한 쌍을 뽑아 첫 마디는 토스트로, 둘 다는 로그로 남긴다. 대화창(`run.talk`)이
열려 있는 동안은 쉰다. 사냥터엔 없다 — 싸우는 자리라 한가한 잡담이 안 어울린다.

**고유 장비** (`js/data-unique.js` · `js/gear.js`) — 이 판은 장비에 접사(무작위 옵션)가
없다(`data-gear.js` 의 표값 그대로). 그래서 고유도 사가블로처럼 접사 위에 얹는 게 아니라
**부위마다 Lv.20(마지막 단) 물건 하나에 매인, 표에 적힌 값 그대로 나오는 이름 있는
물건** 일곱 개로 두었다(`u_sword`·`u_hat`·`u_top`·`u_bottom`·`u_shoes`·`u_glove`·
`u_cape`). **보스만** 떨구고, 그것도 보스가 마침 그 부위의 Lv.20 물건을 떨굴 때만
16%(`drop.uniqueChance`, 균형 손잡이) 확률로 고유로 바뀐다 — 낮은 단이 고유가 되면
표의 마지막 물건보다 세져 버려 어색하다. 이름 앞에 `★` 를 붙여 가방·장착 목록에서
바로 구별되고, 주울 때 소리(`sfx.js` 의 `uniq` 겹)와 토스트·로그가 따로 뜬다(원작에서
유니크는 **소리부터** 다르다는 사가블로의 결을 그대로 따랐다).

자가진단 134/134(3회 동일) · `_admin.html?selftest` ADMIN 10/10 · `_sfxcheck.html`
40/40(고유 겹 포함, 예외 0). **실기기 확인 전**(마을에서 잡담이 실제로 뜨는지, 고유가
실제로 떨어지는 화면은 아직 못 봤다 — 2026-09-10 세션 방침대로 실기 확인은 몰아서 함).

## 소리 (`js/sfx.js`) — 2026-08-26

에셋을 안 들이고 **WebAudio 로 절차적으로** 낸다 — 그림을 코드가 그리는 것과 같은
규칙이고, 단독 실행판이 파일 하나라는 사정도 있다. 사가블로의 `js/sfx.js` 와 **같은
엔진**이고 소리표만 이 판의 것이다(원작의 결을 따라 높고 맑은 쪽으로 잡았다).
겹은 셋뿐이다 — 미끄러지는 톤 · 걸러 낸 잡음 · 음을 차례로 놓는 것. 그 셋으로 **37가지**.

**이 판이 소리를 들인 까닭의 절반은 주문서다.** 원작에서 주문서를 긁는 순간 화면을
보기 전에 소리로 성패를 안다. 그래서 둘을 아주 다르게 잡았다 — 오르는 화음(`scrollok`)
대 떨어지는 잡음(`scrollno`). 나머지 절반은 **레벨업 팡파레**다.

지킨 선 넷 —
**첫 눌림 전에는 못 낸다**(브라우저 규칙. `sfx.js` 가 스스로 문서에 걸어 두니
**다른 파일은 이 규칙을 몰라도 된다**) ·
**요청은 꺼져 있어도 적는다**(`_tail()`. 판정 층과 소리 층을 가른 것이라 헤드리스
진단이 "이때 이 소리를 냈어야 한다" 를 검사할 수 있다) ·
같은 소리는 최소 간격을 둔다(사냥터에 적이 열이면 타격음이 겹쳐 소음이 된다) ·
AudioContext 는 한 번만 만든다.

- **무예 스물여덟에 소리를 하나씩 두지 않았다** — 귀가 가리는 것은 넷뿐이다.
  `skillCue(effect)` 가 가까이 벤다·멀리 쏜다·몸을 두른다·낫는다로 묶는다
- **몸 소리(점프·착지·줄·앉기)는 가장 작고 짧다** — 사이드스크롤이라 가장 자주 나고,
  크게 잡으면 5분 자동 사냥에 귀가 먼저 지친다
- 이미 있는 이벤트로 되는 것(레벨업·사냥터 들기·문·죽음)은 `sfx.js` 가 스스로 받고,
  프레임마다 나는 것(타격·줍기·점프·줄)은 `side.js` 가 `fx.push` 옆에서 부른다
- 규칙 파일은 `sfx()` 잔손 하나만 안다 — **`sfx.js` 가 없어도 그대로 돈다**
  (데모·진단이 그 자리다)
- 상단 🔊 로 끈다(`settings.sound`). 꺼도 요청은 남는다

## 어드민 (`_admin.html`) — 2026-08-26

사가고·사가의숲의 어드민과 같은 결이고, 손잡이 층(`core.tuned`)도 **같은 이름**이다.
게임을 켜지 않고 세이브와 손잡이만 다룬다 — 그림도 루프도 없는 창이다.
탭 여덟: 세이브 · 사냥터/보스 · 장비/가방 · 직업/무예 · 사명 · 균형 손잡이 ·
QA 프리셋 12 · 점검(자가점검 10항목).

**이 판에서 가장 값진 탭은 '균형 손잡이' 의 물리 셋이다** — 사이드스크롤은 손맛이 곧
게임이라, 중력·점프·달리기를 눌러 두고 게임 창에서 곧바로 뛰어 보는 것이 검증이다.
손잡이 넷 벌(기본·가볍게·묵직하게·빠르게)을 단추로 두었다.

- 손잡이는 **세이브와 다른 칸**에 산다(`yeoksa-side/tune`). 세이브에 섞으면 프로필마다
  규칙이 달라지고, 세이브를 넘길 때 규칙까지 따라간다 — 규칙은 "이 기기의 사정" 이다
- 규칙 값은 모듈이 **켜질 때 한 번** 읽는다. 바꾼 뒤에는 게임 창을 새로고침해야 듣는다
- **자가진단·데모는 손잡이를 읽지 않는다**(`DG_NO_TUNE`) — 진단 83 항목 중 넷이 그 못이다
- 세이브를 고치면 `POKE_KEY` 를 두드린다. 게임 창이 열려 있으면 그것을 보고 다시 읽는다
  (게임이 틈틈이 저장하므로, 안 그러면 어드민이 고친 값이 곧 덮인다)
- **게임에서 되돌릴 수 없는 것을 되돌린다** — 전직이 그렇다. 어드민의 몫은 "그 상황으로
  곧장 가는 것" 이지 규칙을 지키는 것이 아니다. 규칙(`job.canJoin`)은 한 줄도 안 건드렸다
- 사명을 채우는 방법은 **갈래마다 다르다.** 쌓이는 것(적·보스)은 세이브의 수를 올리면
  되지만, 보면 아는 것(장비·무예·금)은 세어 두지 않으므로 **실제로 그 상태를 만든다**
- 어드민은 PC 단독판에도 `sw.js` 캐시에도 안 들어간다(빌드가 `index.html` 만 훑는다)

손잡이 열넷 — `side.grav`·`side.jump`·`side.speed`·`side.climb`·`side.mpRegen` ·
`enemy.hpMul`·`enemy.dmgMul` · `gain.expMul`·`gain.goldMul` ·
`drop.potion`·`drop.gearMul` · `scroll.rateMul` · `boss.coolMul` · `gear.bag`.

## 파일

```
js/sfx.js         소리 — WebAudio 로 그 자리에서 만든다 (소리표 40)
js/admin.js       어드민 규칙 — 세이브·손잡이·프리셋 (게임은 이 파일을 모른다)
js/data-side.js   사냥터(발판·줄·문) · 스킬 · 드랍 · 원거리 무기 · 마을 잡담   ← 사냥터 추가는 여기만
js/data-gear.js   장비(부위 7 × 단 4) · 주문서 7            ← 물건 추가는 여기만
js/data-unique.js 고유(固有) 장비 7 — 부위마다 Lv.20 물건 하나에 매인 이름 있는 물건
js/data-job.js    직업 9 · 무예 28                         ← 무예 추가는 여기만
js/data-quest.js  사명 13                                  ← 사명 추가는 여기만
js/gear.js        장비 규칙 — 줍기·끼기·주문서·저자 매매
js/job.js         직업 규칙 — 전직·무예 점수·조작 띠
js/quest.js       사명 규칙 — 받기·세기·바치기 ('side:kill' 을 듣는다)
js/data-enemy.js  적 (던전 게임과 같은 파일의 복사본)
js/side.js        사냥 규칙 — 물리·공격·적·드랍·탕약 (캔버스를 모른다)
js/side-view.js   사냥터 화면 — 하늘·먼 산·발판·적·연출 (계산하지 않는다)
js/auto.js        자동 사냥
js/ui.js          쉬는 판 · 조작 띠(체력·기력·스킬) · 사냥터 · 도감 · 서당 · 기록
js/game.js        부트 · 루프 · 키보드
```

## 다음에 채울 것 (골격 상태다)

- ~~**보스**~~ — **2026-08-25 완료.** 세 사냥터 모두 오른쪽 끝을 보스가 지킨다.
  일반 적의 12~17배 체력 · 2.0~2.5배 공격 · 화면에서 두 배 가까이 크게. 뜸을 들이다
  **달려드는** 패턴 하나. 금 12배 · 경험치 15배 · 탕약 3개 확정 · 공적(토벌).
  잡으면 15~30분 다시 나오지 않고, 사냥터 목록에 남은 시간이 뜬다.
  정의는 `data-side.js` 의 `boss` 칸(`cool`·`hpMul`·`dmgMul`), 굴리는 곳은 `side.js`.
- ~~**사다리·포탈**~~ — **2026-08-26 완료.** 밧줄·사다리(↑↓)와 문(↑)을 넣었다.
  자동도 줄을 쓴다. 진단 33 → **41**.
  줄에서 **손을 떼는 판정은 움직인 방향으로만** 본다 — 아래 끝(바닥)에서 막 잡은 줄이
  그 프레임에 곧바로 풀려 버리는 결함이 거기 있었다.
  자동은 **줄을 타는 동안 목표를 바꾸지 않는다** — 가까운 적은 높이 차를 싫어하는
  점수라 오르내리는 사이 1등이 바뀌고, 그러면 부호가 흔들려 한 마리도 못 잡는다.
- ~~**장비·상점**~~ — **2026-08-26 완료.** 던전 게임의 `item.js` 를 가져오는 대신
  **메이플식으로 새로 짰다**(접사·소켓이 아니라 요구 수준·업횟·주문서다).
  진단 45 → **59**.
- ~~**직업·스킬 트리**~~ — **2026-08-26 완료.** 직업 아홉(무명 + 1차 넷 + 2차 넷),
  무예 28. 진단 59 → **72**.
- ~~**원거리 공격**~~ — **2026-08-26 완료.** 활·조총을 든 적이 화살·탄환을 날린다.
  쏘기 직전 ❗ 를 띄우고, 사거리 안에서는 다가오지 않는다. 진단 41 → **45**.

## 3D 로 옮기는 중 (2026-09-01, `PLAN.md` Phase 1~2)

- **Phase 1** — `js/side-view3d.js` 신설. 하늘·발판·사람·몹을 WebGL 로 그리고,
  `js/side-view.js`(2D)는 조작 판정·미니맵·보스체력·데미지숫자 오버레이만 남는다.
  상단 🧊 단추(`btn-3d`)로 켜고 끌 수 있다(사가의숲·사가국지와 같은 자리) — 손잡이
  `sideView3d.on`(기본 켬)을 그 자리에서 뒤집는다. 실기기 확인 완료(잘 나옴).
- **Phase 2 착수** — `js/asset3d.js` 신설. 오림 숲의 나무, 한중 굴혈의 바위,
  허창 들판·호로곡의 먼 언덕이 Quaternius CC0 GLB 로 선다(출처는
  `assets/ASSET_LICENSES.md`). GLB 를 못 받으면(느린 회선·file:// 단독판) 조용히
  이전의 도형(원뿔·구)으로 남는다 — **한 줄도 판정에 닿지 않는다**.
  천장에 매달린 종유석·불타는 골짜기의 불길은 아직 도형/이펙트 그대로다.
- **Phase 2 마무리 — 사람·짐승 GLB.** 주인공·사람 형 적(황건적·산적…)이 몸+옷+머리를
  한 뼈대에 묶는 조합형 GLB(saga-forest·saga-dungeon과 같은 창고)로, 짐승 형 적
  (들개·코끼리병)은 늑대·소 GLB 로 선다. 걷기·맞음·가만있기 몸짓(UAL1 애니메이션)이
  실제로 돈다. GLB 를 못 받으면 조용히 이전의 도형 캡슐로 남는다 — 판정은 그대로.
  피격 번쩍임은 GLB 에선 emissive 물들임으로(옷 색을 곱셈으로 지우지 않는다).
  헤드리스로 들판·오림 숲·굴혈·호로곡 네 사냥터 모두 사람·짐승 GLB 가 실제로
  붙는 것과 콘솔 에러 없음을 확인했다. 실기기 확인 전.
- **바닥·발판 그림(2026-09-04)** — "코드로 그리지 말고 에셋으로" 감사에서 찾은
  첫 항목. `side-view3d.js`의 바닥·발판이 색 한 장(`MeshLambertMaterial({color})`)
  이던 것에 사가의숲과 같은 Kenney CC0 도트 타일(잔디·흙·돌)을 얹었다 — `stg.ground`
  색은 그대로 물들여 사냥터마다 색은 갈리고, mood(`sky`·`forest`·`cave`·`fire`)별로
  그림만 바뀐다. GLB 지형지물과 달리 순수 이미지라 `file://` 단독판에서도 뜬다.
  자가진단 106/106 그대로(판정에 닿지 않는다). 실기기 확인 전.
- **발밑 밀도(2026-09-04, 이어서)** — PLAN 6·7절. 배경 지형지물(나무·바위·언덕)
  만 있고 사람이 걷는 깊이 바로 앞뒤는 비어 있던 것에 풀·꽃·덤불(숲·마을·들판)과
  이끼 바위(굴혈)를 흩뿌렸다(`side-view3d.js`의 `deco()`, Quaternius CC0
  재사용). 호로곡(불타는 골짜기)은 그대로 민둥이다. 자가진단 106/106 그대로.
  실기기 확인 전.
- **마을 정취(2026-09-04, 이어서)** — PLAN 3절. town:true 사냥터(신야성·허도·
  강릉진·남정성·기산채) 배경에 집 넷·우물·울타리를 세웠다(`house()`·`well()`·
  `fence()`, saga-dungeon·saga-forest 의 CC0 재사용). 배경일 뿐 판정에는 안
  닿는다 — NPC 대화·상점은 아직 없다(PLAN Phase 3 나머지). 자가진단 106/106
  그대로. 실기기 확인 전.
- **마을 사람(2026-09-09)** — PLAN 3·8절. 위 마을들이 집·우물뿐이라 비어
  보였다. `actorShell()`(사람 GLB 조합 로더, 주인공·적과 같은 것)을 그대로
  빌려 town 사냥터 폭에 비례한 인원을 세우고, 제자리 둘레를 느리게 서성이게
  했다(walk/idle 몸짓 전환) — **판정에는 안 닿는 화면 층**이다. 대화·상점
  연동은 아직이다(이 판의 상점은 도구줄 🏪 저자로 언제든 연다 — 원작과 달리
  NPC 위치에 안 매인다). 자가진단 106/106 그대로.
- **필드 채집(2026-09-09)** — PLAN 10절. 사냥터 넷(들판=들꽃 · 숲=덤불 열매 ·
  굴혈=이끼 광물 · 호로곡=그은 돌)에 고정 자리를 두고, 지나가면 자동으로
  줍고 45초 뒤 다시 돋는다(`side.js`의 `run.gathers`). 가방과 달리 칸을
  차지하지 않는 카운터라 `core.save.side.mats`에 쌓이고, 🎒 가방 시트
  아래에 "캔 것"으로 보인다. 3D 표시는 새 GLB를 안 받고 이미 있는 자연물
  (꽃·덤불·이끼바위·바위)로 결을 맞췄다. 제작·사명 연동은 다음 확장으로
  남겨 뒀다(PLAN 10절이 명시한 대로 — "우선 데이터 구조만"). 자가진단
  106 → **108**(채집 픽업·리스폰 항목 추가), 3회 동일. 실기기 확인 전.
- **비전투 동물(2026-09-09)** — PLAN 9절. 사슴·여우(saga-forest가 이미 받아
  둔 Quaternius GLB, `assets/models/animals/`에 하드링크로 이미 있었다 —
  `asset3d.js`에 등록만 하면 됐다) 를 들판·숲(마을·굴혈·호로곡 제외)에
  세웠다. 안 싸운다 — `side.js`는 이 존재를 아예 모르고, `side-view3d.js`가
  화면 층에서만 플레이어가 다가오면 반대로 미끄러지듯 물러나게(`draw()`의
  `critterPool` 루프), 아니면 제자리를 어슬렁대게 한다. 자가진단 108/108
  그대로(판정 무관). **`_demo.html`에 `#stage3d` 캔버스가 없어 지금까지
  3D 화면을 헤드리스로 못 보고 있었던 것도 이번에 같이 고쳤다** — 넣고
  나니 `#hunt`로 들판 3D(나무·산·사람·사슴)가 실제로 찍혔다.
- **랜덤 이벤트(2026-09-09)** — PLAN 11·13절. 사냥터(마을 제외)에 걸어
  들어갈 때마다 7% 확률로 **희귀 몬스터**(체력 3.2배·공격 1.35배·보상
  4배, 새 종을 안 만들고 기존 적 하나를 불려서 만든다 — 3D 에서는
  `side-view3d.js`가 금빛으로 물들인다)가, 22% 확률로 **보물상자**
  (`js/asset3d.js`의 `chest`, saga-dungeon의 KayKit 상자 재사용)가 하나
  생긴다. 지나가면 열려 큰 골드 + 확정 장비 하나를 준다(가방이 차면
  골드만). 이동상인·몬스터 습격·미니보스는 PLAN 11절이 명시한 대로
  "데이터 구조만 확장 가능하게" 남겨 두고 이번엔 안 만들었다. 자가진단
  108 → **110**(희귀 스폰·상자 픽업 검증, 둘 다 최대 200판 반복해
  확률적으로 등장을 확인하는 방식), 3회 동일. 실기기 확인 전.
- **몬스터 다양화(2026-09-09)** — PLAN Phase 4 · 13절. 근접·원거리(기존)에
  **돌진형**(이름에 "기병" — 거란 기병·몽골 기병·근위 기병·흑기병, 뜸을
  들이다 2.6배 속도로 달려든다)과 **탱커형**(코끼리·미늘창 — 남만
  코끼리병·철갑 중장병, 체력 2.2배·이동 0.6배·공격 0.85배·안 밀림)을
  더해 넷으로 늘렸다. `data-enemy.js`는 던전 게임과 나눠 쓰는 파일이라
  **새 칸을 안 만들고** 이름·무기로 가르는 `enemyRole()`을 `side.js`에
  새로 뒀다(`SD.rangedOf()`와 같은 자리). 돌진은 보스의 "뜸 들이다
  달려든다" charge 패턴을 그대로 재사용, 탱커는 3D에서 크게 표시된다
  (`side-view3d.js`, 기존 "코끼리인지" 정규식을 `role==='tank'` 판정으로
  통합). 밟은 자리 — `update(dt)`가 dt를 0.05로 clamp하는 걸 모르고 큰
  dt로 검증하다 헛돌았다. 자가진단 110 → **112**(돌진·탱커 검증), 3회
  동일. 실기기 확인 전.

**2026-09-09, 같은 날 계속 이어져(이 항목 아래로는 README를 안 갱신한 채
커밋만 스무 개 가까이 더 쌓였다) — PLAN Phase 4 나머지 + Phase 5 대부분이
끝났다.** 자세한 배경은 각 커밋 메시지가 더 정확하다(`git log --oneline --
saga-story`) — 여기서는 지금 상태만 절 번호로 정리한다.

- **Phase 4 나머지**: §15 사명 목표에 채집·탐방 추가.
- **Phase 5**: §16 마을 사람 대화 상호작용 + 사냥터 화면 전환(fade),
  §17 가로 폰 전용 압축 레이아웃, §20 보스전 카메라 확대, §22 횃불
  Point Light·마을 조명, §24 후처리(톤매핑·블룸·SSAO·색보정), §26 3D
  반응형 그래픽 품질 착수, §27 three.js 비동기 로딩 최적화, §29 타이틀
  화면(로고·캐릭터·이어하기·설정), §30 설정 시트(효과음·음량·진동) 착수,
  §33 일일 사명·업적(배지) 완결, §35 보스 등장 배너·레벨업/사명완료
  배너·아이템 획득 팝업 완결. 그 밖에 회피(dodge) 액션(§12)·PC 마우스
  조작판+키 재지정도 같이 들어왔다.
- 자가진단 112 → **128**(git log 순서 그대로, 각 커밋이 자기 항목을
  올렸다). `sw.js` → `side-v0.34.0`.
- **아직 안 건드린 Phase 5 항목 — §28 UI 리뉴얼, §34 사운드(BGM 셋:
  마을·숲·전투 — 지금 `sfx.js`엔 절차적 효과음 서른여섯뿐, 배경음악은
  다섯 판 어디에도 없다).** 둘 다 큰 결정이 필요해(UI는 전면 재디자인,
  BGM은 CC0 음원을 새로 구해야 한다 — 다섯 판 중 이 판이 처음이라 옮겨
  올 선례가 없다) 사용자에게 방향부터 물을 것.
- **실기기 확인 전** — 이 구간 전부(카메라·조명·후처리·타이틀·연출) 손맛을
  다음 세션에서 물어볼 것.

**2026-09-09 이어서 — PLAN 34절: 배경음악(BGM) 신설, 다섯 판 중 이 판이
처음 CC0 음원을 들인다.** 사용자가 남은 Phase 5 둘(§28 UI 리뉴얼·§34 사운드)
중 사운드를 골랐다. OpenGameArt.org에서 태그·라이선스로 걸러 마을·숲·전투
셋을 새로 찾았다(직접 들어보고 판단은 못 했다 — 태그·제목만으로 골랐다,
자세한 출처는 `assets/ASSET_LICENSES.md`) — 전부 **CC0**(저작자 표시
불필요):

| 트랙 | 곡명 | 만든 이 | 크기 |
|---|---|---|---|
| `town.mp3` | Quaint Town | neonarkade | 2.1MB |
| `forest.mp3` | Peaceful forest | Samza | 2.5MB |
| `battle.mp3` | Battle Theme A | cynicmusic | 3.3MB |

- `js/bgm.js` 신설 — `<audio loop>` 셋을 만들어 두고 필요한 하나만 재생한다
  (크로스페이드 없이 즉시 전환, "과도하게 쓰지 않는다"는 PLAN 35절과
  같은 결로 일단 단순하게). `desiredTrack()`이 지금 상태만 보고 고른다 —
  사냥 중 아님(마을 화면)이거나 `town:true` 사냥터 안이면 town, 보스가
  나와 있으면 battle, 그 밖의 사냥터는 forest. 순수 함수라 자가진단이
  DOM·오디오 없이도 검증한다.
- **`sfx.js` 머리말의 낡은 전제를 바로잡았다** — "이 저장소는 남의 에셋을
  안 들인다·단독 실행판이 데이터 URI로 부푼다"고 적혀 있었는데 둘 다
  틀렸다. `asset3d.js`의 GLB는 처음부터 fetch로 받아 왔고(코드가 그린 적
  없다), `build-single.mjs`는 css·js만 인라인하지 GLB·이미지는 그때도
  상대경로 그대로다 — **부풀 일이 애초에 없었다.** mp3도 같은 방식이라
  이번엔 정말 파일을 들였다. 짧은 효과음(타격·팡파레)은 여전히 절차로
  낸다 — 그건 파일보다 이 편이 실제로 낫다(수십 개를 안 늘려도 된다).
- 설정 시트(§30)에 미뤄 뒀던 "BGM" 자리를 채웠다 — 켜짐/꺼짐 버튼 +
  음량 슬라이더, 효과음과 같은 패턴.
- `index.html`·`_test.html`·`sw.js`(오프라인 앱 캐시 SHELL)에 `js/bgm.js`
  한 줄씩. mp3 자체는 GLB와 같은 이유로 SHELL에 안 넣었다(오프라인
  캐시는 코드만, 자산은 그때그때 받는다 — 기존 방침 그대로).
- 자가진단 128 → **133**(town/forest/battle 갈림·설정 저장 다섯 개),
  세 번 동일. `sw.js` → `side-v0.35.0`.
- **아직 안 건드린 Phase 5 항목 — §28 UI 리뉴얼 하나만 남았다.**
- **실기기 확인 전** — 무엇보다 **직접 들어봐야 한다**(태그·제목만으로
  골랐다) — 셋 다 톤이 어울리는지, 루프 이음매가 거슬리는지, 음량
  기본값(0.4)이 적당한지 다음 세션에 물어볼 것. 안 어울리면 `js/bgm.js`의
  `TRACKS` 표만 다른 파일로 갈아 끼우면 된다(코드 구조는 안 바뀐다).

**2026-09-09 이어서 — PLAN 28절: UI 리뉴얼, 실제로는 "재디자인"이 아니라
"청소"였다.** `css/style.css`를 훑어보니 상단 프로필·HP/MP 띠·유리
패널·둥근 모서리·스킬 아이콘 등 PLAN 28절이 요구하는 항목이 **이미 다
있었다** — 다크 글래스모피즘으로 이미 잘 정돈된 화면이다. "프로토타입
같다"고 느낄 구석이 있다면 화면이 아니라 **파일 자체**였다: 1449줄 중
약 380줄이 이 판이 한 번도 쓴 적 없는 죽은 CSS였다.

- **원인** — 이 판을 처음 만들 때 `saga-dungeon`의 `style.css`를 통째로
  베껴 온 뒤, JS 쪽은 "던전 전용(본영·부대·장비)을 걷어냈다"(`js/ui.js`
  머리말에 이미 적혀 있었다)면서 **CSS는 안 걷어냈다.** 로그라이크 던전
  오버레이·전리(戰利) 선택·가방/장비/업그레이드/자동화 목록·디아블로식
  HP·기력 오브+스킬바·서고 문답·서당 시트·부대 시트 인물 줄까지 — 전부
  `#dungeon`·`.dg-*`·`.qcard`류 클래스인데, 이 판(메이플風 옆으로 걷기)
  어디에도 `dungeon` 화면이 없다(`_test.html`이 "다른 게임의 모듈은 실려
  있지 않다"로 이미 확인해 두고 있었다 — 문답은 사가국지만 갖는다는
  루트 CLAUDE.md 원칙과도 맞는다).
- **어떻게 걸렀나** — 선택자 140개 가까이를 하나씩 `js/*.js`·`index.html`·
  `_test.html`에서 실제로 쓰는지(문자열·`class="..."`·`id="..."`) 대조했다.
  같은 "던전" 배너 아래 있어도 실제로 쓰는 것과 안 쓰는 것이 섞여 있어서
  —`#autobar`(이 판 자체의 자동 순행)·`.rulelist`(도움말 시트 "규칙"
  목록)·`#camp-bg`/`#camp`/`.camp-*`(동행 없을 때 뜨는 "쉬는 화면", 원래
  "본영" 이름만 물려받았을 뿐 이 판이 실제로 쓴다)·"오프라인/온라인·사관"
  절 전체(`net.js`·`ai.js`) — 는 **그대로 남겼다.**
- **밟은 함정** — 첫 삭제 때 뭉텅이로 지우다가 `.rulelist`를 실수로 같이
  지웠다(도움말 시트가 여전히 그 클래스를 쓰는데, 큰 삭제 범위 안에 섞여
  있었다). 지운 직후 "삭제한 선택자가 코드에 남아 있나"를 다시 검사하는
  단계에서 잡았다 — 되살려 "시트 내부 공통" 절로 옮겨 놨다(원래 있던
  "던전 시트·경고" 절은 통째로 죽었으므로).
- 죽은 헤더 배너("사가스토리 v1.0 — 고딕 던전·오브 HUD·서당·3D")도 함께
  걷어내고, 살아 있던 "3D 모드 지평선 안개" 절의 주석에 무엇을 왜 지웠는지
  남겼다. "본영" 절 제목도 실제 쓰임(쉬는 화면)에 맞게 고쳤다.
- CSS 라서 `_test.html`이 직접 검증할 수 있는 대상이 아니다(자가진단은
  그대로 133/133, 세 번 동일 — 회귀 없음, 단 이건 JS 쪽만 본다는 뜻이지
  CSS 삭제 자체를 검증한 게 아니다). 검증은 선택자 대조표(위 문단)가
  전부다. `sw.js` → `side-v0.36.0`.
- **PLAN 28절이 요구한 실제 룩(반투명 패널·둥근 모서리·HP/MP 등)은
  손대지 않았다** — 이미 있었고, 코드만 봐서 "더 예쁘게" 바꾸는 건 눈으로
  못 보고 하는 억측이라 하지 않았다. **이걸로 다섯 판 중 이 판이 PLAN
  Phase 1~5를 전부(적어도 한 번은) 지나갔다.**
- **실기기 확인 전** — 지운 뒤 화면이 그대로인지(당연히 그래야 한다 —
  안 쓰던 규칙만 지웠다) 다음 세션에 한 번 열어서 확인해 줄 것. 만약
  실제로 UI가 "프로토타입 같다"고 다시 느껴진다면, 이번처럼 코드를 보고
  짐작하지 말고 **어디가 그렇게 보이는지**부터 짚어 줄 것 — 그래야 다음
  세션이 진짜 재디자인을 할 수 있다.

**2026-09-09 이어서 — PLAN §11 랜덤 이벤트 예시 중 "채집 보너스 지역"을
새로 얹었다.** 남아 있던 예시(이동 상인·몬스터 습격·미니보스·채집 보너스
지역) 중 가장 부딪힘이 없는 것을 골랐다 — 이 판의 상점은 원래 NPC 위치에
안 매이고 도구줄 🏪로 언제든 여는 구조라 "이동 상인"은 얻는 게 거의 없고
(상점을 게이트하는 게 아니라 장식만 되는 자리), 미니보스·몬스터 습격은
`data-enemy.js`(던전 게임과 나눠 쓰는 공용 파일)를 건드릴 여지가 있어
더 큰 결정이 필요하다.

- 보물상자(`buildChest`)와 완전히 같은 패턴 — 사냥터(마을 제외)에 걸어
  들어갈 때 18% 확률로 폭 260px 구간(`run.forage = {x1,x2,notified}`)이
  하나 생긴다. **새 오브젝트를 만들지 않는다** — 이미 있는 `run.gathers`
  (§10)가 이 구간 안에서만 `FORAGE_MUL`(2)배 나오게, `update()`의 채집
  판정 한 곳에만 곱을 끼웠다.
- 처음 그 구간에 들어서는 순간 한 번만 "🍀 채집이 넘치는 곳이다!" 토스트
  (보물상자의 `opened`와 같은 한 번뿐 패턴 — `notified` 플래그).
  이후 그 구간 안에서 캐면 토스트에 "+2 🍀"로 보너스가 났음을 알린다.
- 3D(`side-view3d.js`)에는 그 구간 바닥에 옅은 초록 반투명 판(순수 primitive,
  `MeshBasicMaterial`)을 얹었다 — chest처럼 `worldGroup`에 얹어 사냥터가
  바뀔 때 통째로 지우고 다시 그린다(따로 dispose 관리 안 해도 된다).
  **이 3D 화면은 `_test.html`에 안 실린다**(원래도 시각 층은 자가진단
  대상이 아니다, chest도 마찬가지) — 코드만 확인, 실제로 보이는지는
  실기기 확인 대기.
- 자가진단 133 → **134**(보너스 구간에서 캐면 정확히 두 배가 나오는지,
  보물상자 테스트와 같은 최대 200판 재시도 방식), 세 번 동일. `sw.js` →
  `side-v0.37.0`.
- **아직 남은 예시** — 이동 상인·몬스터 습격·미니보스는 이번에도 안
  만들었다(위에 적은 이유). 이동 상인을 정말 넣으려면 먼저 "상점이
  언제든 열리는 지금 구조를 이번엔 위치에 매야 하는가"부터 사용자에게
  물어야 한다.
- **실기기 확인 전** — 보너스 구간의 초록 반투명 판이 실제로 보이는지,
  "+2 🍀" 토스트가 거슬리지 않는지 다음 세션 손맛부터 물어볼 것.

## 몬스터 습격 — PLAN §11 랜덤 이벤트 잔여분 (2026-09-10)

남아 있던 예시(이동 상인·몬스터 습격·미니보스) 중 **구조 변경이 필요 없는
쪽**을 골랐다. 이동 상인은 "상점을 위치에 매는 구조로 바꿀지"부터, 미니보스는
`data-enemy.js`(던전 게임과 나눠 쓰는 공용 파일)를 건드릴 여지가 있어 둘 다
더 큰 결정이 필요하다(README 이전 항목 그대로) — 몬스터 습격은 **희귀
몬스터·보물상자와 같은 결**로 새 종·새 파일 없이 끝난다.

- 보물상자와 완전히 같은 패턴 — 사냥터(마을 제외)에 걸어 들어갈 때 15%
  확률로 매복 지점(`run.ambush = {x, triggered}`) 하나가 생긴다. 그 자리를
  지나면(±70px) **기존 `spawnEnemy(atX)`를 그대로 세 번 불러** 그 자리
  둘레에 잡졸 셋이 한꺼번에 나타나고, "🚨 몬스터 무리가 덮쳤다!" 토스트 +
  `boss` 소리 겹(등장 경보로 이미 쓰던 것을 재사용)이 함께 뜬다.
- 3D 쪽은 손대지 않았다 — 스폰된 잡졸은 이미 있는 사람/짐승 GLB 렌더 경로를
  그대로 타므로 별도 표시가 필요 없다(경고 없이 갑자기 나타나는 편이 "습격"
  이라는 이름에 더 맞는다고 판단).
- 밟은 자리 — 자가진단은 seed 20260824 하나로 처음부터 끝까지 이어지는
  Math.random() 스트림이라, `buildAmbush()`가 매 입장마다 새로 소비하는
  난수 한 번이 그 뒤에 도는 **다른 테스트들의 난수 정렬을 통째로 밀어냈다.**
  그 결과 "필드 채집물을 지나가면 줍고..." 테스트가 우연히 채집 보너스
  구간과 겹쳐 +1 가정이 깨졌다(로직 결함이 아니라 순서 밀림) — 그 테스트에
  `run.forage = null`을 끼워 보너스 구간과 무관하게 만들어 고쳤다. **새
  난수 소비를 하나 추가하면 그 뒤 테스트 전부가 이 함정에 걸릴 수 있다** —
  이번처럼 실패가 나면 로직보다 순서 밀림부터 의심할 것.
- 자가진단 134 → **140**(4차 전직·UI 통일 세션에서 이미 139까지 올라 있었다,
  이번 건 습격 검증 1개), 세 번 동일. `sw.js` → `side-v0.40.0`.
- **아직 남은 예시** — 이동 상인·미니보스는 이번에도 안 만들었다(구조 결정
  먼저 필요, 위 사정 그대로).
- **실기기 확인 전** — 매복 지점을 지나는 순간 잡졸 셋이 갑자기 나타나는
  느낌이 "습격"답게 읽히는지, 토스트·소리 타이밍이 거슬리지 않는지 다음
  세션 손맛부터 물어볼 것.

## 미니보스 · 이동 상인 — PLAN §11 랜덤 이벤트 마지막 둘 (2026-09-10, 이어서)

몬스터 습격에 이어 남은 예시 둘도 마저 채웠다. **이걸로 PLAN §11이 적어 둔
랜덤 이벤트 예시 넷(희귀 몬스터·보물상자·채집 보너스·이동 상인·몬스터
습격·미니보스 — 실제로는 여섯) 전부 최소 한 형태로 갖춰졌다.**

**미니보스** (`js/side.js`) — 희귀(3.2배)와 진짜 보스(12~17배) 사이 자리를
채웠다. 새 종을 안 만들고 `spawnEnemy(atX, boost)`에 `{hp, dmg}` 배수를
얹는 셋째 인자를 냈다(희귀 굴림과는 배타적 — 겹치면 값을 못 읽는다). 체력
7배·공격 1.7배, 보상은 희귀의 두 배(8배). 진짜 보스처럼 **쿨타임·전용
UI·카메라 연출은 없다** — `e.boss`가 아니라 `e.mini` 플래그라 보스 체력
바·등장 배너·"안 밀림" 판정을 하나도 안 물려받는다(그 전부를 물려주려면
`bossAt` 쿨타임 키 충돌 등 더 큰 결정이 필요해, 이번엔 "센 잡졸"로 남겼다).
잡으면 탕약 확정 2개 + 보스와 같은 표의 장비 드랍률, 처치음도 `bosskill`
팡파레를 함께 쓴다.

**이동 상인** (`js/side.js` · `js/gear.js`) — README에 이미 적힌 "상점을
위치에 매는 구조로 바꿀지"에 대한 답을 **안 바꾸는 쪽**으로 냈다 — 이
판의 상점(🏪 도구줄, 언제든 연다)은 그대로 두고, 마주치면
`core.save.player.merchantUntil`만 90초 뒤로 밀어 그동안 `gear.js`의
`priceMul()`이 장비·주문서·탕약 값을 30% 깎는다. `shopList()`가 원본
데이터를 물들이지 않도록 `withPrice()`로 얕은 복사본만 내보낸다 — 표시
값과 실제 결제 값이 항상 같은 곳(`priceMul()`)에서 나오므로 어긋날 일이
없다.

- 밟은 자리 — `spawnEnemy(atX)`가 그동안 **한 번도 실제로 쓰인 적 없는
  죽은 매개변수**였다(이번 습격이 첫 실사용). 45% 확률로 발판 위에
  다시 세우는 분기가 atX를 무시하고 덮어써서, 습격 자리를 지정해도
  근처가 아니라 엉뚱한 발판에 떨어질 뻔했다 — `atX === undefined`일 때만
  그 분기를 타게 좁혀 고쳤다(습격도 이 수정을 함께 받는다).
- 자가진단 140 → **142**(미니보스 체력 배율·이동 상인 가격 검증), 세 번
  동일. `sw.js` → `side-v0.41.0`.
- **실기기 확인 전** — 미니보스가 "센 잡졸"로 읽히는지(보스급 연출이
  없다는 것이 밋밋하게 느껴지는지), 이동 상인의 "🛒 90초 할인" 토스트
  타이밍과 실제 상점가 반영이 자연스러운지 다음 세션 손맛부터 물어볼 것.

## NPC 구조 — PLAN §11 랜덤 이벤트 마지막 하나 (2026-09-10, 이어서)

§11이 적어 둔 랜덤 이벤트 예시는 실제로 **일곱**(보물상자·희귀 몬스터·
NPC 구조·이동 상인·몬스터 습격·미니보스·채집 보너스)이었다 — 앞선 두
세션에서 "이동 상인·몬스터 습격·미니보스"만 세다가 "NPC 구조 이벤트"를
빠뜨린 걸 이번에 다시 세어 보다 찾았다. **이걸로 일곱 전부 갖췄다.**

- 마을 사람(`NPC_TALK`)을 사냥터로 들여오지 않았다 — 표지판처럼 선
  존재라 "붙잡혀 있다"는 그림과 안 맞는다. 대신 몬스터 습격의 "역"으로
  짰다 — 걸어 들어갈 때 10% 확률로 자리가 하나 생기고, 가까이 가면
  (`spawnEnemy()` 재사용, 새 종 없음) 지키는 잡졸 둘이 나타난다. **그
  둘을 다 잡으면**(`e.guard` 플래그로 셈) 사례금(🪙)을 추가로 준다 —
  잡졸 자체의 평소 처치 보상 위에 얹는 보너스다.
- 새 UI·새 상태 저장 없음(한 판 안에서만 사는 `run.rescue`). 몬스터
  습격과 코드 모양이 대칭이라(붙잡힌 사람이 있다 → 지키던 잡졸 등장
  → 다 잡으면 보상) 유지보수 부담이 따로 없다.
- 자가진단 142 → **143**, 세 번 동일. `sw.js` → `side-v0.42.0`.
- **실기기 확인 전** — "😱 붙잡힌 사람이 있다" → 잡졸 처치 → "🙏 사례금"
  흐름이 실제로 자연스럽게 읽히는지 다음 세션 손맛부터 물어볼 것.
