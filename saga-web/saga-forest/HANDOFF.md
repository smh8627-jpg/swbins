# saga-forest HANDOFF — 이 판의 세션 이력

**이 파일은 grep 으로만 읽는다.** `grep -n "^# \|^## "` 로 목차를 뽑고, 날짜·절 번호로 grep 해 필요한 절만 `sed -n` 으로 읽는다.
새 세션 기록은 날짜를 붙여 **끝에 append** 한다. 설계 결정이 바뀌면 `PLAN.md` 를 고치고, 여기엔 "무엇을 왜 바꿨나" 만 남긴다.
시리즈 전체 이력·규칙은 `../../SAGA-HANDOFF.md`, 공통 개편 설계는 `../../SAGA-DESIGN.md`, 현재 상태는 `README.md` 머리 절.

## PLAN.md 에서 옮겨 온 세션 기록 (2026-09-16 재편)

아래는 옛 PLAN.md §34-1, §42~§46-5 를 **한 글자도 바꾸지 않고** 옮긴 것이다(옛 PLAN 1890행 중 653행).
절 번호(§42 등)는 옛 PLAN 기준이다 — 새 PLAN 은 절 체계가 다르다(§0~§10).
설계 결정(전체지도 M키, 소리·BGM, 퓨전 허용, 안개 기본 꺼짐, 택배 아르바이트, 우주기지·폐허 목적지)은 새 PLAN §2·§3·§5 에 수치 그대로 옮겨져 있다.

# 34-1. 전체지도 — M키식 토글 (2026-09-01, 사용자 요청)

사가블로(`../saga-dungeon/PLAN.md` 28-1절)에 "마을에서 나가 다른 마을/지역으로
간다 + 디아블로 M키식 전체지도"를 적어 두면서, 사용자가 다른 네 판에도 같은
방향을 남겨 두라고 했다. 이 판은 **마을 하나**(동물의숲 원작처럼 내 마을)가
기본이라 "다른 마을로 걸어나간다"는 사가블로식 오버월드가 그대로는 안 맞는다 —
다른 플레이어 마을 방문 같은 기능은 이 판의 방침(개인 취미용, 서버 없음)과도
안 맞다.

다만 **디아블로 M키처럼 미니맵(34절)을 펼쳐 보는 전체 지도**는 유효한
아이디어다 — 마을이 구면 투영(`SAGA-HANDOFF.md`의 원칙, 되돌리지 말 것)이라
평면 미니맵만으로는 전체 모양이 한눈에 안 들어온다. 펼친 지도에서 집·상점·
채집 구역·낚시터 같은 34절의 "주요 장소"를 한눈에 보여주는 정도로 쓸모가
있다.

**2026-09-02 구현 완료.** `village-view.js`의 project()/unproject()(화면에
보이는 굽은 투영)는 전혀 건드리지 않았다 — village.js 가 이미 들고 있는
**타일 좌표 그대로**(`tileAt`/`raw().props`/`lakeCenter`/`waterfallSpot`/
`hamletSpot`/`caveSpot`)를 그대로 위에서 내려다본 SVG 로 펼쳤을 뿐이다.

- `js/ui.js`: `viewMap()` 신설(SHEET_TITLE.map 추가) — 4타일 간격으로 샘플링한
  `tileAt()` 결과를 색 타일로 깔고, 호수→폭포 물길을 `riverCenterX()`로 따라
  그리고, 집·전방·편지함·침선방·게시판·사고·마을기·호수·폭포·작은마을·동굴을
  이모지로 찍고, 내 위치를 빨간 고리로 표시한다. 시트가 열려 있는 동안만
  400ms 간격으로 다시 그려 걷는 위치가 흘러가게 했다(`mapRefreshTimer`) —
  'changed' 이벤트는 걷기만으로는 안 뜨기 때문.
- `M` 키(입력칸 포커스 중이면 무시) + 하단 독 🗺️ 버튼으로 토글. 기존 코드에
  'm' 키 바인딩이 없어 충돌 없음.
- `index.html`: 독에 `data-sheet="map"` 버튼 추가. `css/style.css`: `.mapsvg` 한 줄.
- `_test.html`: 기존 "시트 4개+카드" 테스트의 tabs 배열에 'map' 을 끼워 넣어
  렌더 예외·빈 화면을 함께 잡는다. 헤드리스 3회 `RESULT 195/195` 동일.

---


# 42. 소리(音) — 절차적 효과음 신설 (2026-09-10)

다섯 판 중 사가블로(`sfx.js`, 33종)·사가고(`audio.js`)·사가스토리(`sfx.js`+CC0
BGM)는 이미 소리가 있는데 **이 판만 여태 무음**이었다 — 계절·낚시·채집·유성·
편지 등 "삶의 결" 콘텐츠가 다섯 판 중 가장 풍부한데 정작 무음이라 손실이
크다는 판단으로 최우선으로 뽑혔다(다른 판에서 빌려올 기능 후보 조사, 1위).

**사가블로 `js/sfx.js`를 그대로 본으로 삼았다** — WebAudio 로 절차적으로
낸다(에셋 없음), 겹은 tone·noise·chime 셋, 첫 입력 전엔 못 낸다(`unlock()`이
스스로 문서에 건다), 요청은 꺼져 있어도 `_tail()`에 남는다(판정 층과 소리
층을 가른다), 같은 소리는 최소 간격, `AudioContext`는 한 번만. 소리 스무 가지:
발소리 다섯(흙길·돌길·물길·풀밭·모래펄, 공사 다섯 갈래와 같은 이름) · 낚시
셋(입질·놓침·건짐) · 곤충 셋(벌떼·달아남·채집) · 문 둘(집·굴, 다른 잔향) ·
레벨업 · 계절 전환 · 유성 · 편지 · 기증 · 선물 둘(그냥/아주 반김).

**`village.js`·`animal.js`·`weather.js`·`town.js`는 한 글자도 안 건드렸다.**
두 갈래로 듣는다 — ① 이미 있는 이벤트(`village:fish`·`village:bug`·
`village:home`·`village:cave`·`levelup`)를 `js/sfx.js`가 스스로 구독 ②
판정 파일엔 없지만 **읽기 전용 공개 함수로 값이 드러나는 것**(발소리 지형·
계절·유성)은 `js/sfx.js`가 220ms마다 폴링해서 값이 바뀔 때만 운다
(`village.raw()`·`village.tileAt()`·`villageData.season()`·`town.starNow()`
전부 이미 있던 읽기 전용 함수라 판정을 안 건드리고도 듣는다). 딱 세 자리에만
한 줄씩 보탰다 — `mail.js`의 `put()`(새 편지가 오면 알아야 하는데 그런
이벤트가 없었다) · `ui.js`의 기증·선물 결과 처리(이미 결과가 난 다음의 UI
반응 지점이라 안전하다).

상단 🔊(`#btn-sound`, `game.js`의 `bindTopbar()`가 다른 아이콘 버튼과 같은
결로 잠금·해제) 로 끄고 켠다(`core.save.settings.sound`, 다른 판과 같은 이름).

검증 — `_test.html`에 소리표 완결성·꺼져도 기록됨·이벤트 연결·계절/유성
폴링(값을 잠깐 바꿔치기해 "바뀌었을 때만 운다"를 확인, `season()`이 실제
달력 달을 읽어 진단 중엔 못 바꾸기 때문) 항목 여덟을 더했다. 자가진단
237→245(3회 동일). `_sfxcheck.html`(스무 소리를 하나씩 내 보는 확인용
페이지, `unlock()`을 직접 불러 브라우저 제스처 없이도 들어볼 수 있다)도
새로 두었다.

**실기 확인 전이다** — 실제로 소리가 나는지·거슬리지 않는지는 사용자가
직접 들어봐야 한다. `_sfxcheck.html`을 열어 스무 소리를 순서대로 들어보고,
실제 플레이에서 발소리 간격·볼륨이 적당한지 확인해 줄 것.

---

# 42-1. 배경음악(BGM) — 2026-09-10, "다른 게임 참조" 요청으로 신설

위 42절이 채운 건 짧은 절차 효과음뿐 — 몇 분짜리 루프 음악은 절차로 못 낸다.
사가스토리(`js/bgm.js`, CC0 town/forest/battle 세 트랙)를 본으로 삼되, 이
판은 마을 하나뿐이고 전투가 없어 **트랙 하나만 늘 돈다**. 새로 안 받고
사가스토리의 `forest.mp3`(Samza, CC0, 출처는 `assets/ASSET_LICENSES.md`)를
그대로 옮겼다. `core.save.settings.music`/`musicVol`에 저장, ⚙️ 설정 시트에
효과음 바로 아래 얹었다. 자가진단 245→248(3회 동일).

**실기 확인 전.**

---

# 42-2. 집·동굴 안에서 3D가 안 꺼지던 것 (2026-09-10)

`village-view3d.js`는 실내(`village.js`의 `indoors()`/`caveInside()`)라는
개념을 몰라서, 3D가 켜진 채(2026-09-09부터 기본값) 집·동굴에 들어가면 문
앞 바깥 풍경이 얼어붙은 채로 계속 떠 있고 방 안(2D 전용)은 안 보였다.
방을 3D로 새로 세우지 않고(PLAN에 없는 일) — **실내에선 3D를 끈 것처럼
굴어 2D가 저절로 앞에 나서게** `active()`를 고쳤다(`indoorSuppressed()`로
뺀 순수 함수, 진단이 `ready`와 무관하게 확인한다). `step()`이 매 프레임
`syncVisibility()`를 다시 불러 새 이벤트 없이 들고 나는 순간을 잡는다.
자가진단 248→249(3회 동일).

---

# 42-3. 움직이는 모션을 더 자연스럽게 — 회전 스냅 없앰 (2026-09-10)

인물의 걷는 방향(`facingYaw`)과 걷는 짐승 일곱 종(사슴·여우·늑대·토끼·
다람쥐·오리·새)의 몸 회전이 둘 다 **매 프레임 순간 대입**이었다 — 방향을
바꾸면 한 프레임 만에 홱 돌았고, 짐승은 아예 회전이 없어 옆으로 미끄러지듯
보였다. 최단 경로 각도 보간(`angleLerp`)과 프레임 길이와 무관한 지수감쇠
계수(`turnLerpK`, `village3d.turnRate` 손잡이, 기본 8)를 새로 얹어 몸·
카메라(어깨너머 모드)가 같은 값으로 같이 부드럽게 돈다. 짐승은
`TURNING_KIND` 표에 있는 일곱 종만 이전 프레임 자리와 비교해 방향을 잡는다
(나무·건물처럼 안 움직이는 건 계산을 안 한다). 자가진단 249→253(3회 동일).

**실기 확인 전.**

---

# 42-4. 개구리·뱀 보탬 + 짐승 걷기 애니메이션 (2026-09-10)

"더 추가할 캐릭터·동물이 없는지" 확인 — 캐릭터(주민·NPC 외형)는 이미
공유 `HEROES` 105명 + MPFB2 20종이 표 기본이라 더 채울 자리가 없었다.
동물은 바이옴별로 세어 보니 mushroom 이 여우 하나뿐이라 진짜 비어 있었다
— `saga-go`의 CC0 짐승(Quaternius)에서 **개구리(mushroom·dark)·뱀
(rocky·dark)**을 옮겨 보탰다(9종 §42-3의 `TURNING_KIND`에도 합류).

**덤으로 찾은 것** — 개구리·뱀 GLB를 열어 보다가 사슴·여우·늑대
(Quaternius) GLB에도 Idle·Walk 클립이 원래 있었는데 3D 화면이 짐승을
나무·바위처럼 정지 오브젝트로만 세워(`mixer`를 안 만듦) 그 클립을 한 번도
안 썼다는 게 드러났다. `asset3d.js`의 `buildGeneric()`이 원본에 클립이
있으면 `hero`와 같은 결로 mixer·actions·clipMap 을 실어 주게 고쳤고,
`syncScatter()`가 `playAction()`으로 걷는 동안 walk·멈추면 idle 을 튼다.
사슴·여우·늑대·개구리·뱀 다섯 종이 이제 실제로 다리가 움직인다(토끼·
다람쥐·오리·새 넷은 원본 자체에 클립이 없어 여전히 정지). §42-3의
회전-보간 고침이 "방향만 부드럽고 다리는 안 움직이던" 반쪽이었던 걸
마저 채운 셈이다. 자가진단 253/253 그대로(테스트 설명·기대값만 갱신).

**실기 확인 전.**

---

# 42-5. 손을 쓰는 순간에도 몸짓 하나 (2026-09-10)

회전·짐승 걷기까지 고쳤는데 정작 인물이 나무를 흔들거나 낚싯대를 던지거나
상자를 열 때도 화면엔 아무 몸짓 없이 idle·walk 둘만 오갔다.
`village-view3d.js`에 `triggerAction()`을 신설 — `interaction`(없으면
`attack`) 클립을 한 번(`LoopOnce`, 끝에서 멈춤) 튼다. 재생 중엔
`syncCamera()`가 idle/walk 로 안 덮어쓴다(`actionTimer` 가 dt 만큼 줄며
막는다). `ui.js`의 `doInteract()`가 결과 종류를 가려 부른다 — 대화·시트
열기·문 여닫기는 손짓이 안 어울려 뺐다(`NO_GESTURE_KIND`), 나머지(채집·
낚시·상자·잡초·소원 빌기 등)는 전부 튼다. 자가진단 253→254(3회 동일).

**실기 확인 전.**

---

# 43. 퓨전 방향 선언 + 첫 콘텐츠 셋 (2026-09-10)

**사용자 지시.** 동물의숲 오마주에 갇힐 필요가 없다는 방향 전환이다.

> "완전 모방 할 필요 없어"
> "괴물이 나와도 되고 캐릭터가 달라도 되고"
> "퓨전이야"
> "현대적이어도 되고 중세적이어도 되고 미래적이어도 되고"
> "더 추가할 사항 있나 보고 원작보다 너 좋으면 좋겠어"

2절이 이미 "특정 게임의 그래픽을 그대로 복제하지 않는다"고 적어 뒀지만
그건 그림체 얘기였다. 이번 지시는 더 넓다 — **콘텐츠·캐릭터·시대 배경도**
동물의숲 틀에 안 갇혀도 된다. 목표는 원작 재현이 아니라 **원작보다 나은
것**이다. §4(에셋 구조)의 `characters/monsters/`, §20(랜덤 이벤트)의
"몬스터 습격", §10(고정 배치)의 "보스 지역"·"폐허"는 이제 다 정당한
확장 후보다.

**지키는 것은 그대로다** — 실존 인물 실명 금지(다섯 판 공통 정책, 이 지시와
무관), 원작사의 실제 에셋 금지, 공개 저장소 라이선스 정책(CC0 우선,
재배포 불가 자산 금지). "퓨전"은 콘텐츠·시대 자유를 뜻하지, 라이선스·
저작권 원칙을 느슨하게 한다는 뜻이 아니다.

첫 콘텐츠 셋(전부 크로스게임 CC0 자산 재사용, 새 전투 시스템 없음):

- **몬스터** — 버섯숲에 "포자괴물"(saga-dungeon의 Quaternius Ultimate
  Monsters Bundle 재사용). 여우 대신 8%로만 드물게. `VD.ANIMALS`에
  `rare:true`로 등록, `VD.MONSTER_BIOME`이 바이옴→몬스터를 잇는다.
- **동굴 보스방** — 동굴 방에 깊은 칸을 더해 네 번째 상자(보상 3,500).
  기존 상자 셋을 다 열어야 봉인이 풀린다(`bossUnlocked()`). "포자대왕"은
  순전히 장식.
- **폐허** — 서북쪽에 무너진 아치(saga-go 재사용) + 이끼바위. 순전히 장식.

자가진단 254 → 259(세 번 동일). `sw.js` → `village-v0.53.0`.

**다음 세션이 이어갈 후보** (이 지시를 처음 보는 세션도 바로 집을 수 있게
적어 둔다):

- **미래적 소품** — 이 저장소가 지금까지 CC0 확인해 둔 자산 중 SF/미래풍이
  없다. 새로 찾아야 한다(WebSearch·GitHub API 미러 등 그때 되는 경로로).
- **늑대·토끼·오리·뱀의 2D 스프라이트** — §44(아래)에서 5종(사슴·여우·
  다람쥐·개구리·새)만 CC0 애니메이션 스프라이트를 구했다. 나머지 넷은
  여전히 emoji. 맞는 CC0 팩을 더 찾으면 `ANIMAL_SPRITE` 표에 한 줄씩 보태면
  끝난다(`village-view.js`).
- **몬스터 종류 늘리기** — saga-dungeon에 이미 CC0 확인된 몬스터가 100종
  가까이 있다(`../saga-dungeon/js/asset3d.js`의 `monster:*` 키, `PEOPLE_MONSTERS_Q`
  경로). 다른 바이옴(dark·rocky)에도 `MONSTER_BIOME` 한 줄 + `SCATTER_KIND`
  한 줄이면 새로 하나 더 생긴다.
- **보스방을 더 크게** — 지금은 "잠금 해제 + 큰 상자"뿐이다. 실제 전투가
  필요하다면 이 판엔 아직 그 체계가 전혀 없다(saga-dungeon의 전투를
  참고할 수는 있지만 옮기는 건 큰 작업이다) — 사용자에게 먼저 원하는지
  물어볼 것.

---

# 44. 3D NPC "안 보인다" 재신고 — 진짜 원인은 안개였다 (2026-09-10)

2026-09-09에 카메라~플레이어 거리를 늘려 "고쳤다"고 남겼지만 사용자가
"아직도 안 보임"으로 다시 신고했다. 실제 좌표를 재 보니(Node로 `village.js`를
그대로 불러 좌표만 계산) 숲 NPC 여섯이 마을 중심에서 **67~113m** 떨어져
있는데, `village-view3d.js`의 안개 먼 끝(`FOG_FAR`)이 **160m**뿐이었다 —
맑은 날에도 절반 넘게 안개에 덮이고 비·눈이면 80~136m로 더 좁아져 가장 먼
NPC는 거의 안 보였을 것이다. NPC는 거리 컬링이 없어(원래부터 그렇게
설계됐다) 장면엔 계속 서 있었다 — 안개가 시각적으로 삼켰을 뿐이다.

`FOG_FAR` 160 → 320(`FOG_NEAR` 30 그대로). fog는 셰이더 블렌딩 값이라
렌더 비용이 늘지 않는다. 자가진단 259/259 그대로(세 번 동일, fog는 픽셀
검사가 아니라 새 항목 없음). `sw.js` → `village-v0.54.0`.

**곧바로 사용자가 "안개는 중요 하지 않으니 제거 하던지 옵션에 키고
끄는걸 추가해 끄는게 기본이고"로 위 수치 조정 자체를 되돌렸다.** 안개
거리를 맞게 늘렸는지 다투는 대신, **안개를 아예 옵션으로 빼고 기본을
꺼짐으로** 정하는 쪽이 더 단순하고 확실하다 — 꺼져 있으면 거리와 무관하게
NPC·모든 사물이 또렷이 보인다. `village3d.fog` 손잡이(`core.tuned`/`setTune`,
새로고침해도 유지, `village3d.quality`와 같은 결) 신설, 기본값 0(꺼짐).
`init()`의 `scene.fog` 생성과 `syncSky()`의 안개 갱신을 `FOG_ON()`/`scene.fog`
존재 여부로 감쌌다 — 꺼져 있으면 `scene.fog`가 아예 `null`이라 안개 계산
자체가 안 돈다. ⚙️ 설정 시트(그래픽 품질 옆)에 켜짐/꺼짐 버튼 신설
(`ui.js`의 `fog-toggle`). 위 `FOG_FAR` 320 값은 **죽지 않았다** — 사용자가
직접 안개를 켜면 그 넉넉해진 거리로 켜진다. 자가진단에 기본값·손잡이
왕복 확인 하나 추가, 259 → **260**(세 번 동일).

**실기 확인 전.**

---

# 44-2. 3D NPC "아직도 안 보인다" — 진짜 빠진 건 residents였다 (2026-09-11)

§44의 안개 진단은 틀리지 않았지만(안개를 꺼도 여전히 신고가 이어져), **정작
사용자가 마주치는 "NPC"는 숲 고정 여섯(`village.js`의 `npcs`, §44가 다룬
대상)이 아니라 마을 한복판을 어슬렁대는 **주민 다섯**(`residents`,
`buildResidents()`가 HEROES 로스터에서 뽑는다, `folk.js`가 매 프레임 걷게
한다)이었다. 2D(`village-view.js`)는 `drawResident()`로 이 다섯을 그리고
있었는데, **`village-view3d.js`에는 이들을 세우는 함수 자체가 없었다** —
`syncNpcs()`가 숲 여섯만 돌고, `step()`엔 residents를 다루는 호출이
한 줄도 없었다(grep으로 확인, `resident`라는 문자열이 이 파일에 아예
없었다). 안개·거리를 아무리 고쳐도 애초에 안 세워지는 인물이라 신고가
안 사라진 것이 당연했다.

`syncNpcs()` 바로 아래 `syncResidents()`를 새로 만들어(같은 `asset3d().build
('hero', {id: res.id}, cb)` 조합, `res3d` 라는 별도 창고, `step()`의
`syncNpcs(dt)` 다음 줄에 호출 추가) 마을 주민도 3D에 실제로 선다. 주민은
`folk.js`가 실제로 걸어 다니게 하므로(숲 NPC 여섯은 고정 자리라 회전이
없었다) `syncScatter()`의 짐승 처리(TURNING_KIND)와 같은 결로 이동 방향으로
몸을 트는 것과 idle/walk 몸짓 전환도 같이 얹었다. 진단 전용
`residentMeshCount()`를 `npcMeshCount()` 옆에 추가(둘 다 init 전엔 0).

**실기 확인 전** — 위 원칙(§"Claude Code 최종 작업 원칙")대로 지금 바로
확인을 요청하지 않는다. 다음에 실기 확인할 때 마을 안 3D 화면에서 다섯
주민이 걸어 다니는지, `DG.villageView3d.residentMeshCount()`가 5를
돌려주는지 확인할 것.

---

# 45. 다음 큰 방향(아직 미착수, 설계만) — 맵 크기 대비 빈 밀도 + "다른 마을" + 배달 아르바이트 (2026-09-10)

**아직 코드는 하나도 안 건드렸다.** 사용자가 여러 메시지에 걸쳐 낸 아이디어를
그대로 받아 적고, 실현 가능성을 확인한 뒤 하나의 설계로 정리해 둔다 — 다음
세션(이 대화 맥락이 없어도)이 바로 시작할 수 있게.

## 사용자 발언 그대로

> "맵이 큰거에 비해 npc가 없고 다른 마을도 있다는 걸 추가하려고"
> "새로운 이벤트가 있는 거지 다른말은 현대 미래 과거 일수 있고"
> "예를 로봇트랑 논다던지 택배를 한더지 같은 우체국 처럼 여러 마을을
> 돌아 다닌 거지 배달 알바같은"
> "우주 여행도 있고" / "여러가지" / "생각해봐"

## 진단 — 왜 "크기에 비해 빈다"고 느끼나

`forestMargin` 기본값이 2026-09-09에 20→32로 늘면서 세계가 커졌는데
(바깥 경계까지 반지름 약 100타일 ≈ 300m대), 그 안의 **고정 콘텐츠는 여섯
NPC·캠프 둘·동굴·폐허 하나**뿐이라 인구 밀도가 넓이를 못 따라간다. 게다가
NPC는 원래 거리 컬링이 없어(§44) 늘 그 여섯 자리 그대로다 — 늘어난 땅
대부분은 그냥 바이옴 장식(나무·바위 등)만 있는 빈 걸음이다.

## 설계 — "택배 아르바이트"로 다른 마을들을 잇는다

**핵심 루프**: 우체통(`mail.js`가 이미 "편지함"으로 가진 개념)에서 소포를
받아, 다른(멀리 떨어진, 시대색이 다른) 마을까지 걸어가 전해주고 보상을
받는다. **반복 가능한 일**이라 "배달 알바" 표현과 맞고, 넓은 지도를 걷는
이유를 계속 만들어 준다(한 번 발견하고 끝나는 랜드마크가 아니다).

- 데이터 하나(`DELIVERIES` 같은 표: `from`·`to`·`item`·`reward`)로 정한다
  (PLAN 19절 "복잡한 시스템은 안 만든다" 그대로 따른다). 소포는 가방에
  들어가되 못 팔게(`giftOnly` 같은 플래그) 막는다.
  픽업·배달 둘 다 기존 `focus()`/`interact()` 표에 한 줄씩 끼우면 된다 —
  새 상호작용 프레임워크가 필요 없다.
- 첫 목적지는 **1~2곳**만 — 한 번에 다 짓지 않는다(PLAN "절대 하지 말 것"
  원칙 그대로).

## 목적지 후보 — 시대 혼합(현대·미래·과거), 새로 찾을 필요 없이 이미 CC0 확보됨

**놀랍게도 이미 저장소 안에 있다.** `saga-dungeon`이 2026-09-09 "Ultimate
Space Kit"(Quaternius, CC0, poly.pizza 경유 — GitHub 미러
`trebeljahr/quaternius-showcase`의 `public/glb/ultimate_space_pack/`에도
그대로 있다, 라이선스 확인 근거는 `../saga-dungeon/assets/ASSET_LICENSES.md`
"캐릭터 5차분"·"몬스터 100개" 근방)를 받아 두고 **우주비행사 3벌만 쓰고
나머지는 안 썼다**:

| 후보 | 쓸 곳 | 비고 |
|---|---|---|
| `Astronaut_*.glb` (4벌) | **우주기지의 사람** — 이미 `hero` 뼈대(`CharacterArmature`, mapClips 그대로 받음)라 이 판의 hero 조합 파이프라인에 바로 얹을 수 있다 | saga-dungeon은 3벌만 받았다, 미러엔 4벌 다 있다 |
| `Mech_*.glb` (4종) | "로봇이랑 논다" — 타는 게 아니라 **세워 두고 살펴보는/말 거는** 장식+상호작용 소품(포자대왕과 같은 결, 새 탈것 시스템 불필요) | |
| `Rover_1/2/Round.glb` | 우주기지 마당의 장식 차량 | |
| `Base_Large`·`Building_L`·`House_*`·`GeodesicDome`·`SolarPanel_*`·`Connector`·`Roof_*` | 우주기지 건물 — 기존 캠프처럼 "건물 몇 채 + 장식"으로 충분 | |
| `Tree_Floating/Lava/Spike/Spiral/Swirl_*`·`Rock_Large_*`·`Planet_*` | **외계 바이옴 장식** — 이 구역만 지금 다섯 바이옴과 완전히 다른 땅으로 보이게 한다(`Planet_*`은 하늘에 떠 있는 장식으로 써도 된다) | |
| `Enemy_Small/Large/Flying/ExtraSmall.glb` | 우주기지 전용 희귀 "짐승"(§43 포자괴물과 같은 결 — `MONSTER_BIOME` 표에 한 줄) | saga-dungeon이 "사람형이 아니라" 몬스터 트랙에서 보류해 둔 것 — 이 판(짐승 개념이 이미 있다)엔 오히려 잘 맞는다 |
| `Pickup_Crate/Jar/KeyCard.glb` | **배달 소포 자체의 모양** — 이미 "택배 상자"로 쓰기 좋은 모델이 있다 | |
| `Spaceship_*.glb` | 우주기지 한켠에 "불시착한 우주선" 랜드마크 — "우주 여행"을 진짜 이동시스템 없이 **암시**만 한다(다른 행성처럼 보이는 땅에 이미 와 있는 사람들, 어떻게 왔는지는 그림으로만) | |

**"현대" 목적지는 아직 후보가 없다** — 다섯 판 다 사극·판타지·이제 우주뿐이라
현대(도시·편의점·버스정류장 같은) CC0 자산은 새로 찾아야 한다(다음
WebSearch 되는 세션 몫). **"과거"는 §43의 폐허(무너진 아치)를 확장하는
쪽으로 갈음할 수 있다** — 지금은 장식뿐이지만, "폐허를 조사하다 숨겨진
옛 마을을 발견한다" 같은 훅으로 이벤트화할 수 있다.

## 남는 설계 질문 (다음에 사용자에게 먼저 확인할 것)

**→ 2026-09-11에 셋 다 확인받고 §46에서 구현했다.** 아래는 그 결정이
나오기 전 기록이라 그대로 남겨 둔다.

- 목적지가 **늘 거기 있는 고정 마을**인가, 아니면 사용자가 말한 "새로운
  이벤트"에 더 가깝게 **한 번 발견해야 열리는 곳**인가 (예: 폐허를 조사 →
  우주기지 존재를 알게 됨 → 그제서야 지도에 표시)
  → **고정 마을**로 결정.
- 우주기지가 **어느 방향**에 서나 — 지금 서북(폐허)·북(동굴)·동(캠프1)·
  동남(캠프2)·서(호수)가 다 찼다. margin을 더 늘리거나(성능 트레이드오프,
  §PHASE7 경고 그대로 유효) 더 먼 대각선에 놓아야 한다
  → **더 먼 대각선**(남서)으로 결정.
- 배달은 **하루 한 번** 같은 원작풍 제한을 두나, 아니면 무제한 반복인가
  → **무제한 반복**으로 결정.

## 성능 경고(누적)

고정 콘텐츠(NPC·마을)는 거리 컬링이 없다(§44) — 목적지를 늘릴수록 항상
그려지는 것이 늘어난다는 뜻이다. 지금까지는 감당됐지만, 우주기지처럼
사물 수가 많은 구역을 통째로 더하면 PHASE 7이 미뤄 둔 "진짜 거리 기반
컬링/LOD"를 이번에는 정말 먼저 해야 할 수 있다 — 짓기 전에 먼저 재 볼 것.

---

# 46. 우주기지 · 배달(택배 알바) 첫 목적지 — §45 설계를 실제로 지었다 (2026-09-11)

§45가 설계만 하고 멈췄던 것을 사용자가 "진행해줘"로 승인, 세 질문(고정
마을·남서 더 먼 대각선·무제한 반복)에 답을 받고 그대로 지었다. **PLAN
"절대 하지 말 것" 원칙대로 첫 목적지 하나만** — 우주기지 하나, 현대·과거
목적지는 여전히 미착수(§45 표 그대로 다음 몫).

## 지은 것

- **`village.js`**: `spaceBaseSpot()`/`inSpaceBase()`(다른 다섯 고정 자리와
  같은 결의 spot 함수, `SPACEBASE_MIN_MARGIN=20`·비율 0.7/0.75로 "더 먼"을
  좌표에 그대로 반영) 신설. `buildProps()`의 바이옴 장식·채집 노드 제외
  루프, `buildAnimals()`의 짐승 제외 조건에 각각 `inSpaceBase()` 한 줄씩
  보탰다(다른 네 자리와 같은 자리에 나란히). `tileAt()`은 이 구역만 이미
  있는 `sand` 타일을 돌려준다(새 타일 종류를 안 늘렸다 — TILES·
  terrainColors() 손 안 댔다).
- **소품**: crate(보급 상자, `mail`과 같은 `box_small.gltf.glb`를 새 키로
  재등록)·fence·cart(둘 다 asset3d.js에 **등록만 되고 여태 3D scatter에
  안 이어져 있던 것** — 다리(bridge)가 그랬던 것과 같은 결로 처음 이었다)·
  lantern(기존 그대로 재사용) 다섯을 deco:true로 놓았다. **새 GLB는 하나도
  안 받았다** — 전부 이미 저장소 안에 있던 파일의 재사용/재등록.
- **택배 접수대(`courierPost`)**: 마을 안, 우편함(`mail`) 곁에 있는 **새
  프롭**(우편함과 다른 kind — mail.js의 편지·이사 상태를 안 건드리려고
  일부러 갈랐다). deco 아님(손이 닿는다), 소포가 없을 때만 하나 내준다.
- **배달원(`courier`)**: 일곱 번째 숲 NPC, 우주기지 한복판에 선다. 다른
  여섯과 달리 **QUESTS 표에 없다** — 반복 가능한 일이 한 번뿐인 부탁 표와
  안 맞아서(질문 ③ "무제한 반복" 결정), `talkNpc()`가 `courier`만 따로
  갈라(`talkCourier()`) 처리한다. `metNpcs` 기록은 그대로 남아 explorer의
  meetnpc 부탁 count도 5 → 6(자신 뺀 나머지 전부)으로 같이 올렸다.
- **상태**: `s.village.delivery = { carrying, n }` 하나뿐(가방·item
  카테고리를 안 늘렸다 — PLAN 19절 "복잡한 시스템은 안 만든다"를 그대로
  따랐다. §45 설계안의 "가방에 소포로 들어간다"는 이 한 가지 점만
  단순화했다). 접수대(`pickupParcel()`)에서 받고(`carrying:false`일 때만),
  배달원(`talkCourier()`)에게 주면 🪙 220 + `n++`, `carrying`이 다시
  `false`로 돌아가 **곧바로 또 받을 수 있다**(무제한 반복).
- **새 상호작용 코드 없음** — `interact()`의 `prop.kind` 분기, `talkNpc()`의
  `npc.kind` 분기에 각각 한 줄씩만 보탰다. `ui.js`의 `doInteract()`는 이미
  'talk'·'quest'·'no' kind를 다 처리하고 있어 **UI 쪽은 한 글자도 안
  고쳤다**.

## 자가진단

`_test.html`에 새 항목 넷을 보탰다(자리·안 겹침, margin에 따라 사라짐,
땅·장식 5개·접수대, 접수대↔배달원 왕복 무제한 — 마지막 것은 두 바퀴를
직접 돌려 🪙 +220씩 두 번, `n===2`까지 확인). 기존 NPC 수 하드코딩(6→7)·
Quest "여섯 다 부탁 있음"(courier 제외로 재작성)·explorer meetnpc
count(5→6, "0/5"→"0/6") 넷도 맞춰 고쳤다. `node -c`로 손댄 다섯 파일
(village.js·data-village.js·asset3d.js·village-view3d.js·_test.html의
인라인 스크립트)을 전부 구문 확인했다.

## 실기 확인 전

우주기지가 실제로 그 자리에 서는지, 3D에서 crate·fence·cart·courier가
제대로 보이는지, 접수대→배달원 왕복이 화면에서 자연스러운지는 **사용자가
직접 확인해야 한다.** 헤드리스 화면 확인은 정책대로 안 했다.

## 다음에 이어갈 것 (§45 표 그대로)

- 현대(도시) 목적지 — CC0 자산을 아직 못 찾음(다음 WebSearch 되는 세션 몫)
- saga-dungeon의 Ultimate Space Kit 나머지(우주기지 건물·로버·메카 등,
  실물 GLB는 poly.pizza에서 아직 안 받아 옴 — 이번엔 이미 로컬에 있던
  Fence·Cart·box_small만 재사용해 새 다운로드를 피했다)를 받아 우주기지를
  더 채우는 일
- 배달원에게 우주비행사 GLB(saga-dungeon이 이미 받아 둔 Astronaut 3벌)를
  입혀 다른 여섯과 다른 실루엣으로 보이게 하는 꾸밈(기능엔 필요 없다 —
  다른 여섯도 전부 무작위 hero 외형이라 지금 그대로도 일관적이다)

---

# 46-2. 폐허를 "과거" 목적지로 확장 + 동굴 보물상자 그림 (2026-09-11)

§46 뒤이어 사용자가 "사가의숲 더 찾아봐, 현대 목적지 아니여도 다 되니
상관없어 꼭 맞출 필요는 없다"고 해 §45가 제안했던 "현대·과거·우주" 셋 중
안 지은 "과거"를 찾아봤다. 우주기지 때처럼 **새 GLB를 안 받는** 쪽을
우선했다 — 이 환경은 poly.pizza 등 외부 다운로드가 SSL 인증서 문제로
사실상 막혀 있다(직접 `curl`로 확인).

## 지은 것

- **폐허 확장(아치 하나뿐이던 §43 폐허 → 실제 목적지급)**: `saga-go`가
  Sketchfab에서 CC0로 받아 이미 정제해 둔 **진짜 13~14세기 탑성(tower
  house) 폐허 사진측량 스캔**(Renvylle Castle, 584.5KB, 한쪽 모서리가
  무너진 실물 모습)을 하드링크로 옮겼다(`assets/models/buildings/
  realistic/tower_ruin.glb`, md5 saga-go 쪽과 동일). asset3d.js에
  `building:ruinTower`로 등록(`BLD_REAL` 경로라 PBR을 안 벗긴다, 집 셋과
  같은 결). 곁에 **정자(gazebo)**도 세웠다 — `asset3d.js`에 등록만 되고
  여태 3D scatter에 안 이어져 있던 `Gazebo.glb`(fence·cart가 그랬던 것과
  같은 패턴)를 처음 이었다. 새 GLB는 탑성 하나뿐, 정자는 이미 저장소에
  있던 파일을 재등록만 했다. `village.js`의 기존 `inRuin()` 5×4칸을 안
  넓혔다 — 탑성·정자 둘 다 그 안에 들어간다(Node로 직접 좌표 확인함).
- **동굴 보물상자 — 이모지 대신 캔버스로 직접 그린 나무 궤**:
  "동굴 보물상자를 3D 모델로"를 조사하다 **동굴 안은 3D 자체가 꺼져 있다**
  (`village-view3d.js`의 `indoorSuppressed()` — 실내는 2D 전용, 2026-09-10에
  이미 그렇게 고쳤었다)는 걸 확인, GLB를 갖다 놔도 안 쓰인다는 결론에
  이르렀다. 대신 `village-view.js`의 `drawChest()`를 우편함·게시판
  (`drawMailbox`·`drawBoard`)과 같은 결로 다시 썼다 — 나무 궤 몸통·둥근
  뚜껑·테두리 띠·자물쇠를 직접 그린다. 연 상자는 뚜껑이 열린 채 빛바래고,
  보스 상자는 더 크고(1.35배) 금빛 띠로 갈라 도드라진다. **새 에셋 없이**
  이모지 한 글자보다 나은 모습을 냈다.

## 자가진단

`_test.html`의 폐허 관련 항목("무너진 아치·이끼바위 둘"을 확인하던 것)에
탑성·정자 둘을 보태 다시 썼다. `drawChest()`는 캔버스 그림이라 자가진단이
픽셀을 안 보므로(README에 이미 적힌 한계) 새 자동 검사는 없다 — 다른
캔버스 그리기 함수들과 같은 사정이다.

## 실기 확인 전

탑성 폐허가 실제로 저 자리에 서는지(3D), 정자가 어울리는지, 상자 그림이
이모지보다 나아 보이는지는 사용자가 직접 봐야 한다.

---

# 46-3. 배달원(courier)에게 우주비행사 외형 — §46 "다음에 이어갈 것" 셋째 항목 (2026-09-14)

§46이 세 가지 남은 일 중 마지막(현대 목적지·Space Kit 나머지·배달원 외형)을
채웠다. 다른 둘(현대 목적지·건물/로버/메카 확충)은 여전히 새 CC0 자산을
찾아야 해 이번엔 손대지 않았다 — §46-2가 이미 확인한 대로 이 환경에선
poly.pizza 등 외부 다운로드가 SSL 문제로 막혀 있어, **이미 저장소 안에
있는 자산만** 옮겼다.

## 지은 것

- **`assets/models/people/space/Astronaut1.glb`** — `saga-dungeon`이
  "캐릭터 5차분"(2026-09-09)에서 이미 CC0 확인해 둔 Ultimate Space Kit
  우주비행사 3벌 중 1벌을 그대로 복사(md5 동일, 213KB). 새로 받지 않았다.
- **`js/asset3d.js`**: `PEOPLE_SPACE`/`HERO_RECIPE_COURIER` 신설,
  `DEFAULTS['hero:npc_courier']`로 등록. `village.js`의
  `npcAt('courier', ...)`가 주는 `id: 'npc_courier'`를 `keysFor()`가
  `'hero'`보다 먼저 찾아 이 좁은 키가 걸린다 — 배달원만 이 몸을 입고
  나머지 여섯 NPC·다섯 주민·플레이어는 그대로 `HERO_RECIPES`(QRPG·MPFB
  섞음) 해시를 탄다. **판정·상호작용 코드는 한 줄도 안 건드렸다**(순전히
  외형 한 줄 등록). Astronaut1.glb는 saga-dungeon 쪽 주석대로
  `CharacterArmature|*` 제 클립(idle·walk 포함)을 갖고 있어 body/anim을
  같은 파일로 주는 것만으로 `mapClips()`가 그대로 찾는다 — retarget 불필요.
  `EXT_meshopt_compression`로 압축돼 있는데 `asset3d.js`가 2026-09-09부터
  `GLTFLoader`에 `MeshoptDecoder`를 이미 물려 둔 상태라 추가 작업 없이 풀린다.
- **`assets/ASSET_LICENSES.md`**: 새 절 추가(출처·라이선스, saga-dungeon
  쪽 원출처 절 참고 링크).

## 검증

Node로 `asset3d.js`를 직접 실행해 `heroRecipe({id:'npc_courier'})`가
`space_astronaut1`(Astronaut1.glb)을 돌려주고, 다른 npc id(`npc_keeper`·
`npc_herbalist`)는 여전히 기존 MPFB 해시를 그대로 타는 것을 확인했다
(courier만 예외로 갈라짐). `node -c`로 구문 확인.

`_test.html` 헤드리스 자가진단 — **261/265, 세 번 동일**(수정 전 기준선도
동일하게 261/265 — "공사"(지형 개조) 테스트 넷은 이 변경과 무관한 기존
실패로 확인, 회귀 없음). 새 REG 키(`hero:npc_courier`) 하나가 늘었지만
기존 "27개 키 다 참 · 등록 55개" 검사는 특정 kind 어휘 목록만 세므로
영향 없다.

## 실기 확인 전

배달원이 실제로 우주비행사 모습으로 서는지, 다른 NPC와 실루엣이 뚜렷이
구별되는지는 사용자가 3D 화면에서 직접 봐야 한다.

## 다음에 이어갈 것 (§45 표 갱신)

- 현대(도시) 목적지 — 여전히 CC0 자산 미확보
- Ultimate Space Kit 나머지(건물·로버·메카) — 실물 GLB를 poly.pizza에서
  아직 못 받음(SSL 문제, §46-2 참고). 외부 다운로드가 뚫리는 세션에서 이어갈 것

---

# 46-4. 우주기지에 이 팩 자체의 건물·로버·메카 — §46-3 "다음에 이어갈 것" 마지막 항목 (2026-09-14)

§46-3이 셋 중 배달원 외형만 채우고 남긴 마지막 항목("Ultimate Space Kit
나머지(건물·로버·메카) — poly.pizza SSL 문제로 못 받음")을 이번에 채웠다.
**§46-2가 적어 둔 "poly.pizza가 이 환경에서 SSL로 막혀 있다"를 이번에
다시 확인해 보니, 실제로는 `poly.pizza`·`static.poly.pizza` 모두 이번
세션엔 200으로 응답했다** — 막혔던 것이 상시가 아니라 그때그때 환경
사정이었던 듯하다. 그래도 이 저장소가 이미 Frog.glb 등에 써 온
GitHub 미러(`trebeljahr/quaternius-showcase`)로 그대로 받았다 — 배달원
우주복(§46-3)과 같은 Ultimate Space Kit 팩(Quaternius, CC0)이라 출처
확인은 이미 서 있다.

## 지은 것

지금까지 우주기지(spaceBaseSpot)는 **이 팩과 무관한 다섯 소품**(fence·
cart·crate·lantern, 다른 자리에서 빌린 것)로만 채워져 있었다 — "우주기지"
라면서 정작 우주기지 팩 자체의 건물이 하나도 없었다. 이번에 여섯을
새로 받았다: 본관(Base_Large)·숙소(House_Single)·측지돔(GeodesicDome)·
태양광판(SolarPanel_Ground)·탐사차(Rover_1)·메카(Mech_FinnTheFrog) —
합쳐도 240KB 남짓(PLAN 31절 "적은 수의 좋은 에셋" 그대로).

- **`js/asset3d.js`**: `BLD_SPACE`(`assets/models/buildings/space/`)·
  `PROP_SPACE`(`assets/models/props/space/`) 신설, `DEFAULTS`에
  `building:spaceBase`·`building:spaceHouse`·`building:spaceDome`·
  `rover`·`solarPanel`·`mech` 여섯 키로 등록.
- **`js/village.js`**: `buildProps()`의 우주기지 블록(§45~46)에 이어
  여섯 줄을 더했다 — 전부 `deco:true`(순전히 장식, 새 상호작용 없음),
  기존 다섯 소품·배달원(courier) 자리와 안 겹치게 7×7 자리(`inSpaceBase`)
  안에서 나눠 잡았다.
- **`js/village-view3d.js`**: `SCATTER_KIND`(3D 렌더용 asset3d 키 매핑)·
  `SCATTER_H`(정규화 기준 눈대중 높이)에 여섯 줄씩 추가.
- **`js/data-village.js`**: `VD.PROPS`에 여섯 항목 추가(이름·이모지 —
  3D가 꺼진 기기·프로필을 위한 2D 대체 그림, 다른 deco 소품과 같은 결).
- **`_test.html`**: 우주기지 deco 소품 수를 세던 기존 테스트(5개 →
  crateA·crateB·fence·cart·lantern)를 11개로 고치고, 새 여섯 kind가
  실제로 섞여 있는지 확인하는 조건을 더했다.

새 상호작용·판정 코드는 하나도 없다 — 순전히 장식 확충이다(메카는 PLAN
45절 표가 미리 적어 둔 "세워 두고 살펴보는 장식", 새 탈것·전투 시스템
불필요 그대로).

## 검증

`node -c`로 손댄 네 스크립트 파일(asset3d.js·village.js·
village-view3d.js·data-village.js) 전부 구문 확인, `_test.html`의 인라인
스크립트 두 블록도 마찬가지. 헤드리스 자가진단 — **261/265, 세 번
동일**(§46-3과 같은 기준선 — 공사(工事) 테스트 넷은 이 변경과 무관한
기존 실패, 회귀 없음). "흩뿌릴 kind는 전부 asset3d 표에 실제로 있다"
테스트가 새 여섯 종을 포함해 그대로 통과, 우주기지 소품 수 테스트도
11개로 통과 확인.

## 실기 확인 전

우주기지가 실제로 그 자리에 서는지, 3D 화면에서 본관·숙소·돔·태양광판·
탐사차·메카가 서로 겹치지 않고 자연스럽게 보이는지, 규모(눈대중으로만
잡은 SCATTER_H 수치)가 어색하지 않은지는 사용자가 직접 봐야 한다.

## 다음에 이어갈 것

- 현대(도시) 목적지 — 여전히 CC0 자산 미확보(다음 WebSearch 되는 세션 몫)
- 이 팩에 남은 조각 — Connector·Roof_*·MetalSupport·Stairs·Ramp(결합용
  소품, 본관 하나로도 "기지" 느낌은 나서 이번엔 안 받았다), Enemy_*(포자괴물과
  같은 결의 우주기지 전용 희귀 몬스터 후보), Pickup_*(택배 소포 모양을
  상자 대신 이걸로 바꾸는 안) — PLAN §45 표 참고

---

# 46-5. 우주기지 전용 희귀 몬스터 "성간충"(Enemy_Small) — §46-4 "다음에 이어갈 것" 중 하나 (2026-09-14)

§46-4가 남긴 셋(현대 목적지·이 팩 남은 조각·Enemy_*·Pickup_*) 중 **Enemy_***
(PLAN §45 표가 이미 "포자괴물과 같은 결의 우주기지 전용 희귀 몬스터 후보"로
적어 둔 것)를 채웠다. 같은 GitHub 미러(`trebeljahr/quaternius-showcase`,
Ultimate Space Kit)에서 `Enemy_Small-transformed.glb`를 새로 받았다(41.2KB).

## 지은 것

- **왜 기존 `MONSTER_BIOME`(포자괴물 경로)을 그대로 못 쓰나** — 그 경로는
  `buildAnimals()`의 `BIOME_CELL`(22칸) 격자 뽑기에 얹혀 도는데, 우주기지는
  `spaceBaseSpot()`/`inSpaceBase()`로 정해지는 7×7 자리라 격자 칸 하나보다도
  작다 — 격자 표본점이 그 안에 들어갈 확률이 사실상 0이다. 그래서 courier
  NPC(§45)와 같은 결로 **고정 위치 + 세이브 시드별 확률**(40%, 새 hash2
  스트림 하나)로 따로 심는 길을 골랐다. 새 전투는 안 만들었다 — 다른 짐승과
  똑같이 idle/wander/flee만 탄다.
- **`js/asset3d.js`**: `monster:spacebug` → `assets/models/monsters/space/
  EnemySmall.glb` 등록. 원본에 Idle·Walk·Attack 클립이 있어 `buildGeneric()`가
  자동으로 걷는 몸짓을 태운다(다른 몬스터·짐승과 같은 경로).
- **`js/data-village.js`**: `VD.ANIMALS.spacebug`(이름 '성간충', 이모지 👾,
  `rare:true`) 신설. `biomes`에 다섯 바이옴을 다 적어 뒀다 — 실제로 그 다섯
  아무 데서나 나서가 아니라, 자가진단의 "제 바이옴에만 선다" 검사가 그
  자리의 `biomeAt()` 해시값(우주기지와 무관하게, 세이브마다 다르게 계산된다)과
  안 어긋나게 하기 위해서다.
- **`js/village.js`**: `buildAnimals()` 끝에 우주기지 전용 블록 추가 —
  `spaceBaseSpot()`이 있고 `VD.ANIMALS.spacebug`가 있으면, 새 hash2(다른
  좌표 계수라 기존 hgate·hpick·hmon 흐름은 안 건드린다)로 40% 확률을 굴려
  우주기지 중심에서 (+0.6, +2.3)칸 자리(다른 소품·건물·courier와 안 겹치는
  빈자리, 7×7 경계 안)에 심는다.
- **`js/village-view3d.js`**: `SCATTER_KIND`·`TURNING_KIND`·`SCATTER_H`에
  각각 한 줄(다른 짐승과 같은 syncScatter()/Object Pool/회전-보간 경로).
- 2D는 맞는 CC0 스프라이트가 없어 이모지(👾)로 뜬다 — `ANIMAL_SPRITE`
  표에 없는 kind는 자동으로 이모지 fallback을 타는 기존 경로 그대로다.

## 자가진단

`node -c`로 손댄 네 스크립트(asset3d.js·data-village.js·village.js·
village-view3d.js) 구문 확인, `_test.html` 인라인 스크립트 두 블록도
마찬가지. 새 항목 하나 추가 — 세이브 시드 40개를 돌며 `buildAnimals()`를
다시 불러 `spacebug`가 나타날 때만·항상 우주기지 안인지 확인(0/40이나
40/40이면 확률·위치 로직이 잘못된 것 — 실제로는 그 사이 값, 나타나면 전부
`inSpaceBase()` 안, `asset3d` 등록도 함께 확인). 기존 TURNING_KIND 완결성
검사·짐승 종 목록 검사의 `need` 배열도 새 종에 맞춰 고쳤다.

헤드리스 자가진단 — **262/266, 세 번 동일**(byte-diff 없음). §46-3·§46-4와
같은 기준선 261/265에 새 항목 1개(성간충 씨앗 훑기)만 더해 262/266 —
여전히 남은 FAIL 넷은 전부 "공사"(지형 개조) 테스트로, `git diff` 없이도
이 변경과 무관함을 확인했다(회귀 없음).

## 다음에 이어갈 것

- 현대(도시) 목적지 — 여전히 CC0 자산 미확보
- 이 팩에 남은 것 — Connector·Roof_*·MetalSupport·Stairs·Ramp 같은 결합용
  조각, Pickup_*(택배 소포 모양을 상자 대신 이걸로 바꾸는 안)

## 실기 확인 전

성간충이 실제로 우주기지 안에서 자연스럽게 보이는지(등장 확률이 체감상
적당한지), 이모지(👾) 폴백이 다른 소품들 사이에서 안 어색한지는 사용자가
직접 봐야 한다.

---


## 2026-09-17 — SAGA-DESIGN §8-3 세이브 마이그레이션 지뢰 제거 (다섯 판 공통, saga-go 세션에서 발견)

`js/core.js`의 `load()`가 `parsed.v !== 1`이면 무조건 세이브를 버리는 하드 체크였다 — 사가고
그래픽 작업 중 이 판이 다섯 벌 복사본이라는 걸 확인차 훑다가 다섯 판 전부 같은 코드임을 발견했다.
`SAVE_VERSION`·`MIGRATIONS`·`migrate()`(순수 함수)를 추가해 버전이 안 맞아도 지우지 않고
`mergeDeep(freshSave(), ...)`로 넘기게 고쳤다(자세한 설계 이유는 `saga-web/saga-go/HANDOFF.md`
2026-09-17 절 참고 — 다섯 벌이 동일 로직이라 설명을 안 되풀이한다). `sw.js` VERSION 도 같이 올렸다.

## 2026-09-17 — SAGA-DESIGN §6.1 툰 재질(외곽선 제외)

사가고→사가블로→사가스토리에 이어 "이어해줘 묻지말고"로 이 판까지(다섯 판 마지막은
사가국지, 다음 세션에서). 세이브 마이그레이션 지뢰(core.js `parsed.v !== 1`)는 다섯 판
공통이라 이미 별도 커밋으로 먼저 고쳤다(`saga-go/HANDOFF.md` 2026-09-17 절 참고).

**새 파일 `js/toon3d.js`** — 다른 판과 같은 3단 램프 `MeshToonMaterial`. 이 판은
`asset3d.js`에 이미 `looksRealistic()` 스킵(사진측량 탑성 등 PBR+HDRI 자산은 `delam()`
자체를 안 태움)이 있어 사가고와 같은 구조 — 실사 예외를 새로 만들 필요가 없었다.

**`js/village-view3d.js`** — 땅 배경판(`ground`)과 타일 8종(`initTerrain()`, 물 제외)이
`world3d.toon` 손잡이로 Toon/Lambert 를 고른다. **물 재질(`waterMaterial()`)은 절대
안 건드렸다** — `scene.environment` HDRI 반사를 쓰는 `MeshStandardMaterial`이라 툰으로
바꾸면 그 함수 주석이 자랑하는 "공짜 반사"가 죽는다.

**`js/asset3d.js`** — `delam()`·`primitive()`(GLB 실패 시 캡슐 자리표시) 둘 다 적용.

**외곽선은 이번에도 뺐다** — 땅이 `InstancedMesh`로 수백~수천 칸을 그리는 구조라, 칸마다
외곽선을 더하면 격자 전체가 검은 테두리로 뒤덮여 오히려 나빠 보일 위험이 크다(화면 확인이
안 되는 세션이라 무릅쓰지 않았다). **후처리 자동 끔(§8-6)도 해당 없음** — 이 판엔
`post3d.js` 자체가 없다(별도 렌더 타깃 합성 없이 `renderer.render()`를 바로 부른다 —
사가고·사가블로·사가스토리처럼 "실패하면 영구히 끄는" 대상 함수가 애초에 없는 구조).

**HTML·sw.js** — `index.html`·`_demo.html`·`_test.html`에 `toon3d.js`를 `asset3d.js`
앞에 얹었다. `sw.js` `SHELL`에도 추가(이 판은 `asset3d.js`가 원래 캐시 목록에 있었다 —
사가스토리와 다름), VERSION `v0.55.2`→`v0.56.0`.

**검증** — `node -c` 전 파일 통과, 세 HTML 인라인 스크립트 `new Function()` 파싱 확인,
`bash tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. 헤드리스 `_test.html` 3회
확인은 이번에도 안 돌렸다(precheck.sh 자체 규칙 — 실기 확인은 사용자 몫).

**남은 것** — 외곽선(배우만 골라 태그), §6.3 바이옴 5×24색 팔레트 스냅, 변형 배가,
오류 링버퍼 — PLAN §6·§7 에 남겨 뒀다. **다섯 판 중 사가국지(saga-realm)만 이번
연속 세션에서 손 안 댔다** — 다음에 이어서 하면 여기까지의 패턴(toon3d.js + delam +
땅 재질, 실사 스킵 확인, 후처리 유무 확인)을 그대로 따르면 된다.

## 2026-09-17 — SAGA-DESIGN §8-2 오류 링버퍼 (다섯 판 공통, saga-go 규격 그대로)

사가고→사가블로에 이어 셋째. 새 `js/errlog.js`(storageKey `yeoksa-village/errlog`,
`index.html`/`_admin.html`/`_test.html` 맨 첫 스크립트). `_admin.html`에 "오류" 탭(QA
프리셋과 점검·백업 사이) — `js/admin.js`의 `renderAll()`에 `renderErr()` 추가(전체가
try/catch 로 감싸여 있어 오류가 나도 다른 탭 렌더는 안 죽는다), `bind()`에 다시
읽기·복사·비우기 버튼 배선. `_test.html`에 순수 함수 `push()` 상한 진단 1항목. `sw.js`
`SHELL`에 `errlog.js` 추가, `VERSION` `village-v0.56.0` → `village-v0.57.0`.

**검증** — `node -c` 통과, `bash tools/precheck.sh saga-web/saga-forest` → PRECHECK OK.

## 2026-09-17 — 공사 진단 4 FAIL 원인 찾음 (3세션째 미확인이던 것) + 덤 2개

사용자 승인을 받아 이 항목 하나만 예외로 헤드리스 크롬을 세 번 돌려(진단용 정적
서버를 임시로 띄우고 끝나면 포트로 PID 를 찾아 죽였다 — `node.exe` 넓게 죽이지 않음)
실제 FAIL 메시지를 확보했다. `terrain.js`(덮개층)는 무죄였다 — **원인은
`_test.html` 자신의 테스트 위생 문제**였다.

**공사 FAIL 4 원인**: "묘목은 아직 딸 수 없다"(1217행 부근) 테스트가 사과를 심고
`s.planted`에 기록을 남긴 채 **끝나고 안 치웠다**. 한참 뒤 "공사" 구획 바로 앞의
다른 테스트가 `V.buildProps()`를 **`syncPlanted()`와 짝 없이 단독으로** 불러(2966행
근처) 그 심은 사물이 잠깐 배열에서 빠지는 순간, `findWorkPair()`가 그 자리(2,1)를
빈 칸으로 집었다. 그 직후 공사 테스트의 `TR.work()`가 `buildProps()+syncPlanted()`를
짝으로 불러 심은 사물이 되살아나면서 방금 고르고 있던 바로 그 칸을 막아버렸다 —
"되돌리면"부터 "삯이 모자라면"까지 4개가 전부 이 하나의 되살아난 사물 때문이었다
(`TR.can()`이 "묘목 이(가) 서 있습니다"로 막는 것을 직접 찍어 확인했다).
**고침**: "묘목은..." 테스트 끝에 `s.planted = []; V.syncPlanted();` 추가 — 심은
자리를 다음 테스트에 안 남긴다. `terrain.js`·`village.js` 등 프로덕션 코드는
한 줄도 안 건드렸다.

**덤으로 찾은 FAIL 2개**(PLAN엔 없던, 실행한 날짜에 우연히 걸린 것): "가방·판매 —
판 만큼 금이 들어온다"·"행사 — 그날은 그 갈래가 비싸게 팔린다"(대조군 쪽). 오늘
(2026-09-17)이 이 판 달력의 **진짜 한가위**라(다른 테스트가 이미 확인해 둔 사실),
`VD.eventOf()`가 실제 오늘 날짜를 그대로 읽어 "과일 갑절" 행사가 실제로 걸려
있었다 — 그 값을 안 건드리는 "여느 날" 전제 테스트까지 조용히 갑절을 받았다.
**처음엔 `core.setTune({'time.event':'none'})`으로 전역 고정을 시도했으나, `eventOf(date)`가
그 손잡이를 명시적 `date` 인자보다 먼저 본다는 걸 재확인 없이 넘겼다가 "행사 —
날짜로 정해지고..." 같은 순수 날짜 테스트 2개를 새로 깨뜨렸다** — 헤드리스로 다시
돌려 바로 잡아냈다. **최종 고침**: 전역 손잡이 대신 이미 있던 `fakeEvent(null)`
(파일 안 함수 선언이라 앞쪽 테스트에서도 호출 가능 — hoisting)로 그 두 테스트만
국소적으로 "행사 없음"을 박았다.

**검증** — 고치는 중간에도 매번 헤드리스 3회로 확인했다(디버그용 임시 진단 항목을
넣었다 빼며 원인을 좁혔고, 최종본은 전부 지웠다). 최종 `RESULT 267/267` 세 번 동일.
`bash tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. `sw.js` 버전 안 건드림
(`_test.html`만 고쳤다).

**교훈**: `V.buildProps()`를 `syncPlanted()`와 짝 없이 단독으로 부르는 테스트가
있으면 심은 사물이 일시적으로 사라져 그 틈에 실행되는 다른 테스트(특히 "빈 칸
찾기" 류)가 잘못 판단할 수 있다 — 이 패턴이 또 있는지는 안 훑었다(오늘 못 박은
자리 하나만 고쳤다).

## 2026-09-17 — SAGA-DESIGN §8-3 "옛 세이브 안 버려짐" 진단 1항목 추가

`core.js`의 `SAVE_VERSION`/`MIGRATIONS`/`migrate()`는 이미 있었는데(같은 날 앞 세션)
이 회귀를 잡아 줄 `_test.html` 진단이 saga-go 에만 있고 나머지 네 판엔 없었다
(PLAN §7 목록의 "아직" 항목). saga-go 것을 그대로 옮겨 붙였다 — `C.migrate({v:999,...})`가
지우지 않고 그대로 돌려주는지만 확인(`aa4b8b8` 류 지뢰 재발 방지, 다섯 판 공통 지뢰라
`saga-go/HANDOFF.md` 2026-09-17 절 참고).

**검증** — 헤드리스 3회 동일 확인(직접 돌림, 예외적으로 이번엔 코드 한 줄 진단이라
가벼움). `bash tools/precheck.sh` 대상에 포함해 PRECHECK OK.

## 2026-09-17 — PLAN §5.1 오늘의 일과판(목표판 + 일일·주간)

Phase 1 첫 후보. 채집·주민·탐험 세 축에서 하루 하나씩 날짜 해시로 뽑고,
완료하면 골드를 준다 — 표준 A(목표판)·H(돌아올 이유)를 채운다.

- **진행은 baseline-delta 로 잰다** — `bagCatCount()`(현재 가방, 팔면 준다)를
  그대로 쓰면 팔았을 때 일과 진행이 거꾸로 간다. 새 `caughtCatCount()`(도감
  `s.caught` 를 센다, 안 줄어든다)를 만들고, 롤할 때 그 시점 값을 `base` 로
  세이브에 박아 뒀다가 `지금값 - base` 로 진행을 낸다.
- **새 누계 셋만 보탰다** — `s.giftTotal`(giveGift)·`s.weedPulled`(pullWeed)
  ·`s.donateTotal`(museum.donate). 배달은 이미 있던 `s.delivery.n` 을 그대로
  빌렸다. 채집은 새 카운터가 필요 없다(`caughtCatCount` 재사용).
- **축 중복이 구조로 0** — 추첨이 아니라 `TASK_POOL` 을 axis(gather/social/
  explore)로 갈라 축마다 하나씩 뽑는다. PLAN 이 요구한 "100일 굴려 같은 축
  중복 0" 진단은 그래서 항상 통과할 수밖에 없다(그래도 진단은 넣었다 —
  나중에 축 배정을 잘못 고치면 바로 걸리게).
- **행사일 override** — `VD.eventTaskOf(ev)`가 셋째(탐험) 줄을 대신한다.
  `up.cat` 있는 행사는 그 갈래로 자동 파생, 설날(up 없음)만 `task` 필드를
  직접 적었다("대화" 전용 카운터가 없어 선물로 근사 — 실명·대화 시스템
  없는 한계, §5.1 범위 밖). `town.js`의 기존 festival 조회처럼 `time.dayShift`
  는 아직 안 탄다(day-shift 로 행사를 밀어 봐도 축제 판정 자체는 실제
  오늘 날짜를 본다 — 기존에도 그랬다, 회귀 아님).
- **UI** — 새 시트를 안 만들고 게시판(🪧)이 이미 열던 `town` 시트 맨 위에
  얹었다(§5.1 "게시판이 전체 목록을 연다"). 상단 `.profile` 에도 한 줄
  (`.p-goal`, saga-go 것과 같은 CSS) — 첫 미완료 일과 이름 + 오늘 진행.
- **뺀 것** — "완료 즉시 다음 줄 채움(당일 최대 6)"·목표판 접기/펼침·✓ 0.4s
  빛나는 연출·번들(§5.3)과의 연계는 이번엔 안 했다. 마무리 카드(§5.2)도
  다음 차례.

**검증** — 헤드리스 3회 `RESULT 275/275` 동일(268 + 신규 7). `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. `sw.js`
`village-v0.57.0` → `village-v0.58.0`.

**남은 것** — 실기 체감(3택 다양성·행사날 문구), §5.2 마무리 카드, §5.8①
채집 손맛 5요소가 Phase 1 나머지.

## 2026-09-17 — PLAN §5.2 하루 마무리 카드

Phase 1 두 번째 후보. 트리거 ②(자정 넘겨 첫 부팅)만 했다 — 트리거 ①(침구
상호작용)은 이 판에 "침구" 가구가 아예 없어(FURNITURE 표에 방석·화로 등은
있어도 bed 류가 없다) 새 가구 한 종을 짓는 몫이 커서 미뤘다(§5.2 §10 결정
없이 임의로 미룸 — 다음에 이어할 때 먼저 사용자와 확인).

- **`village.js`** — `rollDay()`가 날을 넘기기 **직전**(리셋 전)에
  `buildDayLog(어제날짜)`로 전날 값을 굳히고, 리셋 뒤 `snapshotDayMark()`로
  다음 비교 기준(그 시점의 gathered·gold·donateTotal·friend 표·마을 평가)을
  다시 박는다. "주민 한 줄"은 대사 데이터가 없어 오늘 정(`friend`)이 가장
  많이 오른 사람 이름으로 근사했다(§5.4가 서면 그 사람의 고유 대사로 바꿀
  여지를 남겨 뒀다). "내일 한 가지"는 `taskList()[0]`(§5.1, 방금 rollTasks()
  가 뽑은 오늘 첫 일과)을 그대로 쓴다.
- **`ui.js`** — `#encounter`(이 판이 여태 안 쓰던 자리 — grep 해 보니 CSS는
  있는데 부른 곳이 없었다)를 빌려 카드를 띄운다. 8초 자동 닫힘 또는 확인
  버튼, 닫히면 `dayLogSeen()`으로 그날은 다시 안 뜬다.

**검증** — 헤드리스 3회 `RESULT 278/278` 동일(275 + 신규 3: next 안 빔·한
번만 뜸·채집/금 델타가 실제 값을 따라감). `bash tools/precheck.sh
saga-web/saga-forest` → PRECHECK OK. `sw.js` `village-v0.58.0` →
`village-v0.59.0`.

**남은 것** — 트리거 ①(침구), 카드 그림(종이 질감 9-slice, §2-6 에셋),
실기 체감(정보량 5개 이하인지). Phase 1 마지막 후보는 §5.8① 채집 손맛
5요소.

## 2026-09-17 — PLAN §5.8① 채집 손맛 표준 C (Phase 1 마지막 후보)

일과판(§5.1)·마무리 카드(§5.2)에 이어 Phase 1 세 후보 전부 코드분 완료.

- **`village.js`** — `bumpGatherStreak()`(§5.8①의 "연속 채집 3회마다 리듬
  보너스"). 타이밍 기준(8초 안에 이어 쳐야 연속)이라 §5.1 일과의 "하루
  몇 번"과 다른 축이다. `gather()` 결과 자리에 `core.emit('village:gather',
  {item, n, streak, bonus})` 한 줄 — 화면 층(흔들림·팝·효과음)이 듣는다.
- **UI 레이어에서만 흔들림·팝** — PLAN이 이미 "sprite.js는 안 건드린다"고
  적어 둔 대로, 캔버스·3D 는 손 안 댔다. `#focusbar`(손이 닿는 카드) 자체에
  0.25s CSS 펄스(`gather-pulse`), `document.body`에 fixed로 뜨는 "🍑 +1"
  텍스트가 0.6s 떠올랐다 사라진다(`gather-pop`).
- **효과음** — 새 mp3 없이(원작사 에셋 금지, 루트 CLAUDE.md) `sfx.js`(전부
  WebAudio 절차 생성)에 `gather_a/b/c` 세 파형(파형·음높이만 다르다)을
  만들어 `village:gather` 를 들을 때마다 라운드로빈으로 돌린다. 보너스는
  `gather_bonus`(쨍한 3음 차임)로 따로 알린다. `sfx.js` 머리말의 "판정
  코드는 한 글자도 안 건드렸다"를 "딱 네 곳"으로 고쳤다 — `gather()`가
  다섯 번째 예외.
- **함정 하나** — 벌집(`shake()` 의 `SHAKE_BEE`)이 뜨면 그 자리에 말벌이
  스폰되고, 잠자리채 없인 다음 `interact()` 마다 그 말벌이 최근접이 돼
  전부 `'no'`로 샌다. 연속 채집 진단이 처음엔 이걸 몰라 30번 중 4번만
  채집에 성공했다 — `core.setTune('shake.bee', 0)` 로 끄려 했지만
  `SHAKE_BEE`가 모듈 로드 시 **한 번만** `core.tuned()`로 읽히는 상수라
  안 들었다(걸음 배속과 같은 함정, 새로고침 필요). 대신 매 시도 뒤
  `DG.bug.reset()`으로 스폰된 말벌을 치우는 쪽으로 고쳤다.

**뺀 것** — ②(마을 평가를 별 5로) · 자동 순행 손맛 건너뛰기(이미 그렇다,
새로 할 것 없음 확인만) · 낚시 성공 시 0.2s 줌인(카메라 손대는 몫이라
3D 파일을 봐야 한다, 다음 차례) · "입력→반응 ≤100ms"는 애초에 동기 호출
이라 잴 것이 없다(구현 0줄, 검증만).

**검증** — 헤드리스 3회 `RESULT 282/282` 동일(278 + 신규 4). `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. `sw.js`
`village-v0.59.0` → `village-v0.60.0`.

**Phase 1 마무리** — 세 후보(5.1·5.2·5.8①) 전부 코드분 완료. 남은 건
실기 확인(표준 A·B·C 체감, "할 게 보인다" 여부)과 이번에 미룬 조각들
(침구 트리거·별5 평가·낚시 줌인). 다음은 Phase 2(마을 변화, 5.3·5.4·5.8②)
또는 다른 판.

## 2026-09-17 — PLAN §5.3 마을 번들① (Phase 2 첫 후보)

Phase 1(5.1·5.2·5.8①) 마무리에 이어, 로드맵 Phase 2("마을 변화") 첫 후보.
사고(史庫) 기증이 "칸이 채워지는 것"으로 끝나던 걸 마을 시설로 잇는다
(SAGA-DESIGN §3-G "보이는 것 1개").

- **한 가지 정책 결정을 안 하고 넘겼다** — PLAN 원안은 꽃·열매까지 6번들인데,
  `MUSEUM_CATS`(museum.js)가 애초에 곤충·물고기·화석·조개 넷만 기증을 받는다
  (꽃·열매는 팔거나 선물하는 물건이라 이 판에 "사고에 들인다" UI 자체가
  없다). 기증 갈래를 늘리는 건 선물·판매와 겹치는 물건을 사고에도 받을지
  정하는 별도 결정이라 **이번엔 넷만 채우고 꽃·열매는 §10에 열린 질문으로
  남겼다** — 다음에 이어할 때 먼저 사용자와 확인.
- **`js/data-village.js`** — `VD.BUNDLES`(갈래→시설 이름) 신설. 새 PROPS
  둘: `stele`(석비)·`fireflyplot`(반딧불이 정원) — 둘 다 `gather:null`이라
  `village-view.js`의 `default:` 이모지 폴백이 그대로 그린다(새 draw 함수
  없이).
- **`js/village.js`** `buildProps()` — `museum.byCat()`(이미 있는 갈래별
  done/total 계산, 새로 안 만들었다)로 완결 여부를 보고 시설을 심는다.
  화석→석비(사고 왼쪽), 곤충→반딧불이 정원(마을 광장 한쪽), 물고기→
  호숫가 평상(**기존 `bench` 재사용**, `lakeCenter()` 동쪽 기슭), 조개→
  강가 조개 두 점(**기존 `shell` 재사용**, `riverCenterX()` 둑 — "길"이라기엔
  짧다, 다음에 늘릴 여지). 전부 캠프·museum과 같은 결로 **해시가 아니라
  세이브 조건부 고정 자리**. 호수/강 자리가 필요한 둘(물고기·조개)은
  `lakeCenter()`가 null(고리를 좁혀 호수를 포기한 판)이면 같이 건너뛴다.
- **`interact()`** — 새 프롭 둘은 `sapling`과 같은 결로 `{kind:'empty', text}`
  toast만 띄운다. `village:open`을 쐈으면 `ui.js` `SHEET_TITLE`에 없는
  이름이라 `renderSheet()`의 마지막 삼항 분기(`viewLog()`)로 떨어져
  엉뚱하게 "기록" 시트가 열렸을 것 — 코드를 안 돌려 보고도 `renderSheet()`
  읽어서 미리 잡았다.

**안 한 것** — 꽃·열매 번들(위 정책 결정 대기), 반딧불이 밤 파티클 ×3
(자리 표시만), 조개 길 모래 타일 덮개층(`terrain.js`, 지금은 그냥 모래밭에
조개 두 점), 사고 시트 "번들" 탭 UI(진척 바 4~6개), 6개(또는 4개) 완성 시
마을 평가 상한 해제(§5.8②가 아직 없어 연결할 대상이 없다), 깃발 문양,
3D(`village-view3d.js` SCATTER 표 — 2D는 이모지 폴백으로 이미 됨).

**검증** — `node -c js/data-village.js js/village.js` 통과, `_test.html`
인라인 스크립트 2블록 `new Function()` 파싱 확인. 새 진단 2항목 — ① 화석
갈래만 채우면 석비만 서고 곤충 갈래는 그대로 없음(조건부 등장 확인) ②
넷 다 채워도 시설 5개(석비·반딧불이·평상·조개×2)가 기존 고정 건물(전방·
집·사고·게시판·우편함·침선방·깃대)과 좌표가 안 겹침. `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK(`sw.js` VERSION
`village-v0.60.0` → `village-v0.61.0`). 헤드리스 `_test.html` 3회 확인은
이번에도 안 돌렸다(사용자 실기 확인 몫, 다른 항목들과 같은 이유).

**남은 것** — 꽃·열매 번들 정책 결정(§10), 위에 적은 "안 한 것" 전부,
그리고 이번 시설 다섯이 실제로 마을 안에서 자연스러운 자리인지(특히
조개 두 점이 "길"로 보이는지) 실기 확인.

## 2026-09-17 — PLAN §5.3 마을 번들, 이어서 — 사고 시트 UI + 즉시 등장

앞서 지은 번들 넷(화석·곤충·물고기·조개)에 남겨 뒀던 두 가지를 채웠다:
시설이 완성 "순간" 바로 보이는 것, 그리고 사고 시트에서 그 사실이 보이는 것.

- **`js/museum.js`** — `catStatus(catKey)`(갈래 하나의 done/total, `byCat()`
  의 홑겹판) 신설. `donate()`가 기증 전/후로 그 갈래 상태를 비교해 **막
  다 채웠는지** 판정하고, 그랬으면 그 자리에서 `V().buildProps()`를 부른다
  — 원래 `buildProps()`는 자정 넘김(`init()`의 날짜 롤오버)에만 저절로
  도는 함수라, 안 그러면 시설이 다음 날이 돼야 보였을 것(§5.2 하루
  마무리 카드보다도 늦게). 반환값에 `bundleCompleted`(갈래 키 또는
  `null`) 필드를 얹고, 토스트·로그 문구 끝에 "🎉 [번들 이름]!"을 붙인다.
- **`js/ui.js`** `viewMuseum()` — 전시실 네 칸(곤충·물고기·화석·조개) 마다
  `VD.BUNDLES`에 있으면 진척 바 아래에 "다 채우면 마을에 시설이 하나
  섭니다" / "✓ 다 채웠습니다 — 마을에 시설이 섰습니다" 한 줄을 더한다.
  **새 탭을 안 만들었다** — PLAN 원안은 "번들 탭"이었지만 이미 있는
  전시실 칸(갈래별 진척 바)이 사실상 그 역할이라 탭을 하나 더 얹는 대신
  거기 문구만 보탰다 — 더 적은 코드로 같은 정보를 준다.

**검증** — `node -c js/museum.js js/ui.js` 통과, `_test.html` 인라인
스크립트 2블록 파싱 확인. 새 진단 2항목 — ① 화석 갈래 마지막 한 종을
들이는 순간 `bundleCompleted==='fossil'`이고 토스트에 🎉가 있고 석비가
`V.raw().props`에 바로 잡힘(다음 날까지 안 기다려도 됨) ② 첫 한 종만
들인 기증은 `bundleCompleted`가 없고 축하 문구도 없음. `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK(`sw.js` VERSION
`village-v0.61.0` → `village-v0.62.0`). 헤드리스 3회 확인은 이번에도
안 돌렸다(사용자 실기 확인 몫).

**남은 것** — 여전히: 꽃·열매 번들 정책 결정(PLAN §10-Q7), 반딧불이 밤
파티클 ×3, 조개 길 모래 타일 덮개층, 마을 평가 상한 해제 연결(§5.8②
대기), 깃발 문양, 3D 반영. §5.3 자체는 이번 두 세션으로 "①(자리)+UI"
까지는 닫혔고, 나머지는 전부 §10 결정이나 다른 후보(§5.8②)가 먼저 서야
잇는 것들이다.

## 2026-09-18 — §10 열린 질문 Q1~Q5 확정, Q6·Q7 보류

PLAN.md §10 일곱 질문을 Q1 순서부터 처리:

- Q1 목적지 성격: 우주기지식 고정 마을 결(2026-09-11 결정) 그대로 따름.
- Q2 현대 목적지 자산: kitbash.py 로 Space Kit+Fantasy Town 조합(권장안 ②) 채택.
- Q3 탑성 폐허 PBR: 툰 재질로 스타일 통일(권장안) 채택.
- Q4 생성 에셋 커밋: assets/generated/ 산출물 커밋(사가블로 2026-09-18 결정과 같은 이유).
- Q5 보스방 퍼즐: 지금은 넣지 않음, §5 우선순위 밖으로 미룸.
- Q6 관계 하트 이사 조건 대체, Q7 사고(史庫) 꽃·열매 겹침 문제는 원작 감각·설계
  취향 판단이 필요해 보류 — 사용자 결정 대기.

코드 변경 없음, PLAN.md §10 문구만 갱신.

## 2026-09-18 — §7 Phase 0 코드 항목 5·6 마저 닫음

실기 없이 코드만으로 닫을 수 있는 Phase 0 항목을 정리하던 중:

- 항목 5(진단 구멍): `data-village.js`에 `weatherOverride`+`_setWeather(v)` 신설.
  `weatherOf()`가 `knob()`보다 먼저 이 값을 본다 — `town.js` `_setStar`·
  `turnip.js` `_setOpen`과 같은 결이다. `knob()`은 주석에 "자가진단·데모는
  안 읽는다"고 이미 적혀 있어 자가진단 쪽엔 별도 구멍이 필요했다. 실제
  축제·행사 진단 항목 자체는 Phase 4(달력, 미착수)와 맞물려 있어 이번엔
  구멍만 뚫었다.
- 항목 6(sw.js VERSION precheck): 조사해 보니 루트 `tools/precheck.sh`
  (다섯 판 공통)에 이미 있었다 — js 고치고 sw.js 그대로면 WARN. 이 판
  전용으로 새로 만들 것 없이 완료로 표시.

**검증** — `node --check js/data-village.js` 통과. `bash tools/precheck.sh
saga-web/saga-forest` → PRECHECK OK. `sw.js` `village-v0.62.0` →
`village-v0.63.0`.

## 2026-09-18 — §5.1 침구 트리거(①) 구현, Phase 1 마지막 셋 중 하나 닫음

2026-09-17에 트리거 ②(자정 넘겨 첫 부팅)만 하고 미뤘던 트리거 ①(침구)을 마저
지었다. 이 판에 "침구" 가구가 아예 없었던 게 미룬 이유였다.

- `data-village.js`: `FURNITURE`에 안방 가구 "요"(`key: 'yo'`, `price: 1000`,
  `bed: true`) 신설. 전방 진열(`shopToday()`)이 `VD.FURNITURE` 전체를 그대로
  훑으므로 등록만으로 자동으로 팔린다.
- `village.js`: `sleepNow()` 신설 — **`rollDay()`를 부르지 않는다**(PLAN
  §5.1 "실시간 규칙" 그대로). `buildDayLog(s.day)`만 다시 굴려 지금까지
  쌓인 값(채집·금·기증·마을 평가 변화)으로 카드를 미리 만든다. 날짜·과제·
  마을 상태는 안 건드린다 — 자정이 지나면 `rollDay()`가 그때 다시 온전한
  하루치로 갈아 낸다.
- `ui.js`: 놓인 가구가 `bed`면 초점 카드가 "잠든다"(`v-sleep`, 기본)와
  "거둔다"(`v-do`, 기존 그대로 유지 — 여전히 창고로 거둘 수 있다) 둘 다
  보여준다. `v-sleep`은 `sleepNow()` 뒤 바로 `checkDayCard()`를 불러 다음
  주기(tickRefresh)까지 안 기다리고 그 자리에서 카드가 뜬다.
- `village-view.js`: 캔버스가 그리는 가구 도형에 `bedding` 케이스 추가
  (`furnBedding` — 편 요 + 베개 + 개킨 이불), 포커스 말풍선도 `d.bed`면
  "거둔다" 대신 "잠든다"로 갈린다(이 라벨은 캔버스가 직접 그리는 것이라
  ui.js HTML 카드와 별개로 손대야 했다).
- `_test.html`: "침구 — 잠들면(sleepNow) 날짜는 그대로에 카드만 미리 뜬다"
  회귀 진단 1항목 추가 — 기존 "채집·금 변화" 테스트와 같은 요령(스냅샷 뒤
  금만 올리고 확인, 끝나면 상태 복원).

**검증** — `node --check` 4개 파일(data-village.js·village.js·ui.js·
village-view.js) 전부 통과, `bash tools/precheck.sh saga-web/saga-forest`
→ PRECHECK OK. `sw.js` `village-v0.63.0` → `village-v0.64.0`. **헤드리스
확인은 안 함**(이 판 규칙 — `_test.html` RESULT·실제 화면은 사용자 실기
몫). 특히 새로 그린 `furnBedding` 모양과 "요"를 실제로 사서 놓아 보는 것은
사용자가 확인해야 한다.

**남은 것** — Phase 1의 나머지 둘: §5.8② 평가 별 5(조건 공개·경고), §5.8①
낚시 줌인(카메라 손대는 몫, 3D 파일을 봐야 한다).

## 2026-09-18 — §5.8② 평가 별 5 + 경고 구현, Phase 1 남은 건 낚시 줌인뿐

- `town.js`: `beauty()`가 `stars`(1~5, `level+1`)·`home`·`museum` 필드를 더
  낸다. 새 `beautyWarning()` — 잡초가 하루 최대(`WEED_PER_DAY`)로 늘어도
  지금 등급을 지키는지 보고, 못 지키면 `{weeds, who, text}`를 낸다(`who`는
  `s.residents` 중 날짜 해시로 고른 한 명 — 하트가 없어 "정 든 사람" 대신
  아무나다).
- **PLAN §5.8 "수치"에 적힌 20/40/60/80/95 대신 기존 `BEAUTY_GRADES`
  (0/60/100/150/200)를 그대로 별로 매겼다** — `mail.js`(주민 이탈)가 이미
  이 표를 보고 있어 새 눈금을 따로 만들면 두 표가 어긋날 위험이 있었다.
  조건 공개도 §5.3(시설)·§5.4(하트)가 아직 없어 **지금 실제로 점수에
  들어가는** 잡초·꽃·심은 나무·집 꾸미기·사고 기증 다섯 줄로 채웠다.
- `ui.js`: 마을 시트의 "마을 평가" 카드가 등급명 옆에 별 5개(★☆)를 보여주고,
  집·사고 줄을 추가. 경고가 있으면 기존 안내문 대신 그 자리에 뜬다. 하루
  마무리 카드(§5.2)에도 같은 경고 줄을 얹었다.
- `_test.html`: 별이 `level+1`인지, 그리고 "안전 문턱에서는 경고 없음 →
  1포기만 더 늘어도 경고가 뜬다"를 확인하는 진단 2항목 추가(안전 문턱은
  현재 등급·`WEED_PER_DAY`로 역산).

**검증** — `node --check` town.js·village.js·ui.js 통과. `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. `sw.js`
`village-v0.64.0` → `village-v0.65.0`. **헤드리스 확인은 안 함** — 별
표시·경고 문구가 실제 화면에서 보기 좋은지는 사용자 실기 몫.

**남은 것** — Phase 1 마지막 하나: §5.8① 낚시 줌인(카메라, 3D 파일을
봐야 하는 몫이라 이어서 진행).

## 2026-09-18 — §5.8① 낚시 줌 인 구현, Phase 1 코드분 전부 닫힘

Phase 1(루프)에 남아 있던 마지막 하나. 카메라를 손대는 몫이라 미뤄 뒀던
것을 `village-view3d.js`에 넣었다.

- `fishZoomTimer`(0.2s 감소 카운터) + `triggerFishZoom()` 신설. `init()`
  안에서 `sfx.js`가 이미 듣던 `village:fish` 이벤트를 그대로 구독해
  `state==='catch'`일 때만 튼다 — `init()`은 `game.js`에서 한 번만 불러
  중복 구독 걱정이 없다.
- `syncCamera()`의 radius0·height0·radius1·height1 넷을 새 `zoomPulse`
  (1 + 0.18 × 남은시간/0.2)로 한 번 더 나눠 0.2초 동안 살짝 당겼다 풀린다.
  **사람이 손으로 정한 `userZoom`은 안 건드리고 그 위에 덧씌우는 배율이라**
  핀치줌 등 기존 카메라 조작과 안 부딪힌다.
- `triggerAction`(몸짓)과 달리 `player.actions` 준비 여부에 기대지 않아
  init 전에도 타이머는 그대로 세워진다 — `_test.html`에 그 성질 진단
  1항목 추가(`fishZoomTimer()` 게터도 `actionTimer()`와 같은 자리에 신설).

이걸로 **사가의숲 Phase 1(루프)의 코드분이 전부 닫혔다** — 일과판·마무리
카드·채집 손맛 ①(침구·별5·줌인 포함) 모두 코드는 끝났고, 남은 건 §7
실기 확인 목록과 이번 세 가지(요 가구·별 표시·낚시 줌인)가 실제 화면에서
어떻게 보이는지뿐이다.

**검증** — `node --check js/village-view3d.js` 통과. `bash
tools/precheck.sh saga-web/saga-forest` → PRECHECK OK. `sw.js`
`village-v0.65.0` → `village-v0.66.0`. **헤드리스 확인은 안 함** — 카메라
줌이 실제로 자연스러운지(당기는 정도 0.18, 시간 0.2s가 적당한지)는
WebGL 화면이 있어야 보이는 것이라 여느 3D 시각 효과처럼 실기 확인 몫.

## 2026-09-18 — §10-Q7 결정: 사고(史庫)는 꽃·열매를 안 받는다

사용자 결정: 꽃·열매는 판매·선물 갈래로만 남기고 사고 기증에는 안 넣는다.
`PLAN.md` §5.3·§10 을 이 결정대로 고쳤다 — 사고 번들은 곤충·물고기·화석·
조개 **4갈래 고정**(원안의 꽃→등롱길·열매→시장 천막 시설은 없앤다).
"6개 완성 → 마을 평가 상한 해제"도 "4개 완성"으로 내렸다. 코드는 이미
`MUSEUM_CATS`가 넷뿐이라(2026-09-17 구현 당시부터) 문서만 실제 구현에
맞춘 것 — 코드 변경 없음.

## 2026-09-18 — §10-Q6 결정: 이사 조건은 마을 평가 + 하트 둘 다

사용자 결정: "둘 다 반영" — 이사 확률은 기존 `mail.js` 결(마을 평가
`beauty()` 가 방치할수록 확률을 올리는 원작 감각)을 그대로 두고, 후보를
고르는 기준(`onNewDay()` 의 `fr >= FRIEND_KEEP` 컷)만 §5.4 가 새로 들일
`heart` 로 바꾼다. `friend` 는 그대로 대화·선물 로그용으로 남을지, `heart`
로 완전히 대체될지는 §5.4 착수 시점(Phase 2)에 코드 보고 정한다.
`PLAN.md` §5.4·§10 을 이 결정대로 고쳤다 — 코드 변경 없음(§5.4 는 아직
미착수).

## 2026-09-18 — PLAN §5.3 마을 번들 마무리: 4개 완성 시 마을 평가 상한 해제

Phase 2(마을 변화)의 §5.3(마을 번들)이 2026-09-17에 "①만" 구현되며
남겨 둔 "안 한 것" 다섯 줄 중 하나를 닫았다. 그날은 §5.8②(별 5 평가)가
아직 없어 "연결할 대상 자체가 없다"고 적어 뒀는데, 바로 다음 세션
(같은 날 늦게)에서 §5.8②가 생겼으니 이번에 이었다.

- **`museum.js`**: `allBundlesDone()` 신설 — `byCat()`(이미 있는 계산)의
  네 갈래(곤충·물고기·화석·조개) 전부 `done>=total`인지만 본다.
- **`town.js`**: `beauty()` 안의 상한 로직을 순수 함수 `capBeauty(score,
  bundlesFull, grades)`로 뽑았다(게임 상태 없이 값으로 검증하려고) — 번들을
  다 못 채웠는데 점수가 `BEAUTY_GRADES` 마지막 등급 문턱 이상이면 그
  문턱 −1로 눌러 앉힌다. `beauty()`는 `museum.allBundlesDone()`을 불러
  이 함수에 넘기고, 반환값에 `bundlesFull`·`capped`를 얹는다. **등급표
  자체(`BEAUTY_GRADES`)는 안 바꿨다** — "잡초·꽃·나무·집·기증만으론
  최고 등급이 안 된다"는 상한만 걸었을 뿐, 등급 문턱 수치는 그대로다.
- **`ui.js`**: 마을 시트 "마을 평가" 카드에 `bt.capped`일 때만("🏛️ 사고
  네 갈래를 다 채우면 최고 등급까지 오를 수 있습니다") 한 줄 — 다 채웠으면
  조용히 빠진다(더 알릴 것이 없다). `admin.js` QA 텍스트에도 "상한 걸림"
  꼬리표 하나.
- **검증** — `node -c`(museum.js·town.js·ui.js·admin.js) 통과. `node` vm
  으로 `town.js`를 core stub 하나만 채워 로드해 `_capBeauty`를 실제
  숫자로 대조(상한 미만 통과, 상한서 번들 미완=−1점 clamp, 번들
  완성=아무리 높아도 안 막힘 — 넷 다 예상과 일치). `_test.html`에
  진단 2항목: ①`_capBeauty` 경계값 넷(상한 아래·상한서 미완·상한서
  완성·상한 훌쩍 넘어도 완성이면 그대로), ②`museum.allBundlesDone`을
  몽키패치해 `beauty()`가 실제로 그 값을 그대로 반영하는지(스코어는
  게임 상태에 달려 있어 안 재고, "완성 쪽은 절대 안 막힌다"는 안전한
  성질만 확인).
- **여전히 안 한 것**(§5.3 "안 한 것" 나머지 넷): 반딧불이 밤 파티클
  ×3, 조개 길 모래 타일 덮개층(`terrain.js`), 깃발 문양, 3D
  (`village-view3d.js` SCATTER 표) — 전부 그래픽·에셋 몫이라 다음에
  따로 좁혀서 갈 것.

**실기 확인 남음**: 상한 문구가 마을 시트에서 자연스럽게 보이는지,
번들을 마지막으로 완성하는 순간 그 문구가 사라지는 것이 체감되는지.

## 2026-09-18 — PLAN §5.3 반딧불이 밤 파티클 ×3 (마을 번들 남은 것 하나 더)

바로 위 세션이 §5.3 "안 한 것" 목록에 남긴 넷(반딧불이·조개 길·깃발
문양·3D SCATTER) 중 반딧불이를 좁혀서 짰다.

- **`village-view3d.js`**: 앰비언트 반딧불이(`WEATHER_FX.firefly`, 40개,
  인물 둘레 반경 18에 고르게 — 늘 인물을 따라다닌다)는 그대로 두고,
  `WEATHER_FX.fireflyBoost`(24개, 반경 5)를 새로 지었다. 앰비언트는
  `makePoints()`가 늘 원점(인물) 중심으로 짓는데, 정원 무리는 **세계
  좌표에 고정**돼야 하므로 같은 함수로 짓되 매 프레임 `syncWeatherFX()`
  에서 `fireflyplot` 소품의 자리로 그룹 위치를 옮긴다(`syncScatter()`가
  일반 소품에 쓰는 `(p.x-px)*scale` 변환을 그대로 빌렸다).
- **순수 함수 `fireflyPlotOf(props)`** 신설 — 배열에서 `kind==='fireflyplot'`
  인 소품을 찾는다(없으면 null). `syncWeatherFX()`는 이 함수로 소품을
  찾고, 있어도 카메라에서 너무 멀면(`CULL_R()` 밖) 숨긴다 — 밤이 아니거나
  비/눈이면(`fireflyVisible()`, 기존 조건 그대로) 역시 안 보인다.
- **밀도 배율(×3)은 식으로 안 맞췄다** — 반경 5 안에 24개라는 눈에 띄는
  고정 수를 그냥 골랐다(면적 비례로 정확히 3배를 내려면 4개 안팎이라
  너무 적어 보일 것 같았다). 실기 확인 전까지는 숫자 자체가 맞는지
  알 길이 없다.
- **검증** — `node -c`(village-view3d.js) 통과. `node` vm으로
  `village-view3d.js`를 `DG.core.tuned` 스텁 하나만 채워 통째로 로드해
  `fireflyPlotOf`를 실제 값으로 대조(없으면 null, 있으면 그 소품 그대로).
  `_test.html`에 같은 성질 진단 1항목 추가.
- **여전히 안 한 것**(§5.3 "안 한 것" 나머지 셋): 조개 길 모래 타일
  덮개층(`terrain.js`), 깃발 문양, 3D(`village-view3d.js` SCATTER 표) —
  다음에 따로.

**실기 확인 남음**: 정원 근처에서 반딧불이가 실제로 "몰려든" 느낌인지
(24개가 너무 적거나 많은지), 앰비언트 무리와 겹쳐 이상하게 보이지
않는지, 밤이 돼도 정원 자리를 못 찾겠으면(카메라가 멀어) 안 보이는
것이 자연스러운지.

## 2026-09-18 — PLAN §5.4 관계 하트 착수: §10-Q6 배선 + 7♥ 동행

§5.4(관계 하트)는 여태 `heart` 값 자체(대화·선물·부탁·편지·세배로 오르는
0~10 게이지)와 UI 표시(주민 목록의 💗·다음 해제 조건)는 있었지만, **그
숫자가 실제로 뭘 여는지는 하나도 없었다**(`HEART_UNLOCKS` 는 이름표
데이터뿐, 잠금을 여는 코드가 없었다) — grep 으로 직접 확인했다. 네 해제
(3♥ 집 방문·5♥ 고유 대화·7♥ 동행·10♥ 기념품) 중 코드로 바로 옮길 수
있는 것만 좁혀서 이었다.

- **§10-Q6 이사 조건 배선** — 그날 결정만 하고 "코드 변경 없음"이라 적어
  뒀던 것을 이제 이었다. `mail.js`의 떠날 뜻 **후보** 조건을 `s.friend[rid]
  >= FRIEND_KEEP`(친밀도) 대신 `V.heartOf(rid) > HEART_LEAVE_KEEP`(새
  손잡이 `move.heartLeaveKeep`, 기본 2)로 바꿨다 — §5.4 진단이 원문에
  적어 둔 그대로("하트 2 이하만 이사 후보"). 확률·문구·마을 평가 배율은
  안 건드렸다. **안부 편지**(`warm`, 여전히 `friend` 기준)는 §10-Q6이
  건드리라고 한 자리가 아니라 그대로 뒀다.
- **7♥ 동행** — `folk.js`에 `startFollow(id)`/`stopFollow()`/
  `followStatus()` + 30초 카운트다운(`FOLLOW_SEC`). `update(dt)`의 어슬렁
  루프 맨 앞에서 동행 중인 사람만 골라 플레이어 쪽으로 걷게 하고(`FOLLOW_
  DIST`=40 밖에서만 다가선다, 그 안쪽은 붙어서 안 걷는다), `continue` 로
  어슬렁 로직을 건너뛴다 — 풀리면(시간 소진 또는 그 사람이 마을을
  떠나면) `r.aim`이 비어 있어 다음 어슬렁 걸음이 제 자리(`home`) 둘레로
  저절로 되돌아간다(억지로 텔레포트 안 시킨다). 자격 판정(하트 7)은
  `village.js`의 새 `heartUnlockAt(name)`(`HEART_UNLOCKS` 를 이름으로
  찾아, 표 순서·값이 바뀌어도 인덱스에 안 얽맨다)·`canFollow(id)`·
  `requestFollow(id)`가 갖고, `folk.js`는 "따라 걷기" 그 자체만 안다 —
  두 파일이 책임을 나눠 가진다.
  `ui.js`: 곁에 서 있을 때(`nearFolk()`, 선물 버튼과 같은 문맥) "🚶 함께
  걷기" 버튼 하나(`data-act="v-follow"`), 주민 목록엔 동행 중이면 남은
  초를 보여 준다(`folk.followStatus()`).
- **"채집 확률 +10%"는 이번에 안 짰다.** 코드를 뒤져 보니 이 판엔
  "채집 성공률" 이라는 확률이 애초에 없다 — 나무를 흔들거나 벌레를
  잡으면 손에 닿는 순간 100% 받는다. `shake()`(나무)의 벌집/가구/돈주머니
  세 갈래는 "무엇이 더 나오나"를 가르는 보너스 굴림이지 실패가 있는
  굴림이 아니다. 이 수치를 어디에 붙일지(보너스 갈래 확률을 올릴지, 다른
  걸 만들지)는 판단이 필요해서 마음대로 정하지 않고 열어 둔다 — PLAN
  §5.4에 그렇게 적어 뒀다.
- **3♥ 집 방문·5♥ 고유 대화도 이번엔 안 했다** — 집 방문은 "주민 개인의
  집"이라는 세계 좌표 자체가 이 판에 없다(플레이어 집만 `home.js` 하나
  뿐이다), 늘리려면 건물·문·자리를 새로 잡는 큰 작업이다. 고유 대화는
  주민 5+NPC 7 = 12명 × 3줄을 실명 금지 원칙 아래 새로 써야 하는 콘텐츠
  작업이라 이번 세션 범위 밖으로 남긴다.
- **검증** — `node -c`(folk.js·village.js·mail.js·ui.js) 통과. `node`
  vm 으로 `folk.js` 를 최소 스텁(`village.walkable`·`raw`·`state` 세
  함수만)으로 채워 실제 로드해, `startFollow` → 10초 뒤 이동 거리(제
  자리 반경 밖으로) → `followStatus()` 남은 시간 → 35초 뒤 자동 해제까지
  전부 실제 숫자로 대조했다(예상과 정확히 일치 — 10초=속도22×10=220
  이동, 20초 남음, 35초 뒤 null). `_test.html`에 진단 3항목(이사 후보가
  하트로 갈리는지 · 동행 중 이동+시간+중복거절+자동해제 · `canFollow`/
  `requestFollow` 하트 7 문턱) 추가.

**실기 확인 남음**: "🚶 함께 걷기" 버튼이 자연스러운 자리(선물 버튼
옆)에서 헷갈리지 않는지, 동행 중 그 사람이 화면에서 실제로 따라오는
게 보이는지(2D 캔버스·3D 둘 다), 30초가 짧거나 긴지, 이사 후보가 하트
기준으로 바뀌면서 체감(누가 먼저 떠나려 하는지)이 원작과 비슷한지.

**남은 것**: §5.4 의 3♥·5♥·10♥ 해제, §5.3 의 조개 길·깃발 문양·3D
SCATTER, §5.5~5.7 전부 — 다음엔 그중 스코프가 작고 명확한 것부터.

## 2026-09-19 — PLAN §5.4 5♥ 고유 대화 구현 + NPC 하트 틈 메움

지난 세션이 "인물 12명(주민 5+NPC 7) × 3줄"이라 콘텐츠 양 때문에 미룬
5♥ 고유 대화를 열어 봤다. 실제로 세어 보니 주민은 12명이 아니라
**105명**(`buildResidents()`가 `global.DG.data.heroes` 전체에서 매번
무작위로 5명을 뽑는다, 세이브마다 다르다) — 12명 몫 콘텐츠로는 애초에
안 맞는 계산이었다.

- **주민 쪽 — 새로 안 쓰고 재사용**: `data.js`의 열전(`BIOS`, "인물
  상세 화면에 뜨는 한 줄")이 이미 실명 없이 다듬어진 "역사 오마주 한
  줄"이었다. `node`로 직접 세어 105/105 전원이 갖고 있는 걸 확인했다
  (procedural genchar 인물은 이 판에 안 나온다 — genchar.js는 saga-go
  전용). `village.js`에 `uniqLineOf(id)` 신설: 하트 5 미만 null, 이상이면
  `'문득 지난 이야기가 떠오르는구려... ' + DG.data.bio(id)`. `talk()`의
  "오늘 할 일 다 끝났을 때" 인사 자리(`req.done`)에서 성격 유형 인사
  (`ty.idle`)보다 이 값을 앞세운다.
- **NPC 쪽 — 하트가 원래 전혀 안 올랐다.** `giveGift`/`nearFolk`가
  `residents` 배열만 보고 `talkNpc()`는 하트를 건드리는 줄이 하나도
  없었다 — "주민 5+NPC 7" 이라던 §5.4 마디표가 실제로는 절반만 맞는
  말이었다(지난 세션도 못 알아챈 틈). 이번에 메웠다: `talkNpc()`에
  주민과 같은 "오늘 첫 대화 +1"(하루 한 번), 부탁 완수 +2, 배달원
  배달 완수 +2(`talkCourier()`). 선물 UI를 NPC까지 넓히는 건 손 안
  댔다(`nearFolk`가 여전히 주민만 본다) — 범위가 더 커져서 다음으로.
  `data-village.js`의 `NPCS` 일곱 항목에 `uniq` 배열(3줄씩, 21줄)을
  새로 썼다 — 전부 원작 없는 창작 인물(숲지기·낚시꾼·상인·탐험가·
  약초꾼·나그네·배달원)이라 실명 금지에 걸리지 않는다. `village.js`에
  `npcLine(npc, def)` 신설: 하트 5 이상이면 `core.hash2(idNum(id), day)`
  로 날짜마다 고정된 하나를 고르고, 아니면 평소 `def.line`. `talkNpc()`
  의 "평소 인사" 반환 두 곳과 `talkCourier()`의 "소포 없음" 반환이
  이걸 쓴다.
- **검증** — `node -c`(village.js·data-village.js) 통과. `data.js`만
  단독으로 `node`에 올려 열전 커버리지 실측(105/105, "빠지는 사람
  없음"을 코드로 확인). `_test.html`에 진단 2항목 추가: 주민이 하트
  4→5로 넘어가면 BIOS 문장이 인사말에 섞여 들어오는지, NPC(낚시꾼으로
  표본)가 하트 4→5로 넘어가면 대사가 `uniq` 셋 중 하나로 갈리고
  대화 한 번에 하트가 +1 오르는지.
  `village.js` 전체는 `core.js`가 브라우저 전역(`addEventListener` 등)에
  기대 `node` vm 스텁으로 못 돌렸다(town·mail·folk 까지 얽혀 동행 때보다
  의존이 크다) — 그래서 이번 로직의 실제 실행 확인은 `_test.html` 진단이
  유일한 자리다. **헤드리스로 그 진단을 돌려 보진 않았다**(서버는 사용자가
  실기 테스트를 요청할 때만 띄운다는 규칙) — 다음에 서버를 띄울 일이
  있으면 `_test.html`의 RESULT 를 함께 확인할 것.

**실기 확인 남음**: 주민 인사말에 열전 문장이 섞였을 때 말투(3인칭
서술체 그대로 붙인 것)가 튀어 보이지 않는지, NPC 셋 대사가 반복 방문
시 자연스럽게 느껴지는지, 대화만으로 NPC 하트가 오르는 속도(하루 +1,
부탁 있으면 +2 더)가 주민보다 너무 느리진 않은지.

**남은 것**: §5.4 의 3♥·10♥ 해제(10♥ 기념품은 가구 세트 하나 얹는
정도라 3♥보다 작을 수 있다), NPC 선물 UI 확장(하려면 `nearFolk`·
`giveGift`·`ui.js` 포커스 카드 셋 다 손대야 한다), §5.5~5.7 전부.

## 2026-09-19 — PLAN §6.1 그래픽: 배우 외곽선 + §10-Q3(사진측량도 툰) 코드분

사용자가 "캐릭터 그래픽이 구리다"고 지적 — Mixamo 실사는 재배포 금지로
막혀 있고(위 Mixamo 조사), 대안은 CC0 팩을 더 뒤지는 것도 아니라 **이미
사가고돗이 검증해 둔 방향**(원신류 카툰/셀셰이딩)을 이 판에 마저 입히는
것으로 결론지었다. `toon3d.js`(2026-09-17)가 재질(밴드형 3단 램프)까지는
이미 해 뒀고, §6.1이 "다음 후보"로 적어 둔 **외곽선**과 §10-Q3(확정만
되고 코드는 안 갔던 "사진측량 탑성도 툰으로") 둘을 닫았다.

- **외곽선(뒤집힌 헐)** — `toon3d.js`에 `addOutline(root, width)`
  신설: root 안의 Mesh/SkinnedMesh 마다 1.045배(기본) 부풀린 BackSide
  전용 복제를 같은 부모에 덧붙인다. SkinnedMesh 는 `dup.bind(o.skeleton,
  o.bindMatrix)`로 같은 skeleton 에 다시 물려 걸을 때 몸과 같이 움직인다.
  새 손잡이 `world3d.outline`(기본 1) — 툰 자체가 꺼지면(`world3d.toon`)
  같이 꺼진다(`OUTLINE_ON()`이 `TOON_ON()`을 먼저 본다). **땅은 여전히
  안 건다** — `toon3d.js` 머리말의 "InstancedMesh 격자 전체가 테두리로
  뒤덮일 위험" 경고는 그대로 유효해서, 배우(사람·짐승)만 골라 건다.
  적용 지점 둘: `asset3d.js`의 `loadHeroRecipe().assemble()`(사람 —
  **주민과 NPC 가 둘 다 `kind==='hero'` 한 경로를 타므로 이 한 곳으로
  충분**)과 `buildGeneric()`의 `kind==='animal'` 분기.
- **§10-Q3 코드분** — `acquire()`가 `/realistic/`(사진측량 PBR) GLB를
  받을 때 지금까지는 `delam()` 자체를 안 태웠다(재질을 안 건드림 = 그대로
  PBR). 새 `toonifyRealistic(root)`를 얹어, **툰이 켜져 있을 때만**
  `MeshStandardMaterial`/`MeshPhysicalMaterial`을 `toonify()`로 덮는다
  — 꺼져 있으면 예전처럼 손 안 댄다(원래 "사진측량은 실사로 남기자"고
  따로 뺐던 취지를 존중). **텍스처의 팔레트 스냅(§6.4)은 이번엔 안 했다**
  — diffuse 맵은 원본 그대로라 색감이 §6.3 판별 팔레트와 안 맞을 수 있다,
  다음 후보로 PLAN §6.2에 적어 뒀다.
- **검증** — `node -c`(toon3d.js·asset3d.js) 통과. `_test.html`에 실제
  THREE(r169, 이 파일이 이미 로드해 둔 라이브러리) 객체로 진단 4항목:
  `OUTLINE_ON()`이 `TOON_ON()`을 따라 꺼지는지 · `addOutline`이 메시를
  2개로 늘리고 BackSide·1.05배인지 · SkinnedMesh 복제도 같은 skeleton
  을 쓰는지 · `world3d.outline`을 끄면 아예 안 붙는지. **렌더된 화면이
  실제로 어떻게 보이는지는 이번에도 확인 못 했다** — 헤드리스·서버는
  사용자가 실기 확인을 요청할 때만 띄운다는 규칙 그대로, 이 진단 넷은
  전부 렌더러 없이 지오메트리·재질 값만 본다.
- **원신과 똑같이는 안 된다고 사용자에게 설명함** — 미호요 IP 재현은
  저작권 문제, 지금 CC0 모델 자체의 디테일(조각·텍스처)은 셰이더로 못
  메운다. 셰이딩 기법(밴드 명암+외곽선)만 흉내 낸 것이라고 기대치를
  맞춰 뒀다.

**실기 확인 남음**: 외곽선 두께(1.045)가 실제 화면에서 너무 두껍거나
얇지 않은지, SkinnedMesh 외곽선이 관절 부위(팔꿈치·무릎)에서 갈라지거나
겹쳐 보이지 않는지, 사진측량 탑성이 툰 재질을 입었을 때 실루엣이 뭉개져
안 보이지 않는지(노멀맵을 버리므로 굴곡이 평평해질 수 있다).

**남은 것**: 텍스처 팔레트 스냅(§6.4, `palette.py`), 짐승 외 다른 배우
(정령 §5.5 등 나중에 생길 것들)에도 같은 외곽선 적용, §5.4 3♥·10♥,
§5.5~5.7 전부.

## 2026-09-19 — PLAN §6.2·§6.4: 사진측량 집 셋을 Kenney 킷배싱으로 교체

이어서 진행하다가 사용자가 "원신급이 나와야해"→"가능하겠어?"로 기대치를
물어, 원신 IP 재현(저작권)·원신 급 완성도(전문 아티스트 인력·예산)는
불가능하고 "셰이딩 스타일만 흉내"까지가 한계라고 설명했다. 사용자가
"지금처럼 말고" → "에셋 자체를 새로 구함"을 골라, 방향을 툰 재질 씌우기
에서 **원본 에셋 자체를 스타일라이즈드로 교체**로 틀었다.

- **받은 것**: Kenney "Fantasy Town Kit"(CC0, `kenney.nl` 직접 zip, 856개
  파일 중 모듈 벽·지붕·굴뚝 10개만 사용). KayKit "Forest Nature Pack"도
  같이 검토했으나(§6.4 다음 후보, 자연물 NAT_REAL 대체용) **itch.io
  다운로드는 클릭 흐름이라 자동화가 까다로워 이번엔 못 받았다** —
  csrf_token으로 `/download_url`까지는 서명 URL을 받아냈지만 그 다음
  `/file/<id>` 트리거가 404 (세션/키 결합 방식을 못 풀었다). 다음에
  이어갈 것.
- **킷배싱**(신설 `tools/asset-forge/kitbash.py`): Kenney 모듈은 1×1
  셀 +X변에 벽 하나(회전 0/90/180/270°로 나머지 세 변)를 두는 관례다.
  RECIPES 셋으로 조립: `house_wood_home`(문+창+지붕+굴뚝),
  `house_wood_cottage`(문+원형창+고딕 지붕, 굴뚝 없이 실루엣 구분),
  `house_stone_museum`(석재 벽+평지붕, "곳간" 느낌). 부품·공유 텍스처는
  `assets/models/buildings/kenney_parts/`, 조립 결과는
  `assets/generated/buildings/`.
- **함정 하나**: Kenney 의 `.glb` 도 텍스처를 임베드 안 하고
  `images[].uri: "Textures/colormap.png"` **외부 상대경로**로 참조한다
  (보통 glb는 다 임베드하는데 이 킷은 예외) — 부품만 복사하고
  `Textures/` 폴더를 안 옮기면 trimesh 가 `baseColorTexture=None`으로
  조용히 실패한다(에러 없이 재질이 통째로 날아간다). `kenney_parts/`
  밑에 `Textures/colormap.png` 를 같이 둬야 한다.
- **팔레트 스냅**(§6.4 첫 실사용): `palette.py`에 `forest_green`
  팔레트를 추가(PLAN §6.3 green 바이옴 base8 그대로, 24색). 조립된
  집 셋에 `snap-glb`를 그 자리에서 덮어썼다 — Kenney 원본 colormap.png
  는 캐릭터 피부색·깃발 등도 같이 담은 공용 아틀라스라 그대로 두면
  세계 색감과 어긋난다. `preview` 로 전/후 텍스처 비교 PNG 한 장 봤다
  (색은 팔레트로 확실히 바뀜, 다만 이 아틀라스는 856개 부품 전체가
  공유해서 이 집 셋이 실제로 쓰는 영역만 보고 판단하긴 어려웠다).
- **배선**: `asset3d.js`에 `BLD_GEN`(`assets/generated/buildings/`)
  신설, `building:home`·`tailor`·`museum` 세 키를 새 GLB로 바꿨다.
  `/realistic/` 경로가 아니므로 `looksRealistic()`을 안 타 보통
  건물처럼 `toon3d.delam()`을 그대로 받는다(§10-Q3 실사 예외 처리가
  필요 없어졌다). 옛 PolyScan 집 셋 파일은 이 판에서만 지웠다
  (`saga-dungeon` 등은 하드링크 별도 사본이라 안 건드림, `saga-forest`
  안에서만 orphan이라 지운 것 — `assets/ASSET_LICENSES.md`에 옛 절은
  "교체됨"으로 남기고 새 절을 추가했다).
- **탑성(`ruinTower`, Renvylle Castle)은 이번 교체에서 뺐다** — 유일한
  대형 유적 자산이라 그대로 두고, §10-Q3(재질만 툰) 상태 그대로다.
- **검증** — `kitbash.py`·`palette.py` 둘 다 `python -m py_compile`
  통과, 조립 결과 bounds 가 NaN/Inf 없이 유한한지 스크립트로 확인,
  텍스처가 3개 GLB 전부(7/6/6 지오메트리)에 실제로 붙었는지 재로드해
  확인. **렌더된 화면은 이번에도 못 봤다**(헤드리스·서버는 사용자
  요청 시에만 — §2-보안·§검증 규칙 그대로) — 실루엣·비례·팔레트가
  실제로 잘 어울리는지는 실기 확인 대기.

**실기 확인 남음**: 집 셋의 실제 렌더(외곽선·툰 램프와 어울리는지,
지붕 크기가 벽 대비 과하거나 작지 않은지, 굴뚝 위치가 지붕을 뚫고
나온 것처럼 자연스러운지), 팔레트 스냅 후 색이 §6.3 green 바이옴과
실제로 잘 섞이는지.

**남은 것**: KayKit Forest Nature Pack 받기(itch.io 자동 다운로드
막힌 부분 재시도 또는 사용자가 직접 받아 전달) → NAT_REAL(바위·나무
사진측량) 교체, 킷배싱 레시피에 캠프 마을 3종·주민 집 외형 3종
추가(§6.4), 짐승 외 다른 배우 외곽선, §5.4 3♥·10♥, §5.5~5.7 전부.

## 2026-09-19 — "원신급" 요청 이어서: 림 라이트(프레넬) 셰이더

건물 교체 보고 뒤에도 "원신급이 나와야해"가 또 나와, 이번엔 다음 레버
셋(①셰이더/조명 ②인물 모델 애니메 비례화 ③환경 연출) 중 어디부터
갈지 물었더니 "1,2,3 다 해줘"로 답함 — 셋을 순서대로 이어가되 하나씩
검증 가능한 만큼 끊어 가기로 했다(3D 는 렌더 확인이 세션에서 원천
불가능해 한 번에 다 밀면 어디가 틀렸는지 못 짚는다).

**①번(셰이더) 첫 조각 — 림 라이트**: `toon3d.js`에 `applyRimLight()` +
`RIM_ON()` 신설. `MeshToonMaterial.onBeforeCompile`로 프레넬 항 하나만
더해(지오메트리 불변) 가장자리가 빛을 받아 살짝 밝아지는 애니메이션
셰이더 인상을 흉내낸다. `toonify()`·`lambertLike()`가 만드는 재질에는
전부 자동으로 걸린다(외곽선과 달리 드로우콜이 안 늘어 땅 타일에도
안전 — PLAN §6.1 그 자리에 적어 둠).

- **셰이더가 실제로 컴파일되는지는 세션에서 확인 불가**(WebGL 컨텍스트
  필요) — 대신 `js/vendor/three.iife.js`(이 판이 실제로 얹는 r169 빌드)
  의 MeshToonMaterial 정점/프래그먼트 템플릿을 직접 열어 내가 쓴 청크
  이름(`common`·`worldpos_vertex`·`dithering_fragment`)이 정확히 그
  자리에 있는지 소스 대조로 확인했다 — `onBeforeCompile`이 청크
  치환 *전* 원문(`#include <x>` 토큰이 그대로 남은 문자열)을 받는다는
  전제도 확인(예제 코드가 늘 이 패턴을 쓰는 것과 일치).
- `node -c` 통과. 색·강도는 고정값(따뜻한 흰 0xfff4d6, 세기 0.35)이라
  손잡이 표로 노출은 안 했다 — 실기로 보고 세기부터 조정할 가능성 큼.

**②③번은 아직 손 안 댐**: 인물 애니메 비례화(VRoid Studio)는 이
세션에서 자동화가 안 되는 GUI 작업이라 사용자 쪽 개입이 필요할
가능성이 크고(itch.io 다운로드와 비슷한 결), 환경 연출(물·하늘·파티클)
은 물 재질을 안 건드리기로 한 기존 결정(§6.1 "반사는 공짜로 얻는다")
과 부딪히는 부분이 있어 범위를 좁혀 다시 짚어야 한다 — 다음 이어갈 때
먼저 이 둘의 실제 범위·제약을 사용자와 맞추고 시작할 것.

**실기 확인 남음**(누적): 림 라이트 번짐 두께·색이 과하지 않은지(특히
땅 타일처럼 넓은 면에 걸렸을 때), 위 §6.2 집 셋 항목 그대로.

## 2026-09-19 — ②는 사용자 개입 필요로 확인, ③환경 레버 셋 구현

②(인물 애니메 비례화)를 "vroid 툴로 직접 해줄수 없어?"로 다시 물어와
확인해 보니 **VRoid Studio는 마우스로 슬라이더 돌리는 Windows GUI 앱이라
CLI·스크립트 인터페이스가 아예 없고, 이 PC에 설치도 안 돼 있다**(`C:\Program
Files` 밑 검색으로 확인). 이 세션엔 마우스/화면을 조작할 도구 자체가 없어
GUI 앱을 못 돌린다 — 설치돼 있었어도 마찬가지였을 것. 사용자에게 이대로
설명하고 답 대기 중(② 는 보류, 다음에 "VRoid 로 직접 만들어 전달" 또는
"무료 완성 모델 검색"인지 정해지면 이어감).

③(환경 연출)은 사용자가 세 갈래(하늘 그라디언트·안개 색 보정, 반딧불이
파티클 확장, 물 재질 재검토) 전부를 골라 그대로 진행했다 — 전부
`village-view3d.js`:

- **하늘 그라디언트**: `makeSkyDome()` 신설. 반지름 380 짜리 뒤집힌 구에
  `ShaderMaterial`(텍스처 없이 수직 두 색 lerp, `normalize(vWorldPos).y`
  기반) 하나만 얹었다 — PLAN §6.1 이 예전부터 적어 둔 "camTiltMix 를
  낮추지 말고 원경 안개·하늘 그라디언트로 지평선을 밝게" 를 실제로
  구현한 것. `syncSky()`가 매 프레임 `skyDome.position`을 카메라로
  옮기고(스카이박스처럼 항상 시야를 둘러싸게), 바이옴/시간대/날씨가
  바뀔 때만 `lighten(c,0.35)`(지평선)·`darken(c,0.55)`(천정)로 색을
  새로 먹인다. 손잡이 `village3d.sky`(기본 1, 안개와 달리 드로우콜을
  안 늘려 기본을 꺼둘 이유가 적다고 판단). `darken()` 짝인 `lighten()`
  헬퍼도 새로 뺐다(순수 함수).
- **반딧불이 반짝임**: 캔버스 방사형 그라디언트를 런타임에 구워(별도
  이미지 파일 없음, `fireflyGlow()`) `PointsMaterial.map`+가산 블렌딩+
  정점색(`vertexColors`)으로 바꿨다 — 딱딱한 점 대신 번지는 빛. 정점색은
  `floatStep(pts, true)`가 매 프레임 0.45~1.0 사이로 sin 변조해 반짝이게
  하는데, 위상을 위아래 흔들림(`0.8·+i`)과 다르게(`1.3·+i*1.7`) 줘 박자가
  안 겹치게 했다. 개수도 늘렸다 — 앰비언트 40→64, 정원 무리 24→36
  (Points 는 드로우콜이 그대로라 정점 수만 늘어나는 값싼 확장으로 봤다).
- **물비늘**: `waterMaterial()`의 재질 자체(PBR+HDRI 반사, "반사는
  공짜로 얻는다")는 그대로 두고 — 이 결정은 여전히 맞다고 판단해 안
  뒤집었다 — 이미 있던 파동용 `onBeforeCompile` 자리에 프래그먼트 항
  하나만 더 얹었다. 월드 xz 를 3×3 격자로 잘라 칸마다 해시 난수를 뽑고,
  문턱(0.985)을 넘는 칸만 시간에 따라 반짝이게 한다(원신류 스타일라이즈드
  물의 "표면 잔별" 인상 흉내). 손잡이 `village3d.waterSparkle`(기본 1).

셋 다 새 varying 이름(`vSparkleXZ`·`vWorldPos`)을 직접 선언해 조건부로만
잡히는 three.js 내장 varying(`vViewPosition` 등)과 안 겹치게 했다 — 림
라이트 때 확인한 것과 같은 요령. `node -c` 통과, vendor
three(`js/vendor/three.iife.js`)의 MeshStandardMaterial(meshphysical)
템플릿에서 `common`·`worldpos_vertex`·`dithering_fragment` 청크가 그대로
있는지도 소스 대조로 확인했다. sw.js VERSION 올림.

**실기 확인 남음**(누적): 위 전부 + 하늘 그라디언트가 안개·바이옴 색과
이질감 없이 이어지는지, 반딧불이 반짝임이 너무 빠르거나(20Hz 항) 눈에
거슬리지 않는지, 물비늘 격자(3칸/m)·반짝임 빈도(1.5%)가 과하거나 부족
하지 않은지 — 셋 다 값 자체가 눈대중이라 조정 여지 큼.

**남은 것**: ② 인물 방향 사용자 답 대기, KayKit Forest Nature Pack,
킷배싱 마을·주민 집 외형 확장(§6.4), §5.4 3♥·10♥, §5.5~5.7 전부.

## 2026-09-19 — ② 인물 애니메 비례: VRoid 아바타 받아서 끝까지 연결

"vroid 설치 되어 있는데..."로 정정받았다 — 아까 `find`가 필터링된 결과를
놓쳐 "설치 안 됨"이라 잘못 말했다(실제로는 `AppData\Local\Programs\VRoidStudio\2.14.0\`
에 있었다, 레지스트리·시작메뉴로 재확인). 그래도 Unity 빌드(CLI 없음)+
이 세션엔 GUI 조작 도구가 없어 "직접 실행"은 여전히 불가라고 정정해
설명했다. 사용자가 "응 해봐 우선은"으로 완성된 무료 팩 검색 쪽을 골라
진행:

- **찾은 것**: VRoid 공식 CC0 샘플 `AvatarSample_A/B/C`. VRoid Hub 자체는
  curl 이 403(Cloudflare, 예전 세션도 `Temp/vroid_dl.html` 남겨 둔 걸
  보니 이미 한 번 막혔던 자리) — GitHub 미러 `madjin/vrm-samples`로
  우회해 `raw.githubusercontent.com` 직접 다운로드로 받았다(세션·계정
  불필요, itch.io 때보다 오히려 쉬웠다).
- **라이선스 확인**: 이 저장소 README 가 `VRoidPreset_A~Z`(조건부·비CC0)
  와 `AvatarSample_A/B/C`(진짜 CC0)를 구분해 둬서, 처음엔 헷갈렸지만
  검색으로 재확인 — AvatarSample 셋만 CC0 맞다.
- **압축**: `pygltflib` 새로 설치(pip, 이 환경 첫 사용). VRM 은 GLB인데도
  텍스처를 `images[].uri: "Textures/..."` 처럼 **외부 참조**가 아니라
  진짜 임베드는 돼 있었지만(Kenney 때와 다른 함정), 28장 중 15장만
  `baseColorTexture`로 실제 쓰이고 나머지(노멀맵·MToon 매트캡·썸네일)는
  plain GLTFLoader 가 안 읽는다는 걸 JSON 대조로 확인 — 쓰는 것만 512px
  로 줄이고 나머지는 8×8 로 비워 15MB→5MB대. bufferView 를 통째로
  다시 쌓는 방식이라(이미지 말고는 원본 그대로) 스킨·메시가 안 다치게
  **220~261개 안 건드린 bufferView 가 바이트째 그대로인지 스크립트
  자체 검증**까지 넣었다 — 전부 OK.
- **뼈 매핑**: `asset3d.js`를 먼저 읽어 보니 `retargetInto()`가 **이미
  있었다**(MPFB 20종용) — 다만 `boneNameMap()`이 이름이 완전히 같은
  것만 잇는 항등 매칭이라 VRM(`J_Bip_*`)엔 하나도 안 걸렸다. 새로
  만들지 않고 그 함수에 `VRM_TO_UAL1_BONES` 보충표(항등으로 이미 잡힌
  건 안 건드림)만 얹어 기존 파이프라인을 그대로 태웠다 — UAL1 원본
  이름을 직접 열어(`pelvis`·`spine_01/02/03`·`clavicle_l`·`upperarm_l`
  등, UE 마네킹 결) 대조했다. `hip:'Hips'` 하드코딩 인자는 현재도 실제
  두 스켈레톤 어느 쪽에도 없는 이름이라 원래 비활성 로직이었다는 것도
  three.js 소스(`vendor/three.iife.js`)를 직접 읽어 확인 — 그대로 안
  건드렸다(잘못 "고치면" MPFB 쪽 동작이 바뀔 위험).
- **재질**: VRM 이 `KHR_materials_unlit`로 나가 `delam()`(Standard/Physical
  만 봄)이 못 잡는다는 것도 실제 JSON 대조로 미리 확인하고, `delam()`
  자체는 안 건드린 채(다른 700여 GLB에 영향 없게) `toonifyAnime()`을
  새로 뺐다.
- **배선**: `HERO_RECIPES_ANIME`+`wantsAnimeAvatar()`
  (`world3d.animeAvatar`, 기본 꺼짐) — `HERO_RECIPES_MIXAMO`와 같은
  안전장치. 기본 꺼짐이라 지금은 화면에 아무 영향이 없다(MPFB 처음
  들어왔을 때와 같은 상태).
- **검증**: `node -c`, `_test.html`에 진단 3항목(VRM 보충 매칭 4개 나옴·
  기존 항등 매칭은 VRM 표 간섭 없음·손잡이 기본 꺼짐+레시피 3개 경로
  확인) 추가, 노드에 파일을 직접 `require`해 `boneNameMap()` 결과값도
  한 번 더 대조. **렌더 자체(실제로 걷는지, 비례·재질이 어울리는지)는
  여전히 확인 못 했다** — 손잡이가 꺼져 있어 지금은 위험도 없다.

**실기 확인 남음**(새로 추가): `world3d.animeAvatar`를 켜고 `anime_avatar_a/b/c`
가 실제로 서서 걷는지, 팔다리가 안 뒤틀리는지, 툰+외곽선+림 라이트와
비례·재질이 어울리는지 — 전부 미확인.

**남은 것**: 위 실기 확인 뒤 주민 실제 배정 여부 결정, KayKit Forest
Nature Pack, 킷배싱 마을·주민 집 외형 확장(§6.4), §5.4 3♥·10♥, §5.5~5.7 전부.

## 2026-09-19 — "GUI 조작 도구 설치해서 해줘": PowerShell로 VRoid 직접 조작

사용자가 GUI 자동화를 직접 요청. 별도 설치 없이 PowerShell 자체 기능
(`System.Drawing`으로 스크린샷, `user32.dll` P/Invoke `SetCursorPos`/
`mouse_event`로 클릭)으로 스크린샷→확인→클릭 루프를 짜서 VRoid Studio
2.14.0(이미 설치돼 있었음)을 실제로 조작해 캐릭터를 하나 새로 빚었다.

- **중간에 발견한 것 — 동시 작업 충돌**: 자동화 도중 클릭 좌표가 어긋나며
  **saga-godot 세션의 VS Code 창**(터미널에 "vroid_face_bake.py", "얼굴을
  한 장으로 굽기" 논의 중)이 화면에 잡혔다 — 그 세션도 같은 시점에 VRoid
  관련 작업(3D 얼굴 렌더 굽기 스크립트)을 하고 있었다는 뜻. 같은 데스크톱을
  두 세션이 동시에 쓰다 보니 마우스·키보드·창 포커스가 충돌할 수 있다고
  사용자에게 알렸고, "매 클릭마다 VRoid 창을 재확인하며 조심스럽게 계속"
  으로 답 받아 이후 모든 클릭 전에 `SetForegroundWindow`+스크린샷 재확인을
  넣었다. 실제 피해는 없었다(그 세션 터미널에 입력이 안 들어감을 확인).
- **진행 순서**: 새로 만들기 → 여성 베이스 → 얼굴 세트 변경 → 헤어 세트
  변경(긴 갈색머리) → 전신 세트(하와이안 셔츠+데님 반바지) → "다른 이름
  으로 저장"(.vroid, **이 단계를 거쳐야** 상단 공유 아이콘에 "VRM 내보내기"
  가 나타난다 — 저장 전엔 계정/로그인 메뉴만 보였다) → VRM 내보내기
  (VRM0.0, 상업 이용 허가 체크, 라이선스 CC BY) → `saga_forest_avatar_01.vrm`
  (17.7MB) 실제 생성 확인.
- **VRoid 는 Unity 빌드라 버튼·슬라이더가 캔버스 그림**(네이티브 컨트롤
  아님) — 이름으로 못 찾고 좌표로만 클릭 가능. 창 크기·위치가 조작 중
  바뀌기도 해서 매번 최신 스크린샷의 좌표를 써야 했다.
- **가공**: 기존 `vrm_shrink.py`(스크래치패드, pygltflib) 그대로 재사용 —
  17.7MB→9.0MB. 이미지는 803KB뿐이라 나머지는 메시 자체 무게(VRoid 폴리곤
  감량 슬라이더는 이번엔 안 씀, §6.5 다음 후보).
- **배선**: `HERO_RECIPES_ANIME`에 `anime_avatar_custom01` 추가(같은
  `world3d.animeAvatar` 손잡이, 기본 꺼짐 — 기존 주민에 영향 없음).
  `node -c` 통과.

**실기 확인 남음**(추가): 커스텀 아바타도 위 샘플 셋과 같은 항목 전부
미확인 — 손잡이를 켜고 한 번에 몰아 볼 것.

**남은 것**: 위와 동일. 추가로 — VRoid 폴리곤/재질/뼈대 감량 슬라이더를
써서 다음 아바타는 처음부터 더 가볍게 뽑는 것도 고려.

## 2026-09-19 — 초상 3D↔2D 깜빡임 버그 수정(다섯 판 공통, 상세는 saga-go HANDOFF)

제보: "캐릭터 인물 사진들이 2D 스프라이트랑 겹쳐서 깜빡인다"(화면 전체 공통).
`js/ui.js`의 `pt()`가 이미 3D 초상을 구워 둔 인물도 매번 캔버스 2D 그림으로
`<img>`를 새로 시작하고 40ms 뒤 `sweep()`이 갈아 끼우는 구조라, 같은 인물을
반복해서 열 때마다 2D→3D 로 깜빡였다. `pt()`와 도감 상세 두 자리(`dt-portrait`,
hero/pet 150×172)에 `p3src()`를 추가해 캐시가 있으면 처음부터 그 그림으로
시작하게 고쳤다. `node -c js/ui.js` 통과. 실기 확인 대기.

## 2026-09-19 — 애니메 아바타 "마을 배경과 이질감" 제보 → 숲 NPC 7명 축소 + 옷 팔레트 물들임

§6.5 "다음(실기 확인 뒤 결정)"에 대한 사용자 답: "마을 배경이랑 이질감 있는
게 제일 커". 재질(툰 램프·외곽선·림 라이트)은 이미 배경과 같은 함수를
타는데도 이질감이 크다면 원인은 셰이더가 아니라 **디테일·채도 수준
자체**라고 판단 — 정교한 VRoid 인물이 저폴리+절제된 팔레트 마을 옆에
서면 셰이더를 맞춰도 튄다.

**결정 둘 다 적용**(사용자 "둘다해"):
1. **범위 축소** — 전체 105명이 아니라 이름 있는 숲 NPC 7명(`data-village.js`
   `NPCS`: keeper·angler·merchant·explorer·herbalist·wanderer·courier)에만
   건다. `asset3d.js` `buildHero()`가 `oneOf(HERO_RECIPES_ANIME, ref)`(전체
   해시 배정)를 쓰던 자리를 새 `HERO_RECIPES_ANIME_NPC[ref.id]`(정확히
   이 7개 id만 매칭) 로 바꿨다. 표에 없으면(마을 주민) 그냥 기존
   `buildHeroDefault`(QRPG 저폴리)로 빠진다.
2. **옷 색 팔레트 매칭** — `tools/asset-forge/palette.py`에 `tint-glb`/
   `tint-preview`/`_material_matches`(신규)를 추가했다. 처음엔 기존
   `snap-glb`(최근접 색 스냅)로 시도했는데 `preview` 비교 PNG로 보니
   부드럽게 음영진 천이 얼룩덜룩한 패치워크로 뭉개졌다(스크린샷 금지
   규칙과 무관한 평면 이미지 비교 — palette.py 자체 기능). 그래서
   **명도(V)는 원본 유지, 색상·채도(H·S)만 팔레트 색으로 바꾸는**
   `tint_image()`를 새로 짰다 — 원단 그라디언트가 안 깨지고 색만 바뀐다.
   `--only-suffix CLOTH`로 VRoid material.name 의 `_CLOTH` 토큰만 골라
   옷(Tops/Bottoms/Shoes)만 물들이고 얼굴·피부·머리는 원본 그대로 뒀다.
   - **주의**: `avatar_custom_01.glb`(GUI 자동화로 병합한 파일)는 material
     이름 끝에 `(Instance)`가 더 붙어(`..._CLOTH (Instance)`) 단순
     `endswith` 매칭으로는 하나도 안 걸렸다 — `_material_matches()`를
     "끝 문자열 일치"에서 "`_<접미사>` 토큰 포함"으로 바꿔 고쳤다.
   - NPC별 배정(몸/팔레트 role/채도 배율)은 `assets/ASSET_LICENSES.md`
     "옷 색 물들인 숲 NPC 전용 애니메 아바타" 절 표 참고. 출력은
     `assets/generated/people/anime/npc_<id>.glb`(7개, 커밋 대상).

**검증**: `node -c js/asset3d.js`·`tools/asset-forge/palette.py` 구문 통과.
`_test.html` 헤드리스(file:// 단독판, 서버 안 띄움) 304/306 — 새로 고친
애니메 아바타 테스트 2건(레시피 4개 검사 수정 + NPC 7명 매칭 신규) 통과.
남은 실패 2건(채집·마무리카드)은 이 변경 전 기준선(302/305, `git stash`로
확인)에서도 이미 실패하던 것이라 무관 — 이번 작업 범위 밖으로 둔다.

**실기 확인 전**: 옷 색이 실제 화면에서 배경과 어울리는지, 채도 배율이
적당한지는 렌더가 있어야 판단 가능.

## 2026-09-19 (이어서) — §6.4 "주민 집 외형 3종" 완료 + 옛 PLAN 기록 정정

"이어해"로 §6.4 다음 후보 두 개(캠프 소품 3종·주민 집 외형 3종) 중
사용자가 "둘 다(집 먼저, 캠프 이어서)"로 답해 시작. **착수 전 확인하다
캠프 쪽이 이미 완료돼 있는 걸 발견** — `village.js` `buildProps()`에
hamletSpot·hamlet2Spot·ruinSpot 세 자리 모두 천막·모닥불·벤치·우물·등롱이
이미 배치돼 있었다(첫 캠프 기준 천막 2+모닥불+벤치 2+우물+등롱 2). PLAN.md
§6.4·이 문서 옛 기록이 "미착수"로 잘못 적혀 있었던 것 — 옛 세션이 코드는
앞서가고 문서를 안 갱신했다. PLAN.md·ASSET_LICENSES.md 양쪽에 바로잡는
절을 남겼다. (참고: 그 소품들 자체는 여전히 옛 팩 그대로라 킷배싱 대상은
아니다 — 그건 남겨 둔 채로.)

그래서 실제로 한 일은 **주민 집 외형 3종**뿐이다 — 첫 캠프(hamletSpot)의
집 세 채(hamletHouse·hamletHut·hamletShed, Quaternius `House_1~3.glb`)를
이 판 자체 킷배싱 조립으로 갈아 끼웠다(home·tailor·museum과 같은 계열로
통일). 벽 배치는 `house_wood_home`과 완전히 같고, **지붕 색만** 셋을
가른다.

**함정 하나 겪음** — 조립된 house_wood_home.glb를 열어 보니 부품 7개가
전부 같은 `colormap` 재질 하나로 합쳐져 있었다(재질 내용이 같으면
트리메시/glTF 내보내기가 자동으로 하나로 합친다). 그래서 "재질 이름으로
지붕만 골라 물들인다"는 처음 접근(VRoid 옷에 썼던 `_material_matches`)이
안 먹혔다 — 부품마다 재질 이름이 다 `colormap`이라 구분이 안 됐다.
**해결**: (1) 지붕 부품(`roof-gable.glb`)을 조립 **전에** 미리
`tint-glb`로 물들여 벽과 텍스처 내용 자체를 다르게 만들어 둔다(그러면
조립해도 재질이 안 합쳐진다) → (2) 조립 뒤 벽·굴뚝만 forest_green으로
마저 스냅할 때는 재질 이름이 아니라 **씬 그래프 노드 이름**(kitbash.py가
부품 파일명으로 붙인다, 예: `roof-gable-stem_5`)으로 지붕을 가린다 —
`palette.py`에 `snap-glb --exclude-node-prefix roof` 새로 추가.

**만든 것**:
- `tools/asset-forge/palette.py` — `_node_excluded()`·`snap_glb()`의
  `exclude_node_prefix` 인자·CLI `--exclude-node-prefix` 추가.
- `tools/asset-forge/kitbash.py` — `house_camp_a/b/c` 레시피 3개(지붕만
  다름: stem 갈색·stone 회색(채도 0.6)·accent 산호빛(채도 0.75)).
- 지붕 물들인 부품 3개: `assets/models/buildings/kenney_parts/
  roof-gable-{stem,stone,accent}.glb`(파이프라인 중간 산출물, 원본 옆에 둠).
- 최종 GLB 3개: `assets/generated/buildings/house_camp_{a,b,c}.glb`.
- `asset3d.js` — `building:hamletHouse/hamletHut/hamletShed`를 위 세
  파일로 갈아 끼움. `hamlet2House`(House_4, 두 번째 캠프)는 이번 범위 밖.

**검증**: `node -c js/asset3d.js`·`tools/asset-forge/{palette,kitbash}.py`
구문 통과. 조립 직후(스냅 전) 지붕 재질 id가 벽과 다른지 trimesh로 직접
확인, 최종 GLB에서 지붕/벽 텍스처를 각각 이미지로 뽑아 눈으로 대조(스냅
전후처럼 평면 이미지 비교 — 3D 스크린샷 금지 규칙과 무관). `_test.html`
헤드리스(file://, 서버 안 띄움) 304/306 — 기존에도 있던 무관한 실패
2건(채집·마무리카드) 제외 전부 통과, 새 회귀 없음.

**실기 확인 전**: 세 지붕 색이 실제 화면에서 forest_green 벽·툰 외곽선과
어울리는지, 서로 충분히 구분되는지는 렌더가 있어야 판단 가능.

## 2026-09-20 — 자체 몸짓 `js/anim-own.js` (VRM 몸이 Mixamo·UAL1 없이 움직인다, 다섯 판 공용 복사본)

사용자 "mixamo 가 깃에 배포가 불가하니 VRM 캐릭터에 애니메이션으로 재제작" → "자체 애니메이션을 제작해줘"
(범위: **사가웹만**, 이동 5종 + 전투 4종). (이 판이 기준 구현 — 다른 넷이 복사)

**무엇**: `anim-own.js`(다섯 판 md5 동일 — 고치면 다섯 벌 함께) 가 VRM 휴머노이드(`J_Bip_*`) 몸짓 아홉을 **코드로 직접 짠다**:
Idle_Loop · Walk_Loop · Run_Loop · Jump · Land · Attack · Hit · Death · Dodge. 남의 데이터 없음(전부 수식·키).
자세는 뼈마다 (롤·피치·요)를 **정지 자세에서 뽑은 몸 축(위·앞·왼)** 기준 도(°)로 적고 뼈 로컬 회전으로 바꿔 30/60fps 로 굽는다.
발이 땅에 붙는 클립은 FK 로 발바닥 높이를 재서 엉덩이 높이를 자동으로 맞춘다(발이 뜨거나 박히지 않는다).
모든 클립이 같은 뼈 전부에 트랙을 갖는다(크로스페이드 때 앞 클립 자세가 안 남게).

**연결**(각 판 `asset3d.js`): `wantsOwnAnim(url)` — 경로에 `/people/anime/` 가 든 VRM 몸만, 손잡이 `world3d.ownAnim`(기본 1)이 0 이면 예전 길
(UAL1 retarget)로 되돌아간다. 자체 몸짓이면 UAL1(7MB)을 **받으러 가지도 않는다**. 몸이 조립된 장면에서 실제 뼈를 읽어 그 몸에 맞춰 굽고
(`parts.body.ownClips` 캐시), 못 만들면 그때 UAL1 을 받아 예전 길로 간다. `mapClips` 에 슬롯 `jump`·`land` 추가(다른 슬롯은 그대로).
`index.html`·`_test.html`·`_demo.html`·`sw.js` 에 스크립트 추가, sw VERSION 올림.

**검증(렌더 없이 FK 로 잰 것 — 눈으로는 못 봤다)**: 표준 VRoid 뼈대 + 아바타 4종 실제 GLB 뼈대 5개에서 — 아홉 클립 값 유한·단위 쿼터니언,
루프 셋 이음매 0, 걷기 보폭 0.6m·왼발 앞일 때 오른팔 앞, 접지 오차 <1cm, 공격 순간 오른손 0.58m 앞·예비 동작 머리 위, 쓰러진 뒤 머리 높이 ~0.2m·몸통 수평,
구르기 한 바퀴 뒤 서 있는 높이 복귀, 어느 관절도 땅 아래 4.5cm 넘게 안 박힘, 각속도 <3200°/s. 실제 three 뼈대에 `AnimationMixer` 로 돌린 왼발 위치가
FK 계산과 <1mm. 각 판 `_test.html` 에 항목 6개씩(`anim-own — …`). 시험은 **jsdom(node)** 으로 돌렸다(크롬·서버 안 띄움) — WebGL·캔버스는 스텁.
결과: 사가국지 156/156, 사가의숲 311/312, 사가고 505/507, 사가블로 353/355, 사가스토리 188/192 — **남은 실패는 전부 변경 전 HEAD 에서도 똑같이 난다**
(사가의숲 "깃발 그림"·사가고 "배우 조립·걸음"(jsdom 에 `Request` 없음)·사가블로 미니맵 둘·사가스토리 §5-1 궁수/§5-4 관문 대장 등 넷 —
**사가스토리 넷은 jsdom 한계인지 진짜 버그인지 아직 모른다**, 실크롬 `_test.html` 로 확인할 것).

**모른다(실기 몫)**: 실제 화면에서 자연스러운지(관절 각도·발 미끄러짐·구르기 궤적). 진폭은 `anim-own.js` 의 클립 함수(`walkPose` 등)에 숫자로 있다 —
보고 어색하면 그 숫자를 고치면 된다. 발 미끄러짐은 이동 속도와 보폭이 안 맞으면 생긴다(게임 쪽 걷기 속도에 맞춰 `Walk_Loop` 의 `dur`(1.0s)·`26`°를 조정).
**손 안 댄 것**: Mixamo 관련 옛 코드(`HERO_RECIPES_MIXAMO`·`world3d.mixamoReal`, 파일은 .gitignore)는 그대로 — 지워도 공개판 동작은 같다.
Godot·Unity 트랙은 이번 범위 밖. 새 인물 VRM 을 넣을 땐 경로에 `/people/anime/` 가 있어야 자체 몸짓 길을 탄다.

## 2026-09-20 — 인물 전원 VRoid 몸 + 인물별 색 변형 `js/vroid-variant.js` (다섯 판 공용 복사본, md5 동일)

사용자 "104명은 다 바꾸라고 했는데" → (a) 각자 새로 제작 + (b) 기존 몸 4개 배정·색 변형. **이 절은 (b)** — (a)(인물마다 다른 몸을 VRoid Studio 로 한 명씩 제작)는 코드로 못 하고 아직 시작 안 했다.
(인물 수는 104가 아니라 **105**다 — `DG.data.heroes.length`.)

**무엇**: `world3d.animeAvatar` 손잡이를 **기본 켜짐(1)** 으로 바꿨다(끄면 0). 그러면 인물이 `avatar_sample_a·b·c`·`avatar_custom_01` 넷 중 하나를
id 해시로 받는다(배정 코드는 이미 있었고 기본 꺼짐이었을 뿐). 넷만으로는 백여 명이 똑같아 보여서 `vroid-variant.js` 가 조립된 몸의 **머리(`_HAIR`)·옷(`_CLOTH`: 윗도리·아랫도리·신을 칸을 비켜 다르게)·눈동자(`EyeIris`)**
재질만 복제해 12×12×12 색 칸 중 인물 id 해시 칸으로 곱한다. 피부·얼굴·원본 재질은 안 바뀐다(재질을 복제하고 uuid+칸으로 캐시). `asset3d.js` 에 `applyVroid(model, rec, id)` 한 줄 배선.

**사가의숲만 다른 점**: 09-19 에 "이질감"으로 NPC 7명에만 걸었던 것을 되돌렸다 — 표(`HERO_RECIPES_ANIME_NPC`)에 없는 주민도 `oneOf(HERO_RECIPES_ANIME, ref)` 로 VRoid 몸 넷에 나눠 입히고 색만 바꾼다(NPC 7명은 이미 숲 팔레트로 물들여 둔 GLB 라 변형을 안 얹는다). 마을 배경과 어울리는지는 실기 확인 몫이고, 튀면 `buildHero` 의 그 한 줄만 지우면 NPC 7명으로 돌아간다. 옛 진단 "기본 꺼짐"은 "기본 켜짐"으로 바꿨다.

**검증(렌더 없이)**: 진단 311/312 → 314/315(새 항목 3 — 인물 전원 칸 결정성·서로 다른 조합 ≥90%, 재질 분리·원본 불변·캐시·두 번 입혀도 원본 기준, 경로 판정·손잡이 기본 켜짐/끄기). 남은 실패 하나("깃발 그림", 캔버스 스텁)는 변경 전 HEAD 와 같다. jsdom(node)으로 돌렸다(크롬·서버 없음).
**모른다(실기 몫)**: ① **색이 실제로 어떻게 보이는지** — 색은 텍스처 × `material.color` 곱이라 원본이 어두우면(검은 머리 등) 색이 덜 산다. 팔레트(`vroid-variant.js` `palette(채도, 명도, 밝기배수)` 세 줄)를 보고 조정.
② 기본 켜짐이라 처음 인물이 뜰 때 VRoid 몸을 받는다(몸당 5~9MB, 네 개). ③ 인물 역할·성별과는 무관하게 해시로만 배정한다(데이터에 성별 칸이 없다) — 여성형 몸이 장군에 배정될 수 있다.
**손잡이**: `world3d.animeAvatar` 0 → 예전(QRPG 등)으로 즉시 복귀. `sw.js` VERSION 올림.

## 2026-09-20 (이어서) — VRM 정면 -Z 보정 `faceFront` (상세는 saga-go HANDOFF 같은 날짜 절)

VRM 0.x 몸은 정면이 -Z 인데 배우는 +Z 가 앞이라는 가정으로 돌려 세워서 이동 반대·등 돌림·초상 뒷모습이 났다. `js/vroid-variant.js`(다섯 판 md5 동일)에 `faceFront` 를 더하고 이 판 `asset3d.js` 에서 부른다. 진단 1개 추가, `sw.js` VERSION 올림. **화면은 직접 못 봤다** — 실기에서 방향이 바로 섰는지 확인 필요.

## 2026-09-21 — PLAN §5.3 마을 번들 마무리 — 강가 조개 길 · 깃발 문양 · 3D 소품 표 (코드분 완료)

"사가웹 이어해" → 사가고 §5 ①~⑧ 이 다 끝나 이어갈 판·항목을 사용자에게 물었고 **사가의숲 5.3** 으로 정해졌다. 열어 보니 5.3 은 2026-09-17~18 에 이미 시설 넷·사고 시트·평가 상한·반딧불이 파티클까지 있었고, PLAN 에 "안 한 것"으로 남은 시각 세 조각만 비어 있었다(5.4 관계 하트·5.8② 별 5 도 이미 끝). 그 셋을 채웠다.

- **강가 조개 길**(`village.js`): 조개 갈래를 다 채우면 강 동쪽 기슭(호수 y-4~y+1 여섯 줄, 물 바로 동쪽 두 칸)이 **잔디일 때만** 모래가 된다. 세이브에 칸을 쓰지 않는다 — `computeShellPath()` 가 `buildProps()` 맨 앞에서 (조개 갈래 완결 · 강 자리)로 표(`shellPath`)를 다시 세우고, `tileAt()` 은 옛 몸체를 `tileBase()` 로 내리고 그 위에 얹은 얇은 겉옷이다(잔디 칸·사람이 고치지 않은 칸만 모래로, 물·길·공사한 땅은 그대로). 표가 비면(완결 전) 예전과 한 글자도 안 다르다 — 진단이 -12..40 격자 전체로 비교한다. 잔디 칸이 아니게 돼서 숲 고리 식생 스캐터도 그 자리를 자동으로 비운다. 조개 소품 둘(`bundle_shell0/1`)은 그 위에 선다.
- **3D 지면 캐시 무효화**: `village-view3d.js` `syncTerrain()` 은 인물이 안 움직이면 지면을 다시 안 세운다(성능 감사 때 넣은 캐시) — 그러면 제자리에서 땅만 바뀌면(사고 기증으로 조개 길이 깔리는 순간, **그리고 기존 `terrain.js` 공사도**) 다음 걸음까지 옛 땅이 보인다. `buildProps()` 가 `village:terrain` 이벤트를 쏘고 3D 가 캐시 키를 비운다. **터레인 공사의 같은 잠복 결함도 이걸로 같이 닫혔을 수 있다** — 실기에서 확인 필요.
- **깃발 문양 "사고비"**(`data-village.js` `FLAG_SYMS`·`village-view.js` `drawFlagSym`·`town.js`·`ui.js`): 윗머리 둥근 비석 + 글줄 셋. `gate:'bundles'` — 네 갈래를 다 채워야 `setFlag('sym','stele')` 가 열린다(`town.symLocked()`; 잠김 문구 "🔒 사고 네 갈래를…"). 마을 시트 깃발 고르기에서는 잠긴 문양이 🔒 로 비활성. 어드민은 `setFlag(part,key,true)` 로 잠금을 넘는다(QA).
- **3D 소품 표**(`village-view3d.js`): `SCATTER_KIND`/`SCATTER_H` 에 `stele: 'rock:moss'(1.3m)`·`fireflyplot: 'bush'(0.9m)` — 두 시설은 지금까지 2D 폴백 이모지로만 보이고 **3D 에는 아예 안 섰다**. 새 GLB 없이 기존 키를 빌렸다(석비 = 이끼 선돌, 정원 = 덤불, 밤엔 `fireflyBoost` 파티클이 위에 얹힌다). 진단용 `scatterOf(kind)` 노출.
- **바꾼 곳**: `village.js`·`village-view3d.js`·`village-view.js`·`data-village.js`·`town.js`·`ui.js`·`admin.js`·`_test.html`·`sw.js`(`village-v0.84.0`)·`PLAN.md`(5.3 "안 한 것" 줄·로드맵 2행 상태).

**확인**: `node -c` 통과. jsdom 러너 **322/323**(세 번 한 줄도 안 다름). HEAD 는 **318/319** — 새 진단 4건(조개 길: 완결 전 null·완결 뒤 모래·물 칸 0·조개 소품이 모래 위 / 물·고친 칸 그대로·비우면 격자 전체 복원 / 깃발 잠김·어드민 force·완결 시 열림·그림 / 3D 표에 석비·정원) 전부 통과, 실패 1건("깃발을 그림으로 뽑아 준다")은 HEAD 와 같은 jsdom 캔버스 한계. **화면은 못 봤다.**

**실기 확인 대기**: 조개 길 모래가 강둑·물가 식생과 어울리는지(폭 2·길이 6 이 적당한지) · 석비(이끼 선돌)·정원(덤불)이 시설처럼 보이는지, 눈에 안 띄면 새 GLB 를 들일지 · 사고 기증 직후 제자리에서 모래가 바로 깔리는지(3D 캐시 무효화) · `terrain.js` 공사도 제자리에서 바로 갱신되는지 · 깃발 사고비 문양 모양.
**열린 것**: PLAN 로드맵 다음 = Phase 3 세계(5.5 발견 밀도 격자·정령 60 · 5.7 택배 사슬) → 4 달력(5.6 축제) → 5 그래픽 통일.

## 2026-09-21 (이어서) — PLAN §5.5 ② 숲의 정령 60 (코드분 완료, ① 격자 규칙은 안 함)

5.3 마무리(6710ac1e) 커밋·푸시 뒤 로드맵의 다음 항목. 5.5 는 ① 격자 규칙(사건 6종)과 ② 정령 60 두 덩어리인데 **② 만** 지었다.

**CREATE `js/spirit.js`**(+ `village.js` 얇은 훅 여섯 곳): 숲 고리에 정령의 터 60곳이 씨앗 해시로 늘 같은 자리에 선다. `village.ringSpotOk()`(마을 안·캠프·동굴·폐허·우주기지·물·다리·**숲 채집 노드('gn…') 둘레 세 칸** 제외, 자연 지형 기준이라 세이브와 무관) 위에서 해시로 뽑고 터끼리 6칸 이상 띄운다. 터 둘레 2칸은 숲 고리 사물이 안 선다(`blocked()`). 안 푼 터 = `spiritmark`(손이 닿음, `interact()` 분기 한 줄), 푼 터 = `spiritdone`(deco 자국). 어느 시드에서든 60 이 나온다(시드 5개 확인) — 다만 **바이옴 분포는 시드마다 들쭉날쭉**(예: 초록 숲 3~18)이라 PLAN 의 "바이옴별 12" 대신 총 60 을 고리 전체에 흩뿌리고 도감에서 바이옴별로 묶는다.
- **수수께끼 다섯 — PLAN 원안에서 둘을 바꿨다**: 원안의 "낚시터 반짝"·"나무 흔들기 3연속" 은 이 판 **숲 고리에 낚시터·흔들 나무가 없다**(고리 사물은 전부 `deco:true`, 마을 안에만 있다). 대신 **숨죽임**(🐾 살금살금 걸음으로 터 둘레 3.5칸에 8초 가만히 — `village.update` 에서 `spirit.tick`, 0.25초마다 훑고 움직이면 리셋)·**반딧불**(터 둘레에서 곤충 잡기 — `village:bug` 이벤트 구독, bug.js 는 이 파일을 모른다). 나머지 셋은 원안대로: **돌무리**(터에서 손을 쓰면 광석 1 소모) · **꽃 원**(터 둘레 3.5칸에 꽃 5송이 심기, **초록 숲 터에만** 걸린다 — `canPlantHere()` 가 `'grass'` 타일에서만 심게 해서 다른 바이옴 땅엔 못 심는다) · **별 자리**(별똥별 밤에 터에서 손을 쓰면 `town.wish()`).
- **씨앗 보상**: 단계 6·12·20·30·45·60(`TIERS`, 단조 증가) — 금·별조각(`stardust`)·공적. PLAN 의 "가방 칸 +2·전방 상품 +1" 은 이 판에 가방 용량·상품 확장 체계가 없어 **금·별조각·공적으로 갈음**했다(사용자 결정 여지). 단계마다 한 번만(`claimed`).
- **UI**: 마을 시트에 "숲의 정령" 칸(만난 수·씨앗·바이옴별 진척·다음 보상). 새 효과음 `spirit`(맑은 네 음, `village:spirit` 이벤트). 3D 는 `spiritmark→lantern`·`spiritdone→flower` 기존 키를 빌렸다(정령 GLB 는 안 만듦).
- **세이브**: `s.spirits={found:{터번호:날짜}, seeds, claimed:[…]}`(없으면 초기값, 읽기는 세이브를 안 만든다 — 진단이 본다). `sw.js` `village-v0.85.0`, `index.html`·`_test.html`·`sw.js` 에 `spirit.js` 등록.
- **바꾼 곳**: `village.js`(`ringSpotOk`·`buildProps` 훅 둘·`interact` 분기·`update` 훅·export)·`data-village.js`(PROPS 둘)·`village-view3d.js`(SCATTER 둘)·`sfx.js`·`ui.js`·`_test.html`·`PLAN.md`(5.5 구현 결정·로드맵 3행).

**확인**: `node -c` 통과. jsdom 러너 **331/332**(세 번 동일; 직전 322/323 에 새 진단 9건 — 60곳 재현·간격·마을/캠프/물/고정 자리와 겹침 0·살피기 세이브 불변 / 수수께끼 갈래·꽃 원은 초록만·힌트 / 돌무리 / 꽃 원 / 별 자리 / 반딧불 / 숨죽임 / 씨앗 단계 단조·한 번만 / deco 자국·도감 합), 실패 1건은 HEAD 와 같은 jsdom 캔버스 한계("깃발을 그림으로 뽑아 준다"). 진단을 짜다 두 번 헛디뎠다 — 새 헬퍼 `standAt` 이 기존 낚시 진단의 같은 이름을 덮어써 다섯 건이 깨졌고(`standNear` 로 개명), `_setStar` 의 복원값은 `null` 이 아니라 `undefined` 여야 한다(기존 별똥별 진단이 깨졌다). **화면은 못 봤다.**

**실기 확인 대기**: 터 표지(횃불 모양)가 숲에서 눈에 띄는지·60곳이 걸어서 찾을 만한 밀도인지(고리 반경 최대 ~58칸) · 손을 쓰면 나오는 힌트 문구가 알아듣기 쉬운지 · 숨죽임 8초·꽃 원 5송이·반딧불(잠자리채가 있어야 한다)이 적당한지 · 꽃 원이 초록 숲 3~18곳뿐인 게 부족한지 · 씨앗 보상 금액이 순무 장·전방 벌이에 견줘 어떤지.
**열린 것**: 5.5 ① 격자 규칙(사건 6종을 20타일 격자마다 보장) · 나그네 힌트 판매·전체지도 힌트 원 · 5.7 택배 사슬 → 4 달력(5.6 축제).
