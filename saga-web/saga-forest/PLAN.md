# saga-forest (사가의숲) PLAN — 정본

## 0. 읽는 법

- 이 파일은 **설계 정본**이다. 크기 상한 70KB(`../../SAGA-DESIGN.md` §9). 목차는 `grep -n "^## \|^### "`, 필요한 절만 `sed -n`.
- **날짜 달린 세션 기록은 여기 쓰지 않는다.** 그건 `HANDOFF.md`(이 폴더, append-only, grep 으로만). 2026-09-16 재편 때 옛 PLAN §34-1·§42~§46-5(653행)를 그대로 옮겼다 — 옛 절 번호는 거기서만 유효하다.
- 현재 상태는 `README.md` 머리 절(덮어쓰기, ≤15KB 목표). 공통 규칙은 루트 `CLAUDE.md`, 공통 개편 설계는 `../../SAGA-DESIGN.md`(재미 표준 8 = §3, 참고 게임 카탈로그 = §4, 그래픽 = §6, 에셋 = §7, 버그 = §8).
- 코드는 grep 으로만 읽는다. `village.js` 96KB·`village-view.js` 106KB·`ui.js` 92KB·`village-view3d.js` 88KB·`sprite.js` 91KB·`_test.html` 178KB.

## 1. 정체성

- **한 줄**: 역사 인물(가명) 105명이 **주민**으로 사는 마을에서 걷고, 모으고, 나누는 생활 게임. 마을 밖으로 숲·호수·동굴·폐허·우주기지까지 끊김 없이 걸어 나가는 작은 오픈월드.
- **원작 오마주**: 동물의숲(투영·하루·계절·주민·박물관·순무 장). 원작에서 가져올 큰 축은 2026-08-26 에 전부 끝났다(README "다음에 채울 것"). 안 넣기로 한 것: 마이디자인 도트 편집, 여럿이 한 마을(서버).
- **차별점**: ① 화면이 **구면 투영**으로 굽는다(원작 감각의 핵심) ② 주민이 다섯 판 공통 인물이라 성격·대사에 역사 오마주가 묻어 있다 ③ **퓨전** — 동물의숲 틀에 갇히지 않는다: 괴물(포자괴물·성간충)·보스방·시대 혼합 목적지(우주기지·13세기 탑성 폐허)가 정당한 콘텐츠다. "완전 모방 할 필요 없어, 원작보다 나으면 좋다"(사용자, 2026-09-10).
- **인물이 되는 것**(SAGA-DESIGN §5): 주민 5(`residents`, HEROES 로스터에서 뽑음) + 숲 NPC 7. 이 판의 성장은 **내 힘이 아니라 마을과 관계**가 자란다.
- **플레이어 판타지**: "내가 돌보는 마을이 매일 조금씩 달라지고, 주민이 나를 알아본다."
- **전투 없음.** 손맛(§3-C)은 채집·낚시·나무 흔들기·문 열기의 반응으로 재정의한다.

## 2. 반드시 지킬 것

루트 `CLAUDE.md` 공통(세이브 키 `yeoksa-village/save/<프로필>` 불변·펫/인물 id 불변·실명 금지·다섯 벌 복사·원작 에셋 금지)에 더해, 이 판 고유:

1. **구면 투영을 평평한 탑다운으로 되돌리지 않는다.** `village-view.js` 의 `project()`/`unproject()` 가 심장이다. **집 안(`projIn`)만 일부러 안 휜다.** 탭 좌표는 `V.indoors()` 로 갈라 되짚는다.
2. **좌표 해시로 세계를 만든다.** 같은 마을은 늘 같은 모습(`core.hash2`, 부호 없는 시프트). 지형 개조는 타일 표가 아니라 **덮개층**(`terrain.js`) — 안 고친 마을은 세이브가 한 자도 안 는다. **절벽은 넣지 않는다**(투영에 높이 축이 없다).
3. **시간은 실제 시계다.** 계절 = 실제 달, 시간대(dawn/day/even/night) = 실제 시, 행사 = 양력 고정 날짜, 날씨 = 날짜 해시(`VD.weatherOf`). 가상 시계로 바꾸지 않는다. 진단은 구멍(`VD.season`·`_setStar`·`_setOpen`)으로 붙든다.
4. **프레임마다 도는 것은 공용 `Math.random` 을 먹지 않는다**(`bug.js`·`folk.js` 는 제 난수). 이사·편지는 `rollDay()` 에서만.
5. **복잡한 시스템을 새로 만들지 않는다.** 데이터 표 하나(`QUESTS`·`EVENTS`·`DELIVERIES` 식) + 기존 `focus()`/`interact()`/`talkNpc()` 분기 한 줄. 새 상호작용 프레임워크·새 전투 체계 금지. 프로젝트 전체 재작성·정상 기능 삭제·무거운 프레임워크 추가 금지.
6. **코드로 그리지 말고 에셋으로**(다섯 판 공통, 2026-08-28 사용자 지시). 3D 소품·인물뿐 아니라 초상·카드·아이콘까지. 못 구하면 "이래서 막혔고 대신 이것을 할 수 있다" 로 보고. 되돌림 길(옛 그림)은 손잡이로 남긴다. 출처는 `assets/ASSET_LICENSES.md`, CC0 우선, 재배포 불가 자산 금지.
7. **적은 수의 좋은 에셋을 배치·스케일·바이옴으로 다양하게.** 나무 GLB 5개로도 충분하다. 모바일 최적화 없는 고해상도 텍스처·거대한 단일 맵 모델·모든 동물 상시 AI·수백 개 독립 파티클 금지.
8. **모바일 세로·가로 둘 다.** Safe area, orientation 재계산, 한 손 조작. 성능 문제는 그래픽을 낮추기 전에 중복 로딩 제거→풀링→인스턴싱→LOD→거리 활성화→DPR→그림자 순서.
9. **파일을 늘리거나 고쳤으면 `sw.js` VERSION 을 올린다**(지금 `village-v0.55.1`). 안 올리면 한 화면에 세대가 섞여 죽는다.
10. **실기 확인은 배치 끝에 한 번만.** 기능마다 "실기 확인 전" 표시는 남기되 그때마다 요청하지 않는다. 개발 중 헤드리스 스크린샷 금지(루트 CLAUDE.md).
11. **다른 네 판·`saga-godot`·`saga-unity` 는 여기서 안 본다.** CC0 자산은 저장소 안 다른 판에서 md5 동일 복사로 재사용할 수 있다(출처 절 링크).

## 3. 현재 시스템 지도

세계: 마을 30×20 타일(`W`·`H`, `TILE`=40 논리단위) + 숲 고리 `forest.margin` 32(손잡이) → 약 94×84 타일. 바이옴 5(`green`·`meadow`·`dark`·`mushroom`·`rocky`, `BIOME_CELL` 22 격자 해시).

| 시스템 | 주 파일 | 상태 | 진단(`_test.html`) |
|---|---|---|---|
| 마을 생성·좌표 해시·숲 고리·바이옴 | `village.js` | 완료 | 마을이 만들어졌다 / 숲 고리 4 / 바이옴 재현 |
| 고정 자리: 호수·강·다리·폭포·캠프 2·폐허·동굴·바위산·우주기지 | `village.js` (`*Spot()`/`in*()`) | 완료 | 각 자리 재현·margin 부족 시 소멸 |
| 채집(열매·견과·광석·꽃·약초·화석·조개)·낚시(입질 1.2~3.5s, 창 0.7s)·심기(사흘) | `village.js` | 완료 | 낚시·심기 항목 |
| 곤충 채집 | `bug.js` | 완료 | 제 난수 항목 |
| 짐승 11종(사슴·여우·늑대·토끼·다람쥐·오리·새·개구리·뱀 + 포자괴물·성간충) idle/wander/flee | `animal.js`·`data-village.js` `ANIMALS` | 완료 | 제 바이옴에만 / 성간충 씨앗 40 |
| 숲 NPC 7(숲지기·낚시꾼·상인·탐험가·약초꾼·나그네·배달원) + 부탁 `QUESTS` 6 | `data-village.js`·`village.js` | 완료 | 부탁 항목 |
| 주민 5·성격 6·주민끼리 대화·걷기 | `folk.js` | 완료 | — |
| 편지·이사·감사장·내가 쓰는 편지 | `mail.js` (`rollDay`) | 완료 | 이사·편지 항목 |
| 집 꾸미기(안방·사랑방·부엌·뜰)·벽지·장판 | `home.js` | 완료 | — |
| 사고(史庫, 박물관) 기증 | `museum.js` | 완료 | — |
| 마을 이름·깃발·평가(`score = 100 − weeds×3 + min(flowers,30) + planted×2 + …`)·별똥별·소원 | `town.js` | 완료 | 평가·별 항목 |
| 순무 장 | `turnip.js` | 완료 | `_setOpen` 구멍 |
| 옷(침선방) | `wear.js` | 완료 | — |
| 계절 행사 8(설날·대보름·삼짇날·단오·칠석·백중·한가위·동지) — 그날 그 갈래 값 ×2, 하늘 태그만 | `data-village.js` `EVENTS` | 완료(장식 수준) | — |
| 날씨 4(맑음·흐림·비·눈, 날짜 해시)·시간대 4 | `data-village.js` | 완료 | — |
| 지형 개조(공사, 덮개층, 삯, 3×3) | `terrain.js` | 완료·**진단 4 실패** | 공사 6 항목 중 4 FAIL |
| 택배 아르바이트(접수대↔배달원, 🪙220, 무제한) | `village.js` `delivery{carrying,n}` | 완료 | 왕복 무제한 |
| 2D 구면 투영·집 안 평면·캔버스 그림(상자 등) | `village-view.js` | 완료 | 투영 항목 |
| 3D(three r169): 품질 3단 auto·InstancedMesh 8종·풀·시간대/날씨 하늘·안개 기본 꺼짐(near 30/far 320)·유성·물결·반딧불이 | `village-view3d.js` | 완료 | 3D 순수 함수 40여 항목 |
| GLB 등록표·hero 조합 해시(QRPG·MPFB)·monster/building 키·meshopt | `asset3d.js` | 완료 | 표 완결성 항목 |
| 전체지도(M키·🗺️) | `ui.js` `viewMap()` | 완료 | 시트 렌더 항목 |
| 설정 시트: 효과음·음악·그래픽 품질·안개 | `ui.js`·`core.save.settings` | 완료 | 손잡이 왕복 |
| 절차 효과음 20·BGM 1트랙(CC0 forest.mp3) | `sfx.js`·`bgm.js` | 완료 | 소리표 8 항목 |
| 인물 3D 초상 | `portrait3d.js` | 완료 | — |
| 자동 순행 | `auto.js` | 완료 | 자동 항목 |
| 어드민(때·손잡이·QA 프리셋) | `admin.js` | 완료 | ADMIN 셀프테스트 |
| 계정(브라우저 안 가입, 서버 없음) | `account.js` | 완료 | — |
| 세이브 `v:1`, 손잡이 `core.tuned` | `core.js` | 완료 | — |

자가진단 **262/266**(세 번 동일). 실패 4 = 공사 항목(기존, 원인 미확인 → §7 Phase 0 첫 일).

## 4. 재미 진단 (SAGA-DESIGN §3 표준 8)

| 표준 | 상태 | 근거 | 
|---|---|---|
| A 목표판 | **×** | 부탁 6은 시트 안에만. 부팅 직후 화면에 "지금·오늘·이번 주" 가 없다. 게시판(`board`)은 읽기만 |
| B 마무리 카드 | **×** | 없음. `rollDay()` 가 자정에 조용히 돈다 |
| C 손맛(채집형) | **△** | 효과음 20·손짓 몸짓(`triggerAction`)·회전 보간 있음. 없음: 수확 숫자 팝·화면 반응·연속 채집 리듬. 사가블로 `hitstop` 류 표준 미적용 |
| D 선택 3택 | **×** | 성장 선택이 없다(도구·옷·가구는 "사면 끝") |
| E 발견 밀도 | **×** | 세계 약 94×84 타일을 20타일 격자로 나누면 ~20칸. 마을 칸(1.5칸)은 빽빽하지만 바깥 고리 ~18칸에 **사건**(NPC·이벤트·비밀)은 10곳 남짓 → 절반이 "장식만 있는 칸". 채집 노드는 흩뿌려져 있으나 30초마다 **새로운 종류**의 일이 안 생긴다. "맵이 큰 것에 비해 npc 가 없다"(사용자) |
| F 실패·회복 | **△** | 평가 낮으면 주민이 떠난다(`mail.js`) — 있지만 **경고·회복 루프가 안 보인다**. 낚시 놓침 외 실패 없음 |
| G 성장 가시화 | **△** | 전방 성장·집·옷·깃발·박물관·마을 평가 — 원작 축은 다 있다. 없음: **수집이 마을을 바꾸는 사슬**(번들), 관계 단계 |
| H 돌아올 이유 | **△** | 실제 달력(계절 4·행사 8·별똥별·순무 장·편지)이 이 판 최대 강점. 없음: 일일 목표, "오늘 뭐가 다른지" 첫 화면 안내 |

**가장 큰 구멍 3**: ① A+H — 있는 달력 콘텐츠를 목표판·일일로 **보여 주지 않는다** ② E — 바깥 고리에 사건 밀도 ③ G — 수집→마을 변화 사슬과 관계 단계가 없어 "모아도 마을이 안 변한다".

## 5. 게임성 확장 — 후보 8 (우선순위 순)

9필드: 참고 / 표준 / 왜 / 메커니즘 / 수치 / UI·조작 / 세이브·마이그레이션 / 진단 / 범위·우선순위·충돌. 세이브는 전부 **`s.village.*` 에 칸 추가(additive)**, `v:1` 유지, 빠진 칸은 `auto.js` 의 `st()` 처럼 읽을 때 기본값으로 채운다 — 옛 세이브 마이그레이션 코드가 따로 필요 없다.

### 5.1 오늘의 일과판 (목표판 + 일일·주간)
- **참고**: 원신 일일 위탁, 포켓몬GO 리서치, 스타듀 "오늘 할 일" 메모. **표준**: A·H.
- **왜**: 달력 콘텐츠(행사·계절 채집·순무 장·편지)가 이미 있는데 안 보여서 "할 게 없다"고 느낀다. 새 콘텐츠 0으로 A·H 를 채우는 가장 싼 후보.
- **메커니즘**: 매일 `rollDay()` 가 날짜 해시로 **일일 3개**(채집 1·주민 1·탐험 1) + **주간 1개**(월요일 갱신) 를 `TASK_POOL` 표에서 뽑는다. 풀 예: "복숭아 3 모으기(계절)", "○○에게 선물", "폭포 다녀오기", "물고기 2종 기증", "잡초 5 뽑기", "택배 1회", "정령 1(§5.5)". 완료 즉시 다음 줄 채움(당일 최대 6). 행사날은 1줄이 행사 과제로 고정. 보상은 🪙 소량 + **도감 진척**(꽃/물고기 힌트) — 강제 없음(놓쳐도 벌칙 0).
- **수치**: 일일 3(각 🪙 80~150), 주간 1(🪙 600 + 옷/가구 1). 과제 소요 ≤3분 설계. 연속 7일 달성 시 "부지런한 사람" 깃발 장식.
- **UI**: 상단 목표판 3줄(접기 가능, 세로 모드 기본 1줄 + 펼침). 완료 시 줄이 ✓ 로 0.4s 빛나고 밀려 나간다. 게시판(`board`) 상호작용이 전체 목록 시트를 연다(기존 `SHEET_TITLE` 에 `tasks` 추가).
- **세이브**: `s.village.tasks = { day: 'YYYY-MM-DD', list: [{id, n, done}], week: 'YYYY-Www', weekly: {id, n, done}, streak }`.
- **진단**: 부팅 직후 목표 3줄 비어 있지 않다 / 날짜 바꿔 `rollDay()` 시 갱신 / 100일 굴려 같은 축 3개 중복 0 / 행사날 1줄 고정.
- **범위**: `data-village.js`(`TASK_POOL`), `village.js`(`rollDay` 한 블록, `taskProgress()` 훅을 기존 채집·선물·기증·배달 결과 자리에 한 줄씩), `ui.js`(목표판·시트), `index.html`·`css`. **우선순위 1.** 충돌: 없음(실시간 시계 규칙 그대로 씀).
- **구현(2026-09-17)**: 진행은 **누계에서 뺀 값**(baseline)으로 잰다 — `bagCatCount`(현재 가방, 팔면 준다)가 아니라 `s.caught`(도감, 안 준다)를 세는 `caughtCatCount()`를 새로 만들어 팔아도 진행이 안 사라지게 했다. 채집·선물·잡초는 새 누계 한 줄(`s.giftTotal`·`s.weedPulled`), 기증은 `s.donateTotal`, 배달은 기존 `s.delivery.n`을 그대로 빌렸다. 셋은 축(채집/주민/탐험)마다 하나씩 뽑아 **구조로** D 표준의 "같은 축 중복 0"을 만족한다(추첨이 아니라 애초에 같은 축에서 안 뽑는다). 행사날 override 는 `knob('time.event')`를 그대로 타(어드민 "때"로 시험 가능), `town.js`의 기존 festival 조회와 같은 결로 `time.dayShift`는 아직 안 탄다(§7-5류 구멍, 실기에서 문제되면 그때 잡는다). 목표판 3줄 접기·완료 시 ✓ 빛나는 연출·번들 6과의 연계는 아직 — 지금은 상단 한 줄 + 게시판(town 시트) 안 전체 목록까지만.

### 5.2 하루 마무리 카드
- **참고**: 스타듀 하루 결산, 동물의숲 이사 편지. **표준**: B.
- **왜**: 세션의 끝이 없다. "오늘 이만큼 했고 내일은 이것" 이 있어야 돌아온다.
- **메커니즘**: 두 트리거 — ① 집에 들어가 **자리(침구)** 상호작용 "오늘을 마친다" ② 자정 넘겨 첫 부팅 시 자동. 카드 1장: 얻은 것(채집·🪙·기증), 마을 평가 변화(±), 주민 한 줄(가장 많이 만난 주민의 대사), **내일 한 가지**(§5.1 의 내일 첫 과제 미리보기 또는 다음 행사 D-n). 5초 안에 닫힘. ①로 마치면 `rollDay()` 를 미리 굴리지 않는다(실시간 규칙) — 카드만.
- **수치**: 표시 항목 ≤5, 자동 닫힘 8s, 보상 없음(정보만).
- **UI**: 시트 1장(`SHEET_TITLE.dayend`), 카드 그림은 에셋(§2-6): 종이 질감 9-slice 1장.
- **세이브**: `s.village.dayLog = { date, gathered, gold, donated, met: {id: n}, ratingDelta }` — 하루치만, 다음 날 덮어쓴다.
- **진단**: 카드 데이터에 `next` 필드가 비어 있지 않다 / 자정 넘김 시 1회만 뜬다.
- **범위**: `village.js`(dayLog 누적 한 줄씩), `ui.js`, `home.js`(침구 상호작용). **우선순위 2.** 충돌 없음.
- **구현(2026-09-17)**: 트리거 ②(자정 넘겨 첫 부팅)만 했다 — 트리거 ①(침구)은 이 판에 "침구" 가구 자체가 없어(FURNITURE 표에 방석·화로 등은 있어도 bed 류가 없다) 새 가구 한 종을 짓는 몫이 커 미뤘다. `rollDay()`가 날을 넘기기 **직전**(리셋 전)에 `buildDayLog()`로 전날 값을 굳히고, 새 날이 열리자마자 `snapshotDayMark()`로 다음 비교 기준을 다시 박는다. "주민 한 줄"은 대사 대신(그런 대사 데이터가 없다) 오늘 정(`friend`)이 가장 많이 오른 사람 이름으로 근사했다. `#encounter`(이 판이 안 쓰던 자리)를 빌려 8초 자동 닫힘 카드로 띄운다.
- **구현(2026-09-18, 트리거 ① 마저)**: `data-village.js`의 `FURNITURE`에 안방 가구 "요"(`key: 'yo'`, `bed: true`)를 신설 — 전방에서 여느 가구처럼 사고 방에 놓는다. `village.js`에 `sleepNow()` 신설: **`rollDay()`는 안 부른다**(실시간 규칙 그대로) — `buildDayLog(s.day)`만 다시 굴려 지금까지 쌓인 값으로 카드를 미리 만든다. `ui.js`는 놓인 게 `bed`면 초점 카드에 "잠든다"(`v-sleep`, 기본)와 "거둔다"(`v-do`, 기존 그대로) 둘 다 보여주고, `v-sleep`은 `sleepNow()` 뒤 바로 `checkDayCard()`를 불러 다음 주기까지 안 기다리고 카드가 뜬다. `village-view.js`에 요 모양(`furnBedding`)과 포커스 말풍선("잠든다"로 갈림)도 얹었다. `_test.html`에 "날짜는 그대로에 카드만 미리 뜬다" 회귀 진단 1항목 추가.

### 5.3 마을 번들 — 모으면 마을이 변한다
- **참고**: 스타듀 커뮤니티 센터 번들, 동물의숲 박물관 완성. **표준**: G·A·H.
- **왜**: 사고(史庫) 기증이 "칸이 채워지는 것" 으로 끝난다. 수집이 **마을 지형·시설을 바꾸면** 성장이 눈에 보인다(SAGA-DESIGN §3-G "보이는 것 1개").
- **메커니즘 — 확정(§10-Q7, 2026-09-18)**: 사고는 **꽃·열매를 받지 않는다** — 둘은 판매·선물 갈래로만 남긴다. 번들은 곤충·물고기·화석·조개 **4갈래 고정**(`BUNDLES` 표). 각 번들 완성 시 **마을 시설 1개가 생긴다**(고정 자리, `buildProps()` 조건부): 물고기→호수 정자+낚시 발판, 곤충→반딧불이 정원(밤 파티클 배율 ×3), 화석→사고 앞 석비, 조개→강가 조개 길(모래 타일 덮개층 자동). 4개 완성 → **마을 평가 상한 해제**(§5.8) + 깃발 문양 1.
- **수치**: 번들당 5~8종, 전체 ~27종(현재 도감 어휘 안에서, 4갈래). 시설은 `deco` 위주, 상호작용 1개씩(정자 앉기·석비 읽기 등, 기존 `bench`·`board` 결 재사용).
- **UI**: 사고 시트에 "번들" 탭(진척 바 4). 완성 순간 마을 지도(`viewMap`)에 새 시설 아이콘이 반짝인다(§5.2 카드에도 한 줄).
- **세이브**: `s.village.bundles = { done: ['fossil', …] }` (기증 목록은 이미 `donated` 에 있어 진척은 계산으로 낸다 — 새 칸 최소).
- **진단**: 번들 완성 조건 = 도감 항목 부분집합(어휘 검사) / 완성 시 `buildProps()` 에 시설 프롭 생김·미완성 시 없음 / 시설 자리가 다른 고정 자리와 안 겹침.
- **범위**: `data-village.js`(`BUNDLES`), `village.js`(`buildProps` 조건 6블록·`terrain.js` 덮개층 1건), `museum.js`, `ui.js`, `asset3d.js`(석비·천막 등 기존 키 재사용, 새 GLB ≤2), `village-view3d.js` SCATTER 표. **우선순위 3.** 충돌: 좌표 해시 규칙 — 시설은 해시가 아니라 **세이브 조건부 고정 자리**(캠프와 같은 결)라 충돌 없음.
- **구현(2026-09-17, ①만)**: **넷만 했다** — `MUSEUM_CATS`가 애초에 곤충·물고기·화석·조개 넷뿐이라(꽃·열매는 이 판에 기증 UI 자체가 없다, "선물·판매와 겹치는 물건을 사고에도 받을지"는 정책 결정이라 이번엔 안 건드리고 §10 에 남긴다). `VD.BUNDLES` 표(갈래→시설 이름)를 새로 두고, `village.js` `buildProps()`가 `museum.byCat()`으로 갈래 완결(도감 항목 부분집합, 이미 있는 계산 재사용)을 보고 시설을 심는다 — 화석→석비(새 `stele` PROPS), 곤충→반딧불이 정원(새 `fireflyplot` PROPS, 밤 파티클 배율은 아직 자리 표시만), 물고기→호숫가 평상(기존 `bench` 재사용, `lakeCenter()` 동쪽 기슭), 조개→강가 조개 두 점(기존 `shell` 재사용, `riverCenterX()` 둑 — "길"이라기엔 짧다). 상호작용은 `village.js` `interact()`에 `sapling`과 같은 결로 toast 문구만(시트를 열 게 없다 — `village:open`을 쐈으면 `ui.js` `SHEET_TITLE`에 없는 이름이라 엉뚱하게 "기록" 시트가 열렸을 것). **안 한 것(당시)**: 반딧불이 밤 파티클 ×3, 조개 길 모래 타일 덮개층(`terrain.js`), 4개 완성 시 마을 평가 상한 해제(§5.8②가 아직 없어 연결할 대상 자체가 없다), 깃발 문양, 3D(`village-view3d.js` SCATTER 표 — 2D `village-view.js`는 손 안 대도 `default:` 이모지 폴백이 이미 그린다). **검증** — `node -c` 통과, `_test.html`에 진단 2항목(갈래별 조건부 등장·넷 다 채워도 고정 자리 안 겹침) 추가.
- **구현(2026-09-18, 마을 평가 상한 해제)**: §5.8②(별 5 평가)가 그날 생겨 연결 대상이 드디어 있어졌다. `museum.js`에 `allBundlesDone()`(네 갈래 `byCat()` 각각 `done>=total`) 신설. `town.js` `beauty()`는 순수 함수 `capBeauty(score, bundlesFull, grades)`로 상한 로직을 뽑아 뒀다 — 번들을 다 못 채웠는데 점수가 최고 등급 문턱(`BEAUTY_GRADES` 마지막 `at`) 이상이면 그 문턱 −1로 눌러 앉힌다(등급표 자체는 안 바꾼다, "정갈함만으론 최고 등급이 안 되고 사고를 완성해야 한다"는 신호). 반환값에 `bundlesFull`·`capped` 필드 추가 — `ui.js` 마을 시트에 상한에 막혔을 때만("사고 네 갈래를 다 채우면 최고 등급까지 오를 수 있습니다") 한 줄이 뜨고, 다 채웠으면 조용히 빠진다. `admin.js` QA 표시에도 "상한 걸림" 꼬리표. **검증** — `node -c`(museum.js·town.js·ui.js·admin.js) 통과, `node` vm으로 `_capBeauty` 실제 값 대조(상한 미만 통과·상한서 번들 미완이면 −1점·번들 완성이면 무제한). `_test.html`에 진단 2항목(순수 함수 경계값 넷·`beauty()`가 `museum.allBundlesDone()`을 실제로 배선해 쓰는지) 추가. **여전히 안 한 것(당시)**: 반딧불이 파티클·조개 길 덮개층·깃발 문양·3D SCATTER(전부 그래픽/에셋 몫이라 다음에 따로).
- **구현(2026-09-18, 반딧불이 밤 파티클 ×3)**: `village-view3d.js`에 앰비언트 반딧불이(`WEATHER_FX.firefly`, 인물 둘레 고정)와 별도로 `WEATHER_FX.fireflyBoost`(24개, 반경 5 — 훨씬 좁다)를 새로 지어, `fireflyplot` 소품이 서 있으면(곤충 번들 완성) 매 프레임 그 세계 좌표로 그룹을 옮겨 얹는다(`syncScatter()`가 소품에 쓰는 `(p.x-px)*scale` 변환과 같은 요령 — 파티클은 늘 원점 중심으로 지어 두고 그룹 위치만 옮긴다). 순수 함수 `fireflyPlotOf(props)`로 그 소품을 찾는다(없으면 null → 안 보임, 밤이 아니거나 비/눈이면도 안 보임 — 기존 `fireflyVisible()` 조건 그대로 같이 건다). **정확한 밀도 배율(×3)을 식으로 안 맞추고 눈에 띄는 고정 수(24)를 골랐다** — 실기 확인 전까지는 그 수가 맞는지 알 길이 없다. **검증** — `node -c` 통과, `node` vm으로 `village-view3d.js`를 core stub 하나로 로드해 `fireflyPlotOf` 실제 값 대조. `_test.html`에 순수 함수 진단 1항목 추가.
- **여전히 안 한 것**: 조개 길 덮개층·깃발 문양·3D SCATTER — 다음에 따로.
- **구현(2026-09-17, 이어서 — 사고 시트 UI + 즉시 등장)**: `museum.js` `donate()`가 그 기증으로 갈래가 막 다 채워졌는지(`catStatus()` 전/후 비교)를 보고, 막 채웠으면 **그 자리에서 바로 `V().buildProps()`를 부른다** — 원래는 자정 넘김에만 자동으로 도는 함수라, 안 그러면 다음 날이 될 때까지 시설이 안 보였을 것. 반환값에 `bundleCompleted`(갈래 키 또는 null)를 얹고, 토스트·로그 문구에 "🎉 [번들 이름]!"을 붙인다. `ui.js` `viewMuseum()`은 전시실 네 칸마다 `VD.BUNDLES` 유무를 보고 "다 채우면 시설이 섭니다" / "✓ 다 채웠습니다 — 시설이 섰습니다" 한 줄을 더한다(PLAN 원안의 "사고 시트 번들 탭"을 새 탭 대신 **기존 전시실 칸에 얹어** 만족 — 이미 갈래별 진척 바가 있어 새 탭을 안 만들어도 됐다). **검증** — `node -c` 통과, `_test.html` 진단 2항목 추가(마지막 기증 순간 `bundleCompleted`·석비가 바로 섬 / 안 다 채운 기증은 축하 문구 없음).

### 5.4 관계 하트 — 주민 5 + NPC 7
- **참고**: 스타듀 하트·선물 취향, 동물의숲 친밀도·초상 사진, 하데스 관계도. **표준**: G·H·D.
- **왜**: 주민 성격 6·선물·편지·부탁이 있는데 **누적이 안 보인다**. 관계 단계가 있으면 "이 인물" 이 다섯 판 공통 인물이라는 정체성이 살아난다.
- **메커니즘**: 인물별 `heart` 0~10. 상승: 대화(하루 1회 +1)·선물(취향 맞으면 +3/보통 +1/싫으면 −1, 성격 6 × 갈래 표)·부탁 완수 +2·편지 답장 +1·행사날 세배/나눔 +2. 하루 상승 상한 +4. 단계 해제: 3♥ 집 방문 허용(주민 집 = 기존 `home.js` 방 1개 재사용, 초상 3D `portrait3d.js`)·5♥ 고유 대화 3(역사 오마주 한 줄, 실명 금지)·7♥ 동행(30초간 뒤따라 걷기 `folk.js` follow 모드, 채집 확률 +10%)·10♥ 인물 기념품 1(가구, `sarang` 세트). **이사 판정 — 확정(§10-Q6, 2026-09-18, 둘 다 반영)**: 기존 `mail.js` 결(마을 평가·`beauty()` 가 확률을 좌우 — 방치할수록 잘 떠남, 원작 감각 유지)은 그대로 두고, `friend` 대신 **`heart` 로 교체**해 후보 조건(현재 `fr >= FRIEND_KEEP` 이면 제외)만 하트 기준으로 바꾼다 — 정갈한 마을에선 하트 낮은 사람도 잘 안 떠나고, 방치된 마을에선 하트 낮은 사람부터 떠난다.
- **수치**: 하루 상한 +4, 10♥ 까지 최소 12일. 선물 취향 표: 성격 6 × 카테고리(fruit·nut·flower·fish·bug·shell·fossil·furniture·cloth) 좋아함 2·싫어함 1.
- **UI**: 대화 말풍선 옆 ♥ 미니바(0.6s 페이드). 인물 시트(초상+하트+다음 해제 조건). `viewMap` 에 주민 위치 ♥ 아이콘.
- **세이브**: `s.village.hearts = { [heroId]: { h, lastTalk: 'date', gifts: n } }`.
- **진단**: 하루 상한 +4 넘지 않음 / 취향 표 완결성(성격 6 전부) / 하트 2 이하만 이사 후보 / 동행 30s 뒤 해제.
- **범위**: `data-village.js`(취향 표·해제 표), `folk.js`(follow), `mail.js`(이사 조건 한 줄), `village.js`(gift/talk 결과 자리 한 줄), `ui.js`. **우선순위 4.** 충돌: HEROES `id` 불변 — 키로만 쓴다. 실명 금지 — 고유 대사는 사건 오마주만.
- **구현(2026-09-18, 이사 조건 §10-Q6 결정 배선 + 7♥ 동행)**: 그날 결정만 해 두고 "코드 변경 없음"으로 남겨 뒀던 §10-Q6을 이제 배선했다 — `mail.js`의 떠날 뜻 **후보** 조건(`fr >= FRIEND_KEEP`)을 `heartOf(rid) > HEART_LEAVE_KEEP`(새 손잡이, 기본 2)로 바꿨다. 확률·문구·마을 평가 배율은 그대로다(§10-Q6이 "후보 조건만"이라고 정확히 그렇게 정했다). **안부 편지**(`warm`, `friend >= FRIEND_KEEP`)는 다른 자리라 안 건드렸다.
  이어서 **7♥ 동행**도 짰다 — `folk.js`에 `startFollow(id)`/`stopFollow()`/`followStatus()`와 30초 카운트다운을 얹었다. 동행 중인 사람은 그 프레임엔 어슬렁 대신 플레이어 쪽으로 걷는다(`FOLLOW_DIST`=40까지만 다가서고 그 안쪽은 안 붙는다) — 풀리면(시간이 다 되거나 마을을 떠나면) 다음 어슬렁 걸음이 제 자리(`home`) 둘레로 저절로 돌아간다(자리를 강제로 되돌리지 않는다). 자격 판정(하트 7 문턱)은 `village.js`의 `canFollow(id)`/`requestFollow(id)`가 갖고(`heartUnlockAt('동행')`으로 `HEART_UNLOCKS` 표를 이름으로 찾아 인덱스에 안 얽맨다), 실제 "따라 걷기"는 `folk.js` 몫으로 나눴다. `ui.js`는 곁에 서 있을 때(`nearFolk()`, 선물 주는 자리와 같은 문맥) "🚶 함께 걷기" 버튼 하나, 주민 목록엔 동행 중 남은 초를 보여 준다.
  **"채집 확률 +10%"는 이번에 안 짰다** — 이 판에 "채집 성공률"이라는 확률 자체가 없다(나무 흔들기·벌레 잡기 전부 손에 닿으면 100% 받는다, `shake()`의 벌집/가구/돈주머니는 "무엇이 더 나오나"의 보너스 갈래일 뿐 실패가 없는 굴림이다). 이 수치를 어느 자리에 붙일지(보너스 갈래 확률을 올릴지, 다른 걸 만들지)는 판단이 필요해 임의로 정하지 않고 열어 둔다.
  **3♥ 집 방문·5♥ 고유 대화**도 이번엔 안 했다 — 집 방문은 이 판에 "주민 개인의 집"이라는 자리 자체가 없어(플레이어 집만 `home.js` 하나) 새 세계 좌표·건물을 늘리는 큰 작업이고, 고유 대화는 인물 12명(주민 5+NPC 7) × 3줄 콘텐츠를 실명 금지 원칙 아래 새로 써야 해 다음으로 미룬다.
  **검증** — `node -c`(folk.js·village.js·mail.js·ui.js) 통과. `node` vm으로 `folk.js`를 최소 스텁(`village.walkable`·`raw`·`state`)만 채워 로드해 실제 동행 움직임을 값으로 확인(10초 뒤 제 자리 반경 밖까지 다가섬 · 중복 시작 거절 · 35초 뒤 자동 해제). `_test.html`에 진단 3항목(이사 후보 하트 갈림 · 동행 움직임·시간 · canFollow/requestFollow 하트 문턱) 추가.
- **구현(2026-09-19, 5♥ 고유 대화 + NPC 하트 배선)**: 지난 세션이 "인물 12명 × 3줄 콘텐츠"라 미뤘던 걸 열어 보니, 주민(5명)은 매번 105명 전체 로스터에서 무작위로 뽑힌다(`buildResidents()`) — 12명이 아니라 105명 몫을 새로 써야 하는 일이었다. 대신 **`data.js`의 열전(BIOS) 한 줄을 그대로 재사용**했다 — 이미 실명 없이 다듬어진 "역사 오마주 한 줄"이라 새로 쓸 필요가 없고, 105명 전원이 갖고 있어(`node` 로 직접 셈, 확인) 빠지는 사람이 없다. `village.js`에 `uniqLineOf(id)` 신설: 하트 5 미만이면 null, 이상이면 `'문득 지난 이야기가 떠오르는구려... ' + DG.data.bio(id)`. `talk()`의 "오늘 할 일 다 끝난" 인사 자리(`req.done`)에서 이 값이 있으면 성격 유형 인사(`ty.idle`)보다 앞세운다.
  **NPC 7 쪽은 실제로는 하트가 전혀 오르지 않고 있었다** — 선물도 못 주고(`giveGift`·`nearFolk`가 `residents`만 본다), `talkNpc()`도 하트를 안 건드려서 "주민 5+NPC 7" 이라던 §5.4 마디표는 절반만 맞는 말이었다. 이번에 그 틈을 메웠다(선물 UI 확장은 손 안 댐 — 범위 밖): `talkNpc()`에 주민과 같은 "오늘 첫 대화 +1" 을 얹고, 부탁 완수(+2)·배달원 배달 완수(+2, `talkCourier()`)에도 `bumpHeart`를 달았다. `data-village.js`의 `NPCS` 일곱 항목에 각각 `uniq` 배열(원작 없는 창작 인물이라 실명 위험이 없다, 3줄씩 21줄 새로 씀)을 얹고, `village.js`에 `npcLine(npc, def)` 신설 — 하트 5 이상이면 날짜 해시로 셋 중 하나를 고정해 돌려주고, 아니면 평소 `def.line`. `talkNpc()`의 두 "평소 인사" 반환 자리와 `talkCourier()`의 "소포 없음" 반환 자리가 이걸 쓴다.
  **검증** — `node -c`(village.js·data-village.js) 통과. `node`로 `data.js`만 단독 로드해 열전 커버리지 실측(105/105). `_test.html`에 진단 2항목(주민 하트5→BIOS 노출 · NPC 하트5→대사 갈림 + 대화 시 하트 상승) 추가 — `core.js`가 브라우저 전역(addEventListener 등)에 기대 `village.js` 전체를 `node` vm 스텁으로 실행하진 못했다(동행 때보다 의존이 많다: town·mail·folk까지 걸림), 그래서 실제 실행 확인은 이 `_test.html` 진단이 유일한 자리다 — **헤드리스 확인은 안 했다**(서버 기동 규칙), 사용자 실기/헤드리스 확인 전.

### 5.5 발견 밀도 격자 + "숲의 정령" 60
- **참고**: 마리오 오디세이 30초 규칙, BOTW 코로그(작은 퍼즐 → 수집품) + 힌트 판매, 동물의숲 화석 자리. **표준**: E·G.
- **왜**: §4-E. 바깥 고리에 "종류가 다른 일" 을 격자 단위로 보장해야 걷는 이유가 생긴다.
- **메커니즘**: ① **격자 규칙**: `buildProps()` 끝에 20타일 격자를 훑어 사건(NPC·상자·비밀·이벤트 노드) 0 인 칸에 해시로 1개 심는다(`ENCOUNTER_KINDS` 6: 보물상자·쪽지 병·정령 퍼즐·희귀 채집 노드·짐승 무리·나그네 야영). ② **숲의 정령 60**: 바이옴별 12. 퍼즐 5종(전부 기존 상호작용 재조합): 돌무리 완성(바위 4 중 빠진 1 놓기 — 광석 소모)·꽃 원(꽃 5 심어 원 완성)·낚시터 반짝(특정 시간대 낚시)·나무 흔들기 3연속·별 자리(별똥별 소원 밤에 특정 자리). 완성 시 정령이 나타나 **정령 씨앗 1**. 씨앗 10개 → 가방 칸 +2 / 전방 상품 +1 / 순무 보관 +… (원작 코로그 씨앗식 편의 보상). 나그네(`wanderer`)가 🪙로 **가까운 정령 힌트** 판다(전체지도에 반경 표시).
- **수치**: 격자 20타일, 칸당 사건 ≥1(마을 칸 제외), 정령 60, 씨앗 보상 단계 6·12·20·30·45·60. 상자는 하루 1회 리셋 절반·영구 절반.
- **UI**: 정령 발견 시 0.8s 반짝+효과음(새 sfx 1). 도감 "정령" 탭 60칸(바이옴별). 전체지도 힌트 원.
- **세이브**: `s.village.spirits = { found: [id…], seeds }`, `s.village.grid = { opened: [cellKey…] }`(상자 리셋용 날짜 포함).
- **진단**: 격자 스캔 빈칸 ≤10% / 정령 60 자리 재현·고정 자리와 안 겹침 / 퍼즐 5종 어휘가 기존 상호작용 표에 있음 / 씨앗 보상 단조 증가.
- **범위**: `village.js`(격자 훑기·정령 자리·퍼즐 판정 5), `data-village.js`(`SPIRITS`·`ENCOUNTER_KINDS`), `ui.js`(도감 탭·지도), `sfx.js` 1, `asset3d.js`(정령 = 기존 반딧불이 파티클 + 작은 GLB 1, CC0). **우선순위 5.** 충돌: §2-2 좌표 해시 — 격자 심기는 해시로 결정적. 성능(§7) — 사건 노드는 거리 컬링 대상으로.

### 5.6 축제 하루 — 행사 8 을 놀이로
- **참고**: 동물의숲 계절 이벤트, 스타듀 축제(달걀 찾기·낚시 대회·춤), 원신 미니게임. **표준**: H·C.
- **왜**: 행사 8이 "값 ×2 + 하늘 태그" 로만 있다. 그날 **놀이 1개**가 있으면 달력이 돌아올 이유가 된다.
- **메커니즘**: 행사별 미니게임 1(전부 기존 조작 재조합, 새 입력 없음): 설날 세배(주민 5 에게 말 걸기, 세뱃돈 — 기존)·대보름 달집(모닥불에 견과 5 태우기, 불꽃 파티클)·삼짇날 꽃놀이(1분 안에 꽃 8 종류 찾기)·단오 그네(낚시 입질 창 재사용: 타이밍 5회)·칠석 소원(별똥별 확정, 소원 1 = 다음 날 채집 ×1.5)·백중 불꽃(하늘 파티클 + 등롱 밤 산책 사진)·한가위 줄다리기(주민 vs 나, 연타 10초)·동지 팥죽(부엌에서 팥+견과 → 주민 5 에게 나눔, 하트 +2). 보상: 행사 가구 1(연 8종, 첫해 수집).
- **수치**: 놀이 1~2분, 하루 1회. 행사날 `sky` 는 표대로(`clear`/`snow`).
- **UI**: 행사날 부팅 시 §5.1 목표판 첫 줄 고정 + 마을기 앞 안내판. 놀이는 시트가 아니라 **필드에서** 한다.
- **세이브**: `s.village.fest = { [key]: { year, done } }`.
- **진단**: 행사 8 모두 놀이 키 있음 / 날짜 구멍으로 그날 만들어 놀이 1회 후 재실행 불가 / 놀이 판정은 `update(dt)` 로 굴려 결정적.
- **범위**: `data-village.js`(`EVENTS` 에 `play` 필드), `village.js`(판정 8 블록, 연타는 기존 `BITE_WINDOW` 결), `home.js`(팥죽), `ui.js`, `sfx.js`. **우선순위 6.** 충돌: 실시간 규칙 — 어드민 "때" 탭으로 미리 볼 수 있다.

### 5.7 택배 사슬 — 다른 마을을 잇는 이유
- **참고**: BOTW 랜드마크 유도, 스타듀 배달 의뢰, 포켓몬GO 걸음 보상. **표준**: A·E·H.
- **왜**: 택배(우주기지 1곳, 🪙220 반복)는 뼈대만 있다. 목적지 사슬·소포 변화가 있어야 "배달 알바" 가 된다.
- **메커니즘**: `DELIVERIES` 표(`from`·`to`·`item`·`reward`·`rule`). 목적지: 우주기지(완료, 남서 대각선 `SPACEBASE_MIN_MARGIN` 20·비율 0.7/0.75)·**폐허=과거**(탑성 폐허를 배달 지점으로 승격: 폐허 안 "옛 우체통")·캠프 2·**현대 목적지**(CC0 자산 미확보 — §10-Q2). 소포 규칙 3: 보통 / 깨지기 쉬움(달리기 금지, 뛰면 파손 → 보상 절반) / 시간제한(현실 5분). **사슬 보너스**: 연속 3배달 ×1.5. 배달 등급(누적 n): 10→수레 소품 내 집 앞, 30→배달원 우주복 옷 해제(침선방), 60→로버 옆 "탑승" 상호작용(암시만, 이동 시스템 없음). 소포 외형은 Space Kit `Pickup_*`(미러에 있음).
- **수치**: 보상 기본 🪙220 유지, 거리 계수 +🪙2/타일, 사슬 ×1.5, 파손 ×0.5. 하루 상한 없음(사용자 결정 "무제한").
- **UI**: 접수대에서 소포 3택(보통/깨지기/시간) — **D 표준의 첫 적용**. 들고 있으면 머리 위 📦, 시간제한은 상단 타이머.
- **세이브**: `s.village.delivery` 확장 `{ carrying, n, kind, since, chain, grade }` — 기존 두 칸 유지.
- **진단**: 왕복 무제한(기존) / 3택 100회 중복 축 0 / 파손 판정(달리기 플래그) / 사슬 리셋 조건.
- **범위**: `data-village.js`(`DELIVERIES`), `village.js`(`pickupParcel`·`talkCourier` 확장, 폐허 우체통 프롭), `ui.js`(3택), `asset3d.js`(Pickup 1~2, CC0 Space Kit). **우선순위 7.** 충돌: 성능 경고 — 고정 콘텐츠는 거리 컬링이 없으므로 목적지 추가 전 §7-3 먼저.

### 5.8 채집 손맛 표준 + 마을 평가 재설계
- **참고**: 사가블로 손맛 1차(hitstop·흔들림), 동물의숲 섬 평가(별 5·조건 공개). **표준**: C·F·G.
- **왜**: 채집이 "탭 → 아이템" 으로 끝난다. 평가는 숫자만 보인다.
- **메커니즘**: ① 채집 5요소 — 입력→반응 ≤100ms, 대상 흔들림(나무 0.25s 스프링, 3D 는 `triggerAction` 연동), 수확 팝(아이템 아이콘 0.6s 포물선 → 가방), 효과음 3종 라운드로빈(기존 20 중 갈래별), 연속 채집 3회마다 리듬 보너스(+1 확률). 낚시 성공 시 화면 0.2s 줌 인. ② 평가 별 5(현행 `score` 를 0~100 → 별 5 로 표시, 조건 공개: 잡초·꽃·심은 나무·시설(§5.3)·주민 하트 평균). **경고**: 별이 떨어질 조건이 생기면 게시판 + 카드(§5.2)에 "잡초 7 → 주민 ○○ 가 걱정" — 회복 ≤2분(잡초 뽑기). ③ 자동 순행(`auto.js`)은 손맛을 건너뛴다(기존 흐름 유지).
- **수치**: SAGA-DESIGN §3-C 기본값. 별 경계 20/40/60/80/95. 이사 경고는 별 2 이하에서만.
- **UI**: 팝·흔들림은 캔버스/3D 양쪽. 평가는 깃대 상호작용 시트에 별 + 조건 5줄.
- **세이브**: 없음(계산).
- **진단**: 채집 이벤트가 5요소 훅을 모두 발생시킨다(리스너 카운트) / 별 경계 단조 / 경고 조건 → 회복 후 해제.
- **범위**: `village.js`(채집 결과 훅 1곳), `village-view.js`·`village-view3d.js`(흔들림·팝), `sfx.js`, `town.js`(별·조건), `ui.js`. **우선순위 8(단, ①은 Phase 1 에 같이).** 충돌: `sprite.js` 는 건드리지 않는다(팝은 UI 레이어).
- **구현(2026-09-17, ①만)**: 흔들림·팝은 **캔버스·3D 를 안 건드리고 UI 레이어에서**(PLAN이 이미 그러라고 적어 둔 그대로) — `#focusbar` 카드 자체에 0.25s CSS 펄스, `document.body` 에 fixed 뜨는 "+n" 텍스트가 0.6s 떠올랐다 사라진다. 효과음은 새 mp3 없이(에셋 규칙) `sfx.js`(절차 생성)에 `gather_a/b/c` 세 파형을 만들어 라운드로빈, 보너스만 별도 차임. 리듬 보너스(3연속마다 +1개)는 손짓 타이밍(8초 안에 이어 침) 기준 — 하루 몇 번이 아니라 "연타가 안 끊겼는가"를 잰다(§5.1 일과와 다른 축). ②(별 5 평가)·③(자동은 손맛 건너뜀, 이미 그렇다)은 안 했다. 낚시 줌인도 안 했다(카메라 손대는 몫이라 3D 파일을 봐야 한다).
- **구현(2026-09-18, ② 마저)**: **위 "수치"의 20/40/60/80/95 대신 기존 `BEAUTY_GRADES`(0/60/100/150/200, 다섯 단계)를 그대로 별 1~5로 매겼다** — `town.js` `beauty()`가 이미 이 표로 등급을 매기고 `mail.js`(주민이 잘 떠나지 않는다)도 이 등급을 본다. 새 0~100 눈금을 따로 만들면 두 표가 어긋날 위험이 있어 기존 걸 재사용하는 쪽을 골랐다(`beauty().stars = level + 1`). 조건 공개도 §5.3(시설)·§5.4(하트, 둘 다 미착수)는 아직 없어 **지금 실제로 점수에 들어가는** 잡초·꽃·심은 나무·집 꾸미기·사고 기증 다섯 줄로 채웠다 — 그 둘이 들어오면 조건 줄도 같이 늘린다. 경고(`beautyWarning()`)는 "잡초가 최대로 늘어도(하루 최대 `WEED_PER_DAY`) 지금 등급을 지키는가"를 보고, 못 지키면 게시판(마을 시트)과 하루 마무리 카드(§5.2) 양쪽에 같은 문구를 낸다 — 걱정하는 주민은 `s.residents` 중 날짜 해시로 하나 고른다(하트가 없으니 "정 든 사람" 대신 아무나다, §5.4 들어오면 하트 낮은 사람으로 바꿀 것). `_test.html`에 별 범위·경고 문턱 진단 2항목 추가.
- **구현(2026-09-18, ① 낚시 줌인 마저 — 이걸로 Phase 1 코드분 전부 닫힘)**: `village-view3d.js`에 `fishZoomTimer`(0.2s) + `triggerFishZoom()` 신설 — `sfx.js`가 이미 듣던 `village:fish` 이벤트를 `init()` 안에서 그대로 구독해 `state==='catch'`일 때만 튼다(다른 사가블로·사가고 판의 카메라 손잡이와 안 부딪히게 **`userZoom`은 안 건드리고 그 위에 덧씌우는 배율**로 뒀다 — `syncCamera()`의 radius0/height0/radius1/height1 넷을 이 배율로 한 번 더 나눔). 몸짓(`triggerAction`)과 달리 `player.actions` 준비 여부와 무관해 init 전에도 타이머는 세워진다. `_test.html`에 그 성질(초기화 전에도 0.2s로 세워짐) 진단 1항목 추가 — 실제 카메라가 당겨 보이는지는 WebGL 컨텍스트가 있어야 해 여느 3D 시각 효과처럼 실기 확인 몫.

**웹에서 검증된 순서대로 3D 트랙이 가져간다**(SAGA-DESIGN §5). 5.1·5.2·5.8① 이 첫 실험 묶음.

## 6. 그래픽·에셋 (SAGA-DESIGN §6·§7 적용분)

### 6.1 이 판의 렌더 두 겹
- **2D 구면 투영**(`village-view.js`, 기본 화면의 절반): 후처리 없음. 적용할 것은 **팔레트 스냅**(스프라이트·타일 색을 §6.3 팔레트로, 로드 후 캔버스 1회)과 §5.8 반응만. SSAO·비네트·톤매핑은 **넣지 않는다** — 캔버스 구면 왜곡 위에 화면 공간 효과를 얹으면 가장자리에서 어긋난다.
- **3D**(`village-view3d.js`, three r169): SAGA-DESIGN §6.1 그대로 — `MeshToonMaterial` 3단 램프 + 뒤집힌 헐 외곽선(1.03), ACESFilmic·노출 1.0, 안개는 **기본 꺼짐 유지**(사용자 결정) — 켜면 near 30/far 320·색은 `FOG_COLOR` 바이옴별. 그림자는 high 만(기존). 초목 InstancedMesh 8종 유지·상한 2000. 픽셀 비율 상한 1.5(`QUALITY_PRESET` 에 이미 있음).
  **재질(2026-09-17 땅·소품, 2026-09-19 배우 외곽선 + §10-Q3 실사 병합으로 마저)** — `js/toon3d.js`. 땅 배경판·타일 8종(물 제외, `initTerrain()`), `asset3d.js`의 `delam()`·`primitive()` 모두 `world3d.toon`(기본 1) 손잡이로 Toon/Lambert 선택. 물(`waterMaterial()`, HDRI 반사 PBR)은 여전히 안 건드림 — GLB 파이프라인을 안 타는 별도 절차 재질이라 툰 대상이 아니다. `/realistic/`(탑성 등 사진측량)은 **§10-Q3(2026-09-18 확정)대로 툰이 켜져 있으면 같이 덮는다**(`toonifyRealistic()`, 새 함수 — 꺼져 있을 때는 예전처럼 PBR 그대로, Lambert 로 낮추지 않는다). **외곽선은 배우(주민·NPC·짐승)만 붙였다** — 땅은 여전히 안 건다(`InstancedMesh` 격자 전체가 테두리로 뒤덮일 위험은 그대로 유효). `toon3d.addOutline(root, width)`(뒤집힌 헐: 메시마다 1.045배 부풀린 BackSide 복제, SkinnedMesh 는 같은 skeleton 에 재바인딩) + 새 손잡이 `world3d.outline`(기본 1, 툰이 꺼지면 같이 꺼진다) — `asset3d.js`의 `loadHeroRecipe().assemble()`(사람 — 주민+NPC 공용 경로) 과 `buildGeneric()`의 `kind==='animal'` 분기 두 곳에서 부른다. **검증** — `node -c` 통과, `_test.html`에 진단 4항목(OUTLINE_ON 이 TOON_ON 을 따라가는지 · 메시 복제·BackSide·배율 · SkinnedMesh 도 skeleton 공유 · 손잡이 끄면 안 붙음)을 실제 THREE(r169, 이 파일이 로드해 둔 라이브러리) 객체로 추가 — 렌더 결과(실제로 검게 테두리가 보이는지)는 화면이 있어야 해 실기/헤드리스 확인 몫. **후처리 자동 끔(§8-6)은 이 판엔 해당 없음** — `post3d.js` 자체가 없다(별도 렌더 타깃 합성 패스 없이 `renderer.render()`를 바로 씀).
- **주의 — 구면 투영과 3D 카메라**: 2D 의 굽은 지평선 감각을 3D 에서도 내려면 `camTiltMix`(기본 1, 3/4 부감) 를 낮추는 게 아니라 **원경 안개·하늘 그라디언트로 지평선을 밝게** 처리한다. 2D/3D 토글 시 카메라 중심·줌이 같은 타일을 가리켜야 한다(진단 `camPose` 항목 유지).

### 6.2 아트 바이블 적용
- 스타일 하나: 스타일라이즈드 로우폴리 + 툰 램프. **탑성(`BLD_REAL`, Renvylle Castle 사진측량 등)도 2026-09-19부터 §6.1 재질 손잡이로 같이 덮인다**(§10-Q3 확정분 코드분) — 재질은 툰으로 통일됐지만 **텍스처 자체의 팔레트 스냅(실루엣만 남기기)은 아직**이다(`tools/asset-forge/palette.py`, §6.4 몫) — 사진측량 원본 diffuse 맵을 그대로 쓰므로 색이 §6.3 판별 팔레트와는 안 맞을 수 있다.
- 스케일: 사람 1.7 기준 `SCATTER_H` 표를 전부 재검(눈대중 값이라 "규모 어색" 실기 지적 가능성).
- 빛: 키 NW45°/55°, 시간대 4 표는 밝기만 바꾼다(방향 고정).
- 실루엣: 짐승 11·인물 조합·정령(§5.5)은 128px 축소 검사.

### 6.3 판별 팔레트 초안 — 바이옴 5 × 기본 8 (명·암 2단은 L ±12% 자동 → 24색)
기존 타일색(`TILES`: grass #63b04a·path #cbab74·sand #ecdfb0·water #4fbcda·stone #a9a7a2·rocky #8f9280)과 `FOG_COLOR` 를 앵커로 잡았다. 스냅 스크립트(`tools/asset-forge/palette.py`, SAGA-DESIGN §7.2)에 그대로 넣는다.

| 바이옴 | 땅 | 길 | 잎 | 줄기 | 물 | 돌 | 강조 | 하늘/안개 |
|---|---|---|---|---|---|---|---|---|
| green | #63b04a | #cbab74 | #3f8f3a | #6b4a2e | #4fbcda | #a9a7a2 | #e85a6a(열매) | #8fc7e8 |
| meadow | #86c45a | #d6bb85 | #9bd25c | #7a5a35 | #5cc7e2 | #b5b3ae | #f2b3d1(꽃) | #bfe0a8 |
| dark | #3a5a3c | #6f6350 | #24422a | #3c2a1c | #2f6f80 | #6b6d68 | #b8e04a(이끼빛) | #445a48 |
| mushroom | #5f7a58 | #8f7a5a | #7a9a6a | #5a3a2a | #4a8a8a | #7f8a80 | #d95f8a(버섯) | #5f7a68 |
| rocky | #8f9280 | #b0a88f | #6a8a5a | #5a4a3a | #5fb0c8 | #7c7a72 | #e0b040(광석) | #9a988a |

공통 UI 4: 종이 #f4ead6 · 먹 #33383f · 금 #e0b040 · 위험 #d9534f. 폰트 본문 1 + 숫자 1(SAGA-DESIGN §6.4).

### 6.4 에셋 변형 배가(§7.2) — 이 판 계획
- `palette.py`: 나무 5종(계절 갈래 포함) × 바이옴 5 → 25변형, 꽃·버섯·바위 동일. 정점색 스냅, 원본은 `assets/models/`, 출력 `assets/generated/`(§10-Q4 커밋 방식).
- `kitbash.py`: 캠프 천막·우물·평상·등롱 조합으로 **작은 마을 3종**(캠프 A/B/폐허 야영). 주민 집 외형 3종(집 GLB + 지붕 색).
- `procgen.py`: 돌무리·꽃 원(§5.5 퍼즐)·비석(§5.3)·조개 길 타일.
- `tilegen.py`: 잔디·흙·모래·돌·눈 5 타일 × 바이옴 팔레트(3D 바닥 트라이플레이너, 2D 는 그대로).
- `spritegen.py`: 정령 아이콘 60(바이옴 12 × 5 형태 변주), 번들 6 아이콘, 하트·목표판 UI.
- 우선 교체 대상(코드가 그리는 것, §2-6): 캔버스 상자·우편함·게시판(`draw*`)·2D 짐승 이모지 4종(늑대·토끼·오리·뱀)+성간충·주민 초상 카드. 사람 손그림 필요한 것: 없음(전부 스크립트로 가능).

## 7. 버그·안정화

1. **완료(2026-09-17)** — 공사 진단 4 FAIL(262/266, 세 세션째 원인 미확인이던 것) + 덤으로 발견한 날짜 의존 FAIL 2개(가방·판매, 행사). 지금 267/267(세 번 동일). 원인·수정은 `HANDOFF.md` 2026-09-17 절 참고 — `terrain.js`는 무죄였고, `_test.html` 자체의 테스트 위생 문제였다.
2. **실기 확인 대기 목록**(HANDOFF §42~§46-5 에서 모음, 전부 미확인): 효과음 20(`_sfxcheck.html`)·BGM·실내 3D 억제·회전 보간·개구리/뱀·손짓 몸짓·포자괴물/보스방/폐허 아치·안개 옵션 기본 꺼짐·**주민 5 3D(`residentMeshCount()===5`)**·우주기지 자리/소품 11·접수대↔배달원·탑성 폐허·정자·캔버스 상자·배달원 우주복·우주기지 건물 6 규모·성간충 40%. → 한 번에 몰아 확인, 결과는 `HANDOFF.md` 에 날짜로.
3. **성능 경고(누적)**: 고정 콘텐츠(NPC·목적지)는 거리 컬링이 없다. 목적지·사건 노드(§5.5·§5.7)를 늘리기 **전에** "거리 기반 활성화(0~30 High / 30~70 Medium / 70+ Hidden)" 를 NPC·프롭에 실제로 건다. 짓기 전에 먼저 잰다(프레임 시간, 폰 medium 기준).
4. SAGA-DESIGN §8 공통: `window.onerror` 링버퍼 50 → `_admin.html` 오류 탭(**완료 2026-09-17** — 새 `js/errlog.js`, `index.html` 맨 첫 스크립트, `_test.html` `push()` 상한 진단 1항목) / 세이브 `v` 마이그레이션(**완료 2026-09-17** — `core.js` `SAVE_VERSION`/`MIGRATIONS`/`migrate()`, 예전 `parsed.v !== 1` 하드 체크가 다섯 판 공통 지뢰였다) / `tools/precheck.sh`(다섯 판 공통, 이미 있고 sw.js 버전 경고도 추가됨) / 포스트 실패 시 후처리 자동 끔(이 판은 `post3d.js` 자체가 없어 해당 없음).
5. **완료(2026-09-18)** — 진단 구멍 추가: `data-village.js`에 `weatherOverride`+`_setWeather(v)` 신설, `weatherOf()`가 `knob()`보다 먼저 이걸 본다(`town.js` `_setStar`·`turnip.js` `_setOpen`과 같은 결 — `knob()`은 자가진단이 안 읽어서 별도로 뚫었다). `node -c` 통과. 실제 축제·행사 진단 항목 추가는 Phase 4(달력, 미착수)와 맞물려 있어 이번엔 구멍만.
6. **완료** — `sw.js` VERSION 규칙(§2-9) 위반 검사는 루트 `tools/precheck.sh`(다섯 판 공통)에 이미 있다(js 고치고 sw.js 그대로면 WARN). 이 판 전용으로 새로 만들 것 없음.

## 8. 로드맵

옛 작업지시서 PHASE 1~8(분석→GLB→넓은 숲/바이옴/호수/강/폭포/마을/동굴→짐승/NPC/상호작용/채집/상자/부탁→날씨/밤/파티클→세로·가로·safe area·터치→LOD/인스턴싱/풀/캐시→QA)은 **2026-08-25~09-14 사이 전부 지어졌다**(§3). 아래가 새 순서다.

| Phase | 내용 | 후보 | 완료 조건 |
|---|---|---|---|
| **0 안정화** | 공사 FAIL 4 원인·수정(**완료**), 실기 확인 몰아서 1회, 오류 수집(완료), precheck(완료), 날씨 구멍(**완료 2026-09-18**), 거리 활성화 실측 | §7 | 267/267 세 번 동일(달성), 실기 목록 전부 닫힘(남음) |
| 1 루프 | 일과판·마무리 카드·채집 손맛 ①(**전부 코드분 완료** — 침구 트리거·별5 평가·낚시 줌인 모두 2026-09-18에 마저 닫음) | 5.1·5.2·5.8① | 표준 A·B·C 진단 통과, 사용자 "할 게 보인다" — **실기 확인만 남음** |
| 2 마을 변화 | 번들 6·시설 6·관계 하트·평가 별 5·이사 조건 | 5.3·5.4·5.8② | G·F 진단, 번들 1개 완성 실기 |
| 3 세계 | 격자 규칙·정령 60·택배 사슬(폐허 목적지·3택) | 5.5·5.7 | E·D 진단, 거리 활성화 켠 상태에서 폰 medium 30fps |
| 4 달력 | 축제 놀이 8 | 5.6 | 어드민 "때" 로 8일 전부 1회 |
| 5 그래픽 통일 | 툰 재질·외곽선·팔레트 스냅·변형 배가·코드 그림 교체 | §6 | 스타일 혼재 0, 128px 실루엣 검사 |

각 Phase 끝에 `README.md` 머리 절 덮어쓰기 + `HANDOFF.md` 날짜 항목 1개. 한 Phase 안에서도 "절대 하지 말 것" 원칙대로 후보 하나씩.

## 9. 검증

```
구문   node -c js/*.js  (인라인은 _test.html)
진단   chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom http://127.0.0.1:8793/_test.html | grep -o "RESULT [0-9/]*"
어드민 _admin.html?selftest  ·  때 탭으로 날짜·행사·시간대 강제
데모   _demo.html#<장면>  ·  소리 _sfxcheck.html
```

- 씨앗 mulberry32(20260824) 고정. **세 번 돌려 한 줄도 다르지 않은지.** 시각 기반 값은 결과 문구에 넣지 않는다.
- 시각 의존 축 붙들기: 계절 `VD.season` 갈아 끼움, 별똥별 `_setStar`, 순무 장 `_setOpen`, 날씨는 §7-5 구멍(추가 예정). 진단은 게임 루프를 켜지 않고(`DG_NO_LOOP`) `update(dt)` 를 직접 굴린다.
- 개발 중 스크린샷·CDP 금지, 서버는 실기 요청 때만(루트 CLAUDE.md). 크롬을 띄웠으면 그 turn 에 `taskkill //F //IM chrome.exe`.
- 커밋: `git commit -F <메시지> -- <손댄 경로>` 곧바로. `sw.js` VERSION 동반.

## 10. 열린 질문(사용자 결정)

- **Q1 목적지 성격 재확인 — 확정(2026-09-18)**: 그대로 따른다 — 폐허·캠프 2 도 우주기지처럼 **늘 있는 고정 마을** 결(2026-09-11 결정)에 맞춘다.
- **Q2 현대 목적지 자산 — 확정(2026-09-18)**: 권장안 ② 채택 — `kitbash.py` 로 Space Kit + Fantasy Town 조합해 "근미래 정류장" 으로 대신한다.
- **Q3 탑성 폐허 PBR — 확정(2026-09-18)**: 권장안 채택 — 사진측량 스캔도 툰 재질로 덮어 스타일을 맞춘다.
- **Q4 생성 에셋 커밋 — 확정(2026-09-18)**: `assets/generated/` 산출물을 커밋한다(사가블로와 같은 이유 — GitHub Pages 가 정적이라 빌드 단계 없이 바로 서빙하려면 산출물이 필요). SAGA-DESIGN §10-Q4 는 아직 저장소 전체 답이 안 잡혀 있어 이 판 국지적 답으로만 적용.
- **Q5 보스방 — 확정(2026-09-18)**: 지금은 넣지 않는다 — §5 우선순위 밖으로 미루고, 필요해지면 퍼즐형으로 다시 검토.
- **Q6 관계 하트가 이사 조건을 대체 — 확정(2026-09-18)**: 둘 다 반영 — 이사 확률은 기존대로 마을 평가(방치)가 좌우하고, 그 안에서 후보를 고르는 기준만 `friend` 에서 `heart` 로 바꾼다(§5.4).
- **Q7 사고(史庫)가 꽃·열매도 받을지 — 확정(2026-09-18)**: 안 받는다 — 꽃·열매는 판매·선물 갈래로만 남긴다. 사고 번들은 곤충·물고기·화석·조개 4갈래 고정(§5.3).
