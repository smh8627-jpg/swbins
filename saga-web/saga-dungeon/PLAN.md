# 사가블로(saga-dungeon) PLAN — 정본

## 0. 읽는 법

- 이 파일은 **설계·규칙·로드맵**만 든다. 날짜 달린 세션 기록은 여기 쓰지 않는다 — 이 폴더 `HANDOFF.md`(이 판 이력, append-only)와 루트 `../../SAGA-HANDOFF.md`(시리즈 이력)에 쓴다.
- 상위 문서: `../../SAGA-DESIGN.md`(재미 표준 8·참고 게임 카탈로그·그래픽·에셋·문서 규칙). 이 PLAN 의 §4~§6 은 그 문서의 §3~§7 을 이 판에 적용한 것이다. 겹치는 내용은 여기 다시 쓰지 않는다.
- 크기 상한 90KB(사가블로 예외, 다른 판은 70KB). 목차는 `grep -n "^## \|^### "`, 필요한 절만 `sed -n`.
- 현재 상태(자가진단 수·커밋·에셋 목록)는 `README.md` "어디까지 왔나"·`assets/ASSET_LICENSES.md`. 이 PLAN 의 §3 표는 **시스템이 있는지·검증됐는지**만 적는다.
- 옛 PLAN 의 절 번호(§1~§60, §28-1~§28-8)는 `HANDOFF.md` 와 코드 주석에 남아 있다. 새 절 번호와 다르다 — 코드 주석의 "PLAN §28-8" 류는 HANDOFF 의 같은 번호 절을 가리킨다.

목차: §1 정체성 · §2 반드시 지킬 것 · §3 현재 시스템 지도 · §4 재미 진단 · §5 게임성 확장(후보 8) · §6 그래픽·에셋 · §7 버그·안정화 · §8 로드맵 · §9 검증 · §10 열린 질문

---

## 1. 정체성

- **한 줄**: 역사 인물 105명을 부대로 이끌고, 마을 104개가 이어진 들판과 여섯 결의 던전을 도는 **탑다운 3D 액션 RPG**. 디아블로 2의 규칙(소켓·룬워드·정예 접사·난도·역참·결사·행상·투전·연단·내구·감정·창고·투장·고유)을 거의 다 옮겨 왔고, 디아블로 4의 오픈월드(이어진 마을·길·필드 이벤트·수집품)를 얹었다.
- **원작 오마주**: 디아블로 2·4. 표시 이름은 전부 가명(루트 CLAUDE.md 이름 정책). 원작 에셋 없음.
- **인물이 되는 것 — 서명 무예(署名武藝)**: 다섯 판 공통 인물 105명이 이 판에서는 **인물마다 하나씩 다른 서명 무예**(`js/data-hero-skill.js` `SIG`, 105/105 완료)로 존재한다. 직업(무기가 정한다) 트리는 공유, 서명 무예는 인물 고유. 부대(최대 5)의 2번째 인물은 동행(同行)으로 곁에서 싸운다.
- **이 판만의 차별점**: ① 부대 전투력이 곧 몸(`hero.partyPower`) — 인물을 키우면 더 깊이 내려간다. ② 인물은 던전에서 온다(출사표 3명 + 보스층 3층마다 1명 합류). ③ 은사(恩賜)는 회차 한정 — 층을 내려갈 때 셋 중 하나. ④ **퓨전 선행 판**(2026-09-10 창작 방향, `SAGA-HANDOFF.md` "창작 방향"): 과거×현대×미래 캐릭터·건물·무기를 다섯 판 중 이 판에서 먼저 실험한다. 첫 반영이 원소 전자(電磁, `emp`)와 여덟째 무예 갈래.
- **플레이어 판타지**: "내가 고른 인물들이 내 손끝에서 하나씩 다른 기술을 뽐내며, 걷다 보면 뭔가 나오는 넓은 세계를 내려간다." 지금은 이 판타지의 재료(인물·무예·세계)는 다 있는데 **루프와 손맛이 재료를 못 살린다**(§4).

## 2. 반드시 지킬 것 (이 판 고유 — 루트 CLAUDE.md 공통 규칙은 링크만)

### 2.1 구조 불변식
- **판정(`dungeon.js`)과 화면(`dungeon-view.js` 2D·`dungeon3d.js` 3D)은 갈라져 있다.** 3D 는 화면 계층으로만 얹는다. 판정 파일에 three.js 를 부르지 않는다. WebGL 이 없으면 조용히 2D 로 돈다.
- **`strike()` 가 모든 피해의 단일 통로**다(평타·무예·강공격·투장 무예·서명 무예·동행·분신). 새 피해 경로를 만들지 않는다 — 어그로 각성·hitstop·콤보가 전부 여기 걸려 있다.
- **모양(shape)은 열 가지뿐**: swing·nova·bolt·dash·buff·heal·curse·summon·chain·passive(`applyShapeSkill`). 무예를 늘릴 때 모양을 늘리지 않는다 — 모양 하나마다 `dungeon.js` 에 동작을 새로 써야 한다. curse·heal·buff·summon 은 `el` 을 읽지 않는다(붙이지 말 것).
- **던전 내부는 방 단위 로그라이크로 유지**(`run.room` 단일 객체, `seedOf(floor,roomIdx,theme)`, `isBoss = roomIdx>=roomTotal-1`). 방 개념을 없애는 A안은 반려됐다 — 방-방 전환은 문별 통로(동쪽 벽 `doorCorridorUnits()`)로 위장 전환한다. 자동 순회(`auto.js`)는 통로를 건너뛰고 `goRoom()` 직접 호출.
- **마을↔필드↔마을은 진짜 이어진 하나의 세계 좌표계**(옛 §28-8 A안). `ctx.anchor`(마을 로컬 원점의 세계 좌표)·`ctx.noRoom`(열린 들판) 이 없으면 옛 동작과 100% 같아야 한다(던전 방은 이 필드를 안 넘긴다). 마을 간 순간이동은 없다 — 되돌리지 말 것. `travel()`·통로 클램프 표(`corridorsFor` 등)는 은퇴했다.
- **그림·판정·자동지도가 같은 산식을 본다**: 마을·들판은 **고정 세계 지도**(`world-map.js`, §5.12) 하나 — `WM.pieces/clutter/info(세계 칸)` 를 `dungeon3d.fieldJobChunkWorld`(그림)·`dungeon.fieldBlockedAt`(ctx.town, 충돌)·`town.worldKindAt/worldPropsAt`(지도)이 **같은 배열로** 읽는다. 칸 내용은 세계 칸 좌표만의 함수다 — 서 있는 마을·앵커·그래픽 밀도로 바꾸지 않는다(바꾸면 발밑이 바뀌어 낀다). 던전 방 둘레는 옛 방 씨앗(`field3d.seedOf`) 그대로.
- `town.js` `exits` 데이터(갈대나루 3·자작재 2·소금벌 2)는 자가진단이 지킨다. 길은 `exits` 와 별개인 `ROAD_SEGMENTS` 선분이다.
- `run` 은 세이브에 안 남는다(회차는 앱을 끄면 사라진다). `core.save.dungeon` 은 메타(best·runs·kills·clears·mode)만.

### 2.2 수치 불변식(바꾸면 자가진단·배치가 같이 깨진다)
| 상수 | 값 | 어디 | 왜 못 바꾸나 |
|---|---|---|---|
| `ANCHOR_DIST` | 6400 | town.js | 마을 발판+필드 반경 양쪽(4640)+흔들림 2×600 을 넘어야 안 겹친다 |
| `TOWN_JITTER` | 600 | town.js | 위 여유 안에서 격자 이탈. 최소 거리 실측 5503 |
| `GEN_GRID_RADIUS` / `GENERATED_TOWN_COUNT` | 7 / 100 | town.js | 후보 221칸 중 100(45%). 앵커 최대 46520 < `WORLD_LIMIT` |
| `WORLD_LIMIT` | 60000 | dungeon.js | 이 밖은 못 나간다. 테스트 좌표가 이 경계와 얽히지 않게 |
| `TOWN_SAFE_R` | 1300 | dungeon.js | 스폰·추격 둘 다 막는 안전지대 |
| `TOWN_ROAD_BAND` | 70 | field3d.js | 길 폭. 유적은 길에서 180~400 떨어진다 |
| `CHUNK` / `fieldRadiusUnits()` | 200 / 등급별 2~6칸 | field3d·dungeon3d | AUTO 등급이 R 을 바꾼다 — 배치는 build 시점 R 을 고정해 쓴다 |
| `TILT()` / FOV / 던전 dist 배수 | 0.62 / 46° / 1.05 | dungeon3d.js | 좁히면 몹이 화면 밖으로 밀린다(실기 반려). 정지 화면으로 카메라를 확정하지 않는다 |
| `DESK_SCALE` | 2.0 | town.js | 데스크톱 가로만. 폰은 `BASE_W×BASE_H`(560×380) 그대로 |
| 마을 소품 최소 간격 | 95 (스폰 기준) | town.js `placePoints` | 못 찾으면 **건너뛴다**. 억지로 겹치게 두던 옛 동작으로 되돌리지 말 것 |
| 진단 씨앗 | mulberry32(20260824) | _test.html | three 는 씨앗보다 **먼저** 실린다(UUID 로 `Math.random` 76번) |

### 2.3 작업 규칙(이 판에서 실제로 밟아 정한 것)
- 파일을 늘리거나 고쳤으면 **`sw.js` VERSION 을 같이 올린다**. 새 js 파일은 `sw.js` `SHELL` 목록·`index.html`·`_admin.html`·`_demo.html` 네 곳에 다 넣는다(`data-hero-skill.js`·`starter.js` 가 빠졌던 적 있다).
- **RNG 순번 함정**: 밑감·적·데이터 표를 늘리면 `Math.random()` 소비 순서가 밀려 무관한 자가진단이 깨진다. 새 판정에 무작위가 필요하면 `core.hash2(고정 입력)` 순수 해시로. 자가진단 항목은 **파일 맨 끝에** 추가하고 앞선 테스트 상태(`room.enemies=[]`·`room.decor=[]`·`uniq:false`)에 기대지 않게 격리한다.
- `_test.html` 은 3D 렌더를 켜지 않는다(`DG_NO_DRAW`). 화면 층(`dungeon3d.js`)이 세계 좌표를 다루는 코드는 진단 통과만으로 안심하지 않는다 — 실기 확인 항목으로 §7 에 적는다.
- 에셋: CC0 만, 출처는 `assets/ASSET_LICENSES.md`. **Quaternius 최신 팩 상당수가 QAL(재배포 금지)** — 이름만 보고 받지 않고 팩 페이지에서 낱개 확인. itch.io 는 curl 로 못 받는다 → OGA 미러·poly.pizza 직접 URL. `npm install fbx2gltf` 가 이 환경에서 실제로 돈다. 승인 제외 목록(alien·alpaking·armabee·bunny·cat·chicken·fish·pigeon류 — 이 판 결에 안 맞음, 2026-09-09)은 되살리지 않는다.
- "코드로 그리지 말고 에셋으로"(사용자 지시 2026-08-28, 다섯 판 공통): 도형을 조립해 그리던 것(3D 소품·건물·인물·초상·카드·아이콘)은 실제 에셋으로 갈아 끼운다. 되돌림 길(옛 그림)은 손잡이로 남긴다. 결이 안 맞는다는 이유로 안 하지 않는다(품질 우선, 사용자 확인). 못 구하면 "막혔고 대신 이것" 으로 보고한다.
- 3인칭 카메라·등신 비례 토글·그림 양식 토글은 지웠다(`diablo` 하나로 고정). 되살리지 않는다.
- 헤드리스 크롬은 PID 로만 죽인다(`taskkill //F //PID`). 이미지 이름으로 죽이지 않는다(사용자 브라우저를 끊은 적 있다).

## 3. 현재 시스템 지도

상태: ○ 완료·검증 / △ 완료·실기 미확인 / × 없음. 진단 = `_test.html` 항목군 이름.

| 시스템 | 주 파일 | 상태 | 진단 |
|---|---|---|---|
| 부대·인물 성장·합류(출사표 3, 보스층 합류, 중복→승급) | game.js·hero.js | ○ | 출사표·합류·보스 보상·도감 완성 |
| 던전 회차(층·방·문·은사·역참 5층·결사·난도) | dungeon.js·data-dungeon.js | ○ | 진입·은사·탈출·역참·결사·난도 |
| 층 테마 6(고분 1·폐성 6·산채 11·수궁 16·지옥문 21·천계 26) | data-dungeon.js | ○ | 월드맵 잠금 |
| 문별 통로(방-방 위장 전환)·계단 통로 테마 | dungeon.js·field3d.js | △ | 통로 진입점 |
| 직업 5(궁장·무장·책사·도독·방사, 무기가 정함)·무예 8갈래×3=24/직업=120 | data-skill.js·skill.js | ○ | 무예 군 |
| 모양 10·원소 7(phys·fire·cold·lit·pois·chi·emp) | dungeon.js·data-elem.js | ○ | 원소·저항 군 |
| 서명 무예 105/105(쿨다운만, MP 없음, F 슬롯과 별개 슬롯) | data-hero-skill.js | △ | 서명 무예 3항목, Node 105/105 발동 |
| 투장 10벌 + 투장 전용 무예(3점, F 키) | data-set.js·item.js | △ | 투장·투장 무예 |
| 고유 31·등급 5(상품~전설, mul 1.0~2.15)·접사·소켓·보석 6·부문·부문어·내구·감정·창고·요대 4×4·단약 | data-item.js·data-gem.js·data-unique.js·item.js·potion.js | ○ | 장비·gem·rune·고유·내구·창고·감정·요대 |
| 행상·투전·연단 | vendor.js·forge.js | ○ | 행상·투전·연단 |
| 강공격(2.6×, cd 1.3)·회피(0.22s 무적, cd 0.9) | dungeon.js·dungeon-view.js | △ | (진단 없음 — §9) |
| 손맛 1차: hitstop(dt×0.08)·콤보(1.6s 창, 3/6/12)·크리 흔들림 3 | dungeon.js·fx3d.js | △ | (진단 없음) |
| 어그로(잡졸 230·원거리 300·무리 110, 보스 상시) | dungeon.js | △ | 어그로 4항목 |
| 동행(부대 2번째, 배율 0.2~1.5×0.55, 적이 안 때림) 3D+2D | dungeon.js·dungeon3d.js·dungeon-view.js | △ | 동행 4항목 |
| 보스 10·패턴 4종(무기별) | data-enemy.js·dungeon.js | △ | — |
| 몬스터 135(짐승 95 + 사람형 + 공룡 6 + 괴물 5), 티어 1~4 | data-enemy.js·asset3d.js | △ | Node vm 구조 검사만 |
| 오픈월드: 손 마을 4 + 절차 100, 세계 좌표, 활성 마을 자동 전환, 포그오브워 | town.js·dungeon.js | △ | 세계 앵커·마이그레이션·seen |
| 길(격자 이웃 전부)·길목 발견거리(45%, 금14·공적1)·유적 수집품(30%, 도감 relics, 금20·공적4)·탐험 문턱 [50,150,350,700,1200,2000](금25·공적2)·마을 첫 방문 보상 | town.js·ui.js | △ | 탐험 보상 1항목 |
| 필드 이벤트: 정예가 지키는 보물·방랑 상인(60s) | dungeon.js·town.js·ui.js | △ | — |
| 퀘스트: 메인·지역·이벤트(정적 1회) + 현상판(무작위 반복) + 지역 사연 사슬 아홉(§5.14) | quest.js·data-quest.js | ○(사연 △) | 퀘스트·지역 사연 4 |
| 도감: 인물·펫·지역·유적, 완성 보상(1200금·50공적) | ui.js·core.js | ○ | dex 군 |
| 자동 순회(🤖) | auto.js | ○ | 자동 군 |
| 자동지도 M(지형+소품 점)·코너 미니맵(지형, 플레이어 중심) | minimap.js | △ | 미니맵 3항목 |
| 3D: 방·들판·인스턴싱 초목·가림 페이드·품질 AUTO(1.5s 쿨다운)·SSAO·후처리 | dungeon3d.js·field3d.js·field-instance.js·post3d.js·ssao3d.js·fx3d.js | △ | 3D 회전·줌 항목 |
| 장비→겉모습(무기·투구·갑주 GLB 2)·외모 커스텀(QRPG 6) | dungeon3d.js·asset3d.js | △ | 5항목 |
| 소리(WebAudio 절차 생성)·진동 7종 | sfx.js | ○ | 진동 4항목 |
| 설정 시트(효과음·음량·진동·그래픽 품질) | ui.js | △ | 1항목 |
| 계정(프로필별 세이브 키) | account.js | ○ | — |
| 어드민(3D 손잡이·QA 프리셋·서명 무예 시험 발동) | admin.js·_admin.html | ○ | ADMIN 12/12 |
| 부팅 예외 배너·프레임 예외 캐치·build 실패 복구 | game.js·town.js·dungeon3d.js | △ | — |
| **목표판·세션 카드·일일/주간·죽음 비용·월드 보스·낮밤** | — | × | — |

자가진단 현재 299/299(3회 동일), ADMIN 12/12. `sw.js` `dungeon-v0.115.0`.

## 4. 재미 진단 (SAGA-DESIGN §3 표준 8)

| 표준 | 상태 | 근거 |
|---|---|---|
| A 목표판 | × | HUD 에 "지금 뭘 하나" 가 없다. 퀘스트는 시트를 열어야 보이고(`herald` NPC), 메인·지역·이벤트는 정적 1회라 금방 끝난다. 현상판만 반복 |
| B 세션 카드 | × | 탈출 시 노획물 확정만 토스트. 도감 진척·다음 할 것이 안 나온다 |
| C 손맛 | △ | hitstop·콤보·크리 흔들림·강공격·회피는 있다(1차). 없는 것: 평타 흔들림(크리만), 타격음 라운드로빈(큐 1종), 피격 플래시 규격, 숫자 팝 시간 규격. 회피·시전 모션은 3D 만, 2D 는 사인파 걷기뿐 |
| D 선택 3택 | △ | 은사 3택(`rollBoonChoice`)이 있지만 축 구분·중복 금지·거절 보상이 없고 `BOONS` 표가 스탯 % 위주다. 무예 8갈래 120개는 "다 같은 축" — 모양×원소 조합 채우기라 고르는 이유가 없다(사용자도 "그냥 모방" 이라 멈춤) |
| E 발견 밀도 | △ | 길목 발견거리 45%·유적 30%·필드 보물·상인은 있다. 그러나 마을 사이 6400 을 걷는 동안 상호작용 종류가 3(발견거리·유적·로머) 이고, 던전 방 안은 항아리·함정·상자뿐. 60m 격자 빈칸 측정 자체가 없다 |
| F 실패·회복 | × | 죽으면 회차 종료·노획물 전부 소멸. "죽어도 남는 것" 을 화면이 말해 주지 않는다. 결사(하드코어)만 극단으로 있다 |
| G 성장 가시화 | △ | 장비→겉모습(무기·투구·갑주 2종), 등급색은 있다. 전설 이펙트·서명 무예 발동 연출 차이·부대 성장의 시각 변화는 없다 |
| H 돌아올 이유 | × | 일일·주간·시각 기반 이벤트 없음(이 판엔 낮밤·날씨 자체가 없다) |

**가장 큰 구멍 3개**
1. **루프 부재(A·B·F)**: 콘텐츠(인물 105·무예 120·몬스터 135·마을 104)는 다 있는데 "지금 이걸 하고, 죽으면 이걸 잃고, 끝나면 이걸 얻었다" 가 화면에 없다. 사용자가 "재미없다" 고 느끼는 첫 원인.
2. **선택의 동질성(D)**: 갈래를 기계적으로 늘린 무예 120 이 서로 구별되지 않는다. 축복 3택으로 "이번 회차 빌드" 를 만들게 재해석해야 인물·무예가 살아난다.
3. **미검증 누적(§7)**: 실기 확인 대기 항목이 30개 넘게 쌓였다. "버그가 많다" 는 인상의 실체.

## 5. 게임성 확장 — 후보 8 (상세 설계)

각 후보의 9필드: 참고 게임 / 채우는 표준 / 왜 / 메커니즘 / 수치 / UI·조작 / 세이브·마이그레이션 / 진단 문안 / 범위·우선순위·불변규칙 충돌. 우선순위는 §8 로드맵과 같다.

### 5.1 축복(祝福) 3택 — 은사를 회차 빌드로 재해석
- **참고**: 하데스 축복 조합, PoE 스킬+서포트, 슬레이 더 스파이어 유물. **표준**: D·G(부분 A).
- **왜**: 은사 3택이 이미 층마다 걸리는데 표(`BOONS`)가 스탯 % 라 "뭘 골라도 같다". 무예 120·서명 무예 105 라는 재료를 **회차 안에서 조합**하게 만들면 갈래를 더 안 늘려도 선택이 생긴다.
- **메커니즘**: `descend()` 의 `rollBoonChoice()` 를 세 축에서 하나씩 뽑게 바꾼다 — ① **무예 축**(걸어 둔 무예 4칸 중 하나를 강화: 모양별 고유 효과 — swing 범위+25%, bolt 관통 1, nova 링 2회, dash 무적 +0.1s, chain hops +2, summon 수 +1, curse 지속 +40%, heal 즉시 +30%, buff 지속 +50%) ② **인물 축**(서명 무예 강화: 쿨다운 -25% / 위력 +40% / 발동 시 부대 전원 흔들림·플래시, 또는 부대 3~5번째 인물의 "그림자 서명" — 그 인물 서명 무예를 20% 확률로 평타에 얹음) ③ **세계 축**(원소 시너지 — 화+뇌 "폭발" 처치 시 반경 80 화상 / 빙+기 "결빙" 슬로우 중 피해 +30% / 독+전자 "부식" 저항 -20% / 물리+화 "작열" 콤보 6 이상에서 화상 등 7원소 조합표 12개). 희귀도 3단(일반 60%·희귀 30%·전설 10%), 같은 축 중복 금지, **거절** 시 금 30×층. 회차 최대 스택 8(3층마다 1 + 사당방 보너스). 축복은 회차 한정(기존 은사 규칙 그대로), 처음 본 축복은 도감 "은사첩" 에 등록(메타 진행 — F 표준의 "죽어도 남는 것").
- **수치**: 강화 배율은 위 값 고정, 전설 축복은 두 효과 합성. 원소 시너지 피해는 `strike()` 안에서 `run.boons` 를 읽어 한 번만 계산(새 통로 없음). 밸런스 목표: 8스택 회차의 DPS 가 0스택의 2.2배 이내(어드민 시뮬레이터로 잰다).
- **UI·조작**: 기존 은사 카드 3장 UI 재사용, 카드 위 축 아이콘(무예🗡·인물👤·세계🌐)과 희귀도 테두리색(등급색 TIERS 재사용). 거절 버튼 추가. HUD 오른쪽 위 축복 아이콘 띠(최대 8).
- **세이브**: `run.boons` 는 회차(저장 안 함). 메타 `save.dex.boons = {key:true}` 신설 — `freshSave()` 에 추가, `mergeDeep` 이 옛 세이브에 채운다(마이그레이션 코드 없음). `DEX_COMPLETE` 에 `boons` 갈래, 완성 보상은 기존 파이프라인.
- **진단 문안**: "축복 — 세 카드가 서로 다른 축이다(100회)" · "축복 — 거절하면 금이 층×30 만큼 든다" · "축복 — 화+뇌 시너지가 처치 시 반경 80 화상을 건다" · "축복 — 처음 본 축복은 은사첩에 남고 회차가 끝나도 지워지지 않는다". 전부 `core.hash2` 로 굴려 씨앗 무관.
- **범위**: `data-dungeon.js`(BOONS→3축 표), `dungeon.js`(rollBoonChoice·strike 시너지·applyShapeSkill 강화값), `ui.js`(카드·HUD 띠·도감), `core.js`(dex.boons). **우선순위 1**. 충돌: 없음 — 은사 회차 한정 규칙·strike 단일 통로·모양 10 유지. 무예 갈래는 더 늘리지 않는다(사용자 "그냥 모방" 정정).
- **구현(2026-09-18)**: `BOONS`(옛 14개 스탯% 은사)를 3축 36개로 **전부 교체**했다 — 무예 축 18(모양 9×common/legendary), 인물 축 6(cd·dmg 각 common/rare + legendary 2종), 세계 축 12(원소쌍 조합, common 6·rare 4·legendary 2). `rollBoonChoice()`는 축마다 하나씩 뽑아 구조적으로 축 중복이 없다(`axisPool`→`rollFromAxis`, 등급을 `core.hash2`로 굴리고 후보가 없으면 한 단계 물러선다 — `Math.random()`을 안 써서 `tryCatchPet()`과 같은 이유로 진단이 다른 자리의 난수열을 안 민다). `descend()`는 이제 `DD.isBossFloor()`(기존 "3층마다"와 같은 함수)일 때만, `run.boonPicks`가 8 미만일 때만 `rollBoonChoice()`를 부른다 — 매 층마다 뜨던 옛 방식을 대신한다. `pickBoon()`은 처음 고르는 축복을 `save.dex.boons`(은사첩)에 등록하고, 새 `rejectBoon()`이 금 30×층을 받고 넘긴다.
  - **무예 축**: PLAN 원문의 "무예 4칸 중 하나"가 아니라 **그 모양(shape)을 쓰는 무예 전부**에 건다(어느 칸인지 추적 안 함 — 후하지만 로그라이트 특성상 문제없어 그대로 감수). `applyShapeSkill()`의 아홉 분기 전부에 `boonVal()` 한 줄씩만 얹었다(swing→`swingRangePct`, bolt→`boltShotAdd`, nova→`novaExtraRing`, dash→`dashInvulnAdd`, chain→`chainHopsAdd`, summon→`summonCountAdd`, curse→`curseDurPct`, heal→`healBonusPct`, buff→`buffDurPct`) — 새 통로를 안 만들고 기존 `boonVal()` 범용 합산을 그대로 재사용했다.
  - **인물 축**: `castSigSkill()`에서 `sigCdPct`·`sigDmgPct`를 그대로 곱·감산한다. "부대 전원 흔들림·플래시"는 dungeon-view.js의 카메라 흔들림 변수가 모듈 로컬이라 못 건드려 기존 `ring` fx 연출로 대신했다(단순화, 실기 확인 대상). 그림자 서명(`hr_shadow_l`)은 새 `tryShadowSig()`가 평타 두 곳(들판 `stepFieldCombat`·방 `update`)에서 부대 3~5번째 인물의 `HS.sigOf()`를 확률로 얹는다.
  - **세계 축**: 12조합 전부 `strike()`/`kill()` 안에서만 계산한다("새 통로 없음" 그대로 지켰다) — 크리 전 판정(뇌빙 확정크리)·데미지 배율(결빙·빙염·부식)·크리 부가(빙인·폭기)·콤보 부가(작열·경혈)는 `strike()`에, 처치 반경 80 셋(폭발·부패·감전은 예외로 즉발 피해·저지)은 `kill(e, kind, dmg)`(호출 하나뿐이라 인자를 늘렸다)에 넣었다. 두 원소가 한 타격에 실렸는지는 새 `hasElemInHit(el, kind, edmg)`가 "타격 결 kind" 또는 "무기에 박은 보석 elemDmgOf()"로 판정한다.
  - **자가진단 전용 훅**: `DN._rollBoonChoice`(층 게이팅 없이 굴리기), `DN._forceElemDmg(v|null)`(원소 배합 강제 — 사가고 `weather.force()`와 같은 결, 실제 장비 세공에 기대면 세계 축 시너지를 결정적으로 못 검증한다).
  - **auto.js**: `BOON_SCORE`(옛 키 표)를 `boonScore(key)`(축·희귀도 점수)로 갈아엎었다. `_test.html`의 "자동 — 은사는 오래 버티는 쪽" 회귀도 새 키로 다시 썼다.
  - **잃은 것**: 옛 체계의 `hpPct`·`atkPct`·`moveSpdPct` 같은 순수 스탯 성장이 새 체계엔 없다(세 축 다 스탯%가 아니라 구조·시너지). 생존력 성장은 §5.2(유품)·§5.8(손맛)이 다른 방식으로 맡는 쪽으로 방향을 잡았다 — 실제 사망률 변화는 실기 확인 몫.
  - **밸런스 추정(실측 아님, §9 실기 확인 필요)**: 8스택 전형 조합(legendary skill×2~3 + hero dmg + world synergy 다수)을 손으로 어림하면 skillMul() 보너스 +20~30%(skillPct 합), 서명 위력 +25~65%, 세계 시너지 조건부 +15~30% — 그런데 **평타는 세 축 어디에서도 안 커진다**(새 축은 전부 스킬·서명·시너지에만 붙는다), 그래서 평타 비중이 큰 실제 플레이의 블렌디드 DPS 상승은 대략 ×1.4~1.9 로 어림된다 — 목표 ×2.2 이내에 여유 있게 들어오지만, 정확한 값은 `_admin.html` 시뮬레이터(§9) 실측이 필요하다.
  - **검증** — `node -c`(data-dungeon.js·dungeon.js·auto.js·dungeon-view.js·core.js·ui.js) 통과, `bash tools/precheck.sh saga-web/saga-dungeon` → PRECHECK OK. `_test.html`에 §5.1 진단 4항목 추가 + 기존 자동선택 테스트 갱신 — 헤드리스로는 안 띄웠고(이 판 규칙) 인라인 스크립트를 `new Function()`으로 구문만 확인했다. 실기 확인 남음: 위 밸런스 어림값 실측, "부대 전원 흔들림" 단순화가 화면에서 어색하지 않은지, 카드 UI(축 아이콘·테두리색)가 실제로 잘 읽히는지.

### 5.2 유품(遺品) — 죽음 비용과 회수, 죽어도 남는 것
- **참고**: 다크소울 소울 회수, 하데스(죽음→서사), 로그라이트 메타 진행. **표준**: F·B.
- **왜**: 지금은 죽으면 노획물 전부 소멸 + 아무 안내 없음. 긴장은 없고 허탈만 있다. 비용을 **회수 가능하게** 만들면 "다시 내려갈 이유" 가 생기고, "남는 것" 을 카드로 보여 주면 죽음이 진행이 된다.
- **메커니즘**: 사망 시 노획물(금·장비·재료)을 **유품**으로 그 층에 남긴다. 다음 회차에 그 층에 도달하면 유품 표식(기존 `MARKS` 결, 비석 GLB 재사용)이 서고 밟으면 회수 — 금 100%, 장비는 3점까지 선택(나머지 소멸). 회수 전에 다시 죽으면 옛 유품 소멸(1개만 유지). 사망 화면 = **세션 카드**(5.6 과 같은 카드): 남은 것(인물 경험·도감·공적·은사첩)·잃은 것(유품으로 남김)·다음 할 것 1개("N층 유품 회수"). 결사 모드는 유품 없음(원작 하드코어 그대로).
- **수치**: 유품 유지 1개, 장비 회수 3점, 회수 시 금 100%. 목표판(5.6) "지금" 줄이 유품 회수를 최우선으로 보여 준다. 사망→재도전 조작 3회 이하(카드 닫기→본영→굴혈).
- **UI·조작**: 사망 카드 1장(5초 자동 닫힘 아님 — 사용자가 닫는다, 단 조작 1회). 층 진입 시 "유품이 이 층에 있다" 토스트, 표식에 💀 이름표.
- **세이브**: `save.dungeon.grave = null | {floor, gold, items:[…], at}` 신설(`freshSave`, mergeDeep). 장비 객체는 `item.js` 의 직렬화 그대로(가방과 같은 모양).
- **진단 문안**: "유품 — 죽으면 노획물이 그 층 유품으로 남고 가방엔 안 들어온다" · "유품 — 그 층에 다시 닿으면 표식이 서고 밟으면 금 100%·장비 3점까지 돌아온다" · "유품 — 회수 전에 다시 죽으면 옛 유품은 사라지고 새 것만 남는다" · "유품 — 결사에서는 유품이 안 생긴다".
- **범위**: `dungeon.js`(die 경로·buildFloor 표식), `ui.js`(사망 카드), `core.js`. **우선순위 2**. 충돌: 없음. `run` 비저장 규칙 유지(유품은 메타).
- **구현(2026-09-18)**: 코드분 완료 — `graveOf`·`openGrave`·`toggleGraveItem`·`claimGrave`(`dungeon.js`). 상세는 `HANDOFF.md` "PLAN §5.2 유품(遺品) 구현" 절. (2026-09-22 확인: 이 표시가 빠져 있어 한동안 미착수로 잘못 보였다 — 절 번호 재편 때 누락.)

### 5.3 부적(符籍) 던전 — 나이트메어 티어와 변형자
- **참고**: 디아블로 4 나이트메어 던전, PoE 맵 변형자. **표준**: F·G·H.
- **왜**: 난도(normal/험 …)는 최고 층으로 열리는 계단 하나라 30층 넘으면 할 것이 없다. 던전 자체를 소모품으로 만들면 반복에 이유와 변화가 생긴다.
- **메커니즘**: 새 재료 **부적**(층 10+ 보스·월드 보스(5.4)·유적 완성 보상에서 드랍). 굴혈 앞 "부적 던전" 선택 → 티어 1~10 + 변형자 2~3개(`core.hash2(부적 id)` 로 결정적): 적 이동 +30% / 원소 저항 +40%(원소 하나 지정) / 정예 2배 / 보물 +50% / 방마다 75초 제한(몬헌 나우) / 항아리에서 적 / 어둠(시야 -30%) / 재생(적 HP 초당 1%) / 유리대포(피해 ±50%). 티어별 적 배율 1+0.35×T, 보상 배율 1+0.25×T, 클리어 시 다음 티어 부적 드랍 확률 60%. 보스층 없이 5방 고정 + 마지막 방 정예 무리.
- **수치**: 위 표. 티어 10 적 배율 4.5(현 30층 상당). 부적 인벤 상한 20.
- **UI·조작**: 굴혈 표식 접근 시 선택 카드(일반/부적). 부적은 가방 "재료" 탭(mats 결). 변형자 아이콘 3개를 HUD 왼쪽 위에.
- **세이브**: `save.gear.mats.sigil = [{id, tier, mods:[…]}]`, `save.dungeon.nmBest` 신설. 마이그레이션 없음(mergeDeep).
- **진단 문안**: "부적 — 변형자는 부적 id 로 결정적이다(같은 부적 두 번 굴려 같음)" · "부적 — 티어 T 적 HP 가 1+0.35T 배다" · "부적 — 75초 변형자는 시간이 다 되면 방을 실패로 끝낸다" · "부적 — 클리어하면 티어 T+1 부적이 60% 로 나온다(1000회 ±3%)".
- **범위**: `data-dungeon.js`(MODS 표), `dungeon.js`(enter 옵션·enemyHp/Dmg 배율·타이머), `item.js`(mats), `ui.js`. **우선순위 3**. 충돌: 난도 회차 고정 규칙 유지(부적 회차도 회차 안에서 티어 고정). 방 단위 구조 유지.
- **구현(2026-09-18)**: 코드분 완료 — `run.nightmare`(tier·mods)·`nmHasMod`·`nmMul`·`dieNightmare`(`dungeon.js`). 상세는 `HANDOFF.md` "PLAN §5.3 부적(符籍) 던전 구현" 절. (2026-09-22 확인: 표시 누락으로 미착수로 잘못 보였다.)

### 5.4 월드 보스 시간표 — 실시간 75초 전투
- **참고**: 디아블로 4 월드 보스, 몬스터헌터 나우(75초·부위 파괴·저스트 회피). **표준**: H·E·C.
- **왜**: 마을 104·필드는 넓은데 "어디로 왜 가나" 가 없다(§4-E). 시각 기반 예고는 돌아올 이유(H)와 이동 목적(E)을 동시에 준다. 이 판에 낮밤·날씨는 없지만 `Date.now()` 하나로 충분하다.
- **메커니즘**: 15분마다(실시간, `Math.floor(now/900000)` 슬롯) 손 마을 4 중 하나의 들판(안전지대 밖 1600 지점, `core.hash2(slot)`)에 월드 보스 예고 표식(3분 전부터 HUD 카운트다운·자동지도 아이콘). 보스는 기존 보스 10 중 하나 + 체력 8배 + **부위 3**(무기·갑주·머리 — `foeGear` look 조각을 부위로 재사용, 부위 파괴 시 그 패턴 봉인). **75초 제한**: 시간 안에 못 잡으면 도망(보상 30%). HP 66%·33% 에서 패턴 단계 전환. 저스트 회피(공격 0.15s 전 회피 성공) 시 hitstop 0.2s + 콤보 +3. 참가 보상: 전설 확률 ×3, 부적(5.3) 1, 도감 "토벌첩" 등록.
- **수치**: 주기 15분, 예고 3분, 전투 75초, HP 8×`enemyHp(floor=best)`, 부위 HP 각 20%, 도망 보상 30%.
- **UI·조작**: HUD 상단 카운트다운(예고 중만), 자동지도 붉은 아이콘, 전투 중 부위 게이지 3개 + 타이머.
- **세이브**: `save.world = {bossSlot, bossDone:{slot:true}}`, `save.dex.worldBoss = {id:true}` 신설.
- **진단 문안**: "월드 보스 — 슬롯이 같으면 마을·자리가 같다(Date 고정)" · "월드 보스 — 부위를 깨면 그 패턴이 안 나온다" · "월드 보스 — 75초가 지나면 전투가 끝나고 보상이 30% 다" · "월드 보스 — 같은 슬롯은 두 번 보상하지 않는다". `Date.now` 를 진단에서 고정(사가고 `weather.force` 결).
- **범위**: `dungeon.js`(필드 보스 스폰·부위·타이머), `town.js`(예고 표식), `dungeon3d.js`(부위 게이지는 DOM), `ui.js`, `core.js`. **우선순위 4**. 충돌: 없음. 시각 의존 축이므로 진단 고정 필수(§9).
- **구현(2026-09-18)**: 코드분 완료 — `save.dungeon.world`(slot·bossDone)·`save.dex.worldBoss`(`dungeon.js`). 상세는 `HANDOFF.md` "PLAN §5.4 월드 보스 구현" 절. (2026-09-22 확인: 표시 누락으로 미착수로 잘못 보였다.)

### 5.5 난입(亂入) — 15분 생존 파도
- **참고**: 뱀파이어 서바이버, 하데스(레벨업 3택). **표준**: C·D·G.
- **왜**: "손맛" 을 가장 싸게 극대화하는 형식. 기존 `spawnEnemy`·`applyShapeSkill`·축복 3택(5.1) UI 를 전부 재사용해 새 모드 하나가 나온다. 폰에서 15분 짧게 놀기 좋다.
- **메커니즘**: 모루골 결사비 옆 "난입" 표식. 방 하나(큰 방, 3×3 방 크기)에 30초마다 파도(적 수 6+2×파도, 티어는 파도/8), 레벨(처치 경험) 오를 때마다 즉석 3택(5.1 의 무예·인물·세계 축 그대로, 단 회차 한정 아니라 **난입 한정**). 15분 생존 또는 사망으로 끝. 보상 = 생존 시간 비례 금·공적 + 10분 이상 부적 1. 체력 회복은 파도 클리어 보너스만.
- **수치**: 파도 간격 30s, 적 상한 40, 15분 = 30파도, 경험 곡선 파도²×10.
- **UI·조작**: 타이머·파도 수·처치 수 HUD, 3택은 5.1 카드.
- **세이브**: `save.dungeon.horde = {best(초), runs}`.
- **진단 문안**: "난입 — 파도 N 의 적 수는 6+2N 이고 40 을 넘지 않는다" · "난입 — 레벨업 3택이 서로 다른 축이다" · "난입 — 15분이 되면 생존 종료 보상이 붙는다".
- **범위**: `dungeon.js`(horde 모드 진입·파도 타이머), `town.js`(표식), `ui.js`. **우선순위 5**(5.1 뒤). 충돌: 없음 — 자동 순회는 난입을 안 탄다.
- **구현(2026-09-18)**: 코드분 완료 — `run.horde`(hordeWave·hordeLevel·hordeT)·`dieHorde`(`dungeon.js`). 상세는 `HANDOFF.md` "PLAN §5.5 난입(亂入) 구현" 절. (2026-09-22 확인: 표시 누락으로 미착수로 잘못 보였다.)

### 5.6 목표판·세션 카드·일일/주간
- **참고**: 포켓몬GO 리서치, 원신 일일 위탁, 메이플 주간 보스. **표준**: A·B·H.
- **왜**: §4 의 가장 큰 구멍. 재료는 다 있다 — 유적·발견거리·현상판·층·보스·상인·월드 보스(5.4)·부적(5.3). 그것을 **화면 위 3줄**로 엮기만 하면 된다.
- **메커니즘**: HUD 위 3줄 목표판 — **지금**(≤30초: "가까운 유적 1", "이 방 정리", "유품 회수"), **이번 세션**(≤10분: "3층 내려가기", "현상판 1", "상인 만나기"), **이번 주**(월드 보스 1·부적 티어 3·인물 1 합류). 완료 즉시 다음 줄 채움(우선순위 표에서). **세션 카드**: 탈출·사망·마을 귀환·탭 닫기(visibilitychange) 때 카드 1장 — 얻은 것(금·장비 최고 등급·인물 경험)·도감 진척(%)·다음 할 것 1개. **일일**(3분: 유적 1·필드 상인 1·층 3, 보상 금 100+감정서)·**주간**(20분: 월드 보스 1·부적 3·인물 합류 1, 보상 부적 1+전설 확정 1) — 놓쳐도 벌칙 없음.
- **수치**: 목표 후보 표 20개(가중치·조건), 일일 리셋 로컬 자정, 주간 월요일.
- **UI·조작**: `#dg-top` 아래 접이식 3줄(폰에서 1줄로 접힘, 탭하면 펼침). 카드는 `#encounter` 재사용.
- **세이브**: `save.goals = {daily:{date, done:{}}, weekly:{week, done:{}}, session:{start, gold0, …}}` 신설.
- **진단 문안**: "목표판 — 부팅 직후 세 줄이 비어 있지 않다" · "목표판 — 하나를 채우면 같은 줄에 다음 목표가 즉시 들어온다" · "일일 — 날짜를 바꿔 부팅하면 일일 목표가 새로 뽑히고 done 이 비워진다" · "카드 — 탈출 카드에 next 필드가 있다".
- **범위**: 새 `js/goals.js`(순수 계산 + 이벤트 구독 — 판정 파일 안 건드림), `ui.js`(HUD·카드), `core.js`, `index.html`·`sw.js`. **우선순위 1과 동급**(5.1 과 같은 Phase). 충돌: 없음.
- **구현(2026-09-18)**: 새 `js/goals.js` — 지금/이번 세션은 후보 표(각 5개, `NOW_POOL`/`SESSION_POOL`)를 순서대로 돌며 완료 즉시 다음 후보로 넘어간다(진행도는 절대 카운트가 아니라 "후보가 배정된 시점의 카운터 값"과의 차이라 세션이 오래돼도 안 어긋난다). 이번 주는 고정 묶음(`WEEKLY_BUNDLE`)의 남은 항목을 먼저 보여 주고, 다 채우면 `WEEKLY_POOL`로 넘어간다. 월드 보스(5.4)·부적(5.3)은 아직 없어 주간 묶음에 `stub:true`로 넣고 완성 조건에서 뺐다(그 둘 없이도 인물 합류만으로 주간 보너스가 열린다) — 두 시스템이 들어오면 stub을 풀 것. 일일/주간 묶음을 다 채우면 그 자리에서 보상을 준다(일일: 금100·감정서1, 주간: `item.roll(floor,{tier:4})`로 전설 확정 지급 — PLAN 원안의 "부적 1"은 5.3이 없어 뺐다). 이벤트는 이미 나가는 것만 듣는다(`dex:new`·`town:npc`·`town:mark`·`dungeon:room`·`dungeon:floor`·`levelup`·`dungeon:end`) — `dungeon.js`·`town.js`는 한 줄도 안 건드렸다. **세션 카드 계기 넷 중 셋(탈출·사망·마을 귀환)은 하나로 합쳤다** — 이 판은 던전에서 나오는 길이 `dungeon:end`(reason leave/dead) 하나뿐이라 마을 귀환이 곧 그 사건이다(집으로 걸어 돌아오는 별도 동작이 없다). 탭 닫기(`visibilitychange`)는 세션을 조용히 마감만 하고 카드는 안 띄운다(볼 사람이 없다). 카드의 "장비 최고 등급"은 `dungeon:end` 페이로드에 등급 정보가 없어 개수로, "인물 경험"은 `player.exp`가 레벨업마다 0으로 꺾여 델타를 못 재 `player.featTotal`(누적 공적) 델타로 각각 대체했다 — 문구도 `card.feat`(공적)로 바꿨다. UI는 `#dg-top`이 이 판엔 없어 대신 `#top` 그리드에 세 번째 줄로 낀다(`css/style.css` `grid-template-areas`에 `goals` 행 추가) — 폰 폭에서는 첫 줄만 보이다 탭하면 펼쳐진다(`#goals.open`). 카드는 PLAN대로 `#encounter`(`encOpen`) 재사용. `game.js`에 `vendor.init()`과 같은 자리에 `goals.init()` 한 줄을 추가했다(PLAN 범위엔 없었지만 부팅 훅이 없으면 모듈이 아예 안 켜진다 — vendor.js와 같은 이유의 최소 추가). `_test.html`에 PLAN 진단 문안 4개 그대로 추가. `core.js` `freshSave()`에 `goals` 기본 모양 추가. **검증** — `node -c`(goals.js·core.js·game.js·ui.js) 통과, `bash tools/precheck.sh saga-web/saga-dungeon` → PRECHECK OK. `_test.html`은 헤드리스로 안 띄웠다(이 판 규칙) — 인라인 스크립트만 별도로 `node -c`로 구문 검사했다. 실기 확인 남음: `#goals` 줄바꿈 위치·펼침 애니메이션이 실제 화면에서 지갑 줄과 안 겹치는지.

### 5.7 시대 퓨전 — 현대·미래 인물 30·미래 무기 look·시대 혼재 건물
- **참고**: 크로노 트리거·FF(시대 혼재), 사용자 창작 방향(2026-09-10). **표준**: G·E(세계 정취).
- **왜**: 사용자가 못박은 방향("이 세상에 없는 게임"). 이 판이 선행 판. 루프(5.1~5.6) 위에 얹는 살이라 **Phase 5** 로 둔다.
- **메커니즘**: ① **인물 30**(현대 15 `md_*`: 재벌·과학자·스포츠 스타·탐험가·의사 등 가상 오마주, 근미래 15 `ft_*`: 우주비행사·해커·생체공학자·드론 지휘관 등) — `data.js` `HEROES` 에 추가, era 필드 `modern`·`future`, 서명 무예 30 추가(`SIG`), 초상은 CC0 저폴리 캐릭터 팩(§6.2 변형 배가). ② **미래 무기 look 2**: `lance_e`(전자창, 무장) · `gauntlet`(동력장갑, 도독) — `data-item.js` 밑감 2 + `dungeon3d.js` 그리기(기존 spear·fist 자리에 발광 재질). 직업은 무기가 정하므로 스킬 시스템 무변경. ③ **시대 혼재 건물**: biome 별 decor 레시피에 "시대 층" 1개 — 고분 옆 폐공장 굴뚝(`ruins` biome), 기와집 옆 태양광 판(`forest`), 늪 위 녹슨 관측탑(`swamp`), 산채에 케이블카 기둥(`mountain`), 사당에 홀로그램 비석(`shrine`). 전부 CC0 소품 조합(§6.2 kitbash) 또는 절차 생성(trimesh). ④ 몬스터: 기존 짐승형 파생 규칙으로 "기계화" 변종 10(같은 GLB + 금속 팔레트 + emp 저항).
- **수치**: 인물 30 은 rarity 3~4 분포(레어 5 는 3명만), 시대 층 decor 는 마을당 1~2개(밀도 규칙 95 유지).
- **UI·조작**: 도감에 era 필터(과거·현대·미래). 인물 카드에 시대 아이콘.
- **세이브**: 없음(dex.heroes 는 id 키). **단 `data.js` 는 다섯 판 공통 사본** — 인물 30 추가는 다섯 벌 md5 일치가 조건(§10-Q1). 이 판만 먼저 하려면 `data-hero-ext.js` 로 이 판 전용 확장 파일을 두고 나중에 `data.js` 로 합친다(권장).
- **진단 문안**: "퓨전 — 인물 이름 30 이 실명 블랙리스트에 없다" · "퓨전 — 135 인물 전원 sigOf 가 null 이 아니다" · "퓨전 — 시대 층 decor 가 스폰 95 간격을 지킨다" · "퓨전 — lance_e 밑감이 무장을 열고 gauntlet 이 도독을 연다".
- **범위**: `data-hero-ext.js`(신설)·`data-hero-skill.js`·`data-item.js`·`data-enemy.js`·`town.js`(decor)·`asset3d.js`·`dungeon3d.js`·`ui.js`. **우선순위 6**. 충돌: 이름 정책(가명·블랙리스트 검사 필수), 다섯 벌 복사(전용 확장 파일로 회피), "코드로 그리지 말고 에셋으로"(소품은 CC0 조합).
- **구현(2026-09-18)**: 네 갈래 전부 코드분 완료 — ① 인물 30(`data-hero-ext.js`) ② 미래 무기 룩 `lance_e`·`gauntlet`(`data-item.js`·`dungeon3d.js`) ③ 시대 층 소품 5종(`field3d.js` `ERA_LAYER`/`eraLayerSlots`, `t_factory`/`t_solar`/`t_tower`/`t_pylon`/`t_hologram` — biome 5개, town.js 의 decor 목록이 아니라 필드 청크 자리다) ④ 기계화 변종 10종(`data-enemy.js`, 강철랑·동합귀·철갑해 등, `resist.emp` + 금속 팔레트). 상세는 `HANDOFF.md` "PLAN §5.7 시대 퓨전 구현" 절.

### 5.8 손맛 2차·성장 가시화 — 표준 C·G 완성
- **참고**: 몬헌 나우, 하데스, 데드셀. **표준**: C·G.
- **왜**: 1차(hitstop·콤보·크리 흔들림)는 "있다" 수준. SAGA-DESIGN §3-C 5요소를 규격으로 완성하고, 성장이 **보이게** 한다.
- **메커니즘**: ① 타격 이벤트 5요소 — hitstop(잡졸 60ms·정예 90·크리 120: 현 dt×0.08 을 시간 기준으로 재정의), 화면 흔들림(평타 1.5px/80ms·크리 4px/120ms·처치 유지), 피격 플래시(적 emissive 80ms — 3D `fx3d`·2D 캔버스 둘 다), 숫자 팝(0.6s, 크리는 1.4배 크기·주황), 타격음 3종 라운드로빈(`sfx.js` CUES `hit1/2/3`, 무기 look 별 결 — 도검·둔기·활·기공). ② 저스트 회피(적 공격 0.15s 전 회피 → 0.2s 슬로우·콤보 +3·`dodge` 소리 변주). ③ 성장 가시화 — 등급별 무기 이펙트(보물 이상 잔광, 전설 파티클), 서명 무예 발동 시 인물 초상 컷인(0.4s, `portrait3d` 재사용), 부대 5명 채우면 마을 스폰 뒤에 부대 인물이 서 있음(장식, 판정 없음), 콤보 12+ 화면 채도 +10%. ④ 2D 자세 — `sprite.js` 에 공격·회피 프레임 2장씩(코드 그림이 아니라 기존 CC0 스프라이트 시트에서 자르기 — 없으면 3D 만).
- **수치**: 위 값. 규격은 SAGA-DESIGN §3-C 와 같다(다섯 판 공통 숫자).
- **UI·조작**: 설정 시트에 "화면 흔들림 0~2"·"타격 정지 on/off" 손잡이(멀미 배려).
- **세이브**: `save.settings.shake`, `save.settings.hitstop` 추가(기본 1, true).
- **진단 문안**: "손맛 — strike 한 번에 hitstop·흔들림·플래시·숫자·소리 다섯 이벤트가 모두 난다" · "손맛 — 크리 hitstop 이 120ms 다" · "손맛 — 타격음 셋이 순서대로 돈다" · "저스트 회피 — 0.15s 창 안 회피는 콤보 +3 이고 밖은 0".
- **범위**: `dungeon.js`(hitstop 시간 기준·저스트 창), `fx3d.js`·`dungeon-view.js`(플래시·팝), `sfx.js`(큐 3종), `dungeon3d.js`(이펙트·컷인), `ui.js`(설정). **우선순위 1과 동급**. 충돌: 없음.
- **구현(2026-09-18)**: 이걸로 Phase 1(§8) 세 후보(5.6·5.1·5.8)가 다 코드분으로 닫혔다.
  - **① 타격 이벤트 5요소**: hitstop 을 잡졸60·정예90·크리120ms 3단으로 재정의(`strike()`, `e.elite` 로 정예를 가른다) — 처치(`kill()`)는 이 3단 밖의 별개 박자라 값(보스180·일반110ms)은 그대로 두고 설정 손잡이만 같이 탔다. 화면 흔들림은 2D(`dungeon-view.js`)·3D(`fx3d.js`) 둘 다 "내가 때릴 때"(평타 1.5·크리 4)가 아예 없던 걸 채웠다(3D 는 기존에 크리만 3 이 있었다). 피격 플래시는 `e.hurt` 초기값을 0.2→0.08 로 낮추고, 3D `flashAllMat` 의 span 인자(0.2→0.08)도 같이 맞췄다(안 맞추면 최고 밝기까지 안 닿는다). 숫자 팝 크리는 2D 13→18px·3D 20→28px(정확히 1.4배)로, 색은 2D·3D 둘 다 옛 노란빛(`0xffdc78`)에서 뚜렷한 주황(`0xff8c3c`)으로 바꿨다. 타격음은 `sfx.js` `hit1/2/3` 신설 — `run.combo` 로 순서를 돌리고(콤보가 끊기면 자연히 처음으로 되돌아온다), 무기 look(도검·활은 밝게, 둔기는 둔탁하게, 부채·지팡이·붓·병서는 중간)은 `play()` 에 새로 얹은 `opts.lpMul`(노이즈 켜의 저역통과만 배로 미는 값, 기존 30여 개 큐는 안 건드림)로 얹었다.
  - **② 저스트 회피**: **범위를 줄였다** — 보스 패턴(`bossSlam`/`bossCharge`/`bossThrust`/`bossFlurry`)의 공용 예고 타이머 `en.slamWarn` 만 대상이다. 잡몹은 `en.cd<=0` 이면 그 프레임에 바로 맞는 구조라 예고 자체가 없어, 새 텔레그래프 시스템을 전체 몹에 얹지 않는 한 저스트 판정이 안 된다 — 그건 전투 AI 전체를 건드리는 손맛(연출) 범위 밖의 일이라 이번엔 안 했다(**사용자와 상의한 적 없는, 이번 구현이 스스로 내린 판단**). `run.slowT` 신설(hitstop 과 같은 dt 배율 자리지만 ×0.4 로 더 부드럽게, hitstop 이 걸려 있으면 그쪽이 우선). `dodgeJust` 소리 신설.
  - **③ 성장 가시화 — 절반만 했다.** 콤보 12+ 채도는 **2D 만**(`ctx.filter='saturate(1.1)'`) — 3D 는 `post3d.js` 에 톤매핑·블룸·비네트뿐 채도 조절 패스 자체가 없어, 새 셰이더 패스를 만드는 건 손맛 범위를 넘는다고 보고 스킵했다. 무기 등급 이펙트(보물+ 잔광·전설 파티클)는 새 3D 지오메트리 없이 기존 `trail`/`burst` fx 표를 재사용해 `strike()` 에서 매 타격마다 얹었다(`weaponTierOf()`). 서명 무예 컷인은 `portrait3d.js` 가 정지 이미지를 굽는 유틸일 뿐 애니메이션 API가 없어(재사용할 게 없었다) 대신 `sprite.portraitCard()`(§5.1 카드·도감이 이미 쓰는 함수)로 얻은 이미지를 `ui.js` 가 만든 DOM 오버레이에 0.4초 페이드로 띄운다(`dungeon:skill` 의 `sig:` 접두 페이로드를 듣는다, `dungeon3d.js` 는 안 건드림). **부대 5명 마을 스폰 장식은 스킵했다** — `town.js` 에 인물별 3D 액터를 장식으로 세울 기존 자리(빈 배치 슬롯)가 없어, 새로 만들려면 마을 렌더 파이프라인을 따로 조사해야 해 이번 범위에서 뺐다(다음 손질 후보).
  - **④ 2D 자세는 통째로 스킵했다** — PLAN 원문이 "없으면 3D만" 이라고 이미 열어 둔 길이다. `sprite.js` 를 확인해 보니 이 판은 스프라이트 시트가 아니라 **캔버스로 직접 그리는** 벡터 그림이라(자를 CC0 시트 자체가 없다), 새로 만들면 "코드가 그림을 그린다"는 저장소 금기를 어기게 된다.
  - **자가진단 전용 훅**: `DN._forceCrit(v|null)`(크리를 강제 — `_forceElemDmg` 와 같은 결, `Math.random()` 기반이라 결정적 진단이 안 됐다).
  - **검증** — `node -c`(dungeon.js·dungeon-view.js·dungeon3d.js·fx3d.js·sfx.js·ui.js·core.js) 통과, `bash tools/precheck.sh saga-web/saga-dungeon` → PRECHECK OK. `_test.html`에 §5.8 진단 4개 추가(인라인 스크립트는 `node -c`로 구문만 확인, 헤드리스는 안 띄웠다 — 이 판 규칙). **실기 확인 남음**: 화면 흔들림·플래시·채도가 실제로 "적당히"(과하지 않게) 느껴지는지, 서명 무예 컷인 위치·크기가 화면과 안 겹치는지, 저스트 회피가 보스 전투에서 실제로 체감되는지.

### 5.9 비결(秘訣) — 디아블로3식 무예 변형 + 전리품 빛기둥 (2026-09-24 사용자 선택 "빌드 룬")
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로3 스킬 룬(무예 하나의 쓰임을 다섯 갈래로) · 전설 낙하 빛기둥·낙하음 |
| 표준 | D G C |
| 왜 | 무예 120 이 모양 열에 값을 끼운 것이라 "다 같은 축"(§4 D) — 고를 이유가 없었다. 좋은 물건이 떨어져도 줍기 전엔 몰랐다 |
| 메커니즘 | 무예마다 비결 하나(한 무예에 하나). 🔥분노(위력 ×1.45·기력 ×1.4) · ❄️한기(결→빙, 위력 ×0.9, 지속형 지속 ×1.4) · 🌀확산(범위 ×1.35·발수 +2·연환 +2·분신 +1·위력 ×0.8) · ⚡신속(재냉각 ×0.5·위력 ×0.7) · 🩸흡혈(3초 흡수 12%·위력 ×0.9). 무예 단수 1~5 에 하나씩 열린다. 이름은 소켓 룬(data-gem.js)과 겹치지 않게 '비결'. 명품 이상 드롭은 등급색 빛기둥(전설·고유는 굵고 높게), 보물·전설·고유는 떨어지는 순간 소리 |
| 세이브 | `save.secrets = { 인물: { 무예: 비결 } }` — 단수가 내려가면(환원) 없는 것으로 읽는다 |
| 진단 | 다섯·해금 · 모양별 변형·원본 불변 · 단수 잠금·풀기·환원 · 실제 시전(신속·분노) · 흡혈 버프(5) |
| 범위 | CREATE `js/secret.js` · MODIFY `dungeon.js`(castSkill·status·dropItem) `ui.js`(무예 나무 아래 비결 단추) `dungeon-view.js`(칸에 비결 그림) `dungeon3d.js`(빛기둥 — 드롭 자리가 같은 번호로 다른 물건을 받으면 다시 짓게도 고침) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(비결 선택 체감·빛기둥 높이) |

### 5.10 비전(秘傳) — 비결을 키우는 전설 장비 (§8 "빌드 다음" ①)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로3 전설 능력·세트 보너스("이 룬을 쓴 기술이 ×N") |
| 표준 | D G H |
| 왜 | 비결(§5.9)을 골라도 그 뒤 파밍과 이어지지 않았다 — 전설을 주워도 "내 빌드가 커졌다" 가 없었다 |
| 메커니즘 | 전설(4) 한 점에 비전 하나(다섯 비결 중 하나). 선두가 입으면 **그 비결을 건 무예 위력 ×1.6**(부르기는 수 +1). 같은 비전 두 점은 안 겹친다 — 다른 비전 둘로 두 갈래를 키운다. 부서진 것·미확인은 안 센다. 이름: 노화·빙혼·만상·섬광·혈해 |
| 수치 | `LORE_MUL` 1.6(분노 1.45 와 곱해 2.32) |
| 굴림 | 굴리지 않는다(RNG 순번, §2.3) — 고유는 고유 키 해시(늘 같은 비전), 나머지는 uid 해시, `it.lore` 가 있으면 그것 |
| 세이브 | 없음(물건에서 읽는다). `it.lore` 는 선택 칸 |
| UI | 물건 설명 📜 줄(툴팁·가방 줄) · 무예 나무 비결 단추에 📜 · 무예 칸 제목에 "📜 비전 ×1.6" |
| 진단 | 전설에만·다섯 다 나옴·고유 고정·미확인 숨김·지정 우선 / 입으면 ×2.32·실제 시전·딴 비결 1·부서지면 꺼짐(2) |
| 범위 | MODIFY `secret.js`(loreOf·boostOf·activeLores·loreLine) `dungeon.js`(secretMod·status) `ui.js` `dungeon-view.js`. `item.lines()` 는 안 건드림(고유 줄 수 진단) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(×1.6 체감·툴팁 줄 색) |

### 5.12 고정 세계 지도 — 디아블로식 지역 아홉, 미리 구운 들판 (2026-09-24 사용자 지시)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로 2·4 의 지역(Act·Zone) — 이름·땅빛·소품 결이 다른 고정 지역, 들어서면 지역명 |
| 표준 | E G (그리고 버그: 끼임) |
| 왜 | 사용자: "맵이 생성되는 중간중간 캐릭터가 끼고 못 움직인다 — 전체를 미리 생성해 랜덤 말고 특색 있게 고정으로, 현대·미래·과거가 다 있는 퓨전 디아블로". 원인 셋: ① 들판 칸이 **가장 가까운 마을**의 씨앗으로 뽑혀 마을이 바뀌면 둘레가 통째로 다시 뽑혔다 ② 그림은 로컬 칸·충돌은 세계 칸으로 `chunkAt` 을 불러 모루골 밖에선 보이는 나무와 막히는 자리가 달랐다 ③ 그림은 그래픽 밀도로 나무를 줄였는데 충돌은 안 줄였다. 덧붙여 창이 옮겨질 때 들판을 지우고 여러 프레임에 걸쳐 채워 땅이 비었다 차올랐다 |
| 메커니즘 | `world-map.js` — 중원(원점 반경 11500) + 여덟 방위(동 폐도시·동남 소금 개펄·남 지옥 균열·서남 태양 신도시·서 서역 모랫길·서북 천계 사당·북 북방 설산·동북 기계 황무지), 경계는 고정 잡음으로 굽힌다. 지역마다 가중치 표(`region:<key>`)·땅빛·시대 소품(t_factory·t_tower·t_solar·t_hologram·t_pylon)·절차 마을 성격. 세계 칸 481×481 을 부팅 뒤 4ms 예산으로 나눠 굽는다(안 구운 칸은 같은 산식으로 즉석). 소품 배열은 칸마다 한 번 만들어 붙든다. 3D 는 세계 좌표로 짓고 묶음을 앵커만큼 민다(마을이 바뀌어도 안 다시 짓는다), 창이 옮겨가면 **뒤에서 다 짓고 한 번에 교체**. 이미 소품 안이면 빠져나오는 걸음은 막지 않는다. 지역에 1.2초 머물면 "○○ 에 들어섰다" 배너·큰 지도 지역명·땅빛 |
| 수치 | `CENTER_R` 11500 · 칸 ±240 · 시대 소품 비율 0.09~0.24 · 배너 머묾 1.2초 · 굽기 예산 4ms/틱 |
| 세이브 | 없음(지도는 코드가 정한다). 절차 마을 id·앵커는 그대로 — 이름·성격만 지역을 따른다 |
| 진단 | 지역 아홉·방위·마을 성격 / 앵커 다섯 모두 같은 자리 막힘·자동지도 같은 표 / 안→밖 통과·밖→안 막힘 / 구운 표 = 즉석 셈 / 빠른 길(마을 격자·길 래스터) = 전수(5) |
| 범위 | CREATE `world-map.js` · MODIFY `town.js`(마을 성격·nearestTownId 격자·배너·굽기 시작) `dungeon.js`(fieldBlockedAt·boundPlayer) `dungeon3d.js`(세계 좌표 들판·뒤에서 짓기) `field3d.js`(setBias·길 래스터) `minimap.js`(큰 지도 지역빛·이름) |
| 남은 것 | 던전 **안**은 §5.15 명소 층(테마 끝 층 여섯)만 고정 — 나머지 층은 문 갈림길 무작위 그대로 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(끼임 사라졌는지·마을 사이 걸을 때 땅 교체가 안 보이는지·지역 경계·배너) |

### 5.13 지역 몬스터·위험도·지역 우두머리 (§8 "고정 세계" 다음)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로 2 구역 레벨(Area Level)·고유 몬스터(Super Unique, 늘 같은 자리) · 디아블로 4 지역별 몬스터 군 |
| 표준 | E G H |
| 왜 | 마을 들판은 `ctx.floor` 가 늘 0 이라 **어디를 가든 1단계 잡졸**이었다 — 지역 아홉을 세워도 "멀리 갈수록 위험하고 좋은 게 나온다" 가 없었다 |
| 메커니즘 | 지역마다 기본 위험도(중원 0·개펄 2·폐도시/서역 3·신도시/설산 4·천계 5·지옥/기계 6) + 가운데 원 밖으로 3000 마다 +1(상한 30). 마을 `raw().floor` = 그 자리 위험도 — 적 세기·내 원소 피해(1+층×0.06)·전리품·경험치가 같이 탄다. 중원은 0 이라 예전 그대로. 지역 명단(12~23종) 안에서만, 위험도가 감당하는 단계(tier)까지, 지금 단계는 세 몫. 지역 우두머리 아홉 — 고정 자리(방위 한가운데 반지름 28000·중원 8500, 제 지역·마을 발판 2200 밖이 될 때까지 정해진 순서로 옮김), 1500 안에 들면 호위 정예 셋과 선다. 세기는 자리 위험도+4 층 보스·체력 ×3. 처치: 전설 한 점·보물 bias 45 둘·재료·금 ×6·공적 15+층, 10분 뒤 다시 선다 |
| 세이브 | `save.dungeon.regionBoss = { 지역: { kills, firstAt, lastAt } }` |
| UI | 지역 배너·HUD 에 "위험 N"(10 이상 붉게), 들판 HUD 머리는 지역 이름 · 큰 지도에 우두머리 아홉 ☠(쉬는 중 흐리게, 토벌 ✓) |
| 진단 | 명단 이름·1단계·몸 / 위험도 중원 0·멀수록·상한·모루골 floor 0 / 명단·단계 밖 0 / 자리·호위·전설·10분(4) |
| 범위 | MODIFY `world-map.js`(FOES·levelAt·bossSpot) `data-enemy.js`(poolFor roster·byName) `dungeon.js`(pickEnemyRef·stepRegionBoss·보상) `town.js`(raw floor·region·HUD 값) `dungeon-view.js` `minimap.js` |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(위험도 오름 속도가 가혹한지·우두머리 세기·호위 셋·10분 쉼) |

### 5.14 지역 사연(事緣) 사슬 — 지역마다 네 걸음, 평정 (§8 "고정 세계" 다음)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로 2 막(Act) 퀘스트 — 마을 사람의 부탁 → 단서 → 고유 몬스터, 지역마다 사연이 이어진다 |
| 표준 | E H A |
| 왜 | §5.12·5.13 으로 지역 아홉과 우두머리가 섰지만 "이 지역에서 무엇을 하나" 가 없었다 — 퀘스트는 던전 층 테마(여섯)만 셌고, 들판 지역은 우두머리 ☠ 말고 갈 까닭이 없었다 |
| 메커니즘 | 지역에 처음 발 들이면(들판 틱 0.4초) 그 지역 사슬이 열린다(의뢰인·한 줄 사연 토스트). 걸음 넷 **순서대로만**: ① 토벌 — 그 지역 들판 몬스터(처치 자리로 지역 판정) ② 흔적 — `world-map.clueSpot`(방위 한가운데 +0.22 rad·반지름 19000, 중원 5200, 우두머리와 같은 옮김 규칙) 320 안에 닿기, 큰 지도 🔍 ③ 정예 — 그 지역 정예·우두머리 ④ 우두머리 — §5.13 토벌(`regionboss:kill`). ④에 닿으면 쉬던 우두머리의 쉼(lastAt)을 지워 곧바로 다시 선다. 우두머리를 먼저 잡아도 걸음은 안 건너뛴다. 넷째를 마치면 평정 🏳️, 아홉 다 평정하면 구주 평정 한 번. Math.random 을 하나도 안 쓴다 |
| 수치 | L = 지역 기본 위험도(0~6). 토벌 10+2L · 정예 3(중원 2) · 흔적 반경 320. 보상 금 60+30L / 80+40L / 100+50L / 400+200L, 경험 10+5L~60+20L, 평정 공적 20+L · 구주 평정 금 20000·경험 1000·공적 100 |
| 세이브 | `save.quest.chain = { 지역: { step, have, done, at } }` · `save.quest.chainAll` — 없으면 빈 것으로 채운다 |
| UI | 퀘스트 탭 "📜 지역 사연 n/9 평정"(안 간 곳 ❔ 잠김, 걸음 (k/4 이름)·의뢰인) · 큰 지도 🔍 흔적 자리(흔적 걸음인 사슬만) |
| 진단 | 표·자리(제 지역·마을 2200 밖·우두머리와 3000 밖) / 순서·던전 처치 안 셈·딴 지역 안 셈·먼 흔적 안 됨·쉼 해제·평정 / 구주 평정 한 번 / 화면 값·Math.random 0 (4) |
| 범위 | MODIFY `data-quest.js`(CHAINS·chainStep·CHAIN_ALL) `quest.js`(chainOpen·stepField·onKill·regionboss:kill·status) `world-map.js`(placeSpot·clueSpot) `town.js`(들판 틱) `ui.js`(퀘스트 탭) `minimap.js`(🔍) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(토벌 수가 지루하지 않은지·흔적 자리 찾기 쉬운지·사연 문구) |

### 5.15 명소 층(名所層) — 테마 끝 층은 손으로 짠 고정 층 (§5.12 "남은 것", 답 없는 확인에 "이어해" → 추천안)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로 2 고정 퀘스트 구역(늘 같은 모양·같은 주인 — 무작위 층 사이에 박힌 이름 있는 땅) |
| 표준 | E G H |
| 왜 | 던전 안은 층·방마다 문 갈림길을 무작위로 뽑아, 들판(§5.12)과 달리 "기억에 남는 자리" 가 없었다. 전부 고정하면 로그라이트 반복이 죽으므로 **테마 여섯의 끝 층만** 고정한다(추천안 — 나머지 층은 그대로) |
| 메커니즘 | `data-dungeon.js` FIXED 여섯 — 5 순장 왕릉 · 10 무너진 망루성 · 15 흑풍 산채 · 20 가라앉은 용궁 · 25 업화 대문 · 30 구름 위 금궐. 방 다섯이 늘 같은 순서(문 하나, 문에 다음 방 이름), 방 종류는 ROOMS 판정 그대로(상자·우물·사당·퍼즐·구출·채집·광맥), 적은 표의 명단·수를 늘 같은 자리(황금비 가로·고른 세로)에 세우고 표에 정예라 적힌 것만 정예. 기둥 배치 넷(hall·ring·cross·open)·항아리 넷·균열 둘도 고정(횃불·가시는 기존 3D 잡동사니와 같은 자리). 마지막 방 층 주인(몸은 base 몬스터, 이름 창작) — 첫 토벌에 전설 한 점·공적 10+층. 5·10…은 역참 층이라 역참에서 곧장 명소로 들어간다. 보스 층(15·30)의 축복 3택은 그대로 |
| 수치 | 방 5 · 첫 답파 전설 1(tier 4, 층+1) · 공적 10+층 · 손잡이 `dungeon.fixed`(기본 1, 0 이면 예전 갈림길) |
| 세이브 | `save.dungeon.fixed = { 층: { clears, firstAt, lastAt } }` — 없으면 빈 것 |
| UI | HUD 테마 자리에 "⚱️ 순장 왕릉 · 참배길", 들어갈 때·방마다 토스트, 2D 문 아래 다음 방 이름 |
| 진단 | 표(여섯·방 다섯·이름·몸·테마) / 두 번 지어 방 30 모두 같음·적은 방 안 / 입장·문 하나·층 주인·첫 토벌만 전설·다음 층 갈림길 / 손잡이 끄면 갈림길(4) |
| 범위 | MODIFY `data-dungeon.js`(FIXED·FIXED_PAT·fixedOf) `dungeon.js`(makeFixedRoom·fixedDoors·grantFixedReward·buildFloor·goRoom·kill·status) `dungeon-view.js`(HUD·문 이름) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(고정 방 배치가 3D 에서 답답하지 않은지·층 주인 세기·다른 층도 고정하길 원하는지) |

### 5.16 몸짓(身振) — 마을 사람·동행이 일과 명령에 맞춰 움직인다 (§8 "빌드 다음" ③)
| 필드 | 내용 |
|---|---|
| 참고·표준 | 사가국지 §5-10 ② · 디아블로 2 마을 사람 · C G |
| 왜 | 마을 사람은 늘 idle, 동행은 걷기·치기뿐 — 세공을 해도 야장은 가만있었다 |
| 메커니즘 | `gesture.js`, 화면 층만. 닿으면 인사 👋 1.0s · 그 사람이 연 시트가 열린 채 `changed` 면 제 일 1.6s(`JOB`: 야장 🔨 벼림 attack·행상 💰 흥정…) · 틈틈이 7~11s 마다 1.4s 제 일(키 해시) · 동행: 선두 서명에 ❗ 호응 0.8s, 보스·우두머리·명소 주인·레벨업에 🎉 환호(jump·튐) 1.4s. 걷거나 치는 중이면 글자만. 3D 머리 위 풍선·2D 이름표 위 글자 |
| 수치·세이브 | 위 초 · 손잡이 `dungeon.gesture` · 세이브 없음 · Math.random 0 |
| 진단 | 표 / 수명·틈틈이 / 사건 / 손잡이 (4) |
| 구현(2026-09-24) | 코드분 완료, **실기 확인 대기**(야장 attack 이 망치질로 읽히는지·풍선 겹침) |

### 5.17 동행 서명(同行署名) — 동행도 제 서명 무예를 쓰고, 선두 서명 직후면 합격
| 필드 | 내용 |
|---|---|
| 참고·표준 | 디아블로 4 용병 지원 기술 · 삼국무쌍 합체 무쌍 · D C G |
| 왜 | 서명 105종은 선두 것만 쓰였다 — 동행 고르기가 전투력 숫자 말고 뜻이 없었다 |
| 메커니즘 | 곁(120)에 적 셋·정예·보스면 동행이 제 서명(원소·v 제 것, × 동행 배율)을 동행 자리 파동으로. 선두 서명 뒤 1.5s 안이면 적 하나로도 곧장 **합격** ×1.5·토스트. 몸짓 ✨/⚡ |
| 수치·세이브 | 첫 4s · 쿨 max(8, cd×1.5) · 넉백 ≤30 · 손잡이 `dungeon.allySig` · 세이브 없음(`run.allySigs`·`allyCombos`) |
| 진단 | 쓸 까닭 / 실제 방·쿨 / 합격 ×1.50·창 밖·손잡이 (3) |
| 구현(2026-09-24) | 코드분 완료, **실기 확인 대기**(쿨 체감·합격 타이밍·세기) |

### 5.18 명소 층 주인 고유 수 — 여섯 주인이 저마다 다른 한 수 (§5.15 다음 후보)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로 2 고유 몬스터(늘 같은 자리·같은 능력) · 디아블로 3 보스의 바닥 예고 원 · 로스트아크 관문 패턴 |
| 표준 | B H G |
| 왜 | 주인은 `spawnEnemy(floor, true)` 라 보스 패턴을 받지만, 빌린 몸(해골대장·산군…)에 무기가 없으면 여섯 다 같은 강타 하나였다 — 이름만 다르고 싸움은 같았다 |
| 메커니즘 | `guard.sig`(data-dungeon.js) 갈래 여섯, 판정 `guardZones`(순수, 주인·나 자리와 시전 횟수만). ⚱️ 순장장군 **순장 호령** — 체력 ⅔·⅓ 에 한 번씩 해골졸개 둘 · 🏯 성주 망령 **망루 화살비** — 내 자리+양옆 원 셋(시전마다 방향이 돈다) · 🏴 흑풍 채주 **삼연돌** — 내 자리로 세 번 연달아 뛰어든다 · 🐚 용궁지기 **심연 소용돌이** — 예고 동안 나를 끌어당기고(구르면 안 끌림) 둘레 폭발(빙) · ⛩️ 업화 문지기 **업화 장판** — 내 자리가 터지고 불바닥이 6초 남아 0.5초마다 태운다(넷까지) · 🏛️ 천장군 **천뢰 십자** — 줄 넷(+ 와 × 번갈아, 원 스물, 뇌). 예고 원(`fx 'zone'`)은 제 크기에 서서 터질 때가 다가올수록 진해진다. 고유 수를 예고·시전하는 동안은 무기 패턴을 쉰다. 첫 시전만 토스트 |
| 수치 | 첫 3s · 화살비 cd 7·예고 0.9·r 42·×1.2 · 삼연돌 cd 8·0.45×3·r 34·×0.9 · 소용돌이 cd 9·1.2·r 95·끌림 60/s·×1.4 · 장판 cd 6·0.8·r 46·×0.6·바닥 6s·×0.2/0.5s · 천뢰 cd 8·1.0·r 22·간격 48·×1.3 · 손잡이 `dungeon.guardSig`(기본 1) · Math.random 0 |
| 세이브 | 없음(`en.gs` 개체 상태) |
| 진단 | 표(여섯·순수·모양·예고 원 크기 고정) / 화살비 서면 맞음·비키면 안 맞음·쿨·게임 틱이 부름 / 삼연돌 세 번·소용돌이 끌림 / 장판 틱·꺼짐·호령 두 번·손잡이 0 (4) |
| 범위 | MODIFY `data-dungeon.js`(guard.sig) `dungeon.js`(guardZones·stepGuardSig·보스 루프) `fx3d.js`·`dungeon-view.js`(zone 효과, 링 풀 18→36) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(예고 원이 바닥에서 읽히는지·삼연돌이 너무 빠른지·소용돌이+장판이 억울하지 않은지) |

### 5.11 시련(試鍊) — 대균열식 15분 시간 도전 (§8 "빌드 다음" ②)
| 필드 | 내용 |
|---|---|
| 참고 | 디아블로3 대균열(진척 막대·수호자·시간 안 완주·단계 상승·순위표) |
| 표준 | H G F |
| 왜 | 비결·비전 빌드를 "얼마나 세졌나" 로 잴 자리가 없었다. 부적 던전은 소모품이라 반복 목표가 못 됐다 |
| 메커니즘 | 굴혈 선택 카드에서 열린 단계(위 셋)를 고른다. 부적 던전 틀(방·문 하나·적 배율 1+0.35×단계, 변형자 없음) 위에 15분 시계. 처치마다 진척(잡졸 3·정예 8·보스 10·그림자 1), 100 이 되면 그 방에 수호자(보스). 쓰러뜨리면 완주 — 전설 한 점(비전, §5.10)·공적 6+단계·순위표. 절반 넘게 남기면 두 단계, 아니면 한 단계 열림. **죽어도 안 끝난다** — 시계 −30초, 제자리에서 일어선다. 시간 초과는 주운 것만 확정(기록 없음) |
| 수치 | `TRIAL_SEC` 900 · `TRIAL_GOAL` 100 · 사망 −30초 · 상한 100단계 · 순위표 10 · 문턱 제10층 |
| 세이브 | `save.dungeon.trial = { best, open, runs, board:[{lv,sec,hero,deaths,at}] }` — 없으면 기본값으로 채운다 |
| UI | 굴혈 선택 카드 "⏳ 시련" 절(단계 단추 셋·순위표 5) · HUD 단계·시계(1분 미만 붉게)·진척 막대/수호자 · 종료 카드(단계·시간·순위·열린 단계) |
| 진단 | 문턱·열린 단계·배율 / 진척→수호자→완주·순위·전설·두 단계 / 쓰러짐 −30초·시간 초과(3) |
| 범위 | MODIFY `dungeon.js`(enter·die·update·kill·status·내보내기) `ui.js` `dungeon-view.js` `goals.js`. 자동 순회는 안 탄다 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(15분·진척 속도·수호자 세기·순위표 읽힘) |

### 5.19 몰이 사냥 1차 (2026-09-24 사용자 "몰이 사냥 못함·좁음·오브젝트 많음·모션")
방 700×440(1.5배) · 전투방 잡졸마다 졸개 `dg.pack`=2(체력55%·r10·드롭½, `core.hash2` — 난수 0) 상한 30 · 평타 앞 120° r×1.35 곁 60% · 강공격 앞 180° r×1.8 · 경직 `dg.stagger` 0.25초 · 들판 상한 `dg.fieldCap` 18·무리 4~6·3초 보충 · 세계 소품 `dg.worldDens` 0.5 · 던전 들판 충돌을 그림 밀도(`FIELD_D`)로(안 보이는 나무 버그) · 쓰러지면 날아가 눕고 가라앉음·움찔 0.28초(그림만). 진단 2. 실기 확인 대기. **2차**: swing 에 `arc`(원뿔 — 분쇄·기격·선풍·화선, 반경 2.6~3.2)·nova/curse 에 `pull`(0.3초 발밑으로, 보스 제외 — 진각·주박·위압·특이점) · 쓰러짐 방향 ±0.45rad 흔들림(해시)·힘 0.7~2.4(밀침·넘친 피해)·회전. 손잡이 `dg.cone`·`dg.pull`·`dg.scatter`. 진단 3. 실기 확인 대기

### 5.20 세 시대 사람·적 (SAGA-DESIGN §13 전체 퓨전, 2026-09-25 — 다섯 판 중 마지막)
| 필드 | 내용 |
|---|---|
| 왜 | §5.7 기계화 변종은 "몸은 그대로, 겉만 금속"(색만 다른 몸)이고, 지역은 시대 하나·둘뿐이었다 — §13 은 한 자리에 세 시대 |
| 적 | `data-enemy.js` ERA_ENEMIES 여덟(ENEMIES 밖): 단계마다 현대·미래 하나 — 폭주 청년·정찰 드론(원거리) / 방역복 추적자·경비 보행기 / 진압 특공대(원거리)·별바다 손님 / 실험실 장갑벌·강철 거신. 몸은 저마다 다른 제 클립 GLB(기존 적·보스가 안 쓰는 것, poly.pizza 사람 셋·Quaternius 외계·장갑벌·사가고에서 복사한 로봇 셋) |
| 섞기 | 들판 잡졸만 `dg.eraMix`(0.4) 몫을 그 지역 era 에 **없는** 쪽에서 — 현대·미래는 `eraPoolFor`(단계 이하·지금 단계 세 몫), 과거는 중원 명단. 보스·지역 우두머리·던전 방은 그대로 |
| 손님 | `town.js` ERA_FOLK 넷(택배 기사·출장 온 회사원 / 시간 여행자·탐사 대원) — 마을마다 id 해시로 하나, 기존 NPC·소품·표식·스폰에서 100 떨어진 빈자리(없으면 안 섬), 시트 없이 말만(대사 넷 돌림), 몸짓 JOB·`folk:*` 제 몸. NPC_DEFS 일곱은 그대로. `dg.eraFolk` |
| 세이브 | 없음 |
| 진단 | 여덟 제 몸·단계별 둘 / 땅 시대별 섞기·몫 약 40%·보스·방 그대로 / 손님 한 명·거리·볼일 없음(3, 제 씨앗 — 공유 수열 안 밂) |
| 구현(2026-09-25) | 코드분 완료. **실기 확인 대기**(사람 몸 적이 타격감·크기에서 어색하지 않은지, 손님 대사) |

## 6. 그래픽·에셋 (SAGA-DESIGN §6.1·§7 의 이 판 적용분)

### 6.1 적용 파일과 순서
1. `post3d.js` — 노출 1.0·블룸 임계 0.9·비네트 0.25 를 공통 규격으로(이미 그렇다, `EXPOSURE()`·`THRESHOLD()` 기본값). AUTO 등급 진동 쿨다운 1.5s 유지. LOW 는 후처리 off + `setRenderTarget(null)`(이미 있음). **(2026-09-17) 렌더 실패 시 자동 끔은 완료** — `draw()`를 `drawInner`로 옮기고 try/catch 로 감쌈(SAGA-DESIGN §8-6, saga-go 와 같은 처방). **(2026-09-19 문서 정정) 톤매핑 곡선도 실은 이미 결정돼 있었다** — 이 파일 머리글(2026-08-29대, saga-go 이식 당시)이 이미 `ACESFilmicToneMapping`을 걸었다가 "밤 화면이 망가진다"(하늘은 옅어지고 지붕은 그대로 검어 마을이 사라짐)로 물러나 `NeutralToneMapping`(Khronos PBR Neutral)으로 정착한 경위를 적어 뒀다 — SAGA-DESIGN §6.1 원문의 "ACESFilmic"은 이 판·saga-go·saga-story 셋 다 겪은 뒤 물러선 사실을 못 따라간 옛 기록이라 SAGA-DESIGN.md 쪽을 고쳤다(§6.1 참고). 이 항목이 "아직 안 건드림"이라 적혀 있던 것 자체가 착오였다 — §6.1 은 이제 코드분으로 전부 닫혔다.
2. `ssao3d.js` — 반경 0.6, 폰(픽셀 비율 상한 1.5)에서는 off.
3. **재질(툰)·외곽선 둘 다 완료(2026-09-17 재질, 2026-09-18 외곽선).** 새 `js/toon3d.js`: `dungeon3d.js`의 `mat()`·`texMat()`·`groundMat()` 셋 다, `asset3d.js`의 `delam()`(PBR 강제 해제)도 `world3d.toon`(기본 1)이 켜져 있으면 `MeshToonMaterial`+3단 그라디언트로 나간다. 재질 캐시는 그대로 색·질감 키 공유(가림 페이드는 여전히 인스턴스 복제).
   **뒤집힌 헐 외곽선(2026-09-18) — `box()` 호출부 태그가 아니라 `delam()`의 GLB 폴더 판별로 절충했다.** 이 판은 사가고처럼 소품 전용 파일이 따로 없어 `asset3d.js` 하나가 사람·짐승·몬스터·나무·건물·무기·갑주를 다 같은 `build()`/`delam()` 경로로 태운다 — `box()` 대신 `isActorAsset(url)`(`/models/(people|animals|monsters)/`)로 최종 GLB 폴더를 갈라, 배우(사람·짐승·몬스터 GLB)만 `toon3d.outline()`을 받는다. 나무·바위·건물·무기·갑주(NATURE·PROPS·BLD·WPN·GEAR)는 이 판별에서 자동으로 빠져 방 경계에 테두리가 생기는 옛 위험이 없다. `world3d.outline`(기본 1) 손잡이로 끌 수 있다. **실기 확인 남음**(§7.2에 반영) — 무기·갑주엔 외곽선이 없어 몸통과 어색하게 갈리는지.
4. **안개는 없다**(`scene.fog=null`, 사용자 지시). 땅 끝이 각지면 안개를 되살리지 말고 `fieldVisR` 을 넓힌다. 대신 하늘 그라디언트(`hdri/` 재사용)와 지평선 색 일치로 깊이를 낸다.
5. **지형 트라이플레이너 3타일 완료(2026-09-18)** — `buildField()`(들판 칸, `fieldJobChunk`)의 바닥만 새 `js/terrain3d.js`로 잔디·흙·돌(`textures/land/`) 셋을 실시간 블렌드한다. `MeshToonMaterial.onBeforeCompile`로 `map_fragment` 청크를 갈아 끼워, 월드 좌표 3방향(XY·YZ·XZ) 투영 + 노멀 가중(진짜 트라이플레이너, UV 없음) + 경사(slope, 계단처럼 놓인 칸 옆면일수록 돌 비중↑) + 절차 노이즈(잔디/흙 전환, fbm 2옥타브)로 섞는다. 방(room) 바닥의 `groundMat()`(노이즈 반점)과 멀리 있는 `skirt`(배경 벌판)는 안 건드렸다 — 성능·카메라 거리상 디테일 차이가 안 보인다. **풀 바람 셰이더 완료(2026-09-19)** — 새 `js/sway3d.js`(사가고 `world3d.js`의 `swayify()`를 그대로 옮김)가 `dungeon3d.js`의 `mat(hex,'sway')`(잡초 도형 fallback·갈대)와 `asset3d.js`의 `delam()`(잡초 GLB, `isSwayAsset()`로 나무·바위와 구분)에 둘 다 건다. **길 데칼도 완료(2026-09-19)** — 새 `js/road3d.js`가 `p.t==='path'`의 단색 상자를 흙 텍스처(`textures/land/dirt.webp`, terrain3d 와 같은 파일) + 가장자리 알파 페이드(`onBeforeCompile`로 `map_fragment` 뒤에 `vUv.y` 기반 smoothstep)로 갈아 끼운다. **남은 것: 항목 1(톤매핑 곡선 자체, 위 참고)뿐** — 나머지 §6.1 항목은 전부 코드분으로 닫혔다. §6.3(팔레트 스냅·kitbash·타일 24)도 여전히 남아 있다.
6. 그림자 — `PCFSoftShadowMap`, 카메라 추적 직교 라이트 1(범위 60), 접지 blob 그림자 겸용(횃불 점광은 그림자 없음).
7. 카메라 — TILT 0.62·FOV 46 고정(§2.2). 흔들림은 §3-C 규격만.

### 6.2 판별 팔레트 24색 초안(바이옴 기본 8 + 명·암 각 8 = 24, 명은 L+18%·암은 L−18%)
| mood | 바닥 | 바닥2 | 벽/돌 | 나무/기둥 | 잎/천 | 금속 | 빛(횃불·이펙트) | 강조(적·위험) |
|---|---|---|---|---|---|---|---|---|
| 마을(모루골·손 4) | `#7a6a4e` | `#5e5240` | `#8b8378` | `#5a3e2b` | `#6f8f4a` | `#9aa3a8` | `#ffb347` | `#c94b3b` |
| 들(forest·mountain·swamp·ruins·shrine 공통 기조) | `#5f7a3f` | `#4a5f33` | `#6e6a62` | `#4d3626` | `#8bb060` | `#7d858a` | `#ffd27a` | `#b8432f` |
| 굴혈(고분·폐성·산채) | `#2a2620` | `#1e1b17` | `#4a4238` | `#3b2f24` | `#4e5a3a` | `#6c6a66` | `#ff9a3c` | `#d3402e` |
| 굴혈(수궁·지옥문·천계) | `#1e2a30` | `#2c1f1f` | `#453c58` | `#2f4650` | `#3a5a66` | `#7a7088` | `#7ad0ff` | `#ff5a4a` |
- 층 테마 `floor`/`wall`/`tint`(data-dungeon.js) 는 이 표의 굴혈 행과 맞춘다(값은 이미 근접). 마을 biome 5 는 들 행에서 잎·바닥만 biome 별로 바꾼다(forest 잎 `#8bb060`, swamp `#6f8a5a`, ruins 바닥 `#6e6a62`, mountain 바닥 `#7d7368`, shrine 강조 `#c9a76c`).
- 모든 GLB 정점색·텍스처는 임포트 시 이 24색으로 스냅(SAGA-DESIGN §7.2 `palette.py`). 사람형 GLB(QRPG 6·MPFB 20)는 피부·머리색만 예외 8색.

### 6.3 변형 배가 계획(SAGA-DESIGN §7.2)
- **몬스터 135**: 짐승형 GLB 47 + 공룡 6 + 괴물 5 = 58 몸을 이미 파생(색·저항)으로 95 로 늘렸다. 다음: `palette.py` 로 티어별 팔레트(1 회색·2 갈색·3 붉은·4 보라+발광) 자동 스냅 → 같은 몸이 티어마다 달리 보임(G 표준). 정예 접두는 크기 1.15× + 외곽선 색.
- **건물**: Kenney Fantasy Town 조각 + 기와·초가 지붕 조각으로 `kitbash.py` 조합 20종 → 손 마을 4 는 손 배치, 절차 100 은 biome 별 5종 풀. 시대 층(5.7) 소품 5 는 `procgen.py`(굴뚝·태양광 판·관측탑·케이블 기둥·홀로그램 비석 = 원기둥·판·격자·발광 재질만으로 절차 생성 가능).
- **지형 타일**: `tilegen.py` 로 잔디·흙·돌·늪·눈·용암 6종 × 팔레트 4행 = 24 타일.
- **UI**: 9-slice 패널 1종(놋쇠 테두리, 현 diablo.css 결)·아이콘은 SVG→PNG 일괄. 폰트 Cinzel(OFL) + 본문 1종 유지.
- **소리**: `sfx.js` 절차 생성 유지, 타격음 3종·저스트 회피·축복 획득 큐 추가. 사가국지 절차 사운드 결 이식.
- 출력은 `assets/generated/`(원본 팩과 분리), 씨앗 고정. 커밋 방식은 §10-Q3.

### 6.4 교체 대상(코드가 그리는 것 → 에셋)
| 지금 코드가 그리는 것 | 갈아 끼울 것 | 비고 |
|---|---|---|
| 갑주 fallback 상자(가죽 갈색·판금 은회색) | `gear:armor:leather/plate` GLB 2(이미 등록) | 실기 fit 미확인 |
| 유적·길목 표식(기본 비석 갈래) | 제단·비석·나그네 GLB 3 | Kenney Graveyard altar 재사용 가능 |
| 2D 스프라이트 걷기 사인파 | CC0 스프라이트 시트 자르기 | 없으면 2D 는 현행 유지 |
| 인물 초상(portrait3d 렌더) | 유지(3D 렌더가 이미 에셋 기반) | 5.8 컷인에 재사용 |
| 데미지 숫자·콤보 텍스트(fx 'get') | 유지(텍스트는 코드가 그려도 됨) | 규격만 §3-C |

## 7. 버그·안정화 (Phase 0 에서 닫을 목록)

### 7.1 원인 미확정·방어만 건 것
1. **"이동할 때마다 갈색 화면"** — `render()` rk 재구성·`town.build()` 실패를 try/catch 로 감쌌고 부팅은 배너로 잡는다. 안개 제거 뒤 제보가 멎었지만 근본 원인은 모른다. 재발 시 콘솔 `[loop] 프레임 실패`·`[던전 3D] 방/들판을 다시 짓다가 실패`·`[마을] build() 실패` 중 무엇이 찍히는지로 가른다. **오류 링버퍼(SAGA-DESIGN §8-2)를 `_admin.html` 에 넣어 제보가 스택으로 오게 한다.**
2. **anchor 점프** — 들판 한복판에서 `nearestTownId` 가 바뀌면 `run.anchor` 가 한 프레임에 튀어 카메라가 휘청. **코드분 완료(2026-09-24, 실기 확인 대기)** — `dungeon3d.js` `rebaseCam()` 이 앵커가 바뀐 프레임에 camPos·camLook 을 (옛 앵커 − 새 앵커)만큼 옮겨 세계 기준 제자리로 둔다(처방이던 "0.3s lerp" 는 로컬 공간에서 오히려 쓸림을 만들어 안 씀). 진단 1. 앵커 전환 때 들판 지형이 바뀌어 보이는 것(씨앗이 앵커 기준)은 별개로 남음.
3. **가림 페이드(InstancedMesh)** — 진짜 페이드가 아니라 0.001 배 숨김. 처방: `instanceColor` 알파 대신 커스텀 셰이더 `aFade` 속성(Phase 6 그래픽에서).
4. **QRPG_SEEDS 표 안전성** — `mpfb_female` 이 헤드리스를 죽인 것이 레시피 탓인지 환경 탓인지 미확정. 실기 확인 항목.

### 7.2 실기 확인 대기(HANDOFF 에서 모음 — 닫히면 이 목록에서 지운다)
- 오픈월드: 걸어서 모루골→갈대나루 순간이동·끊김 없음 / 절차 마을 하나 진입 / 격자 흔들림(TOWN_JITTER 600)이 "자연스러운가" / 마을 20→100 뒤 데스크톱 집·기둥 밀도 / 통로 어귀 테마·안개 없는 땅 끝
- 카메라·화면: 들판 끝에서 캐릭터 따라옴(§55) / 가림 페이드 나무 뒤 / 안개 제거 뒤 땅 끝 각짐 / 코너 미니맵 들판 추적 / 자동지도 소품 점 밀도
- 전투: hitstop 3단(잡졸60·정예90·크리120ms, §5.8①)이 "버벅임" 없이 "묵직함"으로 느껴지는지 / 강공격·회피·F 버튼 배치가 미니맵·조이스틱과 안 겹치는지 / 어그로 "!" 타이밍·소리 / chain 손맛 / 회피 굴림 모션(레시피별 대체) / 서명 무예 105 각 체감(어드민 시험 발동으로) / 투장 무예 발동 손맛 / 화면 흔들림(평타1.5·크리4px, §5.8①)이 과하거나 부족하지 않은지 / 저스트 회피(§5.8②, 보스 예고 0.15s 창)가 실제로 맞춰 누르기 적당한지 / 타격음 3종(hit1/2/3)·무기 look 결이 귀에 걸리지 않는지
- 콘텐츠: 방랑 상인 만나서 사기 / 길목 발견거리·유적·탐험 문턱 토스트 / 마을 경비(넉백으로 마을 진입 안 함) / 위성 마을 NPC 배치(미완)
- 그래픽·에셋: 갑주 GLB 2 fit / 몬스터 88종 GLB 첫 표시(`monster:` lookup 고침 뒤) / 공룡 6·괴물 5 크기·위압감(`외눈귀` 사람 크기, `집게괴` quad 근사) / 외모 커스텀 6 버튼 / 티어별 색 구분 / 보물+ 무기 잔광·전설 파티클(§5.8③)이 화려하거나 초라하지 않은지 / 외곽선(§6.1-3) 몸·무기 갈림·몬스터 88종·자세 변화 / 트라이플레이너(§6.1-5) 잔디·흙·돌 세 갈래·테마 색조·폰 로딩 / 풀 바람(한 박자 흔들림·도형→GLB 차이) / 길 데칼 페이드 폭·지형 색조
- UI: 설정 시트 그래픽 품질 즉시 반영 / 진동 지원 기기에서만 줄 뜸 / 전체지도 안내문 겹침 / 목표판(§5.6) `#goals` 줄이 지갑 줄과 안 겹치는지·폰 폭 펼침 탭 동작 / 축복(§5.1) 카드 축 아이콘·테두리색이 실제로 잘 읽히는지·거절 버튼 오조작 여부 / 화면 흔들림·타격 정지 손잡이(§5.8①)가 설정 시트에서 잘 보이는지 / 서명 무예 컷인(§5.8③, 화면 좌하단)이 다른 HUD와 안 겹치는지
- 동행: 장비 바꿔도 장면 재진입 전엔 겉모습 안 바뀜(알려진 한계)
- 유품(§5.2): 표식 비석이 첫 방 어디 서야 자연스러운지(지금 0.3W 고정) / 죽었다가 그 층에 다시 왔을 때 "여기서 죽었었지"가 체감되는지 / 회수 카드에서 4점 이상 잃었을 때 3점 고르는 손맛이 아쉽지 않은지 / 사망 카드 "유품으로 남은 장비" 문구가 헷갈리지 않는지
- 난입(§5.5): 방 크기를 표준 그대로 쓴 것이 "좁다"고 느껴지는지(원문은 3×3, 전역 상수 리팩터 범위 밖이라 축소 구현) / 파도 30초 간격이 숨 돌릴 틈 없이 빡빡한지 여유로운지 / 8스택 상한을 넘겨 레벨업이 계속될 때 뽑을 축복이 바닥나 보이는지(모든 축 만렙 근접) / 결사비 옆 표식 자리(205,335)가 붐비지 않는지 / 15분 완주 보상(초당 금0.8)이 일반 던전 파밍과 비교해 적당한지
- 부적 던전(§5.3): 변형자 9종 중 방마다 실제로 체감이 다른지('암흑'은 2026-09-23 에 시야 반경 150 밖 적을 미니맵에서 지우고 화면 가장자리를 어둡게 덮게 했다 — 덧씌우기 농도·반경이 적당한지, 3D 안개는 그대로라 "어둡다"가 충분히 느껴지는지) / 방 하나당 door 하나뿐인 단순 진행이 밋밋하게 느껴지는지(트로브·사당 같은 갈림길을 뺀 대가) / 5방째 정예 무리 수(4+⌊T/3⌋, 상한 7)가 그 티어에서 벅찬지 헐거운지 / '촉박' 75초가 빡빡한지 / 굴혈 앞 선택 카드에서 부적 목록이 많아지면(20까지) 스크롤이 불편한지 / 실패(시간 초과)했을 때 노획물이 통째로 사라지는 것이 납득되는지
- 밸런스(§5.1): 8스택 회차 DPS 가 0스택의 2.2배 이내인지 `_admin.html` 시뮬레이터 실측 / "부대 전원 흔들림" 을 `ring` fx로 대신한 것이 어색하지 않은지
- 월드 보스(§5.4): 예고 HUD("⚠️ 세계 보스 예고")가 지갑·목표판 줄과 안 겹치는지 / 75초가 실전에서 타이트한지 여유로운지(HP 8배 기준 손 계산만 했다, 실측 없음) / 부위 파괴 아이콘(🗡️🛡️⛑️)이 사라지는 게 눈에 띄는지 / 15분 간격으로 마을을 옮겨 다니는 동선이 억지스러운지 / 3D 에서 무기·갑주가 실제로 사라져 보이는지(actorOf 키 갱신 방식, 처음 실기 확인)
- 세 시대(§5.20): 사람 몸 적(특공대·방역복)의 크기·타격감 / 로봇 셋 걷기 / 손님 자리·대사 돌림
- 시대 퓨전(§5.7): 현대·미래 30명의 2D 초상이 기존 룰(ruleLook, might→창·wisdom→죽간 실루엣)을 그대로 타서 "시대에 안 맞아 보이는지"(전용 초상 자산 없음, 알려진 한계) / lance_e·gauntlet 3D 도형(발광 상자)이 기존 무기들과 어울리는지 / 절차 마을에서 시대 층 건물(폐공장 굴뚝 등)이 실제로 보이는지(들판 반경 안쪽이어야 한다, ring 3~4권) / 시대 층이 판정에 안 걸려 뚫고 지나가지는지(의도한 설계 — 잡초 층과 같은 자리라 원래도 안 막는다) / 기계화 몬스터 10종의 금속색이 원본과 헷갈리지 않는지 / 도감 era 필터 버튼이 폰 폭에서 안 넘치는지

### 7.3 Phase 0 작업
1. **완료(2026-09-17)** — 오류 링버퍼 50건(`js/errlog.js`) → `_admin.html` "오류" 탭.
2. **완료(2026-09-17)** — `core.js` `SAVE_VERSION`·`MIGRATIONS`·`migrate(save)`(옛 세이브를 버리지 않는다).
3. **완료(2026-09-17)** — 강공격·회피·hitstop·콤보 자가진단 4항목.
4. **완료** — `tools/precheck.sh` 구문·문서 크기 검사(상세는 HANDOFF 2026-09-17).
5. 7.2 목록을 사용자 실기 세션 1회로 몰아 확인 → 닫힌 것은 지우고 남은 것만 이슈로.

## 8. 로드맵

| Phase | 내용 | 채우는 표준 | 완료 조건 |
|---|---|---|---|
| **0 안정화** | §7.3 전부, 실기 확인 세션 1회 | — | 7.2 목록 절반 이하, 오류 탭 동작, 진단 303+ |
| **1 루프·손맛** | 5.6 목표판·카드·일일주간 + 5.8 손맛 2차 + 5.1 축복 3택(**코드분 전부 완료, 2026-09-18** — 실기 확인 남음) | A B C D G | 부팅 직후 목표 3줄, strike 5요소, 축복 축 중복 0 |
| **2 실패·회복** | 5.2 유품(**코드분 전부 완료, 2026-09-18** — 실기 확인 남음) | F B | 사망→재도전 조작 3회, 유품 회수 진단 4(닫힘) |
| **3 반복 구조** | 5.3 부적 던전 + 5.5 난입(**둘 다 코드분 완료, 2026-09-18** — 실기 확인 남음) | F G H C | 티어 10 도달 가능(닫힘), 난입 15분 완주(닫힘) |
| **4 세계 시계** | 5.4 월드 보스(**코드분 완료, 2026-09-18** — 실기 확인 남음) + 일일/주간에 연결(완료 — goals.js `w_worldboss` 스텁 해제) | H E C | 슬롯 결정성 진단(닫힘), 75초 종료(닫힘) |
| **5 퓨전** | 5.7 인물 30·무기 look 2·시대 층 decor·기계화 변종 10(**코드분 완료, 2026-09-18** — 실기 확인 남음, 다섯 벌 편입은 여전히 보류·§10-Q1) | G E | 블랙리스트 0(닫힘), 135 sigOf 전원(닫힘), 다섯 벌 동기 결정(보류) |
| **7 빌드** | 5.9 비결·빛기둥(**코드분 완료, 2026-09-24** — 실기 확인 남음) | D G | 비결 다섯 진단(닫힘) |
| 8 빌드 다음 | ① 5.10 비전 · ② 5.11 시련 · ③ 5.16 몸짓 · ④ 5.17 동행 서명·합격(**넷 다 코드분 완료, 2026-09-24**) | H G C D | 비전 2·시련 3·몸짓 4·동행 서명 3(닫힘) |
| **9 고정 세계** | 5.12 고정 세계 지도·지역 아홉·끼임 수정 · 5.13 지역 몬스터·위험도·우두머리 · 5.14 지역 사연 사슬 · 5.15 명소 층(**넷 다 코드분 완료, 2026-09-24**) · 5.18 명소 주인 고유 수(**코드분 완료, 2026-09-24**) · 5.20 세 시대 사람·적(**코드분 완료, 2026-09-25**) · 다음 후보: 명소 층 전용 3D 소품(왕릉 석상·산채 깃발 등) | E G H | 세계 지도 진단 5·지역 진단 4·사연 진단 4·명소 진단 4(닫힘) |
| **6 그래픽 통일** | §6.1 1~7 + §6.3 팔레트 스냅·건물 kitbash·타일 24 | G(시각) | 스타일 혼재 0, 폰 60fps(MEDIUM), 실기 스크린샷 확인 |

각 Phase 끝: `_test.html` 3회 동일, ADMIN n/n, `sw.js` 버전, HANDOFF 에 날짜 절 append, README "어디까지 왔나" 덮어쓰기, 이 PLAN 은 결정이 바뀐 자리만 수정.

## 9. 검증

```
구문     node -c js/<파일>.js
진단     chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom http://127.0.0.1:8792/_test.html | grep -o "RESULT [0-9/]*"
어드민   _admin.html?selftest → ADMIN n/n   ·  _admin.html 프리셋(갓 시작·명품·보물·…)·서명 무예 시험 발동
데모     _demo.html#dg(4인 부대 실시간) · #camp · #gear3d(몬스터 장구) · #me3d(-plate/-style)
Node     _admin.html 스크립트 순서를 vm 으로 읽어 데이터 구조 검사(몬스터 135 lookup·서명 무예 105 발동·이름 블랙리스트)
```
- 세 번 돌려 출력이 한 줄도 다르지 않아야 한다(SHA256 대조). 씨앗 mulberry32(20260824), three 는 씨앗 전에 실린다.
- 시각 의존 축(5.4 월드 보스·5.6 일일)은 진단에서 `Date.now` 를 고정한다 — 안 그러면 돌린 시각에 따라 깨진다.
- **개발 중 스크린샷 금지**(루트 CLAUDE.md). 실기 확인은 사용자. 스크린샷을 요청받아 찍으면 PID 로만 정리.
- 데이터 표를 늘린 뒤 무관한 항목이 깨지면 로직이 아니라 RNG 순번(§2.3)부터 의심한다.

## 10. 열린 질문(사용자 결정)

- **Q1 인물 30 추가 방식 — 확정(2026-09-18)**: 이 판 전용 `data-hero-ext.js` 로 먼저 실험한다(권장안 그대로). 검증되면 다섯 판 공통 `data.js` 편입 여부를 다시 정한다 — 그 전까진 다른 네 판 도감엔 안 나온다.
- **Q2 낮밤·날씨 — 확정(2026-09-18)**: 넣지 않는다(권장안 그대로, 던전 러닝 결 유지). 월드 보스(5.4)는 `Date` 슬롯만 쓰므로 낮밤 없이도 지장 없다.
- **Q3 생성 에셋 커밋 — 확정(2026-09-18)**: `assets/generated/` 산출물을 커밋한다. GitHub Pages 가 정적이라 빌드 단계 없이 바로 서빙하려면 산출물 커밋이 필요하다는 판단 그대로 채택.
- **Q4 결사(하드코어)와 유품 — 확정(2026-09-18)**: 명예 도감 등 대체 보상을 두지 않는다. 결사는 "유품 없음"이 곧 하드코어 정체성이라, 다른 형태로라도 안전망을 만들면 그 결이 흐려진다.
- **Q5 무예 120 축소 — 확정(2026-09-18)**: 겹치는 조합(모양×원소 재탕)을 4갈래로 접는다 — 축복 3택 도입 뒤에도 선택지가 줄지 않는다고 이미 확인됐다. 데이터만 삭제하고 세이브 `skills` 키는 유지.
- **Q6 실기 확인 세션**: Phase 0 의 7.2 목록을 한 번에 확인할 시간(폰 30분 + PC 30분)을 잡을 수 있는지 — 사용자 일정에 달려 있어 보류. 못 잡으면 Phase 1 은 진단만으로 진행하고 7.2 는 계속 쌓인다.
