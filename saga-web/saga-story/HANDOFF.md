# 사가스토리 HANDOFF — 세션 이력 (append-only)

이 판의 날짜 달린 세션 기록만 여기 쌓는다. 설계 정본은 `PLAN.md`, 지금 상태는 `README.md`, 저장소 전체는 `../../SAGA-HANDOFF.md`.
**통째로 읽지 않는다** — `grep -n "^## \|^### "` 로 목차를 뽑고 날짜·키워드로 grep 해 그 절만 `sed -n` 으로 읽는다.
2026-09-16 이전의 세션별 이력은 `README.md` 하단(2026-08-25 ~ 2026-09-11 절들)과 `../../SAGA-HANDOFF.md` 의 사가스토리 항목에 있다.

## PLAN.md 에서 옮겨 온 기록 (2026-09-16, 한 글자도 바꾸지 않았다)

# 6-1. 오버월드 — 마을에서 나가 다른 마을로 + 전체지도 (2026-09-01, 사용자 요청)

사가블로(`../saga-dungeon/PLAN.md` 28-1절)에 적은 것과 같은 방향이다: "던전을
통해 가는 게 아니라 마을 통해서 입구를 나가서 가는 거임, 다른 마을도 가고
다른 지역이라는 개념." 이 판(메이플스토리식)은 원작부터가 **마을↔필드↔마을을
포탈로 잇는 구조**라 이 개념이 가장 자연스럽게 들어맞는 판이다.

- 5절이 이미 "시작 마을 + 초원 + 숲 + 폐허 + 동굴"을 구역으로 나눠 뒀다 — 이걸
  각자 독립된 마을이 아니라 **하나의 사냥터 지역**으로 볼지, 마을을 여럿 두고
  그 사이를 필드로 연결할지는 아직 정하지 않았다.
- 디아블로 M키처럼 **미니맵을 펼쳐 보는 전체 지도**도 같이 필요하다 — 지금까지
  밟은 사냥터/마을이 오버월드의 어디에 있는지 한눈에 보여주는 것.

**착수함(2026-09-02).** 사용자가 "마을을 여러 개로 늘려 각 구역 초입에 배치"를
골랐다. 삼국지 실제 지명으로 마을 다섯을 새로 넣고 기존 사냥터 넷 사이에
끼워 넣었다(`js/data-side.js` STAGES) —

```
신야성(시작) → 허도 → 허창 들판 → 강릉진 → 오림 숲
             → 남정성 → 한중 굴혈 → 기산채 → 호로곡
```

- 마을은 `town:true` 만 붙은 사냥터 항목이다 — 옆으로 걷기·문(포탈)·발판 엔진은
  그대로 쓰고 `spawn:0`·보스 없음으로 전투만 뺐다(따로 엔진을 새로 만들지 않았다).
  각 마을의 `need` 는 그 마을이 지키는 사냥터와 같아 레벨 문턱이 그대로 이어진다.
- 새 세이브 기본 위치를 `field` 에서 `sinya`(신야성)로 바꿨다(`js/side.js`).
  기존 세이브는 이미 있던 스테이지 키(field/forest/cave/gorge)를 그대로 쓰므로
  깨지지 않는다.
- `js/ui.js`에 **M키(데스크톱) + 상단 도구줄 🗺️ 버튼(모바일) + 사냥터 시트 안
  "🗺️ 전체 지도" 단추**로 여는 오버월드 전체 지도(`#owmap`, `openOverworldMap`)를
  새로 넣었다. 9칸(마을 5 + 사냥터 4)을 사슬로 그리고 지금 있는 곳을 금빛으로,
  잠긴 곳은 흐리게 보여 준다 — 읽기 전용(이동 버튼 없음).
  `viewField()`의 던전-게임 `viewWorldMap()`류와 겹치는 이름은 없다(이 판엔
  애초에 그 개념이 없었다).
- 자가진단(`_test.html`)도 새 구조에 맞춰 고쳤다: 문 하나짜리 이동 검증은
  '들판→강릉진'으로, 넷째 계단 검사는 `town:true` 를 뺀 사냥터 넷만 보게,
  기존 '굴혈→호로곡' 단발 사슬 검사는 '신야성→…→호로곡' 전체 9칸 사슬을
  한 칸씩 걸어보는 항목으로 넓혔다. 헤드리스 3회 106/106 동일.


## 2026-09-17 — SAGA-DESIGN §8-3 세이브 마이그레이션 지뢰 제거 (다섯 판 공통, saga-go 세션에서 발견)

`js/core.js`의 `load()`가 `parsed.v !== 1`이면 무조건 세이브를 버리는 하드 체크였다 — 사가고
그래픽 작업 중 이 판이 다섯 벌 복사본이라는 걸 확인차 훑다가 다섯 판 전부 같은 코드임을 발견했다.
`SAVE_VERSION`·`MIGRATIONS`·`migrate()`(순수 함수)를 추가해 버전이 안 맞아도 지우지 않고
`mergeDeep(freshSave(), ...)`로 넘기게 고쳤다(자세한 설계 이유는 `saga-web/saga-go/HANDOFF.md`
2026-09-17 절 참고 — 다섯 벌이 동일 로직이라 설명을 안 되풀이한다). `sw.js` VERSION 도 같이 올렸다.

## 2026-09-17 — SAGA-DESIGN §6.1 툰 재질(외곽선 제외) + §8-6 후처리 자동 끔

사가고→사가블로에 이어 "이어해줘 묻지말고"로 이 판까지. 세이브 마이그레이션 지뢰(core.js
`parsed.v !== 1` 하드 체크)는 다섯 판 공통이라 이미 별도 커밋으로 먼저 고쳤다(자세한 설계는
`saga-go/HANDOFF.md` 2026-09-17 절).

**새 파일 `js/toon3d.js`** — 3단 램프 `MeshToonMaterial`(다른 판과 같은 규격). 이 판은
`side-view3d.js`가 하늘·바닥·소품·배우 재질을 **곳곳에서 낱개로** `new Tc.MeshLambertMaterial({...})`
로 만든다(공용 캐시 함수가 없다) — 그래서 사가고식 outline 시스템 대신, `side-view3d.js`
맨 위에 `LM(opts)`(같은 옵션 객체를 받아 손잡이 보고 Toon/Lambert 를 고르는 대역 함수)를
추가하고 파일 안 19곳의 `new Tc.MeshLambertMaterial(` 를 전부 `LM(` 로 기계적으로 치환했다
(옵션 객체 리터럴은 손 안 댐 — 생성자 이름만 대역). **외곽선은 뺐다** — choke point 가
없는 파일 구조라 소품마다 잘못 붙을 위험을 무릅쓰지 않았다.

**`js/asset3d.js`** — `delam()`이 손잡이 보고 `toon3d.toonify()`로 벗긴다(다른 판과 동일).

**`js/post3d.js`** — 사가고·사가블로와 동일한 처방: `draw()`→`drawInner`+try/catch, 실패
시 `ready=false`·`failed=true`로 후처리 영구 끔 + (`DG.errlog` 있으면) 기록.

**`index.html`·`_demo.html`** — `toon3d.js`를 `asset3d.js` 앞에 얹었다. **`sw.js`는 안
건드렸다** — 확인해보니 이 판의 `SHELL`(오프라인 캐시 목록)엔 애초에 `asset3d.js`·
`side-view3d.js`·`post3d.js`가 없었다(3D 레이어는 SW 프리캐시 대상이 아닌 게 이 판의
기존 방침으로 보인다) — 없는 목록에 새 파일만 끼워 넣는 건 일관성이 안 맞아 그대로 뒀다.

**검증** — `node -c` 전 파일 통과, 두 HTML 인라인 스크립트 `new Function()` 파싱 확인,
`bash tools/precheck.sh saga-web/saga-story` → PRECHECK OK. `_test.html`은 애초에
`side-view3d.js`/`asset3d.js`를 안 실어(순수 함수만 별도로 검증하는 구조) 이번 손질과
무관 — 헤드리스 3회 확인도 이번엔 안 돌렸다(사용자 실기 확인 몫, 다른 판과 같은 이유).

**남은 것** — 외곽선, mood 별 24색 팔레트 스냅(`sky`/`forest`/`cave`/`fire` 넷), 변형 배가,
오류 링버퍼 — 전부 PLAN §6·§7 에 남겨 뒀다.

## 2026-09-17 — SAGA-DESIGN §8-2 오류 링버퍼 (다섯 판 공통, saga-go 규격 그대로)

넷째. 새 `js/errlog.js`(storageKey `yeoksa-side/errlog`, `index.html`/`_admin.html`/
`_test.html` 맨 첫 스크립트). `_admin.html`에 "오류" 탭(QA 프리셋과 점검·백업 사이) —
`js/admin.js`의 `renderAll()`에 `renderErr()` 추가, `bind()`에 다시 읽기·복사·비우기 버튼
배선. `_test.html`에 순수 함수 `push()` 상한 진단 1항목. `sw.js` `SHELL`에 `errlog.js`
추가, `VERSION` `side-v0.46.8` → `side-v0.47.0`.

**검증** — `node -c` 통과, `bash tools/precheck.sh saga-web/saga-story` → PRECHECK OK.

## 2026-09-17 — asset3d.js를 _test.html에 얹고 mapClips 진단 추가

사가국지 3D 진단 공백을 메꾸다가 이 판도 `_test.html`이 `asset3d.js`(GLB 클립 이름
매칭 `mapClips()` 등)를 아예 안 실어 진단 0항목이던 것을 발견해 메꿨다. `index.html`과
같은 순서로 `side-view.js` 뒤·`gear.js` 앞에 `vendor/three.iife.js`·`toon3d.js`·
`asset3d.js`만 추가했다(`portrait3d.js`·`ssao3d.js`·`post3d.js`·`side-view3d.js`는
`mapClips` 진단에 필요 없어 안 얹음 — `game.js`의 `global.DG.sideView3d` 참조는 전부
`if (global.DG.sideView3d)`로 존재 확인 뒤 부르므로 안 얹어도 안전).

새 진단 1항목: `mapClips` — 사가고·사가의숲과 같은 표로 검증(뒤섞인 클립 이름 4개가
제자리를 찾는지, 빈 목록이 빈 표인지).

**검증** — `node -c` 대상 없음(html 자체 편집), `bash tools/precheck.sh
saga-web/saga-story` → PRECHECK OK. `sw.js` 버전 안 건드림(`_test.html`은 SHELL 목록
밖). 헤드리스 3회 확인은 이번에도 안 돌렸다(사용자 실기 확인 몫) — 대신 `asset3d.js`
모듈 최상위에 THREE·DOM 의존 부작용이 없는지(전부 함수 안에서만 `three()`를 늦게
부른다) 코드로 확인했다.

## 2026-09-17 — SAGA-DESIGN §8-3 "옛 세이브 안 버려짐" 진단 1항목 추가

`core.js`의 `SAVE_VERSION`/`MIGRATIONS`/`migrate()`는 이미 있었는데(같은 날 앞 세션)
이 회귀를 잡아 줄 `_test.html` 진단이 saga-go 에만 있고 나머지 네 판엔 없었다
(PLAN §7 목록의 "아직" 항목). saga-go 것을 그대로 옮겨 붙였다 — `C.migrate({v:999,...})`가
지우지 않고 그대로 돌려주는지만 확인(`aa4b8b8` 류 지뢰 재발 방지, 다섯 판 공통 지뢰라
`saga-go/HANDOFF.md` 2026-09-17 절 참고).

**검증** — 헤드리스 3회 동일 확인(직접 돌림, 예외적으로 이번엔 코드 한 줄 진단이라
가벼움). `bash tools/precheck.sh` 대상에 포함해 PRECHECK OK.

## 2026-09-18 — §10 열린 질문 Q1·Q2·Q4·Q5·Q6 확정, Q3 보류

PLAN.md §10 여섯 질문을 Q1 순서부터 처리:

- Q1 웹 사가스토리는 SAGA-DESIGN §2-1 "샌드박스"(다섯 판 공통 결) 그대로 —
  Phase 3~5 는 3D 트랙에서만.
- Q2 SP 총량 축소(레벨당 3→2): 옛 세이브 초과분 유지(권장안) 채택, 환급 안 함.
- Q3 resetJob() 비용은 밸런스 체감 판단이 필요해 보류.
- Q4 스승 인물 16명: HEROES 의 faction·stats 로 자동 배정(권장안) 채택.
- Q5 비경은 발판 기반 노드 지도로 한정 — 사가블로 던전과 안 겹치게, 다섯 판
  별개 프로젝트 원칙에 맞춤.
- Q6 펫 초상 62종: 조사해 보니 이미 2026-09-11 에 사슴·구미호만 3D 로 적용되고
  나머지 62종은 2D 로 남은 상태였다(완료된 작업, PLAN 문구가 옛 상태였을 뿐) —
  그 상태를 그대로 확정으로 반영.

코드 변경 없음, PLAN.md §10 문구만 갱신(Q6 은 실제 완료 상태를 반영하도록 수정).

## 2026-09-18 — §10-Q3 확정: resetJob() 하루 1회 무료

사용자 결정: 금 비용은 두지 않고 **하루 1회**로만 제한한다. `js/job.js`
`resetJob()`에 실제 달력 날짜 기준 잠금을 넣었다 — `quest.js`가 일일 사명에
쓰는 것과 같은 결(`todayKey()`, `getFullYear()-getMonth()-getDate()`)을
job.js 안에 따로 두었다(모듈 간 로드 순서에 기대지 않으려고 복붙, 두 줄짜리라
공유 모듈로 뺄 정도는 아니다). 세이브에 `save.jobResetDay` 한 칸 추가 — 오늘
이미 되돌렸으면 토스트만 띄우고 막는다.

PLAN.md §2-5·§7(체크 항목 7번 지움)·§10-Q3 갱신. `_test.html`에 회귀 진단
1항목 추가("전직 되돌리기는 하루 1회로 막힌다") — 첫 되돌리기는 통과, 같은
날 두 번째는 막히고 직업이 그대로 남는지 확인.

**검증** — `node -c js/job.js` 통과.

## 2026-09-19 — 초상 3D↔2D 깜빡임 버그 수정(다섯 판 공통, 상세는 saga-go HANDOFF)

제보: "캐릭터 인물 사진들이 2D 스프라이트랑 겹쳐서 깜빡인다"(화면 전체 공통).
`js/ui.js`의 `pt()`가 이미 3D 초상을 구워 둔 인물도 매번 캔버스 2D 그림으로
`<img>`를 새로 시작하고 40ms 뒤 `sweep()`이 갈아 끼우는 구조라, 같은 인물을
반복해서 열 때마다 2D→3D 로 깜빡였다. `pt()`와 도감 상세 두 자리(`dt-portrait`,
hero/pet 150×172)에 `p3src()`를 추가해 캐시가 있으면 처음부터 그 그림으로
시작하게 고쳤다. `node -c js/ui.js` 통과. 실기 확인 대기.

## 2026-09-19 (이어서) — PLAN §5-7 손맛 표준 적용 — Phase 1 착수

§8 로드맵이 Phase 0(코드분 완료, 실기 확인 대기) 다음으로 남긴 Phase 1
"감각"의 첫 후보(5-7 손맛 표준)를 짰다. 사가블로 `dungeon.js`가 먼저
밟은 hitstop·라운드로빈 타격음 요령을 이 판 구조(사이드스크롤, `strike()`
하나로 근접·돌진·AOE·화살이 다 모인다)에 맞춰 옮겼다.

- **hitstop** — `side.js`에 `run.hitstopT`·`run.hitSeq` 신설. `update()`
  머리에서 `dt = Math.min(dt, 0.05)`로 자른 **그 값**을 `hitstopT`에서 뺀
  뒤, 남아 있으면 그 프레임에 쓸 `dt`만 ×0.15 — 사가블로 `dungeon.js`
  `update(dt)`와 같은 자리, 같은 순서(줄이기 전에 시간부터 깎아야 hitstop
  이 실제 경과 시간대로 풀린다 — 순서를 바꾸면 hitstop이 사실상 안 풀리는
  사고가 난다, 테스트로 직접 잡았다). 값은 잡졸 70ms·급소 120ms·보스
  90ms(`HITSTOP_NORMAL/CRIT/BOSS`) — 이 판은 보스가 이미 12~17배 피해라
  손맛보다 "안 밀린다"쪽이 더 크므로 급소보다 살짝 낮게 잡았다(PLAN 수치
  그대로).
- **피격 플래시** — `e.hurt`를 크리 여부와 무관하게 `HURT_FLASH`(0.08) 한
  값으로 통일(예전엔 0.22/0.3 두 값). `side-view3d.js`의 `tintHurt(g,hurt,
  span)`에 `span` 인자를 새로 받아 `k = min(1,hurt/span)`으로 정규화했다
  — 예전 `hurt*3` 그대로 두면 0.08짜리 짧은 값은 `k=0.24`(옅은 번쩍)로
  펀치감이 없었을 자리다. `span`을 안 주면(플레이어 피격, 옛 호출부라
  이번엔 안 건드림) `1/3` 기본값으로 예전과 같은 곡선을 낸다. GLB 사람형
  (`stepActor`의 `flashMats` 길)도 같은 `hurtSpan`을 읽게 짝을 맞췄다.
- **흔들림** — 이제 **모든 타격**이 2px/80ms 를 낸다(예전엔 급소·100 이상
  거함타만 흔들렸다, PLAN이 "지금은 급소만"이라 적어 둔 그 구멍). 급소
  8px/220ms·거함타 3.6px/180ms는 그대로 얹는다. `fx.push`가 `amt`·`span`
  을 실어 보내고, `side-view.js`의 `shakeOf()`는 그 값이 있으면 그대로
  쓰고 없으면(보스킬·플레이어 피격 같은 옛 호출부) `big` 이진값으로 옛
  두 단계로 돌아간다 — 그 호출부는 안 건드렸다. **설정 시트 "화면 흔들림"
  손잡이(0 없음·1 약·2 보통, 기본 2)** 신설 — `core.save.settings.
  shakeLevel`, `side-view.js`의 `shakeLevel()`/`setShakeLevel()`을
  `ui.js`가 부른다(`gq-set`과 같은 자리, `data-act="shake-set"`).
- **타격음 라운드로빈** — `sfx.js`에 `hit2`·`hit3` 신설(필터·박자는
  `hit`과 같고 톤 층 주파수만 ±6% 밀었다 — 사가블로가 필터로 셋을 가른
  것과 다르게, 이 판은 PLAN 원문의 "피치 변주"를 그대로 따랐다).
  `run.hitSeq`를 급소가 아닌 타격마다 돌려 `HIT_CUES[seq]`를 부른다 —
  급소는 여전히 `crit` 소리 그대로(라운드로빈 밖).
- **숫자 팝(④)**: 손 안 댔다 — 이미 25/21/17px(급소 비율 1.47×)로 PLAN
  수치(1.4×)에 근접해 있었다.
- **효과별 감촉 차이(멀리·돌진 잔상·착탄 다발·관통 표시선)는 이번에 안
  갔다** — 별개의 시각 효과 작업이라 스코프 밖으로 남겨 뒀다(PLAN §5-7
  구현 완료 각주에도 적어 뒀다).

**검증** — 이 판은 다른 네 판과 달리 헤드리스 1회가 §9 정본 절차다(다른
판처럼 "이 판 규칙"으로 뺀 적 없음) — 실제로 돌렸다. `node -c`(side.js·
side-view.js·side-view3d.js·sfx.js·ui.js) 통과. `chrome --headless=new
--dump-dom` 로 로컬 정적 서버(임시 node 서버, 포트 8794 — python이
WindowsApps 스텁이라 안 돌아 대신 씀) 위에서 3회 돌려 **161/161 동일**.
처음 돌렸을 때 기존 테스트 하나("맞은 적은 밀리고, 보스는 밀리지 않는다")
가 hitstop과 같이 걸려 6프레임 안에 넉백이 못 끝나 깨졌다 — 그 테스트가
재는 것은 hitstop이 아니라 넉백이라 `run.hitstopT = 0`으로 그 축만 풀어
줬다(테스트만 고침, 게임 로직은 안 건드림). 새로 넣은 라운드로빈 진단도
`castSkill()`이 `swing` 소리를 같이 적는다는 것을 놓쳐 한 번 깨졌다가,
`hit`/`hit2`/`hit3`만 걸러 보게 고쳐 통과시켰다. `_test.html`에 §5-7
진단 3개 추가(5요소 동시 발생·hitstop dt 축소+실제 시간 감소·라운드로빈
3종). 임시 서버·헤드리스 크롬 전부 정리(PID로, `node.exe`/`chrome.exe`
이미지 이름으로는 안 죽였다). `sw.js` `side-v0.48.0` → `v0.49.0`.
`bash tools/precheck.sh saga-web/saga-story` → PRECHECK OK.

**실기 확인 남음**: hitstop이 버벅임 없이 묵직하게 느껴지는지, 모든
타격에 흔들림이 걸려도 과하지 않은지, 흔들림 설정 3단이 실제로 체감이
갈리는지, 타격음 피치 차이(±6%)가 귀에 걸리는지, GLB 사람형 적의 플래시
세기가 도형 적과 같아 보이는지 — §7(10번)에 반영.

**남은 것**: §5-7의 효과별 감촉 차이(멀리·돌진·AOE·관통 각각 다른 시각
효과)와 실기 확인. 로드맵 Phase 1의 다음 후보는 5-5(이동 손맛) → 5-6
(목표판·카드) 순서(PLAN §8).

## 2026-09-19 (이어서) — §5-7 남은 조각: 효과별 감촉 차이(dash·rain·bolt 시각효과)

바로 위 세션이 "손 안 댔다"고 남긴 조각을 마저 했다. `melee`(짧고 무거움)는
기존 hitstop·흔들림 표준 자체가 그 값이라 따로 손댈 게 없다고 판단해
건드리지 않았다 — 나머지 셋만 순수 시각(fx) 추가, 판정·수치는 무수정:

- **dash 잔상** — 밀고 나가는 순간이 한 프레임뿐이라(순간이동에 가깝다)
  지나간 자리에 형체가 안 남아 허전했다. `from`→`p.x` 사이에 몸 그림자
  4개(`ghost` fx, foot-center 좌표, 40×56 반투명 사각형)를 심었다.
- **rain 착탄 다발** — 기존 `rain` fx는 띠 하나를 덮어씌울 뿐 몸에 닿는
  느낌이 없었다. 맞은 적마다 몸 위 무작위 점 3곳에 작은 X자국(`impact` fx,
  0.22~0.32s)을 흩뿌렸다.
- **bolt 관통 표시선** — 화살(점 하나)과 구별이 안 됐다. 쏜 자리(`ox`,
  생성 시 고정)부터 지금 위치까지를 옅은 그라디언트 선으로 그어 "뚫고
  지나간다"는 궤적을 남긴다(`pierce` 참인 것만).

범위: `side.js`(fx 데이터 push만, `castSkill()`의 dash·rain·bolt 분기 +
`strike()` 호출 앞뒤), `side-view.js`(`ghost`·`impact` 렌더 케이스 추가,
기탄 그리는 루프에 `pierce` 분기 추가). `side-view3d.js`는 이 fx 타입들을
안 봐서(2D 오버레이가 전부 그린다) 손대지 않았다. `strike()` 시그니처·
hitstop·흔들림 수치는 그대로 — 감촉 차이는 전부 화면 층 몫으로 남겼다.

`node -c side.js side-view.js` 통과. `bash tools/precheck.sh
saga-web/saga-story` → PRECHECK OK(도감 md5 불일치는 기존 것, 이번과
무관 — WARN 그대로). `sw.js` `side-v0.49.0` → `v0.49.1`. 헤드리스 검증은
안 돌렸다(그림만 늘렸지 판정 진단 대상이 아니다 — 실기 확인으로 미룸,
PLAN §7 10번에 반영).

**실기 확인 남음**: 그림자·X자국·관통 선이 눈에 띄면서도 화면을 어지럽히지
않는지. **다음 후보**: PLAN §8 로드맵대로 5-5(이동 손맛) → 5-6(목표판·카드).

## 2026-09-19 (이어서) — PLAN §5-5 이동 손맛: 대시 재정의·코요테·버퍼·벽 차기·착지 롤

로드맵 다음 후보 그대로 진행. 판정을 안 흔들고 방향·타이밍만 너그럽게
받아 주는 것들이라 전부 `side.js` 입력·물리 층 안에서 끝났다.

- **대시(회피 재정의)** — 수치만 PLAN대로: 쿨 3.0→0.9s, 무적 0.35→0.12s
  (거리 140 은 그대로). 원래도 공중에서 막혀 있지 않았어서 "공중 1회
  포함"은 새로 짤 것 없이 쿨타임이 그 역할을 한다.
- **코요테 타임(0.1s)** — `update()`가 발판을 "걸어서" 막 떠난 프레임(코드가
  직접 onGround 를 끈 게 아니라 물리가 감지한 경우, `vy>=0` 로 구분)에
  `p.coyoteT` 를 켠다. `jump()`는 `p.onGround` 대신 `p.onGround ||
  p.coyoteT>0` 로 받는다.
- **점프 버퍼(0.12s)** — 셋 다(땅도 코요테도 벽도) 아니면 `jump()`가
  `false` 를 돌려주는 대신 `p.jumpBufferT` 에 담아 두고, 착지 판정
  (`update()`의 `p.onGround && wasFalling` 블록) 안에서 그대로 이어 뛴다.
- **벽 차기** — 전용 `walls` 배열이 없어 **발판(`stg.plats`) 옆면(표면부터
  바닥까지)을 벽으로 삼는다**(PLAN 이 적어 둔 대안 그대로). `wallSide(p)`가
  방향을 돌려주면 반대로 튄다(수평 320·수직 620).
- **착지 롤** — 이 판엔 원래 착지 경직이 없었다. "경직 대신"이 아니라
  **낙하 속도 1200 초과 시 새로 얹는 보상 동작**으로 넣었다 — 0.15s 동안
  입력과 무관하게 보던 방향으로 밀린다.
- **줄 점프 수평 가속 +30%** — `letGo()` 호출 인자에 ×1.3 만 곱했다(기존
  0.8배 위에 얹는다).
- **구조적으로 걸린 함정**: 이 판의 좌우 이동은 **관성이 없다** — 매 프레임
  입력으로 `p.vx`를 다시 정한다(`update()`). 그래서 벽 차기·착지 롤처럼
  "입력과 무관하게 잠깐 밀리는" 동작은 일반 이동과 같은 자리에서
  `p.wallKickT`/`p.rollT` 창 동안만 `p.vx`를 강제로 덮어쓰는 분기로 넣어야
  했다 — 물리에 새 축(관성)을 얹지 않고 기존 프레임 재계산 구조에 얹은
  것이라, 두 창이 겹치면(착지 롤 도중 벽 차기 등) 나중 것이 이긴다(코드
  순서상 `rollT` 분기가 먼저라 벽 차기가 이긴다 — 실전에서 거의 안 겹칠
  조합이라 우선순위를 더 다듬지 않았다).
- **auto.js "대시 사용 1줄"(PLAN 지정)** — 적의 공격 몸짓(`atkAnim`)이 걸린
  채 사거리 안이고 회피 쿨이 찼으면 자동으로 `S.dodge()`. 사람 손 없이도
  대시의 무적 창을 살려 쓴다.

`_test.html`에 §5-5 진단 5개 추가(코요테 성공/실패·점프 버퍼·벽 차기
방향·착지 롤) — §5 항목을 먼저 넣고 코드를 짠 게 아니라 코드를 짜며 같이
넣었다(구현이 단순해 앞뒤 순서가 결과에 안 갈렸다). 임시 정적 서버(node,
스크래치패드에 둠, 포트 8794)로 `chrome --headless=new --dump-dom` 3회 →
**166/166 동일**. 서버는 PID로 정리(`node.exe` 이미지 이름으로는 안
죽였다), 크롬은 `taskkill //F //IM chrome.exe`(이미 다 끝나 있었다).
`node -c side.js auto.js side-view.js` 통과. `bash tools/precheck.sh
saga-web/saga-story` → PRECHECK OK(도감 md5 불일치는 기존 것). `sw.js`
`side-v0.49.1` → `v0.50.0`(새 손맛 항목이라 마이너 올림).

**실기 확인 남음**: §7 11번 그대로 — 대시가 잦아져도 긴장이 안 빠지는지,
코요테·버퍼가 "덜 억울하다"로 느껴지는지, 벽 차기 방향이 안 헷갈리는지,
착지 롤이 "굴렀다"로 읽히는지. **다음 후보**: PLAN §8 로드맵대로
5-6(목표판·세션 마무리 카드).

## 2026-09-19 (이어서) — PLAN §5-6 목표판 + 세션 마무리 카드 — Phase 1 코드분 마감

로드맵대로 5-6 을 마저 해서 Phase 1(감각) 셋(5-7·5-5·5-6)이 전부 코드분
완료됐다. §5-4(관문 대장·주간)·§5-3(비경)이 아직 없어(Phase 3) PLAN 원문
그대로는 못 채우는 두 자리가 있었다 — 대역과 함께 넣고 PLAN §5-6 에도
남겼다:

- **HUD 3줄째 "이번 주"** → 관문 대장이 없어 **다음 예고**(추적 사명이
  끝나면 이어질 사명 미리보기)로 대신했다.
- **세션 목표 후보 "비경 1층"** → 뺀 5종(사냥 30·보스 1·채집 20·금
  3000·사냥터 2곳)만 돌린다.
- **카드의 "도감 진척(인물 등용·업적)"** → **업적만** 반영했다. 찾아보니
  이 판엔 "등용서"(재화)를 써서 도감에 새 인물을 들이는 기능 자체가 없다
  (재화만 있고 소비하는 자리가 없다 — 아마 다른 판에서 그대로 물려받은
  뼈대). 인물 등용 델타는 항상 0일 수밖에 없어 넣어 봐야 죽은 줄이라 뺐다.

**구조**: `core.save.track`(추적 사명 키 하나) + `quest.js` 안의 메모리
전용 `session{kills,bossKills,gathers,gold,visited}`(세이브 안 함, PLAN
그대로) — 기존 `side:kill`·`side:gather`·`side:enter`/`side:travel` 훅에
그냥 한 줄씩 얹었고, 금만 이벤트가 없어 `changed` 를 새로 걸어 player.gold
오름폭을 누적했다. `take()`가 사명을 받자마자 `core.save.track`을 스스로
물고, `turnIn()`이 그 사명을 바치면 `autoAdvanceTrack()`으로 다음을 잇는다
("완료 시 다음 사명 자동").

**밟은 함정(바로 그 자리에서 고쳤다)**: 처음엔 HUD 가 읽는 `trackedInfo()`
가 스스로 `autoAdvanceTrack()`을 불러 "추적 중인 게 없으면 즉석에서 하나
물어 오는" 편한 설계였다. 그런데 이 함수는 `renderTop()`(=`renderGoals()`)
가 **'changed' 마다, 그리고 tickRefresh() 0.15s 마다** 부르는 자리라 —
기존 사명 진단 하나("받기 전엔 0이어야 한다")가 실제로 **깨졌다**: 테스트가
`clearQuests()`(=`C.emit('changed')`)를 부르자마자 HUD 가 다시 그려지며
`q_first`를 조용히 자동으로 받아 버려, 정작 테스트가 `Q.take('q_first')`를
부르기도 전에 이미 진행도가 올라 있었다. **화면을 그리기만 해도 판정이
바뀌면 안 된다**는 원칙이 깨진 자리였다 — `trackedInfo()`를 완전히
읽기 전용으로 되돌리고, 판정을 실제로 바꾸는 자리를 `ensureDailyTrack()`
(부팅 1회)·`turnIn()`(사명 완수 시) 딱 둘로 좁혔다. `_test.html`에 이
회귀를 그대로 진단으로 남겼다("trackedInfo() 는 읽기만 한다").
그 진단을 처음 짤 때도 한 번 더 헛짚었다 — `Object.keys(save.quests)
.length===0`으로 "아무 사명도 안 받아졌다"를 재려 했는데, `rec()`가
**조회만 해도**(`taken()` 등) 빈 칸을 만들어 두는 게 이 파일의 원래
습관이라(모든 `list()` 호출이 그런다) 그 자체로 FAIL이 났다 — "키가
느는가"가 아니라 "`taken` 시각이 찍힌 게 있는가"로 바꿔 잡았다.

**세션 카드**: 새 시트를 안 만들고 기존 `openSheet`/`renderSheet`/
`closeSheet` DOM 을 그대로 썼다(`sessionEnd` 라는 이름만 하나 늘었다).
`side:end`(나가기·죽기 둘 다)를 듣고 열며, 5초 뒤 저 혼자 닫힌다(그새
사람이 다른 시트를 열면 `openTab==='sessionEnd'`가 아니라 안 건드린다).
`side.js`는 `run.expGained`(잡을 때마다 누적, 사명 보상 exp 는 안 잡는다
— 사냥 자체로 번 것만)·`run.gearFound`(실제로 주운 것만, 가방 가득 차
못 주우면 안 잰다)를 얹었고, 업적 진척은 `enter()`/`leave()`·`die()`
스냅 차로 잰다.

**HUD 자리**: `.profile`/`.wallet` 처럼 `#top` grid 에 세 번째 줄
(`goals goals goals`)로 넣었다 — 새 오버레이 컨테이너를 안 만들고 기존
고정 상단 바에 얹었다. 폭이 좁아지면(≤560px, 폰 세로) 2·3번째 줄을
`display:none` 으로 접어 1줄만 남긴다 — **"+ 탭" (눌러서 펼치기)은 이번엔
안 넣었다**, 그 부분만 미룬다(§7-12 에 실기 확인으로 반영).

`node -c side.js quest.js ui.js` 통과. `_test.html`에 §5-6 진단 5개
추가(부팅 직후 3줄·읽기전용 회귀·추적/자동전환·카드 next·일일 자동수령).
임시 정적 서버(포트 8794, 스크래치패드) + 헤드리스 3회 → **171/171 동일**.
서버는 PID로 정리, 크롬은 `taskkill //F //IM chrome.exe`. `bash
tools/precheck.sh saga-web/saga-story` → PRECHECK OK. `sw.js`
`side-v0.50.0` → `v0.51.0`.

**실기 확인 남음**: §7-12 — HUD 3줄이 사냥 중 거슬리는지, 접속목표 5종이
반복으로 느껴지는지, 좁은 화면 1줄 접힘이 정보 부족으로 느껴지는지, 세션
카드 5초가 적절한지, "다음에 할 것"이 실제로 다음 행동을 끌어내는지.

**다음 후보**: Phase 1(감각) 코드분이 다 끝났다 — PLAN §8 로드맵대로
**Phase 2(선택)**: 5-2(유파 재해석·SP 총량 축소) → 5-1(직업 정체성·스승
인물) 순. 착수 전 PLAN §5-1·5-2 원문부터 다시 읽을 것(스승 인물은 기존
스토리 시스템과 맞물려야 해서 범위가 더 넓을 수 있다).

## 2026-09-19 (이어서) — PLAN §5-2 무예 96 재해석 — 유파·SP 총량 축소

로드맵대로 Phase 2 첫 항목. `data-job.js` SKILLS 96행 중 92행(무명 넷
제외)을 사슬(need 로 이어진 1~4차)별로 묶어 4갈래×6유파=24개 유파에
태그했다 — 갈래마다 tier1이 정확히 6개씩이라(멀리서 보면 "6사슬"이 데이터에
이미 있었다) 태그 자체는 스크립트 하나로 끝났다(스크래치패드에 매핑표 두고
정규식 치환, 92/92 확인).

**유파 이름**: 최대한 실제 스킬 이름 한자와 맞춰 골랐다(예: 궁수 "연(連)"은
연사(連射), "환(環)"은 환시(環矢), 협객 "풍(風)"은 네 사슬 스킬 이름에 전부
들어 있다) — 다만 완전한 규칙은 아니다. PLAN 원문 예시 "질(疾)=무사 dash
유파"부터가 돌진(突進)·철기돌격 등 스킬명에 疾 이 전혀 없는, 설계자가
순전히 어감으로 고른 이름이었다(질주=疾走 연상) — 그래서 이름 못 찾은
자리는 감으로 지었다(무사 파(破)=관통 계열, 협객 혼(魂)=버프 계열 등).

**보정 6종**(`SCHOOLS.kind`, 새 효과 없이 기존 수치만 곱하거나 더한다):
`dmg`(피해 mul, 7유파)·`aoe`(반경 r, 4유파)·`buff`(북돋움 지속 sec, 4유파)·
`heal`(회복 비율, 2유파)·`volley`(발수 +1/+2, 3유파)·`dash`(회피 재사용대기
배율 + 4세트는 급소 확정, 4유파 — PLAN 예시 그대로 재현). `job.js`
`schoolBonus(sk)` 하나가 다 계산해 `{dmgMul,aoeMul,buffMul,healMul,
shotsAdd,dodgeCdMul,critForce}`를 돌려주고, `side.js castSkill()`은 이걸
받아 제자리(mul·r·buff.sec·heal 비율·shots)에 곱하거나 더하기만 한다 —
**dmg 보정은 `mul *= sb.dmgMul` 한 줄**이라 PLAN이 말한 "적용 1곳"의
핵심은 정말 한 줄이었다(다른 보정들은 자기 갈래 분기 안에서 자기 필드만
만지니 필연적으로 여러 곳이지만, 전부 `schoolBonus()` 결과를 그대로 쓸
뿐 유파가 뭔지는 모른다). dash 세트의 회피 재사용대기 감소는 `dodge()`
(§5-5 대시)에도 한 줄 얹었다 — 지난 세션 것과 이번 것이 실제로 이어졌다.

**SP 총량**: `SP_PER_LEVEL` 상수 하나만 3→2 로 바꿨다. `spLeft()`가 이미
`Math.max(0, spTotal()-spSpent())`로 막아 두고 있어서 "옛 세이브가 넘게
찍어 뒀으면?" 걱정이 공짜로 해결됐다 — 찍은 건 그대로, 새로 못 찍기만
한다. 코드 한 줄 안 늘렸다.

**UI 자리를 PLAN 원문과 다르게 잡았다**: PLAN은 "띠 위에 유파 아이콘"을
말했지만, 이 판의 실제 사냥 HUD는 사용자 결정(2026-09-02, "스킬은 자동으로
써주면 공격 버튼만 보여도 되지 않나")으로 **공격 한 칸만** 보인다(나머지
여덟-슬롯 띠는 auto.js 가 조건대로 알아서 쓴다, 화면엔 없다). 유파 아이콘을
거기 얹는 건 있지도 않은 UI에 새 UI를 만드는 일이라, 대신 **무예 탭**
(`viewJob()`)에 넣었다 — 실제로 사람이 무예를 찍고 띠 구성을 보는 자리다.
무예 목록을 유파별 소제목으로 묶고("N세트 발동 중" 표시), 조작 띠 목록
각 줄에 `[유파명]`을 붙이고, 켜진 유파는 카드 테두리를 파랑(2세트)·보라
(4세트)로 물들인다(`--blue`/`--purple` 재사용, 새 색 안 늘림).

**밟은 함정**: 기존 회귀 진단 중 정확한 피해량을 어서트하는 것은 없었지만
(다행히 다 비율·부등호 비교였다), "3차 무예라야 호로곡의 정예를 감당한다"
테스트는 `{w_cut:10,g_smash:10}`(2개, w_jung 소세트)와 `{...,n_heaven:10}`
(3개, 여전히 소세트)을 비교해 **두 쪽 다 같은 ×1.15 를 받아 비율이
그대로 보존**됐다(1,413→1,625, 3,677→4,229 로 절댓값은 올랐지만 ×2.6 비율
동일) — 우연이 아니라 "세트 배율이 분자·분모에 똑같이 걸리는 비율 비교는
안전하다"는 걸 미리 따져보고 진행했다. 절대값을 재는 자리(`4차/3차 무예도
실제로 적을 친다`)는 원래도 `dealt > 0`만 봐서 무관했다.

`_test.html`에 §5-2 진단 3개 추가(세트 2/4 판정·배율만 바뀌고 효과 종류는
그대로·SP 초과 세이브 무손실), SP 식이 바뀌며 깨진 기존 진단 2개("무예
점수는 레벨에서 나온다"의 30/60 하드코딩, "무예 점수는 있는 만큼만 쓴다"의
"남은 점수" 계산식)를 `JD.SP_PER_LEVEL`/`J.spTotal()` 참조로 고쳤다.
`node -c data-job.js job.js side.js ui.js` 통과. 임시 정적 서버(포트 8794)
+ 헤드리스 3회 → **174/174 동일**. 서버 PID 정리, 크롬 `taskkill //F //IM
chrome.exe`. `bash tools/precheck.sh saga-web/saga-story` → PRECHECK OK.
`sw.js` `side-v0.51.0` → `v0.52.0`.

**실기 확인 남음**: §7-13 — 무예 탭 유파 묶음·세트 색이 "빌드를 고른다"로
읽히는지, SP 축소로 유파 2~3개만 만렙 가는 게 부족한지, dash 세트 회피
잦아짐이 체감되는지, 24개 유파 한자 이름이 감이 오는지.

**다음 후보**: PLAN §8 로드맵대로 **5-1(직업 정체성 — 갈래별 고유 조작
1개 + 스승 인물)**. 스승 인물은 기존 스토리(NPC_TALK·마을 사람) 체계와
맞물려야 해서 범위가 이번 것보다 넓을 수 있다 — 착수 전 PLAN §5-1 원문과
NPC 관련 구조부터 다시 훑을 것.

## 2026-09-19 — PLAN §5-1 직업 정체성 — 갈래별 고유 조작 + 스승 인물(Phase 2, 코드분 완료)

이전 세션이 "스승 인물은 NPC_TALK·마을 사람 체계와 맞물려야 할 수 있다"고
남겨 뒀지만, PLAN §5-1 원문이 "data.js HEROES 에서 faction·stats 로 고른다"고
명시해 그대로 따랐다 — NPC_TALK 는 안 건드렸다(마을 사람과는 별개 인물 풀).

**스승 16명 선정**: HEROES 의 `faction`은 위/촉/오 같은 **나라**라 직업군과는
무관해서, 실제로는 "직업 이모지·대사가 그 갈래와 맞는 인물"을 기준으로 골랐다
(4국 삼국지·한국사·일본사·세계사 고루 섞음) — data-job.js `MENTORS` 주석에
경위를 적어 뒀다. 무사=여포·계백·미야모토 무사시·레오니다스, 궁수=황충·태사자·
미나모토노 요시츠네·칭기즈칸, 협객=핫토리 한조·간녕·토모에고젠·샤카(줄루),
방사=히미코·제갈량(현책)·이븐 시나·다빈치.

**입력 층** — 회피 버튼(HUD `#hud-dodge`, 키보드 Shift)을 0.18s 넘게 누르면
갈래별 고유 조작이 터진다. 무사·협객·방사는 누르는 순간 바로(기력 12·쿨
6s), 궁수만 놓는 순간 눌린 시간(0.4~1.2s)으로 위력·관통을 정한다. 회피
단추는 `click` 을 버리고 `pointerdown`/`pointerup` 으로 직접 여닫는다(안
그러면 짧은 탭에도 click 이 한 번 더 `dodge()`를 불러 겹쳤다).

**판정** — 받아치기(다음 타격 ×1.5)·그림자 걷기(첫 타 ×1.8)·원소(화 지속·
빙 둔화·전 사슬)를 모두 `strike()` 한 곳에서 꺼내 쓰고 지운다(§5-2 유파
배율과 같은 요령). 화살·연사의 "관통 +1"은 `pierceLeft` 카운터를 새로
둬서 구현(원래 `pierce:false`면 첫 하나에서 멈추던 것). 원소는 새 적
데이터 칸이 아니라 `e.burnT`/`e.slowT` 런타임 값(§2-2 유지).

**UI** — PLAN 의 "링 게이지"는 이 판 쿨다운 표시(사각 채움 `<u>`)와 같은
결로 얇은 바(`<i class="hold">`)로 단순화했다. 무예 탭에 "고유 조작" 카드
1장 + 스승 4명(차수·등용 여부).

`_test.html`에 §5-1 진단 7개 추가(174→181). **`node -c` 전체 통과,
`bash tools/precheck.sh saga-web/saga-story` → PRECHECK OK — 다만 이번
세션은 서버·헤드리스 크롬을 안 띄우라는 지시가 있어 `_test.html` 자체를
실행해 보지는 못했다.** 코드 정독으로 갈래마다 맞물리는 것은 확인했지만,
**다음 세션에서 가장 먼저 헤드리스 3회 돌려 새 진단 7개가 실제로 통과하는지
확인할 것** — 이 세션의 "코드분 완료"는 그 확인 전이라는 뜻이다. `sw.js`
`v0.52.0` → `v0.53.0`.

**실기 확인 대기**(§7-14로 등록): 0.18s 임계 감촉, 궁수 당기기 이동 40%가
흐름을 끊는지, 협객 그림자 걷기가 "통과"로 보이는지, 원소 3종이 화면에서
구별되는지(현재는 다 같은 `impact` 이펙트 재사용이라 거의 안 구별될 수
있다), 스승 카드 4명이 폰 폭에 다 들어오는지.

## 2026-09-19 (이어서) — PLAN §5-4 관문 대장 — 미사용 보스 5(6 중 하나는 보류)를 마을에 주간 도전으로 (Phase 3 착수, 코드분 완료)

사용자에게 "§5-1 헤드리스 검증 먼저 vs Phase 3 §5-4로 진행" 을 물었더니
**"2,3 순으로"**(Phase 3 §5-4 코드부터, 그 다음 다른 판으로) — 이번 세션 시작
절차가 "헤드리스 크롬·서버는 띄우지 않는다"고 명시해 §5-1 헤드리스 검증은
건너뛰었다(§5-1 도 여전히 미확인인 채로 남아 있다).

**6 중 5만 배치, 하나는 보류** — PLAN 원문 "마을 5 + 비경 입구"(6 자리)인데
§5-3 비경이 아직 없어(로드맵상 5-4 다음 항목) 자리가 하나 모자란다. 이름이
가장 그 역할에 맞는 **관문 수호장**(tier4)을 남겨 뒀다 — §5-3 이 비경 입구를
낼 때 그리로 옮긴다. 나머지 다섯(산채 두령·왜구 선장·거란 도통·몽골
만호장·왜장)은 마을 이름과 어울리게 배치했다(산채 두령→기산채 등) — 순전히
멋뿐이고 난이도는 다섯 다 같다(그 마을의 `enemyLv` 기준 체력×40·공격×2.5,
보스 자체의 tier 는 이 기능에서 안 쓴다).

**도전은 사람이 연다** — 마을은 `spawn:0`(안전지대)라 사냥터 보스처럼
`enter()` 끝에서 저절로 안 나온다. `side.js`의 `challengeGate(key)`가 그
마을에 없으면 먼저 `enter(key)`로 들여보낸 뒤 그 자리에서 바로 gate 보스를
띄운다 — 사냥터 목록 시트(`viewField`)와 쉬는 중 카드(`renderCamp`) 둘 다에
"🏯 도전" 단추를 얹어서, 그 마을 안에 있든 메뉴에서 바로 누르든 같은 길로
들어간다.

**패턴 1(달려들기)은 새로 안 만들었다** — 기존 `e.boss` 돌진 분기(`chargeCd`)가
gate 보스에도 `e.boss=true`라 그대로 걸린다. 패턴 2(범위 표시 후 내려찍기)·
패턴 3(소환 2)은 `e.patternCd`로 번갈아 돈다. **패턴 실행 중(`e.patternT>0`)엔
`e.dir`이 안 바뀌게 얼어붙는다** — "등 뒤" 판정이 이 창에서만 성립하게 하는
장치다(`strike()`가 `e.dir`과 반대쪽에서 온 피해만 `gateShieldHp`에 쌓는다).
30%(`hpMax*0.30`) 넘으면 방패가 깨지고 10초 `gateVulnT`(그동안 피해 ×1.5) —
10초 지나면 방패가 다시 채워진다(한 판 안에서 여러 번 깰 수 있다, PLAN에
반복 여부가 안 적혀 있어 3분 싸움이 늘어지지 않게 이렇게 잡았다). 3분
초과는 패배가 아니라 `e.dmg` ×1.5 광폭만(PLAN 그대로, "실패"는 죽는 것뿐이다).

**리젠 규칙이 사냥터 보스와 다르다** — `bossAt`(분 단위 쿨)이 아니라
`gateWeek{stageKey:weekKey}`(이겼을 때만, 그 주 잠금) + `gateDay{stageKey:
dayKey}`(이기든 지든 도전한 순간 바로, 그날 잠금) 둘로 나눴다.
`challengeGate()`가 시작하자마자 `gateDay`부터 찍어 도전 중 새로고침으로
하루에 여러 판 하는 것을 막는다. PLAN 세이브 절엔 `weekly{...}` 하나만
적혀 있었는데 "실패 시 재도전은 다음 날"을 살리려면 최소 둘이 있어야 해서
늘렸다.

**보상은 확정**(뽑기 없음) — 고유 장비는 `core.save.side.gateUniq`가
`data-unique.js`의 `UNIQUES`를 순서대로 도는 커서(부위 순환), 주문서는
rate=0.6 셋(공격·방어·체력) 중 둘을 뽑는다. **기억 조각**(§5-3 재화)이
§5-4가 먼저 나와 버려 `core.save.player.memFrag`에 새로 얹었다(mergeDeep 로
옛 세이브도 채워진다) — §5-3 이 나올 때까지 **쓰는 자리가 없다**(§5-6 의
대역과 같은 결, 나중에 §5-3 이 여기 재화를 그대로 받아 쓴다).

`_test.html`에 §5-4 진단 5개 추가(181→186: 도전 성공·같은 주 재도전 거절·
방패 파괴+취약·3분 초과 광폭·보상 부위 순환). **`node -c` 전체 통과(side.js·
core.js·data-side.js·ui.js·side-view.js), 인라인 테스트 스크립트도 추출해
`node -c` 통과 — 하지만 이번 세션은 헤드리스를 띄우지 말라는 지시라 실제로
돌려서 RESULT 를 본 적은 없다.** §5-1 의 미확인 진단 7개와 함께 **다음
세션에서 가장 먼저** `chrome --headless=new ... _test.html` 3회 돌려 볼 것 —
이 세션의 "코드분 완료"는 그 확인 전이라는 뜻이다. `sw.js` `v0.53.0` →
`v0.54.0`.

**실기 확인 대기**(§7 추가): 관문 대장 등 뒤 판정이 실제로 손에 잡히는지
(패턴 실행 중에만 돌아갈 수 있다는 게 급박하게 느껴지는지), 3단 체력바 금이
보기에 자연스러운지, 마을에서 "🏯 도전" 단추가 다른 단추들 사이에서 튀지
않는지, 소환된 잡몹 둘이 좁은 마을 폭에서 난전을 만드는지.

## 2026-09-20 — 자체 몸짓 `js/anim-own.js` (VRM 몸이 Mixamo·UAL1 없이 움직인다, 다섯 판 공용 복사본)

사용자 "mixamo 가 깃에 배포가 불가하니 VRM 캐릭터에 애니메이션으로 재제작" → "자체 애니메이션을 제작해줘"
(범위: **사가웹만**, 이동 5종 + 전투 4종). (사가의숲에서 옮김 — 상세는 사가의숲 HANDOFF 같은 날 절)

**무엇**: `anim-own.js`(다섯 판 md5 동일 — 고치면 다섯 벌 함께) 가 VRM 휴머노이드(`J_Bip_*`) 몸짓 아홉을 **코드로 직접 짠다**:
Idle_Loop · Walk_Loop · Run_Loop · Jump · Land · Attack · Hit · Death · Dodge. 남의 데이터 없음(전부 수식·키).
자세는 뼈마다 (롤·피치·요)를 **정지 자세에서 뽑은 몸 축(위·앞·왼)** 기준 도(°)로 적고 뼈 로컬 회전으로 바꿔 30/60fps 로 굽는다.
발이 땅에 붙는 클립은 FK 로 발바닥 높이를 재서 엉덩이 높이를 자동으로 맞춘다(발이 뜨거나 박히지 않는다).
모든 클립이 같은 뼈 전부에 트랙을 갖는다(크로스페이드 때 앞 클립 자세가 안 남게).

**연결**(각 판 `asset3d.js`): `wantsOwnAnim(url)` — 경로에 `/people/anime/` 가 든 VRM 몸만, 손잡이 `world3d.ownAnim`(기본 1)이 0 이면 예전 길
(UAL1 retarget)로 되돌아간다. 자체 몸짓이면 UAL1(7MB)을 **받으러 가지도 않는다**. 몸이 조립된 장면에서 실제 뼈를 읽어 그 몸에 맞춰 굽고
(`parts.body.ownClips` 캐시), 못 만들면 그때 UAL1 을 받아 예전 길로 간다. `mapClips` 에 슬롯 `jump`·`land` 추가(다른 슬롯은 그대로).
`index.html`·`_test.html`·`_demo.html`·`sw.js` 에 스크립트 추가, sw VERSION 올림.

**검증(렌더 없이 FK 로 잰 것 — 눈으로는 못 봤다)**: 표준 VRoid 뼈대 + 아바타 4종 실제 GLB 뼈대 5개에서 — 아홉 클립 값 유한·단위 쿼터니언,
루프 셋 이음매 0, 걷기 보폭 0.6m·왼발 앞일 때 오른팔 앞, 접지 오차 <1cm, 공격 순간 오른손 0.58m 앞·예비 동작 머리 위, 쓰러진 뒤 머리 높이 ~0.2m·몸통 수평,
구르기 한 바퀴 뒤 서 있는 높이 복귀, 어느 관절도 땅 아래 4.5cm 넘게 안 박힘, 각속도 <3200°/s. 실제 three 뼈대에 `AnimationMixer` 로 돌린 왼발 위치가
FK 계산과 <1mm. 각 판 `_test.html` 에 항목 6개씩(`anim-own — …`). 시험은 **jsdom(node)** 으로 돌렸다(크롬·서버 안 띄움) — WebGL·캔버스는 스텁.
결과: 사가국지 156/156, 사가의숲 311/312, 사가고 505/507, 사가블로 353/355, 사가스토리 188/192 — **남은 실패는 전부 변경 전 HEAD 에서도 똑같이 난다**
(사가의숲 "깃발 그림"·사가고 "배우 조립·걸음"(jsdom 에 `Request` 없음)·사가블로 미니맵 둘·사가스토리 §5-1 궁수/§5-4 관문 대장 등 넷 —
**사가스토리 넷은 jsdom 한계인지 진짜 버그인지 아직 모른다**, 실크롬 `_test.html` 로 확인할 것).

**모른다(실기 몫)**: 실제 화면에서 자연스러운지(관절 각도·발 미끄러짐·구르기 궤적). 진폭은 `anim-own.js` 의 클립 함수(`walkPose` 등)에 숫자로 있다 —
보고 어색하면 그 숫자를 고치면 된다. 발 미끄러짐은 이동 속도와 보폭이 안 맞으면 생긴다(게임 쪽 걷기 속도에 맞춰 `Walk_Loop` 의 `dur`(1.0s)·`26`°를 조정).
**손 안 댄 것**: Mixamo 관련 옛 코드(`HERO_RECIPES_MIXAMO`·`world3d.mixamoReal`, 파일은 .gitignore)는 그대로 — 지워도 공개판 동작은 같다.
Godot·Unity 트랙은 이번 범위 밖. 새 인물 VRM 을 넣을 땐 경로에 `/people/anime/` 가 있어야 자체 몸짓 길을 탄다.

## 2026-09-20 — 인물 전원 VRoid 몸 + 인물별 색 변형 `js/vroid-variant.js` (다섯 판 공용 복사본, md5 동일)

사용자 "104명은 다 바꾸라고 했는데" → (a) 각자 새로 제작 + (b) 기존 몸 4개 배정·색 변형. **이 절은 (b)** — (a)(인물마다 다른 몸을 VRoid Studio 로 한 명씩 제작)는 코드로 못 하고 아직 시작 안 했다.
(인물 수는 104가 아니라 **105**다 — `DG.data.heroes.length`.)

**무엇**: `world3d.animeAvatar` 손잡이를 **기본 켜짐(1)** 으로 바꿨다(끄면 0). 그러면 인물이 `avatar_sample_a·b·c`·`avatar_custom_01` 넷 중 하나를
id 해시로 받는다(배정 코드는 이미 있었고 기본 꺼짐이었을 뿐). 넷만으로는 백여 명이 똑같아 보여서 `vroid-variant.js` 가 조립된 몸의 **머리(`_HAIR`)·옷(`_CLOTH`: 윗도리·아랫도리·신을 칸을 비켜 다르게)·눈동자(`EyeIris`)**
재질만 복제해 12×12×12 색 칸 중 인물 id 해시 칸으로 곱한다. 피부·얼굴·원본 재질은 안 바뀐다(재질을 복제하고 uuid+칸으로 캐시). `asset3d.js` 에 `applyVroid(model, rec, id)` 한 줄 배선.

**검증(렌더 없이)**: 진단 188/192 → 191/195(새 항목 3 — 인물 전원 칸 결정성·서로 다른 조합 ≥90%, 재질 분리·원본 불변·캐시·두 번 입혀도 원본 기준, 경로 판정·손잡이 기본 켜짐/끄기). 남은 실패 넷(궁수 당기기·화면 넷 분할·채집 보너스 지역·관문 대장)은 변경 전 HEAD 와 같다 — jsdom 한계인지 진짜 버그인지는 여전히 모른다(실크롬 확인). jsdom(node)으로 돌렸다(크롬·서버 없음).
**모른다(실기 몫)**: ① **색이 실제로 어떻게 보이는지** — 색은 텍스처 × `material.color` 곱이라 원본이 어두우면(검은 머리 등) 색이 덜 산다. 팔레트(`vroid-variant.js` `palette(채도, 명도, 밝기배수)` 세 줄)를 보고 조정.
② 기본 켜짐이라 처음 인물이 뜰 때 VRoid 몸을 받는다(몸당 5~9MB, 네 개). ③ 인물 역할·성별과는 무관하게 해시로만 배정한다(데이터에 성별 칸이 없다) — 여성형 몸이 장군에 배정될 수 있다.
**손잡이**: `world3d.animeAvatar` 0 → 예전(QRPG 등)으로 즉시 복귀. `sw.js` VERSION 올림.

## 2026-09-20 (이어서) — VRM 정면 -Z 보정 `faceFront` (상세는 saga-go HANDOFF 같은 날짜 절)

VRM 0.x 몸은 정면이 -Z 인데 배우는 +Z 가 앞이라는 가정으로 돌려 세워서 이동 반대·등 돌림·초상 뒷모습이 났다. `js/vroid-variant.js`(다섯 판 md5 동일)에 `faceFront` 를 더하고 이 판 `asset3d.js` 에서 부른다. 진단 1개 추가, `sw.js` VERSION 올림. **화면은 직접 못 봤다** — 실기에서 방향이 바로 섰는지 확인 필요.

## 2026-09-22 — PLAN §5-3 비경(祕境) — Phase 3 마무리 (코드분 완료, 실기 확인 대기)

"사가웹 이어서해 → 커밋 푸시하고 이어해". PLAN Q1(웹은 Phase 2까지)과 5-4·Q5 가 스스로 어긋나 있었으나 사용자가 5-4 를 웹에 짓게 한("2,3 순으로")
흐름을 이어 5-3 을 지었다 — PLAN §10 Q1 에 그 사실을 적었다. 설계 근거·원안과 다른 점은 PLAN §5-3 "구현 완료" 항목이 정본이다.

- **새 파일**: `js/data-rift.js`(표: 노드 종류·층 풀·축복 18·이벤트 3·주간 변형자 4)·`js/rift.js`(지도·축복·이벤트·보상·저장). index·_test·_admin·sw.js 에 등록, `sw.js` v0.63.0.
- **side.js 훅**(전부 `run.rm`/`run.rift` 가 있을 때만): power()(기억 조각 체력 강화)·enter(key, stgOverride)·swapStage/placeIn(travel 곁, 문 없이 방 갈아 끼우기)·strike(피해·급소·흡혈·정예 배율)·hurtMe(방패·철벽·불굴·보호)·kill(금·경험치 배수 + `rift.onKill` 이 사냥터 보스·리젠 분기 대신)·update 머리(`run.riftTick` → `rift.tick()`)·기력·체력 회복·이동·회피·탕약·leave/die(`got.rift` 요약)·resume(진행 중이면 `rift.restore()`).
- **왜 tick 인가**: 층이 끝났다는 걸 kill() 안에서 바로 처리하면 타격 반복문 한가운데서 `run.enemies`·`run` 이 바뀐다(정복 땐 leave()). 그래서 onKill 은 표시만 하고 update() 머리에서 정리한다.
- **UI**: 시트 `rift` 하나(문 앞·축복 3택·지도·이벤트·포기 두 단계), 사냥터 시트·쉬는 중 카드에 입구, 고를 차례가 오면 `rift:phase` 로 자동 열림·싸움이 시작되면 닫힘, 세션 카드에 비경 줄.
- **진단**: `_test.html` 10개 추가(지도 200씨앗 규칙·3택 100회·주간 변형자·시작→축복→전투 층·정예 실제 처치→축복·죽음→기록 삭제·수호장 정복·축복 엔진 반영(강건·방패·불굴)·조각 강화·메마른 길). jsdom **205/209 ×3 동일**, 실패 4건은 HEAD(195/199)와 같은 것 — 궁수 당기기·화면 넷 분할·채집 보너스·관문 대장 방패(jsdom 한계인지 진짜 버그인지 여전히 미확인, 실크롬에서 볼 것).
- **함정**: ① `rift.js` 를 `index.html` 에서 `quest.js` 뒤에 둔다(side.js 는 `global.DG.rift` 를 늘 존재 검사 뒤에만 읽는다). ② `swapStage` 는 무대만 갈고 `run.rift`/`run.rm` 은 그대로라 rift.js 가 `syncRun()` 으로 다시 맞춘다. ③ 비경 층 무대는 열린 사냥터의 `mood|width` 를 그대로 물려받아야 side-view3d 의 재빌드 키가 안 어긋난다(발판이 같은 사냥터 복제라 안전) — 새 무대를 손으로 지을 때는 `mood|width` 를 유일하게. ④ 갈림길 방(HUB)은 mood 'cave'·width 900.
- **실기 확인 대기**: 갈림길 방 배경(cave 무대에 발판 없음)이 어색하지 않은지, 시트가 싸움 시작 때 닫히고 정예·이벤트 뒤 저절로 열리는 흐름, 전투 층 40~60초 목표(목표 처치 수 7~10)·정예·수호장 난이도(체력 ×7·×26), 축복 18개 체감(특히 흡혈·방패·불굴), 기억 조각 획득 속도(정복 한 번 ≈ 조각 8~9).
- **남은 것**: 비경 입구를 마을 문 그림으로(지금은 단추), 갈림길 방 그림, 이벤트 종류 늘리기, Phase 4(5-8 동료 교대)는 Q1 때문에 사용자에게 다시 물을 것.

## 2026-09-22 (이어서) — PLAN §5-8 동료 교대 (Phase 4, 코드분 완료, 실기 확인 대기)

"커밋 푸시하고 이어해" — 비경(c04398e3) 푸시 뒤 이어서. 설계·원안과 다른 점은 PLAN §5-8 "구현 완료" 항목이 정본이다.

- **새 파일** `js/party.js`(교대·서명·복귀·HUD 요약). index·_test·_admin·sw.js 등록, `sw.js` v0.64.0. CSS `.hud-sk.pty.active/.dead`, 키 E(game.js), auto.js 체력 낮음 → 동료 교대.
- **side.js**: `meId()` 가 `run.pty` 를 본다(나온 인물 = 몸) · `enter()` 끝에서 `party.attach()` · `hurtMe()` 치명타에서 `party.onFall()`(불굴 다음, die() 앞) · `update()` 머리 `party.tick(dt)` · `travel()` 이 마을에 들면 `party.restore()` · `castSkill`→`castBody(sk, swapMul)` 분리 + `castSwap` 내보냄 · `status().party`.
- **side-view3d.js**: `playerMeshId` 로 인물이 바뀌면 지난 몸을 `disposeDeep` 하고 새로 세운다(2D 는 `meRef()` 를 매 프레임 읽어 저절로 바뀐다).
- **진단 함정**: 서명 1발이 **다음 update()** 에 나가므로 테스트에서 교대 직후 update 를 한 번 흘려야 한다(돌진이면 자리가 바뀐다) · 무작위 잡졸(role 이 magic/ranged)은 접촉 피해를 안 주니 `role='melee'`·`ranged=null` 로 못박고, 서명에 안 죽게 체력을 크게 · 새 진단 6개는 jsdom 211/215 ×3 동일(실패 4는 HEAD 와 같음).
- **실기 확인 대기**: HUD 초상 칸이 폰 세로 더미(28px 격자)에서 3+탕약+공격+회피가 안 답답한지 · 교대 때 3D 몸이 바뀌는 순간(VRM 새 로딩이라 잠깐 빌 수 있다) · 서명 아홉 효과 체감(특히 돌진·고취·치유) · 자동 사냥의 동료 교대 · 쓰러짐→자동 교대 흐름.
- **남은 것**: 편성 화면(도감 밖에서 셋 고르기·순서), 벤치 체력 회복 규칙 손보기(지금은 마을에 들 때만), 교대 몸짓·이펙트.

## 2026-09-22 (이어서 2) — 진단 실패 4건의 정체: 셋은 테스트 버그, 하나는 jsdom 한계

"미확인"으로 남겨 두던 `_test.html` 실패 4건을 계측한 임시 사본(삭제함)으로 가렸다. **jsdom 214/215 ×3 동일**, 코드(`js/`)는 안 고쳤다.

- **관문 대장 방패(§5-4)**: 테스트가 플레이어를 `e.x+10`(몸 중심 `e.x+26` 보다 왼쪽)에 세워 "정면 피해"가 됐다 — `e.x+40` 으로 고침. 게임 로직 문제 아님(등 뒤 판정 자체는 그대로).
- **채집 보너스**: 들판의 풀 셋(x=480·1050·1750 고정)이 우연히 보너스 구간 안에 있으면 같이 캐져 +4 가 됐다(구간이 무작위라 간헐적) — 다른 풀을 비워 둠.
- **궁수 당기기**: 당기는 동안 이동이 32.4px(기대 상한 30) — 첫 프레임 몫이 얹혀서다. 정상(67.5px)의 6할 아래(<40)로 완화.
- **화면 넷 분할**: `readZone()` 이 `getBoundingClientRect()` 를 쓰는데 jsdom 은 0 이라 늘 실패 — **jsdom 한계**(실크롬에서는 통과할 것). 이 하나만 남는다.

## 2026-09-22 (이어서 3) — 2D Phase 4: 사람·적 되돌림 코드 그림 삭제, 펫은 예외

사가고·사가블로·사가의숲에 이어 사가스토리도 2D Phase 4 요청. 옆면 뷰 `bake()`(사람·적 스탬프)와 `portrait()`/`portraitCard()`의 hero 갈래는 Kenney `human_*`(항상 있음)·`mon-manifest.js`(3종뿐이지만 적은 전부 `tier` 만 있고 `body` 가 없어 **모두 generic `beast` 한 장**으로 덮인다 — 사가블로에서 몸별로 60종을 따로 구운 것과 달리 이 판은 종별 구분이 없다)로 안전하게 전환 — 이미지 미로드 순간만 `loadingMark()` 자리표시.

**펫은 사가블로와 같은 문제** — 105종 중 원래 7종만 디스크 초상이 있고(`assets/portraits/pet`), 나머지는 `bake-portraits --kind=pet` 재시도(210회)로 **14회만 성공**. 2D 대체 그림도 없다. 사가블로에서 받은 사용자 결정("펫만 절차적 그림 유지")을 같은 상황이라 그대로 따랐다 — `human()`/`beast()`/전용 헬퍼는 안 지움, 펫 초상 경로에서만 여전히 부른다.

**진단**: jsdom 214/215(변경 전과 동일, 남은 하나는 `getBoundingClientRect` jsdom 한계). `node -c` 통과. precheck 통과. `sw.js` `side-v0.64.0`→`side-v0.65.0`.

**실기 확인 필요**: 옆면 뷰 사람·적이 이미지로 잘 보이는지(전엔 절차적 그림), 도감 hero 초상. 펫 초상은 전과 같다(변한 것 없음).

**남은 것**: 사가국지만 Phase 4 미착수.

## 2026-09-23 — PLAN §6 "성장 가시화" 잔여: 전직 4차 잔상 이펙트

"다른 판 이어서 봐줘" → 3D 뷰(`side-view3d.js`)가 `S.fx()`(돌진 잔상·데미지 숫자 등 연출 배열)를 전혀 안 읽는 것을 보고 "3D 모드엔 이 연출들이 아예 안 뜨는 것 아닌가" 의심했는데, 조사해 보니 착오였다 — `game.js`가 매 프레임 `sideView.draw()`(2D)와 `sideView3d.draw()`(3D)를 **둘 다** 부르고(`stage3d` 캔버스 위에 `stage`(2D) 캔버스가 겹친다, `index.html` 주석), `side-view.js`의 `draw()`는 3D가 살아 있으면 하늘·바닥·사람·몹 같은 "세계"만 건너뛰고 `drawMe()`·`drawFx()`(미니맵·데미지숫자·돌진 잔상 등 오버레이 전부)는 **그대로** 그린다. 즉 fx 는 2D·3D 어느 쪽이든 이미 다 뜬다 — 지난 턴에 "3D엔 이 fx 파이프라인이 없다"고 보고한 것을 정정한다.

다만 이 조사 중에 진짜로 비어 있던 것 하나는 찾았다 — §6 "성장 가시화(G)" 절의 "4차는 잔상 이펙트 상시"는 몸 색·무기 교체(캐릭터 틴트 파이프라인을 건드려야 해 보류)와 달리, 이미 있는 `fx('ghost')`(§5-7 dash 잔상이 쓰는 것과 정확히 같은 모양)만 재사용하면 되는 손쉬운 조각이라 이었다.

**한 일**(`js/side.js`만):
- `TIER4_GHOST_INT = 0.09` 상수 신설(다른 연출 시간 상수들과 같은 자리).
- `update(dt)` 끝(fx 수명 정리 직전)에 4줄 — `global.DG.job.cur().tier >= 4` 이고 `p.vx`(이동 입력 있음)·`p.onGround`(공중이 아님)일 때만 `p.tier4GhostT`(새 타이머 필드, `p.holdT` 류와 같은 요령으로 그냥 얹었다)를 깎아 0 이하가 되면 `fx.push({t:'ghost', x:p.x+P_W/2, y:p.y+P_H, life:0.22})`. dash 잔상(1424~1432줄)이 이미 쓰는 것과 똑같은 필드 모양이라 `side-view.js`의 렌더 쪽은 **한 글자도 안 고쳤다**.
- 가만히 서 있거나 공중(점프·낙하)일 땐 안 심는다 — 정지 중 잔상이 겹쳐 보이면 "따라오는 잔상"이 아니라 몸이 두 겹으로 보이는 얼룩이 되기 때문.

**검증**: `node -c` 통과. 새 식별자(`TIER4_GHOST_INT`·`J2`·`jobCur`) 중복 없음을 grep으로 확인. `p.vx`가 매 프레임 실제 이동 방향(−1/0/1)으로 갱신된 뒤(1579~1589줄) 그 이후로는 안 건드리는 것을 코드로 대조해, 내가 읽는 시점(함수 끝)의 `p.vx`가 이번 프레임의 실제 이동 여부를 정확히 반영함을 확인. `data-job.js`에서 `tier:4`가 4차 직업 넷(전신·궁성·명왕·천존)에만 붙어 있음도 확인. 이 판 `_test.html`은 판정 로직(`side.js`)을 이미 돌리는 스위트라 회귀 대상이지만, 전직 4차 도달·이동을 굴리는 시나리오가 없어 이 조건을 실제로 타는 진단은 없다(fx 개수를 세는 기존 진단도 없음) — 새 진단은 화면 연출 취향(터울 0.09s가 적당한지)이라 실기 확인 몫으로 남겼다. precheck 통과, `sw.js` `side-v0.65.0`→`side-v0.66.0`.

**실기 확인 필요**: 4차 전직 상태로 걸을 때 잔상 터울(0.09s)이 촘촘하거나 성긴지, 3D 화면에서 2D 오버레이 잔상 사각형이 3D 모델과 자리가 맞는지(이 판은 3D 카메라가 2D 좌표계와 고정 매핑이라 원래 맞아야 하지만 처음 실기로 본다), 돌진 잔상과 겹칠 때(4차로 대시) 과하지 않은지.

**남은 것**: 몸 색·무기 GLB 교체·장비 외곽선(§6 "성장 가시화" 나머지)은 캐릭터 틴트 파이프라인을 건드려야 해 여전히 미착수. 사가국지 Phase 4.

## 2026-09-23 — 원 지시서 잔여 "채집→제작 연결": 캔 것으로 탕약·주문서 만들기

§8 Phase 1~4 가 전부 "코드분 완료·실기 확인 대기"라, 렌더 확인 없이 닫을 수 있는 원 지시서 잔여 하나를 골랐다. 그동안 `side.mats`(채집 재료)는 쌓이고 가방 "캔 것" 칸에 보이기만 했을 뿐 쓸 데가 없었다.

**한 일**:
- `data-side.js` — `RECIPES` 5개 + `recipe(key)`. 들꽃 3 → 탕약 2 · 열매 3+들꽃 1 → 탕약 4 · 이끼 광물 5 → 물리방어 100% · 광물 4+그은 돌 3 → 공격력 100% · 열매 4+그은 돌 4 → 체력 60%. 값은 저자 값의 대략 절반 어치(한 사냥터 두어 바퀴 = 하나). 장비는 안 만든다(가방 칸·등급 체계).
- `gear.js` — `craftLack(key)`(모자란 개수)·`craft(key)`(모자라면 아무것도 안 덜림, 차면 재료 덜고 `potions`/`addScroll`, 로그·토스트·`side:craft` 이벤트).
- `ui.js` — 🏪 저자 맨 아래 "🧺 캔 것으로 만들기" 칸(재료 가진/필요 표시, `sh-craft` 단추), 가방 "캔 것" 칸에 안내 한 줄.
- 세이브 스키마 무변경(`side.mats`·`potions`·`scrolls` 기존 칸만 씀). `sw.js` `side-v0.66.0`→`side-v0.67.0`.

**진단**: 새 1개(모자라면 거절·재료 안 덜림 / 차면 덜고 탕약 +2·주문서 +1 / 레시피 재료·산출 키 유효 / 저자 시트에 단추 5개). **jsdom 215/216 ×3 동일** — 남은 하나는 기존 `getBoundingClientRect` jsdom 한계(화면 넷 분할). 이번 러너는 scratchpad 에 새로 만들었다: 가짜 http 출처 + 파일 ResourceLoader(file:// 은 localStorage 가 막혀 부팅이 빈다) + 무엇이든 받는 Proxy 2D 컨텍스트(`getContext` 가 null 이면 `setTransform` 에서 부팅이 죽어 139/216 으로 보인다).

**실기 확인 필요**: 레시피 값(개수)이 체감상 적당한지, 저자 시트가 길어져 폰에서 만들기 칸까지 스크롤이 거슬리지 않는지.

## 2026-09-23 (이어서) — 원 지시서 잔여 "폐허·동굴 지역": 다섯째·여섯째 사냥터

마지막 사냥터가 호로곡(Lv.25)이라 3차(Lv.45)·4차(Lv.70) 전직 뒤로 갈 곳이 없었다(넷째를 넣던 2026-08-26 과 같은 빈틈).

**한 일**:
- `data-side.js` — `ruin` 낙양 옛터(洛陽 舊址): need 45 · enemyLv 30 · mood 'fire' · 폭 3600 · 발판 9 · 캐는 것 그은 돌·광물 · 보스 폐도 흉장. `deepcave` 검각 암굴(劍閣 巖窟): need 70 · enemyLv 34 · mood 'cave' · 폭 3800 · 발판 10 · 광물 4 · 보스 암굴 귀장. 호로곡 오른쪽 끝(x 3330)에 옛터로 가는 문, 옛터↔암굴 문.
- **enemyLv 는 한 걸음씩(26→30→34)만** — 적 체력이 18·1.22^(lv-1) 이라 문턱(45·70)만큼 올리면 수십~수천 배가 된다. 정예 체력 2,596 → 5,751 → 1.3만(×2.2·×4.9), 기존 진단의 "3차가 2차의 1.8배"와 같은 폭.
- `data-enemy.js` BOSSES 에 tier 4 둘 추가(있는 칸만 — §2-2 "새 필드 금지" 지킴, 근접 무기라 `rangedOf` 없음). 보스 열 명이 이미 사냥터·관문 대장·비경에 다 쓰여 있어 새로 세웠다. 사가블로 사본과는 원래부터 md5 가 다르다.
- 새 mood 없음 → 2D `drawBackdrop`·3D `moodLight` 는 한 줄도 안 고쳤다. `ui.js` 전체 지도 `OW_CHAIN` 에 둘 추가.
- 원 지시서 잔여의 "BGM 숲/전투"·"비전투 동물"은 이미 들어가 있었다(`bgm.js` town·forest·battle, `side-view3d.js` rebuildCritters 사슴·여우) — PLAN 행을 바로잡았다. 토끼·새는 에셋이 없어 안 넣었다.
- `sw.js` `side-v0.67.0`→`side-v0.68.0`.

**진단**: 새 1개(문턱 = 3·4차 전직 레벨, 잠금/열림, 보스 이름이 BOSSES 에 있음·tier 4, 체력 오름 폭 <6배). 기존 둘 갱신(계단 넷→여섯, 사슬을 검각 암굴까지·Lv.71). **jsdom 216/217 ×3 동일**(남은 하나는 기존 getBoundingClientRect 한계).

**실기 확인 필요**: 3·4차로 실제 잡히는 속도(너무 무르거나 단단한지), 불탄 옛터가 호로곡과 똑같아 보이지 않는지(sky 색만 잿빛으로 바꿨다 — 3D 는 mood 조명이 같다), 암굴 발판 높이(230)가 줄로 닿는지 손으로.

## 2026-09-23 (이어서 2) — 경험치 곡선 무릎 + 호로곡·옛터·암굴 사명 7

사명 보상을 정하려 경험치를 재 보다 발견: `core.expNeed = 50·1.28^(lv-1)` 을 끝까지 두어 **Lv.25→45 가 호로곡 정예 약 8.4만 마리, Lv.70 누적 44억** — 3차·4차 전직과 바로 앞 세션의 다섯째·여섯째 사냥터에 실제 플레이로는 못 닿았다(Lv.12→25 는 굴혈 약 1천 마리). 처치 경험치는 `6+lv*4` 로 1차라 지수 곡선과 벌어지기만 한다.

**한 일**:
- `core.js`(이 판 사본만 — 다섯 벌 md5 가 원래 다 다르다) `expNeed`: Lv.25 까지 그대로, 뒤로는 `EXP_KNEE=25`·`EXP_LATE=1.04`. 이제 25→45 호로곡 약 5천 마리, 45→70 옛터 약 1.4만 마리(보스·희귀·사명·자동 순행 빼고). 세이브의 `player.exp` 는 그 레벨 안의 진행이라 기존 세이브가 필요치 이상이면 다음 획득 때 올라간다 — 마이그레이션 불필요.
- `quest.js` `onKill`: `boss` 목표에도 `stage` 한정(kill 과 같은 한 줄).
- `data-quest.js`: 사명 7 — 호로곡의 불길(Lv25·120)·그은 돌을 모은다(Lv25·cinder 20)·골짜기의 주인(Lv30·보스)·옛 도읍의 잿더미(Lv45·150)·폐도의 흉장(Lv50·보스)·검각 깊이(Lv70·180)·암굴의 귀장(Lv75·보스). 경험치는 그 레벨 한두 칸 어치.
- 원 지시서 잔여 "지역별 lazy 로딩"은 이미 되어 있어(GLB 는 사냥터 들어갈 때만 `acquire`, three.js async) PLAN 행을 바로잡았다. 남은 건 스토리 연출뿐.
- `sw.js` `side-v0.68.0`→`side-v0.69.0`.

**진단**: 새 2(보스 사명 사냥터 한정·새 사명 키 유효 / 곡선 Lv.25 까지 동일·단조 증가·처치 수 상한). **jsdom 218/219 ×3 동일**(남은 하나는 기존 getBoundingClientRect 한계). `_admin.html` 은 안 돌렸다.

**실기 확인 필요**: Lv.25 이후 레벨업 체감(너무 빠르거나 여전히 느린지 — 손잡이는 `EXP_LATE` 한 곳, 어드민 `gain.expMul` 로도 본다), 사명 보상 경험치가 한두 칸으로 느껴지는지.

## 2026-09-23 (이어서 3) — 원 지시서 잔여 "스토리 연출": 첫 발 장면

원 지시서 잔여의 마지막 항목. 사냥터 여섯 + 시작 마을(신야성 서막)에 **처음** 들어설 때 한 번 뜬다.

**한 일**:
- `js/story.js`(새 파일, 로직만) — `side:enter`·`side:travel` 을 받아 `STORY` 에 있고 안 본 곳이면 연다. `next()`(마지막 줄이면 닫힘)·`skip()`·`seen()`·`reset()`. 본 기록은 `core.save.side.story = {key:1}`(없으면 빈 것 — 마이그레이션 불필요). 안 띄우는 때: `DG_NO_STORY`(진단), 자동 순행 중(다음에 손으로 들어올 때로 미룸), 비경 임시 방(`stage.rift`).
- `data-side.js` — `STORY` 7곳(2~3줄씩, 말하는 이는 'me'=앞에 세운 인물 또는 NPC 직책 키 — 실명 없음) + `EMOTES` 6(담담·결의·놀람·불안·기쁨·분노 → 표시 이모지와 흔들림 모양).
- `ui.js` `renderStoryBox()` — 초상은 기존 `pt('hero', …)`(3D 초상 교체 경로 그대로), 누르면 다음 줄, 건너뛰기 단추. `body.cine` 로 위아래 11vh 검은 띠.
- `game.js` — 장면 동안 `auto.update`·`S.update` 를 건너뜀(그림은 계속). 키보드는 Space·Enter 다음, Esc 건너뛰기, 나머지 조작 무시.
- CSS: `style.css` 끝에 띠·카드·감정 애니메이션(`prefers-reduced-motion` 이면 끔), `maple.css` 테마 선택자에 `.story-card` 추가.
- 스크립트 등록: `index.html`(빌드 `build-single.mjs` 는 여기서 목록을 읽는다)·`_test.html`·`_admin.html`·`sw.js` SHELL. `_demo.html` 은 원래 rift.js 등도 안 싣는 축약 목록이라 안 넣었다(없어도 ui·game 이 `DG.story` 유무를 본다). `sw.js` `side-v0.69.0`→`side-v0.70.0`.
- **카메라 이동은 안 넣었다** — 3D 카메라는 2D 좌표계와 고정 매핑이라 렌더 확인 없이 건드리면 위험.

**진단**: 새 1(데이터 키 유효 / 자동 중 미룸 / 열림·DOM·cine / 줄 수만큼 넘기면 닫히고 seen / 재입장 안 뜸 / 건너뛰기). 진단이 끝날 때 들어가 있던 사냥터로 되돌린다(안 그러면 뒤 진단 셋이 run 없음으로 깨진다 — 한 번 밟았다). **jsdom 219/220 ×3 동일**(남은 하나는 기존 getBoundingClientRect 한계).

**실기 확인 필요**: 띠·카드가 폰 세로·가로에서 조작 띠·안전 영역과 안 겹치는지, 초상(72px)이 3D 초상으로 제대로 바뀌는지, 대사 톤, 첫 부팅 때 신야성 서막이 계정 창과 겹치지 않는지.

## 2026-09-23 (이어서 4) — 외곽선: 헤드리스 스크린샷 보며 배우 GLB 에 넣음

사용자가 "화면 보며 그래픽 통일"을 골랐다(스크린샷 허용). PLAN §6 에서 "화면 확인 없이 붙일 위험"으로 빼 두었던 외곽선부터.

**한 일**: `toon3d.js` 에 `outline()`·`outlineMaterial()`·`OUTLINE_ON()`(사가블로 `toon3d.js` 방식 — 법선 방향으로 민 BackSide 헐, SkinnedMesh 는 같은 뼈대), `asset3d.js` `isActorAsset(url)`(people/animals/monsters 폴더) + `delam(root, url)` 끝에서 외곽선. 사람 조립(`assembleHero`)은 옷·머리 외곽선도 스킨 메시라 몸 뼈대에 같이 다시 묶인다.

**스크린샷으로 밟은 것 셋**(_demo.html #hunt·#boss·#gorge·#town, 1400×900):
1. 짐승 외곽선이 **톱니·점선** — 로우폴리 GLB 는 면마다 꼭짓점을 따로 둬 법선이 갈려, 밀면 면끼리 벌어진다. → 같은 자리 꼭짓점 법선 평균을 `outlineNormal` 속성으로 따로 싣고 셰이더가 그걸 민다(본 몸 음영은 그대로).
2. 고치자 짐승이 **새까만 덩어리** — CDP 로 실측: 늑대·사슴·여우 GLB 기하 반지름 0.03, 뼈대 월드 스케일 ×926~2147. 사가블로식 `Math.max(0.006, r*0.03)` 의 최소 0.006 이 반지름의 20%였다. → 최소폭 없앰, 한 모델은 **가장 큰 부품 반지름 × 1.5%** 한 폭(사가블로 3% 의 절반 — 이 판 배우는 화면 키 40px 안팎이라 3% 면 사람이 검게 묻혔다).
3. 눈 같은 작은 부품(반지름이 최대의 10% 안팎)에 검은 점 → 12% 미만 부품은 안 두름.

**확인 방법(다음에 또 쓸 것)**: scratchpad 에 정적 서버(`/__go?key=&val=&to=` 로 localStorage 손잡이를 잡고 넘어가는 경로 포함)와 CDP 도구(크롬을 직접 띄워 그 PID 만 끈다 — 사용자 크롬 무관). **함정 둘**: ① 헤드리스는 `document.hasFocus()` 가 false 라 `game.js` 루프가 그리기를 멈춘다 → `addScriptToEvaluateOnNewDocument` 로 참으로. ② three 의 `WebGLRenderer.render` 는 인스턴스 속성이라 프로토타입을 감싸도 안 잡힌다 → `Object3D.prototype.updateMatrixWorld` 를 감싸 `isScene` 을 붙잡고 `DG.sideView3d.draw()` 한 번.

**진단**: jsdom 219/220 ×3(남은 하나는 기존 한계, three 없는 진단이라 외곽선은 안 탄다). `sw.js` 판올림.

**실기 확인 필요**: 폰(더 작은 화면)에서 1.5% 가 너무 가늘거나 굵은지, VRoid(anime) 아바타 얼굴 모프가 외곽선과 어긋나지 않는지(외곽선 셰이더는 모프를 안 탄다 — 표정이 바뀌면 입가가 살짝 어긋날 수 있다), 프레임 영향(배우 드로콜 약 2배).

## 2026-09-23 (이어서 5) — 성장 가시화: 장비 세트 등급으로 주인공 외곽선 색

"사가웹 이어해"로 받아 PLAN §6 성장 가시화 잔여 중 "장비 4단마다 외곽선 색"을 골랐다(바로 앞 절에서 외곽선이 들어와 자리가 생김). 스크린샷은 이번엔 안 찍었다(요청 없음).

**한 일**: `gear.js` `setGrade()` — 열 부위 중 **절반(`SET_HALF`=5 칸) 이상이 닿은 가장 높은 등급**(빈 칸·요구 Lv 미달은 0, `bonus()` 와 같은 기준). 무기 하나만 전설이어도 안 바뀌고 한 단을 절반쯤 갈아입어야 넘어간다. `toon3d.js` `outlineMaterial(width, skinned, color)`(색도 캐시 키), `gradeTint(hex, rank)`(RARITY 색을 검정 0x14120f 쪽으로 섞음 — 손잡이 `world3d.gearOutline` 기본 0.75, 0 이면 늘 검정), `recolorOutlines(root, color)`(`_outline` 메시 재질을 같은 폭·같은 스키닝의 색 재질로 갈아 끼움). 외곽선 재질은 배우끼리 나눠 쓰니 uniform 을 바꾸면 적까지 물든다 — 그래서 색마다 캐시. `side-view3d.js` `applyGearOutline()` 가 주인공(GLB 도착 뒤)만, 색이 바뀐 프레임에만 갈아 끼운다(교대로 몸이 바뀌면 `olColor` 초기화).

**섞은 색**: 상급 #4c9653 · 희귀 #3d7fb8 · 영웅 #8955b8 · 전설 #b9802f. 등급 이름·색·경계는 값·세이브 무관.

**진단**: 새 1(빈·넷 → 기본, 다섯 → 상급, 무기만 전설 → 여전히 상급, 영웅 다섯 → 영웅, Lv 미달 안 셈, 등급 색 넷이 서로·검정과 다름). **jsdom 220/221 ×3 동일**(남은 하나는 기존 getBoundingClientRect 한계). `sw.js` side-v0.73.0.

**실기 확인 필요**: 1.5% 폭에서 색이 실제로 읽히는지(너무 가늘면 `world3d.gearOutline` 을 1 로 올리거나 등급 있을 때만 폭을 키우는 안), 밝은 하늘(sky mood) 앞에서 상급 초록이 잔디와 묻히지 않는지. 전직 몸 색·무기 교체는 여전히 미착수.

## 2026-09-23 (이어서 6) — 성장 가시화: 전직 차수마다 주인공 옷 빛깔

같은 "이어해" 안에서 PLAN §6 "전직 1~4차마다 몸 색·무기 교체" 로 넘어갔다.

**한 일**: `data-job.js` `BRANCH_TINT`(무사 #b8412f·궁수 #4f8f3f·협객 #5b4a8c·방사 #2f6fb0). `toon3d.js` `mixHex()`(외곽선 등급 색도 이걸로 정리)·`jobLook(baseCss, jobKey)` — 세력 색에 갈래 색을 차수 × `world3d.jobTint`(기본 0.12, 상한 0.6)만큼, 무명은 세력 색 그대로, 세력 색이 없고 무명이면 예전처럼 undefined(안 물들임). `side-view3d.js` draw() 의 주인공 key 를 `id|jobLook.key` 로 — 전직하면 동료 교대와 같은 길로 몸을 다시 세운다(asset3d applyTint 는 조립 때 한 번 굽는 캐시라 제자리 교체가 안 된다). 순수 계산을 toon3d 에 둔 까닭: `side-view3d.js` 는 `_test.html` 에 안 실린다(실으면 game.js 가 WebGL 초기화를 부른다).

**무기 GLB 교체는 막힘** — 저장소 `assets/models/` 에 무기 팩이 없다. 받아 올 수 있게 되면 그때.

**진단**: 새 1(무명=세력 색, 1차=0.12·4차=0.48 섞음, 갈래끼리 다름, 전직마다 key 다름, 세력 색 없는 무명은 undefined, BRANCH_TINT 키 = 1차 직업 넷). **jsdom 221/222 ×3 동일**(남은 하나는 기존 getBoundingClientRect 한계). `sw.js` side-v0.74.0.

**실기 확인 필요**: 세력 색이 갈래 색과 비슷하면(붉은 세력 + 무사) 4차에도 거의 안 바뀐다(#aa3333 → #b13a31) — 체감이 약하면 명도·채도를 차수마다 올리는 축을 더하거나 `world3d.jobTint` 를 올린다. 전직 순간 몸이 한 번 캡슐로 돌아갔다 GLB 로 돌아온다(캐시라 짧다) — 거슬리면 새 몸이 올 때까지 옛 몸을 두는 방식으로.

## 2026-09-23 (이어서 7) — 전직 순간 캡슐 깜빡임 없앰

바로 앞 절에 남긴 흠. `side-view3d.js` draw(): 주인공 key 가 바뀌었는데 **같은 인물이고 지금 몸이 GLB** 면(= 전직으로 빛깔만 바뀜) 새 몸(`pendingMesh`)을 화면 밖에서 조립하고, GLB 가 도착하면(못 받으면 4초 뒤) 그때 옛 몸과 바꿔 낀다. 동료 교대(다른 인물)는 예전처럼 곧바로 치우고 새로 세운다. 진단은 three 없는 jsdom 이라 이 길을 못 탄다 — `node -c` 만. `sw.js` side-v0.75.0. 실기 확인 대기.

## 2026-09-23 — "원신급": VRoid 셀 셰이딩 + 원신식 얼굴 그림자 + 배우 림 + 하늘 그라디언트

VRoid 주인공·동료가 unlit(`MeshBasicMaterial`) 그대로라 명암 없이 평면이었다. `asset3d.js` GLB 받는 자리에서 다섯 벌 공용 `vroidVariant.shade()` 를 먼저(툰 + 얼굴 그림자 + 림), 배우 GLB 툰 재질에도 `toon3d.applyRimLight`(밝기 비례). `ownAllMat`(맞으면 번쩍이는 사본)을 `toon3d.cloneMat` 으로 — 기본 `clone()` 이 셰이더를 떨궈 **첫 피격부터** 림·얼굴 그림자가 사라졌다. `toon3d.skyBackground()`: 사냥터 단색 하늘 → 세로 그라디언트(아래 35% 는 안개색 그대로, 손잡이 `world3d.skyGrad`), `side-view3d.js` rebuildStage 한 자리. 상세·원리는 `saga-forest/HANDOFF.md` 같은 날 "원신급 다시" 절, 공통 함정은 `SAGA-HANDOFF.md` 함정 표(clone 이 셰이더를 떨굼·스킨 메시 배율 외곽선).
진단 jsdom 221/225 ×2 동일(새 3: shade·림·하늘), 실패 4 는 HEAD 와 같은 목록. `sw.js` side-v0.76.0. **실기 확인**: 장비 등급 외곽선 색(`recolorOutlines`)과 림이 같이 있을 때 과하지 않은지, 굴·불 골짜기 하늘 위쪽이 너무 검지 않은지.

## 2026-09-23 (이어서) — 스크린샷으로 다듬기: VRoid 전용 외곽선·피부 색조·초상 Neutral 0.8·인물 초상 다시 굽기

헤드리스 스크린샷으로 확인하며 고쳤다. 판 외곽선이 VRoid 의 알파 컷 머리·옷과 얼굴 부품에서 검은 덩어리를 만들어 `vroid-variant.js`(다섯 벌) 전용 외곽선(알파 존중·색 외곽선·피부·옷·머리만)으로 바꾸고 VRoid 메시는 판 외곽선이 건너뛴다. 얼굴 그늘 살구빛·그림자 맵 안 받음, 피부 색조. `portrait3d.js` 톤매핑 ACES 1.3 → Neutral 0.8, 인물 초상 전부 다시 구움(`tools/bake-portraits --kind=hero`). `applyTint` 는 외곽선 재질을 건너뛰고 `cloneMat` 으로(사가스토리·사가국지는 VRoid 면 옷만 물들임 — 예전엔 몸 전체가 세력 색 단색이었다). 사가국지는 VRoid 레시피의 `anim: 몸 파일` 을 빼 초상·일기토 T자세를 고쳤고(자체 몸짓 길), 초상 `settle` 이 `actions` 를 먼저 보며, VRoid 엔 투구를 안 씌운다. 상세·스크린샷 경위는 `saga-forest/HANDOFF.md` 같은 날 "(이어서) 스크린샷 찍으면서 다듬어줘" 절.

## 2026-09-23 (이어서 2) — 머리카락 하이라이트(천사 고리)·흉상 초상, 법선 없는 GLB 예방

"원신급" 남은 셋 중 다섯 판 공통 둘. `vroid-variant.js`(다섯 벌, md5 동일) `hairShadeMaterial` — VRoid `_HAIR` 재질에 화면 기준 법선의 위쪽 성분이 일정 범위(0.34~0.48)인 띠 × 카메라를 마주 보는 만큼 밝기 비례 광택(텍스처 밝기로 가장자리를 끊는다), 손잡이 `world3d.hairHi`(0 끔). `portrait3d.camPlan` 사람 초상을 흉상으로(span 0.62→0.37, look 0.78→0.86, 손잡이 `portrait3d.bust` 0 이면 예전) — 인물 초상 전부 다시 구움(210장, 실패 0). 스크린샷(스크래치 확인 페이지·초상 모음)으로 맞췄다. 진단: 머리 하이라이트 1 추가, 222/226, 실패 목록은 직전과 같다(전부 기존 jsdom 한계). `sw.js` side-v0.78.0. `delam` 에 법선 없는 GLB 면 `computeVertexNormals()` 한 줄(예방 — 사가국지 킷배싱 탑이 이것으로 새까맸다, `MeshToonMaterial` 은 `flatShading` 을 안 받는다). 상세·경위는 `saga-realm/HANDOFF.md` 같은 날 "(이어서 2)" 절.

## 2026-09-23 (이어서 3) — 펫 초상 구도: 사람 흉상 구도로 굽던 것

커밋 `c301bd6e`(메시지 "11") 로 먼저 올라갔고 세션 기록이 끊겨, 이 절은 다음 세션이 커밋 차이로 되짚어 적었다.

- **증상**: 소 초상 여섯 장(`pt_bull`·`pt_cow`·`pt_cow_farm` 의 `_c`·`_s`)이 몸통 한가운데만 크게 찍혔다(스크린샷). 이 판만 `portrait3d.camPlan` 에 펫 구도가 없어 사람 구도(흉상 span 0.37)로 구웠다.
- **고침**: `camPlan(w, h, kind)` — `kind === 'pet'` 면 span 1.7(1.5 는 여우 머리가 잘림)·look 0.28·yaw 0.95, 다른 세 판(사가고·사가블로·사가의숲)과 같은 값에 폭만 조금 넓게. `bake` 가 `kind` 를 넘긴다.
- **다시 구움**: 소·여우·구미호·사슴·수사슴 초상, 새로 `pt_jindo`·`pt_sapsal`(`_c`·`_s`). `sw.js` side-v0.79.0. 진단 항목 추가 없음.

## 2026-09-23 (이어서 4) — 외곽선 스키닝 순서(예방)

외곽선 셰이더를 **스키닝 전**(`begin_vertex` 직후, `outlineNormal`)에 밀도록 옮김 — 폭은 지오메트리 반지름으로 재는데 스키닝 뒤(메시 지역 공간)에 밀어, 역바인드 행렬이 배율을 품은 양자화 GLB(Koi)에서 외곽선이 몸의 몇 배가 됐다(사가블로 잉어 초상이 검은 덩어리). 사가고·사가블로·사가스토리·사가의숲 `toon3d.js` 같은 수정, 사가국지는 뷰 공간 방식이라 무관. 이 판 펫(소·여우 등)은 우연히 공간 배율이 맞아 겉으론 멀쩡했다. 초상 다시 굽지 않음. 진단 225/226(실패 하나는 기존 getBoundingClientRect jsdom 한계). `sw.js` side-v0.80.0. 경위 `saga-go/HANDOFF.md` 같은 날 "(이어서 4)".

## 2026-09-23 (이어서 5) — 펫 초상 9종 → 79종(실제 동물 전부), 상자 맞춤 구도

SAGA-DESIGN §11 Phase 4 의 "사가스토리 펫 이미지 공백(보류)" — 예전엔 `PET_MAP` 9종(사슴·여우·소·개과)만 3D 초상이 돼 나머지는 코드 그림 초상이었고, 그래서 `human()`/`beast()` 를 펫 초상용으로 남겨 뒀다.

- 사가블로 모델 복사: `models/animals/` 13파일(범·곰·판다·원숭이·멧돼지·부엉이·고양이·두루미·잉어·개구리·까마귀 gltf 셋), `animals_extra/` 22, `animals_extra2/` 42(약 9.5MB). `asset3d.js` `critter:*` 73줄 + `ANIMALS_EXTRA`·`ANIMALS_EXTRA2`. 옆면 뷰가 부르는 다섯 키(사슴·여우·소·시바·허스키)는 그대로.
- `portrait3d.js` `PET_MAP` 79종 — 큰사슴·황소·젖소는 제 모델(Stag·Bull·Cow_Farm)로 옮김, **발바리도 Pug 로**(2026-09-14 "소형견 CC0 없음" 판단이 사가블로의 Pug 로 풀림). 구미호는 여우 그대로. 신수 10·포켓몬 오마주 16은 여전히 없다.
- 사가고·사가블로의 `petFrame`·`petYaw`·`bodyBox`·`PET_FIT`(손잡이 `portrait3d.petFit`)와 camera far = max(40, dist × 3) 이식, `sprite.js` BEAST_FORM 에 물고기·말·공룡 줄(사가고와 같은 줄, 물고기 옆모습용).
- 펫 초상 158장 구움(79종, 못 구움 26종 = 신수·오마주). 모음 그림으로 빈 카드·파편 없음 확인. 진단 1 추가 → 226/227(실패 하나는 기존 jsdom 한계). `sw.js` side-v0.81.0.
- **남은 것**: 신수·오마주 26종을 어떻게 할지(사가고처럼 형태별 대신 모델을 입힐지) — 사용자 결정. 그게 정해져야 `human()`/`beast()` 펫 경로를 지울 수 있다(Phase 4 마무리).

## 2026-09-24 — 신수·오마주 대역으로 펫 초상 105종 전부, 코드 그림 함수 삭제(Phase 4 마무리)

사용자 "응 진행해줘"(Phase 4 범위 짚기) → 신수·오마주를 어떻게 할지 물어 **"사가고처럼 대역 입히기"** 로 정함.

- 대역: 사가고가 형태별 풀에서 id 해시로 고르는 것과 **같은 모델**(해태=당나귀·청룡=스테고사우루스·도깨비=오크 등, 세 판이 같은 대역). 이 판에 이미 있는 같은 종 모델을 쓰고, 없던 여섯(Birb·Pigeon·Snake 둘·Orc·Demon, 3.2MB)만 사가고에서 `assets/models/standin/` 으로 복사(`ASSET_LICENSES.md` 새 절).
- `isActorAsset` 이 `animals_extra`·`animals_extra2`·`standin` 도 배우로 — 전엔 정규식이 `animals/` 만 맞아 사가블로 "펫 100개" 모델들이 외곽선·림 없이 구워졌다.
- **SAGA-DESIGN §11 Phase 4 마무리**: 펫 초상용으로 남겨 두던 `human`·`limb`·`hand`·`foot`·`headgear`·`weapon`·`beast`·`leg2`·`eye`·`beastPatternOf`·`BEAST_PATTERN`(약 1100줄) 삭제, 두 펫 갈래는 `loadingMark` 자리표시(사가의숲과 같은 결). 호출부 0 은 `sprite.js` 밖(진단·데모·어드민 포함) grep 으로 확인.
- 구미호는 원래대로 여우. `critter:*` 26줄·`PET_MAP` +26. 펫 초상 210장 다시 구움(210/210). (이어서 5)의 진단을 "105종 전부·대역·옆면 뷰 키 그대로·함수 없음" 으로 고침 → 226/227(실패 하나는 기존 jsdom 한계). `sw.js` side-v0.82.0.
