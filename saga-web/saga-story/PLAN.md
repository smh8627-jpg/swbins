# 사가스토리(saga-story) PLAN — 정본

상위 문서 `../../SAGA-DESIGN.md`(재미 표준 8·참고 게임 카탈로그·그래픽·에셋·문서 규칙). 이 파일은 그중 **이 판에 해당하는 것**만 든다.
공개 주소 <https://smh8627-jpg.github.io/swbins/saga-web/saga-story/> · 포트 8794 · 세이브 키 `yeoksa-side/save/<프로필>`.

## 0. 읽는 법

- 목차만 `grep -n "^## \|^### "` 로 뽑고 필요한 절만 `sed -n` 으로 읽는다. 이 파일은 70KB 를 넘기지 않는다.
- **여기에 날짜 달린 세션 기록을 쓰지 않는다.** 세션 이력은 `HANDOFF.md`(append-only), 지금 상태는 `README.md`(현재 절만 덮어쓴다). 결정이 바뀌었을 때만 이 파일을 고친다.
- 작업 원칙(토큰): 구조 파악 → 필요한 파일만 → 최소 변경 → 기존 시스템 재사용 → 새 시스템은 재사용 불가를 확인한 뒤에. 판단 순서 **토큰 절약 > 기존 코드 재사용 > 성능 > 기능 추가 > 세부 polish**. 세부는 `SAGA-DESIGN.md` §9.
- 코드 확인은 `node -c` 와 `_test.html` 진단(§9)만. 화면을 찍어 보지 않는다(루트 CLAUDE.md).

## 1. 정체성

- **한 줄**: 역사 인물을 앞에 세워 싸우는 **모바일 2.5D 횡스크롤 액션 RPG**. 메이플스토리 문법(발판·줄·문·전직·주문서·사명)을 따르되 복제하지 않는다.
- **원작 오마주**: 메이플스토리(구조·성장), 디아블로 계열의 어두운 판타지 색감(분위기만).
- **이 판만의 것**: **내 몸 = 선두 인물**(`side.power()` 가 `hero.stats` 에서 체력·공격을 뽑고 장비를 얹는다). 도감·승급·편성이 곧 성장이다. 다른 네 판과 인물 105·펫 64 도감(`data.js`)을 같은 값으로 나눠 든다.
- **인물이 되는 것(SAGA-DESIGN §5)**: **직업 스승**. 전직은 레벨 문턱만이 아니라 "그 갈래의 인물에게 배우는" 사건이 된다(§5-1). 스승 인물은 초상·대사·서명 무예로 그 직업의 얼굴이 된다.
- **플레이어 판타지**: "5분 사냥해도 손에 남는 게 있다" — 발판을 타고 오르며 베고, 갈래를 골라 내 무예 띠를 짜고, 주간 관문 대장을 잡아 고유 장비를 얻는다.
- **퓨전 방향**(`SAGA-HANDOFF.md` 2026-09-10 "창작 방향"): 원작 문법을 재미를 위해 깰 수 있다. 이 판은 그 순서(사가블로 선행)의 **예외로 전직 되돌리기를 먼저 넣었다**(§2).

## 2. 반드시 지킬 것

루트 `CLAUDE.md`(세이브 키·id·이름 정책·다섯 벌 복사·원작 에셋 금지·헤드리스 금지)는 링크만 한다. 아래는 이 판 고유.

1. **갈래 4·효과 9 고정**(사용자 명시). 갈래(무사·궁수·협객·방사)를 다섯으로 늘리지 않는다 — 인물 능력치·무기가 따라가야 한다. 효과는 `melee·aoe·bolt·arrow·volley·dash·rain·buff·heal` 아홉, **어떻게 하는지는 `side.js` `castSkill()` 한 곳**. 데이터(`data-job.js`)는 무엇을 할지만 적는다. 무예를 더 늘려도 효과는 안 늘린다(진단 '무예 아흔여섯은 side.js 가 아는 효과 아홉만 쓴다').
2. **`data-enemy.js` 는 사가블로와 나눠 든 파일** — 새 칸(필드)을 만들지 않는다. 이 판 고유 규칙(원거리 사거리 `rangedOf()`, 유형 `enemyRole()`)은 `data-side.js`·`side.js` 에 둔다.
3. **무예 점수(SP)는 파생값** — 세이브에 담지 않는다. (레벨-1)×3 에서 찍은 합을 뺀다. 총량을 바꾸면(§5-2) 그 식 한 줄만 바꾼다.
4. **조작 띠 8칸, 찍은 무예만, 윗자리부터.** 띠를 늘리지 않는다(4차 넷+3차 넷이 정확히 여덟).
5. **전직 되돌리기(`resetJob()`)는 허용** — 원작은 불가지만 퓨전 방향의 예외로 사용자가 이 판만 먼저 확인했다. 비용·쿨은 실기 체감 뒤 결정(§10).
6. **상점은 위치에 매지 않는다** — 🏪 도구줄로 어디서든 연다. 마을 장사치 NPC 는 같은 상점의 지름길일 뿐.
7. **세로·가로 둘 다** 지원. 한쪽만 되는 상태로 두지 않는다. 폰엔 방향키가 없다 — 화면 넷 분할 터치.
8. **코드로 그리지 말고 에셋으로**(2026-08-28 다섯 판 공통 지시): 코드가 도형·그림을 조립하던 것은 실제 에셋으로. 남는 스크립트는 판정·규칙·상태만. 에셋을 못 받는 자리(`file://` 단독판·저품질·오프라인)는 조용히 옛 그림으로 되돌아가는 **손잡이를 남긴다**. 출처는 `assets/ASSET_LICENSES.md`, **여기 없는 파일은 두지 않는다**. 재배포 불가는 애초에 받지 않는다. 결이 안 맞아서(한옥·갓·도포 ≠ 서양 로우폴리)는 안 하는 이유가 못 된다 — 품질 우선.
9. **절대 하지 말 것**: 전체 재작성 · 거대 단일 파일 · 불필요한 라이브러리 · 모든 오브젝트 고해상 그림자 · 모든 에셋 최초 로딩 · 단순 색 변경을 그래픽 개선이라 부르기 · 같은 나무만 반복 배치 · UI 로 화면 가득 채우기.
10. **성능 상한(폰)**: 픽셀 비율 ≤1.5, 품질 3단(`post3d.js` `TIER_POST` HIGH/MEDIUM/LOW) 자동, 파티클·인스턴스 상한을 설정 시트 손잡이로. 실시간 광원은 마을 횃불 정도만.

## 3. 현재 시스템 지도

| 시스템 | 주 파일 | 상태 | 진단 |
|---|---|---|---|
| 물리·조작(중력 1900·점프 760·달리기 270, 발판 위에서만, 줄 ↑↓, 문 ↑, ↓+점프 하강, 앉아 쉬기) | `side.js`, `data-side.js`(ropes·portals) | 완료 | 중력·점프·발판·줄·문 항목 |
| 사냥터 사슬 9(마을 5 `town:true` + 사냥터 4, need 1/5/12/25), 전체 지도(M·🗺️) | `data-side.js` STAGES, `ui.js` `openOverworldMap` | 완료 | '사슬이 끊기지 않는다' |
| 전투(급소 15%×1.6, 넉백 62, 흔들림, 숫자 팝 0.6s, 무적) | `side.js` | 완료·손맛 1차 | 공격·급소·밀림 |
| 적 42(잡졸 32·보스 10, tier 1~4), 유형 5(근접·원거리·돌진·탱커·마법형 12%), 원거리=든 무기 | `data-enemy.js`(공용), `side.js` `enemyRole()` | 완료, 실기 미확인(마법형) | 유형·화살 항목 |
| 보스(오른쪽 끝, 12~17배, 쿨 15~30분) — **10종 중 4종만 사냥터에 물림** | `data-side.js` `boss`, `side.js` | 완료 | 보스 항목 |
| 랜덤 이벤트 7(보물상자·희귀·채집 보너스·습격·미니보스·이동 상인·NPC 구조) | `side.js` | 완료, 실기 미확인(뒤 넷) | 일부 |
| 직업 17(무명+4갈래×4차), 무예 96(갈래당 6자리 사슬), 효과 9, `resetJob()` | `data-job.js`, `job.js`, `side.js` | 완료, 실기 미확인(5·6째 자리 32개) | 갈래·need·효과 항목 |
| 장비 부위 10(7+장신구 3)×4단, 주문서 100/60/10, 고유 10, 가방 24, 저자 3할 | `data-gear.js`, `data-unique.js`, `gear.js` | 완료 | 장비·주문서·저자 |
| 사명 20(일일 2 포함), 업적 9 | `data-quest.js`, `quest.js`, `data-achieve.js`, `achieve.js` | 완료 | 사명·일일·업적 |
| 자동 사냥(줄 타기·유형별 무예·탕약 35%·이탈 18%, 5분 108마리) | `auto.js` | 완료 | '5분치를 굴리면' |
| 3D 배경 렌더러(🧊), 지형지물·발밑 밀도·마을 정취(집4·우물·울타리), 바닥 도트 타일 | `side-view3d.js`, `asset3d.js` | 완료, 실기 미확인 | 화면 예외 없음 |
| 사람 GLB(Quaternius QRPG 6종 색 물들임), 짐승 2크기, 몸짓 표(attack·dodge·interaction·run·death) | `asset3d.js`, `side-view3d.js` | 완료, 실기 미확인(몸짓) | `run.dying` 항목 |
| 후처리 3단(HIGH/MEDIUM/LOW), SSAO | `post3d.js`, `ssao3d.js` | 완료 | — |
| 인물 초상 3D(손잡이 `portrait3d.on`), 펫 초상 3D 는 사슴·구미호만 | `portrait3d.js` | 부분(펫 62종 2D) | — |
| UI(사가고 tools-drawer 패턴 통일, 세로/가로, ⛶, 설정 시트: 효과음·진동·음량) | `ui.js`, `css/` | 완료 | 시트·설정 항목 |
| 소리(효과음 4묶음·BGM town/field), 절차 합성 | `sfx.js`, `bgm.js` | 완료 | 소리 항목 |
| 계정·프로필·옛 세이브 이어받기 | `account.js`(복사본) | 완료 | 가입 항목 |
| 어드민(세이브·균형 손잡이 `tuned()` 8개·QA 프리셋) | `admin.js`, `_admin.html` | 완료 | ADMIN 10/10 |

## 4. 재미 진단(SAGA-DESIGN §3 표준 8)

| 표준 | 상태 | 근거 |
|---|---|---|
| A 목표판 | × | 사명 20 개가 사냥터 시트 안에 있다. HUD 에 "지금 할 일" 이 없다. 부팅 직후 화면에 목표 0줄 |
| B 세션 마무리 카드 | × | `die()` 는 로그 한 줄 + 금 절반. 사냥터를 나가도 카드 없음. "다음에 할 것" 을 알려 주는 자리 없음 |
| C 손맛 | △ | 있음: 급소 15%×1.6·넉백 62·흔들림 0.22s(급소/100+ 때만)·숫자 팝 0.6s·hit/crit 소리. 없음: **hitstop**, 피격 플래시, 타격음 라운드로빈, 보통 타격의 흔들림. 입력 지연은 미측정 |
| D 선택 3택 | × | 무예 96 = 4갈래 × 6사슬 × 4차. 여섯 사슬이 서로 독립이라 **동시에 다 찍을 수 있다**: Lv.70 SP = 207, 갈래 한 벌 만렙 = 24개×10 = 240 → 86% 를 찍는다. 트레이드오프는 띠 8칸 배치만. 갈래 간 차이는 효과 9 중 6~7종을 각각 가져 **이름만 다르다** |
| E 발견 밀도 | △ | 랜덤 이벤트 7 있음. 그러나 사냥터 4 곳(마을 5)만, 숨은 길·비밀 방·수집품 0. 한 사냥터 안에서 30초 걸으면 적 말고 만나는 것이 없다 |
| F 실패와 회복 | △ | 비용 = 이번 판 금의 절반(레벨·장비·위치 무손실). 회복 = 즉시 재진입. 비용이 낮아 긴장이 없고, "죽어도 남는 것" 을 말해 주지 않는다 |
| G 성장 가시화 | △ | 레벨업 연출·보스 확대 있음. 전직·장비·승급이 **외형에 안 나온다**(GLB 6종 색만). 4차 전직해도 같은 사람이다 |
| H 돌아올 이유 | △ | 일일 사명 2(토벌·채집), 보스 쿨 15~30분. 주간·계절 0. 일일도 사냥터 시트를 열어야 보인다 |

**가장 큰 구멍 3**: ① **D 선택** — 96개를 거의 다 찍는 구조라 고르는 재미가 없다. ② **A·B 목표·마무리** — 5분 놀고 나면 무엇을 했고 다음에 뭘 할지 남지 않는다. ③ **C 손맛** — hitstop·플래시가 없어 96개 무예가 다 같은 감촉이다.

## 5. 게임성 확장 — 후보 8(우선순위 순)

각 후보는 9필드(참고·표준·왜·메커니즘·수치·UI·세이브·진단·범위) 를 든다. **무예 수는 늘리지 않는다**(후보 2 가 96 을 재해석한다).

### 5-1. 직업 정체성 — 갈래별 고유 조작 1개 + 스승 인물
- **참고**: 메이플(직업별 고유 조작), 몬헌 나우(무기별 리듬). **표준** D·G·C.
- **왜**: 갈래 넷이 효과 9 를 나눠 가져 이름만 다르다(§4-D). 효과를 늘리지 않고 **조작 하나**로 갈래를 갈라 놓는다. 인물이 "직업 스승" 이 되면 도감이 성장 서사에 들어온다(§1).
- **메커니즘**: 갈래마다 **회피 버튼을 길게 누르면** 나오는 고유 조작 1개(효과 9 밖의 "조작" 이므로 §2-1 을 안 깬다 — `castSkill()` 이 아니라 `side.js` 입력 층의 상태 하나).
  - 무사 **받아치기**: 0.25s 창 안에 맞으면 무효 + 다음 무예 위력 ×1.5(급소 확정).
  - 궁수 **당기기**: 누른 시간 0.4~1.2s 에 따라 `arrow`/`volley` 관통 +1·위력 ×1.0~2.2, 이동 속도 40%.
  - 협객 **그림자 걷기**: 0.5s 무적 이동(dash 와 다름: 적 통과 + 뒤에서 첫 타 ×1.8).
  - 방사 **원소 전환**: 다음 3발의 `bolt·volley·rain` 에 속성(불=지속 5%/s 3초, 얼음=둔화 40% 2초, 번개=사슬 1). 속성은 **색·소리·상태 표시**만, 적 데이터에 칸을 만들지 않는다(§2-2).
  - **스승 인물**: 1~4차 전직 때 그 갈래 인물(무사 4·궁수 4·협객 4·방사 4 = 16명, `data.js` HEROES 에서 `faction`·stats 로 고른다, 가명 그대로)의 초상·대사 1줄이 뜨고, 스승이 **도감에 있으면**(등용됨) 고유 조작의 수치가 한 단 오른다(예: 받아치기 창 0.25→0.32s). 없어도 전직은 된다.
- **수치**: 위 각 항목. 고유 조작 쿨 6s(공통), 기력 12.
- **UI**: 회피 버튼 길게 누름(0.18s) → 링 게이지. 무예 탭에 "스승" 카드 1장(초상·이름·보너스). 조작 안내 1줄.
- **세이브**: `player.mentorSeen{jobKey:true}` 만 추가(파생 가능하면 넣지 않는다). 마이그레이션 불필요(없으면 false).
- **진단**: 갈래별 고유 조작이 서로 다른 상태를 만든다 · 받아치기 창 안/밖 판정 · 스승이 도감에 있을 때만 보너스 · 효과 9 만 쓰는 기존 항목 그대로 통과.
- **범위**: `side.js`(입력·상태 4개), `data-job.js`(갈래에 `signature` 키·`mentors` 16), `ui.js`(카드·게이지), `asset3d.js` 안 건드림. 불변규칙 충돌: 없음(효과 9 밖의 조작, 갈래 4 유지).

### 5-2. 무예 96 재해석 — 여섯 사슬을 "유파(流派)" 로, 띠 조합 세트 효과, SP 총량 축소
- **참고**: PoE(스킬+서포트 조합), 하데스(빌드 다양성). **표준** D.
- **왜**: 96 개를 86% 찍는 지금 구조엔 선택이 없다. 사슬 여섯을 **유파 이름**으로 묶고, **띠 8칸에 놓인 무예의 유파 조합**이 효과를 내게 하면 "무엇을 찍나" 가 아니라 "무엇을 띠에 놓나" 가 빌드가 된다. 무예 수·효과 수는 그대로다.
- **메커니즘**: 갈래마다 사슬 6 = 유파 6(예: 무사 — 정(正)·패(覇)·파(破)·수(守)·생(生)·질(疾)). 띠 8칸에서 **같은 유파 2개 → 소효과, 4개 → 대효과**(유파별 1쌍, 총 24쌍, 전부 기존 수치 배율·쿨·기력·지속 조정으로만 정의 — 새 효과 없음). 예: 질(dash 계열) 2 = 회피 쿨 -20%, 4 = dash 무예가 급소 확정. SP 를 **레벨당 3→2** 로 낮춰 Lv.70 에 138점 = 갈래 만렙 240 의 58% → 유파 2~3개만 만렙 가능. `resetJob()` 이 있으니(§2-5) 갈아탈 수 있다.
- **수치**: 소효과 ±15~20%, 대효과 ±35% 또는 판정 1개(급소 확정·관통 +1·무적 +0.1s). 배율 표는 `data-job.js` `SCHOOLS` 24행.
- **UI**: 띠 위에 유파 아이콘 6 + 현재 세트 수(2/4). 무예 탭에 유파별 열 정렬. 세트 발동 시 띠 테두리 색.
- **세이브**: 변경 없음(찍은 무예 `skills`·띠 배치 그대로). SP 총량 식 한 줄 변경 → **옛 세이브가 총점을 넘게 찍었을 수 있다**: 로드 시 초과분은 그대로 두고 새로 못 찍게만 한다(환급·삭제 안 함, 진단 항목으로 고정).
- **진단**: 유파 세트 2/4 판정 · 세트 효과가 배율만 바꾸고 효과 종류를 바꾸지 않는다 · SP 초과 세이브 로드 무손실 · 96/9 기존 항목 통과.
- **범위**: `data-job.js`(사슬에 `school` 키 96행 + `SCHOOLS`), `job.js`(SP 식·세트 계산), `side.js`(배율 적용 1곳), `ui.js`. 충돌: §2-1·2-3·2-4 모두 준수.

### 5-3. 비경(祕境) — 경로 선택 미니던전 + 진입 축복 3택 + 기억 조각
- **참고**: 슬레이 더 스파이어(노드 지도), 하데스(축복 3택·죽음이 진행), 원신 나선비경. **표준** D·E·F·H.
- **왜**: 사냥터 4 곳은 선형이고 실패 비용이 없다. 짧고(8~12분) 매번 다른 경로와 빌드가 나오는 반복 콘텐츠가 이 판에 없다.
- **메커니즘**: 마을 문 하나가 비경 입구. 진입 시 **5층 노드 지도**(층마다 2~3 노드 중 1 선택: 전투·정예·보물·휴식·이벤트, 5층은 보스). 진입 직후와 정예 처치 후 **축복 3택**(서로 다른 축: 공격/방어/유틸, §3-D 규칙): 축복 = **도감 인물의 서명 효과를 이번 비경 동안 빌려 쓴다**(인물 16~24명, 각 1효과 — 기존 배율·상태만). 죽으면 나가고 주운 것은 잃되 **기억 조각**(메타 재화)은 남는다 → 마을 NPC 에게 영구 소폭 강화(최대 HP +2% ×10단) 또는 축복 풀 해제. 클리어 보상: 고유 장비 후보 1 + 주문서. 주간 변형자 1개(적 체력 +30%·보상 +50% 등) 로 H 도 채운다.
- **수치**: 층당 전투 40~60s 목표, 전체 8~12분. 축복 배율 +20~35%. 기억 조각 층당 1, 보스 3. 변형자 주 1 갱신(요일 키).
- **UI**: 노드 지도 전체화면(세로 5행), 축복 카드 3장, 상단 층 표시, 종료 카드(§5-6 과 같은 카드).
- **세이브**: `rift{seed, floor, path[], boons[], weekKey}` (진행 중만), `player.shards`, `player.shardUpgrades{}`. 마이그레이션: 키 없으면 초기값. 세이브 `v` 1→2 로 올리고 `_test` 에 v1 로드 항목.
- **진단**: 씨앗 고정 시 지도 동일 · 축복 3택 축 중복 0(100회) · 죽음 시 조각만 남음 · 변형자 주간 키 갱신.
- **범위**: 새 파일 `rift.js`·`data-rift.js`(판정은 `side.js` 사냥터 엔진 재사용 — 노드 = 짧은 사냥터 생성), `ui.js`, `data-side.js`(입구 문 1). 충돌: 사가블로의 던전과 겹치지 않도록 이름·구조를 사이드뷰 발판 기반으로 유지. 새 적 칸 없음(§2-2).

### 5-4. 관문 대장 — 주간 보스(미사용 보스 6종 활용)
- **참고**: 메이플 주간 보스, 몬헌 나우 부위 파괴, D4 월드 보스. **표준** H·G·C.
- **왜**: 보스 10 중 4 만 쓰인다. 주간 목표가 0 이다. 보스 패턴이 "달려들기" 하나다.
- **메커니즘**: 미사용 보스 6 을 마을 5 + 비경 입구에 **주 1회 도전** 관문 대장으로 배치(요일 키). 패턴 3(달려들기·범위 표시 후 내려찍기·소환 2)·**방패 파괴**(등 뒤에서 누적 피해 30% → 방패 깨짐 → 10s 취약 ×1.5) · 3단 체력바. 보상: 고유 장비 1 확정(부위 순환) + 주문서 60% 2 + 기억 조각 3. 실패 시 재도전은 다음 날(일일 1회 재도전).
- **수치**: 체력 잡졸 ×40, 공격 ×2.5, 제한 시간 3분(초과 시 광폭 +50%). 부위 파괴 임계 30%.
- **UI**: 마을에 대장 문(주간 남은 횟수), 보스 HUD(3단 바·방패 아이콘·타이머), 범위 표시 바닥 데칼.
- **세이브**: `weekly{bossKey: weekKey}`. 없으면 미도전.
- **진단**: 주간 키 같으면 거절 · 방패 파괴 임계 · 3분 초과 광폭 · 보상 부위 순환.
- **범위**: `data-side.js`(대장 6 정의 — 기존 `BOSSES` 이름만 참조), `side.js`(패턴 2·방패·타이머), `ui.js`. 충돌: `data-enemy.js` 무수정(§2-2).

### 5-5. 이동 손맛 — 대시·벽 차기·코요테·버퍼·착지
- **참고**: 할로우 나이트, 데드셀. **표준** C·E.
- **왜**: 이동이 걷기·점프·줄뿐이다. 횡스크롤의 재미 절반은 이동에 있다.
- **메커니즘**: 회피를 **대시**로 재정의(공중 1회 포함, 2단 아님 — 5-1 고유 조작과 길게 누름으로 구분), **벽 차기**(발판 옆면에서 점프 → 반대로 튐, 사냥터 `walls` 배열 없으면 발판 모서리로 판정), **코요테 타임** 0.1s(발판 떠난 직후 점프 허용), **점프 버퍼** 0.12s(착지 직전 입력 저장), **착지 롤**(낙하 속도 > 1200 이면 0.15s 경직 대신 앞으로 굴러 속도 유지), 줄에서 손 떼며 점프 시 수평 가속 +30%.
- **수치**: 대시 거리 140·0.16s·쿨 0.9s·무적 0.12s. 벽 차기 수평 320·수직 620. 물리 상수(`tuned('side.*')`)는 그대로.
- **UI**: 대시 잔상 3프레임, 착지 먼지 파티클(기존 fx 재사용).
- **세이브**: 없음.
- **진단**: 코요테 창 안 점프 성공/밖 실패 · 버퍼 착지 즉시 점프 · 벽 차기 방향 반전 · 대시 무적 창 · 자동 사냥 5분 수확 ≥108 유지(이동이 바뀌어도 자동이 안 깨짐).
- **범위**: `side.js` 입력·물리 층, `auto.js`(대시 사용 1줄 선택). 충돌: 없음.

### 5-6. 목표판 + 세션 마무리 카드
- **참고**: 포켓몬GO 리서치, 원신 위탁. **표준** A·B·H.
- **왜**: §4-A·B 둘 다 ×. 가장 싸고 효과가 크다.
- **메커니즘**: HUD 상단 3줄(지금: 추적 중 사명 1 · 이번 세션: "잡기 30 / 보스 1 / 비경 1층" 중 자동 선택 · 이번 주: 관문 대장 남은 수). 사명 받으면 자동 추적, 완료 시 다음 사명 자동. 사냥터를 나가거나 죽으면 **카드 1장**: 얻은 경험치·금·장비, 도감 진척(인물 등용·업적), **다음에 할 것 1개**(미완 사명·열린 전직·주간 대장 중 우선). 5초 자동 닫힘.
- **수치**: 세션 목표 후보 5종, 하루 첫 부팅에 일일 사명 2 를 자동 추적.
- **UI**: HUD 3줄(세로에선 접힘 → 1줄 + 탭), 카드는 시트 1장 재사용.
- **세이브**: `track{questKey}`, `session{startedAt,kills,gold,...}` 는 메모리만.
- **진단**: 부팅 직후 3줄 비어 있지 않다 · 카드에 `next` 존재 · 일일 갱신.
- **범위**: `ui.js`, `quest.js`(추적 1개), `side.js`(`side:end` 이벤트에 요약 실기). 충돌: 없음.

### 5-7. 손맛 표준 적용(SAGA-DESIGN §3-C)
- **참고**: 하데스, 뱀파이어 서바이버. **표준** C.
- **왜**: hitstop·플래시가 없다. 96 무예의 감촉이 같다.
- **메커니즘**: 타격 1회 = ① hitstop 70ms(급소 120, 보스 90 — 적·플레이어 `dt` 0.15 배율, 화면 전체 아님) ② 피격 플래시 80ms(적 재질 emissive 흰색, 2D 는 `hurt` 이미 있음) ③ 흔들림: 보통 2px/80ms 도 추가(지금은 급소만) ④ 숫자 팝 0.6s(유지, 급소는 크기 1.4) ⑤ 타격음 **3종 라운드로빈**(`sfx.js` 절차 합성에 피치 ±6% 변주). 효과별 감촉 차이: `melee` 짧고 무거움, `dash` 잔상, `rain` 착탄 다발, `bolt` 관통 표시선.
- **수치**: 위. 입력→반응 ≤100ms 는 `performance.now()` 로 어드민 표시.
- **UI**: 없음(감각만). 설정 시트에 "흔들림 세기" 손잡이(0/약/보통).
- **세이브**: 설정 값 1개.
- **진단**: 타격 이벤트가 5요소를 모두 큐에 넣는다 · hitstop 동안 판정 정지 · 라운드로빈 3종 순환.
- **범위**: `side.js` `hit()`·`update()`, `side-view3d.js`(emissive), `sfx.js`. 충돌: 없음.

### 5-8. 동료 교대 — 인물 셋 편성, 즉시 교대, 교대 서명 1발
- **참고**: 원신(교대), 메이플 파티 퀘스트(솔로 재해석). **표준** D·G.
- **왜**: "내 몸 = 선두 인물" 인데 선두를 바꾸는 순간은 도감 화면뿐이다. 전투 중 교대가 되면 인물 105 가 빌드 재료가 된다.
- **메커니즘**: 편성 3인(선두+둘). 전투 중 교대 버튼 → 즉시 교대(0.2s 무적), 나오는 인물이 **서명 1발**(인물의 faction·stats 로 정한 효과 9 중 1 + 배율 — 새 효과 없음). 각 인물 체력 개별, 쓰러진 인물은 마을 복귀까지 교대 불가. 교대 쿨 4s. 직업·무예는 **계정 단위**(지금과 같음), 인물은 몸(체력·공격)과 서명만 다르다.
- **수치**: 서명 배율 ×1.6~2.2, 교대 쿨 4s, 무적 0.2s.
- **UI**: HUD 왼쪽 아래 초상 3(체력 바), 교대 버튼 1(폰은 초상 탭).
- **세이브**: `party[3]`(인물 id). 없으면 선두 1인.
- **진단**: 교대 시 체력 개별 유지 · 서명 1발 효과 9 안 · 쓰러진 인물 교대 거절.
- **범위**: `side.js`(run.party), `hero.js` 읽기만, `ui.js`, `portrait3d.js` 재사용. 충돌: 없음(도감 id 무수정).

## 6. 그래픽·에셋(SAGA-DESIGN §6·§7 중 이 판 적용분)

- **아트 바이블 적용**: 스타일 = 로우폴리 + 3단 툰 램프 + 외곽선. **재질(2026-09-17 완료, 외곽선 제외)** — 새 `js/toon3d.js`. `side-view3d.js`가 곳곳에서 직접 만들던 `new MeshLambertMaterial({...})` 19곳을 전부 `LM({...})`(같은 옵션, `world3d.toon` 손잡이로 Toon/Lambert 선택)로 바꿨다. `asset3d.js`의 `delam()`도 같은 손잡이를 본다. **외곽선은 뺐다** — 이 파일이 낱개 `new Mesh(...)` 호출뿐이라(공용 캐시 함수가 없다) 태그할 choke point가 없어, 화면 확인 없이 소품마다 잘못 붙일 위험을 무릅쓰지 않았다. `post3d.js`(ACES·노출 1.0·블룸 임계 0.9·비네트 0.25, 안개 색 = mood 지평선색 — `TIER_POST` 3단 유지, **렌더 실패 시 자동 끔도 2026-09-17 완료** — `draw()`→`drawInner`+try/catch, SAGA-DESIGN §8-6), `ssao3d.js`(HIGH 만). 카메라 1개(사이드뷰 기울기 고정), 줌 2단(보스전 확대는 유지).
- **판별 팔레트 24색 초안(mood 별, `data-side.js` `mood`)**: `sky`(마을·들판 6곳) — 하늘 #8fc1e3·잔디 #7fb069·흙 #b08a5a·돌 #9a9a8f·기와 #5b4a3f·목재 #a9744f·기본 8 + 명암 2단 ; `forest` — 짙은 초록 #3f6b3a 기준 ; `cave` — 회청 #4a5568 + 횃불 주황 ; `fire`(호로곡) — #c0392b·#2c1810. 각 mood 는 이 24색으로 **팔레트 스냅**(§7.2 `palette.py`) 한 텍스처만 쓴다.
- **변형 배가(§7.2)**: 짐승형 4 종(`kind:'beast'`) → 팔레트 5 × 크기 2 = 40 변형(적 데이터 무수정, 표시만). 사람형 38 → QRPG 6 종 × 머리·무기 킷배시(`kitbash.py`) 로 관문대별 실루엣 차이. 지형지물(나무·바위) mood 별 스냅. 출력 `assets/generated/`, `ASSET_LICENSES.md` 에 "생성물(원본 CC0 팩 파생)" 항목.
- **성장 가시화(G) 그래픽**: 전직 1~4차마다 몸 색·무기 GLB(Quaternius 무기 팩) 교체, 장비 4단마다 외곽선 색. 4차는 잔상 이펙트 상시.
- **펫 초상**: 3D 는 사슴·구미호만(완료, 권장안). 나머지 62 종은 2D 유지 — `spritegen.py` 로 팔레트만 통일(그림을 새로 그리지 않는다).
- **2D 배경(`side-view.js` 하늘·먼 산·종유석·불길)**: 의도된 스타일, 유지.
- **폰 상한**: §2-10.

## 7. 버그·안정화(Phase 0)

실기기 확인 대기(README 에서 옮김, 닫히면 여기서 지운다):
1. Phase 1~2 GLB(사람 6종·짐승 2)·바닥 도트 타일·발밑 밀도·마을 정취(집 4·우물·울타리) — 실제 폰에서 밀도·성능.
2. UI 사가고 통일(tools-drawer·⛶·독이 #hud 안 덮음).
3. 랜덤 이벤트 뒤 넷(습격·미니보스·이동 상인·NPC 구조) 자연스러움.
4. 마법형 homing 구슬 위협도, 보랏빛이 희귀·미니보스와 구별되는지.
5. 몸짓 attack·dodge·interaction·run·death 가 UAL1/QRPG 모델에서 실제로 도는지(death 클립 존재 여부).
6. 다섯째·여섯째 자리 무예 32 개 위력·기력 체감(특히 궁수·방사 dash).
7. `resetJob()` 무비용이 너무 후한지.
8. 장신구 3 부위가 상점·가방·낀 것 화면에 자연스럽게 섞이는지.
9. 잡졸 32 유형 분포(1관문 원거리·3관문 탱커 신설) 체감.
10. 마을 장사치 대화창 "🏪 저자를 연다" 단추.

적용(SAGA-DESIGN §8): 오류 링버퍼 50건 → `_admin.html` 오류 탭(**완료 2026-09-17** — 새 `js/errlog.js`, `index.html` 맨 첫 스크립트, `_test.html` `push()` 상한 진단 1항목) · 세이브 `v` 필드 + 마이그레이션 체인(**완료 2026-09-17** — `core.js` `SAVE_VERSION`/`MIGRATIONS`/`migrate()`, 예전 `parsed.v !== 1` 하드 체크가 다섯 판 공통 지뢰였다. `_test` 회귀 진단도 **완료 2026-09-17**, saga-go 것 그대로) · QA 프리셋을 제보마다 추가 · `tools/precheck.sh`(node -c + RESULT, 다섯 판 공통이라 이미 있음 + sw.js 버전 경고 추가됨) · 후처리 실패 시 자동 끔(**완료 2026-09-17**, 갈색 화면 류 방어).

## 8. 로드맵

| Phase | 내용 | 상태 |
|---|---|---|
| 0 안정화 | §7 열 항목 실기 확인·닫기, 오류 수집, 세이브 v 필드, precheck | 미착수 |
| 1 감각 | 5-7 손맛 표준 → 5-5 이동 손맛 → 5-6 목표판·카드 | 미착수 |
| 2 선택 | 5-2 유파·SP 축소 → 5-1 직업 정체성·스승 | 미착수 |
| 3 반복 | 5-4 관문 대장(주간) → 5-3 비경 | 미착수 |
| 4 편성 | 5-8 동료 교대 | 미착수 |
| 5 그래픽 | §6 툰 재질·후처리 규격·팔레트 스냅·변형 배가·전직 외형 | 미착수 |
| 완료(접음) | 물리·줄·문·사냥터 9·전투 1차·적 42·유형 5·보스·이벤트 7·직업 17·무예 96·장비 10부위·사명 20·업적 9·자동·3D 배경·몸짓·후처리 3단·UI 통일·소리·계정·어드민 | 완료(§3) |
| 원 지시서 잔여 | 폐허·동굴 지역(사냥터 5·6), 비전투 동물(도망치는 토끼·사슴·새), 채집→제작 연결, 스토리 연출(초상·감정·카메라 이동·화면 전환), 지역별 lazy 로딩, BGM 숲/전투 — 우선순위는 위 Phase 뒤 | 미착수 |

웹 판이 "샌드박스"(SAGA-DESIGN §2-1) 로 확정되면 Phase 1~2 까지만 웹에서 검증하고 3~5 는 3D 트랙으로 간다(§10-Q1).

## 9. 검증

```
구문   node -c js/*.js
진단   chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom http://127.0.0.1:8794/_test.html | grep -o "RESULT [0-9/]*"
       → 현재 153/153, 세 번 돌려 한 줄도 다르지 않아야 한다(씨앗 mulberry32 20260824)
어드민 _admin.html?selftest → ADMIN 10/10 · 균형 손잡이 tuned(): crit.rate·crit.mul·hit.knock·drop.potion·side.grav·side.jump·side.speed·side.climb
데모   _demo.html#<장면>
```
- 진단은 가입 화면을 띄우지 않는다. 일일 사명은 `lastDoneDay` 를 세이브에 직접 넣어 "오늘" 을 흉내낸다.
- 서버·헤드리스는 진단 한 번(`--dump-dom`)만. 화면 스크린샷은 사용자가 요청할 때만. 크롬을 띄웠으면 `taskkill //F //IM chrome.exe`(node.exe 금지).
- 새 후보를 넣을 때마다 §5 의 진단 항목을 `_test.html` 에 먼저 추가한다. `sw.js` 버전(`side-vX.Y.Z`)을 올린다.

## 10. 열린 질문(사용자 결정)

- Q1 웹 사가스토리를 SAGA-DESIGN §2-1 대로 "샌드박스" 로 두고 Phase 3~5 는 3D 트랙에서만 할 것인가.
- Q2 5-2 의 SP 총량 축소(레벨당 3→2): 옛 세이브의 초과분을 그대로 두는 안(권장) vs 전액 환급.
- Q3 `resetJob()` 비용: 무비용 유지 / 금 / 하루 1회.
- Q4 5-1 스승 인물 16명 선정 — `HEROES` 의 faction·stats 로 자동 배정(권장) vs 사용자가 지목.
- Q5 5-3 비경과 사가블로 던전의 선 긋기 — 비경은 발판 기반 노드 지도로 한정하는 안에 동의하는가.
- Q6 펫 초상 62종 2D 유지(권장) vs 3D 확대.
