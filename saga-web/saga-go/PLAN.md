# 사가고(saga-go) PLAN — 정본

상위 문서 `../../SAGA-DESIGN.md`(재미 표준 8·참고 게임 카탈로그·그래픽·에셋·문서 규칙). 이 문서는 그중 **사가고 몫**만 든다.

## 0. 읽는 법

- 이 파일은 설계·규칙·로드맵만 든다. **날짜 달린 세션 기록은 쓰지 않는다** → `HANDOFF.md`(append-only). 현재 상태는 `README.md` "어디까지 왔나" 절.
- 크기 상한 70KB. 넘으면 완료된 Phase 를 한 줄로 접는다.
- `grep -n "^## "` 로 목차를 뽑고 필요한 절만 `sed -n` 으로 읽는다. `README.md`(135KB)·`HANDOFF.md`(230KB+)는 목차·마지막 절만.
- 코드 주석의 "PLAN n절" 은 2026-08 판 번호다(부록 A 대응표). 새 번호와 다르다.
- 2026-09-16 재편 전의 원문(63절 작업 지시서)은 git 이력에 있다. 규칙·수치는 전부 아래로 옮겼다.

목차: §1 정체성 · §2 반드시 지킬 것 · §3 시스템 지도 · §4 재미 진단 · §5 게임성 확장(후보 8) · §6 그래픽·에셋 · §7 버그·안정화 · §8 로드맵 · §9 검증 · §10 열린 질문 · 부록

---

## 1. 정체성

**한 줄**: 실제 GPS 로 걸으면(보급) 역사 인물·짐승을 만나(등용·포획) 도감을 채우는, 걷다가 사건을 만나는 3D 역사 탐험 RPG.

| 항목 | 내용 |
|---|---|
| 원작 오마주 | 포켓몬GO(등용·포획·보급·역참·성채·토벌·천거장·반려·연성·적도·천후) + **젤다 BOTW**(오픈월드 — 랜드마크·탐험 보상·발견. 사용자 확인) |
| 인물이 되는 것 | **동료(등용)**. HEROES 105 명이 설득 3라운드로 동행이 되고, 동행 5 명이 지도 위 아바타·토벌 부대·성채 수비대가 된다. 이 판의 성장·선택·관계는 전부 "누구를 데리고 걷는가" 로 모인다 |
| 이 판만의 차별점 | 다섯 판 중 유일하게 **실제 위치**가 곧 월드다. 권역(한국 9·일본 9·중국 9 대표점)이 걸어서 도달하는 실제 장소와 엮인다. 걷지 못할 때는 자동 순행이 판단만 대신한다 |
| 플레이어 판타지 | "오늘 걸은 만큼 내 열전(列傳)이 두꺼워진다" — 걸음이 곧 보급·부화·인연이다 |
| 없는 것(되살리지 말 것) | 던전·장비(→ saga-dungeon), 문답·경영(→ saga-realm). 원작에 없는 축이라 뺐다 |
| 최종 목표 | "간단한 웹 게임 프로토타입" → "모바일에서 할 수 있는 제대로 된 3D 탐험 RPG". 판정 기준 네 질문: 10초 더 걸으면 새 것이 보이는가 / 30초 더 가면 새 장소가 나오는가 / 1분 탐험하면 새 사건이 날 수 있는가 / 다시 해도 다른 것을 발견하는가 |

## 2. 반드시 지킬 것

루트 `CLAUDE.md`(세이브 키 `deungyong-go/save/<프로필>` 불변 · 펫 id 불변 · 이름 정책 · IV/CP 금지 · 다섯 벌 복사 · 원작 에셋 금지 · 헤드리스 스크린샷 금지)는 링크만. 아래는 **이 판 고유**.

### 2.1 개발 원칙(토큰·범위)
- 구조부터 본다: `index.html` 스크립트 순서 → `game.js`(boot) → 렌더 핵심(`world.js`·`world3d.js`) → UI(`ui.js`) → `assets/`. 필요한 파일만 읽고, 한 번 본 구조는 재분석하지 않는다.
- **금지**: 기존 파일 전체 삭제 · 시스템 전체 재작성 · 필요 없는 리팩토링 · 컴포넌트 구조 일괄 변경 · 라이브러리 대규모 추가 · CSS 통째 교체 · 프레임워크 교체.
- **허용**: 함수 추가 · 기존 함수 수정 · 컴포넌트 추가 · 로더 추가 · 데이터 구조 확장.
- 작업은 단계별. 단계마다 `MODIFY`/`CREATE` 파일을 구분해 적고, 끝나면 변경 파일·내용·테스트 결과·남은 문제만 보고한다.
- 기존 UI(지도·2.5D·파티·사명·행낭·천거·도감·사관·기록·서당)는 유지한다. 새 시스템이 기존 기능을 깨면 안 된다.
- 단계마다 확인: JS 구문 · import · asset 경로 · 404 · GLB 로딩 · 모바일 Safari/Chrome · orientation · resize · touch · memory leak. 회귀 확인 대상: 게임 시작·이동·지도·파티·사명·행낭·도감·기록·기존 버튼·UI·터치.

### 2.2 에셋 원칙("코드로 그리지 말고 에셋으로", 다섯 판 공통 지시)
- 코드가 도형·그림을 조립해 만들던 것은 **전부 실제 에셋**으로 간다 — 3D 소품·건물·인물뿐 아니라 도감 일러스트·인물 초상·UI 그림까지. 스크립트에 남는 것은 이벤트와 기능(판정·규칙·상태)뿐.
- 사용자는 **품질을 먼저** 골랐다. 받을 수 있는 CC0 에셋이 이 판의 결(한옥·갓·도포)과 달라도 "둘 다 얹는다(품질 우선)". "결이 안 맞아서" 는 안 하는 이유가 되지 못한다.
- **되돌림 길은 지우지 말고 손잡이로 남긴다**(`file://` 단독판 · LOW 등급 · 오프라인은 옛 그림으로 돌아간다).
- 에셋을 못 구하면 "안 한다" 가 아니라 "이래서 막혔고 대신 이것을 할 수 있다" 로 보고한다. 이 망은 GitHub(`raw.githubusercontent.com`·`api.github.com`)만 열려 있다.
- 출처·라이선스는 `assets/ASSET_LICENSES.md`, 기술값(폴리곤·텍스처 크기)은 `assets/ASSET_CATALOG.md` — 둘 다 같이 고친다. CC0 우선, 재배포 불가는 받지 않는다, CC-BY 는 표시 문구를 옮긴다.
- 품질 방향: stylized · 적당한 폴리곤 · 모바일 친화 · 일관된 스타일·광원·스케일. 서로 다른 스타일을 섞지 않는다(→ §6 아트 바이블과 충돌하는 실사 인물은 §10-Q1).
- 로더는 공통 `asset3d`(AssetManager 역할: load/clone/preload/clearUnused). 같은 GLB 를 두 번 받지 않고 clone/`InstancedMesh` 로 세운다.

### 2.3 월드·콘텐츠 규칙
- 목적지 사슬: 시작 마을 → 논밭 → 숲 → 계곡 → 강 → 산길 → 폐허 → 도적 소굴 → 성곽 → 숨겨진 지역. 랜드마크 5~10(성·산 정상·폭포·거대 나무·옛 사당·붉은 다리·폐허 성문·거대 바위·호수·탑).
- 평면 지형 금지(평지·언덕·경사·계곡·산·강·절벽·숲·농지·마을). 격자 배치 금지 — scale·rotation·position·색 variation·군집.
- 밀도 목표 비율(기준값, 고정 아님): 자연물 30 · 건축 20 · NPC/동물 15 · 수집/상호작용 15 · 전투 10 · 랜덤 이벤트 10.
- 지역별 생태계·분위기: 마을(따뜻·밝음) · 숲(녹색·안개·늑대·멧돼지·도적) · 산(바위·절벽·강풍·산적·야수·정예병) · 폐허(어둡고 황량·망령·도적·보스) · 성(웅장). 밤·희귀 지역엔 특수/엘리트 적.
- 시간(아침·낮·해질녘·밤; 밤엔 횃불·조명·NPC 변화·야행성·특별 이벤트)·날씨(맑음·흐림·비·안개·눈)·계절은 **시각의 순수 함수**(어느 기기·언제 물어도 같은 답, 진단이 값으로 붙든다).
- NPC 는 서 있기만 하면 안 된다(걷기·대화·앉기·작업·바라보기). 마을엔 집·시장·상점·여관·대장간·창고·우물·농장·NPC·동물·길·장식.
- 발견은 남는다(발견 연출 + 도감). 탐험 보상: 지역 발견→경험치 · 보물→아이템 · NPC→퀘스트 · 희귀 장소→도감 · 숨은 지역→특별 보상. 숨은 자리는 가 보기 전까지 지도에 안 뜬다.
- **절대 넣지 말 것**: 의미 없는 거대한 맵 · 빈 평원 · 동일 나무 반복 · 모든 건물 같은 모델 · 과도한 파티클 · 모바일에서 무거운 후처리 · 무작정 고폴리 · 과도한 애니 · UI 만 바꾸고 월드는 그대로.

### 2.4 모바일·성능 규칙
- 우선순위: **1 안정성 · 2 FPS · 3 로딩 · 4 그래픽**. 저사양 30FPS 안정, 고사양 60FPS 목표.
- 세로(상단 최소 HUD·하단 조작·미니맵·플레이어 중심)·가로(넓은 시야·확장 미니맵·파티·퀘스트) 둘 다. `orientationchange`+`resize` 대응. 고정 px 최소화(clamp·vw·vh·safe-area-inset·flex·grid). 노치·홈 인디케이터 고려.
- 터치: 이동 조이스틱 · 카메라 드래그 · 핀치 줌 · 탭 선택 · 탭 상호작용. 데스크톱 WASD·마우스·휠 유지.
- 카메라: 추적·부드러운 follow·줌·회전·경계·지형 충돌·터치.
- 성능: draw call(인스턴싱·geometry/material 공유·atlas·model cache) · LOD 3단(고품질/중간/빌보드) · 품질 HIGH/MEDIUM/LOW 자동(모바일 초기 MEDIUM) · 그림자는 중요 오브젝트만 · 4K 텍스처 금지 · 필수 에셋(플레이어·기본 맵·시작 마을) 먼저 로딩 후 지역별 lazy · 스트리밍 반경(ACTIVE < VISIBLE < UNLOAD) · 캐시 정책으로 재로딩 억제.
- **3D 값을 고쳤으면 `sw.js` 의 `VERSION` 을 올린다**(안 올리면 폰이 옛 캐시를 본다).
- `core.hash2` 는 이 판만 옛 식(0~0.5 반환)이다. 지형 문턱값이 그 범위에 맞춰져 있어 그대로 둔다.

## 3. 현재 시스템 지도

데이터: 인물 105(kr 26·sg 22·eu 22·jp 20·wd 15) · 펫 105(pt 89·pk 16) · 열전 105 · 등급 1~5(기본·상급·희귀·영웅·전설). 자가진단 약 463 항목(2026-09-10 기준, 정확한 수는 진단으로 다시 잰다). `sw.js` VERSION `go-v5.30.0`. 손잡이(`core.tuned`) 182 개.

| 시스템 | 주 파일 | 상태 | 진단 항목(대표) |
|---|---|---|---|
| 지도·스폰·시점 3모드·구역 이름 30 | `world.js` | 완료 | 지형 결정성 · 시점 순환 |
| 실제 지형(OSM) / 손그림 땅 1km | `geo.js` / `land.js` | 완료 · 실기 미확인 | 지형(geo) 레이어 순서 · 땅 — 길이 하나로 이어진다 |
| 손으로 놓은 소품(land.js `deco`) | `land.js` → `world3d.propPlan` | 층만 있음(빈 배열) · 맵 편집기 "3D 배치"가 채운다 | 땅 — 손으로 놓은 소품(deco) · 종류 10(DECO_T) · 집·탑·우물·장터는 어느 칸에 놓아도 벽(마을 칸 밖은 `houseRects` 가 deco 만 본다) · 손잡이 `land.deco` |
| 3D 렌더·배우·소품·높낮이·물·하늘·SSAO·후처리 | `world3d.js` `actor3d.js` `prop3d.js` `relief3d.js` `water3d.js` `sky3d.js` `ssao3d.js` `post3d.js` | 완료 · 실기 미확인 다수 | 조명 · 사물 · 배우 · 땅 · 물 · 그늘 · 후처리 |
| 에셋 창고(HERO_RECIPES 27벌·UAL1 몸짓 41) | `asset3d.js` | 완료 · MPFB 실사 5인 실화면 미확인 | 에셋 — 표/클립/키/빛깔 |
| 등용(설득 3라운드)·포획(타이밍)·3D 무대 | `encounter.js` `encounter3d.js` | 완료 | 등용 성공 → 도감… · 조우 무대 |
| 실시간 전투(속공·필살·회피·스태거·패턴3·연타회심·차지) + **자동 전투 🤖**(입력만 대신 — 반응 0.3초·저스트 회피 없음) | `rogue-action.js`(`duel.js` 되돌림) | 완료 · **손맛 사람 미검증** | 교전 · 전투 손맛 |
| 전투 연출(흔들림 0.5·hold 70~110ms·파티클) | `battle3d.js` | 완료 | 교전 무대에 둘이 마주 선다 |
| 들판 전투(원신식 — 무리·편성 4 교체·원소 반응·방패, §5 ⑨) | `field-combat.js` | 코드분 완료 · 실기 미확인 | 들판 전투 — (15) |
| 지역 지도(원신식 — 1.2km 지역·바이옴 6·랜드마크·순간이동·이름 띠, §5 ⑩) | `biome.js` | 코드분 완료 · 실기 미확인 | 지역 — (7) |
| 토벌(45분 적장·격문) / 성채(수비대 셋·공물) / 역참(5분) | `raid.js` / `fort.js` / `station.js` | 완료 | 적장은 드물게… · 성채 · 역참 |
| 사명(셋·인장 하루 1·일곱→명사) / 주간 사다리 5단 | `quest.js` / `milestone.js` | 완료 | 사명 · 인장 |
| 천거장(2·5·10km·9통) / 반려 / 연성·승화(영초·단사) | `letter.js` / `buddy.js` / `growth.js` | 완료 | 천거장 · 반려 · 연성 |
| 적도(역참 점거·암영·정화) | `rogue.js` | 완료 · 균형 미조정 | 적도 — 해시·물러남·정화 |
| 천후(3시간 결정론 6종) / 계절 | `weather.js` / `season.js` | 완료 | 천후 · 계절 |
| 사건 11종(300m·62%, 평균 484m) / 걷기 흥 | `event.js` / `ambient.js` | 완료 | (사건은 자가진단 간접) |
| 주민 10 / 짐승 5 / 말 걸기 / 땅 사람(탑마다 세 시대 셋, §5 ⑱) | `npc.js` `animal.js` `talk.js` `folk.js` | 완료 · ⑱ 실기 미확인 | 말 걸기 · 땅 사람 ⑱ |
| 발견 도감 / 전체지도(M, 발자취 40m·400점 위경도) / 미니맵 | `codex.js` `overworld.js` `minimap.js` | 완료 | 오버월드 · 미니맵 |
| 자동 순행 / 성능 3단 자동 / 소리(CC0 mp3 5) / 설정 시트 / 목표 한 줄 | `auto.js` `perf.js` `audio.js` `ui.js` | 완료 | 자동 · 손잡이 |
| 권역·절차 인물(한 9·일 9·중 9, `genchar*`) | `region-*.js` `genchar*.js` | 완료 · 스폰 체감 미확인 | (해시 결정성) |
| 온라인 사관(AI) / 가입 저장 | `net.js` `ai.js` `server/` / `account.js` | 완료 · 응답 실청취 미확인 | 오프라인 차단 · 가입 |
| 2D 스프라이트(등신 3·양식 3) | `sprite.js` | 완료 | 스탬프 캐시 |
| 방치 전투·환생 | `js/_expansion/` | 보관(되살릴 땐 `94850f8`) | — |

## 4. 재미 진단 (SAGA-DESIGN §3 표준 8)

사용자 실플레이 판정(2026-09-06): "다섯 가지 모두" — 이동 단조 · 손맛 약함 · 보상이 안 와닿음 · 목표 안 보임 · 지켜보는 재미 없음. 그 뒤 목표 한 줄·포획 연출·실시간 포획·사건 빈도·레벨업 연출을 넣었지만 아래 표대로 구조는 그대로다.

| 표준 | 상태 | 근거 |
|---|---|---|
| A 목표판 | △ | `ui.js goalLine()` 한 줄(첫 미완료 사명)만. 세션·주간 줄 없음. 주간 사다리(`milestone.js`)는 시트 안에 숨어 있다 |
| B 세션 마무리 카드 | × | 없음. 앱을 닫으면 그냥 끝난다 |
| C 손맛 | △ | 스태거·패턴 3·연타 회심·차지·흔들림(`battle3d.shake` 0.5)·hold 70~110ms 는 있다. 타격음 라운드로빈 없음(효과음 5조각 단일), 숫자 팝은 카드 전투만. **회피가 거저**(예고 1.1s 안에 5.5m 벗어나면 되는데 이동 8m/s). 사람이 손으로 겪어 본 적 없음 |
| D 선택 3택 | × | 설득 3라운드의 어필 선택 외에 성장 선택이 없다. 승급은 중복 인물+금 자동 |
| E 발견 밀도 | △ | 사건 평균 484m(8m/s 기준 60초). 표준(30초≈240m)의 절반. 종류는 역참·성채·스폰·사건 11·숨은 자리 4·주민·짐승으로 충분 |
| F 실패와 회복 | × | 토벌·도적전 패배 = 쉬는 시간뿐. 비용도 회수도 없다 |
| G 성장 가시화 | △ | ★5 금테·등급 이름·레벨업 연출은 있으나 연성·승급은 숫자. 동행이 강해져도 아바타가 안 바뀐다 |
| H 돌아올 이유 | ○ | 인장 하루 1 · 주간 사다리 · 천후 3시간 · 계절 · 토벌 45분. **있는데 A 가 약해 안 보인다** |

**가장 큰 구멍 3**: ① 세션 구조 부재(A·B) — 켜서 뭘 하고 언제 끄는지가 없다. ② 선택·실패 없음(D·F) — 결과가 늘 같다. ③ 손맛 미검증·회피 거저(C) — 있는 전투가 긴장을 못 만든다.

## 5. 게임성 확장 — 후보 8 (상세 설계)

공통: 새 세이브 필드는 `core.freshSave()` 기본값에 추가하고 `mergeDeep` 이 채우므로 **옛 세이브 마이그레이션 코드가 필요 없다**(필드 추가만 하는 경우). 기존 필드의 의미를 바꾸는 후보(③)만 마이그레이션을 적었다. 진단은 씨앗 고정·`weather.force('clear')`·`rogue.force(false)` 아래에서 3회 동일해야 한다. 새 파일은 이 판 전용이라 다섯 벌 복사 규칙에 안 걸린다. 이름은 전부 가명(실명 금지).

### ① 봉수대(烽燧臺) — 오르면 지도가 열린다

코드분 완료(2026-09-19, `beacon.js`) · 실기 확인 대기. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ② 사당(祠堂) 시련 — 3분 방

코드분 완료(2026-09-21, `shrine.js`) · 실기 확인 대기 — §5 ⑲ 순서 9 가 이것을 비경으로 넓힌다. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ③ 75초 토벌 — 부위 파괴·저스트 회피

코드분 완료(2026-09-17, 토벌만 — `rogue-action.js` `create({raid:true})`) · 실기 확인 대기. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ④ 일과판 + 마무리 카드

코드분 완료(2026-09-17, `daily.js`, 사명과 나란히 §10-Q3) · 실기 확인 대기 — §5 ⑲ 순서 8 이 풀을 넓힌다. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ⑤ 비석(碑石) 순례 — 실제 위치 수집 (GPS)

코드분 완료(2026-09-21, `stela.js`, 200m 격자) · 실기 확인 대기(실제 GPS 로 걸어야). 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ⑥ 인연(因緣) — 동행 관계·지원 대화

코드분 완료(2026-09-21, `bond.js`·`data-bond.js`) · 실기 확인 대기. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ⑦ 승급 3택 — 특성 갈래

코드분 완료(2026-09-21, `perk.js`) · 실기 확인 대기. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ⑧ 패배 비용과 회수

코드분 완료(2026-09-21, `drop.js`, 15%·상한 300·회수 10분·400m) · 실기 확인 대기. 9필드 설계 원문은 `git show a9b33577:saga-web/saga-go/PLAN.md` 의 이 절(§0 규칙대로 접음).

### ⑨ 들판 전투(野戰) — 원신식 (2026-09-24 사용자 선택 "사가고를 원신 스타일로 전투")
| 필드 | 내용 |
|---|---|
| 참고 | 원신(파티 4 교체·원소 스킬/폭발·원소 반응·스태미나 회피·세계 레벨) · 규칙은 `saga-godot/PLAN.md` 106장 ③⑦ 과 같게 |
| 표준 | C D F G H |
| 왜 | 전투가 사건이 여는 1:1 결투 무대뿐이라 "걷다가 싸운다"가 없고, 한 명만 싸워 동행 편성이 전투에 안 닿았다(사용자 "재미가 없네") |
| 메커니즘 | 160m 격자마다 해시로 **무리**(45%): 보통 여섯 꼴·정예(원소 방패) 셋·우두머리(400m 밖 5%). 12m 안에 들면 붙고 34m 넘게 끌면 돌아가 다시 찬다. 적 셋 꼴: 코앞(melee)·내 자리에 떨어지는 침(spit)·제 둘레 원(slam) — 떨어질 자리를 붉은 원으로 예고. **편성** = 동행 앞 4, 원소는 id 해시(화·수·뇌). 기본 공격 3타(0.9·1.0·1.5, 6m 안이면 파고든다) · 원소 스킬(7초, 4.5m) · 원소 폭발(기력 60, 7m, 1초 무적) · 회피(스태미나 20, 0.35초 무적) · 교체(1초). 반응: 증발 ×1.5 · 과부하(4m 광역 ×1.2·밀침) · 감전(3초 ×0.45). 적 원소 공격: 화상(체력 틱, 이것만으론 안 쓰러짐)·젖음(스태미나 -25)·감전(기력 -10). 쓰러지면 자동 교대, 전멸하면 30% 로 일어나고 금 일부가 ⑧ 짐으로 떨어진다 |
| 수치 | 적 체력 520·공격 80 × 꼴 배수 × 등급(1+0.3×(등급-1)), 등급 = 1 + ⌊원점 거리/900m⌋(최대 6) · 인물 공격 = 무력×0.7+지력×0.3 · 체력 300+통솔×6 · 숙련 보정 1+지력/300 · 방패 300×꼴 배수(같은 원소 0·물리 0.4·상성 2.5, 깨지면 2초 비틀) · 보상 처치 금 3+2등급·경험 6×등급×꼴, 토벌 금 15×등급(+정예 20·우두머리 60 ×등급)·단사 1(우두머리 3) · 다시 서기 15분 · 손잡이 `field.*`(on·hpBase·atkBase·campChance·tierStep·respawnMin 등) |
| UI | 싸울 때만(적 22m 안·교전 중) 우하단 ⚔️ 공격·스킬·폭발(기력 차오름)·회피 + 스태미나 줄, 우상단 편성 초상 4(체력·기력·원소 빛깔, 눌러 교체). 적 머리 위 이름·등급·붙은 원소·체력·방패 줄. 피해 숫자·반응 글자는 원소 빛깔. 그동안 독·근처 줄은 숨는다. 키 J·E·Q·␣·1~4 |
| 세이브 | `save.field = { camps: {칸키: 치운 시각}, kills, clears }` — 읽는 쪽 기본값(마이그레이션 불필요). 체력·기력은 세션에만 |
| 진단 | 반응 표 · 방패 배수 · 무리 결정성·시작점 60m · 3타 사슬 · 증발 ×1.5 · 과부하 광역 · 감전 3틱 · 방패 깨짐 비틀 · 예고/회피 · 교체 쿨 · 자동 교대·전멸 · 폭발 기력 · 스태미나 · 토벌 후 안 섬 · 런타임 보상·세이브·아바타 교체(15) |
| 범위 | CREATE `js/field-combat.js`(판정 순수 함수 + 런타임·HUD·키) · MODIFY `world3d.js`(적 배우·교체된 사람이 아바타·`groundY` 노출·`playAnim` 아무 배우) `world.js`(2D 그림) `game.js`(tick·init) `css/style.css` `index.html` `sw.js`(빠져 있던 `rogue-action.js` 도 넣음) |
| 구현(2026-09-24) | 코드분 완료 — 적 몸은 새 에셋 없이 도감 펫 모델(멧돼지·도깨비·두꺼비·날쌘용·반달곰·홍염마·현무·섬영마·폭군용)을 빌렸다. 자동 전투(🤖) 또는 자동 순행 중이면 0.3초마다 폭발(2마리+)·스킬·공격을 대신 누른다. 옛 1:1 결투는 도적·성채·사건용으로 그대로. **실기 확인 대기**(손맛·난이도·HUD 배치·적 모델 크기) |

### ⑩ 지역 지도 — 원신식 (2026-09-24 사용자 선택 "맵형태도")
| 필드 | 내용 |
|---|---|
| 참고 | 원신(지역마다 다른 풍경·멀리 보이는 랜드마크·순간이동 지점·지역 진입 이름) · BOTW 탑 |
| 표준 | A E H |
| 왜 | 손그림 땅 밖은 노이즈 들판이 끝없이 같아 "저기 가 보고 싶다"가 없다 |
| 메커니즘 | 1.2km 지터 격자 보로노이로 **지역**을 나누고 다섯 바이옴 중 하나: 푸른 들·대숲 골·붉은 협곡·안개 늪·옛 성터 고원. 바이옴이 노이즈 지형 문턱(숲·산·물 비율)·기복 세기·땅 빛깔·무리 꼴(⑨) 가중을 바꾼다(손그림 땅·실제 GPS 지형은 그대로). 지역 한가운데 **랜드마크**(멀리서도 보이게 거리 컬링 예외 + 빛기둥) — 25m 안에 가면 지역 발견 카드 + **순간이동 지점**. 전체 지도(M)에서 발견한 지점을 눌러 순간이동(키보드 모드만). 지역 경계를 넘으면 지역 이름 띠 |
| 세이브 | `save.regions = { 지역키: 발견 시각 }` — 읽는 쪽 기본값 |
| 진단 | 지역 결정성 · 이웃 지역 바이옴이 섞인다 · 바이옴이 지형 비율을 바꾼다 · 랜드마크 발견·순간이동 · GPS 모드에선 순간이동 없음 |
| 범위 | CREATE `js/biome.js` · MODIFY `world.js`(노이즈 지형 문턱) `relief3d.js`(세기) `world3d.js`(랜드마크·땅 빛깔) `field-combat.js`(무리 가중) `overworld.js`(지점·순간이동) `ui.js`(이름 띠) |
| 구현(2026-09-24) | 코드분 완료 — 고향(원점 지역)은 옛 지형 비율 그대로라 기존 판·진단 회귀 없음(문턱을 진단이 옛 값과 견준다). 경계는 둘레 아홉 지역을 거리 가중으로 섞는다(둘째만 섞으면 세 지역이 만나는 자리에서 값이 튀었다). 기복 배수는 자연 지형에만(마을·길·논밭은 어디서든 같은 높이). 랜드마크 탑은 기존 CC0 탑 GLB 다섯(Watchtower·PointyTower·LargeTower·Tower·LargeSquareTowerBricks)을 바이옴마다, 성채의 두 배 크기 + 안개를 뚫는 빛기둥(못 찾음 금빛·찾음 푸른빛). 발견 보상 금 60+30×등급·단사 2·경험 40. 지형 칸마다 문턱 캐시. **실기 확인 대기**(지역 풍경 차이가 눈에 띄는지·빛기둥 크기·이름 띠 자리) |

### ⑪ 지역 수호자 — 원신식 (2026-09-24, §8 "원신식 다음" 첫 줄)
| 필드 | 내용 |
|---|---|
| 참고 | 원신(지역 우두머리·원소 방패 겹 — 방패 속성에 맞춰 캐릭터 교체) |
| 표준 | C D G |
| 왜 | ⑩ 랜드마크는 "찾으면 끝"이라 지역마다 싸워 볼 목표가 없고, ⑨ 교체가 전투를 바꾸는 순간이 드물다 |
| 메커니즘 | 고향을 뺀 지역마다 랜드마크 탑 곁(+16,+10m) 수호자 하나 — 바이옴마다 몸·원소가 다르다(벌판 뿔룡·대숲 백호·협곡 주작·늪 청룡·성터 불가사리, 도감 펫 몸 빌림). 방패 **두 겹**(겉 → 속, 상성 원소가 서로 달라 한 사람으론 속 방패가 느리다): 겉이 깨지면 0.8초 휘청하고 곧 속 방패가 차오르며 "속 방패 💧 — ⚡ 원소 동행으로 바꿔라" 알림, 속이 깨지면 3초 드러눕는다(약점). 머리 위 이름에 남은 겹(🛡️🔥⚡) |
| 수치 | 등급 = 그 자리 등급 + 1(최대 6) · 체력 6.0~7.5 × 기본 · 겹마다 방패 300×1.3×등급 배수 · 보상 금 150×등급·단사 8·경험 30×등급 · 손잡이 `field.guards`(0 이면 안 선다) |
| 세이브 | `save.field.guards = { 지역키: 토벌 시각 }` — 한 번 치면 다시 안 선다(무리처럼 15분 뒤 되살아나지 않는다). 읽는 쪽 기본값 |
| 진단 | 다섯 바이옴 표·결정성·등급 · 고향 없음 · 겉 깨짐 → 속 방패·0.8초·체력 그대로 · 속과 같은 원소 면역 · 속 깨짐 3초 · populate 가 세우고 토벌 뒤 안 세움 |
| 범위 | MODIFY `field-combat.js`(FOES 다섯·`guardianAt`·겹 방패 `breakShield`·`populate(…, lfn)`·보상·HUD) · 새 에셋 없음 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(수호자 크기·탑과 겹치는지·교체 알림이 읽히는지·난이도) |

### ⑫ 인물마다 다른 원소 스킬 모양 — 원신식 (2026-09-24, §8 "원신식 다음" 둘째 줄)
| 필드 | 내용 |
|---|---|
| 참고 | 원신(캐릭터마다 스킬 모양이 달라 편성이 곧 전술 — 찌르기·돌진·장판·소환수) |
| 표준 | C D F |
| 왜 | ⑨ 스킬이 전원 같은 원형 광역이라 교체해도 원소만 바뀌고 손맛은 같다 |
| 메커니즘 | 동행 id 해시(원소와 다른 씨앗)로 넷 중 하나 — **찌르기**(겨눈 쪽 직선 8m·폭 1.6m) · **돌진**(적 1.2m 앞까지 최대 6m 파고들며 길 위를 친다, 0.3초 무적) · **장판**(겨눈 자리 4m 가 5초 동안 1초마다 원소를 묻힌다 — 반응 굴리기) · **소환**(곁의 정령이 8초 동안 1.5초마다 7m 안 가장 가까운 적 하나, 교체해도 남는다). 주인공 '나'는 옛 원형 그대로. 스킬 단추에 모양 이름, 쓸 때 머리 위 "🗡️ 찌르기" |
| 수치 | 찌르기 ×2.8 · 돌진 ×2.4 · 장판 틱 ×0.6(5틱 = ×3.0, 여럿) · 소환 틱 ×0.9(6번 = ×5.4, 하나씩) · 원형 ×2.2 그대로 · 손잡이 `field.thrustMul·dashMul·fieldMul·summonMul` |
| 세이브 | 없음(모양은 id 에서 매번 뽑는다) |
| 진단 | 배정 결정성·넷 고루·주인공 원형 · 찌르기 선 안만 168 · 돌진 3.8m·144·무적 · 장판 5틱 · 소환 6번·교체 뒤 남음 |
| 범위 | MODIFY `field-combat.js`(`shapeOf`·`skill` 갈래·`stepZones`·HUD·그림) · 옛 원형 전제 진단 셋은 모양을 원형으로 박았다 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(모양이 눈에 읽히는지 — 그림은 지금 원 여러 개로만 그린다 · 소환 정령 몸이 없다 · 난이도 균형) |

### ⑬ 지역 사명 사슬 — 원신식 (2026-09-24, §8 "원신식 다음" 셋째 줄)
| 필드 | 내용 |
|---|---|
| 참고 | 원신(지역 탐색도·평판 — 지역마다 할 일 목록이 차오른다) · BOTW 탑→사당 |
| 표준 | A E G |
| 왜 | ⑩ 탑·⑨ 무리·⑪ 수호자가 따로 놀아 "이 지역을 끝냈다"는 매듭이 없다 |
| 메커니즘 | 고향을 뺀 지역마다 세 단 — ① 가운데 탑을 찾아라(⑩ 발견) ② 이 지역 무리 토벌 2 ③ 탑 곁 수호자(⑪). **앞에서부터 차례로** 채워진다(탑 전에 친 무리도 셈은 쌓이고, 탑을 찾는 순간 둘째 단까지 넘어간다). 무리 위치는 `regionAt` 으로 가른다. 지금 선 지역의 사명이 위쪽 가운데 한 줄(📜 이름 사명 n/3 · 다음 할 일), 다 끝나면 숨는다 |
| 수치 | 둘째 단 금 80×등급·단사 2 · 셋째(평정) 금 200×등급·단사 10(+ 수호자 제 보상) · 첫 단 보상은 발견이 준다 |
| 세이브 | `save.missions = { 지역키: { clears, paid } }` — 단마다 보상 한 번(`paid`). 읽는 쪽 기본값 |
| 진단 | 순수 단 계산(앞 단 막힘·1/2 글) · 무리 먼저 → 탑 → 2/3 한꺼번에 · 고향 무리 안 셈 · 다시 불러도 안 줌 · 수호자 → 평정 |
| 범위 | MODIFY `biome.js`(`missionView`·`progressMission`·`field:clear`/`field:guard`/`region:found` 구독·한 줄) `css/style.css`(`#region-mission`) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(한 줄 자리가 상단 지갑·미니맵과 겹치지 않는지) |

### ⑮ 고정 특색 지역 — 사가블로식 이름 있는 땅 열여섯 (2026-09-24 사용자 지시, 전 프로젝트 공통 — SAGA-DESIGN §12)
| 필드 | 내용 |
|---|---|
| 참고 | 사가블로 PLAN §5.12(고정 세계 지도 — 방위별 특색 지역) · 디아블로 지역 · 원신 나라별 풍경 |
| 표준 | E G |
| 왜 | 사용자: "전체 지역을 랜덤이 아닌 사가블로처럼 각각 특색이 있는 지역으로". ⑩ 은 1.2km 칸마다 바이옴·이름을 **해시로** 뽑아 다섯 바이옴이 모자이크처럼 흩어졌다 — 어디가 어떤 땅인지 기억할 수 없었다 |
| 메커니즘 | `biome.js` `ZONES` — 고향을 가운데 두고 방위 여덟 × 고리 둘(안쪽 `RING_R` 6km 안 · 바깥) = 이름 있는 땅 열여섯. 안쪽: 청풍 벌판·갈대 나루·대숲 고을·가마골·옛 성터 언덕·달그림자 호수·솔숲 고개·황금 들녘. 바깥(현대·미래·신화 퓨전): 잿빛 폐도시·소금 갯벌·용의 협곡·태양 신도시·서역 모랫길·천계 구름 사당·북방 설산·기계 황무지. 땅마다 이름·한자·이모지·사연·시대(era)·바탕 바이옴(지형 문턱·기복·무리 꼴을 그대로 빌린다)·지명 여섯. 칸(탑·순간이동·사명·수호자)은 그대로 두고 **칸의 바이옴·이름을 제 땅에서** 받는다. 땅 경계는 칸 좌표만으로 정해지는 고정 흔들림(각 ±0.17 rad·거리 ±0.6칸)으로 굽힌다. 땅을 넘으면 큰 이름 띠("🌾 청풍 벌판(淸風)" + 사연 — 칸 이름)·`zone:enter` |
| 세이브 | 없음(발견 키 `save.regions` 는 칸 키 그대로 — 옛 세이브 호환) |
| 진단 | 열여섯·이름·지명·고리 안팎·±12칸에 다 있음 / 이웃 칸 76% 같은 땅·결정성 / 땅 넘을 때만 zone:enter(3) · 기존 지역 진단 9 그대로 통과 |
| 범위 | MODIFY `js/biome.js`(ZONES·zoneOfCell·cellAt·이름 띠) `js/overworld.js`(전체 지도에 땅 열여섯 이름·왼쪽 위 지금 땅) |
| 남은 것 | 땅마다 전용 3D 소품(폐도시 굴뚝·태양광 판 등 — 사가블로 t_factory 류)·땅 색 · GPS 모드에서 고향(원점)이 첫 수신 자리라 땅 배치가 "내 시작점 기준" 인 점(실제 위경도 고정이 필요하면 다시 결정) |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(땅 크기·경계 체감·이름 띠) |

### ⑭ 지도 위 동행 모션 — 교체 연출 (2026-09-24, §8 "원신식 다음" 넷째 줄)
| 필드 | 내용 |
|---|---|
| 참고 | 원신(교체하면 새 캐릭터가 곁에서 들어서고 앞사람은 흩어진다) |
| 표준 | C G |
| 왜 | ⑨ 교체 때 지도 위 몸이 그 자리에서 뚝 바뀌어 "사람이 바뀌었다"가 안 느껴진다 |
| 메커니즘 | `world3d.syncActors` — 선두가 바뀌면 옛 몸을 지우지 않고 'swapout' 으로 넘겨 옆뒤로 물러나게 한 뒤 떠오르며 흩어지고(`sweepActors` 그대로), 새 몸은 옆 1.6m 에서 걸어 들어와 선다. 판정·카메라는 `pos` 그대로(화면 층만) |
| 수치 | 들어오기 0.35초(ease-out) · 물러나기 0.6초 2.4m · 0.9초 뒤 흩어짐 |
| 세이브 | 없음 |
| 진단 | `swapMotion(ms)` 끝점·중간(ease-out)·끝 시각 |
| 범위 | MODIFY `world3d.js` |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(옛 몸이 물러나는 쪽·연출 길이) |

### ⑯ 싸워서 등용 + 건물 가림 카메라 — 원신식 (2026-09-24 사용자 지시)
| 필드 | 내용 |
|---|---|
| 참고 | 원신 — 필드 보스·정예를 실전으로 꺾는 손맛, 벽에 닿으면 카메라가 당겨 오는 3인칭 |
| 표준 | A C |
| 왜 | 사용자: "등용 컨텐츠가 넘 잼없어 — 3가지 선택이 아니라 원신처럼 싸워서" · "건물 근처로 가면 캐릭터가 안 보여". 등용은 무·지·덕 세 단추 설득뿐이었고, 3D 카메라는 가림 처리가 없었다 |
| 메커니즘 | 들판 인물을 누르면 카드 없이 그 자리에서 ⑨ 들판 전투가 열린다(`field-combat.js challenge`·`duelCamp`). 인물은 제 기질로 싸운다(무=광역 내려찍기·지=떨어지는 원거리·덕=빠른 근접), 원소는 동행이 됐을 때와 같은 `elementOf(id)`. 체력 HP_BASE×(1.6+0.7×★)×등급배수 · ★3 졸개 하나·★4~5 둘(제 원소) · ★4 방패 한 겹·★5 두 겹(제 원소→상성, 교체 퍼즐). 체력이 0 이면 쓰러지지 않고 **굴복**(1초 무릎) → 등용 카드. 전멸이면 떠나고(등용서 1·명성 절반, 옛 실패와 같다), 끌고 34m 밖으로 가면 인물이 돌아가 없던 일(소모 없음). 2D·진단·자동 순행은 옛 설득 그대로. 카메라: 머리→카메라 선이 집 몸통(`houseRects`, 키 h 실음)에 지붕보다 낮게 들면 그 앞까지 당긴다(`camOcclude`, 0.1초마다, 2.5D·3D) |
| 세이브 | 없음(등용 결과는 옛 `gainHero` 그대로) |
| 진단 | 싸워서 등용 ⑯(희귀도→체력·졸개·방패 · 굴복 1초 뒤 win · 전멸 lose · 카드가 도감·등용서·명성) · 건물 가림(당김·지붕 위·비껴감·벽 안) |
| 범위 | MODIFY `js/field-combat.js`(h_might·h_wisdom·h_virtue 틀·duelCamp·challenge·굴복·duelEnd) `js/encounter.js`(startHero 분기·duelResult) `js/world3d.js`(겨루는 동안 원래 인물 숨기고 전투 몸을 hero 로·굴복 자세·camOcclude) |
| 남은 것 | 인물 고유 스킬(⑫ 모양)을 적으로도 쓰기 · 등용 전 짧은 말 겨루기 연출 · 자동 순행도 실전으로 할지 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(난이도·굴복 연출·카메라 당김 체감) |

### ⑰ 지형 설계 — 강·산맥·정상 + 오르기·헤엄·점프 — 원신식 (2026-09-24, 사용자 "맵이 원신식 맞아?" · "점프도")
| 필드 | 내용 |
|---|---|
| 참고 | 원신(멀리 보이는 산을 기어올라 정상에서 내려다보기·강을 헤엄쳐 건너기·기력) · BOTW 오르기 |
| 표준 | A E H |
| 왜 | ⑩⑮ 로 땅 이름은 생겼지만 지형은 노이즈 뭉치(산 16m 언덕·웅덩이 물)라 "저 산 너머"가 없었다 |
| 메커니즘 | `landform.js` 에 손으로 그은 **고정** 지형 — 강 셋(은하강: 설산→고향 동쪽→갈대 나루→소금 갯벌 하구 · 붉은내: 용의 협곡→가마골 · 달빛내: 성터→달그림자 호수)·호수 하나·산맥 일곱(땅마다 제 산, 능선 34~140m, 허리가 가장 가파른 smoothstep 단면)·정상 여덟. 굽이는 고정 노이즈(난수 0). `world.terrainAt` 이 손그림 땅·OSM 다음·노이즈 앞에서 묻고, `relief3d` 가 능선 높이를 얹는다. **고향 1.3km 는 안 닿는다**. 강은 1.8km 마다 여울(걸어 건넌다), 지역 탑 둘레는 ⑩ 대로 비운다. 키보드 모드만: 능선 오르막(기울기 >0.28)은 걸음 ×0.5·기력 소모, 다하면 30% 찰 때까지 못 오름 · 강 헤엄 ×0.6(지치면 0.3) · 가파른 내리막 ×1.2 · 점프(스페이스·점프 단추, 1.3m 화면 층, 적이 곁이면 스페이스는 ⑨ 회피) · 오르다 뛰면 기력 20 으로 4m 도약. 정상 30m 안 = 발견(금 80+높이·단사 2·경험 50). 전체 지도에 강·산맥·정상 |
| 세이브 | `save.peaks = { 정상키: 시각 }`(읽는 쪽 기본값). 기력은 안 남긴다 |
| 진단 | 고정·고향 그대로·끄면 옛 노이즈 / 강줄기 끊김 0·호수·물 파임 / 정상 높이·발견 한 번 / 오르기·헤엄·점프·도약·끄면 1 |
| 범위 | CREATE `js/landform.js` · MODIFY `world.js`(terrainAt·moveByKeys) `relief3d.js` `world3d.js`(점프 높이·jump 몸짓) `overworld.js` `game.js` `css/style.css` |
| 남은 것 | 높은 곳 눈빛 땅색·날개 에셋(지금은 코드 천 두 장)·다리 상판 높이(디딤돌 바위라 실기로 맞출 것) |
| 구현(2026-09-24) | 코드분 완료 — 오르기·헤엄·점프 + 활공(뛰어오른 채 한 번 더, 날개는 코드 천 두 장)·오른 정상 순간이동(`pk:키`)·여울 다리 18·발원지 폭포 2·땅 전용 퓨전 소품 33종(`models/fusion/`, 제 시대 60%·다른 시대 40%, 들·숲 칸 16%). 경위는 HANDOFF 같은 날. **실기 확인 대기**(산 크기·오르기 속도·점프 단추 자리·활공 체감) |

### ⑱ 세 시대 사람·적 — 탑 둘레 사람 셋 · 무리 40% 다른 시대 (2026-09-24, §8 "다음: 사람·적도 세 시대로" · SAGA-DESIGN §13)
| 필드 | 내용 |
|---|---|
| 표준 | E G |
| 왜 | ⑮⑰ 로 땅 이름·소품은 세 시대가 섞였는데 사람은 고향 주민 열·도감 인물, 적은 짐승·도깨비뿐 — 폐도시에도 멧돼지만 나왔다 |
| 메커니즘 | **적**: `field-combat` `ERA_THEMES` — 현대(잿빛 떼쥐·벼락 말벌·떠도는 망자)·미래(정찰 드론·경비 보행기·별바다 손님, 정예 강철 거신 뇌 방패). `campAt(…, zfn)` 이 무리마다 땅 시대(신화 땅은 과거)를 60%, 나머지 둘을 20%씩 해시로 고른다. 과거 몫은 옛 바이옴 무리 그대로 · 우두머리(폭군용)는 과거 · 시작점 300m 안은 과거만. **사람**: `folk.js` — 칸 탑 둘레 24m 에 셋(첫째 제 시대 + 나머지 둘), 수호자 자리는 비킴. 시대마다 역할 넷(나무꾼·무녀·파수꾼·약초꾼 / 여행자·수리공·택배 기사·순찰 대원 / 측량사·탐사 대원·시간 여행자·수리 기사), 4.5m 를 36초 주기로 오감(시각의 순수 함수), 12m 안이면 땅·칸 이름 든 한 마디(조사 받침 맞춤). 몸은 `hero:era:folk_*`(애니메 몸 안 씀) |
| 세이브 | 없음 |
| 진단 | 적 — 제 시대 약 60%·땅 16 모두 세 시대·모델 경로·300m 안 과거·zfn 없으면 옛 무리 / 사람 — 땅 16×3 시대 순서·24m·수호자 8m+·오감·조사·고향 0·끄면 0 |
| 범위 | CREATE `js/folk.js` · MODIFY `field-combat.js` `asset3d.js`(`pet:fc_*`·`hero:era:folk_*`·`flying` 걷기 낱말) `world3d.js` `world.js` `game.js` · 에셋 `models/foes/`(7)·`models/people/folk/`(11) — Quaternius CC0 |
| 손잡이 | `field.eras`·`field.eraMain`(0.6)·`field.eraFrom`(300) · `folk.on`·`folk.viewR`(110)·`folk.talkR`(12) |
| 남은 것 | 도감 인물·주민 옷도 세 시대로 · 현대 정예 · 적 이름 뒤 시대 표시 |
| 구현(2026-09-24) | 코드분 완료. **실기 확인 대기**(로봇·망자 크기·드론 높이·사람 옷 읽힘) |

### ⑲ saga-godot 106장 이식 — 순서표 (2026-09-25 사용자 지시 "웹판에 변경 사항도 적용")

saga-godot GO 가 원신 기준(그 PLAN 106장 ①~㉝)으로 넓힌 것을 이 판에 옮긴다. **코드 공유 없음** — 규칙·수치·대사를 옮기고 코드는 이 판 방식으로 새로 쓴다.

- **한 세션 = 순서 한 줄.** 착수 전에 그 줄의 9필드(참고·표준·왜·메커니즘·수치·UI·세이브·진단·범위)를 이 절 아래에 쓰고, 끝나면 §0 규칙대로 한 줄로 접는다. 수치의 원본은 saga-godot `PLAN.md` 106장 그 번호 칸(`grep -n "^| ⑭ " saga-godot/PLAN.md`) — 거리만 이 판 걸음(GPS·1.2km 지역) 척도로 고친다.
- **이미 있는 웹 시스템을 넓힌다**(새로 짓지 않는다). 겹치는 셋은 §10 Q8~Q10 — 답이 없으면 권장안으로 간다.
- 자리는 Godot 칸 좌표를 옮기지 않고 **⑮ 땅 열여섯·⑰ 정상·⑩ 탑**에 고정으로 잡는다(§2.3·SAGA-DESIGN §12). 이름·대사는 saga-godot `data/story.gd`(가명)를 그대로 옮겨도 된다.
- 옮기지 않는 것(이미 있음): ①이동→⑰ · ②물·하늘→`water3d`·`sky3d` · ③들판 전투→⑨ · ④모델→`asset3d` · ⑤지역 잇기→⑩⑮ · ⑦원소 적→⑨ 정예 · ⑨지도→⑩ · 폰 발열→`perf.js`.
- saga-godot 에 GO 새 번호(㉞~)가 생기면 이 표 끝에 한 줄 더한다.

| 순서 | Godot 106 | 이 판에 지금 | 할 일 요지 |
|---|---|---|---|
| 1~10 ✅ | ⑭⑮ 원소·반응 · ⑧ 조작 · ⑥⑬⑪ 상자·구슬 · ⑩⑫ 육성 · ⑯⑰ 무기·보패 · ⑱ 요리 · ㉒ 등급 · ⑲ 의뢰 · ⑳㉑ 숨은 터 · ㉓ 들판 보스 | — | 끝 — 아래 접은 줄(순서마다 9필드 원문 해시) |
| 11 ✅ | ㉔㉖ 고유·갈래 스킬 | ⑫ 모양 넷(id 해시) | 모양을 기질(무·지·덕·통) 틀 × 원소로, 고유 다섯(주인공 + 대표 넷) |
| 12 | ㉕ 이야기 1~2장 | ⑬ 지역 사명 사슬 · `talk.js` | 대화 창·금빛 기둥·목록(O)·단계 여섯, 임무 인물 셋 자리는 ⑮ 땅 |
| 13 | ㉗㉘㉙ 3·4장·대화 카메라·따라가기·몸짓 | `talk.js` | 어깨 너머 카메라·글자 흘리기 · 입 모양은 VRM 몸(`animeAvatar`)만, 아니면 손짓만 |
| 14 | ㉚㉜ 5·6장·석등 차례·인물 자리·봉우리·이야기 보스 | ⑰ 정상 여덟 | 둘째 제단 석등 해·달·별, 셋째 제단 = ⑰ 정상 하나, 검은 가면(그림자·2단계 졸개) |
| 15 | ㉛㉝ 이야기 동료·편성 | ⑨ 편성 = 동행 앞 4 | 학자·나그네(도감 밖 id·고유 스킬) · 편성 넣기·빼기·앞 자리로 |
| 16 | ㉞ 7장·제단 지키기·검은 가면 둘째 대결 | ⑨ 무리 · ⑮ 물가 땅 | 넷째 제단 = ⑮ 물가 땅 하나, 지키기(제단 체력·물결 셋·무너지면 처음부터), 금 간 검은 가면(물·밀물 원 넷·2단계 물 방패) |
| 17 | ㉟ 이야기 동료 둘 더 | ⑨ 동행 스킬 | 촌장 누리(풍 치유 — 밀어내며 명단 회복·회복 자리)·사공 버들(수 협동 — 물결 밀어냄·기본 공격 뒤 물 노), 6·7장 보상 |
| 18 | ㊱ 편성 여러 벌 | ⑨ 편성 = 동행 앞 4 | 편성 칸 4(세이브 칸 둘)·칸 바꾸기·싸우는 중 막음 |
| 19 | ㊲ 8장·바위섬·도둑 쫓기·배 | ⑮ 물가 땅 · ⑰ 헤엄 | 다섯째 제단 = 물가 땅 앞 작은 섬(헤엄/배), 도둑 쫓기(걷기로는 못 잡는 속도), 배 타기, 해솔 정체 |
| 20 | ㊳ 9장·구름섬·바람 기둥·먹구름 임금 | ⑰ 활공·정상 | 여섯째 제단 = 하늘 섬(바람 기둥으로 올라 활공), 먹구름 임금(왕관·고리 예고), 이야기 1부 끝 |
| 21 | ㊴ 세계 임무 셋·따라가는 임무 바꾸기 | ⑬ 지역 사명 사슬 | 머리 위 푸른 ! 인물 일곱·곁가지 임무 셋, 목록에서 따라갈 임무 하나만 목표·추적 |
| 22 | ㊵ 활 조준 사격·과녁 잠금 | ⑨ 기본 공격 · 순서 3 상자 | 활 인물 조준 모드(어깨 너머·충전 1.4초 → 원소 화살·급소 치명), 상자 잠금 넷째 "과녁"(셋을 10초 안에), 충전 화살로 먼 석등 켜기 |
| 23 | ㊶ 지도 임무 표식 | 지도·미니맵 ⑩ | 따라가는 ◆(이야기 금·세계 푸른)·안 따라가는 ◇·맡을 수 있는 !, 지도에서 표식 눌러 따라가기·가까운 지점 순간이동 |
| 24 | ㊷ 낚시 | 없음(사가의숲 낚시와 코드 공유 없음) | 물가 땅마다 낚시터·물고기 그림자·미끼 셋(채집 재료)·입질·줄다리기 막대, 물고기 여덟(세 시대), 낚시 조합(작살·냥) |
| 25 | ㊸ 업적 | 도감 수집 보상·④ 사명 | 업적 스물둘(갈래 다섯·단계 1~3), 신호로 세는 것과 상태에서 읽는 것, 달성 알림·화면에서 받기 |
| 26 | ㊹ 탐사 파견 | 없음 | 역참 곁 게시판에서 편성 밖 동행을 탐사지 여섯(땅 셋 × 둘, 세 시대)에 4·8·12·20시간(실제 시각) 보내기, 자리 2~5(모험 등급), 잘 맞는 원소 +25%, 탐사 중엔 편성에 못 넣음·부르면 보상 없음 |
| 27 | ㊺-1 넷째 지역 서리봉 고원(이야기 2부 무대) | ⑮ 땅 열여섯 | 눈 땅 하나를 고정 지역으로(눈밭·얼어붙은 호수·흰 산), 명소 옛 산성 터·기상 관측소·추락한 비행선(세 시대), 무리 넷·상자 다섯·별조각 셋·작은 발견 일곱, 그 땅 안에서만 눈 |
| 28 | ㊺-2 이야기 제10장 · 서리 고개 너머 | 순서 12~20 이야기 틀 | 관측원 하람(현대)·조종 기계 반디(미래) 두 인물, 촌장 → 고개 → 신상 → 호수 무리 → 따라가기 → 비행선 → 산성 터 아홉 단계 |

#### ⑲-11 고유·갈래 스킬 (saga-godot 106 ㉔㉖ · 2026-09-26)
- **참고·표준** 오픈월드 RPG(인물마다 E·Q 가 달라 편성이 전술) · C D F. **왜** ⑫ 는 id 해시 모양이라 인물의 기질과 무관하고 해방은 전원 같다.
- **메커니즘** 새 `js/kits.js`(표·`kitOf` 순수) — 고유 → 갈래(도감 `trait`) → 옛 ⑫. **무용** 돌격(돌진)/검기(기본·강·낙하에 원소 부여) · **통솔** 호령(늦게 떨어지는 탄)/군기(명단 공격+) · **인덕** 방패(명단 보호막)/맹세(받는 피해−) · **지략**·도감 밖 = ⑫ 모양 + 해방 기본. 이름 = 원소 앞말(불꽃·물결·번개·바람·서리·바위·덩굴) + 틀. 원소 덧붙임: 화 해방×1.15 · 수 회복 4%/8% · 뇌 다른 인물 기력 +2/+6 · 풍 스킬 대기 −1.5초 · 빙 스킬×1.15 · 암 보호막 +12% · 초 해방 효과 +3초. **고유 다섯**: 나(불꽃 돌진/불새 깃) · 현책(팔괘진 zone/천기 뇌우 haste) · 해장(일제 포격/학날개 진) · 결사(결사 방진/오천의 맹세) · 노궁(화살비/돌개 화살 vortex).
- **수치**(이 판 척도, 기본 스킬 ×2.2·해방 ×4.5·기력 60): 돌격 5m ×2.6 7초 · 검기 6m ×3.6 + 8초 부여 ×1.15 · 호령 10m 둘 0.6초 2.2m ×2.2 8초 · 군기 7m ×3.0 + 10초 공격 ×1.15 · 방패 3m ×1.6 + 20% 12초(대기 12) · 맹세 6.5m ×2.8 + 10초 받는 피해 ×0.8. 고유는 한 단 위(`kits.js` 표). 손잡이 `field.kits` 0 = 옛 ⑫.
- **UI** 스킬 단추·머리 위 글에 스킬 이름, 인물 상세 무예 줄에 [갈래] 이름·설명. **세이브** 없음. **진단** 배정(고유·갈래·지략)·스물한 이름 다름·틀 여섯·덧붙임·끄면 옛 ⑫. **범위** NEW `kits.js` · MODIFY `field-combat.js`·`ui.js`·`index.html`·`sw.js`·`_test.html`. 밖: 효과 상태 줄·솟구침(updraft)·인물 몸짓.

순서 10 **코드분 완료**(2026-09-26, 새 `js/fieldboss.js` — ⑪ 수호자 = 들판 보스·보상 꽃 원기 40·꽃을 받고 150초 뒤 다시 서기·보스 재료 다섯·승급 ★2~★5 에 2·4·8·12) · 실기 확인 대기 — 9필드 설계 원문은 `git show 2c5b3f0c:saga-web/saga-go/PLAN.md` 의 "⑲-10" 절(§0 규칙대로 접음). 보스 체력 막대·원소 결정·지도 표시는 밖.

순서 9 **코드분 완료**(2026-09-26, 새 `js/domain.js` — ⑮ 땅 열여섯마다 비경 입구(무덤·서당·쇠부리)·갈대 나루 먹구름 제단·들판 전투 파도 둘·단계 I~III·지맥 이상·보상 나무·원기 160·주간 보스 2단계·뇌룡 비늘) · 실기 확인 대기 — 9필드 설계 원문은 `git show b42f88eb:saga-web/saga-go/PLAN.md` 의 "⑲-9" 절(§0 규칙대로 접음). ② 사당 시련은 그대로, 벼락 세 원·지도 입구 표시는 밖.

순서 8 **코드분 완료**(2026-09-26, `js/daily.js` 넓힘 — 하루 넷 = 갈래 넷에서 하나씩·새 후보 들판 적·원소 반응·채집·요리·새벽 4시 갈림·넷 다 하면 역참에서 마무리 보상(인연 매듭), Q10 권장안) · 실기 확인 대기 — 9필드 설계 원문은 `git show bdeafbaa:saga-web/saga-go/PLAN.md` 의 "⑲-8" 절(§0 규칙대로 접음). 상자 의뢰는 disabled, 비경은 순서 9.

순서 7 **코드분 완료**(2026-09-25, 새 `js/adventure.js` — 모험 등급 = 플레이어 Lv·세계 등급 0~8 이 들판 적·전리품 배율·등급 보상·한 단계 낮추기, Q9 권장안) · 실기 확인 대기 — 9필드 설계 원문은 `git show d14f5512:saga-web/saga-go/PLAN.md` 의 "⑲-7" 절(§0 규칙대로 접음). 일일 의뢰는 순서 8, 비경 단계 잠금은 순서 9.

순서 6 **코드분 완료**(2026-09-25, 새 `js/cooking.js` — 칸마다 고정 채집 무리·역참 곁 솥·요리 여덟 × 품질 셋·회복/버프·승급 특산물) · 실기 확인 대기 — 9필드 설계 원문은 `git show 7aa70230:saga-web/saga-go/PLAN.md` 의 "⑲-6" 절(§0 규칙대로 접음). 지도 위 채집물 표시·요리 비법 해금은 밖.

순서 5 **코드분 완료**(2026-09-25, 새 `js/weapon.js`·`js/artifact.js` — 무기 종류 다섯·기본 공격 모양·강화/돌파/재련·치명타·성유물 부위 다섯·세트 다섯) · 실기 확인 대기 — 9필드 설계 원문은 `git show cf77dde2:saga-web/saga-go/PLAN.md` 의 "⑲-5" 절(§0 규칙대로 접음). 손에 든 무기 모델·성유물 비경은 순서 9.

순서 4 **코드분 완료**(2026-09-25, 새 `js/talent.js` — 무예 단계 셋·운명의 자리 여섯, Q8 권장안) · 실기 확인 대기 — 9필드 설계 원문은 `git show 08caae39:saga-web/saga-go/PLAN.md` 의 "⑲-4" 절(§0 규칙대로 접음). 무기·성유물은 순서 5, 주간 보스 재료는 순서 9.

순서 3 **코드분 완료**(2026-09-25, 새 `js/treasure.js` — 상자·석등·무리 잠금·수집 구슬·신상 봉헌·원소 시야) · 실기 확인 대기 — 9필드 설계 원문은 `git show dfa0fdc6:saga-web/saga-go/PLAN.md` 의 "⑲-3" 절(§0 규칙대로 접음). 2D 화면 그림·지도 탐험도는 밖.

순서 2 **코드분 완료**(2026-09-25, `field-combat.js` 강공격·낙하 · `landform.js` 내리꽂기) · 실기 확인 대기 — 9필드 설계 원문은 `git show fc3b121d:saga-web/saga-go/PLAN.md` 의 "⑲-2" 절(§0 규칙대로 접음). 쇄빙은 강공격·낙하만.

순서 1 **코드분 완료**(2026-09-25, `field-combat.js`) · 실기 확인 대기 — 9필드 설계 원문은 `git show 9de003f4:saga-web/saga-go/PLAN.md` 의 "⑲-1" 절(§0 규칙대로 접음). 강공격·낙하는 순서 2, 공명·새 원소 스킬 모양은 순서 11, 원소 결정 보상은 순서 5 로 넘겼다.

### ⑳ 원작 겹침 정리 — 이름·수치를 이 판 것으로 (2026-09-26 사용자 선택 "겹침 정리 먼저")

- **왜** — ⑲ 가 옮긴 체계의 **표시 이름과 수치 표가 원작과 거의 같다**(공개 저장소·상용화 검토). 체계(원소 반응·장신구 다섯 칸·무기 종류·단계 올리기)는 장르 문법이라 남기고, **이름·수치·칸 구성**을 이 판 것으로 바꾼다. ⑲ 순서 11~ 은 이 표를 따른다(원본 수치는 saga-godot 칸이 아니라 이 절).
- **세이브** — 키·id(`save.artifacts`·`talentMat`·세트 id `gladiator` 등·부위 `flower` 등·무기 id·반응 키)는 그대로. 옛 세이브는 읽는 쪽이 새 표로 다시 센다(장신구 부옵션은 굴린 값 그대로, 주옵션은 새 곡선). SAVE_VERSION 그대로. 깨달음은 옛 6 → 5 로 잘린다.
- **이름**(표시 글자만): 성유물 → **보패**(부위 꽃·깃·해시계·술잔·관 → 패옥·비녀·가락지·향낭·관모) · 비경 → **숨은 터** · 지맥 이상 → **터 기운** · 지맥의 열매 → 터의 열매 · 운명의 자리 → **깨달음** · 원소 시야 → **기척 보기** · 모험 등급 → **여정 등급** · 세계 등급 → **천하 등급** · 원소 폭발 → **원소 해방** · 신상 봉헌·등급 → 탑 봉헌·탑 등급 · 무기 돌파·재련 → **벼림·울림** · 들판 보스 "돌파 재료" → 승급 재료 · 무기 종류 한손검·양손검·장병기·법구·활 → **칼·대도·창·서책·활** · 상자 평범·정교·진귀·화려 → **나무·무늬·옻칠·금박** · 세트 떠돌이 무사·불꽃 무녀·바람 나그네·절연 깃발·물결 성자 → 떠돌이 무사·대장간 불씨·솔바람 피리·봉화 깃발·나루 안개 · 반응 증발·융해·과부하·감전·빙결·초전도·확산·결정·개화·연소·촉진·활성·발산·쇄빙 → **물안개·녹임·터짐·물벼락·얼어붙음·서리번개·회오리·굳힘·꽃피움·들불·싹틈·번개싹·덩굴뻗음·깨뜨림**. 원소 한 글자(화수뇌풍빙암초)는 오행식 한자라 남긴다.
- **수치**: 원기 상한 160·8분 → **120·10분** · 숨은 터 20 → **15** · 보상 꽃 40 → **30** · 주간 보스 60(주 처음 셋 30) → **45(주 처음 둘 25)** · 보패 주옵션 ★5 +0/+최대 `hp 80/480 · atk 20/120 · %·원소 6%/42% · 방어%·물리 8%/52% · 기력 7%/46% · 치명 4%/28% · 치명 피해 8%/56%` · 부옵션 한 번 `hp 30 · atk 8 · def 5 · %5% · 방어% 6.5% · 기력 5.5% · 치명 3.3% · 치명 피해 6.6%`, 굴림 셋 `0.75·0.85·1.0` · 보패 상한 ★4 12·★5 15, **3단마다** 부옵션 · 칸 구성 가락지 = 공격%·치명·치명 피해, 향낭 = 원소·물리·체력%·방어%, 관모 = 공격%·체력%·방어%·기력 · 세트 2/4 `공격 +15%/근접 기본 +30% · 화 +12%/화 반응 +35% · 풍 +12%/회오리 +50% · 기력 +18%/해방 +20% · 수 +12%/스킬 +25%` · 무기 기본 공격 ★1 23 → **20**, ★3 39·40 → **34·35**, ★4 42·44 → **38·40**, 부옵션 7.7·10.2·8.5·9·6.7·4% → **7·9·8·8·6·3.5%** · 무예 배율 `1.0 +0.08 씩(8단부터 +0.1)` = 1.00~2.10 · 단계 상한 승급 ★0~5 = **3·4·5·7·9·10** · 무예 비용 금 `100·160·220·300·400·1000·2200·4000·6500`, 책 `쪽지 2 · 교본 2·3·5·8 · 비전 3·5·10·14` · 깨달음 **다섯**: ① 스킬 대기 -15% ② 반응 피해 +20% ③ 스킬·해방 무예 +2 ④ 체력 +15% ⑤ 해방 뒤 8초 공격 +20%.
- **안 바꾸는 것** — 원소 일곱과 반응 규칙·배율(장르 문법, 1.5 같은 흔한 값), 치명 5%/50%, 기력 60·스태미나 100, 여정 등급 보상·천하 등급 곡선(이미 이 판 것), 문서 속 원작 이름(설계 근거).
- **진단** — 이름·수치를 읽는 옛 시험은 새 표로 고친다. 새 시험 하나: "화면 글자에 원작 용어가 없다"(바꾼 옛 이름 목록을 `ui`·카드 글에서 찾는다).
- **범위** — 사가고만. saga-godot GO 106(같은 표)은 그 트랙 세션이 정한다. `pk_*` id·파일명 `diablo.css`·`maple.css` 는 따로 결정.
- **상태** — 코드분 완료(2026-09-26, 사가고 js 15개·`_test.html`) · 실기 확인 대기. 이 절은 접지 않는다 — ⑲ 순서 11~ 의 수치 정본이다. 같은 길에 진단 틀이 `"실패 …"` 글을 PASS 로 찍던 것을 FAIL 로 바꿨고(숨어 있던 ⑲-10 둘: 수호자 다시 서기가 한 박자 늦던 순서 버그는 `field-combat.js` 에서, 처치 경험을 셈에 안 넣던 시험 결함은 시험에서 고침).

## 6. 그래픽·에셋

SAGA-DESIGN §6.1(웹 저비용 통일) 적용분. 새 렌더러 없음, 재질·후처리만.

| 항목 | 지금 | 할 것 |
|---|---|---|
| 톤매핑 | `post3d.js` 자체 곡선(**ACES 는 밤을 망가뜨려 물러섰다** — 이 결정 유지) · 블룸 0.9+night·0.7+gold·0.3 · 천후별 보정 | (보류) SAGA-DESIGN 기본값(ACES) 대신 이 곡선 유지. 노출·비네트(CSS `#vignette`) 수치만 §6.0 에 맞춰 문서화 |
| SSAO | `ssao3d.js` 등급별 표본, LOW 끔 | 유지 |
| 재질 | **완료(2026-09-17)** — `js/toon3d.js` 신설. `actor3d.js`(도형 배우)·`asset3d.js`(GLB 배우) 모두 `MeshToonMaterial` 3단 램프(`world3d.toon`, 기본 켬) + 뒤집힌 헐 외곽선(`world3d.outline`, 기본 켬 — 스킨 메시는 스키닝 뒤 법선으로 미는 셰이더라 자세가 바뀌어도 안 어긋난다). 소품(`prop3d.js`)은 그대로 Lambert, 팔레트 스냅만(아래) | — |
| 팔레트 | **완료(2026-09-17)** — `prop3d.js`에 24색 배열 + `snapPalette()`(RGB 최근접). 소품은 원래 면마다 단색이라 캔버스 스냅 없이 `lambertOf()`에서 바로 스냅(`world3d.palette`, 기본 켬) | 권역 27 별 명·암 2단 변주는 미착수(범위 밖으로 미룸) |
| 지형 | `textures/land` WebP 6장 · `relief3d` 높낮이 · `LAND_PAINT`(캔버스로 미리 구운 평판 텍스처, 실제 지도 land-use 반영) | (보류) 트라이플레이너 3타일 + 노이즈 블렌드 — 지금 지형 파이프라인이 이미 크고 잘 도는데(land-use 기반) 셰이더를 갈아 끼우면 회귀 위험이 커서 이번엔 손 안 댐. 실기 확인으로 "허접함"이 실제로 느껴지는지부터 본 뒤 순서를 다시 잡는다 |
| 초목·소품 | `prop3d` 인스턴싱(나무만 그림자) | 유지. 풀 바람 셰이더(정점 흔들림, 이미 잎 흔들림 있음) |
| 그림자 | **이미 있음** — `world3d.js` `groundShadow()`가 모든 배우(빌보드·메시 가리지 않고)에 접지 blob 을 매 프레임 따라 찍는다. PLAN 표가 옛 상태였다 | — |
| 안개·하늘 | **이미 같음** — `lightingAt().bg` 한 값을 `scene.background`·`renderer.clearColor`·`scene.fog.color` 셋에 매 프레임 그대로 물린다(`syncLight()`). PLAN 표가 옛 상태였다 | — |
| 폰 | **완료(2026-09-17)** — `world3d.js` 픽셀 비율 상한 2→1.5 | — |

**사가고 팔레트 24색(적용됨, `prop3d.js` `PALETTE`)**
기본 8: 잔디 `#7FA650` · 흙길 `#B58B5A` · 기와 `#4A4E57` · 회벽 `#E8DCC3` · 물 `#4F86A8` · 소나무 `#3F6B45` · 바위 `#8A8F8B` · 노을 `#E39B5A`
명 8: `#A3C46F` `#D0A874` `#6C717B` `#F5EEDD` `#7FB0CC` `#5E8C62` `#B0B4AF` `#F2B98A`
암 8: `#5C7D38` `#8C6A42` `#2F3238` `#C9B99A` `#355F7A` `#26452C` `#5F635F` `#B56F3C`
계절(`season.js`)은 이 24색을 색상 회전으로 덮는다(가을 +25°, 겨울 채도 -40%) — 그대로 둔다.

**스타일 충돌 — §10-Q1 확정(2026-09-17, 사용자 결정): 기본 툰, 실사는 손잡이 뒤로.**
`asset3d.js`의 MPFB 사진측량 실사 인체 20벌(`mpfb_male`·`mpfb_female`·`mpfb_v3~v23`)과 `vitruvian_v1`(예시로 조운·강감찬·잔다르크·나폴레옹·프리드리히가 이 표본에 해시로 걸렸었다) 전부에 `realistic: true`를 달고, 인물 뽑기(`heroPool()`)가 `world3d.realisticPeople`(기본 0=꺼짐)이 꺼져 있으면 그 줄을 걸러 QRPG 저폴리 6종만 남긴다. 즉 **기본 인물 표는 이제 QRPG 6종뿐**이다 — 실사를 다시 보려면 어드민에서 `world3d.realisticPeople=1`. `/realistic/` 건물(역참·성채 t3)은 이 갈래와 무관(원래도 `delam()`이 안 건드리는 별도 경로).

**§7.2 변형 배가로 이 판이 얻는 것**: 건물 13종 × 권역 팔레트 = 마을 결 27가지 · 나무 3종×계절 4(이미) + 권역 명암 · 비석(⑤)·사당 입구(②)·봉수대(①) 는 `procgen.py`/`kitbash.py` 로 씨앗 생성(`assets/generated/`) · 타격음 3종 라운드로빈은 `sfxgen.py`.

## 7. 버그·안정화

SAGA-DESIGN §8 적용 — **코드로 끝나는 항목은 완료(2026-09-17)**:
- 오류 링버퍼(50건, `js/errlog.js`) → `_admin.html` "오류" 탭에서 보기·복사·비우기. `errlog.on` 손잡이.
- 세이브 `v` 필드 마이그레이션 체인(`core.js` `migrate()`/`MIGRATIONS`) — 버전이 안 맞아도 세이브를 안 버림(예전 `parsed.v !== 1` 하드 체크가 aa4b8b8 류 지뢰였다). `_test.html`에 순수 함수 검사 항목 추가. `_admin.html` QA 프리셋에 "옛/이상한 버전 세이브 시뮬레이션" 추가.
- `tools/precheck.sh`(루트, 다섯 판 공통) — `node -c` 전 파일은 원래 있었고, 이번에 "sw.js VERSION 안 올림" WARN 추가.
- 후처리 실패 시 자동 끔 — `post3d.js` `draw()`가 내부(`drawInner`)를 try/catch 로 감싸, 실패하면 `ready=false`로 영구히 끄고 `errlog`에 기록한 뒤 바로 그리는 길로 돌려보낸다.

**실기 확인이 필요해 남은 것**: QA 프리셋에 제보 상황을 더 넣는 일(제보가 들어와야 무엇을 만들지 정해진다 — 지금은 위 마이그레이션 프리셋 하나만 새로 더함).

**닫아야 할 실기 확인 대기(HANDOFF·README 에서 모음)**
1. 폰 실기 fps 미측정 — 버거우면 `world3d.density` 0.5 → `world3d.mesh` 0
2. 이동 속도 8m/s 조작감 · 도적전 손맛(속공·필살·회피) 사람 미검증 · 회피 거저(§5-③가 고침)
3. (2026-09-17 부로 기본 인물 표에서 빠짐 — `world3d.realisticPeople=1`일 때만 해당) MPFB 실사 실화면 미확인 · 캐릭시안/노년 아시아 눈가 빨간 문제(알파맵 인코딩 의심)
4. "모바일에서 캐릭터 녹색" 실기 전용 문제 — `index.html?debug=1` GLB 오버레이·`_glbcheck.html` 로 재현
5. 잔디·바닥 텍스처 · 물 반사(겨울 물색) · 사당(`wayside_shrine.glb`) · 역참 배율(`stationScale` 1.73) · 성채 t3 배율 실기 확인
6. 적도 균형 — 시간당 0.8~1.5회·9전 9승(자동) → `rogue.rate`/두목 `hpMul` 조정 후 `_autoprobe.html?rogue=1` 재측정
7. 권역 인물(한·일·중) 지도 위 스폰 체감 미확인
8. 세로 시야각(`fovFor`)·상단 `⋯` 서랍 커밋 여부 확인
9. APK 재굽기(`cd mobile && npm run apk`, v1.7 내용) · 사관 응답 실청취(`ANTHROPIC_API_KEY`, 8790)
10. **완료(2026-09-17)** — `tools/precheck.sh`(루트, 다섯 판 공통)에 "js/ 고치고 sw.js 그대로면 WARN" 추가
11. (신규 2026-09-17) §6 그래픽 통일 실기 확인 — 툰 재질·외곽선(스킨 메시 자세 바뀔 때도 안 어긋나는지 특히), QRPG 6종만 남은 인물 표가 실제로 덜 허접해 보이는지, 소품 팔레트 스냅 후 화면이 튀지 않는지, 픽셀비율 1.5 로 낮아진 폰 화면이 흐려 보이지 않는지

## 8. 로드맵

| Phase | 내용 | 상태 |
|---|---|---|
| (완료) 3D 전환 1~13 + 계절·후처리·물·하늘·SSAO·미니맵·오디오·권역 3국 + 서역(넷째, 2026-09-23) | README "어디까지 왔나" | 완료 |
| **0 안정화** | 오류 링버퍼·마이그레이션 체인·precheck 경고·후처리 자동 끔(코드분 완료, 2026-09-17) · §7 목록 1~9(대부분 실기 확인) · **사람이 손으로 한 세션 플레이 기록** | 코드분 완료, 실기 확인 대기 |
| 1 세션 구조·손맛 | ④ 일과판+마무리 카드(코드분 완료, 2026-09-17) · ③ 75초 토벌·타격음 3종(코드분 완료, 2026-09-17) | 코드분 완료, 실기 확인 대기 |
| 2 목적지·도전·비용 | ① 봉수대 · ② 사당 시련 · ⑧ 패배 비용 | ① 코드분 완료(2026-09-19, 실기·헤드리스 확인 대기, 3D 탑 모델은 자산 없어 보류) · ② 코드분 완료(2026-09-21, 실기 확인 대기) · ⑧ 코드분 완료(2026-09-21, 실기 확인 대기) |
| 3 수집·선택 | ⑤ 비석 순례 · ⑦ 승급 3택 | ⑤·⑦ 코드분 완료(2026-09-21, 실기 확인 대기 — ⑤ 는 실제 GPS 로 걸어야 한다) |
| 4 관계 | ⑥ 인연 | 코드분 완료(2026-09-21, 실기 확인 대기) |
| 5 그래픽 통일 | 툰 램프·외곽선·팔레트 스냅·§10-Q1 결정 완료(2026-09-17) · 트라이플레이너 지형만 보류(§6) | 대부분 완료 |
| 6 퓨전 콘텐츠 | 현대·미래 인물 genchar 확장(사가블로 검증 뒤, `SAGA-HANDOFF.md` "창작 방향") | 대기 |
| **7 원신식** | ⑨ 들판 전투 → ⑩ 지역 지도 (2026-09-24 사용자 선택) | ⑨·⑩ 코드분 완료(2026-09-24, 실기 확인 대기) |
| 8 원신식 다음(후보) | ⑪ 지역 수호자(**코드분 완료 2026-09-24**, 실기 확인 대기 — §5 ⑪) · ⑫ 인물마다 다른 원소 스킬 모양(**코드분 완료 2026-09-24**, 실기 확인 대기 — §5 ⑫) · ⑬ 지역 사명 사슬(**코드분 완료 2026-09-24**, 실기 확인 대기 — §5 ⑬) · 지도 위 동행 모션(**코드분 완료 2026-09-24**, 실기 확인 대기 — §5 ⑭) | 다음 세션 — ⑨⑩ 실기 확인 결과를 먼저 반영 |
| 9 고정 특색 지역 | ⑮ 이름 있는 땅 열여섯(2026-09-24 사용자 지시, **코드분 완료**, 실기 확인 대기) · 전체 지도 땅 이름(완료) · ⑰ 지형 설계·오르기·점프·활공·정상 순간이동·다리·폭포·땅 전용 퓨전 소품(**코드분 완료**, 실기 확인 대기) · ⑱ 사람·적 세 시대(**코드분 완료**, 실기 확인 대기) · 다음: 높은 곳 눈빛 땅색 · 도감 인물 옷 세 시대 | ⑮ 진단 3 · ⑰ 진단 8 · ⑱ 진단 2 |
| **10 Godot 106 이식** | §5 ⑲ 순서 1~15(2026-09-25 사용자 지시) — 한 세션 한 줄, 착수 전에 9필드 | 순서 1~10 끝(9de003f4·fc3b121d·dfa0fdc6·08caae39·cf77dde2·7aa70230·d14f5512·bdeafbaa·b42f88eb·2c5b3f0c) — §5 ⑳ 원작 겹침 정리 끝(2026-09-26) — **다음 = 순서 11**(고유·갈래 스킬, 이름·수치는 ⑳ 을 따른다) |

각 Phase 끝: 변경 파일 · 테스트 결과(RESULT 3회 동일) · 남은 문제만 보고. 커밋은 Phase 단위, `git commit -F <메시지> -- <경로>`.

## 9. 검증

```
구문     node -c js/<파일>.js
진단     chrome --headless=new --disable-gpu --virtual-time-budget=45000 --dump-dom http://127.0.0.1:8791/_test.html | grep -o "RESULT [0-9/]*"
어드민   _admin.html#selftest  (해시 트리거 — 이 판만 쿼리가 아니다)  → RESULT n/n
자동관찰 _autoprobe.html?sim=600&party=3  · ?rogue=1 (적도 빈도)
```
- 씨앗 mulberry32(20260824) 고정 · `weather.force('clear')` · `rogue.force(false)` · **3회 돌려 한 줄도 안 달라야** 한다. 로그 시각과 상단 지갑 HUD 한 줄은 렌더 타이밍으로 흔들린다.
- `--dump-dom` 에서는 rAF 가 거의 안 돈다 → 메인 루프가 굴러야 보이는 것은 `_test.html` 처럼 `update(dt)` 를 직접 돌리거나 `_autoprobe.html` 로 본다.
- 화면 스크린샷·서버 띄우기는 루트 CLAUDE.md 규칙(사용자 요청 시만, 띄웠으면 `taskkill //F //IM chrome.exe`).
- 3D 값 수정 → `sw.js` VERSION ↑. 스프라이트는 `_gallery.html`·`_probe.html` 로 눈으로만 판정.

## 10. 열린 질문(사용자 결정)

- **Q1 인물 스타일 — 확정(2026-09-17)**: 기본 툰(QRPG 6종), MPFB 실사는 `world3d.realisticPeople` 손잡이 뒤로. §6 참고.
- **Q2 회피 필수화(③) — 확정(2026-09-17)**: 권장안대로 토벌만 적용, 야생 조우·성채 수비대는 그대로.
- **Q3 일과(④) vs 사명(quest) — 확정(2026-09-17)**: 나란히 둔다(권장안 그대로) — 사명은 역참에서 받는 원작 리서치, 일과는 매일 자동. `quest.js`가 `daily.js`로 진행 신호를 넘긴다.
- **Q4 패배 비용(⑧) — 확정(2026-09-18)**: 15% 상한 300 으로 시작(권장안 채택), 어드민 손잡이로 0 까지 내릴 수 있게 한다.
- **Q5 비석 규모(⑤)**: 격자 200m·12% 로 일단 시작하되, 사용자의 실제 생활 반경에서 체감되는 밀도인지는 실기 측정이 있어야 판단 가능 — 보류.
- **Q6 온라인 사관**: 서버 비용·키 관리를 계속 감당할지는 사용자의 지속 비용 판단 사항이라 보류. 기능은 오프라인과 독립이라 언제든 뺄 수 있다는 점만 확인.
- **Q7 퓨전 인물 시기 — 확정(2026-09-18)**: 사가블로 검증 뒤 진행한다. 루트 `CLAUDE.md`에 이미 "사가블로가 퓨전 선행 판, 다른 네 판보다 먼저 실험"이라고 박혀 있어 이 판이 GPS 권역과 맞아떨어져도 순서를 바꾸지 않는다.
- **Q8 육성 합치기(§5 ⑲ 4)** — 권장안으로 감(2026-09-25, 08caae39): 권장 — `growth.js` 연성을 레벨·경험, 승화를 돌파로 **이름·재료만 맞추고** 새 체계를 따로 안 만든다(⑦ 승급 3택은 그대로). 답이 없으면 권장안.
- **Q9 등급(§5 ⑲ 7)** — 권장안으로 감(2026-09-25, d14f5512): 권장 — 거리 등급(1+거리/900m)은 걷는 게임의 "멀리 갈수록 세다"라 그대로 두고, 모험 등급은 경험에서 파생·세계 등급은 거리 등급에 곱한다. 답이 없으면 권장안.
- **Q10 의뢰(§5 ⑲ 8)** — 권장안으로 감(2026-09-26, bdeafbaa): 권장 — 새 판을 만들지 말고 ④ 일과판 풀에 반응·요리·상자 종류를 더하고 끝 보상에 인연 매듭. 답이 없으면 권장안.

## 부록 A. 옛 절 번호 대응(코드·README 주석용)

코드와 README 는 2026-08 판(3D 전환 PHASE 1~13 계획서) 절 번호를 가리킨다. 그 판은 git 이력에만 있다. 자주 나오는 것:

| 옛 번호 | 뜻 | 새 자리 |
|---|---|---|
| 12절 발견 · 13절 도감 완성률 | 발견은 남는다 | §2.3 · `codex.js` |
| 14절 땅의 높낮이 | 평면 금지 | §2.3 · `relief3d.js` |
| 23·24절 전투 | 탐험 중 전투·생태계 | §2.3 · `battle3d.js` |
| 25-1절 오버월드 M키 | 전체지도(발자취 40m·400점·위경도) | §3 · `overworld.js` |
| 27절 벽 충돌 | 카메라·충돌 | §2.4 · `world.js` |
| 28절 우선순위 1 안정성·2 FPS·3 로딩·4 그래픽 | 성능 우선순위 | §2.4 · `perf.js` |
| 37절 계절(MVP 이후) | 계절 | §3 · `season.js` |
| 44절 폭포 · 47절 주민 열/미니맵 · 48절 숨은 자리 | 디테일·NPC·미니맵·발견 | §2.3 · `prop3d.js` `npc.js` `minimap.js` |
| 부록 A 그래픽 보강 | 후처리·물·하늘·SSAO | §6 |

## 부록 B. 에셋 우선 목록(원문 유지)

자연: 큰 나무·작은 나무·소나무·대나무·풀·꽃·바위·큰 바위·절벽·흙더미·버섯·나뭇가지·낙엽·연못·폭포·강·다리 / 건축: 작은 집·큰 집·농가·창고·마을 입구·성문·성벽·정자·사당·시장·상점·여관·대장간·훈련장·서당 / 생활: 나무 수레·항아리·상자·나무통·울타리·횃불·깃발·천막·우물·장독대·표지판·벤치·짐꾸러미 / 캐릭터: 플레이어·NPC·상인·농부·병사·무사·노인·어린이·여행자(애니 idle·walk·run·attack·hit·death·interact) / 동물: 말·소·닭·개·고양이·사슴·토끼·멧돼지·새 / 디테일: 바람 나무·물 반사·풀 흔들림·새·동물 이동·NPC 이동·연기·불꽃·먼지·낙엽·폭포·물결·횃불·구름. 폴더: `assets/models/{characters,enemies,npcs,buildings,nature,terrain,props,animals,vehicles,effects}` `textures/` `icons/` `audio/`(실제 폴더는 `ASSET_CATALOG.md` 기준).
