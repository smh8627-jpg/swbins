# 사가고 인계 메모 (saga-go)

> **시리즈 전체 인계는 `../SAGA-HANDOFF.md` 다.** 다섯 게임 표·사용자 지시 이력·
> 절대 바꾸면 안 되는 것·밟아 본 함정·다음 할 일이 거기 있다. **새 세션은 그 파일부터.**
> 이 파일은 **사가고 한 판**에 대한 것만 남긴다.

## 2026-09-10 (더더 이어서) — "바닥이 아직도 자연스럽지 않다" → 2D 패턴 방식 전환, 3D 마을 배치 엇갈림

바로 위 절(밤낮·별·번개)을 끝내고 도감 자가진단까지 추가한 뒤, 사용자가
"사가고 아직도 바닥이 자연스럽지 않아서"로 재지적. 화면을 물어보니(2D·3D
"둘다지") 두 자리 다 손봤다.

**2D 폴백(`world.js` `drawFallback`)** — 바로 오늘 낮에 넣은 첫 버전(칸마다
같은 사진을 40px 캔버스에 구워 그대로 욱여넣기)이 **원인 중 하나였다**:
48px 칸마다 독립된 도장을 찍는 꼴이라 오히려 색칠보다 더 규칙적으로 반복돼
보였다. `world3d.js`의 `landPattern()`과 같은 원리(`CanvasPattern`+
`setTransform`)로 바꿔 사진을 **세계 좌표에 붙박아 이어 반복**시킨다 —
옆 칸과 이어지므로 칸 경계 이음매가 없다. 변형 셋 중 첫 번째만 쓴다(섞으면
그 경계에서 다시 이음매가 생긴다).

**3D 마을 배치(`world3d.js` `propPlan('town', …)`)** — 대지(lot) 줄·칸
구조(2026-09-04)는 그대로 두고(허물면 `houseRects` 충돌 판정을 다시
맞춰야 한다), 사용자가 직접 제안한 "각을 랜덤으로"·"지도 건물 배치처럼"을
반영해 **줄마다 엇갈리게**(벽돌쌓기 결, 방향은 칸 해시) 밀고 각도 흔들림
폭을 0.5→0.7로 넓혔다. `houseRects`는 `propPlan`을 그대로 다시 불러 값을
내므로 따로 손댈 곳이 없다(순수 함수 — 자리가 바뀌면 판정도 저절로 따라옴).

건물 형태 자체를 삼각형/곡면으로 바꾸는 것(사용자가 처음 던진 아이디어)은
**안 했다** — 지금 소품은 절차적 box/roof 조합이라 형태를 바꾸려면 지오메트리
자체를 다시 설계해야 하는 큰 작업이고, 배치(줄맞음)가 "격자" 인상의 더 큰
원인으로 보여 그것부터 먼저 손봤다. 그래도 여전히 격자로 보이면 다음
후보다.

**검증**: `node -c` 둘 다 통과. `_test.html` **469/469 3회 동일**
(`propPlan('town',…)` 카운트·타입 단언들은 좌표값을 안 보므로 안 깨짐,
결정성 테스트(`p1===p2` JSON 비교)도 그대로 통과 — 해시 기반이라 순수
함수 성질을 안 잃었다). `sw.js` `go-v5.27.1`. **실기기 확인 전이다.**

## 2026-09-11 — "사가고 이어 해" → 같은 트리에서 동시에 돌던 다른 세션이
도감 완성률 회귀 시험을 이미 커밋해 둔 것을 rebase로 받음

지난 세션 큐의 "① 도감 완성률 자가진단 추가"를 이어서 하려고 같은 시험을
짰는데, 커밋·푸시 직전 `git pull --rebase`에서 **다른 세션이 이미 그 항목을
처리해 먼저 푸시해 둔 것**(커밋 `b73b1a7`, 위 09-10 절 두 개도 같이 따라옴)이
드러났다 — 인물 3·펫 1을 채워 `dex().seen===4`(버그였다면 2 고정)를 확인하는
시험과, 화면에 실제 노출되는 `stats().by.dex` 형식까지 같이 재는 시험 둘로
내가 짠 것보다 더 꼼꼼했다. 내 쪽 시험은 버리고 저쪽을 그대로 받았다
(중복 시험을 남기지 않음). 새로 바뀐 코드는 없다 — 인계 문서 동기화만.

**다음 세션**: 큐 ②(퓨전 방향 구체화, 사용자와 범위부터)·③(건물 종류
다양화)이 그대로 남아 있다.

## 2026-09-10 (더 이어서) — 심층 감사 + 퓨전 방향 확정 + 밤낮 재개·별·번개

**① 심층 감사(fork 에이전트)** — "찾다보면 더 나와"로 재요청받아 PLAN.md
61절 체크리스트·TODO 주석·자가진단 커버리지를 대조. 확신 높은 것 하나:
**도감 완성률(`js/codex.js` `dex()`) 이 자가진단에 하나도 없다** — 2026-09-09에
"항상 2/104로 뜨던" 실제 버그를 고쳤는데(위 09-09 절), 그 회귀를 잡아 줄
테스트가 없다. 다음에 손볼 후보 1순위. 그 다음은 건물 종류가 얇다는 것
(집 둘·탑 넷 정도뿐) — 오늘 "에셋을 더 찾아서" 흐름의 연장으로 볼 수 있다.
둘 다 아직 **손 안 댐**.

**② 퓨전 방향 확정** — "포켓몬고를 모방 할 필요 없어 퓨전이야 미래 현대
과거 합친걸로 배경을 해도 됨." 사가의숲에 먼저 확정된 것과 같은 방향
전환([[saga_forest_fusion_vision]] 메모, 이쪽은 [[saga_go_fusion_vision]]으로
따로 남김). **아직 구체적으로 뭘 바꿀지는 안 정했다** — 방향만 열렸다,
착수 전에 범위부터 사용자와 맞출 것.

**③ 밤낮 재개 + 별·번개** — "지도도 더 자연스럽게 … 별도 보이고 …
천둥번개도 보이고 … 밤이라도 밝게 해 잘보이게." `world3d.dayNight` 기본을
0→1로 되돌렸다(2026-08-30에 실기기 어둠 제보로 껐던 것 — 밝기는 그때
값 그대로, 사이클만 복원, 사용자가 명시적으로 확인). `sky3d.js`에 별
(맑은/바람 부는 밤에만, `light.lamp`로 밝기 연동)과 번개(비 오는 날
6~18초 주기, `duel.js`의 기존 `.flash` 애니메이션 재사용, 판정 없음, 소리는
CC0 음원이 없어 이벤트만 열어 둠 — `sky:thunder`, 듣는 쪽 없음) 신설.
`_test.html`도 기본값 반전에 맞춰 낡은 단언 하나를 고치고 새 순수함수
테스트 넷을 보탰다. 자가진단 467/467(3회 동일). `sw.js` `go-v5.27.0`.
커밋 `4ce9183`, 푸시 완료. **실기기 확인 전** — 밤 화면·별·번개 다 안 봤다.

**다음 세션**: ① 위 감사에서 나온 도감 자가진단 추가(작고 확신 높음)
② 퓨전 방향 구체화(사용자와 범위부터) ③ 건물 종류 다양화. 우선순위는
사용자에게 다시 물어도 된다.

## 2026-09-10 (이어서) — "지도도 더 자연스럽게, 에셋을 더 찾아서" → 2D 폴백 지형도 실제 사진으로, 허수아비는 CC0 없어 보류

바로 아래 절(2D 캐릭터 에셋화)에 이어 사용자가 "지도도 더 자연스럽게 하고
에셋을 더 찾아서"로 요청. AskUserQuestion으로 범위를 확인하니 **3D·2D
둘 다**였다.

**3D**: 남은 코드 그림 자리(허수아비)를 CC0로 채울 수 있는지 실제로
웹검색(poly.pizza·opengameart.org)까지 해 봤지만 **Poly by Google
`Scarecrow`·OpenGameArt `Scarecrow Character` 둘 다 CC-BY뿐**이었다
(Quaternius CC0 미러 1545개에 없다던 기존 기록과 일치). 이 저장소는
2026-09-07에 일부러 CC-BY 자산(까치)을 CC0로 바꿔 "CC-BY 자산 없음"
상태를 만들어 둔 전례가 있어, 허수아비 하나 때문에 그 상태를 깨지 않기로
하고 **코드 그림 그대로 둔다**(보류, 다음에 CC0 소스가 새로 나오면 재검토).

**2D**: `js/world.js`의 `drawFallback()`이 지형 종류마다 단색 사각형만
채우던 것을, **3D가 이미 쓰던 ambientCG CC0 땅 사진 18장**(`assets/
textures/land/`)을 재사용해 실제 질감으로 바꿨다 — 새 에셋을 안 받고
이미 있는 것만 옮겼다. 원본(1024px)을 매 프레임 그대로 그리면 무거울 수
있어(`sprite.js` `human()` 교체 때의 트레이드오프와 같은 걱정) **40px로
한 번만 구운 작은 캔버스**를 소스로 써서 옛 `fillRect(색)`과 비용을
비슷하게 유지했다. 48칸 해시로 종류마다 셋 중 하나를 고정 배정한다(3D의
`variantFor`와 같은 결 — 다만 반복 주기는 48m 단위라 3D(12m)보다 성기다).
`water`는 3D처럼 안 받았다(옅은 색 그대로, 실제 물결이 없는 자리라 색으로
충분).

**검증**: `node -c js/world.js` 통과. 헤드리스 `_test.html` **463/463
3회 동일**(1·2회는 정상, 3회째는 크롬이 원인 불명으로 DOM을 못 덤프해
재시도 — 이번 세션에 두 번째로 겪은 동일 현상, 일시적으로 보이나 다음
세션도 만나면 눈여겨볼 것). `sw.js` VERSION → `go-v5.26.2`. 커밋
`7193583`, `origin/main`에 푸시 완료. **실기기 확인 전이다** — 2D 폴백
지형이 실제로 어떻게 보이는지, 격자무늬가 이번에도 눈에 띄는지는 안 봤다.

## 2026-09-10 — "이어 할 사항 확인" → 2D 지도 사람을 Kenney CC0 스프라이트로

"사가고 이어 할 사항 확인 해줘"로 시작. 루트 `SAGA-HANDOFF.md`의 "이어서 하면
좋은 것" 목록에서 사가고 관련 셋(2D 캐릭터 에셋화·다리 피부색·격자무늬 길)을
사용자가 골랐는데, 확인해 보니 **다리 피부색(2026-09-06 `000c1d1`·`0057f44`로
이미 고치고 실측까지 끝남)과 격자무늬 길(2026-09-08 `2546eb3`로 이미
`terrainNoise`로 고침, 아래 09-08 절)은 코드로는 이미 끝나 있었다** —
루트 인계 문서가 그 뒤로 안 갱신돼 낡은 항목으로 남아 있었을 뿐. 격자무늬
길은 실기기 확인만 아직 안 됐는데, 사용자가 실기기 확인은 몰아서 하겠다고
이미 밝혀 둔 상태라 재촉하지 않았다.

그래서 실제로 진행한 것은 **2D 캐릭터 에셋화(사가고만, sprite.js 사람 그림만
— 사용자가 AskUserQuestion으로 범위 확정)** 뿐이다. `js/sprite.js`의
`bake()` `kind==='human'` 분기에 saga-forest가 2026-09-02에 먼저 깐 선례를
그대로 옮겼다 — Kenney Roguelike Characters(CC0)를 오려 낸 `assets/sprites2d/
human_01.png`~`14.png`(saga-forest에서 그대로 복사)를 인물 id 해시로 골라
붙인다. 그림이 아직 안 실린 첫 프레임만 옛 `human()` 절차적 그림으로
채운다(캐시 박제 방지). **지도 위를 걸어 다니는 몸만 바뀐다** — 도감·상세
초상은 이미 3D(`portrait3d.js`)라 안 닿고, 3D 기본 화면도 안 건드림(2D는
저사양 폴백/2D 모드 전용).

**잃는 것**(saga-forest와 같은 트레이드오프): 세력색·계절 옷 물듦·등신
4단계·그림 양식 3가지·다리 걷기 애니메이션은 고정 그림 한 장이라 못 낸다.
좌우 뒤집기·걸음 통통거림은 그대로 산다. 스폰의 등급은 발밑 고리 색으로
이미 따로 표시돼 등급 구분은 안 잃는다.

**검증**: `node -c js/sprite.js` 통과. 헤드리스 `_test.html` 3회 순차
**463/463 동일**(고친 게 그림뿐이라 자가진단 판정에는 안 닿음 — 진단 통과
개수 자체는 462→463에서 그대로). 3회 중 한 번은 크롬이 원인 불명으로 DOM을
못 덤프해 재시도했다(일시적 현상으로 보임, 서버는 계속 정상 응답). `sw.js`
VERSION → `go-v5.26.1`. 커밋 `dd43a09`. **실기기 확인 전이다** — 지도 위
사람 그림이 실제로 어떻게 보이는지는 안 봤다.

**다음 세션**: 나머지 네 판(사가의숲·사가스토리·사가국지·사가블로 — 사가블로는
지향점에서 빠짐)의 2D 캐릭터 에셋화는 아직 손 안 댐, 이어서 하려면 판마다
sprite.js 구조부터 다시 훑을 것. 다섯 판 공용 에셋 통합 관리 엔진(장기 목표,
범위 미정)도 그대로 남아 있다.

## 2026-09-09 (더욱 이어서) — 코드 감사 이어감(ai·ambient·auto·bag·buddy·codex·quest·milestone·letter·station·talk·icon·net·season·weather·perf.js), 버그 하나 고침

HANDOFF 큐 "6. 코드 감사 이어가기"를 계속 — 이번엔 위 16개 파일을 fork
에이전트로 정독했다.

**찾은 버그(고쳤다)**: `js/codex.js`의 `dex()`가 `core.save.dex`(구조가
`{ heroes: {...}, pets: {...} }`, `core.js:55`에서 그렇게 초기화된다)를
`for (k in save)` 로 훑고 있었다 — 이러면 실제 인물·펫 id가 아니라
**top-level 키인 `'heroes'`·`'pets'` 두 글자만 잡혀** `save[k]`가 늘 진리값인
객체라서 `n`이 항상 2로 고정됐다. 즉 얼마나 많은 인물·펫을 모았든 "발견"
시트(`ui.js` `viewCodex()`, PLAN 13절 "도감 완성률을 표시한다")의 **도감
칸이 늘 `2/104`처럼 실제와 무관한 값**으로 뜨고 있었다(전체 `%`도 그만큼
낮게 잡힘) — 화면에 실제로 노출되는 값이라 사용자가 도감을 채울수록
알아챌 만한 버그였다. `save.heroes`·`save.pets` 각각의 `Object.keys().length`
를 더하도록 고쳤다(`js/codex.js` `dex()`, ai.js의 `summary()`가 이미 쓰는
것과 같은 패턴).

**나머지 15개 파일은 확실한 버그 없음.** 특히 의심하고 봤던 자리들:
- `weather.js`의 `core.hash2` 호출은 `h01()`(두 배 보정)로 감싸져 있어 깨끗함.
- `ambient.js`의 8방위 나침반(`compass()`) 계산을 손으로 검산 — 동서남북
  넷 다 맞음.
- `talk.js`의 `tap()`이 `'walk'`라는 문자열을 반환하는 자리(문서 주석은
  boolean이라고만 적혀 있었다)를 `world.js:1156`이 실제로 어떻게 받는지
  확인 — 정확히 분기 처리하고 있어 오탐이었다.
- `auto.js`·`quest.js`·`milestone.js`·`letter.js`·`station.js`의 기록/카운터
  로직은 npc.js·duel.js류 함정(정렬 가정·배열 자르기)에 안 걸림.

확신도 낮아 남긴 후보는 없다.

**검증**: `node -c js/codex.js` 통과. 헤드리스 `_test.html` 3회 순차
**462/462 동일**(고친 게 표시 숫자 계산이지 자가진단이 짚는 판정 값이
아니라 진단 통과 개수 자체는 안 바뀜 — 도감 완성률 자가진단 항목이
아직 없다는 뜻이기도 하다, 다음에 이 항목을 자가진단에 추가할 여지가
있다). 헤드리스 크롬은 3회 모두 정상 종료, 잔여 프로세스 없음 확인.
`sw.js` VERSION → `go-v5.24.5`.

**다음 감사 후보**: 아직 안 본 파일 — `account.js`·`asset3d.js`·
`actor3d.js`·`battle3d.js`·`core.js`·`data.js`·`encounter.js`·
`encounter3d.js`·`geo.js`·`land.js`·`minimap.js`·`overworld.js`·
`relief3d.js`·`rogue-action.js`·`sprite.js`·`ssao3d.js`·`ui.js`·
`water3d.js`·`world.js`(world3d.js는 이미 끝냄). `sprite.js`(2505줄)·
`ui.js`(1349줄)·`world.js`(1866줄)는 크니 나눠서 볼 것. HANDOFF 큐의
나머지 항목(손맛 검증·녹색 캐릭터 제보·Mesh2Motion 도보·APK·사관 실청취)은
여전히 실기기·도구가 있어야 해서 못 건드렸다.

## 2026-09-09 (더 이어서) — 코드 감사 이어감(npc.js·animal.js), 버그 둘 고침

HANDOFF 큐의 "6. 코드 감사 이어가기"(world3d.js는 09-08에 끝냄, npc.js·
animal.js는 아직) 를 fork 에이전트로 처리. 확실한 버그는 없었지만 낮은
확신도로 걸리는 자리 둘을 찾아 사용자 확인 후 고쳤다:

1. **`js/npc.js` `npc_stranger`의 `spots` 순서.** `segAt()`은 `spots`가
   `from` 오름차순이라고 가정하는데(주석에 이미 그렇게 적혀 있었다),
   이 사람만 `[{from:21},{from:1},{from:3}]`로 자정을 넘겨 뒤섞여 있었다.
   지금까지는 h=21~24 구간에서 골라야 할 인덱스(from=21)와 실제로 골라지는
   인덱스(from=3)가 **우연히 둘 다 `at:'cave'`**라 위치가 안 튀었을 뿐 —
   `since`(구간 시작 후 지난 시간) 계산은 이미 틀려 있었다. `[{from:1,
   shrine},{from:3,cave},{from:21,cave}]`로 오름차순 재배열해 고쳤다
   (커밋 예정). 다른 아홉 명은 이미 오름차순이라 안 건드림.
2. **`js/animal.js` 까치(`magpie`) `move`.** `react()`의 문서화된 불변식
   "알아챈 거리(`sense`) 밖으로는 안 나간다"가 `move < sense`에서만
   성립하는데(수식: `newDist = d·(1-move/sense)+move`, d→0 극한이 곧
   move), 까치만 `move:26 > sense:18`이라 코앞에서 놀라면 감지거리 밖까지
   날아갔다. 다른 도망 동물 셋(사슴 22/30·잉어 9/12·늑대는 chase라 다른
   공식)은 다 `move<sense`. `move:13`으로 낮췄다(sense 대비 비율을 사슴·
   잉어와 비슷하게 맞춤).

`_test.html` 462/462, 헤드리스 2회 동일. `sw.js` VERSION → `go-v5.24.4`.
**실기기 확인 전이다** — 까치가 날아오르는 거리가 자연스러운지는 눈으로
안 봤다.

## 2026-09-09 (이어서) — 지도 스타일·등신 버튼 없앰, "지도 크게 보기" 신설

사용자가 실기기/화면을 보며 세 가지를 연달아 지적 — ① "어두운 지도 흰
지도는 이제 필요 없을 듯" ② "그리고 등신도" ③ "또 전체 화면으로 맵을
못 봐?". 하나씩 확인 질문으로 좁혔다.

1. **지도 스타일 버튼(🗺️) 통째로 제거.** CARTO 가 API 키를 요구하게 바뀐
   뒤로(2026-09-04) 3D 지면은 이미 `terrainTexture`로 대체돼 이 버튼을 눌러도
   눈에 보이는 차이가 없었다. `js/world.js`의 `MAP_STYLES`·`styleIdx`·
   `cycleMapStyle`을 지우고 타일 경로(`rastertiles/voyager`)를 상수로 고정,
   `js/world3d.js`의 `MAP_STYLE()`도 같이 지웠다(`getTile`이 이제 스타일
   인자를 안 받는다). `js/core.js`의 `settings.mapStyle` 필드·`_admin.html`의
   `world3d.mapStyle` 손잡이·`index.html`/`_demo.html`의 `btn-map`·
   README 의 `cycleMapStyle()` 언급까지 다 지웠다.
2. **등신 버튼(4등/2등/8등) 제거, 기본(4등신)만.** `js/game.js`의 `btn-prop`
   순환 핸들러를 지웠다. `sprite.js`의 `setProp`/`prop()` API 자체는 그대로
   둔다 — `_test.html`·`_demo.html`·`_probe.html`·`_gallery.html`이 진단·
   시각 검수용으로 여전히 직접 부른다. 옛 세이브에 `settings.prop`이
   `chibi`/`tall`로 남아 있으면 `game.js:87`이 그대로 존중한다(하위호환,
   버튼만 없앤 것이지 기능을 지운 게 아니다).
3. **"지도 크게 보기" 신설.** 기존 🧭 전체지도(`overworld.js`)는 발자취를
   점으로만 찍는 별개 화면이라 사용자가 원한 것과 달랐다 — 확인해 보니
   "지금 걷는 2D 지도를 도구줄 없이 크게" 쪽이었다. `#top` **밖**에 독립
   단추 `#btn-mapfocus`(⛶)를 새로 뒀다 — 도구줄 안에 넣으면 도구줄을
   감추는 순간 그 단추도 같이 사라져 되돌릴 길이 없어진다. 누르면
   `body.mapfocus`가 붙어 `#top`·`#autobar`·`#near`·`#dock`을 감춘다
   (`css/style.css`). 조우·시트·미니맵처럼 상황에 따라 뜨는 것은 안 건드림
   — 감추면 오히려 못 본다. 세이브에는 안 남긴다(매번 켜진 채로 열리면
   첫 화면부터 조작을 못 찾는다).

`_test.html` 462/462, 헤드리스 2회 동일(첫 회는 map/prop 제거 직후, 둘째는
mapfocus 추가까지 다 반영한 뒤). `sw.js` VERSION → `go-v5.24.3`.
**실기기 확인 전이다** — 특히 ⛶ 단추 자리(우상단, 노치·상태바와 안 겹치는지)는
화면으로 안 봤다. 조이스틱(`#gjoy`/`#gpad`)은 애초에 `#top` 밖의 형제 요소라
`mapfocus`가 건드리지 않는다(코드로 확인) — 감춰지지 않는다.

> ## 2026-09-08 (이어서) — "바둑판" 진짜 원인 찾아 고침(커밋 `2546eb3`), 실기기 확인 대기
>
> 사용자가 "필드맵이 너무 바둑판 같아서 자연스럽게 수정해줘"로 재요청 —
> 위 큐의 "바둑판" 지적을 실제로 파고들었다. **길(`tx%7`/`ty%9`)도 구역
> 이름도 아니었다** — `terrainAt()`의 손그린 땅(`land.js`)·실제 지형
> (`geo.js`, 이 PC 망에선 못 뜬다) 밖 폴백이 `core.hash2(tx,ty)`를 **칸마다
> 완전히 독립으로** 굴려(이웃과 상관없는 소금·후추 노이즈) 숲 한 칸·산 한
> 칸·물 한 칸이 뿔뿔이 흩어져 있었다 — `world3d.js`의 옛 주석(GPU 타일링
> 최적화를 실측하고 접은 자리)이 이미 이 성질을 "이웃 칸과 상관 없다"고
> 스스로 적어 둔 자리였는데, 아무도 그게 화면에 어떻게 보일지는 안 짚었다.
>
> `core.js`에 `noise2()`(hash2 격자점을 부드럽게 잇는 값 노이즈) 신설,
> `world.js`의 `terrainAt()`·`nearTown()`을 2옥타브 값 노이즈(`terrainNoise`,
> 9칸+3칸 결)로 바꿨다. 문턱값은 옛 비율(물14%·산18%·숲36%·마을12%·들20%)이
> 그대로 나오게 400만 표본으로 재보정(옥타브를 섞으면 평균 쪽으로 쏠려 옛
> 문턱을 그대로 쓰면 숲만 태반이 된다 — 실측 후 고침). **손그림 땅·실제
> 지형 레이어는 안 건드림** — 둘 다 안 걸리는 먼 들판에서만 이 폴백이 쓰인다.
>
> **검증**: node로 문턱 보정 전/후 70×32 표본을 텍스트 격자로 찍어 봐서
> "숲은 숲대로, 산은 산줄기로, 물은 호수·강으로" 뭉치는 걸 직접 확인(고친
> 후 코드와 완전히 같은 공식으로). `_test.html` 462/462, 3회 동일 — 게임
> 판정(`relief3d.js`의 "외딴 산은 낮다" 보정처럼 예전 소금·후추 지형을
> 전제로 짠 자리도 그대로 통과, 표본 부족이면 건너뛰는 방어 코드 덕에
> 안 깨짐). **실제 3D 화면 스크린샷은 이 PC 망의 Overpass 재시도 지연 때문에
> 10분 넘게 안 끝나 이번에도 못 봤다** — 수학·자가진단으로는 확인했지만
> **실기기(폰)에서 실제로 자연스러워 보이는지는 여전히 확인 전**이다.
>
> 다음 세션: 사용자 실기기 확인부터 물을 것. 그래도 부자연스러우면 —
> ① `terrainNoise`의 옥타브 스케일(9칸·3칸)이나 가중치(0.65/0.35)를
> 조정할 여지가 있다(패턴이 너무 크게/작게 뭉쳐 보이면), ② 마을(`town`)
> 밴드가 좁아(0.0306 폭) 실제로 마을이 몇 칸씩 뭉치는지 실기기로 확인이
> 필요하다.
>
> ## 다음 세션 큐 — 2026-09-07 저녁, 사용자가 마지막에 남긴 요청 중 나머지
>
> "맵을 정리 정돈 해야 할것 같아 바둑판으로 지저분에" — 지도가 **바둑판처럼
> 지저분해 보인다**는 지적. **주의**: 오늘 세션 초반에 다룬 "격자무늬
> 길·hash2 줄무늬"와 겉으로는 비슷해 보이지만 **같은 건이 아닐 수 있다** —
> 그건 파고들어 보니 실제로는 `regionName()`이 구역 이름 뒤 절반을 못
> 뽑던 버그였고(고쳤다, 커밋 `8260895`), 세계 격자무늬 길 자체는 이미
> 2026-09-04에 `nearTown`+`roadIsVertical`로 마을 근처·동네별 한 축으로
> 좁혀 둔 상태였다(코드만 봐서는 더 손볼 자리를 못 찾았음). 그런데 사용자가
> 오늘 다시 "바둑판" 표현을 썼다는 건 **실기기/실제 화면에서 아직도 눈에
> 띄게 거슬린다는 뜻일 수 있다** — 코드 감사만으로는 "다 됐다"고 판단하지
> 말고, 다음 세션은 **실제로 지도를 화면에 띄워서**(claude-in-chrome 연결
> 되면 그걸로, 아니면 사용자 실기기 스크린샷을 받아서) 먼저 확인부터 할 것.
> 무엇이 "지저분"한지(길 배치·타일 텍스처 이음매·소품 배치·지형 색 경계
> 등) 구체적으로 좁힌 뒤 손볼 것 — 짐작으로 코드부터 고치지 않는다.
>
> **+ "추가 사항은 더 찾고, 전에 못했던 작업들"** — 사용자가 이어서 요청.
> 아래는 이 저장소를 오늘 훑으며 확인한, **여러 세션째 실기기·도구가 없어
> 막혀 있던 것들**을 한데 모은 것이다(개별 항목의 자세한 배경은 이 파일
> 안의 각 날짜 절 참고) — 다음 세션은 이 목록부터 사용자와 하나씩 맞춰볼 것:
>
> 1. **손맛 검증 셋** — 실기기나 claude-in-chrome으로 사람이 직접 조작해
>    봐야 하는 것들, 이 환경(헤드리스·자동화 탭)은 rAF 실시간 조작이 안 돼
>    구조적으로 못 했다: ① 걷기 조작감(이동속도 8m/s로 낮춘 뒤 아직 안 봄)
>    ② 적도 강타 회피 난도(예고 1.1초·반경 5.5m인데 이동속도 8m/s라 코드
>    계산상 거저 피해진다 — 값을 만질지는 실제로 겪어 보고 정할 것)
>    ③ 오늘 고친 둘(구역 이름 30개 표시·긴 교전 "합" 목록)이 실제 화면에도
>    맞게 나오는지
> 2. **"모바일에서 캐릭터가 녹색" 제보** — 헤드리스로 재현 안 됨. 다음엔
>    실기기 스크린샷이나 그 폰의 콘솔 로그부터 받을 것(짐작으로 안 고침)
> 3. **Mesh2Motion 실사 캐릭터 다섯 명**(은창·강우·셀렌느·발데나르·
>    바실로른)이 실제로 걷는 모습 — 리타깃은 자가진단으로 확인됐지만
>    사람이 화면으로 본 적은 아직 없다
> 4. **APK 재빌드** — 이 PC엔 JDK·Android SDK가 아예 없어 이번 세션엔
>    확인만 하고 접었다(다른 PC에서 하거나 도구 설치가 먼저)
> 5. **사관(AI) 응답 실청취** — `ANTHROPIC_API_KEY` 필요, 온라인 모드
>    군략·천기를 한 번씩 불러 확인하는 일이라 사용자 키가 있어야 한다
> 6. **코드 감사 이어가기** — `world3d.js`는 **2026-09-08에 끝냄(아래 새 절,
>    깨끗했다)**. `npc.js`·`animal.js`는 아직 안 봤다.
>
> 사용자가 "더 찾고"라 한 만큼, 이 목록에 없는 다른 막혀 있던 요청이 더
> 있는지 `SAGA-HANDOFF.md`·이 파일의 다른 날짜 절도 한 번 더 훑어 빠진
> 게 없는지 확인하고 시작할 것.

> ## 2026-09-08 — world3d.js 코드 감사 끝(깨끗함), 지도 스크린샷은 이 PC 망 제약으로 판단 불가
>
> 큐의 6번(코드 감사 이어가기)부터 처리 — `world3d.js` 2923줄 전체를 fork
> 에이전트로 정독(문자열/배열 슬라이싱 경계·hash2 오용·오프바이원·조건식
> 반전 유형 위주). **확실한 버그 없음.** `landTexture()`에 5번째 인자를
> 넘기지만 함수가 안 쓰는 사소한 자리 하나만 발견 — 실동작에 영향 없어
> 안 고쳤다.
>
> 큐의 "바둑판" 지적을 실제로 눈으로 보려고 헤드리스 크롬(SwiftShader)으로
> `_demo.html#map-3d` 스크린샷을 시도 — **이 PC의 회사망에서
> `overpass-api.de`(2026-09-05에 붙인 실제 지형 API)가 완전히 막혀 있다**
> (`curl`로 확인, HTTP 000 — `github.com`은 정상). `geo.js`는 이미
> `AbortController`+20초 타임아웃으로 안전하게 짜여 있어(무한 대기 아님)
> 게임이 멎진 않지만, **이 PC에서는 "실제 지형" 레이어가 절대 못 뜨고 항상
> 옛 좌표 해시 폴백만 보인다** — 사용자의 실기기(폰, 다른 망)에서도 같은지는
> 별개로 확인이 필요하다. 찍힌 스크린샷은 회사망 지연(Overpass 재시도
> 백오프)까지 겹쳐 GLB 실제 에셋이 다 못 실린 로딩 중 상태(집이 텍스처 없는
> 회색 도형, 땅이 새까맣게)라 "바둑판" 여부를 판단할 근거가 못 됐다 — 헤드리스
> 왕복이 실제로 10분 넘게 걸렸다(이 환경의 기존 기록 "그날그날 다르다"와 일치).
>
> **다음 세션**: 이 PC로는 "바둑판" 확인이 구조적으로 어렵다(망 제약 +
> 헤드리스 GLB 로딩이 너무 느림) — 사용자 실기기 스크린샷을 받는 쪽이
> 훨씬 빠르다. 큐의 나머지(손맛 검증·녹색 캐릭터·Mesh2Motion·APK·사관 청취·
> npc.js/animal.js 감사)도 그대로 남아 있다.

## 2026-09-07 (같은 날 더 이어서, 열 번째) — 코드 감사 계속(rogue.js·fort.js·event.js·hero.js·growth.js·raid.js) → 새 버그는 없음, 죽은 코드 하나 정리

"rogue.js·fort.js·event.js도 이어서 감사해줘"에 이어, "다른 파일도 감사할까"
물었더니 이어가라 해서 `hero.js`·`growth.js`·`raid.js`까지 봤다.

**새 버그는 안 나왔다.** 이번에 특히 조심한 것 둘 — 지난 두 번(구역 이름·
교전 기록 자르기)과 같은 유형이 더 있는지 — 은 깨끗했다: `rogue.js`·
`fort.js`의 `core.hash2` 호출은 전부 `h01()`(두 배 보정)로 감싸져 있었고,
세 파일 다 기록을 자르는 배열 캡이 없었다. `fort.js`의 공물 하루 상한
(`GOLD_PER_DAY_CAP=480`)이 "성채별이라 원작(계정 전체 상한)과 다르다"로
버그처럼 보여 한 번 의심했지만, `GOLD_PER_HOUR(40) × HOLD_MS(12시간) =
480`으로 **한 성채를 온전히 쥐고 있는 동안 자연스럽게 받는 최대치와
정확히 같다** — 계정 전체가 아니라 "같은 성채를 하루에 여러 번 뺏고
뺏기며 우려먹는 것"만 막는 의도된 장치였다. 숫자를 다시 맞춰 보고
오탐으로 접었다(보고 전에 걸러서 다행이었다).

**죽은 코드 하나는 찾아서 정리했다.** `hero.js`의 `partyPower()`가
`core.effect().atkPct`·`.hpPct`를 곱하고 있었는데, 그 두 키를 만드는
곳은 `js/_expansion/idle.js`·`prestige.js`(본편 어디서도 안 싣는
방치 전투 확장, `battle.js`가 이 계산의 원본이다 — `partyPower()`
자체가 거기서 옮겨 온 것)뿐이었다. `ai.js`가 이미 "본편이 읽는 키가
아니라서 천기 보정 목록에서 뺐다"고 적어 둔 그 키들과 같다 — 즉
곱셈이 늘 `×1`(무효과)이었다. 사용자가 "정리해줘"로 확인 — 두 줄과
그 값을 만들던 `core.effect()` 호출을 지웠다. `_expansion`을 되살릴
때는 `94850f8` 기준으로 그 세 파일을 통째로 되살리는 것이지 이 함수를
다시 손보는 게 아니므로 지워도 안전하다(주석에 남겨 둠).

**검증**: 헤드리스 3회 순차 462/462 동일(값이 원래도 항상 ×1이었으니
결과가 바뀔 자리가 없다 — 실제로 자가진단 어느 항목도 안 흔들렸다).
**순수 정리라 `sw.js` VERSION은 안 올렸다** — 지운 코드가 늘 no-op였으니
캐시된 옛 클라이언트도 똑같이 동작한다(이전 두 커밋과 다른 점).

**다음 감사 후보(다음 세션)**: `world3d.js`(렌더링, 아주 크다)·
`npc.js`·`animal.js`는 아직 안 봤다. 이어갈지는 다음 세션이 사용자에게
먼저 물을 것.

## 2026-09-07 (같은 날 더 이어서, 아홉 번째) — 코드 감사 계속 → 교전 결과의 "합" 목록이 60타 넘는 긴 싸움에서 초반을 통째로 빠뜨리던 버그 고침

"비실시간전투(duel.js) 및 코드 감사 계속"으로 이어 요청 — `duel.js`를 처음부터
끝까지 읽으며 감사했다.

**찾은 버그**: `note(s, kind, dmg)`(교전 한 수를 `s.acts`에 기록하는 함수)가
기록이 **60개를 넘으면 앞을 잘랐다**(`s.acts.shift()`). 그런데 결과 화면이
보여주는 "합" 목록(`fold()`)은 **그 순간 `s.acts`에 남아 있는 것만** 열 덩이로
접는다 — 잘려 나간 초반 타격은 그 목록에서 통째로 사라진다. 속공 쿨(`QUICK_CD`
0.35초)만으로도 60초 제한 시간 안에 최대 170타 가까이 나올 수 있어, 오래
끄는 실전 교전에서 쉽게 밟히는 자리였다(승패·최종 기세는 `s.dealt`라는
별도 누적값을 쓰므로 안 흔들렸다 — **화면에 보여주는 "몇 합에 얼마 깎았다"는
내역만** 실제와 어긋났다). `js/rogue-action.js`(도적전 엔진, "duel.js의
fold()와 완전히 같다"는 주석이 붙어 있던 대로 똑같은 코드를 그대로 복사해
갖고 있었다)도 같은 결함이었다.

**곁들여 찾은 것 — 아직 드러나진 않았지만 잘못된 값**: `duel.js`의
`create(o)`가 `s.left`(남은 시간)는 `o.timeSec`로 채우면서 `s.timeSec`
자체는 안 채우고 있었다. `perf()`의 `timeUsed`가 `s.timeSec || TIME_SEC`
(기본 60)을 쓰므로, `create()`를 화면 없이 다른 제한 시간으로 직접 부르면
`timeUsed`가 늘 60 기준으로 잘못 나온다 — 지금은 어디도 그렇게 안 부르니
겉으로 안 드러났을 뿐이다(`rogue-action.js`의 `create()`는 처음부터
`timeSec`도 같이 채운다 — 그쪽이 맞았다).

**고침**:
1. `js/duel.js`·`js/rogue-action.js`의 `note()`에서 60개 자르기(`shift`)를
   없앴다. `fold()`가 몇 덩이로만 접어 화면에 내므로 원본 기록을 자를
   이유가 없었다 — 한 교전(최대 수백 개)은 안 잘라도 메모리 부담이 없다.
2. `js/duel.js`의 `create()`에 `timeSec: o.timeSec || TIME_SEC`을 추가해
   `rogue-action.js`와 맞췄다. `open()`이 그 뒤에 따로 `cur.timeSec`를
   덧붙이던 줄은 이제 중복이라 지웠다.

**자가진단에 회귀 방지 시험 둘 추가**(둘 다 `duel.js` 대상 — 같은 결함을
그대로 복사해 가진 `rogue-action.js`는 이번엔 시험을 안 늘렸다, 필요하면
다음에): "60타를 넘는 긴 교전도 접은 합이 낸 피해와 같다"(90타를 강제로
돌려 `s.acts.length===90`·`fold 합===dealt` 확인), "교전을 다른
제한시간으로 세워도(open 없이) timeUsed가 그 값을 쓴다"(`timeSec:20`으로
세워 시간이 다할 때까지 굴린 뒤 `timeUsed===20` 확인).

**밟은 함정 — 새 시험을 내가 처음 쓴 값으로 스스로 깨뜨림**: 첫 시험을
`foeHp: 100000000`으로 짰다가 실패했다 — `foeAtk`가 `foeHp * 0.01`로
정해지는 걸 깜빡해서, 적 평타 한 방(약 100만)이 내 사기(30만)를 그 자리에서
넘겨 8타 만에 패퇴로 끝나 버렸다(진짜 코드 버그가 아니라 시험 값 실수).
node vm으로 `duel.js`만 떼어 굴려 원인을 좁힌 뒤 `foeHp: 100000`으로
고쳐 재확인했다 — **큰 수를 아무 데나 쓰지 말고 관련 파생값(여기선 foeAtk)
까지 같이 셈해 볼 것**이라는 교훈.

**검증**: 헤드리스 3회 순차 462/462 동일(새 시험 2개 포함, 회귀 없음).
`sw.js` VERSION `go-v5.21.29`로 올림 — 결과 화면 표시가 바뀌는 변경이라
폰 캐시가 안 올리면 여전히 옛 셈을 본다. **실기기로 실제 긴 교전(도적전
포함) 뒤 결과 화면의 "합" 목록이 끝까지 다 나오는지는 아직 안 봤다** —
claude-in-chrome이 이번 세션 연결이 안 돼 화면으로 확인 못 했다.

**계속 감사할 자리(다음 세션 후보)**: `duel.js`는 끝까지 다 봤다. 아직
안 본 곳 — `rogue.js`(적도 전투 판정)·`fort.js`(성채·토벌 카드 및 결과
화면)·`event.js`(사건 카드) 본문. 같은 방식(끝까지 읽고 실제로 값을
계산해 검증)으로 이어가면 된다.

## 2026-09-07 (같은 날 더 이어서, 여덟 번째) — "격자무늬 길·hash2 줄무늬 다듬기" → 진짜 원인은 구역 이름 절반이 죽어 있던 것, 고침

"사가고 이어 개발 해줘"에 뭘 이을지 사용자에게 물어 이 항목을 골랐다(앞선
여러 세션이 CLAUDE.md/HANDOFF에 "다음에 상의"로 미뤄 뒀던 자리). 조사해
보니 **격자무늬 길 자체는 2026-09-04에 이미 고쳐져 있었다**(`nearTown`+
`roadIsVertical`, 마을 근처에서만·동네마다 한 축만 서게) — 다시 손댈 거리가
없었다. **hash2 줄무늬는 코드를 다 훑어 원인을 좁혔다**: `core.hash2`가
0~0.5만 돌려주는 건 알려진 대로였고, 이걸 쓰는 자리 대부분(`fort.js`·
`rogue.js`·`raid.js`·`weather.js`·`prop3d.js`·`relief3d.js`·`world3d.js`의
`h1`/`h01`, `world.js` 자신의 `stationsIn`용 `h01`)은 이미 **두 배로 펴서**
쓰고 있었다. 딱 하나, **`regionName()`만 안 펴고 원값을 그대로 썼다**
(715행 근처) — `Math.floor(core.hash2(...) * 30)`에서 hash2가 0~0.5만
나오니 `idx`가 0~14만 나와, `REGION_NAMES` 30개 중 **뒤 절반 15개(한양·
경주·전주·의주·동래·진주·남원·철령·압록·두만·탐라·강화·수원·충주)가
영영 안 뽑히는** 죽은 코드였다(node로 -500..500 좌표 전수 스캔해 실측
확인). "구역 이름을 다시 눈으로 보며 조율해야 한다"던 CLAUDE.md 문구가
가리키던 게 바로 이거였다.

**고침**: `js/world.js`의 `regionName()`이 이미 존재하던 `h01(a,b)`
(같은 파일 1242행, stationsIn 이 쓰던 "두 배로 펴는" 도우미)를 그대로
재사용하게 한 줄만 바꿨다 — 새 헬퍼 안 만듦, 지형·조우·스폰·역참·
성채(전부 이미 h01/자체 문턱값으로 안전한 자리)는 한 글자도 안 건드림.
**땅 생성·격자무늬 길·`terrainAt`/`nearTown`/`roadIsVertical`의 문턱값은
의도적으로 그대로 뒀다** — 이것들은 CLAUDE.md가 경고한 대로 hash2의 좁은
범위에 손으로 맞춰진 값이라, 여길 고치면 지형·구역 이름을 전부 다시
눈으로 조율해야 한다(이번엔 그 범위 밖).

**자가진단에 회귀 방지 시험 추가**: "구역 이름 30개가 다 나온다" —
region key -30..30 범위를 전수 순회해 `regionName`이 만들어내는 이름
집합에 뒤 절반 이름(한양·경주·전주·충주)이 실제로 포함되는지 확인한다.
고치기 전 코드로 되돌리면 이 시험이 바로 잡는다.

**검증**: 헤드리스 3회(순차, 새 시험 추가 전) 459/459 동일 → 시험 추가 후
460/460. `sw.js` VERSION `go-v5.21.28`로 올림(지도 위 이름표가 바뀌는
변경이라 폰 캐시가 안 올리면 여전히 옛 이름만 본다). **실기기로 지도 위에
"한양"·"경주" 같은 이름표가 실제로 뜨는지는 아직 안 봤다** — 다음 세션
또는 사용자가 실기기에서 지도(미니맵/전체지도)를 넓게 돌아다니며 확인할
자리로 남긴다.

## 2026-09-07 (같은 날 더 이어서, 일곱 번째) — "물가 경계도 자연스럽게 다듬어줘" → 이어진 강(사각 프롭)에 물가 쪽만 들쭉날쭉한 지오메트리 추가

바로 아래 절이 "남은 것"으로 적어 둔 것을 사용자가 이어서 요청 — `land.js`가
그은 이어진 강(`propPlan`의 `p.sq===true`, 칸을 꽉 채우는 네모 프롭)의 물가가
48m 격자에 딱 맞춰 각져 보이던 것을 손봤다. 고립된 웅덩이(`p.sq===false`,
원반 프롭)는 이미 둥글어 손 안 댔다.

**설계**: `js/world3d.js`에 `shoreGeo(mask)` 신설 — 단위 사각형(SEG=10로
잘게 나눈 `PlaneGeometry`)의 바깥 테두리 정점 중, **마스크(1=동·2=서·4=북·8=남)
에 켜진 변만** 안쪽으로 들쭉날쭉 밀어 넣는다(하나의 지오메트리를 마스크값별로
캐시해 최대 16장까지만 만든다 — `unitGeo()`의 "도형은 나눠 쓴다" 원칙 그대로,
따로 dispose 할 것도 없다). **꺼진 변(강물끼리 이어지는 자리)은 손 안 대
정확히 0.5 그대로 두므로**, 옆 칸의 같은 변도 똑같이 안 밀려 두 칸이 자로
잰 듯 맞물린다 — 이웃이 물인지 뭍인지 굳이 서로 맞춰 볼 필요 없이
`h1()`(좌표 해시)만으로 저절로 이가 맞는다. `propPlan`의 water 갈래에서
`global.DG.world.terrainAt(gx±1,gy)`/`(gx,gy±1)`로 네 이웃을 살펴 이 마스크를
`p.shore`에 실어 보낸다.

**축 방향은 손으로 풀어 확인했다** — `rotation.x=-90°`로 눕히는 물 판의
로컬 Y 는 월드 Z 의 **반대**(`liftTile`의 옛 주석과 같은 결)라, 로컬
`ly=+0.5`→ `gy-1`(북) · `ly=-0.5`→`gy+1`(남)로 대응한다는 걸 회전행렬을
직접 풀어 확정한 뒤 코드에 반영했다 — 대충 짐작했으면 옆 칸과 어긋나
틈이 벌어지는 자리가 나올 뻔했다.

**밟은 함정 — 진폭을 처음엔 너무 작게, 카메라도 잘못된 각도로 봐서
"안 먹힌 줄" 착각할 뻔함**: 처음 값(단위 0.05~0.16 → 실측 2.4~7.8m)으로
강을 가로질러 보는 각도(플레이어가 물가에 서서 강 건너를 보는 구도)에서는
들쭉날쭉함이 화면에 거의 안 보였다 — 픽셀 스캔으로 경계 y좌표를 재봐도
매끈한 그라디언트뿐이었다. **원인은 코드가 아니라 시험 각도**였다 —
`DG.world3d.yaw(Math.PI/2)`로 카메라를 90° 돌려 **강을 따라 내려다보는
구도**로 바꾸자 지그재그가 뚜렷이 보였다(원근 때문에 가로지르는 각도에서는
안쪽 변위가 화면 세로축으로 잘 안 드러난다). 일부러 진폭을 크게(0.30~0.45)
걸어 지그재그가 확실히 보이는 것부터 확인한 뒤, 자연스러운 값(단위
0.07~0.10 → 실측 3.4~8.3m, SEG 6→10로 결도 더 곱게)으로 낮춰 최종 확정했다.

**밟은 함정 — 헤드리스 크롬이 실제로 크래시함(내 코드 탓 아님, 확인함)**:
자가진단을 배경에서 3연속 돌리다 두 번째부터 GPU 프로세스가
`exit_code=-1073741819`(STATUS_ACCESS_VIOLATION)로 죽는 걸 봐서 처음엔
`shoreGeo` 지오메트리 버그를 의심했다 — 그런데 **첫 번째 시도는 이미
459/459로 멀쩡히 끝나 있었다**(로그로 확인). 겹쳐서 쌓인 여러 헤드리스
크롬 인스턴스(이 대화 내내 정리 안 하고 계속 띄운 것들 + 동시에 돌던
claude-in-chrome 실제 브라우저)가 자원을 다투다 뒤에 뜬 것부터 죽은
것으로 보인다(사용자가 "고사양도 아닌데"로 직접 지적) — **원인은
자원 경합이지 이번 코드가 아니다.** 밀린 헤드리스 프로세스를 전부
PID로 콕 집어 정리(`taskkill /PID <n> /T /F`, `/IM chrome.exe` 통째 kill은
여전히 안 씀)하고 **한 번에 하나씩, 끝나길 기다렸다 다음을 돌리는 식**으로
바꾸자 3회 연속 459/459 깨끗이 통과했다. **다음에도 헤드리스를 여러 번
돌릴 때는 백그라운드 반복문으로 겹쳐 띄우지 말고 순서대로, 하나 끝난 것
확인하고 다음을 돌릴 것** — 이 PC는 고사양이 아니다.

**검증**: 헤드리스 3회(순차) 459/459 동일, 회귀 없음. claude-in-chrome으로
실제 저장(형주 서역 인근 이어진 강, `shore=12`)에서 카메라를 강 방향으로
돌려 물가가 완만하게 들쭉날쭉해진 것을 스크린샷으로 확인 — 강물끼리
이어지는 안쪽은 여전히 매끈하게 이어짐. 테스트 중 다시 위치를 옮겼다가
(같은 탭에서 두 번째 새로고침 사이에 어느 tick 함수가 또 조용히
`persist()`를 불러 세이브가 잠깐 다른 좌표로 덮였다 — 이 세션도 이미
두 번째로 겪은 함정) `pos`를 알려진 원래 값(-1079.4814729517343,
545.0261723535723)으로 다시 못박아 복구, `gold`·`feat`·`party` 안 바뀐 것도
재확인.

## 2026-09-07 (같은 날 더 이어서, 여섯 번째) — "water3d 렌더링 원인 마저 파봐줘" → 진짜 원인 확정, 고침(회귀 없음, 실기기로 물결 확인)

바로 아래 절이 "미확정 단서만 남기고 중단"했던 것을 사용자가 이어서
파 달라고 요청 — `world3d.js`를 계속 추적해 확정했다.

**원인**: `propPlan(kind, gx, gy, mapped)`의 `kind==='water'` 갈래(1027행)가
"지도가 깔렸으면(`mapped`) 물은 이미 지도 그림에 칠해져 있다"는 전제로
`if (!mapped || authored)`일 때만 `water3d` 셰이더가 붙는 물 프롭을
세웠다. 이 전제는 **2026-09-04 감사 이전에만 맞았다** — 그 감사가
`LAND_PAINT()`(기본값 켜짐)를 넣어, 절차적 땅(손으로 안 그린 대부분의
세계)은 실제 지도 대신 **제 손으로 구운 텍스처**(`terrainTexture()`)를
덮어 쓰게 바뀌었는데, 그 텍스처 함수는 `water`를 **일부러 뺀다**
(428행 주석: "실제 물결은 water3d.js가 따로 그리므로"). 즉 지도가
와서(`mapped=true`) `propPlan`이 물 프롭을 건너뛰어도, 화면엔 그 지도가
아니라 물이 빠진 자체 텍스처가 깔리는 구멍이 났다 — **procedural 물
지형에서 물 프롭도 안 서고 물 텍스처도 없어, 각진 단색 사각형만
남는** 원인이었다. `LAND_PAINT()`가 기본 켜짐이라 대부분의 플레이가
이 구멍을 늘 밟는다.

**고침**: `js/world3d.js` 1035행 조건에 `LAND_PAINT()`를 더했다 —
`if (!mapped || authored || LAND_PAINT())`. LAND_PAINT가 켜져 있으면
(기본값) `mapped`와 무관하게 늘 물 프롭을 세운다 — 어차피 그 텍스처가
지도를 안 쓰므로 "지도 위에 원반이 겹친다"던 옛 우려가 더 이상
안 맞는다. LAND_PAINT를 끄면(레거시로 실제 지도를 그대로 보여줄 때)
옛 동작(`mapped`면 생략) 그대로 남는다.

**자가진단도 같이 고침**: `_test.html`의 "땅 — 지도가 깔려 있어도
강물은 깐다"가 `outside.indexOf('water') < 0`(절차적 물은 `mapped=true`
면 물 프롭 없음)을 그대로 굳히고 있어 내 수정과 충돌 — 이 시험 자체가
2026-09-04 이전 전제를 그대로 박제한 것이었다. 새 시험
"땅 — LAND_PAINT 가 켜져 있으면(기본값) 절차적 물도 깐다"로 갈아
끼워 **LAND_PAINT 켜짐/꺼짐 두 갈래를 각각** 확인하게 했다(켜짐이면
`mapped=true`에도 물이 서고, 꺼짐이면 옛 동작대로 생략).

**검증**: 헤드리스 3회 459/459 동일(458+새 시험 1, 결정적). 실기기
(claude-in-chrome)로 같은 물 타일에서 `DG.water3d.stats()`가
`asks:0→1·mats:0→1·tier:'-'→'HIGH'·wave:0.72·glint:0.143`로 바뀌는 것,
스크린샷에서 이전엔 완전 단색이던 물이 **결·반사 무늬가 실제로 도는
것**을 직접 봄. 테스트 중 다시 텔레포트를 썼으므로 끝나고 `pos`
원래대로 되돌리고 `persist()`, `gold`·`feat` 안 바뀐 것도 재확인.

**남은 것**: 물/땅 경계 자체가 48m 격자에 딱 맞춰 각진 것은 이번
수정과 별개— `terrainTexture()`의 서브셀 블렌딩(SUB=4, 700행대)이
경계를 흐리게 하는데, 물만 프롭(별도 평면)이라 그 블렌딩을 안 받는다.
더 자연스러운 물가를 원하면 다음 후보로 남긴다.

## 2026-09-07 (같은 날 더 이어서, 다섯 번째) — "콘텐츠 밀도·그래픽 디테일 재점검" → 진짜 원인은 이 환경의 성능 자동강등, 물 렌더링은 미확정 단서만 남기고 중단(세이브 위치 되돌림 포함)

이 세션 맨 처음 절이 "2(콘텐츠 밀도)·3(그래픽 디테일)은 손 댈 명확한
거리를 못 찾았다"고 넘겼던 것을, 사용자가 "다음 진행해" 질문에서
직접 골라 재요청 — 이번엔 claude-in-chrome이 연결돼 있어 실제 세이브를
텔레포트시켜 가며(`core.save.player.pos` 직접 대입 + `world.update`류
tick을 rAF 없이 수동으로 굴리는 `__tick(n)` 헬퍼) 지형별로 스크린샷을
찍어 봤다.

**진짜 원인 하나 확정**: 처음 본 화면(대로/역참 근처)이 등롱 몇 개뿐인
휑한 모습이라 "콘텐츠가 비었나" 싶었는데, `DG.perf.stats()`를 찍어 보니
**`tier: 'LOW'`, `prop 배율 ×0.25`** 로 자동 강등돼 있었다 — 이 자동화
환경(숨은 탭 + 수동 tick 왕복)이 재는 fps가 낮아 `perf.js`의 자동
성능조정이 스스로 낮춘 것이다. `DG.perf.auto(false); DG.perf.set('HIGH')`
로 강제한 뒤 **같은 자리**를 다시 재니 props 8→50개로 확 늘었다 —
**PLAN.md가 요구한 밀도는 실제로 코드에 있고, 이 환경(자동화·숨은 탭)
에서 관찰하면 성능 자동강등 때문에 비어 보일 뿐**이라는 게 이번의
핵심 결론이다. 다음에 이런 "휑하다" 판단을 할 때는 먼저
`DG.perf.stats().tier`부터 확인할 것 — LOW 면 그 관찰은 신뢰 못 한다.
숲·산·성채 근처는 강제로 HIGH를 걸어도 프롭·지형지물이 실제로 풍성했다
(폐허 랜드마크·성채 탑이 안개 속에서도 멀리 보임, 나무·NPC·깃발 다양).

**미확정 단서 하나, 코드는 안 건드림**: 물 지형(procedural water 타일)
경계가 유독 각지고(체스판처럼 딱 잘린 경계) `DG.water3d.stats()`가
`asks:0·mats:0·ticks:0`으로 나왔다 — 새로고침 직후 HIGH를 걸어도
그대로였다(캐시 탓이 아니라는 뜻). `world3d.js`의 `kind==='water'`
프롭 경로(1027행)는 `pmat(hex,'water')`로 `water3d.material()`을 부르는
게 맞는데 실제로는 안 불리고 있다 — **이 procedural 물 타일이 그
경로를 안 타는 것인지, 아니면 애초에 `water3d.js`가 "손으로 그린 땅"의
호수·강 같은 authored 물 자리에만 쓰도록 설계된 것인지 코드만 봐서는
못 갈랐다.** 더 파려면 `world3d.js`의 `syncProps()`가 `propPlan()` 결과를
어떻게 걸러 `kind:'water'` 항목을 만드는지, procedural 타일이 그 목록에
드는지부터 볼 것 — 이번엔 시간상 여기서 멈췄다.

**밟은 함정 — 세이브 오염, 발견하고 바로 고침**: 텔레포트 테스트 중
`core.persist()`가 (내가 직접 부르지 않았는데도) 어느 tick 함수에선가
불려 실제 세이브가 테스트 좌표(-768,432, 강 한복판)로 덮어써진 걸
나중에 확인했다 — 세션 처음 pos(-1079.48,545.03, 실제 마지막 플레이
위치)로 되돌리고 다시 `persist()`해 복구했다. gold(13,658)·feat(7,087)·
distance(73,658m)·party 등 다른 필드는 텔레포트 전후로 안 갈렸다는 것도
확인함(처음엔 HUD의 "7,087"을 gold로 착각해 놀랐는데 실제론 feat 필드였고
변화 없었다). **다음에 이런 식으로 세이브를 직접 조작해 볼 때는 시작
전에 `pos`뿐 아니라 `gold`·`feat`·`distance` 등 핵심 필드를 먼저 찍어
두고 끝나면 대조할 것** — 이번엔 운 좋게 pos 하나만 어긋났지만 항상
그렇다는 보장은 없다.

## 2026-09-07 (같은 날 더 이어서, 네 번째) — "비전투 이벤트 3D 연출도 이어서 확인해줘" → claude-in-chrome 재연결, 코드 변경 없이 재확인만

이 세션 앞부분에서 claude-in-chrome이 처음엔 연결 안 됐다가(확장 재시작
필요했던 것으로 보임) 사용자가 "브라우저 킴"으로 알려 준 뒤 재연결됨.
2026-09-06에 실기기로 이미 다 확인됐던 "비전투 이벤트 3D 무대 연출"
넷(소품·발견 알갱이·사람 몸짓·밤 조명)을, 그 뒤 있었던 다른 변경(초상
3D 전환·도적전 배율 조정)이 뭔가 건드리지 않았는지 **실제 세이브**
(제후→패왕 Lv.19, 이전 세션이 남긴 그대로)로 재확인했다. 코드는 하나도
안 고쳤다 — 순수 재확인.

- **발견 소품** — `DG.event.force('rare_herb')`(약초)·`'flood_ford'`
  (여울) 둘 다 `duelFoe()`가 `{kind:'prop', propType:...}`을 정확히
  내고, `#encounter` 배경을 지우고 카드를 숨겨 스크린샷으로 직접
  확인 — 약초는 흙무더기 위 초록 잎, 여울은 파란 웅덩이+물보라 넷이
  그대로 서 있다.
- **밤 사건 조명** — `wolf_ring`(eerie)에서 `eventMood()`가 0.6, 시각을
  22:30으로 못박고(`DG.world3d.forceTime(ms)` — `DG.world.forceTime`이
  아니라 `world3d`에 있다는 걸 이번에 다시 확인, 헷갈리기 쉬운 자리)
  `gl.readPixels()`로 같은 땅 좌표를 실측: mood 0일 때 rgb(104,102,89)
  대비 mood 0.6일 때 rgb(80,79,67)로 **23~25% 어두워지는 것**을 수치로 확인
  (하늘 픽셀은 두 경우 다 143,182,216으로 안 갈렸는데, 지금 카메라
  각도에서 그 좌표가 스카이돔 상단 고정색을 잡는 것으로 보인다 — 땅
  픽셀이 이미 결정적 증거라 더 파지 않음).
- **사람 사건 몸짓** — `hurt_soldier`(코르비날)는 확대 스크린샷에서
  앞으로 굽은 자세, `road_merchant`(마그나로)는 팔을 든 인사 자세로
  뚜렷이 갈리는 것을 눈으로 확인.
- **뒷정리**: `event.close()`·`forceTime(null)`·`weather.force(null)`·
  카드 스타일 원복까지 전부 되돌림. `party`(등 이전 세션이 남긴 상위
  다섯 명 그대로)는 안 건드림 — 세이브에 흔적 없음.

**결론**: 넷 다 여전히 정상 — 최근 변경(초상 3D·도적전 배율)이 이
자리를 깨지 않았다. "비전투 이벤트 3D 무대 연출"은 계속 마무리 상태.

## 2026-09-07 (같은 날 더 이어서, 세 번째) — "강타 회피 배율도 같이" → 더 조였다가 자가진단이 막은 자리 발견, 제약 지키며 마무리

바로 아래 절(원거리 배율)에 이어 사용자가 "강타 회피 배율도 같이 손봐줘"로
요청 — 1차 세션이 "여전히 charge보다는 쉽게 남겨 뒀다"(비율 heavy≈2.0·
sweep≈2.15 vs charge≈1.14)고 적어 둔 것을, charge와 같은 비율(≈1.14)까지
마저 조이려 했다.

**막힌 자리**: heavy를 비율 1.14로 맞추려면 반경을 8.8m(charge와 같은
자리)를 넘겨 10.7m까지 올려야 하는데, 그러면 `_test.html`의 "적 큰
공격은 두 번째부터 다른 패턴으로 바뀐다"가 **깨진다** — 이 시험이
`m1.range > m0.range`(돌진 반경이 강타 반경보다 커야 한다)를 직접
확인하고 있었다(헤드리스 3회 457/458로 결정적으로 잡힘, 우연한 흔들림
아님). `tellSec`가 charge(0.825초)보다 훨씬 긴 heavy(1.1초)를 charge와
똑같이 타이트하게 만들려면 반경도 그만큼 더 커져야 하는데, 그러면 이
순서가 뒤집힌다 — **반경 하나로는 "회피 난이도를 맞춘다"와 "돌진이
강타보다 더 멀리 물러서야 한다"를 동시에 만족 못 한다**는 걸 자가진단이
잡아 준 것. 이 순서는 우연이 아니라 처음부터 의도된 자리로 보고 그대로
지켰다(사용자에게 새로 확인받지 않음 — 자가진단이 명시적으로 걸어 둔
불변식이라 판단).

**고친 값**: heavy 1.35→**1.55**(비율 2.0→1.59, 반경 8.525m — charge의
8.8m보다 여전히 좁게), sweep은 이 제약이 없어(다른 시험이 순서를 안 봄)
1.55→**2.45**(비율 2.15→1.13, charge 수준까지)까지 그대로 밀었다.
`js/rogue-action.js`의 `foeMoves()` 주석에 수식·근거·이번에 밟은 제약을
전부 남겼다.

**검증**: 처음 값(heavy 1.95)으로 헤드리스 3회 **457/458 동일**(결정적
회귀 확인) → heavy를 1.55로 낮춘 뒤 3회 **458/458**로 복구. 이 값들도
여전히 사람이 걸으며 판단할 몫이다(환경 제약 그대로) — `_admin.html`의
`rogueAction.heavyRange`(공통 배율)로 전체를 한 번에 낮출 수 있다.

## 2026-09-07 (같은 날 더 이어서) — "이어서 개발해" → 남겨 둔 두 후보 중 원거리 배율도 수식으로 고침

바로 아래 절이 남긴 두 후보(원거리 배율·강타 회피) 중 강타 회피는 그 절에서
이미 고쳤다. **원거리 배율**(`rogueAction.rangedAtkMul`)도 이번에 같은
방식(감이 아니라 수식으로 근거를 만든다)으로 손댔다 — 실시간 조작 자체가
이 환경에서 안 되는 건 여전해(`document.visibilityState` 항상 hidden) 이번에도
"완전히 부족했던 것"만 없앴을 뿐 최종 손맛은 아니다.

**계산**: `js/duel.js`·`js/rogue-action.js`의 상수로 60초(`duel.timeSec`)
동안 낼 수 있는 총 피해를 어택(myAtk) 배수로 잡으면 — 속공(쿨다운 0.35초,
연타 5타마다 회심 1.8배 평균 반영)은 60초에 **myAtk×20** 안팎인데, 원거리
(쿨다운 3.5초, 배율 0.16)는 **myAtk×2.74**뿐이었다. `js/rogue.js`가 애초에
받아 주는 교전의 상한이 `pw.atk*7 < rg.hp`면 거부(두목=2등급 기세가 이
비율에 자주 걸린다, `RANKS`의 `hpMul` 7)라 **가장 흔한 상대조차 원거리
만으로는 시간 안에 이론상 못 끝내는 배율**이었다는 게 지난 세션이 실전에서
겪은 것과 정확히 맞아떨어졌다.

**고친 값**: `rogueAction.rangedAtkMul` 0.16 → **0.42** (60초 총 피해
myAtk×7.14로, 상한 myAtk×7을 겨우 커버). 속공(myAtk×20)보다는 여전히
훨씬 느려 "다가가서 때리라"는 기존 설계 의도(지난 세션이 "의도된 설계로
보인다"고 남긴 것)는 그대로 살렸다 — 원거리만으로 버티는 것도 이제
이론상 가능해졌을 뿐, 여전히 접근전이 압도적으로 빠르다.

**검증**: 자가진단 헤드리스 3회 458/458 동일(회귀 없음 — 시험들이
`.dmg > 0`만 보고 절대값을 안 걸어 배율 변경에 안 걸림). 이 값도
**감으로 다듬어야 하는 자리**인 건 여전하다(수식은 "산수로도 안 되던 것"을
없앤 것뿐) — 다음 세션 또는 사람이 실제로 원거리만 눌러 걸어 보고
`_admin.html`의 `rogueAction.rangedAtkMul`로 더 조정할 것. 강타
회피(`heavyRange` 등)도 마찬가지로 실감 확인이 아직 남아 있다(바로 아래
절 참고) — **다음 세션이 이어갈 자리는 이 둘의 실기 확인**이다.

## 2026-09-07 — "완성도 올리는 게 급하다" → 도적전 강타·휩쓸기 회피 거저였던 것 고침(수식 근거, 실감 확인은 아직)

야생동물 도감 3D(82124bb까지)는 사용자가 "실기기 확인 됐다"고 확인. 이어서
"완성도"를 어디부터 올릴지 묻자 "1(전투 손맛)·2(콘텐츠 밀도)·3(그래픽 디테일)
모두" 로 답함. 셋을 다 훑어본 결과:

- **콘텐츠 밀도(2)·그래픽 디테일(3)** — 기존 로그를 다시 훑어보니 이미 다
  코드로 들어가 있었다(사건 빈도·걷기 흥·소품 3D·NPC 다양성·비전투 무대 등,
  전부 지난 세션들이 끝냄). `placeActor()`가 모든 배우·소품을 `groundY()`로
  이미 지형에 붙이고 있어 "소품이 붕 뜨는지" 우려도 코드상 근거 없음(구조
  확인만, 화면 재확인은 안 함). 이 두 축은 손 댈 명확한 거리를 못 찾았다 —
  다음에 구체적 불만이 나오면 그때 다시 팔 것.
- **전투 손맛(1)** — `js/rogue-action.js`의 `foeMoves()`(도적전 실시간 전투
  강타 셋)를 계산해 보니 '강타'·'휩쓸기'는 걷기 속도(8m/s)×예고시간이 반경보다
  훨씬 커서 **가만히 서 있다가 아무 방향으로나 한 걸음만 떼도 항상 피해지는**
  상태였다(회피 난이도 비율 ≈3.5, 사실상 무조건 회피). '돌진'만 비율 ≈1.14로
  실제로 타이트하게 잘 맞춰져 있었다. heavy·sweep의 반경 배수를 1(암묵)·1.15 →
  1.35·1.55로 올려 셋의 회피 난이도를 얼추 맞췄다(비율 ≈2.0·2.15 — 반응은
  필요하지만 여전히 charge보다는 쉽다). 예고 시간(`tellSec`)은 안 건드렸다 —
  다 줄이면 반응 자체가 불가능해지는 다른 실패 모드라 범위를 반경 쪽으로만
  좁혔다. **이 숫자도 결국 감으로 맞춰야 하는 값이라는 건 여전하다** — 수식은
  "완전히 공짜"였던 상태를 없앤 것뿐, 최종 손맛은 이전 세션들이 남긴 방식대로
  사람이 직접 걸으며 확인하고 `_admin.html`의 `rogueAction.heavyRange` 등으로
  더 조정할 것.

**검증**: 자가진단 헤드리스 4회(457/457) 동일 — 순수 배수 변경이라 회귀 없음
(테스트들이 `HEAVY_RANGE()` 상대 배수로 "충분히 멀다"만 확인해 절대값 변화에
안 걸림). 실시간 손맛 자체는 이 환경(`document.visibilityState` 항상 hidden)
에서 확인 불가 — 다음 세션 또는 사용자 실기기가 걸으며 느껴 볼 차례.

## 2026-09-06 (같은 날 더 이어서, 세 번째) — "이어해"는 개발을 이어가란 뜻이었다 → 초상 3D 전환 완료

바로 위 절이 "이어해"를 실제 플레이로 잘못 알아들은 것 — 사용자가 "게임을 하라는
말이 아님, 개발은 이어해"로 바로잡았다. 그 절이 발견해 둔 다음 후보 중 **사건 카드
초상 → `portrait3d.js` 전환**을 실제로 끝냈다.

**한 일**: `js/portrait3d.js`에 `img(kind, ref, size, cls)`·`tag(kind, ref, w, h)`
두 함수를 새로 추가해 "그림은 `sprite.portrait`로 시작하고 `data-p3` 이름표를
붙이면 모델이 다 구워지는 대로 `sweep()`이 `src`를 갈아 끼운다"는 자리를 한
곳으로 모았다. 여태 `ui.js`의 `pt()`(도감 목록)만 이 자리를 썼고, **나머지
전부** — `event.js`(비전투 사건, 애초 발견 지점)·`encounter.js`(등용·포획
카드)·`fort.js`(성채·토벌)·`rogue.js`(적도 카드)·`letter.js`(천거장)·`talk.js`
(주민·짐승 말 걸기)·`duel.js`(구식 교전 카드) — 는 `sprite.portrait()`로 캔버스
그림만 그리고 있었다. 이 여덟 자리를 전부 `portrait3d.img()`로 바꿨다. 덤으로
`rogueAction.open()`에 건네던 `portrait:` 필드 셋(`encounter.js`·`fort.js`·
`rogue.js`)이 실제로는 **한 번도 읽히지 않는 죽은 값**이었다는 것도 찾아 지웠다
(`rogue-action.js`의 HUD는 애초에 초상 없이 막대·버튼뿐이고, `duel.js`쪽 되돌아가는
길도 이제 `stage3d`를 우선하므로 `portrait`는 완전히 안 쓴다).

**진단하다 발견한 진짜 버그**: `ui.js`의 `p3tag()`가 **hero만** 이름표를 붙이고
있었다 — `portrait3d.js` 자신은 2026-09-05부터 펫도 굽는데(주석에 그렇게
적혀 있다), 그 호출 자리만 안 따라갔던 것이다. 함수를 `portrait3d.img()`로
합치면서 이 제약도 같이 풀렸다 — 이제 목록 초상도 펫이 3D로 갈아 끼워진다.

**자가진단이 잡아낸 두 가지, 둘 다 고쳤다**(452/457 → 457/457, 세 번 돌려 결정적
확인):
1. `ui.js`를 고치며 옛 `p3tag()`를 지우다 도감 상세 화면(150×172, 정사각이 아니라
   `img()`를 못 쓰는 자리)이 그 함수를 직접 부르고 있던 걸 놓쳤다 —
   `portrait3d.js`에 `tag()`(이름표만 내는 낮은 층)를 새로 두고 `ui.js`의
   `p3tag()`를 그 위임으로 되살렸다.
2. `_test.html`의 "배우 — 조립하면 부품이 실제로 선다"가 흔들렸다 — 원인은
   내 코드가 아니라 **이 전환 자체가 만드는 새 타이밍**이었다: 카드마다 초상을
   구우려는 시도가 늘면서 `portrait3d`의 굽기 줄(`pump()`)이 이전보다 훨씬 자주
   돌고, 그러다 이 진단이 쓰는 관우·청룡의 GLB가 **진단이 직접 `A3.build()`를
   부르기도 전에** 이미 캐시돼 도형(부품 여럿)에서 실제 GLB(청룡은 부품 7개뿐 —
   17개이던 도형보다 훨씬 적다)로 넘어가 있었다. 두 상태 다 "실제로 섰다"는
   같은 뜻이므로, 문턱을 도형/GLB 어느 쪽이든 통과하게 낮추고 관절 확인도
   `rig`(도형)·`mixer`(GLB) 어느 쪽이든 인정하게 고쳤다. **직접 겪어 보고서야
   보인 결합** — 초상 갈아 끼우기가 다른 자리의 배우 조립 타이밍까지 앞당길 수
   있다는 뜻이니, 다음에 비슷한 "부품 수를 딱 맞춰 재는" 진단을 새로 쓸 때
   참고할 것.

**다음 후보 둘은 조사만 하고 손 안 댔다 — 숫자를 지어낼 근거가 없었다**:
- **도적전 원거리 배율**(`rogueAction.rangedAtkMul` 0.16)이 낮아 원거리만으로는
  제한 시간(60초) 안에 못 이긴다 — 이번 세션 실제 플레이에서 직접 겪었다(절반도
  못 깎고 물러남). 다만 속공(`duel.quickMul` 0.10, 쿨다운 0.35초)이 원거리
  (쿨다운 3.5초)보다 압도적으로 빠른 것은 "다가가서 때리라"는 **의도된 설계**로
  보인다 — 이게 버그인지 그냥 "원거리는 보조일 뿐"인 설계인지는 사람이 직접
  걸으며 붙어 봐야 가른다.
- **강타 회피가 거저다**(`rogueAction.heavyRange` 5.5m, 예고 `duel.tellSec`
  1.1초, 걷기 속도 8m/s → 1.1초에 8.8m까지 벗어날 수 있다) — 다만 `heavyRange`를
  올리면 파생값(돌진 ×1.6·휩쓸기 ×1.15)도 같이 밀려 올라가 세 강타의 상대적
  균형이 통째로 바뀐다(예: 돌진 반경을 12m로 올리면 그 예고 시간 0.825초·
  속도 8m/s로는 **아예 못 피하게** 된다). 셋이 묶여 튜닝된 값이라 하나만
  섣불리 올리면 다른 둘이 깨질 수 있다 — **직접 걸으며 회피해 보고 감으로
  맞춰야 하는 자리**(이 환경은 `document.visibilityState`가 늘 `hidden`이라
  실시간 조작 자체가 안 된다, 여러 세션째 반복 확인). 어드민 손잡이
  (`rogueAction.heavyRange` 등 6개)로 바로 만질 수 있으니 다음 세션이 사람과
  같이 붙어서 감을 잡을 것.

**커밋·푸시 완료**(`67cc6bb`, `main`) — 초상 3D 전환은 끝났고 자가진단
457/457(세 번 결정적). Mesh2Motion 파이프라인(아래 7번)도 오늘 앞선 세션이
이미 끝내 둔 걸 이 세션이 확인만 했다.

**다음 세션이 여기서 이어갈 것**: 이 절 바로 위 "도적전 원거리 배율·강타
회피" 두 가지 — 둘 다 조사는 끝났고 정확한 원인·수치도 여기 적혀 있으니,
사람이 실제로 걸으며 회피해 보고 감으로 숫자를 정하기만 하면 된다
(어드민 `rogueAction.*`·`duel.*` 손잡이로 바로 조정, 코드 수정 불필요 —
느낌이 정해지면 그 값을 `core.tuned()` 기본값으로 옮겨 적을 것).

## 2026-09-06 (같은 날 더 이어서, 두 번째) — "사가고 이어해"로 실제 세이브 계속 진행

이전 절(바로 아래)에서 끊긴 자리부터 이어 플레이 — claude-in-chrome으로 `index.html`을 열어
사명 3개(성채 점령·길 걷기·역참 들르기)를 먼저 거두고, `window.__simTick(n)`(매 틱마다
`auto.update`→`world.update`→`letter/buddy/fort/npc/animal/event/ambient/milestone/codex.tick`을
그대로 도는 헬퍼를 콘솔에 심어 둠)으로 자동 순행을 밀어붙였다. 사건 카드(부상병·사라진
아이·행상·희귀 약초)는 뜰 때마다 스크린샷으로 보고 손으로 골랐다.

**결과 (금 6,637 → 12,717, Lv.19 그대로, 걸음 7.36만m)**:
- 사명 추가 완료 — "짐승을 포획한다"(4/4). 인장 진행 계속.
- ★5 인물 다수 등용(월영·훈민·의조·궁성·발레리안·카시안더·주작 등 포함) — 인물
  도감 **51/70**, 짐승 도감 **41/41(꽉 참)**.
- 도적전 두 번 마주침 — 첫 번째는 실시간으로 원거리(견제기)만 계속 눌러 싸워 봤으나
  (아래 발견 참고) 절반 못 깎고 **물러났다**(금 -70, 소모전 손해 없음). 두 번째는
  그냥 **길세를 치렀다**(금 -45) — 시간 대비 이득이 없어서.

**새로 확인한 것 — 도적전(`rogue-action.js`) 실시간 손맛, 자동화 탭에서 실제로 겪어 봄**:
`rogueAction.ranged()`는 사거리를 안 보고 쿨다운(3.5초)만 보므로 자동화 탭에서도 안전하게
계속 누를 수 있다 — 하지만 배율이 낮아(`RANGED_MUL` 0.16) 원거리만으로는 도적 전체 기세를
빼는 데 아주 오래 걸린다(60초 제한 안에 못 끝낼 정도). `MY_REACH`(3.0m) 안에 실제로
붙어야 나가는 속공(`QUICK_MUL` 0.10, 하지만 쿨다운이 짧고 연타 5타마다 회심 1.8배)이
진짜 딜의 대부분인데, 이 세션은 플레이어를 적에게 실제로 붙이는 조작(조이스틱 없음 —
`world.setStick`/키보드 조작이 필요해 보임)을 안 써 봤다. **다음 세션이 도적전을 자동화
탭에서 이기려면**: ① 원거리만으로는 시간 안에 못 이긴다는 걸 전제하고 ② 실제로 붙는
법을 찾거나(키보드 WASD가 `world.js`에 걸려 있으니 시도해 볼 것) ③ 아니면 그냥 길세를
치르거나 물러나는 쪽이 시간 대비 낫다. `차지 일격`은 사거리(4.2m) 밖이면 그냥 헛치고
소모만 된다 — 붙지 않은 채로 누르면 손해다.

## 2026-09-06 (같은 날 더 이어서) — 실제 세이브로 "성채를 점령한다" 사명 진행, 새 발견 하나 남기고 세션 종료

사용자가 "이어서 해줘"로 실제 플레이(제후→패왕, Lv.15→19) 진행을 요청 — claude-in-chrome으로
`index.html`(진짜 세이브)을 열어 콘솔로 `world.update`/`auto.update`를 수동으로 여러 번
돌리는 방식(rAF가 자동화 탭에서 잘 안 도는 문제 우회)으로 실제 이동·전투·사건을 진행했다.

**한 일 (전부 정상 플레이 흐름, 세이브 직접 조작 아님 — `walkTo`·`fort.challenge()`·
UI 버튼 클릭만 사용)**:
- 성채 둘 점령 — 형주 성채(조선)·한성 성채(로마, 첫 시도는 패배 후 파티를 능력치 상위
  다섯 명(`sg_ganning`·`sg_luxun`·`kr_gwanggaeto`·`kr_choemuseon`·`kr_yisunsin`)으로
  바꿔 재도전해 승리) — "성채를 점령한다" 사명 2/2 완료
- 그 사이 도적전 여러 번(전승), ★5 포함 인물 여러 명 등용, 부상병·상인·아이 등
  비전투 사건 다수, "역참에 들른다"·"길을 걷는다" 사명도 완료
- **세션을 끝내며 정리**: `auto.setOn(false)`로 자동 순행 끄고 `core.persist()`로 저장.
  파티는 방금 바꾼 상위 다섯 명 그대로 남겨 뒀다(원래대로 되돌리지 않음 — 실제 진행이지
  테스트가 아니라서).

**새로 발견한 것 (다음 세션 후보)**: 사용자가 "아직 스프라이트가 있는 것 같아?"로
지적 — 확인해 보니 **비전투 사건 카드 초상**(부상병·상인·사라진 아이 등, `js/event.js`
550행 `portraitImg`)이 아직도 `js/sprite.js`의 `portrait()`(캔버스에 코드로 직접
그리는 옛 방식)를 쓰고 있다. 실사 3D 모델을 오프스크린 렌더링해 굽는 `js/portrait3d.js`는
`js/ui.js`(도감 등)에서만 쓰이고 `event.js`는 안 걸려 있다 — 부록 방침("코드로 그리지
말고 에셋으로")이 아직 이 자리까지는 안 미쳤다는 뜻. 사용자에게 바로 손댈지 물었으나
"새로운 세션에서 이어 할게"로 이번 세션은 여기서 마무리 — **다음 세션이 원하면 이
자리(`event.js`의 `portraitImg` 계산)를 `portrait3d.js`로 갈아 끼우는 작업부터
시작할 것.**

## 2026-09-06 (같은 날 더 이어서) — "테스트만 하지 말고 다른 진행 사항을" 요청 → Mesh2Motion 실사 인물을 실제로 게임에 물림

바로 아래 절들(비전투 이벤트 3D 연출)이 검증만 반복되는 것을 사용자가 지적
("자꾸 산으로 간다"가 아니라 이번엔 "자꾸 테스트만 한다") — 코드 변경 없는
"확인" 커밋이 여러 번 이어진 뒤라, 이번엔 실제로 새 콘텐츠를 넣었다.

`assets/_wip/vitruvian-test/`(2026-09-05, "검증만 하고 게임엔 안 물림" 상태로
남아 있던 Mesh2Motion 실사 인물 시험판)를 실제로 `HERO_RECIPES`에 넣었다:

- `assets/models/people/vitruvian/vitruvian_v1.glb`로 정식 배치, `js/asset3d.js`에
  `{key:'vitruvian_v1', body, anim: body}`(QRPG와 같은 결 — 제 클립 6개
  Fighting_Idle·Walk·Run_Female·Sword_Attack·Hit_Chest·Death_A가 `mapClips()`
  키워드에 그대로 걸려 리타깃이 필요 없다) 한 줄만 추가 — 인물 26→27벌.
  기존 QRPG·MPFB 파이프라인·코드를 하나도 안 건드렸다(순수 추가).
- `_test.html`의 "인물 스물여섯 벌" 하드코딩 테스트를 27로 갱신(새 시험 추가
  아님, 개수만). 헤드리스 3회 457/457 동일 — 회귀 없음.
- `assets/ASSET_LICENSES.md`에 출처(Vitruvian Project CC0 + Mesh2Motion
  MIT/CC0)·알려진 흠(다리 피부가 African이 아니라 범용 Utility 텍스처라
  몸통보다 밝다)을 적었다.
- **실기기(claude-in-chrome)로 화면 확인까지 끝냄** — `_demo.html#people-3d`가
  세우는 여섯 표본 중 `eu_napoleon`이 `oneOf()` 해시로 마침 이 새 레시피를
  뽑는다는 것을 미리 계산해(`h(id)%27===14`) 찾아 확인. 디버그 오버레이에
  `vitruvian_v1:glb h1.00 ... 아이10 [Skinned_Mesh_0..9]`로 정상 로드가 찍혔고,
  카메라를 돌려 실제로 다른 다섯(QRPG·MPFB)과 나란히 선 것·**문서화된 다리
  색조 불일치가 화면에서도 실제로 보이는 것**을 스크린샷으로 확인했다(둘 다
  예상대로 — 코드 버그가 아니라 알려진 텍스처 공백 때문).
- 세이브는 안 건드림(`_demo.html`은 애초에 실제 세이브를 안 읽는 스크린샷용
  화면이고, `party`·자원 등 아무 필드도 안 만졌다).

**남은 것**: 다리 피부색 보정은 여전히 안 했다 — 원본 자료 위치는
`assets/_wip/vitruvian-test/README.md`에 있다. 이 파이프라인(관절 수작업)으로
더 뽑을지는 다음에 필요할 때 판단.

### 2026-09-06 (같은 날 더 이어서) — "응 해줘"(다리 피부색 보정) 요청 → 텍스처 자체를 고침, Photoshop/Blender 없이

포토샵·블렌더가 이 환경에 없어서 Node(`jimp`, 이번에 새로 설치)로 GLB
바이너리를 직접 수술했다.

- **원인 재확인**: `Legs_BaseColor` 평균 RGB `(200,152,131)` vs `Torso`+`Arms`
  평균 `(82,50,38)` — 다리가 몸통보다 훨씬 밝고 붉은 범용(Utility) 텍스처였다.
- **고친 방법**: 다리 JPEG에 채널별 곱 스케일(`r×0.409·g×0.331·b×0.294` —
  목표/원본 평균의 비율, 어둡거나(휘도<12) 밝은(>248) 이상치 픽셀은 평균
  계산에서 뺐다)을 적용 — 세부 음영·주름은 그대로 두고 전체 톤만 Torso·Arms
  쪽으로 옮기는 색 전이(color transfer)다. 보정 뒤 평균이 `(81,50,38)`로
  목표에 거의 정확히 맞아떨어지는 것을 수치로 확인.
- **GLB 재조립**: glTF BIN 청크의 bufferView 544개 중 Legs 이미지가 **맨
  마지막**(오프셋이 파일 끝과 정확히 일치)이라는 걸 먼저 확인하고 나서
  손댔다 — 그래서 이미지 바이트 길이가 바뀌어도(48908→65274바이트) 다른
  bufferView 오프셋을 하나도 다시 계산할 필요가 없었다. JSON 청크는 4바이트
  경계까지 스페이스(0x20)로, BIN 청크는 0x00으로 다시 패딩하고 헤더의 전체
  길이·버퍼 길이를 갱신해 재조립했다.
- **검증**: ① `glb-inspect`로 뼈대(28본×10스킨)·클립 6개가 그대로인 것 재확인
  ② 색 보정 전/후 다리 텍스처를 PNG로 뽑아 **직접 눈으로 대조**(수치만 믿지
  않음) ③ `_demo.html#people-3d`에서 실기기(claude-in-chrome)로 다시 렌더 —
  다리와 몸통이 이제 같은 톤으로 보이는 것을 스크린샷으로 확인, `ready:true·
  failed:false`로 로드도 정상.
- **`sw.js`의 VERSION을 5.21.19→5.21.20으로 올렸다**(3D 에셋 바이트가 바뀌었으니
  — CLAUDE.md 검증 습관 절 참고, 안 올리면 이미 연 폰이 옛 GLB를 캐시에서
  계속 받는다).
- **결과가 완벽하진 않다** — 곱 스케일 색 전이라 실제 아프리카 피부의 웜톤
  반사 특성까지 재현하진 못한다(사진 합성이 아니라 근사). 그래도 "몸통보다
  확 밝은 다른 사람 다리"로 보이던 것은 없어졌다 — 눈에 띄는 결함에서
  미세한 톤 차이 수준으로 낮췄다는 뜻이다.

### 2026-09-06 (같은 날 더 이어서) — "실기기로 직접 확인해줘" 재요청 → `_demo.html` 아니라 실제 세이브(index.html)로 재확인

바로 위 검증은 `_demo.html`(스크린샷 전용 무대)이었다 — 사용자가 진짜
세이브(제후 Lv.15)에서도 보고 싶어해 다시 확인했다.

- `DG.core.save.party[1]`을 `eu_napoleon`(해시가 마침 `vitruvian_v1`을 뽑는
  인물)으로 잠깐 바꾸고 `resetActors()`로 실제 탐험 화면에 세웠다.
- 이 시각(밤)이라 화면이 전체적으로 어두워 눈으로만 보기엔 애매했다 —
  `forceTime()`으로 낮으로 돌려 봐도 (아마 태양광 갱신이 다른 주기에 걸려)
  화면이 곧바로 안 밝아져서, **더 확실한 방법으로 바꿨다**: 화면 좌표를
  캔버스 픽셀로 환산해 `gl.readPixels()`로 몸통·다리 자리를 직접 실측 —
  몸통 `rgb(38,0,1)` · 다리 왼쪽 `rgb(26,8,0)` · 다리 오른쪽 `rgb(34,1,6)`
  (배경 `rgb(20,37,2)`과는 뚜렷이 달라 인물을 제대로 짚었다는 것도 확인) —
  셋 다 같은 어두운 적갈색 범위. 조명이 어두워도 **몸통과 다리가 서로
  가까운 값**이라는 게 요점이라 밤이어도 판정엔 지장 없었다.
- 확인 뒤 `forceTime(null)`·`weather.force(null)`·`fov`·`party[1]`
  전부 원래 값으로 되돌렸다(`resetActors()`까지) — 세이브에 흔적 없음.

## 2026-09-06 (같은 날 더 이어서, 세션이 메모리 부족으로 죽은 뒤 이어받음) — 넷 다 코드로 끝났고 커밋함, 실기기 확인만 남음

**바로 아래 항목("진행 중, 아직 커밋 전")을 남긴 세션이 메모리 부족으로 죽었다**
(사용자가 "메모리 부족 해서 종료 되었어"로 알려줌). 이어받아 보니 그 항목이
"오늘 남은 작업 순서"로 남긴 네 가지(동행·펫 팔로우 마무리·원거리 스킬·
비전투 이벤트 3D 연출·NPC 다양성)가 **전부 이미 코드로 들어가 있었다** —
죽기 전에 실제로는 넷 다 끝냈는데 이 파일에 그 뒤 세 개를 적을 새 없이
죽은 것으로 보인다(git diff로 직접 확인). 아래는 이어받은 세션이 한 일이다.

1. **자가진단 헤드리스 3회 — 454/454 동일**(옛 442 + 이번 죽은 세션이 이미
   심어 둔 새 시험 12개: 원거리 견제기 1 · 비전투 사건 초상 1 ·
   NPC 겉모습 다양성 1, 그리고 그 사이 다른 곳에서 늘어난 것들). 회귀 없음.
2. **실기기(claude-in-chrome) 확인을 시도했으나 확장이 연결 안 됨** — 아마
   메모리 부족으로 크롬(또는 확장)이 같이 죽은 것으로 보인다. 사용자에게
   물어 **"지금은 코드 정리·커밋만, 실기기 확인은 다음"** 으로 진행 방향을
   정함. **다음 세션(또는 크롬 재연결 뒤)이 아래 넷을 실제 화면으로 확인할
   차례**:
   - 동행(부대 2번째)·펫이 탐험 중 뒤에서 따라오고, 실전 중엔 좌우로 갈라
     서서 같이 공격 몸짓을 내는지 (`world3d.js` `syncBuddies`, `actorPos('ally')`/
     `actorPos('petbuddy')`로 좌표 찍어볼 수 있다)
   - 도적전·야생조우 등 실시간 전투에 "원거리" 버튼이 늘 눌리고(쿨다운만
     막음) 화살처럼 바람 이펙트가 나가는지 (`rogue-action.js` `ranged()`)
   - 상인·부상병 같은 비전투 사건 카드에 이제 이모지 대신 실제 인물 초상이
     뜨고, 3D 화면에도 그 사람이 서는지 (`event.js` `npcVisual()`)
   - 같은 세력·기질 NPC들이 무기·투구·색조가 갈려 덜 복제인간처럼 보이는지
     (`sprite.js` `ruleLook()`, `actor3d.js` 세력색 명도 흔들기)
3. **커밋함** — 위 넷 + 이전 세션의 자세·HUD 버그 수정까지 한 번에.

### 2026-09-06 (같은 날 더 이어서, 크롬 재연결 뒤) — 넷 다 실기기로 확인 끝, 이상 없음

사용자가 "진행 순서대로 해줘"로 요청 — claude-in-chrome이 다시 연결돼 바로
위 절이 남긴 체크리스트 넷을 **실제 세이브**(제후 Lv.15, 격리 test 키 아님)로
확인했다. 코드는 하나도 안 고쳤다(순수 재확인).

1. **동행 팔로우** — `actorPos('ally')`가 `actorPos('me')`와 실제로 붙어 있는
   좌표를 냄(탐험 중 ~2.4 거리로 트레일링). 스크린샷에서도 두 사람이 나란히
   서 있는 것을 직접 봄. **펫은 이 세이브가 장착 중인 펫이 없어서(`petEquip`
   비어 있음) 화면 확인은 못 함** — 코드 경로는 `syncBuddies`가 `allyId`와
   같은 조건으로 다루므로 다음에 펫 장착한 세이브(또는 스크립트로 장착)로
   확인할 것.
2. **원거리 견제기** — `DG.rogueAction.open()`을 직접 불러(세이브 안 건드림)
   HUD에 "원거리" 버튼이 처음부터 활성 상태로 뜨는 것, 눌렀을 때 바람
   이펙트(하얀 줄무늬)가 나가고 버튼이 "원거리 (3.0)"으로 바뀌어 쿨다운
   동안 비활성화되는 것을 스크린샷으로 확인. 이 도적 HUD 게이지 바(사기·
   빈틈·차지)가 **처음으로 실제 폭을 갖고 화면에 뜨는 것도 같이 확인**
   (앞서 고친 CSS `flex:1` 버그가 실제로 해결됐다는 뜻).
3. **비전투 사건 초상** — `DG.event.force('road_merchant')`로 상인 사건을
   직접 열어 이모지 대신 실제 인물 초상(모자·수염 있는 스프라이트)이 카드에
   뜨는 것을 확인. "지나간다"로 닫아 세이브에 흔적을 안 남김.
4. **NPC 겉모습 다양성** — 도감 뒷부분(표에 없는, `ruleLook`을 타는 일반
   인물들) 기질별로 6명씩 뽑아 무기·투구가 실제로 갈리는 것을 콘솔로 확인
   (예: might 기질 안에서도 sword/spear/halberd/bow가 섞여 있음, wisdom도
   staff/scroll/brush 섞임). `actor3d`의 세력색 명도 흔들기는 export된 이름이
   달라(`spec()`가 다른 용도) 이번엔 콘솔로 직접 못 짚었다 — 헤드리스
   자가진단이 이미 값으로 확인했으므로 급하지 않다.

**세이브 안전 확인**: 테스트 도중 건드린 것은 `party`(안 바뀜, 확인함)뿐이고
`rogueAction.open()`은 애초에 세이브를 안 건드리는 화면 층, 상인 사건도
보상 없는 "지나간다"로 닫아 실제로 벌거나 잃은 것이 없다.

**남은 것**: `actor3d`의 세력색 명도 흔들기 화면 확인만 남았다(급하지 않음).

### 2026-09-06 (같은 날 더 이어서) — 펫 팔로우도 실제 장착해서 확인함, 동행·펫 팔로우 전부 끝

사용자가 "펫도 장착해서 팔로우 확인해줘" → "모든 단계 진행해줘"로 요청.
이 세이브가 이미 보유한 펫(`dex.pets` 확인 — 진돗개 `pt_jindo` 43마리)을
선두 인물(`party[0] = sg_ganning`)에 `petEquip[sg_ganning] = 'pt_jindo'`로
직접 장착해(정상 UI 흐름과 같은 조작, 자원 소모 없음) 확인했다.

- **탐험 중**: `actorPos('petbuddy')`가 실제로 `me`·`ally` 옆에 붙어 있는
  좌표를 냄. 스크린샷을 확대해 진돗개 모양(주황빛 네발짐승)이 플레이어·
  동행 바로 뒤에서 따라오는 것을 직접 봄.
- **실전 중**: `rogueAction.open()`으로 다시 교전을 열어, 펫이 `ally`와
  **반대쪽**으로 갈라서는 것을 좌표로 확인(펫 x=-263.12 · 아군 x=-262.05 ·
  나 x=-262.56 — `sideX` 부호가 반대라 서로 다른 쪽에 선다는 설계와 일치).
- **뒷정리**: 확인 뒤 `delete petEquip[sg_ganning]`로 장착 전 상태({})로
  되돌림. `party`·자원 등 다른 값은 안 건드림 — 세이브에 남은 흔적 없음.

이걸로 지난 세션이 "오늘 남은 작업 순서"로 남긴 넷(동행·펫 팔로우 · 원거리
견제기 · 비전투 사건 초상 · NPC 다양성) 전부 코드+실기기 확인까지 끝났다.
**남은 것은 `actor3d` 세력색 명도 흔들기 화면 확인 하나뿐**(급하지 않음).

### 2026-09-06 (같은 날 더 이어서) — 세력색 명도 흔들기도 확인 끝, 이번 라운드 넷 전부 마무리

사용자가 "actor3d 세력색 명도 흔들기도 화면으로 확인해줘"로 요청.

- **숫자로 먼저 확인**: `DG.actor3d.spec('hero', h).color`는 두 인자
  (`kind, ref`)를 받는데 지난번엔 한 인자로 잘못 불러 늘 `null`이 나왔던
  것이었다(고쳐서 재확인). 마을 주민 `npc.js`의 `PEOPLE` 열 명(전부 같은
  가짜 세력 `'하북'`이라 **고치기 전이었다면 완전히 같은 색**이었을 자리)이
  `rgb(88,98,111)`~`rgb(104,113,125)` 사이로 실제로 갈리는 것을 확인.
  `조선` 세력(실제 데이터, 열두 명)도 `rgb(131,86,41)`~`rgb(149,107,63)`
  사이로 갈림 — 설계한 ±9% 범위와 일치.
- **화면으로도 확인**: 동행 슬롯(`party[1]`)에 조선 세력 두 인물(`kr_yisunsin`
  ·`kr_hwanghui` — 색 차이가 가장 큰 양끝)을 순서대로 넣고
  `DG.world3d.resetActors()`(캐시 무효화 — `actorOf`가 키로 캐싱해서 이걸
  안 부르면 인물을 바꿔도 예전 배우가 그대로 남는다는 것도 이번에 배움)로
  다시 세워 스크린샷 두 장을 비교. **눈으로는 거의 안 갈렸다** — 그런데
  이건 버그가 아니라 설계 그대로다: 코드 주석 자체가 "인물마다 명도만
  살짝 흔든다"(±9%, 완전히 흩는 게 아니라 세력 정체성을 지키는 선)라고
  못박아 뒀다. **결론: 수치는 확실히 갈리고, 화면에서 확 튀지 않는 것도
  의도대로다.** 재질 색을 텍스처가 아니라 material.color/vertexColor에서
  직접 읽어 픽셀 단위로 대조해 보려 했으나 이 배우들의 색이 `mat()` 캐시
  안에 있어(직접 `build()`로 따로 지어낸 노드는 다른 재질 인스턴스라
  `getHexString()`이 죄다 `ffffff`로 나옴) 그 경로는 막혔다 — 실제 화면
  (스크린샷)과 `spec().color` 값 대조로 충분하다고 판단하고 멈췄다.
- **뒷정리**: `party[1]`을 `eu_newton`으로, `resetActors()`까지 원래대로
  되돌림. 확인함.

**이걸로 지난 세션이 남긴 "오늘 남은 작업 순서" 넷 전부 코드+실기기 확인이
끝났다.** 다음에 새로 잡을 만한 것은 위쪽(진행 중 절)에 남아 있던 세
가지 미착수 요청(사용자 재확인 필요) — 원거리 스킬은 이번에 넣었으니
제외하고, **비전투 이벤트 3D 연출 심화**(이번엔 초상만 붙였다, 무대
배경까지는 아직) 정도가 다음 후보다.

### 2026-09-06 (같은 날 더 이어서) — 비전투 이벤트 3D 무대 연출 착수: 발견물은 사람 대신 알갱이로

사용자가 "비전투 이벤트 3D 무대 연출 시작해줘"로 요청. 지난 라운드가 넣은
`npcVisual()`(비전투 사건에도 인물 초상·3D 인물을 세움)을 다시 보니 결이
안 맞는 자리가 있었다 — **비문을 읽거나 지도 조각을 캐거나 약초를 캐는데
왜 옆에 아무 인물이 서 있나?** 이 넷(`stone_text`·`map_scrap`·`rare_herb`·
`flood_ford`)은 **사람을 만나는 사건이 아니라 자리·물건을 발견하는
사건**이다. 오늘은 이 결 어긋남부터 바로잡는 것으로 "3D 무대 연출"을
시작했다 — 다음 후보(장터·야영지 같은 배경 소품, 사건별 몸짓)는 아래
"남은 것"에 남긴다.

**바뀐 것 (전부 `js/event.js`·`js/battle3d.js`, 화면 층만):**
- `EVENTS`의 `stone_text`·`map_scrap`·`rare_herb`에 `mood: 'discover'`,
  `flood_ford`에 `mood: 'water'`를 얹었다. 나머지(사람을 만나는 사건·
  전투가 걸리는 사건)는 `mood` 없음 — 옛 동작 그대로.
- `event.js`에 순수 함수 `stageOf(ev)` 신설(자가진단이 화면 없이 물을 수
  있다) — `foe`가 있으면 `foeVisual()`, `mood`가 있으면 **`null`**(아무도
  안 세운다), 그 외엔 `npcVisual()`(옛 동작). `open()`이 이걸로 갈아탐 —
  카드에 붙는 초상도 `stage3d`가 `null`이면 자연히 이모지로 돌아간다
  (`portraitImg` 계산이 이미 `stage3d` 유무를 본다 — 손 안 댐).
- `duel:open` 신호에 `mood`도 함께 실어 보낸다. `battle3d.js`의
  `duel:open` 핸들러가 이걸 보고 처음 뜨는 알갱이 종류를 고른다 —
  `discover`는 금빛(`gold`, "발견했다"를 알리는 색 — 걷기 흥의 "발견"
  토스트·스태거 이펙트와 같은 색 언어를 사건에도 맞췄다), `water`는
  파란 바람(`wind`, 물보라에 가장 가까운 기존 알갱이), 그 외는 옛 흙먼지
  (`dust`) 그대로. **상대를 세우는 `duelStage()` 호출도 `stage3d`가
  `null`이면 안 불린다** — "카드만 뜨는 조우"는 `battle3d.js` 머리말에
  이미 있던 갈래라 새로 만들 것이 없었다(`if (o.stage3d && ...)`가 이미
  그렇게 짜여 있었다).
- `_test.html`에 `stageOf`를 직접 겨냥한 시험 하나 신설 — 발견물 셋·여울이
  전부 `null`인지, 사람 사건(상인)·전투 사건(도적)은 그대로 서는지 같이 본다.

**검증**: 자가진단 헤드리스 3회 455/455 동일(454 + 새 1). 실기기
(claude-in-chrome)로 직접 확인:
- `DG.event.force('rare_herb')` → 카드에 이모지(🌿)만 뜨고 초상 없음,
  `DG.world3d.actorPos('duelfoe')`가 `null`, `DG.battle3d.pool`에서 살아있는
  알갱이 10개가 전부 금빛(`ffe066`, `KINDS.gold`와 일치)인 것을 직접 확인.
- `DG.event.force('flood_ford')` → 알갱이 6개가 전부 파란 바람색(`9fd8f5`,
  `KINDS.wind`)인 것 확인, `duelfoe`도 `null`.
- `DG.event.force('road_merchant')`(사람 사건) → 알갱이 8개가 옛 흙먼지색
  (`7d7264`, `KINDS.dust`) 그대로인 것 확인 — 회귀 없음.
- `DG.event.force('bandit_ambush')`(전투 사건) → 카드에 초상 뜨고
  `actorPos('duelfoe')`가 실제 좌표를 내는 것 확인(사람이 실제로 3D에
  섬) — 이 갈래도 안 건드렸다는 것을 재확인.
- `DG.battle3d.pool` 알갱이는 `life`가 짧아(0.3~0.5초) 스크린샷 왕복 지연
  안에 사그라든다(`clickMark`·레벨업 배너와 같은 사정) — `force()` 직후
  같은 호출 안에서 `pool`을 바로 읽어 색을 확인했다(화면으로 못 잡은
  대신 값으로 확실히 확인).

**남은 것 (다음 세션 참고)**: 이번엔 "안 맞는 사람 세우기"를 없앤 것까지만
— 다음 단계로 갈 만한 것:
1. 발견물 자리에 **간단한 3D 소품**(비석·부서진 기둥·약초 덤불 같은)을
   실제로 세우는 것 — `actor3d.js`에 `kind:'prop'` 같은 새 갈래를 넣어야
   한다(지금은 `hero`·`pet`·`station`·`fort`만 있다). 오늘은 "사람을
   잘못 세우는 것을 없애는" 선에서 멈췄다 — 아무것도 없는 것이 엉뚱한
   사람보다는 낫다고 판단했다.
2. 사람 사건들(상인·부상병·부탁·아이)도 사건마다 다른 몸짓(부상병은
   웅크림, 상인은 손짓 등)을 주면 더 살아날 수 있다 — `actor3d.js`의
   `step()`이 이미 `anim`(attack·hit·dodge)별 자세를 갈라 두는 자리가
   있어 새 갈래를 끼우기 어렵지 않다.
3. `enemy_scout`·`wolf_ring` 같은 밤 사건은 조명·안개를 사건에 맞춰
   어둡게/짙게 하는 것도 후보(지금은 낮밤 조명만 시각에 매여 있다).

### 2026-09-06 (같은 날 더 이어서) — 남은 셋 전부 진행: 소품 실제로 세움·사람 사건 몸짓·밤 사건 조명

사용자가 "남은 거 모두 진행"으로 요청 — 위 세 후보를 전부 코드로 옮겼다.

**1. 발견물에 실제 3D 소품을 세운다.** `actor3d.js`에 `kind:'prop'` 새
갈래 신설 — `hero`·`pet`·`station`·`fort`와 나란히 `shape()`/`plan()`/
`spec()`에 얹었다. **뼈대(rig)가 없다**(`step()`이 `node.userData.rig`
없으면 조용히 넘어가므로 저절로 가만히 선다 — 비석이 걷지 않는 게 맞다).
넷 다 기존 부품 창고(`BOX`·`CYL`·`SPH`·`CONE`, `mat()`/`part()`/`solid()`)
만으로 새 지오메트리 없이 조립했다:
- `shrine`(비문): 밑돌 + 세운 판돌 + 앞면 어두운 새김 자국
- `ruin`(지도 조각): 기둥 밑동 둘(높이 다름) + 옆으로 쓰러진 조각 + 이끼 점
- `herb`(약초): 흙무더기 위에 원뿔 잎 다섯이 부채꼴로(색 둘을 번갈아 써
  단조롭지 않게)
- `flood`(여울): 낮은 물웅덩이(발광 재질) + 튀는 물보라 넷(원뿔)

`event.js`의 각 사건에 `prop`(propType 이름) 필드를 얹고, `stageOf(ev)`가
`ev.prop`이 있으면 `{kind:'prop', ref:{propType, id}}`을 낸다(그 전엔
`null`을 내 아무것도 안 세웠다 — 이번에 "아무것도 없는 것"에서 "그
물건 자체가 서는 것"으로 한 걸음 더 갔다). `mood`(알갱이 색)는 그대로
남아 `prop`과 같이 다니되 뜻은 다르다(하나는 "무엇을 세울까", 하나는
"무슨 알갱이를 터뜨릴까").

**2. 사람 사건에 몸짓을 준다.** `EVENTS`에 `pose` 필드 신설 —
`road_merchant`·`village_ask`·`lost_child`는 `'greet'`(느린 손짓, 반김),
`hurt_soldier`는 `'hurt'`(몸통을 앞으로 웅크림). `duel:open`에 `pose`를
실어 보내고 `battle3d.js`가 상대를 세운 직후 `playAnim(w,'foe',pose,20000)`
를 부른다(카드가 그보다 먼저 닫히면 `duel:close`가 배우 자체를 지운다 —
따로 끊을 필요 없음). `actor3d.js`의 `step()`에 `anim==='greet'`·`'hurt'`
갈래 추가(기존 `attack`·`hit`·`dodge`와 나란히) — 전투 몸짓은 짧고 강한
파형(`sin(t*24)`)인데 이 둘은 **카드가 떠 있는 동안 반복**되게 느린
파형(`sin(t*3)`)을 썼다.

**3. 밤 사건(`wolf_ring`·`enemy_scout`)이 조명을 죽인다.** `EVENTS`에
`eerie: true` 딱지, `world3d.js`에 모듈 변수 `eventDark`(0~1)와 손잡이
`eventMood(v)` 신설. `syncLight()`가 매 프레임 계산하는 태양·하늘빛
세기·안개 거리에 `(1 - eventDark*k)`를 곱한다 — **`lightingAt()`이 내는
값 자체·다른 곳이 보는 캐시는 안 건드리고 적용하는 자리에서만 깎는다**
(미니맵 등 다른 소비자에게 안 새게). `battle3d.js`가 `duel:open`에서
`o.eerie`를 보고 0.6으로, `duel:close`에서 늘 0으로 되돌린다. 헤드리스
자가진단은 `battle3d.live()`가 `DG_NO_DRAW`에서 늘 거짓이라 이 경로를
안 타므로 시각 결정론(mulberry32 시드)에 영향 없음.

**검증**: 자가진단 헤드리스 3회 457/457 동일(455 + 새 2 — 소품·pose·
eerie 필드를 순수 함수로 겨냥한 시험, `world3d.eventMood()` 자체를
겨냥한 시험). 실기기(claude-in-chrome)로 하나하나 확인:
- `DG.world3d.duelStage('prop', {propType:'shrine',...})` 등 넷을 직접
  세워 카드를 화면 밖으로 밀어내고 스크린샷 — **비석**(밑돌+세운
  판돌+새김 자국)·**폐허**(기둥 밑동 둘+쓰러진 조각, 실제로 L자로 얹힘)·
  **여울**(웅덩이+물보라 넷)이 설계한 그림 그대로 뜨는 것을 눈으로 확인.
  **약초**는 화면 중앙 카드에 가려 알아보기 어려웠는데, 카드를 치우고
  확대하니 초록 원뿔 잎이 실제로 서 있는 것 확인.
- 발견물 사건은 `mood`가 있어 첫 알갱이(금빛 등)가 같이 터지는데, 이
  탭이 자동화 중 rAF가 자주 멎는 사정(`../SAGA-HANDOFF.md`에도 있는
  known limitation) 때문에 알갱이가 안 사그라들고 화면에 얼어붙어 있는
  걸 봤다 — **코드 버그가 아니라 이 tool 환경의 사정**이라 판단하고
  `DG.world3d.duelStage()`를 직접 불러(알갱이 없이) 소품만 다시 확인했다.
- `hurt_soldier`를 열어 실제로 배우가 서는 것 확인(정면 화면으로는 rAF
  정지 사정 탓에 자세 차이가 잘 안 보여서), `actor3d.step()`을 `anim:'hurt'`
  대 `undefined`로 나란히 불러 `body.rotation.x`가 0.22 대 0으로 실제로
  갈리는 것, `'greet'`도 팔이 흔들리는 값을 내는 것을 수치로 확인.
- `DG.event.force('wolf_ring')` → `DG.world3d.eventMood()`가 즉시 0.6,
  `DG.event.close()` 뒤 0으로 돌아오는 것 확인(밤 사건 조명 조절 배관이
  실제로 맞물리는 것 확인 — 화면의 실제 어두워짐 자체는 rAF 정지 사정상
  스크린샷으로 대조하지 못했다, `syncLight()`의 곱셈 자체는 코드 리뷰로
  확인).
- 세이브는 안 건드림(전부 `force()`·`duelStage()`·`close()`·`reset()`
  호출뿐, 세이브 필드를 쓰는 함수는 하나도 안 불렀다).

**남은 것**: 소품이 화면에서 살짝 붕 뜨거나 파묻히지 않는지(땅 굴곡
위에서), 밤 사건 조명 죽임의 실제 밝기 체감은 사용자가 실제로 밤에
그 사건을 만나 봐야 확실하다 — rAF 정지 사정 때문에 이 세션은 값으로만
확인했다. 이 정도면 "비전투 이벤트 3D 무대 연출"은 이번 라운드로 일단
마무리 — 더 다듬을 거리가 보이면 다시 사용자와 맞출 것.

### 2026-09-06 (같은 날 더 이어서) — 사용자가 "실기기로 직접 확인해줘" 재요청, 위 항목 화면으로 마저 확인함(가짜 원인 하나 정정)

바로 위 절이 "rAF 정지 사정으로 화면 대조는 못 했다"고 남긴 것을 사용자가
다시 확인해 달라 요청 — 다시 붙잡고 보니 **rAF 문제가 아니라 내가 놓친
CSS 한 줄**이었다.

**밟은 함정과 정정**: 사건 카드를 띄우면 화면 전체가 어둡고 흐리게(블러)
보여 "조명이 심하게 죽었다·rAF가 얼어붙었다"로 오판할 뻔했다. `#encounter`
컨테이너 자체의 `filter` 속성만 확인하고(값 `none`) 안전하다 여겼는데,
실제로 어둡게+흐리게 하는 건 **`backdrop-filter: blur(5px)`+
`background: rgba(6,8,12,.66)`**(다른 CSS 속성, `.enc-card` 자식이 아니라
`#encounter` 자신에 걸려 있다) — **사건 카드가 늘 갖고 있던, 이번 작업과
전혀 무관한 원래 UI**였다. `host.style.background='transparent'` +
`host.style.backdropFilter='none'`으로 지우니 그 아래 3D 장면이 바로
밝고 또렷하게 드러났다. (참고로 도적전·야생조우의 실시간 HUD는
`#rogue-hud`라는 **다른** 오버레이를 써서 이 블러가 없다 — 그래서 이전
세션의 원거리 스킬 확인 스크린샷은 처음부터 밝았다.)

**이 방법으로 다시 확인함(전부 화면으로 직접 봄)**:
- **소품 넷** — `#encounter` 배경을 지우고 비문·지도 조각·약초·여울을
  차례로 세워 확인. 비석(밑돌+세운 판돌)·폐허(기둥 밑동+쓰러진 조각)는
  또렷하게, 여울(웅덩이+물보라)도 이전 세션에 이미 확인됨. 약초는 발견
  알갱이(금빛)가 화려하게 터져 잠깐 가렸는데, 걷혀 가는 사이로 원뿔 잎이
  실제로 서 있는 것을 확인.
- **발견 알갱이** — `rare_herb`를 열자 화면 가득 금빛 반짝임이 실제로
  터지는 것을 두 눈으로 확인(이전 세션엔 rAF 정지로 얼어붙은 알갱이만
  봤었다 — 이번엔 자연스럽게 번지는 것까지 봄).
- **사람 사건 몸짓** — `hurt_soldier`(웅크림)·`road_merchant`(손짓,
  먼지 알갱이도 같이 뜸)를 나란히 확인. 둘의 자세 차이가 스크린샷으로도
  분간된다(부상병은 앞으로 굽어 서고, 상인 쪽은 위로 뻗은 팔이 보인다).
- **밤 사건 조명(`wolf_ring`, eerie)** — 켜진 상태·꺼진 상태(`bandit_ambush`)
  화면을 나란히 비교. **설계한 그대로 "확 티 나지 않는" 수준**이라 압축된
  스크린샷 두 장만으로 눈으로 단정하긴 어려웠다 — 대신 `world3d.render()`를
  직접 불러 강제로 한 프레임을 그리고 `canvas#map3d`의 WebGL 컨텍스트에서
  `gl.readPixels()`로 같은 자리 픽셀을 실측: 같은 조건(밤 고정, 맑음)에서
  `eventMood(0)`일 때 땅 픽셀 `rgb(37,51,2)` → `eventMood(0.6)`일 때
  `rgb(30,42,1)`로 **실제 렌더 결과가 측정 가능하게 어두워지는 것**을
  확인했다(사람 눈보다 더 확실한 증거). `eventMood()`가 사건 시작·종료에
  맞춰 0.6↔0을 오가는 배관도 다시 확인.
- **원인이 rAF가 아니었다는 근거**: 어둡던 화면에서 `document.hidden`이
  `true`였길래 처음엔 "자동화 탭이 백그라운드라 그렇다"고 의심했는데,
  새 탭을 열어도(`hidden` 여전히 `true`) 사건 카드를 열기 **전에는** 화면이
  멀쩡히 밝았다 — `hidden` 자체는 무관했고, 사건 카드가 뜬 **순간부터만**
  어두워졌다는 것이 위 CSS 발견으로 이어진 결정적 단서였다.

**세이브는 이번에도 안 건드림**(`force()`·`close()`·`duelUnstage()`·
`eventMood()`·`battle3d.reset()`만 호출, `#encounter`의 인라인 스타일도
DOM 재렌더로 카드를 닫으면 사라지는 임시 손질이라 되돌릴 것이 없다).

**결론**: 넷(소품·발견 알갱이·사람 몸짓·밤 조명) 전부 화면으로 실제
확인 끝났다. "비전투 이벤트 3D 무대 연출"은 이제 코드·수치·화면 셋 다
확인된 상태 — 다음은 사용자가 실제 플레이하며 느낌을 볼 차례다.

## 2026-09-06 (같은 날 더 이어서, claude-in-chrome 실기기 확인 세션) — 진행 중, 아직 커밋 전

**사용자가 "자꾸 산으로 간다·중간에 누락되고 말을 안 들어서 그렇다"고
명시적으로 지적** — 여러 주제가 한 대화에서 동시에 오가며 다음 세션(또는
같은 세션 뒷부분)이 앞부분 요청을 놓칠 위험이 커서, 이번엔 오간 것 전부를
아래에 **순서대로, 처리 상태까지** 남긴다.

### 이번 세션에서 실제로 한 일 (코드 변경, 아직 커밋 안 됨)

1. **`css/style.css` — 도적전 HUD 게이지 바가 실제로는 안 보이던 CSS 버그
   고침.** `#rogue-hud .ra-row .bar`에 `flex:1`이 없어서 막대 요소의 실제
   폭이 **0**이었다(라벨만 보이고 사기·기·빈틈·차지 막대는 화면에 없었다) —
   claude-in-chrome으로 실제 플레이해 보다가 발견. `getBoundingClientRect()`로
   폭 0 확인 → `flex:1` 한 줄 추가 → 재확인(폭 298px, 색상 있는 막대 실제로
   뜸). **이 버그는 오늘 만든 게 아니라 도적전(rogue-action.js) 최초 커밋부터
   있었다** — 즉 지금까지 도적전에서도 이 게이지들이 안 보였을 것이다.
2. **`js/world3d.js` — 실시간 전투 중 플레이어가 상대를 안 보고 있던 버그
   고침.** `syncActors()`가 `duelFoe`(상대)는 항상 나를 보게 회전시키면서,
   **정작 나(`meA`) 자신은 상대를 보게 하는 코드가 없었다** — `placeActor`의
   기본 회전은 "마지막으로 걸어온 방향"이라, 사거리 안에 서서 자동공격만
   하는 실시간 전투에서는 캐릭터가 적을 등지거나 옆으로 보고 때리는 것처럼
   보였다(사용자가 "때리는 자세도 이상하고 어딜 보고 때림?"으로 지적).
   `duelFoe`가 있으면 `meA.node.rotation.y`를 상대 쪽으로 덮어쓰게 고쳤다.
3. **`js/world3d.js`(`syncBuddies` 신설) + `js/battle3d.js` — 동행(부대
   2번째 인물)·펫(선두 장착)이 실제로 화면에서 따라다니게 함.** 사용자
   요청: "펫도 따라다니고 등용한 인물도 따라 다니고" → "등용한 인물은 같이
   싸우는 거지"(전투 참여도 원함) → "내가 말한 부분은 다 기억해서 해줘".
   - **전투력 계산은 이미 되어 있었다** — `hero.js`의 `partyPower()`가
     `core.save.party` 전체(2번째 인물 포함)를 합산해 `myAtk`/`myDef`를
     내므로, 도적전이든 야생조우든 부대 2번째 인물의 스탯은 **판정에는
     이미 반영되고 있었다.** 안 되어 있던 건 순전히 **화면**(안 보였다)이다.
   - `syncBuddies(pos, meAng, h, now)`: `party[1]`을 `actorOf('ally', 'hero', ...)`
     로, `petEquip[party[0]]`을 `actorOf('petbuddy', 'pet', ...)`로 세운다.
     `followPos()`(신설, lag-lerp)로 매 프레임 목표점에 조금씩 다가가게
     해 순간이동이 아니라 뒤에서 따라오는 느낌을 낸다. 탐험 중엔 내 뒤쪽
     (동행·펫이 서로 다른 좌우 오프셋)으로 처지고, `duelFoe`가 있으면(실전
     중) "나→적" 축에 수직으로 좌우로 갈라서 나란히 선다(내가 강타를
     피하려 물러나도 대형이 안 흐트러지도록 축을 매 프레임 다시 잰다).
   - `js/world3d.js`의 `playAnim(who,...)`에 `'ally'` 케이스 추가
     (`actors['ally']`를 찾는다). `js/battle3d.js`의 `duel:fx`(quick·combo)
     핸들러에서 `anim(w,'me',...)`와 같이 `anim(w,'ally','attack',...)`도
     불러 동행이 같이 치는 몸짓을 낸다(피해 계산은 안 건드림 — 이미
     `myAtk`에 들어 있으므로 몸짓만).
   - **아직 실기기(claude-in-chrome)로 실제 화면 확인 전** — 자가진단
     451/451 3회 동일까지만 확인. claude-in-chrome 연결이 이 작업 중
     여러 번 끊겼다 재연결됐다 했다(원인 불명, 사용자 쪽 문제일 수도).

### 오늘 남은 작업 순서 (사용자가 "계획적으로 단계별로" 요청, 2026-09-06)

1. **동행·펫 팔로우 마무리** — 지금 코드는 있음(위 3번), claude-in-chrome
   실기기 확인 → 발견되는 문제 고침 → 커밋
2. **실시간 전투 원거리 스킬 추가**
3. **비전투 이벤트 3D 연출 추가**
4. **NPC 다양성(색상·의상) 추가**

각 단계 끝날 때마다 이 절의 상태를 갱신할 것 — 다음 세션이 어디서부터
이어받을지 이 목록만 보고 알 수 있어야 한다.

### 논의만 하고 코드는 안 건드린 것 (다음에 이어받을 순서)

사용자가 "전투를 파이널판타지처럼 턴제로 바꾸는 게 낫지 않나"로 시작한
논의 — 범위를 물었더니 **"전부(야생조우·성채·토벌·도적전·등용)"**로 답함.
그런데 이어서 **실시간을 계속 유지하는 쪽으로 논의가 정리됐다**(아래
근거로 내가 반대 추천 → 사용자가 별다른 반박 없이 다음 요청 — 펫 동행 등
— 으로 넘어감. **명시적으로 "그래 실시간으로 가자"라고 말한 적은 없다 —
다음 세션은 이 판단을 다시 확인해도 된다**):
- 이 판의 정체성(포켓몬GO식 오픈월드 + 몬스터헌터 나우식 실시간)이 여러
  세션에 걸쳐 사용자가 직접 확정해 온 방향(`../SAGA-HANDOFF.md` "게임성
  방향" 절)이라 턴제는 그걸 뒤집는 것
- 오늘 드러난 문제(자세·전투가 너무 빨리 끝남)가 실시간 시스템 자체의
  결함이 아니라 디테일(자세는 위 2번으로 고침, 초반 잡몹 체력이 낮아서
  빨리 끝난 것 — `event.foeHpMul`을 임시로 60으로 올려 재현해 보니 강타
  예고·스태거까지 정상 확인됨)이었다
- 턴제 전환은 다섯 곳 전체를 명령어 메뉴 시스템으로 새로 갈아엎는 것이라
  오늘까지 쌓은 스태거·패턴·차지 작업을 통째로 버리는 셈이라 부담이 크다

**아직 코드로 안 옮긴 사용자 요청 셋 (순서 무관, 다음 세션이 순서대로
처리하거나 사용자와 우선순위 다시 맞출 것)**:
1. **실시간 전투에 원거리 스킬 추가** — "실시간 전투는 조금 멀리서 스킬도
   쓰고 하는 게 있어야 멋지지". 지금 필살(ult)은 사거리 제한이 없어
   사실상 이미 "원거리 스킬"이지만, 사용자는 그 이상(스킬 종류·연출)을
   원하는 것으로 보인다 — 구체적으로 뭘 원하는지 다시 물어볼 것.
2. **비전투 이벤트(사건 카드)가 텍스트뿐이라 밋밋함** — "만나는 이벤트도
   글로만 보이면 잼없지". `event.js`의 사건 카드(상인·부상병 등, `foe`가
   없는 경우)는 지금 이모지 + 텍스트뿐, 3D 무대 연출이 없다. `foeVisual()`
   처럼 비전투 이벤트도 3D 배경에 인물을 세우는 식으로 갈지 논의 필요.
3. **NPC 다양성 부족** — "주변 캐릭터들도 다양하지 않네", "색깔별로라도
   넣어주고 옷은 더 없어?". 지금 NPC·플레이어가 다 비슷한 모습으로 보인다
   (스크린샷에서 실제로 확인됨 — 파란 옷 두 명이 거의 똑같이 보임). 색상
   variation부터 시작할지, 의상 종류를 늘릴지 확인 필요.

### 다운로드 폴더 관련 (게임 코드와 무관, 참고용)

사용자가 다운로드 폴더의 "Vitruvian Project" 텍스처(4K/8K EXR, 인체 피부
스캔)를 saga-go/사가블로에 넣어 달라 요청 → 포맷(EXR, 브라우저 미지원)·
용량(개당 30~60MB)·**라이선스 불명확**(재배포 조건 확인 안 됨, 공개 저장소라
위험) 셋 다 문제라고 답함 → 4K zip은 삭제, 8K는 로컬로 PNG 변환(2048px,
`Downloads\Vitruvian-8K-converted\`)만 해 두고 **저장소에는 안 넣었다**.
라이선스 확인 결과도 불명확(제작자 본인도 "최악의 경우 CC-BY" 정도로만
언급, 확정 아님) — 이 텍스처를 게임에 쓰는 건 여전히 보류 상태다.

## 2026-09-06 (도적전 개편 이어서) — 스태거·패턴3종·연타회심·차지일격을 야생조우·성채·토벌에도 옮김

바로 아래 항목이 "남은 것"으로 "이 넷은 도적전에만 있고 duel.js(야생 조우·
성채·토벌)에는 없다 — 사용자가 원하면 그쪽에도 옮길지는 아직 안 물어봤다"로
남긴 것을 사용자가 이어서 옮기라 요청(실기기 확인은 생략해도 된다고 함).

`rogue.js:494`가 이미 쓰던 `global.DG.rogueAction || global.DG.duel` 드롭인
패턴을 나머지 세 곳에 그대로 넣었다 — `rogueAction.open()`이 `duel.open()`과
**완전히 같은 매개변수 모양**(`{title, foeName, portrait?, stage3d?, foeHp,
myAtk, myDef, onDone}`)을 받게 이미 짜여 있어서 새 코드는 거의 없다.

- `js/event.js` `openDuel()` — 한 줄만 바꿈. 이 함수는 이미 `close()`를
  `D.open()` 앞에서 부르고 있어서(옛 코드가 이미 `#encounter` 카드를 스스로
  숨기고 있었다) 추가 손질이 필요 없었다.
- `js/fort.js` `startRaidDuel()`·`startFortDuel()` — 한 줄 바꾼 것 말고,
  `rogue.js`가 먼저 밟았던 함정을 그대로 밟을 뻔했다: `duel.js`는 fort의
  카드와 **같은 `#encounter`** 안에 그려 스스로 덮어써 줬는데, `rogueAction`은
  별도 화면(`#rogue-hud`)을 써서 옛 카드가 안 사라진다. `rogue.js`의 수정을
  그대로 옮겨(`D.open()` 전에 `el.classList.remove('show')`, "한 대도 못
  때리고 물러남" 갈래에서 다시 `add('show')`) 고쳤고, 결과 화면
  (`renderResult`·`renderRaidResult`)에도 `classList.add('show')`를 넣어
  `rogueAction` 경로로 온 경우에도 결과창이 실제로 뜨게 했다.
- `js/rogue-action.js` 머리말을 갱신 — "duel.js는 야생조우·성채·토벌이
  그대로 쓴다"는 문장이 더 이상 사실이 아니라서 셋 다 드롭인으로 이 엔진을
  먼저 쓴다고 고쳤다.

**게임성 변화 — 몰랐다면 놀랄 수 있는 것**: `duel.js`의 회피는 버튼(예고
중 눌러 피함)이었는데 `rogueAction`은 **원 밖으로 실제로 걸어나가야** 피한다
(도적전과 같은 결). 야생 조우·성채·토벌도 이제 같은 손맛으로 바뀐다 —
카드 세 버튼(속공·필살·회피)이 아니라 실시간 위치 전투가 된다.

**검증**: 헤드리스 자가진단 451/451 3회 동일(코드 변경 전과 같은 수 —
새 자가진단은 안 늘렸다, `rogueAction`·`duel` 판정 층 자체는 이미
검증돼 있어서 이번엔 "드롭인 스위치가 맞물리는지"만 코드 리뷰로 확인했다).
**실기기·claude-in-chrome 실제 화면 확인은 사용자 요청대로 이번엔 생략했다**
— 다음에 실제로 플레이할 때 야생 조우·성채·토벌에서도 스태거 금빛 이펙트·
차지 버튼·연타 카운터가 잘 보이는지, `#encounter` 카드가 제때 숨고 다시
뜨는지 확인할 것.

## 2026-09-06 (도적전 개편) — "전투를 더 잼나게, 몬스터헌터 나우처럼" — 스태거·패턴 3종·연타·차지 넣음

사용자가 실시간 도적전(`rogue-action.js`)을 두고 "전투를 더 잼나게 할 수 없어?
몬스터헌터 나우처럼?"이라 요청. 어디부터 손댈지 먼저 물었더니(스태거·패턴
다양화·콤보/차지 셋 다 제시) **"추가할 사항은 모두 추가해봐"** 로 셋 다
한 번에 넣으라고 답함 — 이후 세션은 이런 종류 질문에서 스스로 판단해
진행하고, 정말 막힐 때만 물을 것(사용자가 명시적으로 지적한 태도).

**넣은 것 넷, 전부 `js/rogue-action.js`(판정 층, 순수 함수) + `js/battle3d.js`
(화면 층, `duel:fx` 신호만 늘림) 안에서 끝났다** — `duel.js`·`rogue.js`·
`world3d.js`는 한 줄도 안 건드렸다(파일 머리말의 기존 원칙 그대로 지킴).

1. **스태거(기절)** — 낸 피해 × 0.6 만큼 게이지가 차고(적 hp의 35%가 만충),
   다 차면 2.5초 기절(공격 안 함) + 그동안 내 피해 ×1.5. 손 놓으면(연타
   간격 1.4초 넘게) 초당 6씩 식는다. 금빛(`gold`) 알갱이 신설로 알린다
2. **적 큰 공격 3종 순환** — `foeMoves()`가 강타(기존)→돌진(반경 1.6배·
   예고 0.75배·피해 1.25배)→휩쓸기(반경 1.15배·예고 1.35배·피해 0.7배)
   순으로 돈다. **무작위가 아니라 `foeN` 카운터로 도는 순서**라 자가진단이
   재현할 수 있다 — 첫 번째 강타는 옛 상수와 완전히 같아서 기존 도적전
   테스트 넷은 손 안 대고 통과한다
3. **연타 회심** — 다섯 번째 연속 명중마다 피해 ×1.8("회심"), 사거리 밖으로
   물러나 1.4초 지나면 연타가 끊긴다(원작 자동공격은 그대로 계속 나간다 —
   "손을 놓는다"는 곧 "밖으로 물러난다"다, 안에 있으면 자동으로 계속 때린다)
4. **차지 일격** — 필살(때려야 기가 참)과 별개로 **가만있어도 5초면 다 차는**
   자원. 버튼으로 공격력×0.35 피해를 낸다. 사거리(3m)의 1.4배까지는 봐주지만
   그 밖이면 **헛치고 그래도 소모된다**(무를 수 없다 — 신중히 눌러야 한다)

HUD(`#rogue-hud`)에 빈틈(스태거)·차지 게이지, 연타 카운터(`🔥 연타 ×N`),
"차지 일격" 버튼을 새로 붙였다. `_admin.html`에 `rogueAction.*` 손잡이 12개
새 그룹으로. `css/style.css`에 `.ra-tell.stagger`(금빛 배너)·`.ra-combo` 추가.

**밟은 함정 — 자가진단 셋을 내가 직접 깨뜨렸다(코드가 아니라 테스트 실수).**
1. 새로 쓴 콤보 테스트가 `ev.t`를 `ev.kind`로 잘못 읽어 늘 undefined였다
2. 기존 파티클 테스트가 `KINDS` 개수를 5로 못박아 뒀는데 `gold` 하나를
   늘려서 깨졌다 — 6으로 갱신
3. **진짜 발견할 뻔한 것** — 기존 "사거리 안에 있으면 자동으로 때린다" 테스트가
   `ev.t === 'quick'`만 셌는데, 회심 타격은 `'combo'`로 라벨이 달라 개수가
   덜 세어졌다(`s.hits`와 어긋남). `quick`이든 `combo`든 "자동공격이 맞았다"는
   같은 뜻이므로 테스트가 둘 다 세도록 고쳤다 — **새 이벤트 타입을 늘릴 때마다
   "그 타입도 세야 하는 기존 집계가 있는지" 되짚어야 한다**는 교훈
헤드리스 3회 451/451 동일 확인(신설 5개 포함).

**남은 것 (다음 세션 참고)**: 실제 손맛은 아직 화면으로 확인 못 했다(값·
판정만 자가진단으로 봤다) — claude-in-chrome으로 실제 도적과 붙어 스태거
발동 순간의 금빛 이펙트, 차지 버튼 타이밍, 콤보 카운터 표시가 실제로
잘 보이는지 확인할 차례. 또한 이 넷은 **도적전(rogue-action.js)에만**
있고 `duel.js`(야생 조우·성채·토벌)에는 없다 — 사용자가 원하면 그쪽에도
옮길지는 아직 안 물어봤다.

## 2026-09-06 (같은 날 더 — 실기기 테스트로 이어감) — GPS 코드 경로 자체를 태워 재확인, land.js 만 보는 다른 자리는 없음을 확인, 폰 서버 띄움

사용자가 "실기기로 직접 테스트해봐"라고 요청 — 물리 폰·GPS 하드웨어가 없다는
한계를 먼저 밝히고, 대신 할 수 있는 것 중 가장 실제에 가까운 걸 했다.

**1. `navigator.geolocation` 만 가짜로 갈아 끼운 임시 하네스로 진짜 `useGeo()`
경로를 태움.** 바로 위 항목의 검증은 `event.candidates/resolve` 같은 순수
함수만 불러 본 것이라 `contextAt()` 고침 자체, `world.useGeo()` 의
`watchPosition` 콜백, `latLngToWorld()` 변환은 안 거쳤다. `_gpswalk-scratch.html`
(커밋 안 하고 세션 끝에 지움)을 만들어 `window.DG_NO_ACCOUNT=true`(자가진단과
같은 격리된 `deungyong-go/save/test` 키)로 계정 게이트를 건너뛰고,
`navigator.geolocation.watchPosition/clearWatch` 를 손으로 채운 큐로 바꿔
`window.__feedGeo(lat,lng)` 로 원할 때 "GPS 고정"을 흘려보낼 수 있게 했다.
판교역 실좌표(37.3947, 127.1112 — 이전 세션이 이미 실제 지형을 검증한 자리)에서
80m 씩 6,080m 를 "걸어" 봄 — **실제 코드 경로로 사건 3회**(도적의 습격·길
위의 상인·부상당한 병사, 땅 갈래 town·mount) 확인, 풍경 토스트도 계속 뜸,
콘솔 에러 없음, 스크린샷으로 "6,080m 이동" HUD·3D 렌더 정상 확인. **실제
사용자 세이브는 한 번도 안 건드렸다**(격리된 test 키로만 진행).

**2. 열어 뒀던 물음 — "`world.terrainAt()` 을 쓰는 다른 자리(스폰·NPC·짐승)도
`land.js` 만 보고 있을 수 있다" — 훑어서 닫음.** `npc.js`·`animal.js` 는
`land().place(id)` 로 하북의 정해진 자리에만 서고, `live()` 가 `!land().on()`
이면 아예 빈 배열을 준다 — **의도된 설계**다(주민·짐승은 "이 땅에 사는" 것이지
온 세상에 흩어진 것이 아니다, `land.js` 자신의 머리말에도 그렇게 적혀 있다).
`encounter.js`(잡을 것 스폰)는 애초에 `land.js` 를 한 번도 안 본다 — 처음부터
온 세상에서 돌고 있었다. `world3d.js` 의 `L.at()` 은 하북 손그림 **텍스처를
그리는** 자리라 하북 밖을 건너뛰는 게 맞다. **결론: `event.js`(와 그 옆의
`ambient.js`)만 걸려 있던 문제였고, 이번에 고친 걸로 끝이다.** 코드 변경 없음
(감사만).

**3. 사용자 실기기 확인을 위해 폰 서버를 띄움.** `server/certs`·`node_modules`
가 이미 있어 `run-phone.bat` 이 바로 떴다 — `https://192.168.0.15:8790/index.html`
(포트 8790, `dg-server.mjs`, HOST=0.0.0.0). `ANTHROPIC_API_KEY` 가 없어 사관(AI)은
막혀 있지만 오늘 손댄 것(걷기·사건·토스트·발자국)은 전부 오프라인이라 무관.
CA 인증서(`server/certs/dg-ca.crt`)를 폰에 보내 신뢰시키는 것부터는 사용자
몫으로 넘겼다(파일을 폰으로 옮기는 것 자체는 대신할 수 없다).

**다음 세션 참고**: 이 서버가 계속 떠 있을 수 있다 — 세션이 끝났는데도
0.0.0.0:8790 이 떠 있으면 사용자가 실기기 확인을 끝냈는지 먼저 묻고 정리할 것.
사용자가 실제로 걸어본 뒤 축1(과 축5, 전반적 재미) 판정을 다시 낼 차례다.

## 2026-09-06 (직접 걸어보고 체감 확인 중 발견) — 진짜 원흉: 마을 밖에서는 사건이 통째로 0개였다

사용자가 "직접 걸어보고 체감 확인해봐"라고 요청 — claude-in-chrome 으로 화면을
누르는 대신, `EV.stats({x:5000,y:5000})` 처럼 값으로 먼저 물어봤다. **`here: 0`.**
더 먼 좌표(10만)도 0. 바로 위 항목에서 손댄 빈도·토스트·발자국은 전부
**하북 마을(원점 둘레 ~500m) 안에서만** 뜻이 있었던 것 — 그 밖은 `event.js`
자체가 사건을 한 건도 못 내고 있었다.

**원인**: `event.js` 의 `contextAt()` 이 땅 갈래(`kind`)를 **`land.js` 한 곳에만**
물었다. `land.js` 는 하북 손그림 구역 밖이면 `null` 을 준다(의도된 동작). 그런데
`EVENTS` 열한 가지가 죄다 `where`(땅 갈래) 나 `marks`(하북 전용 표식)를 요구해서,
`kind` 가 `null` 이면 **후보가 통째로 0개**였다. 반면 3D 렌더링·미니맵이 쓰는
`world.js` 의 `terrainAt()` 은 이미 **land → geo(실제 OSM 지형) → 해시** 순으로
답을 내고 있어서 마을 밖도 늘 갈래가 있다 — `event.js` 만 그 확장을 안 타고
있었다. `land.js` 가 처음 생길 때("여기서 시험하고, 되면 넓힌다")도, geo.js 가
나중에 실제 지형을 얹을 때도 `contextAt()` 은 그대로였던 것 — **거의 두 세션
전부터 있었을 회귀**로 보인다. `contextAt()` 자체를 겨냥한 자가진단이 이번까지
하나도 없어서(값 내는 함수 대부분이 `candidates`·`resolve` 쪽만 테스트됐다)
아무도 못 잡고 있었다.

**GPS 모드라면 이건 훨씬 크다** — 실제 위치로 플레이하는 사용자는 세계 좌표
원점(하북)에 있을 일이 사실상 없다. 즉 **GPS 모드에서는 사건이 사실상 한 번도
안 났을 것**이다(키보드 모드로 하북 근처를 벗어나지 않았을 때만 사건을 봤을
것). "그냥 걷다가 가까이 가기 누르는 반복" 이라던 원래 불만의 진짜 크기가
바로 위 항목에서 짐작한 것보다 훨씬 컸다는 뜻.

**고침** (`js/event.js` `contextAt()`): `land.at()` 이 `null` 이면 `world.terrainAt()`
으로 떨어진다. `mark`(사당·폐허 등 손으로 심은 표식)는 하북 전용이 맞으므로
그대로 `land.js` 만 본다 — 마을 밖에서 비문·지도 조각까지 나오면 안 된다.

**검증**: 자가진단에 `contextAt()` 을 직접 겨냥한 시험 둘을 새로 얹음(여태
없었다) — 하북 밖 좌표 스무 곳에서 `kind` 가 `world.terrainAt()` 과 실제로
같은지, `mark` 는 그래도 `null` 인지. 442/442(기존 440 + 새 2) 3회 동일.
실제 크롬(claude-in-chrome)으로 원점에서 수천~수만 미터 떨어진 좌표에
`EV.stats()` 를 직접 물어 `here: 2`(잔디밭 → 산적·부상병, 산 → 부상병·약초)로
살아난 것 확인. **순수 함수 시뮬레이션**(세이브 전혀 안 건드림 — `pos`·`dist`
를 로컬 변수로만 굴려 `event.candidates/pick`·`ambient.pickFlavor/microReward`
를 직접 불렀다)으로 마을을 가로질러 남동쪽 3.5km 를 "걸어" 봄:
**사건 7회**(약 500m 간격, 겨냥한 빈도와 일치) · **풍경 토스트 25회**(약
140m 간격) · 진로가 숨은 자리를 안 스쳐 발자국은 이번엔 0회(정상 — 방향에
달렸다). 고치기 전이었다면 하북을 벗어난 뒤로는(약 500m 지점부터) 사건이
영원히 0이었을 자리다.

**남은 것**: 이 고침으로 **사가고의 실제 GPS 플레이 체감이 오늘 세션 전체보다
더 크게 바뀔 가능성이 있다** — 다음 세션(또는 사용자 실기기)이 실제 위치로
걸어보고 사건이 뜨는지부터 확인할 가치가 있다. `world.js` 의 `terrainAt()` 을
쓰는 다른 자리(스폰·NPC·짐승 등)도 혹시 같은 식으로 `land.js` 만 보고 있는
곳이 더 있는지 훑어볼 만하다(이번엔 `event.js` 하나만 고쳤다).

## 2026-09-06 (다음 날 이어서) — 축1(이동·조작 단조로움)에 손댐: 사건 빈도↑ · 새 "걷기 흥"(js/ambient.js) · 토스트 갈래

바로 아래 항목이 "사용자와 먼저 맞출 것"으로 남긴 축1을 시작하기 전에 다시
물었다 — 사용자가 "사건 빈도가 낮다·알림이 약하다·걷는 동작 자체가
밋밋하다, **셋 다** 고치고 게임성을 최대한 끌어올려야 한다"고 답해
`../SAGA-HANDOFF.md`의 "게임성 방향" 절(오픈월드·모든 게임 참조) 그대로 셋을 한 번에 다뤘다.

**1. 사건 빈도 (`event.js`)** — `STEP` 420→300m, `CHANCE` 0.55→0.62. 평균
간격이 420÷0.55≈764m 에서 300÷0.62≈484m 로 준다. 관리자 손잡이 표시값도
같이 맞췄다(`_admin.html`).

**2. 새 파일 `js/ambient.js` — "걷기 흥".** `event.js` 의 큰 사건 사이가
비어 보이는 게 진짜 문제라, 판정에 안 닿는 **가벼운 신호**를 훨씬 잦게
흘려 그 사이를 메운다. `event.js` 와 같은 결(값 내는 함수는 순수, 세이브는
`tick` 한 곳에서만) 로 짰다.
- **풍경 토스트** — 110m 마다(기본 80% 확률) 땅 갈래(들·숲·산·물·길·마을·
  논)×낮/밤 한 줄, 궂은 날(비·눈·안개)엔 날씨 줄이 섞여 나온다. 15%는 소액
  보상(🪙2~6 또는 경험치3~8)이 붙는다.
- **발자국(레이더)** — `land.js` 의 숨은 자리(동굴·사당·폐허)를 `codex.js`
  실제 발견 반경(34m)보다 훨씬 넓은 220m 에서부터 8방위("🧭 남서쪽에서
  심상치 않은 기운이 느껴진다")로 미리 알린다. 정확한 좌표는 여전히 스스로
  찾아야 한다. 이미 발견한 자리는 조용하고, 멀어졌다 돌아오면 다시 알린다.
- 손잡이 `ambient.on`·`ambient.stepM`·`ambient.chance`·`ambient.rewardChance`·
  `ambient.radarM` 을 `_admin.html`에 새로 얹었다.
- `index.html`·`_test.html`·`_demo.html`·`_autoprobe.html`·`sw.js`(캐시
  목록 + 버전 go-v5.21.15)에 스크립트를 추가했다. `mobile/build-www.mjs` 는
  `js/` 폴더를 통째로 복사해 손 안 대도 된다.
- `game.js` 의 루프에 `event.tick()` 바로 뒤 `ambient.tick()` 을 얹었다.

**3. 토스트에 갈래(`ui.js`)** — 여태 문자열 하나뿐이라 늘 같은 금테로
떴다. `toast(msg, type)` 로 'good'(초록기)·'bad'(가라앉은 색)·'find'(더
밝고 더 오래, `style.css` `toastpop` 튕기는 진입) 를 더했다. **옛 호출은
그대로 동작한다** — `core.emit('toast', 문자열)` 이든 `ui.toast(문자열)`
이든 `type` 이 없으면 'info'. 짧은 사이에 여럿이 몰리면(걷기 흥이 실제로
그렇다) 덮어쓰지 않고 **작은 큐**(최대 4, 넘치면 오래된 것부터 버림)에
쌓아 하나씩 보여준다.

**4. 사건 트리거 자체도 "찾아낸 것" 티를 낸다(`event.js`·`style.css`)** —
카드에 `pingnew` 클래스를 얹어 테두리가 한 번 크게 번졌다 잦아드는
`pingring` 애니메이션, `navigator.vibrate`(있으면) 로 짧은 진동.

**검증**: 자가진단 440/440(옛 433 + 새 7개) 3회 동일. 실제 크롬으로
(claude-in-chrome, 헤드리스 아님) 직접 확인:
- `DG.land.place('ruin')` 근처 180m 지점으로 순간이동 → `DG.ambient.tick()` 을
  부르니 실제로 `🧭 남서쪽에서 심상치 않은 기운이 느껴진다`(`t-find` 클래스)가
  화면에 뜨는 것을 스크린샷으로 확인. 이 세이브는 사당·동굴을 이미 예전
  세션들이 다 찾아 놔서(폐허만 남음) 그쪽으로 재현했다 — **이미 찾은 자리는
  실제로 조용한 것도 같이 확인**(`codex.has` 로 걸러짐)
- `DG.event.force('road_merchant')` 로 사건 카드를 열어 `.enc-card` 가
  `pingnew` 클래스를 달고 뜨는 것 확인(섬광 자체는 1.1초짜리라 스크린샷
  왕복 지연 안에 이미 잦아들어 눈으로는 못 잡았다 — `clickMark`·레벨업
  배너와 같은 사정, className 으로 확인)
- `ui.toast()` 를 연달아 세 번(info·good·find) 불러 실제로 **하나씩 순서대로**
  뜨는 것을 확인(덮어쓰지 않는다) — 마지막 것이 `t-find` 클래스로 뜬 것도 확인
- **건드린 세이브 자국**: 사건 하나(road_merchant, "지나간다")를 실제로
  골라 경험치 +11 을 얻었다(정상적으로 벌 수 있는 소액이라 안 되돌렸다).
  진단용으로 옮겼던 좌표는 마을 한가운데(`land.place('village')`)로
  되돌렸고, 날씨 강제(`weather.force`)도 풀었다.

**남은 것 (다음 세션 참고)**: 실기기(사용자 손)로 실제 걸으며 체감 확인은
아직 못 했다 — 이 세션은 순간이동으로 기능만 확인했다. 빈도·문구가
실제로 걸을 때 과하거나 부족하면 위 손잡이들로 조정할 것. 축1의 세 번째
갈래("걷는 동작 자체가 밋밋하다")는 캐릭터 애니메이션(걷기 사이클·bob)은
이미 있어서(`world3d.js`) 이번엔 콘텐츠 밀도(풍경·발자국)로 풀었다 —
그래도 부족하면 포켓몬GO 식 주간 걷기 이정표·인센스 같은 더 큰 장치가
다음 후보다.

## 2026-09-06 (오늘의 마무리) — 다음 세션이 이어받을 자리

오늘 다섯 축("재미없다" 판정, 아래 항목) 중 **축2(전투·포획 손맛)·축3(보상·
성장 체감)·축4(목표 가시성)를 끝냈다.** 가는 길에 진짜 버그 둘도 찾아
고쳤다(실전 중 이동 전체 차단 · 적도 카드가 실시간 화면을 가림). 전부
커밋·푸시 완료, 자가진단 433/433 유지.

**남은 것 — 축1(이동·조작이 단조롭다).** 아직 손 안 댔다. 원문: "그냥
걷다가 가까이 가기 누르는 반복. 걷는 동안 화면이 빈다(사건·풍경 토스트가
있긴 하지만 약한 듯)". `js/event.js`(420m마다 55% 확률로 사건— 산적·
정찰병·늑대무리 셋)가 이미 있는데 왜 약하게 느껴지는지부터 다시 짚어야
한다 — 빈도가 낮아서인지, 알림(토스트)이 약해서인지, 걷는 동작 자체가
밋밋해서인지 사용자와 먼저 맞출 것. **2026-09-06에 사용자가 못박은 시리즈
전체 방향**(`../SAGA-HANDOFF.md` "게임성 방향" 절 참고) — 포켓몬GO·
몬스터헌터 나우 같은 오픈월드를 기준으로 어느 게임이든 좋은 아이디어면
참조해도 된다 — 를 이 축에도 적용할 것(예: 포켓몬GO의 인센스·발자국·
주간 걷기 보상, 젤다류의 발견 유도 등이 후보가 될 수 있다, 확정 아님).

축5(전반적 재미)는 위 넷의 합이라 따로 손댈 자리는 없다 — 넷이 끝나면
사용자가 다시 플레이해 보고 판단할 차례.

## 2026-09-06 (같은 날 더 — 축3 착수) — 레벨업에 화면 연출을 붙임

다섯 축 중 축3("보상·성장이 안 와닿는다")의 구체적 예시였던 "레벨업도 숫자
말고는 연출이 약하다"를 손봤다. `core.js`의 `gainExp()`가 이미
`core.emit('levelup', lv)`를 쏘고 있었는데 **듣는 곳이 어디에도 없었다** —
기록 시트에 남는 글자 한 줄(`pushLog`)이 전부였다. `js/ui.js`에
`core.on('levelup', showLevelUp)`을 추가하고, 화면 가운데 잠깐 뜨는 띠
(`#levelup`, `index.html`에 새로 추가)를 넣었다 — 1.8초 팝인+페이드
(`css/style.css` `@keyframes lvpop`), 조작을 막지 않는다(`pointer-events:none`).
연타 레벨업(한 번에 여러 레벨 오를 때)도 매번 다시 재생되게 `offsetWidth`
리플로우 트릭을 썼다.

**확인**: 콘솔에서 `DG.core.gainExp(999999)`로 실제 레벨업 파이프라인을
끝까지 태워 Lv.14→36, `#levelup`의 class·내용이 매 레벨업마다 올바르게
갱신되는 것을 DOM으로 확인(스크린샷 왕복 지연 때문에 화면으로는 못 잡음 —
`world3d.js`의 `clickMark`와 같은 사정). **이 시험이 실제 세이브 레벨을
36으로 밀어 올렸다 — 바로 `level:14, exp:0`으로 되돌렸다**(다른 축소
시험들과 달리 이건 "정상적으로 벌 수 있는 양"을 훌쩍 넘어서 반드시 되돌려야
했다). `_test.html`에도 `#levelup` 빈 div를 추가해 자가진단이 죽지 않게
했다(자가진단은 이 배너 자체를 검증하진 않는다). 433/433 3회 동일.

## 2026-09-06 (다섯 축 착수) — 목표 HUD·포획 손맛 연출 넣음, 포획을 실시간 전투로 바꾸는 설계는 다음 세션이 코드로

바로 아래 항목("재미없다" 판정)이 남긴 다섯 축 중 둘에 손을 댔다.

**1. 목표 가시성 (축4) — 상단 HUD에 상시 한 줄 붙임.**
`js/ui.js`의 `renderTop()`이 그리는 `.profile`(좌상단 태수 칭호 줄) 안에
`goalLine()`을 새로 넣었다 — 받은 사명 중 첫 미완료 항목을 "🎯 이름 got/need"
꼴로 늘 띄운다. 사명이 없으면 "역참에 들르면 사명을 받습니다", 다 채웠으면
"거두러 가기"로 바뀐다. 누르면 `openSheet('quest')`(사명 탭 그대로 열림).
CSS는 `.p-goal`(style.css). 사명 자체(`js/quest.js`)는 안 건드렸다 — 이미
있던 걸 화면에 상시 노출만 시켰다.

**2. 전투·포획 손맛 (축2, 절반) — 결과창에 성공/실패 연출 갈림.**
`js/encounter.js`의 포획(`throwFeed`)·등용(`succeedHero`/`failHero`) 결과
카드에 클래스를 얹었다 — 성공은 `catchpop`(팝인 스케일 + 초상 금빛 발광,
style.css `@keyframes catchpop`/`catchglow`), 실패는 `jolt`(기존 좌우 흔들림
애니메이션 재사용) + 초상에 `.pt.dark` 필터(달아나는 느낌, 원래 적도 암영에
쓰던 필터를 재사용 — 새 그림 안 그림). 판정 로직(`throwFeed`의 확률 계산)은
한 줄도 안 건드렸다 — 순수 화면 연출.

**3. 전투·포획 손맛 (축2, 나머지) — 포획을 몬스터헌터 나우식 실시간 전투로. 실제로 넣었다.**
사용자가 "그런 전투신을 원해"로 요청, 범위는 "풀 실시간 전투"를 골랐다.
처음엔 "오늘은 설계만"으로 시작했는데, 알고 보니 엔진이 이미 있고 범용이라
(`js/rogue-action.js` — 도적전 전용으로 보이지만 코드 자체엔 도적 특정 로직이
없다. `duel.js`와 완전히 같은 `open(o)`/`onDone` 모양이라 `rogue.js:494`가
`global.DG.rogueAction || global.DG.duel`로 드롭인 교체해 쓰는 것만 봐도 그렇다)
그날 안에 실제로 붙였다.

**바뀐 것:**
- `js/encounter.js` — `startPet(spawn)`이 진입점을 가른다: `DG.rogueAction`이
  있고 사료가 1개 이상이면 `startPetLive()`(신규)로, 아니면(사료 없음·스크립트
  누락) `startPetTimer()`(옛 타이밍 막대, 이름만 바꿈)로. **자가진단(`DG_NO_DRAW`)은
  늘 옛길로 간다** — `_test.html`의 포획 테스트가 `[data-act="throw"]`를 직접
  클릭하므로(219행대) 옛 카드가 그대로 있어야 한다. `rogue.js`의 적도 진단이
  화면 대신 `fight()` 순수 함수를 직접 부르는 것과 같은 관례.
- `startPetLive(spawn)` — `catchBase`를 그대로 난도로 뒤집어 `foeHp`를 낸다
  (`Math.round(partyPower().atk * hpK / catchBase)`, `hpK = core.tuned('petAction.hpK', 0.14)`).
  **새 데이터 필드를 안 만들었다** — `data.js`는 다섯 판이 나눠 든 사본이라
  손대면 다섯 벌을 같이 고쳐야 하는데, 이미 있는 `catchBase`만으로 충분했다.
  `rogueAction.open({stage3d:{kind:'pet',ref:p}, foeHp, myAtk, myDef, onDone})`로
  넘긴다 — `duelStage`는 `event.js`의 늑대 무리가 이미 쓰던 `kind:'pet'` 그대로.
- `finishPetLive(p, perf)` — 한 대도 못 때리고 물러났으면(`fled && dealt<=0`)
  없던 일로 한다(`rogue.js:507`과 같은 판단). 그 외엔 사료 1개를 쓰고,
  `catchBase + effect('catchPct')/100 + (완파면 0.45, 아니면 가한피해비율*0.40)`로
  확률을 내 굴린다 — "정확도" 대신 "가한 피해 비율"이 보너스를 만든다.
  성공/실패 결과 카드는 축2-절반에서 넣은 `catchpop`/`jolt` 연출을 그대로 쓴다.
  **판정 후 로직(`gainPet`·성공/실패 카드)은 옛길과 100% 같은 함수를 부른다.**

**가는 길에 찾은 진짜 버그 — 고쳤다.** 실전을 실제로 플레이해 보니(claude-in-chrome
으로 사료를 쥐여 주고 짐승과 붙어 봄) 3초가 지나도 자동공격이 한 번도 안
나갔다. 원인: `world3d.duelStage()`가 둘을 약 8.5m 떨어뜨려 세우는데
`rogueAction`의 사거리(`MY_REACH` 3.0 · `FOE_REACH` 3.2 · `HEAVY_RANGE` 5.5)는
전부 그보다 짧다 — **다가가지 않으면 공격도 회피도 영영 안 일어난다.** 그런데
`world.js`의 `inputBlocked()`가 `rogue.active`를 보고 전투 내내 `moveByKeys`
전체(WASD·스틱·탭이동)를 막고 있었다(2026-09-06 이전 세션이 "도적이랑 싸울 때
클릭한 데로 이동해서 싸우는 것 같지 않다"를 고치며 넣은 것 — 그런데 CSS
`#rogue-hud`의 자기 주석(437행대)엔 "움직일 수 있어야 사거리 안으로 걸어 들어갈
수 있다. 그래서 컨테이너가 pointer-events:none"이라고 **원래 의도가 이미
적혀 있었다** — 나중 세션이 부순 것). `js/world.js`의 `moveByKeys()`에
`liveDuel()`(= `DG.rogueAction.active`) 예외를 추가해 **탭-이동은 계속 막되
WASD·스틱만 실전 중에 풀었다** — 이건 도적전에도 똑같이 적용되는 수정이라
적도 강타 회피도 이번에 같이 살아났을 것이다(적도 쪽은 이 세션에서 재확인은
안 함). 덤으로 `rogue-action.js`의 HUD에 "🚶 다가가세요 — 사거리 안이면
저절로 공격합니다" 안내를 붙였다(`cur.lastDist`를 매 프레임 재고 `MY_REACH()`
밖이면 뜬다, 강타 예고와 같은 자리를 돌려 쓴다) — 적도 HUD에도 같이 뜬다.

**실제로 끝까지 확인함** (claude-in-chrome, 진짜 크롬, 콘솔로 짐승을 옆에
스폰): 진돗개 실전 → 사거리 밖에선 "다가가세요" 문구 뜨고 정지 → 콘솔로
2m 안으로 옮기니 자동공격이 시작돼 기세가 27~33%까지 실제로 깎임 → 완파 →
"진돗개 포획! 완파하고 붙잡았다 (가한 피해 100%)" 카드가 `catchpop` 연출과
함께 뜸 → 도감 등록 확인. 물러나기(까치, 한 대도 못 때림) → 사료 그대로,
스폰 그대로 남는 것도 확인. **자가진단은 이 변경 전후로 433/433 3회 동일**
(포획 테스트는 `DG_NO_DRAW`라 옛길을 타므로 영향 없음이 당연하지만, 그래도
회귀가 없는지 돌려 확인했다). 테스트하며 실제 세이브(사료·골드·명성·경험치·
도감)를 건드렸다 — 사료는 60으로 되돌리고 가짜 스폰은 지웠지만, 진돗개 한
마리를 실제로 잡은 효과(도감·공적·경험치·명성·금 소액)는 되돌리지 않았다
(정상적으로 벌 수 있는 것과 같은 종류라 해가 없다고 판단).

**추가로 — 자동 방치(`js/auto.js`)의 `autoPet`도 실전 규칙에 맞췄다.**
`encounter.js`의 `autoPet(spawn)`이 이제 `rogueAction.create/tick`을
dt=0.1로 최대 400회(가상 40초) 돌려 실전을 통째로 시뮬레이션하고, 그
`perf()`로 손으로 할 때와 **완전히 같은 확률식**(catchBase+효과+보너스)을
쓴다. 거리(dist)는 늘 2.0m로 고정 — 자동은 다가서기·회피를 할 수 없으니
"맞붙어 최선을 다한다"로 본다(그래서 강타는 자동에선 늘 맞는다 — 손으로
하는 쪽이 자동보다 유리한 게 자연스럽다, 적도 `autoFight`도 같은 결).
`rogueAction`이 없으면 옛 정확도-무작위 방식으로 돌아간다.
**직접 확인**: 콘솔에서 `DG.encounter.autoResolve(spawn)`을 진돗개(catchBase
0.48)로 30회 굴려 28/30(93%) 성공 — 손으로 완파했을 때의 기대 확률(0.48+0.45=0.93)과
일치. **주의해서 볼 것 — 페이지를 새로고침 안 하고 콘솔로 시험하면 옛
코드가 그대로 메모리에 남아 있어 엉뚱한 결과(37%)가 나온다**: 처음 이 시험을
할 때 실제로 이 함정을 밟았다 — 파일을 고치면 반드시 새로고침 뒤에 시험할 것.
자가진단은 `autoPet`을 직접 겨냥한 테스트가 없고(사거리 안 대상을 "처리했는지"만
보는 5238행대 테스트뿐) 433/433 3회 동일이라 회귀 없음.

**적도(rogue.js) 강타 회피도 재확인 — 가는 길에 두 번째 진짜 버그를 찾아 고쳤다.**
`DG.rogue.at(station, i*SLOT_MS)`로 실제 도적(두목 방각, 기세 525)을 세워
`DG.rogue.open()`으로 직접 열어 봤다. **"길을 연다"를 눌러도 접근 카드(기세·
물러가기까지 남은 시간)가 안 사라졌다** — `D.open()`(rogueAction)은 제
`#rogue-hud`만 관리할 뿐, `rogue.js`가 같은 `#encounter`에 띄워 둔 카드는
모른다. 카드가 실시간 HUD·3D 교전을 통째로 덮어 **사실상 아무것도 안 보이는**
상태였다 — 오늘 넣은 "다가가세요" 안내조차 카드 뒤에 가려 있었다. `rogue.js`의
`startDuel()`에서 `D.open()` 부르기 직전에 `host().classList.remove('show')`로
직접 숨기고, "한 대도 못 때리고 물러남" 갈래에서 다시 켜도록 고쳤다(그 외
경로는 `finish()`가 이미 다시 켠다). **실제로 확인**: 카드가 사라지고 실시간
HUD·3D 교전이 그대로 드러남 → 실전 진행(기세 100%→78%, 받은 피해 누적) →
물러나기 → "물리치지 못했다 · 교전·속공 3회·필살 0회·회피 0/0·받은 피해 5"
결과 카드가 깨끗하게 다시 뜸.

**이동(WASD)이 실제로 되는지는 확인 방법을 바꿔야 했다.** claude-in-chrome
탭에서 `computer` 툴의 키 입력과 `window.dispatchEvent(new KeyboardEvent(...))`
둘 다 실제 대기시간 동안 이동을 전혀 못 만들었다 — 하지만 `DG.world.update(0.1)`을
직접 30번 불러보니 그 자리에서 24 단위가 실제로 움직였다(속도 계산이 맞다는
뜻). **원인은 코드가 아니라 이 탭의 rAF가 자동화 중엔 죽거나 크게 늘어지는 것**
으로 보인다(포획 전투도 몇 초씩 진행이 멈췄다 다시 이어지는 걸 봤다) — 그래서
`moveByKeys`의 `liveDuel()` 예외 자체는 **직접 함수를 불러 검증**했고, 실제
키보드로 도적·야생짐승과 붙어 몸을 움직여 피하는 손맛은 **사용자 실기기가
마지막으로 확인해 줘야 한다**(자동화로는 이 탭의 rAF 사정 때문에 한계가 있다).

**남은 것 (다음 세션 참고, 급하지 않음):**
1. **등용(사람 설득)은 이번엔 안 건드렸다** — 사용자 요청이 "포획"(펫)이었다.
   호감도 라운드제(`encounter.js` 등용 함수들)는 그대로다.
2. **강타를 실제로 몸으로 피하는 손맛은 사용자 실기기 확인이 남았다** — 위 참고.

## 2026-09-06 (한 번 더 이어서) — 실제로 손으로 놀아 본 뒤 "재미없다" 판정, 다섯 축 다 손볼 것

사용자가 이 세션에서 직접(claude-in-chrome으로 대신 조작했지만 사용자가 화면을
보며 지시) 등용·포획을 여러 번 해 보고 낸 결론: **"게임 플레이가 재미가
없다."** 어디가 그런지 물었더니 **다섯 가지를 전부** 골랐다 — 하나만 고쳐서
될 문제가 아니라는 뜻이다. 우선순위를 먼저 정하자고 물었더니 "다섯가지
모두임"으로 되짚었다. **코드는 아직 하나도 안 건드렸다** — "저장해놔 새로운
세션에서 이어 할 수 있게"라는 요청 그대로, 다음 세션이 이 다섯을 정면으로
다룰 차례다.

1. **이동·조작이 단조롭다** — 그냥 걷다가 가까이 가기 누르는 반복. 걷는 동안
   화면이 빈다(사건·풍경 토스트가 있긴 하지만 약한 듯)
2. **전투·포획 손맛이 약하다** — 타이밍 미니게임("흰 고리가 초록 테 안에
   들어왔을 때")의 임팩트가 약하다. 이 세션에서 자동화로 여러 번 눌러 본
   느낌으로도 성공/실패가 화면 연출 차이 없이 거의 똑같이 지나간다
3. **보상·성장이 안 와닿는다** — 등용 성공 화면에 "공적 +32·경험치 +62·금
   +60" 처럼 숫자만 뜨고 끝. 레벨업(Lv.12→13 확인됨)도 숫자 말고는 연출이 약하다
4. **목표가 안 보인다** — 화면 어디에도 "지금 뭘 해야 하는지"가 상시로 안
   보인다(사명 탭에 들어가야 보일 수도 있으나 HUD 상시 노출은 없다)
5. **전반적으로 지켜보는 재미가 없다** — 위 넷의 합.

**다음 세션이 할 일**: 코드부터 손대지 말고 이 다섯을 어떤 순서로 다룰지
사용자와 다시 맞추는 것부터. 이 세션이 짚어 본 시작점 후보(참고용, 확정 아님):
목표 가시성(HUD에 "다음 목표" 상시 노출 — 나머지 체감에도 영향 줄 가능성)과
전투·포획 손맛(당장 코드로 만질 자리가 뚜렷함 — 성공/실패 연출 차이,
타이밍 난이도) 둘이 특히 손대기 쉬워 보였다. `js/encounter.js`(등용·포획
판정) · `js/rogue-action.js`(도적 실시간 교전) · `js/event.js`(걷다가 만나는
사건) · 사명 탭(목표) 관련 UI가 시작점이 될 만하다.

바로 위 항목이 남겨 둔 재확인 차례. `claude-in-chrome`이 이번엔 연결돼 정적
서버(8791)를 새로 띄우고 3D에서 직접 클릭해 봤다.

**직접 확인**: 고리 자체는 `syncClickMark`가 0.6초 만에 지우는 자기 관리 fx라
클릭 → 스크린샷의 왕복 지연 안에 자연 소멸해 처음 몇 번은 빈 화면만 찍혔다.
콘솔에서 `DG.world3d.clickMark(x,y)`를 150ms 간격으로 계속 다시 불러(고리가
꺼지기 전에 새 고리가 생기게) 캐릭터 발밑에 노란 고리가 바닥에 얹혀 뜨는 것을
스크린샷으로 확인 — 자리·색·눕는 방향 모두 의도한 대로다. 드래그 회전도
오른쪽으로 끌면 화면 오른쪽 쪽이 새로 들어오는 것을 다시 한번 확인(위 세
번째 버그 수정과 일치). 코드는 안 고쳤다 — 순수 재확인.

## 2026-09-06 (더 이어서) — 3D 클릭 표시 마저 넣음, 다음 세션이 새로 열어 재확인할 차례

바로 위 항목이 "남은 것"으로 미뤄 둔 3D 클릭 표시를 마저 넣었다. `js/world3d.js`에
`clickMark(x,y)`(노란 고리, `beam()`과 같은 자기 관리 fx 패턴 — `syncClickMark(dt)`가
나이 먹여 지운다) · `syncClickMark`를 `render()` 루프에 얹었고, `world3d`
공개 API에 `clickMark`로 내보냈다. `js/world.js`의 `onClick()`이 3D가
켜져 있으면(`W3.active()`) 이걸 불러 2D `clickMarks`(3D에서는 숨는 캔버스라
안 보이던 것)를 보완한다. 자가진단 433/433 3회 동일, `sw.js` →
`go-v5.21.7`.

**직접 화면으로 확인 못 함** — `claude-in-chrome` 연결이 이 항목 작업 중엔
안 됐다(사용자가 "완료되면 새로운 세션에서 할게"로 마무리 지음). **다음
세션(또는 사용자 실기기)이 할 일**: 3D에서 바닥을 클릭했을 때 노란 고리가
실제로 뜨는지, 회전(`turnBy`)·전투 중 정지(`inputBlocked`)까지 오늘 고친
넷이 실제로 다 잘 도는지 한 번에 재확인. 코드는 자가진단·리뷰로만 확인된
상태다.

## 2026-09-06 (이어서) — claude-in-chrome 다시 연결돼 직접 검증, 진짜 세 번째 버그(드래그 회전 방향) 더 찾음

새로고침 뒤에도 사용자가 "마우스는 왜 반대임?"으로 재확인 — `claude-in-chrome`이
이번엔 연결돼 실제로 3D에서 드래그해 봤다. **클릭-이동 계산(위 1번)은 맞게
고쳐졌는데, 회전 자체(`turnBy()`)의 부호가 반대였다.** 오른쪽으로 끌면
카메라가 여태 화면 **왼쪽**에 있던 쪽을 보도록 돌고 있었다(다른 3인칭
게임은 오른쪽으로 끌면 오른쪽을 본다) — `js/world.js`의 `turnBy()`에서
`W3.turn(-px/...)`을 `W3.turn(px/...)`으로 부호만 뒤집었다.

**직접 확인(claude-in-chrome, 진짜 크롬)**:
- 드래그 방향: 고친 뒤 오른쪽으로 끌면 화면 오른쪽 쪽이 더 들어오는 것을
  스크린샷 전후 비교로 확인
- 클릭-이동: 45도 넘게 돌린 채로 화면에 보이는 등불을 클릭 → 캐릭터가
  실제로 그 등불 쪽으로 걸어가는 것을 두 차례(회전 방향 고치기 전/후 모두)
  스크린샷으로 추적 확인 — 위 1번 수정과 이번 수정이 서로 맞물려도 문제없다
- 전투 중 이동 정지: `encounter.active`를 콘솔로 강제로 켜고 멀리 클릭 →
  플레이어 좌표가 클릭 전후로 한 치도 안 바뀌는 것을 좌표값으로 직접 확인
- 자가진단 433/433 3회 동일(회귀 없음), `sw.js`는 그대로(코드만 한 줄 더 고침,
  버전은 아래서 같이 올림)

## 2026-09-06 — 실제 플레이 중 나온 조작 버그 둘: 클릭-이동 방향, 전투 중 이동

사용자가 실제로 3D로 플레이하다가 실시간으로 지적("마우스랑 이동하는게
달라서 헷갈리네" → "왜 반대로 이동하지" → "마우스 클릭 한곳까지 이동을
안하네" → "도적이랑 싸울때 클릭한데까지 이동해서" → "싸우는 것 같지 않네").
코드를 봐서 둘 다 진짜 버그였다.

1. **3D에서 마우스로 화면을 돌린(yaw) 뒤 클릭-이동이 그 회전을 안 본다.**
   `js/world.js`의 `onClick()`이 화면 좌표를 세계 좌표로 바꿀 때
   (`unproject()`) 늘 "안 돌린" 기준으로만 계산했다 — `world3d.js`의
   `camAim()`이 카메라를 `yaw`만큼 돌리는 것과 같은 회전을 여기서도
   걸어야 했는데 안 걸고 있었다. 많이 돌리면 클릭한 곳과 정반대로 가는
   것처럼 보인다. "클릭한 데까지 안 간다"도 같은 원인(방향이 틀렸으니
   도착점도 어긋난다) — 새 버그가 아니라 하나였다.
2. **전투성 모달이 열려 있어도 이동이 안 멈춘다.** `fort.js`·`station.js`·
   `rogue.js`·`letter.js`는 전부 `encounter.active`를 보고 스스로 멈추는데
   `world.js` 자신의 이동(`moveByKeys`·`onClick`)만 그 문이 없었다.
   도적 습격(`rogue.js`)이 뜬 채로도 클릭·키가 캐릭터를 그대로 움직였다.

**고침**: `onClick()`에 `tiltMode()===2`일 때 `world3d.yaw()`만큼 클릭
좌표를 회전(camAim과 같은 회전 행렬)해서 화면과 이동 방향을 맞췄다.
`onClick()`·`moveByKeys()` 둘 다 새 `inputBlocked()`(=`encounter.active`
‖ `rogue.active` ‖ `duel.active`)를 보고 열려 있으면 이동을 멈추게 했다.
`sw.js` → `go-v5.21.5`. 자가진단 433/433 3회 동일.

**아직 실제 플레이로 재확인 안 됨** — 사용자가 원래 켜 둔 브라우저 탭에서
새로고침해 확인해 줄 차례다. `claude-in-chrome` 확장이 이번엔 연결이
안 돼(사용자 본인 창을 쓰고 있어서인 듯) 이 세션에서 직접 브라우저로
재현·확인은 못 했다 — 코드 리뷰 + 자가진단만으로 고쳤다.

**남은 것**: 클릭한 자리를 표시하는 고리(`clickMarks`)가 3D에서는 안
보인다(2D 지면 캔버스에만 그려지는데 3D 켜지면 그 캔버스가 숨는다) —
사용자가 "표시도 안 되고"로 짚었다. 이번엔 안 건드렸다, 원하면
`encounter3d.js`의 `TorusGeometry` 링 패턴을 따라 3D용 클릭 표시를
추가할 수 있다.

## 2026-09-05 (또또또또또또또또 이어서) — OSM 실제 지형, 진짜 원인은 429가 아니라 빠진 export 하나였다

지난 세션이 "3D 화면에 강·도로가 실제로 보이는지 확인이 아직 안 됐다 —
공개 Overpass 서버 429/503 계속 받았다"로 넘긴 것을 이어서 확인했다.
**진짜 원인은 서버 문제가 아니었다.** `js/geo.js`의 `fetchRegion()`이
`classify(elements, W.latLngToWorld, bounds)`로 `world.js`의
`latLngToWorld`를 부르는데, **그 함수가 `world.js` 안에 있긴 했지만
공개 API(`global.DG.world = {...}`) 내보내기 목록에 없었다** — 그래서
실제 게임에서는 매 요청이 HTTP 200을 받고도 `classify()` 안에서
"toWorld is not a function"으로 조용히 죽어 매번 `fail`로 잡혔다(콘솔에
아무것도 안 남는다 — geo.js가 실패를 조용히 삼키는 설계라 지난 세션이
429로 오인할 수밖에 없었다). 자가진단은 안 걸렸다 — `classify()`를
"순수 함수"로만 테스트해서 자체 `toWorld` 목(mock)을 넘기고, 실제
`world().latLngToWorld` 연결은 아무도 안 보고 있었다.

**고침**: `js/world.js`의 내보내기 표에 `latLngToWorld: latLngToWorld`
한 줄 추가(`worldToLatLng` 바로 아래). 이거 하나로 끝났다.

**실제로 확인(claude-in-chrome, 진짜 크롬)**:
- `DG.geo._setFetch()`로 실제 fetch를 가로채 응답을 직접 찍어 HTTP
  200·정상 JSON(13.8KB, 실제 OSM 엘리먼트)인데 `classify()`가 던지는
  걸 재현 → 고침 → 같은 요청이 `ok`로 넘어가는 것까지 앞뒤로 확인
- 판교역 원점 주변 ±60칸(48m 격자)을 실제로 `geo.terrainAt()`으로
  긁어 분류 분포 확인: `road 288·town 45·forest 76·water 5·grass 27`
  (나머지는 아직 못 받아온 구역) — 실제 판교 지형(도로 밀집·탄천)과
  들어맞는다
- `core.setTune('world.speedMul', 64)` + `world.collide=0` 으로 실제
  도로 칸(-21,0)까지 걸어가 3D 화면을 스크린샷·확대 — **회색 길
  텍스처가 주변 밭·풀과 다르게 실제로 갈라져 보이는 것을 눈으로
  확인**. 물(강)은 산 지형에 가려 이번엔 화면에 못 담았다(다음에
  다른 위치에서 확인하면 됨).
- 걷는 길에 도적 습격 인카운터가 뜬 것은 "달아난다"로 처리(전투·구매
  안 함), 이동 중 주운 보급 자원(도끼·가죽·동 등 소량)이 세이브에
  약간 반영됐다 — 개인 개발용 세이브라 문제 아님

**손잡이 원복**: `world.speedMul`·`world.collide` 둘 다 1로 되돌렸다
(디버그용 임시 조정, 저장 안 되는 런타임 튠). `forceTime`·`rogue.force`
도 페이지 새로고침으로 풀렸다.

**여전히 남은 것**: 공개 Overpass 서버는 여전히 가끔 진짜 429/503을
낸다(짧은 시간에 여러 구역을 몰아서 물었을 때 실측함) — `MAX_FAIL_STREAK`·
`BACKOFF_MS`가 이미 그걸 다루므로 급한 건 아니다.

## 2026-09-05 (또또또또또또또또또 이어서) — 강(water)도 다른 자리에서 실제로 확인, 가짜 "새까맣게 죽는" 현상 하나 지나감

바로 위 항목이 "물은 산에 가려 이번엔 못 봤다"로 남긴 걸 이어서, 산이
안 겹치는 다른 물 칸(원점에서 서남쪽 ~830m, `-16,9`)까지 걸어가 확인했다
— **파란 물 타일이 주변 길·밭과 뚜렷이 갈라져 보이는 것을 3D 화면에서
직접 확인**(스크린샷 확대로 재확인).

**가는 길에 한 번 헷갈렸다**: `core.setTune('world.speedMul', 64)`로
멀리(700m대) 순간에 가깝게 이동한 직후 화면이 몇 초간 완전히 새까맸다
(하늘·먼 건물은 밝은데 발밑 바닥만 죽어 있었다) — 처음엔 그림자
카메라(±140m 박스, `world3d.js` 344행 부근)가 플레이어를 못 따라가서
그 밖은 전부 그림자로 죽는 버그인가 의심했다. **아니었다** — 화면
품질 표시가 그 순간 "낮음(13fps)"까지 떨어져 있었던 게 힌트: 큰 순간이동
직후 화면 안에 갑자기 들어온 수백 미터 치 타일을 한꺼번에 굽느라
(`syncGround()`) 몇 초 못 그린 것뿐이었다. 몇 초 더 기다리거나(또는
`world.toggleTilt()`로 화면을 한 번 갱신) 리모트로 정상 밝기로 돌아왔다
— **평소 걷기 속도(1x)에서는 안 걸린다**(타일이 미리미리 한 칸씩
구워지므로). 디버그로 속도를 크게 올려 원거리 순간이동을 흉내 낼 때만
보이는 자기 유발 현상이라 코드는 안 건드렸다.

`world.speedMul`·`world.collide` 도로 1로 되돌리고 페이지 새로고침으로
`forceTime`·`rogue.force` 등 남은 디버그 상태를 전부 지웠다.

## 2026-09-05 (또또또또또또또 이어서) — "지도가 허접하다" → 실제 지형(OpenStreetMap)으로 오픈월드 잇기, 커밋·푸시 완료

사용자가 실제로 플레이해보고(claude-in-chrome, 진짜 크롬) 지적: "지도가
너무 허접해", "현위치랑 같아야 하지 않을까나? 점만 있는게 아니라",
"포켓몬GO의 좋은 부분은 참조해야", "젤다의 전설도 생각하고 있어". 확인해
보니 원점 둘레 1km 손그림 마을(`js/land.js`) 밖은 전부 **좌표 해시**
(`world.js`의 `core.hash2`)라 실제 위치와 아무 상관이 없었다.

**`js/geo.js` 새 파일**로 그 자리에 실제 지형(OpenStreetMap, Overpass
API)을 끼워 넣었다 — `land.js`와 계약이 같다(`terrainAt(tx,ty)` 모르면
null, 다음 레이어가 답한다). 이제 순서는 **손그림 땅 → 실제 지형(geo.js)
→ 좌표 해시**. 그리는 쪽(3D 텍스처 `terrainTexture`·미니맵 틴트·M키
전체지도)은 이 분류값 하나만 보고 이미 돌아가서 **한 줄도 안 고쳤다** —
`world3d.js` 819행 부근의 2026-09-04 감사("실제 지도 사진을 3D 바닥에
그대로 깔면 어색하다"로 그 텍스처를 껐던 결정)는 그대로 존중했다. 사진을
되살린 게 아니라 **분류 값의 출처**만 해시에서 실제 데이터로 바꾼 것.

- 1km 구역 단위로 Overpass에 물어(도로·물·숲·농지·시가지·봉우리·지명)
  48m 격자로 래스터화(면은 점-in-폴리곤, 선은 샘플링), `localStorage`에
  캐시. 오프라인이거나 실패하면(공개 서버라 흔하다 — 실제로 429·503을
  받았다) 조용히 예전 해시로 돌아간다. 손잡이 `geo.on=0`으로 통째로 끌 수
  있다
- 미니맵의 "이름난 자리"에 실제 지명(`place=*`)도 합쳤다 — 젤다식
  "가 볼 곳"
- **밟은 함정**: `stampPolygon`의 내부 폴리곤 순회 루프가 `classify()`
  바깥 요소-순회 루프와 같은 `i` 변수를 공유해 elements 순회가 깨지는
  버그를 헤드리스 검증 전에 코드 리뷰로 잡았다(`pi`로 분리). 자가진단
  하나(geo.on=0 테스트)는 `world.terrainAt`을 거치면 밑에 깔린
  `hash2`가 **우연히 같은 좌표를 물로 찍을 수 있어**(~7% 확률) 처음엔
  흔들렸다 — `GEO.terrainAt`을 직접 보는 쪽으로 고쳐 해결
- `_test.html` 자가진단 9개 추가, 헤드리스 3회 432/432 동일 확인
- 공개 Overpass 서버(overpass-api.de)가 이 세션 테스트 중 429/503을
  계속 돌려줬다 — 요청 간격을 1.5초→3초로 늘렸다. **다음 세션이 다시
  열면**: 실제로 3D 화면에 강·도로가 눈으로 보이는지 확인이 아직 안 됐다
  (`#btn-tilt`로 3D 전환 후 `DG.geo.info()`로 `ok`/`fail`/`pausedMs`
  확인). 계속 막히면 백오프를 더 늘리거나(`BACKOFF_MS`), 다른 Overpass
  미러(예: `overpass.kumi.systems`)로 `ENDPOINT`를 바꿔 볼 것
- 커밋 `2bb53fb`, 푸시 완료(origin/main)

## 2026-09-05 (또또또또또또 이어서) — "무료로 더 찾아봐" → Mesh2Motion 발견, 실제로 한 벌 뽑아 검증(아직 게임에 안 물림)

`HANDOFF.md`(719~737행, 2026-09-04)가 "무료 실사 애니메이션 인간 팩은
없다 — 이 조사 그만해라"라고 못박아 뒀었는데, 사용자가 "무료로 더
찾아봐줘"로 다시 열었다. 실제로 웹 검색해 보니 **"완성된 팩을 찾는다"는
예전 전제 자체가 좁았다** — Mesh2Motion(mesh2motion.org, 코드 MIT +
리그·애니메이션 CC0, 오픈소스)이라는 **CC0 정적 스캔 메시에 직접 리깅·
애니메이션을 입히는 웹 도구**를 찾았다. Mixamo를 아예 안 거치므로 이
판이 계속 걸려 넘어지던 재배포 라이선스 문제를 피해 간다.

**실제로 파이프라인을 끝까지 밟아 한 벌 뽑았다** (claude-in-chrome으로
진짜 크롬을 조작 — 헤드리스 아님):

1. Vitruvian Project(CC0, itch.io `withinamnesia/vitruvian-project-cc0`)
   `VitruvianProject142.blend`(162MB, 실사 사진측량 인체) + 4K 텍스처
   zip(826MB, BaseColor 포함 — **8K zip 은 Roughness/Height 뿐이고
   BaseColor 가 아예 없다**, 헷갈리지 말 것)을 itch.io 에서 받았다
   (itch.io 다운로드는 JS 서명 절차라 curl 로 못 뚫는다 — 반드시
   claude-in-chrome 으로 버튼을 눌러야 한다)
2. Blender(헤드리스, 이미 5.2 설치돼 있고 MPFB 애드온도 이미 얹혀 있다)로
   재질을 다시 짰다 — 원본 재질은 Substance Painter 절차형 셰이더 그룹이라
   glTF 로 그대로 안 나간다(빈 흰 몸으로 나갔던 첫 시도가 그거였다).
   **각 재질을 Image Texture → Base Color → Principled BSDF 로 통짜
   갈아 끼워야** 한다 — Face/Torso/Arms 는 African 변종, Legs 는 African
   변종이 없어서 범용 Utility 로 대신했다(그래서 다리 톤이 뜬다, 알려진 흠)
3. 데시메이트 9%(원본 3.7만 정점) + 텍스처 1024px 로 낮춰 8.6MB —
   **file_upload 툴 자체가 10MB 상한**이라 이 이하로 맞춰야 Mesh2Motion에
   올라간다(Draco 압축은 시도했지만 Mesh2Motion 의 로더가 DracoLoader를
   안 붙여서 못 읽는다 — 압축 대신 데시메이트로 줄일 것)
4. Mesh2Motion 에 업로드 → Human 스켈레톤(Hand Options: **Single Hand
   Bone** 로 낮추면 손가락 15개를 안 맞춰도 된다) → 어깨·팔꿈치·손·
   엉덩이·무릎·발을 손으로 드래그해 메시에 맞췄다(자동 정렬 아님 —
   진짜 사람이 하는 손작업이다) → Fighting_Idle·Walk·Run_Female·
   Sword_Attack·Hit_Chest·Death_A 6종을 골라 다운로드
5. 자가진단(헤드리스 Blender 로 재수입)으로 28본·6액션·7장 1024px
   텍스처가 실제로 다 들었는지 확인 — **거저 나온 게 아니라 진짜 검증했다**

산출물은 `assets/_wip/vitruvian-test/vitruvian_v1.glb`(자세한 건 그
폴더의 `README.md`) — **아직 `HERO_RECIPES`에 안 넣었다.** 다음 세션이
쓸지 말지 정할 것. 원본 대용량 파일(1.2GB)은 `C:\Users\Windows\Downloads\`
에 남아 있어 재작업 시 다시 안 받아도 된다.

## 2026-09-05 (또또또또또 이어서) — 실사 인물 전체가 초록·파랑으로 물들던 진짜 버그: 세력색 틴트가 MPFB 살결에도 곱혀지고 있었다

사용자가 "실기기에서 확인해줘"라고 요청 — claude-in-chrome 확장으로 **진짜
크롬(헤드리스 아님)**을 열어 처음으로 `DG.portrait3d.warm/of`를 직접 불러
MPFB 실사 인물 초상을 구워 봤다. **얼굴·피부가 초록·파랑·자주색으로 물들어
있었다** — Blender EEVEE 렌더·헤드리스 진단으로는 이 세션 내내 못 잡던
것(둘 다 이 코드 경로를 안 타서).

**원인**: `asset3d.js`의 `assembleHero()`가 `applyTint(model, tintOf('hero',
ref))`를 QRPG·MPFB 가리지 않고 무조건 걸고 있었다. `tintOf()`의 세력색
물들이기는 애초에 **QRPG 흰 옷(1,1,1)에 곱해 같은 몸을 나눠 쓰는 티를
없애려는 장치**(주석에 명시)인데, MPFB 실사 몸(사진 그대로의 피부·옷
텍스처)에도 똑같이 곱혀져 인종·계절과 무관하게 얼굴이 세력색(촉=초록
계열 등)으로 물드는 것이었다. **male.glb·female.glb·v3·v7·v8을 포함해
지금까지 뽑은 MPFB 실사 인물 전부가 걸려 있던 문제**다 — 이번에 3벌·12벌
배치를 새로 뽑아서 생긴 게 아니라 원래부터 있었는데, 실제 브라우저로
초상을 직접 구워 눈으로 보기 전까진 아무도(헤드리스 진단도 Blender
렌더도) 못 잡고 있었던 것.

**고침**: `tintOf()` 자체는 안 건드렸다(UI 등 다른 자리가 "이 인물의
빛깔"로 쓰는 순수 값이라 자가진단 세 개가 그 값 자체의 분포·세력 결을
검사하고 있다). 대신 `assembleHero(parts, ref, rec)`에 `rec`(그 인물에게
배정된 레시피 객체)을 넘겨 `rec.key`가 `qrpg_`로 시작할 때만 실제로
`applyTint`를 걸도록 좁혔다 — MPFB는 `applyTint(model, null)`이 되어
`material.color`가 원본 그대로(흰색, 텍스처 그대로 표시) 남는다.

**검증**: 실제 크롬 콘솔로 여러 MPFB 인물의 실제 메시 재질 `color`를
직접 찍어 전부 `ffffff`(안 물듦)임을 확인, 초상 19종(`mpfb_female`~
`mpfb_v23`, `v12`만 70명 중 아무도 안 걸려 표본 없음)을 다시 구워 스크린샷으로
정상 피부색 확인. 자가진단 423/423 3회 동일(회귀 없음, `tintOf` 관련
기존 세 테스트도 그대로 통과 — 그 값 자체는 안 바꿨으니 당연하다).

**부가로 눈에 띈 것(이번 범위 밖, 안 건드림)**: 초상 카메라가 가끔 인물의
**뒤통수**를 보여준다(`mpfb_v10`·`v18`·`v20`에서 관찰, 매번 재현되진 않음 —
호출마다 다름). `portrait3d.js` 주석에 "카메라가 얼굴이 아니라 뒤통수를
보고 있었다"는 옛 버그를 고친 이력이 있는데, 이번엔 간헐적이라 같은
원인인지 다른 것인지 확인이 더 필요하다 — 다음 세션이 볼 만하다.

## 2026-09-05 (또또또또 이어서) — 남은 MPFB 스킨 열두 종 마저 뽑음, HERO_RECIPES 26벌

바로 아래 항목("눈가 빨갛게" 실제로 고침)에서 검증된 마스킹을, 아직 안 쓴
스킨 열두 종(`middleage_*`·`old_*`·`young_african_male`·`young_caucasian_male`·
`young_caucasian_male2`) 전부에 빨간기가 보이든 안 보이든 예방적으로
적용해 마저 뽑았다. `toon01`·`_special_suit` 둘은 뺐다. 대표 셋으로 눈
좌표가 여전히 같은지 먼저 크롭해 확인한 뒤 나머지에 일괄 적용 — 좌표는
`(1690,970)`·`(1690,1132)`(2048 기준) 그대로였다.

`js/asset3d.js`의 `HERO_RECIPES`가 QRPG 6 + MPFB 실사 20 = **스물여섯
벌**이 됐다. `_test.html`의 인물 벌 수 진단 갱신, `sw.js` → `go-v5.21.0`.
가는 길에 "인물이 여섯 벌의 몸을 나눠 입는다" 진단이 완전 커버리지를
요구하다 70명/26벌 확률(기대 미사용 ≈1.8개)에 걸려 깨진 것도 같이
고쳤다(코드 버그 아님 — 테스트를 "대부분 걸림"으로 완화). 자가진단
423/423 3회 동일. 자세한 조합표·검증 방법은 `assets/ASSET_LICENSES.md`
"2026-09-05(또 이어서) — 남은 MPFB 스킨 열두 종을 예방적 마스킹으로
마저 뽑음" 절 참고.

**남은 것**: 열두 개 중 다섯 개(v13·v15·v18·v21·v22)는 렌더 확인 안 함
(export는 다 됐다) · 실기기 확인 여전히 못 함 · `young_caucasian_male2`가
쓰는 `long01` 머리가 남성 캐릭터치고 다소 여성스러워 보임(머리 라이브러리
한계, 급하면 다른 머리로 바꿀 수 있다).

## 2026-09-05 (또또또 이어서) — "눈가 빨갛게" 실제로 고침: 스킨 텍스처 눈 UV 마스킹 + 스킨 3벌 마저 뽑음

바로 아래 항목이 "다음 세션 몫"으로 남긴 진짜 고치는 법을 실행했다.
`young_caucasian_female`·`young_caucasian_female2`·`old_asian_male` 세
스킨의 diffuse 텍스처(`%APPDATA%\Blender Foundation\Blender\5.2\
extensions\.user\user_default\mpfb\data\skins\<스킨>\`)에서 눈 UV
영역을 주변 피부색으로 페더링해 지우고, 그 스킨들로 `mpfb_v9`(캐릭시안
여1+ponytail01+정장)·`mpfb_v10`(노년 아시아 남+short02+캐주얼)·
`mpfb_v11`(캐릭시안 여2+bob01+스포츠) 세 캐릭터를 다시 export했다.

**방법·좌표·검증 전부 `assets/ASSET_LICENSES.md`의 "2026-09-05(더
이어서) — 뺐던 캐릭시안·노년 아시아 스킨 셋을 눈 마스킹으로 마저 뽑음"
절에 있다** — 요약만 남긴다: `python`이 이 PC에서 안 되니 Blender 5.2
자체 Python(`--background --python`)으로 `image.pixels`를 직접
읽고 썼다. 세 스킨이 전부 같은 UV 레이아웃이라(직접 크롭해 확인)
좌표 하나(`(1690,970)`·`(1690,1132)`, 2048×2048 기준)를 그대로
재사용했다. Blender EEVEE 렌더로 셋 다 빨간 눈가 없이 자연스럽게
나온 걸 직접 눈으로 확인했다.

`js/asset3d.js`의 `HERO_RECIPES`에 셋을 더 얹어(QRPG 6 + MPFB 실사 8)
**총 열네 벌**이 됐다. `_test.html`의 인물 벌 수 진단도 14로 갱신,
`sw.js` → `go-v5.20.1`. 자가진단 423/423 3회 동일(회귀 없음).

**원본 스킨 텍스처(AppData, 저장소 밖)를 마스킹된 버전으로 덮어썼다** —
백업은 이 세션 스크래치패드에만 있고 커밋 안 됨(세션이 끝나면 사라진다).
필요하면 `makehuman_system_assets_cc0.zip`을 다시 받아 원본을 복원할 수
있다.

**남은 것**: 실기기 확인(이 세션도 Blender 렌더·헤드리스 크롬 진단에만
의존했다) · 눈썹이 일부 스킨에서 살짝 옅어진 것(눈에 띄진 않지만 더
보수적으로 마스킹 반경을 줄일 여지는 있다).

## 2026-09-05 (또또 이어서) — 캐릭시안/노년 아시아 "눈가 빨갛게" 원인 확정: 코드 버그 아님

바로 아래 항목이 남긴 "캐릭시안·노년 아시아 피부에서 눈가가 빨갛게 깨진다"
버그를 팠다. **결론: 코드 버그가 아니라 CC0 스킨 텍스처 원본 자체의 특성이다
— 고치려면 이미지 편집이 필요하고, 이번 세션에선 안 했다.**

**조사 과정(재현할 필요 없음, 결론만 다음 세션이 참고):**
- `.mhmat`(재질 정의) 파일을 `young_asian_male`(정상)과 `young_caucasian_female`
  (버그)로 직접 대조 — 구조가 완전히 같다(다른 건 색·텍스처 파일명뿐).
- MPFB 소스(`nodewrappergameengine.py`)를 읽어 스킨 재질의 실제 노드 배선을
  확인 — Base Color·Alpha(같은 diffuse 텍스처의 알파 채널을 다시 물림)·
  Normal 셋뿐, emissiveColor·specularColor 등 mhmat의 다른 필드는 애초에
  **안 쓰인다**(죽은 값). `update_principled_sockets_from_mhmat`도 diffuseColor는
  이미 링크된 소켓에 기본값만 헛되이 넣는 코드라 실질적으로 무효.
- 헤드리스 크롬으로 알파 채널을 직접 캔버스에서 읽어 대조 — **두 텍스처 다
  얼굴 영역에서 알파=255(완전 불투명) 균일**, 실제 눈꺼풀 알파컷은 아예 없다.
- Blender에서 두 스킨을 직접 빌드해 EEVEE로 렌더 — 원본 Blender 렌더에서는
  둘 다 비슷하게 "눈이 움푹 들어간" 모습이라 확연한 차이가 안 보였다.
- GLB를 Node로 직접 파싱(`glb_inspect.js` 방식 — fetch/three.js 없이 GLB의
  JSON+BIN 청크를 직접 읽어 재질·이미지를 뽑는다)해 대조 — **양쪽 다
  alphaMode=MASK로 똑같이 정상 export됐다.** 임베드된 diffuse 텍스처를
  다시 꺼내 봐도 원본 자산 파일과 완전히 같다(export가 이미지를 손상시키는
  게 아니다).
- **진짜 원인**: MakeHuman 스킨 텍스처는 실물 얼굴 사진을 그대로 UV에 편
  것이라, 눈 부위에 **눈동자·눈꺼풀이 사실적인 색(붉은기 포함)으로 이미
  그려져 있다**. 그런데 게임은 눈알을 별도 3D 메시(`eyes/low-poly`, 모든
  캐릭터 공통)로 얼굴 위에 얹는 구조라, **텍스처에 그려진 "가짜 눈"과 실제
  3D 눈알이 같은 자리에서 겹쳐 보인다** — 이건 사실 모든 스킨에 다 있는
  현상이다. `young_asian_male`·`middleage_african_male`처럼 사진 속 눈이
  저채도·저대비면 겹쳐도 안 띄고, `young_caucasian_female`·`old_asian_male`
  처럼 눈이 사실적이고 눈가가 붉으면 겹침이 뚜렷하게 "빨갛게 깨진다"로
  보인다. **알파 임계값이나 노드 배선 문제가 전혀 아니라서, 코드를 아무리
  고쳐도 안 없어진다.**

**진짜 고치는 방법(다음 세션이 원하면)**: 문제 되는 스킨의 diffuse 텍스처에서
눈 UV 영역만 주변 피부색으로 덮어 칠하는 이미지 편집(Blender 파이썬으로
`image.pixels`를 직접 건드리거나, Node/캔버스로 사각형을 채우는 식) — 그
스킨만 쓰는 캐릭터를 다시 export하면 된다. **지금은 그냥 그 스킨들을 안
쓰는 것으로 피해 뒀다**(이미 커밋된 상태, `mpfb_v3`·`v7`·`v8`는 다 이 문제가
없는 스킨으로 골랐다) — 급하지 않으면 이대로 둬도 된다.

**부가로 확인된 것 — 헤드리스 크롬 GLTFLoader가 로컬 GLB를 못 불러왔다.**
`file://`로도 `http://127.0.0.1`로도, `--virtual-time-budget`을 150초까지
줘도 `GLTFLoader.load()`의 성공/실패 콜백이 **아예 한 번도 안 불렸다**(에러도
없이 그냥 멈춤 — 오늘 세션 내내 겪은 "무거운 3D 장면에서 헤드리스가 멎는다"
현상과 같은 계열로 보인다, 원인 불명). 우회책으로 **GLB를 three.js/브라우저
없이 Node에서 직접 파싱**했다(GLB는 그냥 `JSON 청크 + BIN 청크`인 바이너리
포맷이라 12바이트 헤더 다음부터 청크 길이·타입을 읽으면 된다) — 재질·이미지
확인이 필요할 때 다음 세션도 이 방법을 쓰면 헤드리스 브라우저 없이 빠르게
검증할 수 있다(스크립트는 이 메모에만 남기고 저장소엔 안 커밋함).

## 2026-09-05 (더더더 이어서) — MPFB 실사 인물 다양성 늘리기(3벌 추가, 캐릭시안 계열은 새 버그로 보류)

사용자 확정("다양성부터 늘리기")으로 기존 남/여 두 벌에 셋을 더했다 —
`mpfb_v3`(중년 아프리카계 남 + 아프로 + 작업복), `mpfb_v7`(청년 아프리카계
여 + 땋은 머리 + 캐주얼), `mpfb_v8`(노년 아프리카계 남 + 짧은 머리 +
캐주얼). QRPG 6직업은 그대로 두고 추가 변형으로만 얹어 `HERO_RECIPES`가
총 열한 벌이 됐다.

**새 버그 하나 발견·회피**: 원래 넷을 뽑았는데 캐릭시안·노년 아시아 피부
셋(`young_caucasian_female`·`young_caucasian_female2`·`old_asian_male`)에서
눈가가 빨갛게 깨지는 게 헤드리스 렌더로 나왔다 — 원인 미확인, 그 셋은
빼고 이미 안전이 확인된 아시아·아프리카 피부 계열로만 채웠다. 자세한
내용은 `assets/ASSET_LICENSES.md`의 "MPFB2 + makehuman_system_assets"
절 참고.

자가진단은 처음 420/423으로 하나 깨졌다(`_test.html`의 "인물 N 벌"
테스트가 개수를 하드코딩하고 있어서 — 이미 문서에 "레시피 늘릴 때마다
같이 봐야 한다"고 적혀 있던 그대로) — 11로 갱신해 421/423 3회 동일로
되돌렸다(남은 둘은 무관한 기존 하늘 테스트). `sw.js` → `go-v5.19.18`.
실기기 확인은 못 했다(이 세션도 헤드리스 스크린샷에 의존했다 — 간이
확인 페이지 하나 만들어 `SkeletonUtils.retargetClip`을 직접 불러
검증했다, `_mpfb_variant_check.html`은 커밋 전에 지웠다).

**남은 것**: 캐릭시안/노년 아시아 눈 버그 원인 규명(알파클립 자체는
정상 판정되는데 눈 소켓만 깨진다 — 안구 메시 정합이나 그 피부들의
알파맵 인코딩을 의심해 볼 것) · 실기기 확인 · 원하면 재현 스크립트로
더 늘리기.

## 2026-09-05 (더더 이어서) — "GPU 직접 타일링" 제안 검토했다가 접음, 대신 진짜 메모리 누수를 찾아 고침

바로 아래 항목(바닥 캔버스 256→768) 뒤에 사용자가 "원본 이미지를 GPU에
직접 타일링하면 더 낫지 않냐"고 물었다 — **방향은 맞다.** 균일한 땅
한 종류라면 캔버스 굽기 없이 사진을 `RepeatWrapping` 텍스처로 바로
타일링하는 쪽이 해상도도 원본 그대로, 비용도 0, 종류마다(최대 6장)
공유해 메모리도 안 는다.

**그런데 이 지형엔 안 맞았다 — 실측하고 접었다.** `terrainAt()`
(`js/world.js`)이 48m 격자마다 `core.hash2(tx,ty)`를 **독립적으로**
굴려 땅 종류를 정한다(이웃 칸과 안 이어지는 순수 해시 노이즈, 값
노이즈처럼 보간되지 않는다). 땅 타일 한 장(≈244m)은 그 격자를 7×7=49칸
덮는데, 5000곳을 무작위로 뽑아 "49칸이 전부 한 종류인 타일"을 세었더니
**0/5000(0.000%)** — 가장 흔한 grass(60%)라도 0.6^49 ≈ 0이니 수학적으로도
당연했다(직접 node 스크립트로 확인, `hash2`를 그대로 복제해 돌림).
그러니 "균일한 타일만 GPU로 건너뛰기" 최적화는 절차적 세계에서
거의 절대 안 걸린다 — 구현했으면 seam·계절/밤낮 틴트 이중곱 같은 버그
자리만 늘리는 죽은 코드가 됐을 것이다. **그래서 캔버스 굽기는 그대로
두고 해상도만 올리는 이전 방식을 그대로 확정했다.**

**대신 벤치마크·설계를 다시 들여다보다가 진짜 있는 문제를 하나 찾았다
— `landTex` 캐시(구운 텍스처를 담는 곳)에 지우는 코드가 아예 없었다.**
`syncGround()`가 화면 밖 땅 **메시**는 지워도(`geometry.dispose()`) 그
메시가 쓰던 텍스처는 캐시에 그대로 남아 GPU 메모리를 계속 붙들었다 —
256px일 땐 한 장 0.3MB라 안 느껴졌을 텐데 768px(2.4MB)로 올린 지금은
오래 걸을수록 메모리가 계속 불어났을 것이다. `syncGround()`에 세대
카운터(`syncGen`)를 두어 고쳤다 — `landTexture()`·`terrainTexture()`가
캐시를 건드릴 때마다(새로 굽든 캐시 히트든) 텍스처에 `userData.gen =
syncGen`을 찍고, 한 세대 끝에 그 세대에 안 찍힌(=이번에 아무 살아있는
타일도 안 찾은) 항목을 `dispose()`하고 지운다. 계절이 바뀌거나
플레이어가 멀리 걸어도 저절로 청소된다 — 별도 LRU가 필요 없다. 화면
밖 메시를 지우는 자리에 `m.material.dispose()`도 같이 추가했다(재질
자체도 안 지워지고 있었다).

**성능 확인** — 굽기 루프만 떼어 벤치마크를 256~4096까지 넓혀 봤다
(GRID=48·span=244, 20회 평균): 256(4.9ms)·768(5.4ms)·1024(5.5ms)까지
사실상 공짜, 1536(7.6ms)부터 눈에 띄고 4096(28.1ms, 텍스처 67MB)에서는
확실히 무겁다 — 진짜 비용은 해상도(fillRect)가 아니라 서브셀마다 새로
만드는 `createPattern` 호출 쪽이었다. 자가진단(`_test.html`) 421/423
3회 동일, 회귀 없음.

**실기기(또는 헤드리스) 화면 확인은 이번에도 못 했다.** 이 세션 내내
헤드리스 크롬이 `--screenshot` 플래그를 쓰면(가벼운 장면조차, `#map-3d`
로도 시도) 5분 넘게 안 끝나 포기했다 — 원인 불명(자가진단은 `--dump-dom`
만 쓰는 가벼운 확인이라 매번 잘 됐다, `--screenshot`을 곁들이면 이 세션
동안 계속 걸렸다). **다음 세션(또는 실기기)이 확인할 것**: 잔디가 실제로
사진 결로 보이는지, 오래 걸어도(특히 계절이 여러 번 바뀌도록) 메모리가
안정적인지, `landTex` 캐시 청소가 실제로 도는지(`window.DG.world3d`엔
`landTex`가 노출돼 있지 않으니 콘솔로 보려면 클로저 안이라 직접 로그를
찍어야 한다).

`assets/ASSET_LICENSES.md`의 "ambientCG — 땅 소재 텍스처" 절에도 같은
내용을 적어 뒀다. `sw.js` VERSION → `go-v5.19.17`.

## 2026-09-05 (또 이어서) — 바닥 사진이 흐려 보이던 원인: 굽는 캔버스가 너무 작았다

사용자가 "바닥은 퀄리티가 없는데?"로 지적. 땅 소재 텍스처(`grass.webp` 등
ambientCG CC0 1K 사진, `assets/ASSET_LICENSES.md`의 "ambientCG — 땅 소재
텍스처" 절 참고)는 이미 2026-08-30부터 진짜 사진이었는데, 화면에 깔릴 땐
사진이 아니라 흐린 단색처럼 보였다 — **원본 사진이 아니라 그걸 화면에
굽는 캔버스가 문제였다.**

`js/world3d.js`의 `landTexture()`·`terrainTexture()`가 땅 타일 하나를 그릴 때
`var S = 256;`(캔버스 한 변 256px)로 구워 왔다. 그런데 땅 타일 한 장이 실제로
덮는 넓이(`span = TILE_PX(256) * metersPerPixel()`)는 이 판의 줌 레벨(17)·
위도에서 **약 244m** — 12m마다 반복되는 텍스처 무늬 한 칸이 캔버스에서 겨우
12.6px 로 뭉개지고 있었다(1024px 원본 사진의 디테일이 사실상 다 사라진다).
캔버스 해상도를 손잡이로 빼고(`LAND_TEX_RES()`, 기본 768) 두 함수의 `S`에
꽂았다 — 12m 반복 한 칸이 이제 ~37.8px 로, 사진 결이 남는다.

**성능 걱정(모바일 저사양)은 따로 벤치마크로 확인했다** — 실제 게임 전체가
아니라 이 두 함수의 캔버스 굽기 루프만 떼어내(`landPattern`·`fillRect`
그대로 복제) 256 vs 768로 20회씩 구워 견줬더니 **거의 차이가 없었다**
(256: 106.1ms, 768: 103.6ms — 타일당 ~5.2ms 로 동일). 진짜 비용은 해상도가
아니라 서브셀마다 새로 만드는 `createPattern` 호출 쪽이었다 — 해상도를
올려도 공짜였다는 뜻. 굽기 자체도 플레이어가 새 타일에 들어갈 때만 한 번
도는 것이라(매 프레임이 아니다) 더 여유가 있다. 너무 무거우면 손잡이로
되돌린다: `core.setTune('world3d.landTexRes', 256)`.

**실기기(또는 헤드리스) 화면 확인은 못 했다.** 이 세션 동안 헤드리스
크롬이 무거운 장면(`#walk-perf-3d` 등, 실사 에셋을 콜드 캐시로 다 불러옴)
에서 계속 멎어(각각 10~30분씩 실제로 기다리다 죽여야 했다) 포기했고, 그
와중에 실제 Chrome 브라우저 자체가 꺼져 있어 `claude-in-chrome` 확장도
연결이 안 됐다 — 원인은 못 밝혔다(이 판의 다른 헤드리스 확인은 가벼운
`_test.html`에선 평소처럼 잘 됐다, 45초 예산으로 421/423 3회 동일). 자가진단
회귀는 없다. **다음 세션(또는 사용자 실기기)이 눈으로 확인할 것** — 잔디가
실제로 사진 결로 보이는지, 768도 부족하면 더 올릴지(위 벤치마크에 따르면
여유가 있다).

## 2026-09-05 (더 이어서) — 옷 가중치 버그도 잡고 MPFB 실사 인물 QRPG에 통합 완료

바로 아래 항목이 남긴 옷 가중치 문제("걷는 동안 손·발이 소매·바지 끝에서
떨어져 보인다")를 사용자가 "옷 가중치부터 마저 고치기"로 확정해 이어서
팠다. **원인**: MPFB가 옷을 몸에 OBJECT 부모로만 붙이고 뼈 가중치를
아예 안 만들어 둔다(`vertex_groups` 0개 직접 확인) — 걷는 동안 스킨되는
몸은 변형되는데 옷은 바인드 포즈에 얼어붙어 있었던 것. 최근접 표면
보간(Data Transfer)으로 고치려 했더니 사타구니·겨드랑이에서 반대쪽으로
잘못 매칭돼 거대한 삼각형으로 늘어나는 부작용이 나서, Blender의
"Automatic Weights"(뼈 열확산, `parent_set(type='ARMATURE_AUTO')`)로
바꿔 완전히 고쳤다 — 걷기 사이클 네 지점에서 스크린샷으로 확인. 자세한
원인·시도·재현 스크립트는 `assets/ASSET_LICENSES.md`의 "MPFB2 +
makehuman_system_assets" 절 "버그 4" 항목에 있다.

**남녀 두 벌을 정식 통합했다**(대체가 아니라 QRPG 여섯 종에 추가) —
`assets/models/people/mpfb_real/male.glb`·`female.glb`(512px 텍스처,
각 3.9~4.1MB)로 최적화해 커밋했고, `js/asset3d.js`의 `HERO_RECIPES`에
`mpfb_male`·`mpfb_female` 두 항목을 추가했다(둘 다 `body`만 있고
`anim`이 없어 `buildHero()`가 공용 `ANIM_SRC`를 빌려 리타깃한다 — 위
버그 1~2가 고쳐져 있어야 맞물리는 경로다). `assets/models/_mpfb_test/`
임시 파일은 지웠다(로컬에도 없음, 재현 스크립트는 문서에 남겨 둠).

**부수 규칙 하나 걸림**: `_test.html`의 "인물 N벌이 갖춰져 있다"
자가진단이 옛 6벌·"outfit+hair 또는 anim 필수"를 하드코딩하고 있어서
레시피를 늘리자마자 깨졌다(421/423 → 420/423) — 몸만 있는 레시피도
유효하다는 조건을 넣고 8벌 기준으로 갱신, 다시 421/423 3회 동일
확인됐다(남은 둘은 기존부터 있던 하늘 관련 실패, 이번 작업과 무관).
`sw.js` VERSION → `go-v5.19.15`.

**아직 안 한 것**: 얼굴·머리카락은 남녀 각 한 벌뿐이라 QRPG의 직업별
다양성엔 못 미친다(다양성을 늘리려면 skin/clothes 조합을 더 뽑아야
한다 — 재현 스크립트가 있으니 어렵지 않다) · 실기기 확인 전이다.

## 2026-09-05 (이어서) — 남은 버그 둘 다 잡음. 옷 가중치가 새 남은 문제, 통합은 다음 상의

바로 아래 항목이 넘긴 버그 둘을 포크로 다시 팠다. **둘 다 리깅이 아니라
코드 문제였다** — 자세한 원인·수정은 `assets/ASSET_LICENSES.md`의
"MPFB2 + makehuman_system_assets" 절 "2026-09-05 세션이 마저 잡은 버그 둘"에
있다. 요약:

1. `js/asset3d.js`의 `buildHero()`가 조합형 몸(제 클립이 없어 `ANIM_SRC`를
   빌리는 몸)에 UAL1 원본 클립을 리타깃 없이 그대로 물리고 있었다 —
   `retargetInto()`로 다시 굽도록 고침(476행 함수는 그대로, 부르는 자리만 고침).
2. `retargetInto()`가 굽는 트랙 이름(`.bones[뼈이름].quaternion`)이
   `AnimationMixer`의 Group 뿌리에서는 안 풀려 **조용히 얼어붙고 있었다** —
   `뼈이름.quaternion` 평범한 이름으로 바꿔 고침. 이 수정은 함수 자체에
   있어 마을 사람 등 다른 배우 리타깃에도 같이 적용된다.
3. 머리카락·얼굴이 깨지던 것은 Blender 5.x 익스포터가 셰이더 노드로
   알파모드를 판정하는 방식으로 바뀐 걸 MPFB 배선이 못 맞춘 것 —
   Alpha 앞에 Greater-Than 노드를 끼워 MASK로 고침.

실제 게임 코드로 헤드리스 크롬 스크린샷까지 확인(휴식·맨몸 걷기 둘 다
정상). **자가진단 421/423 3회 동일**(회귀 없음), `sw.js` VERSION →
`go-v5.19.14`.

**새로 발견한 것 — 옷을 입히면 걷는 동안 손·발이 소매·바지 끝에서
떨어져 보인다**(맨몸은 완전 정상이라 리타깃 버그가 아니다, MPFB
`interpolate_weights`로 옷 가중치를 몸에서 보간하는 과정 추정 — 원인
미확인). 스크린샷(`shot_male_4.png`, 로컬에만 있음)으로 눈으로도
확인했다 — 클립 옷을 입은 채로는 아직 게임에 넣을 만한 상태가 아니다.

**`assets/models/_mpfb_test/`(빌드 스크립트·`.blend`·`.glb`·스크린샷,
약 58MB)는 커밋하지 않았다** — 로컬에만 있다. `DEFAULTS.hero`도
그대로 QRPG 유지(안 건드림) — 6종 QRPG를 완전히 대체할지, 옷 가중치
버그부터 마저 잡고 추가 변형으로만 넣을지는 사용자와 상의할 몫으로
남겨 뒀다.

## 2026-09-05 — 캐릭터 실사화, 처음으로 뚫렸다 (MPFB2). 마무리는 다음 세션

**다섯 판 어디서도 막힌 벽**(`ASSET_LICENSES.md`: "Mixamo 재배포 금지, 대안 CC0
팩은 애니메이션 0개")이었던 사람 캐릭터 지오메트리 실사화를, 이번엔 CC0 완제품이
아니라 **CC0 도구로 직접 뽑아서** 처음 뚫었다. 커밋은 아직 없다 — 버그 두 개를
안고 있어 다음 세션이 마무리할 것.

**증명된 것 (스크린샷으로 확인, 실제 게임 리타겟 코드 `asset3d.js`
`retargetInto`로 검증):**

1. 이 PC에 **Blender 5.2가 이미 깔려 있다**(`C:\Program Files\Blender
   Foundation\Blender 5.2\blender.exe`) — 헤드리스로 파이썬 스크립트를 돌릴 수 있다
   (`blender --background --python <script>.py`).
2. **MPFB2**(makehumancommunity.org, 코드 GPLv3·번들 에셋 CC0)를 Blender 확장으로
   설치했다:
   ```
   git clone https://github.com/makehumancommunity/mpfb2  (src/mpfb 가 확장 루트)
   blender --command extension build --source-dir src/mpfb --output-dir <out>
   blender --command extension install-file -r user_default -e <out>/mpfb-*.zip
   ```
   설치는 이 PC의 Blender 프로필(`%APPDATA%\Blender Foundation\Blender\5.2\`)에
   남는다 — **다음 세션은 이 설치 과정을 다시 할 필요가 없다**, 이미 돼 있다.
3. **뼈 이름이 우리 게임과 거의 완전히 같다.** `js/asset3d.js`가 걷기 등
   애니메이션의 원본으로 쓰는 `assets/models/anim/UAL1_Standard.glb`(Quaternius
   CC0)는 UE 마네킹 식 이름(`pelvis`·`spine_01~03`·`clavicle_l/r`·
   `upperarm_l/r`·`calf_l/r`…, 65개, 손가락 낱마디까지)을 쓴다. MPFB2가 제공하는
   리그 프리셋 중 `game_engine`(`src/mpfb/data/rigs/standard/rig.game_engine.json`)
   이 **정확히 같은 명명 규칙**(53개)이다 — `Root`→`root`, `head`→`Head`
   **두 개만 대소문자를 고치면 53/53 전부 일치**한다(직접 스크립트로 세어
   확인). 이건 우연이 아니라 둘 다 업계 표준 UE 마네킹 규칙을 따라서다.
4. **`makehuman_system_assets_cc0.zip`(267MB, CC0, `files2.makehumancommunity.org`
   에서 직접 받아짐 — 이 세션은 GitHub 아닌 임의 도메인도 `curl`로 바로
   받혔다, 예전 "GitHub만 열린다"던 제약은 이 세션엔 해당 안 됨)를 MPFB
   유저 데이터 폴더(`%APPDATA%\...\extensions\.user\user_default\mpfb\data\`)에
   풀어 넣으면 피부(동아시아 포함 다양한 인종·나이)·머리·옷·눈썹·눈썹속눈썹·
   이·혀까지 다 붙는다.
5. **끝까지 구워 본 스크립트**(`HumanService.create_human()` →
   `set_character_skin(..., skin_type="GAMEENGINE")` → `add_mhclo_asset()`로
   눈·눈썹·속눈썹·혀·이·머리·옷·신발 부착 → `add_builtin_rig(basemesh,
   "game_engine")` → 뼈 이름 두 개 고치기 → **`ExportService.create_character_copy`
   는 쓰지 말 것**(아래 함정 참고) → `bpy.data.objects`에서 타입 MESH 전부 +
   아마추어를 직접 선택해 `bpy.ops.export_scene.gltf(..., export_apply=True)`)로
   `young_asian_male`·`young_asian_female` 두 벌을 옷·피부까지 입혀 뽑았다.
   **실제 게임 리타겟 코드로 `Walk_Loop`를 입혀 렌더까지 확인** — 다리는
   정상적으로 걷는 자세가 나온다.

**밟은 함정:**

- **`ExportService.create_character_copy(basemesh, ...)`를 쓰지 말 것.** MPFB는
  옷·머리·눈 등을 전부 **basemesh의 자식**으로 붙이고, `add_builtin_rig`가
  **basemesh를 다시 아마추어의 자식**으로 만든다(아마추어→basemesh→옷,
  2단 부모). 그런데 `create_character_copy`는 `basemesh.parent`(=아마추어)를
  "루트"로 잡고 그 **바로 아래 자식만**(`get_list_of_children`, 재귀 없음)
  복제한다 — 옷·머리·눈은 그 아래 한 단 더 있어서 통째로 빠진다(직접 겪음 —
  몸통만 있고 재질도 `Human.body` 하나뿐인 GLB가 나왔다). **대신** 그냥
  `bpy.data.objects`에서 MESH 타입 전부 + 아마추어를 직접 선택해서
  `export_scene.gltf(use_selection=True, export_apply=True)`로 내보내면 다
  딸려 나온다(원본을 그대로 쓰므로 "원본 보존" 이점은 포기하지만, 이 스크립트는
  매번 `read_factory_settings`로 새로 시작하니 상관없다).
- **파일이 크면(20MB대) 헤드리스 크롬 `--virtual-time-budget`을 넉넉히
  줄 것.** swiftshader 소프트웨어 렌더가 20MB GLB(텍스처가 안 줄어든 상태)를
  파싱하는 데 45초로는 부족했다 — 120초를 줘서야 로드·리타겟·렌더가 다
  끝나는 걸 확인했다. 진짜 걸린 게 아니라 **느린 것뿐**이었다(콘솔에
  `onError`도 `onProgress`도 하나도 안 찍혀서 처음엔 완전히 막힌 줄
  알았다 — `window.onerror`·`unhandledrejection`까지 걸어도 아무것도
  안 잡혔다. 원인은 단지 메인 스레드가 파싱으로 오래 막혀 있었던 것).

**남은 버그 둘 (다음 세션이 이어갈 것):**

1. **머리카락·얼굴이 깨진다** — 스크린샷에서 대머리로 보이고 입 주변에
   붉은 텍스처가 얹혀 보인다. MPFB 머리카락 재질의 알파(투명) 처리가
   Blender→glTF 내보내기에서 안 넘어간 것으로 추정(정확한 원인 미확인).
   `10_complete_character_export_fbx.py`(MPFB 공식 예제)의 주석도 "쉐이더
   노드의 FBX/glTF 내보내기는 best-effort라 재질을 손봐야 할 수 있다"고
   미리 경고해 둔 자리다.
2. **팔이 T자로 안 움직인다** — 다리는 `Walk_Loop`로 정상 애니메이션되는데
   팔(clavicle·upperarm·lowerarm·hand 체인)은 리타겟이 먹지 않는다. 다리
   체인은 되는데 팔만 안 되는 걸 보면 전역적인 설정 문제(예: `hip: 'Hips'`
   — 우리 뼈대엔 그 이름이 없어 늘 무시돼 왔다, 다리에도 똑같이 영향을
   줬어야 하니 이건 원인이 아닐 가능성이 높다)보다는 팔 체인 자체의 무언가
   (원본·대상의 팔 휴식 자세 차이가 다리보다 훨씬 커서 `retargetClip`이
   그 체인에서만 깨지는 것 아닌가 의심 — 미확인, 다음 세션이 파볼 것).

**아직 안 한 것:** 남녀 각 한 벌만 뽑아 봤다(전사·궁수 등 QRPG의 6종 다양성에는
못 미친다) · 파일 최적화(`gltf-transform optimize`, 지금 20MB대 — 다른 실사
에셋들처럼 1MB 안팎으로 줄여야 함) · `asset3d.js`의 `DEFAULTS.hero`에 실제로
등록 · `ASSET_LICENSES.md`에 MPFB2·makehuman_system_assets 출처·라이선스 기록 ·
`sw.js` VERSION 올리기 · 자가진단 재확인. **테스트로 만든 파일
(`_mpfb_test.html`, `assets/models/_mpfb_test/`)은 커밋 전에 지웠다** — 저장소엔
아무 흔적도 없다, 스크립트만 이 메모에 남겼다.

## 2026-09-04 (더더더더더더더더더더더더더 이어서) — "바둑판 같다" 격자무늬 길, 마을 근처로 한정

열두 곳 실사화를 다 마친 뒤 사용자가 실기기에서 "탑 크기 괜찮아 보여" ·
"타일이 좀 아쉽긴해" → "바둑판같아보여서"로 제보. 원인은 텍스처가
아니라(도로는 이미 `road.webp`, ambientCG 실사 사진이다) **배치**였다 —
`js/world.js`의 절차적 폴백 지형(`terrainAt`)이 `tx%7===0‖ty%9===0`이면
**세상 전체에 무조건** 길을 세우고 있었다(SAGA-HANDOFF.md가 이전부터
"지형 문턱값과 맞물려 있어 다음에 상의"로 남겨 둔 바로 그 자리).

**선택지 넷을 드려 "마을 근처만 도로, 들판엔 없음"으로 확정받았다.**
`js/world.js`에 `nearTown(tx, ty)` 신설 — `terrainAt`이 마을을 가르는 것과
**같은 h 띠(0.34~0.40)**로 이웃을 본다. 반경은 임시 헤드리스 표본 페이지로
직접 세어 골랐다(자가진단엔 안 걸리는 자리라 -200..200 범위를 훑는 페이지를
따로 만들어 지우고 결과만 남김):

| 반경 | 마을 밖 길 밀도 | 판정 |
|---|---|---|
| 자기 칸만 | 2.85% | 마을에도 길이 거의 안 남는다 |
| **자기+상하좌우(십자 다섯 칸)** | **11.20%** | **채택** — 옛 값(23.8%)의 절반 이하, 마을 안팎 길은 안 끊긴다 |
| 3x3(반경 1) | 16.21% | 아직 높다 |
| 5x5(반경 2) | 22.90% | 옛 격자(23.8%)와 거의 같다 — 제한이 사실상 무효 |

`tx%7===0‖ty%9===0`인 칸도 `nearTown()`이 거짓이면 이제 h 문턱(물/산/숲/
마을/들)으로 그대로 떨어진다. **회귀 위험이 낮은 이유 셋**: ① `terrainAt`은
주석에 이미 "그리는 데만 쓴다 — 스폰·거리·조우는 안 본다"고 적혀 있다
② 마을의 집 각도 정렬(`propPlan('town',...)`의 `roadIsNS`)은 `gx%7`·`gy%9`를
**직접** 계산해 쓰지 `terrainAt`을 안 거친다 — 안 어긋난다 ③ `_test.html`의
"길이 하나로 이어진다"(`LD.roadIslands()`) 류 테스트는 전부 `land.js`의
**손으로 그린 시작 마을**(원점 근처, 경계 안쪽) 지도만 보는 것이라 이
바깥 절차적 지형과 무관함을 먼저 확인하고 손댔다. 자가진단 421/423
3회 동일. `sw.js` VERSION → `go-v5.19.13`.

**실기기 확인 전이다.** 십자 다섯 칸이 "적당히 자연스러운" 밀도인지는
숫자로만 가늠했다 — 실제로 보면 여전히 많거나(더 좁힐 것) 마을 사이가
너무 뚝뚝 끊겨 보일 수 있다(다시 넓힐 것). `nearTown()`의 `pts` 배열에
칸만 추가·삭제하면 되므로 다음 세션이 조율하기 쉽다.

## 2026-09-04 (더더더더더더더더더더더더 이어서) — 다리는 정말 없음 확정, 벼는 있는 걸로 대신

"계속 찾아봐"에 Sketchfab CC0(다리 15개+ 검색어)·Poly Pizza·ambientCG까지
더 훑었다. **다리는 CC0로 확정적으로 없다**(CC-BY 다리는 여럿 있으나
라이선스가 안 맞고, CC0 다리는 Poly Pizza 등에 있어도 전부 저다각형이라
실사화 취지에 안 맞는다) — 여기서 탐색 종료. **벼는 무료 생태계 자체에
작물 모델이 없다**(조리된 쌀 소품만 있음, ambientCG는애초에 3D 모델
사이트가 아니라 텍스처 전용이라 관련 없음). 사용자가 "벼가 없으면 다른
거 해도 돼" → "있는 걸로 채워 줄순 없어?"로 확인 — 새로 안 받고 이미
받은 `grass`용 `Shrub_04.glb`(Poly Haven CC0)를 `rice`에도 재사용했다.
`js/prop3d.js`의 `REG.rice.all` 교체, 옛 값은 `RICE_STYLIZED`. 자가진단
421/423 3회 동일. `sw.js` VERSION → `go-v5.19.11`.

**다리도 뒤이어 대체했다** — 사용자가 "비슷한 거나 다른 걸로 채워도 됨
· 자연스럽게만 · 콘셉트가 모두 허용"으로 확인해 줘서, 실사 다리 대신
**디딤돌 콘셉트**로 바꿨다. 이미 받아 둔 `rock`용 `MossRock_a.glb`를
`bridge`에도 재사용 — 짧은 칸을 여러 개 이어 강을 건너는 구조라 건물은
반복하면 어색해도 바위는 자연스럽다. `js/prop3d.js`의 `REG.bridge.all`
교체, 옛 값은 `BRIDGE_STYLIZED`. 자가진단 421/423 3회 동일. `sw.js`
VERSION → `go-v5.19.12`.

**이걸로 이 판의 "실사화" 탐색은 완전히 마무리다** — 손 안 댄 곳이 없다
(허수아비는 사용자가 "필요 없다"고 확인, 코드가 그린 채로 둔다).
**모든 새 실사 자산(집·탑·역참·성채·산봉우리·등롱·우물·장터·사당·굴·
폐허·벼·다리)이 실기기에서 아직 눈으로 확인 안 됐다** — 다음에 마을·
들판·역참·성채를 두루 돌아보며 확인할 것.

## 2026-09-04 (더더더더더더더더더더더 이어서) — 우물·장터·사당·굴·폐허도 실사로, 다리·벼만 남음

산봉우리·등롱 다음 "마저 찾아봐줘" 요청에 Sketchfab CC0 필터(역참 때 쓴
API 토큰 재사용)로 나머지 다섯을 찾았다: `well`→Ballinsloe Well Low(아일랜드
발굴 우물), `market`→Athenry Market Cross(중세 시장 십자가), `shrine`→
Wayside shrine "Pensive Christ"(폴란드 박물관 길가 성상), `cave`→
Toorelectra Sweathouse(아일랜드 한증막), `ruin`→Arco Romano De Cabanes
(스페인 로마 아치 유적). 전부 그 물건 자체는 아니지만 사용자가 이미
"아니여도 됨"으로 승인한 범위. `@gltf-transform/cli optimize`로 다듬음
(16.9MB~33.3MB → 253KB~1.27MB). `js/prop3d.js`의 `REG.well/market/shrine/
cave/ruin`을 갈아 끼우고 옛 값은 `*_STYLIZED` 상수로 남김. 자가진단
421/423 3회 동일. `sw.js` VERSION → `go-v5.19.10`.

**다리·벼는 끝내 못 찾았다** — 10여 개 검색어를 다 돌려도 이 CC0 풀
(유럽 문화유산 디지털화)엔 다리 유적이 없었다(기념물·건물·석상 위주).
벼는 애초에 이 풀에 있을 리 없어 검색 안 함. **이걸로 이 판의 실사화
탐색은 마무리 지어도 될 듯 — 다음에 "실사화" 요청이 다시 오면 이 절부터
읽고 다리·벼는 또 찾지 말 것**(사용자가 다른 사이트를 더 알려주지 않는
한). 자세한 출처·조사 이력은 `assets/ASSET_LICENSES.md`.

**실기기 확인 전이다.** 특히 사당(`wayside_shrine.glb`, 원래 성상 조각상
비례 — 정자만큼 넓적하지 않을 수 있다)·굴(`sweathouse.glb`, 돔형이라
"입구"로 읽히려면 각도가 중요할 수 있다)은 원래 물건과 형태 차이가 커서
크기·회전이 어색할 가능성이 다른 것들보다 높다.

## 2026-09-04 (더더더더더더더더더더 이어서) — 산봉우리·등롱도 실사로

역참·성채 다음으로 "다른 것도 진행해줘" 요청. Poly Haven 전체 521개 모델을
`category`로 다시 훑어 `mountainside`(절벽·산비탈 스캔)·`wooden_lantern_01`
(집·탑을 준 `smugglers_cove` 컬렉션)을 찾았다. `@gltf-transform/cli`로 여러
파일(gltf+bin+텍스처)을 단일 glb로 구웠다(mountainside 15.4MB→1.26MB,
lantern 14MB→233KB). `js/prop3d.js`: `REG.peak.all`→`mountainside.glb`,
`REG.lamp.all`→`wooden_lantern.glb`. 옛 값은 `PEAK_STYLIZED`·`LAMP_STYLIZED`.
우물·장터·사당·굴·폐허·다리·벼는 Poly Haven에 그 모양 자체가 없어서 그대로
저다각형이다(다리 후보였던 `modular_wooden_pier`는 강다리가 아니라 부두라
안 어울려 뺐다). 자가진단 421/423 3회 동일. `sw.js` VERSION → `go-v5.19.9`.
자세한 출처는 `assets/ASSET_LICENSES.md`. **실기기 확인 전이다.**

## 2026-09-04 (더더더더더더더더더 이어서) — 역참·성채도 부분 실사로 (Sketchfab CC0 문화유산 스캔)

집·탑을 실사로 바꾼 뒤 사용자가 역참·성채까지 요청. Poly Haven 전체(521개)·
PolyScan(20개, 새로 나온 4개는 Patreon 가입 필요라 제외)을 다시 훑어도 못
찾았는데, 사용자가 "건물은 현대·미래·과거 상관없어" · "성채가 아니여도 됨"
으로 범위를 넓혀 줘서 Sketchfab에서 CC0 문화유산 사진측량 스캔(아일랜드·
스페인 고성·탑 폐허)을 찾았다. 다운로드는 API 토큰이 있어야 해서 사용자가
직접 무료 Sketchfab 계정을 만들고 토큰을 줬다(계정 생성 자체는 이 세션이
대신 하지 않았다 — 계정 만들기는 대신 못 하는 일이라 안내만 했다).

`Renvylle Castle`(13~14세기 탑성 폐허) 하나를 받아 `@gltf-transform/cli`로
다듬었다(Blender 없이 npm 패키지만으로 됨) — 13.4MB → 584.5KB. 같은 검색에서
나온 두 개는 뺐다: `Castillo de Montroi`는 이 판 로더가 모르는 필수 확장자
(`KHR_materials_pbrSpecularGlossiness`)를 요구해서, `Galway City - Spanish
Arch`는 아무리 다듬어도 15.6MB 밑으로 안 줄어서. 자세한 건
`assets/ASSET_LICENSES.md`의 "Sketchfab" 절.

`js/asset3d.js`: `station`(역참) → `tower_ruin.glb`(옛 `Inn.glb`는
`STATION_STYLIZED`로 되돌림 자리), `fort:t3`(웅진) → `tower_round.glb`(집·탑
때 쓴 그 파일 재사용, 옛 값은 `FORT_T3_STYLIZED`). 보·진 등급은 저다각형
그대로(실사 탑이 한 종류뿐이라 세 등급을 다 못 채운다). `delam()`(사람·짐승
재질 벗기는 함수)에 `/realistic/` 검사를 새로 넣었다 — `prop3d.js`엔 이미
있던 고침인데 `asset3d.js`가 실사 자산을 처음 받아서 이번에 옮겨야 했다.
`_test.html`의 역참·성채 파일명 검사도 새 파일명에 맞춰 고침. 자가진단
421/423, 3회 동일. `sw.js` VERSION → `go-v5.19.8`.

**실기기 확인 전이다.** 특히 `asset3d.stationScale`(1.73)·`fort:t3`의 배율
(`asset3d.fortScale`×1.24≈3.27)은 옛 저다각형 기준으로 잡힌 값인데,
`tower_ruin.glb`·`tower_round.glb` 둘 다 정규화 키 1당 폭도 거의 1인
"뚱뚱한" 모델이다 — 바로 앞 절에서 `prop3d.towerScale`이 똑같은 이유로
너무 커 보였던 것과 같은 함정을 밟을 공산이 크다. **다음에 역참·성채를
직접 보고, 크면 `asset3d.stationScale`·`asset3d.fortScale`을 낮출 것.**

## 2026-09-04 (더더더더더더더더 이어서) — 마을 집·탑도 실사로 (사가블로가 오늘 찾은 것 이식)

사용자가 "실사화" → "안한부분 찾기" → "실사화 작업 진행"으로 요청. `HANDOFF.md`의
"캐릭터 실사화는 천장, 다시 찾지 말 것" 절은 그대로 두고, 아직 안 건드린 자리를
찾아보니 **건물(집·탑)** 이 여태 옛 Kenney 류 저다각형 그대로였다(`js/prop3d.js`
`REG.house`/`REG.tower`, `assets/models/buildings/` 밑, `/realistic/` 아님).

사가블로(`saga-dungeon`)가 **같은 날 먼저** PolyScan(집 둘: `house_stone`·
`house_wooden`)·Poly Haven `modular_fort_01`(탑 하나: `tower_round`)에서 CC0
사진측량/PBR 모델을 새로 구해 `assets/models/buildings/realistic/`에 넣어 둔 걸
발견 — 나무·바위 때와 같은 길로 그대로 옮겼다(md5 동일 확인, 변환 과정은
새로 안 밟음). `js/prop3d.js`:
- `BLD_REAL` 상수 신설, `REG.house.all`→`house_stone`·`house_wooden`,
  `REG.tower.all`→`tower_round`(한 벌뿐)
- 옛 다섯 채·다섯 탑은 `HOUSE_STYLIZED`·`TOWER_STYLIZED` 로 이름만 바꿔 되돌림
  자리로 남김(지우지 않음)
- 재질 안 벗겨지는 것(`looksRealistic()`)은 경로에 `/realistic/`만 있으면 되므로
  **코드 추가 없이 자동으로 적용됨**(나무 때 고친 그 검사를 그대로 탄다)

**`asset3d.js`의 역참(`station`)·성채(`fort:t1~t3`)는 일부러 손 안 댔다** — 그쪽은
등급마다 다른 탑이 서야 하는데(자가진단 `/Watchtower/.test(a)` 등이 그걸 본다)
실사 탑 파일이 한 종류뿐이라 옮기면 세 등급이 다 같아진다. `assets/ASSET_LICENSES.md`
에 새 절 추가. 자가진단 421/423, 3회 동일 — 회귀 없음(집·탑 파일 이름만 바뀌었을 뿐
REG 표를 읽는 로직은 그대로라 원래도 안전할 자리였다). `sw.js` VERSION →
`go-v5.19.6`.

**실기기로 눈으로 보진 못했다** — claude-in-chrome 확장이 이번 세션엔 연결 안 됨.
집이 마을 안에서 잘 서는지(새 GLB의 원본 축·바닥 정렬이 나무·바위와 다를 수
있다), 탑이 예전보다 너무 작거나 커 보이지 않는지(`houseScale` 1.8이 새 모델
비례에도 맞는지) **다음에 보면서 확인할 것**.

**→ 곧바로 사용자가 실기기로 확인, 탑이 "너무 크게" 보인다고 제보.** 원인은
셈이 아니라 모양이었다 — 옛 Watchtower 류 첨탑은 키에 비해 홀쭉해 커도 안
거슬렸는데, `tower_round`(원형 성곽 탑)는 정규화 키 1당 폭도 거의 1이라
굵다. `instGlb`가 x·y·z를 전부 같은 `hh`(키×배율) 하나로 균일하게 늘이는
구조라 폭도 키를 따라 같이 불어난다. `js/prop3d.js`의
`heightMul('tower')`(`prop3d.towerScale`)를 1.4→0.6으로 내려 고침 — 사용자
재확인 "이제 딱 맞아"로 마무리. **집도 이어서 확인 요청 → 사용자가 마을을
둘러보고 "딱 맞아"로 확인** — `houseScale`(1.8) 그대로 두고 코드 변경 없음.
집·탑 둘 다 실기기 확인 끝(2026-09-04). 이 절의 실사 건물 작업은 여기서 닫는다.

## 2026-09-04 (더더더더더더더 이어서) — 겨울 물색 실기기 확인 → 가을과 안 갈려서 진하게 다시

바로 앞 절에서 넣은 겨울 물색(`season.js` 의 `SEASONS.winter.leaf` 값 그대로,
0x5f6a5c)을 claude-in-chrome 으로 실제 화면에 띄워 사용자와 같이 봤는데,
가을(0xa87a2e, 따뜻한 황갈색)과 **거의 안 갈렸다** — 사진 텍스처의 갈색
나뭇가지 톤이 세서 흐린 회녹색 곱으로는 못 이겼다. `season.js` 의 그 값은
애초에 **손그린 단색 땅**(`land.js`)을 물들이려고 고른 값이라 사진 텍스처
곱색에는 안 맞았던 것 — 이번엔 나무 전용으로 **더 차갑고 진한 청회색**
(`0x4d5b66`)으로 바꿨다(`season.js` 와 값이 갈라지는 걸 감수한다 — 주석에
적어 뒀다). `_test.html` 기대값도 같이 고침. 자가진단 421/423 그대로.
`sw.js` VERSION → `go-v5.19.5`.

사용자가 "겨울 CC0 나무 있는지도 찾아봐" → "더 찾아봐줘" → "더 광범위하게" 로
거듭 요청 — Poly Haven(API로 전체 모델 카탈로그를 snow/winter/autumn/frost/ice로
훑음, 0건) · PolyScan · OpenGameArt(2건) · Sketchfab(검색·CC0필터·컬렉션·전문
업체 4Datum) · itch.io Nature Kit(329개, 알고 보니 Kenney 재포장) · ambientCG ·
Poly Pizza · Quaternius 공식 · ToxSam CC0 GLB 레지스트리(991개, christmas
컬렉션의 `XmasTree`도 확인 — 실내 미니어처라 안 맞음) 까지 총 12곳 이상을 봤다.
**여전히 없다** — 사가의숲이 이미 낸 결론과 같다. 사진측량급으로 보이는 것 하나
(Sketchfab "Snowy tree", ScanLife, 8.3만 폴리곤)를 찾았지만 **CC0 아니고
CC-BY + NoAI 제한**이라 이 저장소 기준(CC0만)에 안 맞아 뺐다.

**대신 사용자 승인("그걸로라도")을 받아 다른 길로 풀었다** — 새 모델을 못 찾는
대신, **여름에 쓰는 같은 실사 나무(`IslandTree_02.glb`)를 가을·겨울에도 그대로
쓰고 재질 색만 계절에 맞게 곱한다.** `prop3d.js`:
- `REG.tree.autumn`·`REG.tree.winter` 를 `all` 과 **같은 파일**로 바꿈(옛
  `CommonTree_Autumn/Snow_*.glb` 경로는 `TREE_AUTUMN_STYLIZED`/
  `TREE_WINTER_STYLIZED` 상수에 되돌림 자리로만 남김)
- `seasonTintHex(sk)` 신설 — 가을·겨울에만 16진 색을 낸다. 값은 `season.js`의
  `SEASONS.autumn/winter.leaf` 를 그대로 옮겨 적었다(화면의 다른 나무·풀과
  같은 물색으로 맞추려고 — 손으로 맞춘 값이라 `season.js` 쪽이 바뀌면 여기도
  손으로 맞출 것). 봄·여름은 `null`(실사 텍스처를 그대로 둔다 — 곱하면 탁해진다)
- `tintedOf(mat, url, hex)` 신설 — 원본 재질을 **복제**해 `color` 만 곱한다
  (텍스처는 그대로 물려받아 가볍다). 원본은 손 안 댐 — 그대로 두면 여러 철·
  여러 자리가 나눠 쓰는 재질이 전부 물든다
- `parts()` 가 `name==='tree'` 이고 물색이 있으면 `material.map` 있는 조각만
  물들여 낸다(옛 저다각형 되돌림 조각엔 `map` 이 없어 자동으로 안 물든다)

`_test.html` 의 옛 "철마다 다른 나무가 선다(파일명 Autumn/Snow 검사)"는 이제
안 맞아서 "같은 파일 · 봄여름 안 물듦 · 가을 #a87a2e · 겨울 #5f6a5c 물듦"으로
바꿨다. 자가진단 421/423(3회 동일, 남은 실패 2개는 기존 날씨 항목) — 새 테스트
포함해 회귀 없음. `README.md` 의 옛 설명도 고쳤다. `sw.js` VERSION →
`go-v5.19.4`.

**실기기/실브라우저로 색이 실제로 잘 보이는지는 확인 못 했다** — claude-in-chrome
확장이 이번엔 연결이 끊겨 있었다(사용자가 크롬을 껐다 켰다 한 뒤). 순수 함수
단으로는 확인됐다(가을 물색 0xa87a2e·겨울 0x5f6a5c 를 낸다). **다음에 보면서
확인할 것** — 특히 곱색이라 사진 질감 위에서 실제로 "가을답게" 보이는지,
아니면 그냥 탁한 초록으로 보이는지는 눈으로 봐야 안다.

## 2026-09-04 (더더더더더 이어서) — "실사화 안 적용된듯" 제보 → 진짜 원인은 재질이 벗겨지고 있었다

사용자가 배포 확인 후 실기기에서 "실사화 안 적용된듯" · "사가의숲처럼 적용돼야
해" 로 제보. GLB 자체는 잘 받아지고 있었다(자가진단·네트워크 200 확인) — 문제는
**지오메트리가 아니라 재질**이었다. `prop3d.js` 의 `partsOf()` 가 받은 GLB 재질을
**전부** `lambertOf()` 로 넘겨 빛깔 하나만 남기고 텍스처·거칠기 맵을 통째로 버리고
있었다. 옛 Quaternius 저다각형 모델은 원래 면마다 단색이라 잃을 게 없었지만,
Poly Haven 사진측량 모델은 **그 텍스처 자체가 실사화의 전부**라 벗기면 매끈한
회색 덩어리로 보인다 — 모양만 바뀌고 실사처럼 안 보인 이유. 사가의숲(`asset3d.js`
의 `looksRealistic()`/`delam()`)이 2026-09-02 에 이미 겪고 고친 자리인데, 이번
포팅(`6eacf6e`)이 "새 코드 없이 표만 갈아 끼웠다"고 적은 그대로 **이 exception은
안 옮겨 왔다.**

**고침**: `looksRealistic(url)`(경로가 `/realistic/` 밑이면 참)을 넣고, `partsOf`
에 url 을 같이 넘겨 실사 자산이면 GLB 원본 PBR 재질을 그대로 쓰고(사가의숲과
같은 조건 — `world3d.js` 의 IBL 이 이미 있어 PBR 이 까맣게 뜨는 옛 문제 없음),
옛 저다각형 계열만 여전히 `lambertOf`로 벗긴다. 자가진단 421/423, 3회 동일 —
회귀 없음(이 값을 내는 로직은 three 없이도 도는 순수 함수라 diag 로는 재질까지는
못 본다 — **실기기 재확인이 다시 필요하다**). `sw.js` VERSION → `go-v5.19.3`.

고친 뒤 헤드리스로 스스로 확인해 보려고 `_matcheck.html`(GLB 하나만 떼어 재질
타입·텍스처 유무를 로그로 찍는 최소 페이지, 무거운 게임 장면 없이)을 새로 만들어
찍어 봤는데 **이마저도 걸렸다** — `THREE`·`GLTFLoader`·`WebGLRenderer` 생성까지는
로그가 찍히는데 그 다음 `GLTFLoader.load()` 콜백(성공이든 실패든)이 90초 넘게
한 번도 안 왔다. 즉 아래 절의 "장면이 무겁다" 추정은 **틀렸을 수 있다** — 무거운
`#map-3d` 뿐 아니라 **GLB 파일 하나만 받는 최소 페이지에서도** 이 헤드리스
조합(`--use-angle=swiftshader` 등)이 걸린다. 진짜 원인은 이 환경의 `GLTFLoader`
(텍스처 디코드 쪽 의심)로 보이나 더 파지 않았다 — 확인용 파일이라 커밋 안 하고
지웠다. **다음 세션도 이 판의 GLB 로딩을 헤드리스로 확인하려 하면 시간 버리지
말고 바로 실기기·사용자 확인으로 넘어갈 것.**

## 2026-09-04 (더더더더 이어서) — 자연물 실사화 헤드리스 확인 시도, 실패(장면이 무겁다) + 사고 기록

앞 절이 남긴 "실기기에서 눈으로 확인 전이다"를 이번 세션이 먼저 헤드리스로
때워 보려 했으나 **실패했다** — `CLAUDE.md`의 "헤드리스로 3D를 볼 수 있다"
플래그(`--use-angle=swiftshader` 등)로 `_demo.html#map-3d`를 찍으려 했는데,
90초 하드 타임아웃(`timeout 90`)에도, 600초 넘는 백그라운드 대기에도 스크린샷
파일이 한 번도 안 생겼다. 창 크기를 줄이고(`640x480`) `#noao#nowater`로
후처리를 꺼도 같았다. **이 판의 실사 자연물 장면(사진측량 GLB 여러 개 + IBL +
후처리)이 그 문서가 성공했던(사가블로 바닥·벽) 장면보다 훨씬 무거워 소프트웨어
렌더로는 현실적 시간 안에 안 끝나는 것으로 보인다** — 다음에도 이 판의
`#map-3d` 류를 헤드리스로 찍으려 하면 같은 벽을 만날 가능성이 크니, 시간 쓰기
전에 이 기록부터 볼 것. **실기기 확인은 여전히 사용자 몫으로 남아 있다.**

이 과정에서 걸린 헤드리스 크롬을 정리하며 `taskkill /IM chrome.exe /T` ·
`taskkill /IM node.exe /T` 를 **PID로 안 좁히고 이름으로 통째** 돌렸다 —
이 PC의 허브(`services.json` 기반, 이 세션이 띄운 것과 무관)까지 같이
멎었을 수 있다. 사용자에게 알리고 확인/재시작을 맡겼다. **다음 세션은
헤드리스 정리할 때 반드시 PID로만 끌 것.**

## 2026-09-04 (더더더 이어서) — "실사화" 재요청: 자연물(나무·바위·수풀)은 됐다

앞 절에서 "사람 캐릭터 실사화는 무료로 천장"이라 닫았는데, 사용자가
"사가블로는 했는데"로 다시 요청 — 알고 보니 **사가블로·사가의숲이 이미
한 것은 사람이 아니라 자연물**(나무·바위·수풀)의 Poly Haven CC0 사진측량
스캔 교체였다. 이건 안 막힌 길이라 그대로 옮겼다(커밋 `6eacf6e`) —
`prop3d.js`의 `REG.tree/pine/rock/grass`를 사가의숲이 검증해 둔 실사
GLB로 갈아 끼웠다(md5 동일 확인, 새 코드 없음). 가을·눈은 여전히 저다각형
(CC0 소재 자체가 없다, 사가의숲도 못 채운 자리). 건물(집·역참·성채)은
`이전 절 기록`대로 여전히 막힌 길 — 재조사 안 함.

`tree.all`이 실사 한 벌뿐(가벼운 후보가 그것뿐)이라 자가진단의 "에셋이
갈린다" 검사를 `rock`(다섯 벌)으로 옮겨 통과시켰다 — `_test.html` 참고.
421/423, 기존과 동일. **실기기에서 눈으로 확인 전이다** — 다음에 보면서
확인할 것(특히 `tree.all` 한 벌뿐이라 나무가 전부 똑같아 보이는 게
실제로 거슬리는지).

## 2026-09-04 (더더 이어서) — "캐릭터 실사화" 재조사 후 결론: 지금 수준이 천장, 그만 찾는다

이전 세션(막다른 길 → IBL로 방향 전환)에 이어 이번 세션도 "실사화 더 해달라"
는 요청에 CC0/CC-BY + 재배포 가능 + 애니메이션 포함 실사풍 인간 팩을 다시
찾아봤다(Quaternius·Kenney·OpenGameArt·itch.io CC0 태그, Sketchfab, Mixamo
약관 재확인). **똑같은 결론이 다시 나왔다** — 그런 팩은 사실상 없다.

- 무료 계열(Quaternius·Kenney·OpenGameArt 등)은 전부 스타일라이즈드/로우폴리뿐
- **Mixamo**: "완성된 게임 안에 넣어 배포"는 되지만 "원본 파일을 에셋으로
  재배포"는 금지 — 이 저장소는 `.glb`를 **공개 git 저장소에 원본 그대로
  커밋**해 누구나 그 파일만 따로 받아갈 수 있는 구조라 후자에 걸릴 소지가
  크다(전 세션이 피한 것과 같은 이유)
- 사진 스캔류(Vitruvian Project 같은) CC0 모델은 여전히 애니메이션이 없다

**사용자가 직접 결정**: 돈 안 쓰고, **지금 수준(QRPG 저폴리 모델 + IBL 환경광
반사)을 천장으로 삼는다.** 유료 자산(Synty Realistic·Reallusion CC 등) 구매는
검토 대상이 아니다. **이 주제는 여기서 닫는다 — 다음 세션이 "실사화" 요청을
받아도 이 조사를 또 반복하지 말 것.** 사용자가 먼저 유료 자산 이야기를 꺼내면
그때만 가격·라이선스 조사를 다시 연다.

## 2026-09-04 (더 이어서) — "모바일에서 캐릭터가 녹색" 결말: 코드 결함 아님, 묵은 SW 캐시

지난 절에 남겨 둔 "다음 세션은 실제 폰 스크린샷·콘솔로그부터" 를 따라 실기기
증거를 모아 좁혔다.

1. **격리 진단 페이지**(`_glbcheck.html`, 신설) — GLB 하나만 떼어 fetch·파싱·
   텍스처 디코딩·3D 렌더를 게임 코드와 무관하게 그 자체로 시험한다. 실기기
   (아이폰16·사파리)에서 돌려 보니 **전부 정상**이었다 — GLB 파일·텍스처·
   WebGL 렌더링 자체는 이 기기에서 문제가 없었다.
2. 그래서 의심을 "GLB 자체" 에서 "게임이 GLB로 갈아 끼우는 절차" 쪽으로 옮겨
   `index.html` 에 **`?debug=1`** 로 여는 실기기 상태 오버레이를 심었다
   (`world3d.stats()`·`asset3d.stats()` 를 1초마다 화면 구석에 글자로 —
   콘솔 없이 폰 화면만으로 `built`(도형으로 세운 수) vs `swapped`(실제
   GLB로 갈아 낀 수) 를 바로 읽는다). 평소엔 아무 영향 없다.
3. 처음 폰(아이폰16) 사파리로 열었을 때 **`built=0 swapped=0`** — GLB 시도
   자체가 없었다는 뜻이라 `world3d.glb` 손잡이가 꺼져 있나 의심했으나,
   `_admin.html` 확인 결과 **손잡이 없음**(전부 기본값) — 이 갈래는 아니었다.
4. **진짜 갈림길은 브라우저였다.** 알고 보니 애초에 녹색 덩어리 스크린샷도,
   `built=0` 도 전부 **iOS 크롬**에서 난 것이었다(사파리는 처음부터 멀쩡했다).
   그런데 **같은 크롬으로 다시 열어 보니 이번엔 `built=11 swapped=11`**
   (전부 성공) — 코드를 더 고치지 않았는데 저절로 나았다.
5. **결론(가장 앞뒤가 맞는 설명)**: 코드 결함이 아니라 **묵은 서비스워커
   캐시**였다. 이 세션 동안 `sw.js` VERSION 을 두 번 올렸고(`_glbcheck.html`
   추가·`?debug=1` 오버레이 추가), 그 전 세션에서도 지도·마을 수정 때마다
   올렸다(`CLAUDE.md`: "VERSION 안 올리면 이미 연 폰이 옛 파일만 받는다") —
   **iOS 크롬이 사파리보다 그 옛 캐시를 더 오래 붙들고 있었던 것**으로 보인다.
   `_glbcheck.html`(격리 진단)과 `index.html?debug=1`(실기기 GLB 상태 오버레이)
   둘 다 지우지 않고 남겨 둔다 — 다음에 비슷한 제보가 오면 바로 같은 손으로
   좁힐 수 있다.

## 2026-09-04 (이어서) — "지형·배치가 어색하다" 감사, 지도 서비스 의존 제거

사용자가 실제로 화면을 보고 "오버월드가 맞는지 모르겠다" → "지형·배치가
어색함" → "젤다의 전설 같은 오픈 월드" → "바닥 타일이 허접해, 실사 찾아줘"
로 구체화. 헤드리스 스크린샷을 여러 각도로 찍어 파본 결과:

1. **진짜 원인 — CartoCDN 지도 서비스가 API 키를 요구하게 바뀌었다.**
   `js/world.js`의 `tileUrl()`이 `basemaps.cartocdn.com`에서 배경지도를
   받아 3D 바닥에 깔았는데, 이제 그 서비스가 "API KEY REQUIRED" 라고 적힌
   초록 워터마크 이미지를 돌려준다. `<img>.onload`는 그래도 불러 실패로
   안 잡히고, `landTexture()`가 그 이미지를 12%(1-PAINT_A)로 덧칠해 마을
   한복판에 그 글자가 그대로 비쳤다. **고쳤다**(커밋 `1dc52c7`) — 절차적
   세계는 애초에 그 지도 좌표와 무관한 해시로 정해지므로 지도 사진에 기댈
   까닭이 없었다. `landTexture`(손으로 그린 땅)·`terrainTexture`(그 밖의
   전체 세계, 신설)둘 다 이제 `terrainAt()` 종류별 실사 WebP 텍스처만 쓴다.
2. **경계를 노이즈로 흐림**(커밋 `383d5ee`) — 칸을 4×4로 쪼개 이웃 종류가
   해시로 스며들게 했다. 국지적 경계는 부드러워졌다.
3. **그래도 안 없어지는 직선이 남아 있다** — `terrainAt()`이
   `tx%7===0 || ty%9===0`이면 무조건 'road'를 내는 **세계 전체에 걸친
   완전한 격자무늬 길**을 갖고 있고, `core.hash2`가 옛 식(0~0.5만 반환,
   `CLAUDE.md`에 이미 적힌 문제)이라 특정 축을 따라 줄무늬가 생길 수
   있다. **이 둘은 지형 문턱값과 손으로 맞물려 있다**(CLAUDE.md: "고치려면
   지형·구역 이름을 다시 눈으로 보며 조율해야 한다") — 다음 세션이 사용자와
   상의해 손볼 것. 이번 세션은 그 앞의 텍스처 경계만 고쳤다.
4. ~~**마을 배치**(길·군집 규칙)~~ — **2026-09-04(이어서) 완료**(커밋
   `e1da54e`). `propPlan('town', ...)`을 고쳐 ① 집 각도를 가장 가까운
   길(`tx%7`·`ty%9` 격자길) 방향에 맞춰 정렬 + 살짝만 흔들고, ② 자리는
   칸을 작은 대지(lot) 격자로 나눠 대지 하나에 집 하나씩 앉혔다(대지
   안에서만 흔든다). 스크린샷으로 확인 — 이제 집들이 한 방향으로 늘어선
   줄로 보인다(전에는 각도·자리 다 완전히 따로 놀아 아무렇게나 흩어진
   것처럼 보였다). 곁들여 **길가 등롱 방향 버그**도 고쳤다 — 가로길
   (`ty%9===0`)에도 세로길 등롱 배치를 그대로 쓰고 있었다. `houseRects()`가
   `propPlan`을 그대로 다시 불러 쓰므로 벽 충돌·교전 무대 판정도 저절로
   새 자리를 따라간다(따로 손볼 곳 없음). 진단 421/423 회귀 없음.
   ~~**캐릭터 실사화**~~ — **2026-09-04(이어서) 조사 후 방향 전환 완료**
   (커밋 `bd23a40`). 사가의숲의 "Mixamo 실사 캐릭터"를 그대로 베끼려 했으나
   **막다른 길이었다** — Mixamo 는 재배포 금지라 사가의숲 자신도 그 파일을
   `.gitignore`해 놓고 실제 기본값은 사가고와 **똑같은 Quaternius CC0**로
   되돌린 상태였다(사가고가 이미 그 등급). 대안으로 찾은 CC0 사실적 인간
   팩 "Vitruvian Project"는 Blender 로 직접 열어(headless 스크립트로
   `bpy.data.actions` 등 전수 조사) **애니메이션이 0개**임을 확인 —
   영화 렌더용 809본 정밀 리그일 뿐 게임용이 아니었다. 그래서 **지오메트리는
   그대로 두고 재질 반응만 개선**하는 쪽으로 방향을 틀었다 — 사가의숲이
   쓰는 Poly Haven CC0 HDRI(`alps_field_1k.hdr`)를 그대로 재사용해
   `world3d.js`에 IBL(환경광)을 얹었다. `scene.background`/안개는 그대로
   `lightingAt()`이 정하는 시각별 색에 맡기고 `scene.environment`에만
   물려 GLB 재질의 반사·거칠기만 사실적으로 만든다. **세기는 `hemi.intensity`
   (이미 낮·밤·날씨별로 맞춘 곡선)에 비례해 매 프레임 같이 낮춘다/올린다**
   — 예전에 ACES 톤매핑을 걸었다가 밤 화면이 망가진 사고(`post3d.js` 머리말)
   를 되풀이하지 않으려는 안전장치다. 실측(day/night 강제 토글로 직접 확인):
   낮 environmentIntensity 0.50 · 밤 0.42 — 완만하게만 오르내려 밤이 안
   씻겨나간다. `world3d.stats()`에 `ibl` 필드를 새로 넣어 앞으로도 값으로
   확인할 수 있게 했다. 진단 421/423 회귀 없음. **`sw.js` VERSION 도
   `go-v5.19.0`으로 올렸다**(커밋 `e7a089f`, `RGBELoader.js`도 앱 캐시
   SHELL에 추가) — 안 올리면 이미 한 번 연 폰이 오늘 고친 것을 하나도
   못 보고 옛 캐시만 본다(CLAUDE.md 함정). **실기기에서 실제로 반사가
   자연스러워 보이는지는 확인 전이다** — 사용자가 지금 폰에서 확인 중.
5. **"모바일에서 캐릭터가 녹색"** — 사용자가 실기기에서 온몸이 단색 녹색
   (텍스처 없음)이라고 제보. 헤드리스로 재현을 시도했다(`#noon`으로 낮
   고정, `#noglb`로 GLB 강제 차단 등) — **재현 안 됨**: 어느 경우든 플레이어는
   제대로 된 색·형태로 떴다(`#noglb`는 오히려 바닥 전체가 새까매지는 별개의
   — 아마 원래부터 있던 — 문제를 보였다, 이 세션의 지도 텍스처 변경과는
   무관해 보인다). 데스크톱 헤드리스로는 못 잡는 실기기 전용 문제로 보인다
   (WebP 디코드 실패·텍스처 크기 제한·GLB 로딩 타임아웃 등 후보). **다음
   세션은 실제 폰 스크린샷이나 그 폰의 콘솔 로그를 먼저 받을 것** — 증거
   없이 짐작으로 고치지 않는다.

## 지금 상태

**사가고** — 포켓몬GO 를 모방한 판. 걷고(보급), 만난다(등용·포획).
자가진단 **189/189**. 세이브 키는 `deungyong-go/save/<프로필>` (바꾸지 말 것).

```
걷는다  250m 마다 사료·명성, 500m 마다 등용서 (포켓스탑 대신 걸음이 보급)
만난다  인물 등용(설득 3라운드) · 펫 포획(타이밍) → 도감이 쌓인다
자동    🤖 자동 순행 — 목표를 골라 걸어가고, 조우하고, 승급·장비를 챙긴다
```

- 시점 3모드(2D/2.5D/3D) · 등신 3종(2/4/8) · 그림 양식 3종(전통/그림책/만화)
- 온라인 모드의 **사관(AI)** 이 이 판에만 있다 (군략·대화·천기) — `server/` 는 여기 것
- 안드로이드 APK 껍데기(`mobile/`)도 이 판에만 있다 (내용은 v1.7 시절)

**이 판에 없는 것**: 던전·장비(→ `saga-dungeon`) · 문답·경영(→ `saga-realm`).
원작에 없는 축이라 뺐다 — 되살리지 말 것.

## 이 판만의 파일 지도

```
js/world.js       지도 타일·스폰·시점(2D/2.5D/3D)·구역 이름 30종
js/encounter.js   등용(설득) · 포획(타이밍) · autoResolve(자동용)
                  도감에 넣는 문은 gainHero·gainPet 둘뿐이다 (밖에서 얻는 길도 이걸 쓴다)
js/rogue.js       적도(賊徒) — 역참 점거 · 암영(暗影) · 정화 (원작의 로켓단)
js/auto.js        자동 순행 — 목표 선택만, 조작은 world/encounter 의 공개 함수로
js/net.js·ai.js   오프라인/온라인 · 사관(군략·대화·천기)
js/data.js        인물 70 · 펫 41 · 열전     ← 캐릭터 추가는 여기 (다섯 곳 다 고칠 것)
js/sprite.js      스프라이트 — 등신·양식(storyize/animeFace) 2200줄
js/account.js     가입해서 저장 (다섯 게임 공통 · 이름만 다르다)
js/_expansion/    방치 전투·환생·방치 강화 (아직 보관만 — 되살릴 땐 94850f8 기준)
server/           온라인 모드 서버 (Claude 프록시 · API 키는 여기만)
mobile/           Capacitor 껍데기 — APK · iOS 프로젝트
build/·build-pc.bat  PC 단독 실행판 (dist/사가고.html)
_demo.html        스크린샷용 상태 (#dex · #auto · #walk … · 예외는 화면 위 빨간 띠)
_autoprobe.html   자동 순행 관찰 · `?sim=600` 은 10분치 보고서 (헤드리스에서도 돈다)
_swreset.html     서비스워커·캐시 털기 (`?go=game` 이면 게임으로 이동)
_mobileframe.html 폰 화면(390px)을 헤드리스로 보는 껍데기 — #probe 넘침 점검 ·
                  #fit0↔#fit1 시야각 보정 비교 · #tools 서랍 (데모로는 비교가 안 된다)
_gallery.html · _probe.html   스프라이트를 눈으로 판정하는 판
```

## 이 판에서 손볼 때 조심할 것

- `core.hash2` 는 여기만 **옛 식**(0~0.5 만 반환)이다. 다른 넷은 `>>>` 로 고쳤지만,
  이 판의 지형 문턱값이 그 범위에 손으로 맞춰져 있어 그대로 뒀다.
  고치려면 지형·구역 이름을 다시 눈으로 보며 조율해야 한다
- 스프라이트는 코드만 봐서 판정이 안 된다. `_gallery.html`·`_probe.html` 로 crop·확대해서
  **눈으로** 볼 것. 만화풍은 v1.4~v1.5 에서 인물 48·짐승 25 를 전수 다듬었다 —
  결함표(11행)는 커밋 `eb72695` 의 HANDOFF 에 남아 있다. 되돌리면 그대로 재발한다
- `--screenshot` 은 **절대경로** 필수(상대경로면 안 써져서 옛 이미지를 새 결과로 착각한다)
- **폰 화면은 `index.html` 로 본다.** `_demo.html` 은 상단이 별도 마크업이라 폰 폭에서
  도구가 잘리고, `DG_NO_TUNE` 이라 **손잡이를 켜고 끈 비교도 안 된다**(두 장이 똑같이
  나온다). 폰 폭은 헤드리스 창으로 못 만드니 iframe 390px 안쪽에 띄운다
- **3D 값을 고쳤으면 `sw.js` 의 `VERSION` 을 올린다.** 안 올리면 이미 한 번 연 폰이
  앱 캐시에서 옛 파일을 그대로 받아 **아무것도 안 바뀐 것처럼 보인다**

## 이 판에서 이어서 하면 좋은 것

1. **손으로 걸어 보기** — 이동 속도는 2026-08-25 에 34 → 8m/s 로 낮췄다(커밋 `8d8d98d`).
   숫자로는 맞췄지만 조작감은 아직 사람이 보지 않았다
2. ~~자동만으로는 금이 안 모인다~~ — 같은 날 걷기 보급·역참·성채에 금을 넣었다(`8d8d98d`)
3. **APK 다시 굽기** (v1.7 내용) — `cd mobile && npm run apk` (반드시 이 경로)
4. **사관 응답 실청취** — `ANTHROPIC_API_KEY` 주고 8790 으로 군략·천기 한 번씩
5. **폰 실기기 fps** — 2026-08-26 에 폰(공개 주소)에서 열어 보고 **UI 잘림과 세로 화면
   시야각**을 고쳤다(상단 도구 `⋯` 서랍 · `world3d.js` 의 `fovFor`). **아직 커밋 안 했다.**
   다만 고친 것은 배치와 시야각이라 **그리는 양은 그대로다** — 폰에서 버거운지는
   여전히 안 재 봤다. 버거우면 `world3d.density` → 0.5, 그래도 무거우면 `world3d.mesh` → 0.
   자세한 것은 `../SAGA-HANDOFF.md` 의 '이어서 하면 좋은 것' 1번과 '밟아 본 함정'
6. **적도(賊徒) 균형** — 2026-08-26 에 넣었다(`js/rogue.js`). 어드민의 손잡이 여섯
   (`rogue.*`)으로 바로 잡을 수 있다. 정화 값(등급×60 단사)은 **연성과 같은 자원을
   다툰다** — 그게 이 축의 선택이다.
   **2026-09-04 에 처음으로 수를 재 봤다**(`_autoprobe.html?rogue=1`, 커밋 `2fd9791`) —
   기본값(점거 비율 0.22 · 머무는 시간 20분)으로 자동 순행을 4시간씩 두 번(party=1·3)
   돌리니 **시간당 0.8~1.5회 만나고 9전 9승**(자동 판정)이었다. 너무 잦기보다
   오히려 "축이 없는 것과 같다"던 아래 우려 쪽에 가깝다 — `rogue.rate` 를 올리거나
   두목 기세(`hpMul`)를 올려 보는 쪽이 다음 손볼 자리일 수 있다.
   **다만 이건 auto.js 의 즉시판정(열 합)만 잰 것**이고, 실제 사람이 하는 실시간
   교전(`rogue-action.js`) — 속공·필살·회피 손맛 — 은 여전히 손으로 안 겪어 봤다.
   자동 승률이 100%라고 사람이 하는 교전까지 쉽다는 뜻은 아니다.
   **2026-09-05 에 절반만 진행** — claude-in-chrome 으로 실제 3D 무대를 열어 보니
   강타 예고(⚠️)가 화면 아래 작은 HUD 문구에만 뜨고 3D 화면(적 쪽)엔 아무 신호가
   없어서 놓치기 쉬운 구조였다 → `battle3d.js`에 붉은 경고 파티클(`warn` 종류)을
   추가해 고쳤다(커밋 `4f1e440`). **회피 난도 자체**(예고 1.1초 안에 5.5m 벗어나야
   하는데 기본 이동속도가 8m/s라 사실상 거저 피해진다)는 코드로 확인만 하고 손 안
   댔다 — 이 환경(claude-in-chrome 자동화 탭)은 `document.visibilityState`가
   계속 `hidden`이라 rAF 기반 실시간 조작 자체가 안 된다(포커스는 잡혀도 안 풀림).
   **사람이 직접 걸으며 회피해 보는 손맛 검증은 여전히 못했다.**

7. ~~**Mesh2Motion 실사 캐릭터 파이프라인**~~ — **2026-09-06(같은 날 세 번째 세션)에
   ①~③ 다 끝났다** — `vitruvian_v1`이 `HERO_RECIPES`에 들어갔고(커밋 `9d1423b`),
   다리 피부색 불일치도 텍스처 색 전이로 고쳤다(커밋 `000c1d1`·`0057f44`). **직접
   `DG.asset3d.build('hero', h)`를 70명 전원에 돌려 확인** — 지금 실제로
   **은창(`sg_zhaoyun`)·강우(`kr_ganggamchan`)·셀렌느(`eu_joan`)·발데나르
   (`eu_napoleon`)·바실로른(`eu_frederick`)** 다섯 명이 이 실사 모델로 배정돼
   있다(`ref.id` 해시로 고정 — 앞으로도 이 다섯이다). 자가진단 457/457 통과 —
   리타깃 자체는 깨지지 않았다는 뜻이나, **실제 화면에서 이 다섯이 걷는 모습을
   사람이 본 적은 아직 없다**(claude-in-chrome은 rAF가 하이든이라 실시간 확인이
   안 된다, 여러 세션째 반복되는 한계).
   **남은 건 ④뿐, 둘 다 사람이 결정할 일이라 손 안 댔다**: 관절 정밀도를 더
   다듬을지(손가락 단순화·무릎/팔꿈치 대략 정렬 — Blender 수작업), 이 파이프라인
   으로 인물을 더 뽑을지(인물 하나당 수작업 5~10분 든다, 대량 생산엔 안 맞음).

## PLAN.md 에서 옮겨 온 세션 기록 (2026-09-16 재편)

# 25-1. 오버월드 전체지도 — M키식 토글 (2026-09-01, 사용자 요청)

사가블로(`../saga-dungeon/PLAN.md` 28-1절)에 "마을에서 나가 다른 마을/지역으로
간다 + 디아블로 M키식 전체지도"를 적어 두면서, 사용자가 다른 네 판에도 같은
방향을 남겨 두라고 했다. 이 판은 이미 **실제 GPS 좌표가 곧 오버월드**라
"다른 마을로 걸어나간다"는 사가블로식 구조가 그대로 적용되진 않는다 — 플레이어가
실제로 이동하면 그게 곧 다른 지역이다.

다만 **디아블로 M키처럼 미니맵을 펼쳐 보는 전체 지도**는 이 판에도 그대로
유효한 아이디어다. 지금 미니맵(`js/minimap.js`)은 코앞만 보여 주는데, 그것과
별개로 **지금까지 밟아 본 실제 위치들을 한눈에 보는 펼친 지도**가 있으면
25절의 "지역별 분위기"(마을·숲·산·폐허·성)가 실제로 몇 군데나 밟혔는지
확인하는 용도로 맞아떨어진다.

**착수 완료 (2026-09-02).** `js/overworld.js` 신설:

- `js/world.js`의 `trackTrail()`이 이동거리 40m(`TRAIL_STEP`)마다 한 점씩
  `core.save.player.trail`에 위경도(절대값, `worldToLatLng`)로 남긴다.
  x·y 상대좌표가 아니라 **위경도로 저장**하는 게 핵심 — GPS 를 다시 켜서
  `origin`이 바뀌어도(28-1절 사가블로와 달리 이 판은 원점 자체가 흔들린다)
  이미 남긴 점은 그대로 유효하다. 최대 400점, 넘으면 오래된 것부터 지운다.
  각 점에는 그 자리의 `world.terrainAt` 지형 종류도 같이 담아 25절의
  "지역별 분위기"를 색으로 구분해 보여준다(미니맵의 `TINT` 팔레트 재사용).
- M 키(데스크톱) + 도구줄 🧭 단추(`#btn-owmap`, 모바일)로 여닫는 전체
  화면 오버레이(`#overworld-map`). 미니맵(`viewWorldMap()`류가 아니라
  `js/minimap.js`)과는 별개 화면이라 이름이 안 겹친다.
  `project(points, cur)`는 미니맵의 `project`·`cells`·`blips`처럼
  캔버스 없이도 도는 순수 함수라 자가진단이 따로 본다.
- `js/icon.js`에 `compass` 아이콘을 추가했다(Lucide, ISC).
- 자가진단(`_test.html`) 8항목 추가, 헤드리스 3회 동일 결과 확인.

## 2026-09-17 — SAGA-DESIGN §6.1 그래픽 통일(재질·팔레트·픽셀비율), §10-Q1 확정

사용자 요청: "사가디자인 보고 그래픽 처리해줄래". PLAN §6 표의 "할 것" 항목을 훑어 손볼 수
있는 것부터 한 번에 처리했다. 실기 확인이 필요한 항목은 그대로 §7 목록에 남겨 뒀다.

**새 파일 `js/toon3d.js`** — 3단 램프(`MeshToonMaterial.gradientMap`, `NearestFilter` 3픽셀
텍스처)와 뒤집힌 헐 외곽선을 배우 전용으로 공용화했다. 외곽선은 **균일 스케일 복제가 아니라
정점 셰이더에서 스키닝 뒤 법선 방향으로 미는 방식**(`ShaderMaterial` + `skinning_pars_vertex`
등 표준 청크 조립)이라 — 사람 모양(발이 원점)을 그대로 스케일업하면 발끝은 안 벌어지고
머리만 벌어지는 문제를 피했다. 폭은 지오메트리 바운딩구 반지름의 3%(PLAN "스케일 1.03"의
환산). `world3d.toon`·`world3d.outline` 손잡이(둘 다 기본 1) — 끄면 예전 Lambert/외곽선 없음.
`index.html`·`_test.html`·`_demo.html`·`_autoprobe.html` 스크립트 목록에 얹었다(`_admin.html`·
`_glbcheck.html`은 이 파이프라인을 안 쓰므로 안 건드림).

**`js/actor3d.js`** — `mat()`이 `world3d.toon`을 보고 Toon/Lambert 를 고른다(캐시 키에 갈래
포함). `solid()`(그림자 지는 큰 덩이만)가 이제 `userData.wantOutline`도 같이 표시하고,
`shape()`가 다 조립한 뒤(부모에 매달린 다음이라야 형제로 외곽선을 붙일 수 있다) 한 번에
`applyOutlines()`로 훑어 붙인다.

**`js/asset3d.js`** — `delam()`(GLB PBR → 평면 재질로 벗기는 자리)이 `toon3d.toonify()`를 쓰고
스킨 메시엔 outline도 붙인다. `/realistic/` 밑(건물)은 원래부터 이 함수를 안 타 그대로다.
**§10-Q1(인물 스타일 충돌) 확정** — MPFB 사진측량 실사 인체 20벌(`mpfb_male/female`·
`v3~v23`)과 `vitruvian_v1`에 `realistic: true`를 달고, `heroPool()`이
`world3d.realisticPeople`(기본 0=꺼짐)일 때 그 줄들을 걸러낸다. 그 결과 **기본 인물 표는
QRPG 저폴리 6종만** 남는다(PLAN §7 실기 목록의 "MPFB 실사 5인" 항목이 자연히 뒤로 밀림 —
다시 보려면 어드민에서 켜야 한다).

**`js/prop3d.js`** — PLAN §6 초안의 24색(하북 들판·마을·강)을 그대로 배열로 얹고
`snapPalette()`(RGB 최근접, 순수 함수)를 `lambertOf()`에 물렸다(`world3d.palette`, 기본 1).
소품은 원래 GLB 재질이 면마다 단색이라(주석 "Quaternius 면색") PLAN이 적었던 "캔버스에서
1회 스냅"은 필요 없었다 — 색 하나를 스냅하는 것으로 끝난다. 배우처럼 툰+외곽선은 안 받는다
(PLAN 원문 그대로 — 얻을 게 적다는 판단 유지).

**`js/world3d.js`** — 픽셀비율 상한 2 → 1.5(§6.1 "폰: 픽셀 비율 상한 1.5"). 안개·하늘·접지
그림자는 **점검해보니 이미 되어 있었다**(PLAN 표가 옛 상태를 그대로 베낀 것) — `syncLight()`가
`lightingAt().bg` 한 값을 배경·안개·클리어컬러 셋에 매 프레임 물리고, `groundShadow()`가
배우마다(빌보드든 메시든) 접지 blob 을 이미 찍고 있었다. PLAN §6 표를 실제 상태로 고쳤다.

**미룬 것 — 지형 트라이플레이너**: `LAND_PAINT()`가 이미 실제 land-use 를 반영해 캔버스에
구운 텍스처를 붙이는, 크고 검증된 파이프라인이다. 셰이더를 트라이플레이너로 갈아 끼우면
회귀 위험이 크고 화면으로 검증할 길이 없어(이 세션 규칙상 헤드리스 스크린샷 금지) 이번엔
손 안 댔다. 실기로 "그래도 허접해 보이는지"부터 확인한 뒤 순서를 다시 잡기로.

**검증** — `node -c` 새/수정 파일 전부 통과, `_admin.html` 인라인 스크립트 블록 전부
`new Function()`으로 파싱 확인, `bash tools/precheck.sh saga-web/saga-go` → PRECHECK OK
(data.js md5 불일치 경고는 이 세션과 무관한 기존 상태). **헤드리스 `_test.html` 3회 확인은
안 돌렸다** — precheck.sh 자체 주석대로 "서버·브라우저는 안 띄운다, 실기 확인은 사용자 몫"이라
서버를 새로 띄우는 게 이 규칙과 부딪힌다고 판단했다. 실기 확인 대기는 PLAN §7 11번에 모았다.

**admin** — `_admin.html` 균형 손잡이 탭에 `world3d.toon`·`world3d.outline`·
`world3d.realisticPeople`·`world3d.palette` 네 줄 추가.

## 2026-09-17 (이어서) — SAGA-DESIGN §8 안정화(코드로 끝나는 항목)

앞선 §6 그래픽 통일 뒤 "커밋하고 이어해줘 묻지도 말고" 요청으로 계속. PLAN §8 로드맵이 그래픽
다음으로 "0 안정화"를 앞에 두고 있어(§7 목록을 안 닫으면 새 시스템을 안 넣는다는 SAGA-DESIGN
§8-1 원칙) 그중 **실기기 없이 코드만으로 끝나는 항목**을 골라 처리했다.

**새 파일 `js/errlog.js`** — `window.onerror`·`unhandledrejection`을 잡아 localStorage
링버퍼(50건, `deungyong-go/errlog`)에 쌓는다. `push(arr, entry)`는 순수 함수(자가진단이
55건 넣어 50건만 남는지, 오래된 것부터 밀리는지 값으로 본다). `errlog.on` 손잡이(기본 1).
`index.html`을 비롯해 `js/data.js`를 싣는 화면 전부(`_admin.html`·`_autoprobe.html`·
`_demo.html`·`_gallery.html`·`_probe.html`·`_test.html`)의 **맨 첫 스크립트**로 얹었다 —
다른 모든 파일의 실행 중 오류(파싱 오류만 빼고)를 놓치지 않으려는 것.

**`_admin.html`** — 새 탭 "오류"(다시 읽기·복사·비우기, `DG.errlog.list()`를 그대로 글로
푼다). QA 프리셋에 "🩹 옛/이상한 버전 세이브 시뮬레이션" 추가 — `v`를 999로 바꿔 저장했다
다시 읽어 골드 같은 값이 안 지워지는지 확인하고 정상 버전으로 되돌린다. 균형 손잡이 탭에
`errlog.on` 한 줄.

**`js/core.js`** — **§8-3의 실제 지뢰를 잡았다.** `load()`가 `parsed.v !== 1`이면 무조건
`false`를 줘 그대로 새 세이브로 덮이는 구조였다(주석에 "마이그레이션은 여기서" 라고만
적혀 있고 실제 체인은 없었다 — 사가국지가 이미 밟은 `aa4b8b8` 류를 이 판은 아직 안
밟았을 뿐 구조는 똑같이 위험했다). `SAVE_VERSION`·`MIGRATIONS`·`migrate()`(순수 함수)를
추가해 **버전이 안 맞아도 지우지 않고** `mergeDeep(freshSave(), ...)`로 넘긴다. 지금은
`SAVE_VERSION=1`에 정의된 마이그레이션이 없어 체인은 그냥 통과만 시키지만, 다음에 스키마를
바꿀 때 `MIGRATIONS[1] = function(s){...; s.v=2; return s;}` 식으로 얹을 자리가 생겼다.
`_test.html`에 순수 함수 검사 항목 추가(`v=999`를 넣어도 필드가 안 지워지는지).

**`js/post3d.js`** — `draw()`가 실제 렌더 로직(`drawInner`로 이름을 옮겼다)을 try/catch로
감싼다. 실패하면(폰 GPU 드라이버 등으로 렌더 타깃·셰이더가 죽으면) 타깃을 놓아 주고
`ready=false`·`failed=true`로 **영구히 후처리를 끄고** `errlog`에 기록한 뒤 `world3d`의
바로 그리는 길로 돌려보낸다 — PLAN이 적어 둔 "갈색/녹색 화면" 류의 실제 대응.

**`tools/precheck.sh`(루트, 다섯 판 공통)** — "sw.js 캐시 버전" 절 추가: `<판>/js`가
git 상태에서 바뀌었는데 `<판>/sw.js`는 그대로면 WARN(막지는 않는다 — 캐시 안 타는 파일도
있을 수 있어서다). 이 세션 자신도 `sw.js`(`go-v5.30.0`→`go-v5.31.0`)를 올리고 새 파일
둘(`errlog.js`·`toon3d.js`)을 `SHELL` 목록에 넣어 오프라인 캐시에도 실리게 했다.

**검증** — `node -c` 전 파일 통과, `_admin.html`·`_test.html` 인라인 스크립트 전부
`new Function()` 파싱 확인, `bash tools/precheck.sh saga-web/saga-go` → PRECHECK OK(새
sw.js 경고 절 포함, 이번엔 안 걸림 — VERSION 을 같이 올렸으니까). 헤드리스 `_test.html`
3회 확인은 이번에도 안 돌렸다(§6 세션과 같은 이유 — precheck.sh 자체 규칙).

**남은 것** — §7 목록의 나머지는 거의 다 실기 확인 항목이라 그대로 둔다. Phase 1(세션
구조·손맛) 로 넘어가려면 "사람이 손으로 한 세션 플레이 기록"(§8 Phase 0 마지막 조건)이
먼저 있어야 한다 — 사용자 몫.

## 2026-09-17 — PLAN §5 ④ 일과판 + 마무리 카드 (Phase 1 첫 후보)

Phase 0 마지막 조건("사람이 손으로 한 세션 플레이 기록")은 아직이지만, ④ 는 그
실기 확인 목록과 독립된 새 기능이라 코드부터 먼저 짰다 — 실기 확인은 여전히
사용자 몫으로 남는다.

- **`js/daily.js`(신설)** — 오늘의 일과. 풀 8 중 비석(⑤)·사당(②)은 그 후보가
  안 서서 `disabled` 로 꺼 뒀다(걷기·조우·역참·사건·토벌·반려 6만 돈다). 그날의
  셋은 `dayKey()` 문자열을 djb2 류로 해시해 mulberry32 로 뽑는다 — **순수 함수**
  (`pickToday(dayStr)`)라 같은 문자열은 늘 같은 셋을 낸다.
- **호출부를 안 늘렸다** — `quest.js`의 `progress(kind, n)` 이 자기 사명 표를
  훑은 **뒤에 한 곳에서만** `daily.progress(kind, n)` 로 넘긴다(§10-Q3 "나란히
  둔다" 확정). 걷기·조우·역참·토벌은 기존 호출 그대로 타고 온다. 사건·반려는
  quest 표에 없던 kind 라 `event.js`(`open()`)·`buddy.js`(`tick()`)에 한 줄씩만
  보탰다.
- **`js/ui.js`** — `goalLine()`(사명 한 줄) → `goalBoard()`(지금·이번 세션·이번
  주 3줄). "지금"은 `world.nearest()`/`nearestStation()` 중 더 가까운 쪽(봉수대
  ①·사당②·비석⑤가 서면 후보에 합류). "이번 주"는 새로 안 만들고 `milestone.js`
  의 주간 사다리를 그대로 읽는다. `viewQuest()` 맨 위에 `dailySection()`(일과
  카드 3장)도 얹었다 — 사명 시트를 그대로 쓴다(따로 시트 안 만듦).
- **마무리 카드** — `#encounter`(조우 모달)를 빌려 쓴다. 세 신호: `game.js`가
  `visibilitychange hidden` 에서 diff 만 재 두고(탭이 안 보이면 띄워 봤자 못 본다)
  **돌아올 때** 보여준다 · 5분 무입력(`keydown`/`pointerdown`/`touchstart` 로
  갱신) · 자동 순행을 껐을 때. diff 가 전부 0 이면(막 켰다 끄기 등) 안 띄운다.
  세션 스냅샷은 메모리에만(`core.save` 밖) — PLAN 그대로.

**검증** — 헤드리스 3회 `RESULT 489/490` 동일(기존부터 있던 무관한 실패 1건
"에셋 — 인물이 서로 다른 몸을 나눠 입는다" 만 남음, 내 변경 전에도 481/482 로
같은 자리가 깨져 있었다 — stash 로 대조 확인). `bash tools/precheck.sh
saga-web/saga-go` → PRECHECK OK. `sw.js` `go-v5.31.0` → `go-v5.32.0`(js 8개
바뀌어서, `js/daily.js` 를 SHELL 에 추가).

**남은 것** — 목표판·마무리 카드 체감은 실기 확인 몫(§7 목록과 합류). Phase 1
나머지: ③ 75초 토벌, 타격음 3종.

## 2026-09-17 — PLAN §5 ③ 75초 토벌 — 부위 파괴·저스트 회피

실제 "토벌" 실시간 전투는 `fort.js`의 `startRaidDuel()`이 범용 엔진
`rogue-action.js`(도적전과 같은 것, event.js·rogue.js·fort.js 성채 수비대도
같이 쓴다)를 빌려 돈다는 걸 먼저 확인했다 — PLAN 이 말한 "MODIFY
rogue-action.js"가 정확히 이 자리다.

- **`js/rogue-action.js`** — `create({raid:true})`로 켠 회차에서만: `checkParts(s, ev)`
  가 기세 25%·50%·75% 누적 문턱을 넘을 때마다 `partbreak` 이벤트를 내고 스태거를
  강제로 채운다(부위를 따로 나누지 않고 같은 풀을 세 문턱으로 읽는다 — 3부위
  ×25% = 누적 75%, 나머지 25%는 맨몸). `dodge(s)`(새 판정 함수) — 예고가 풀리기
  `JUST_WINDOW()`(0.25s) 안에서 부르면 다음 강타 판정이 완전 회피 + 기 +30%.
  §10-Q2 확정대로 **토벌 밖에서는 옛 그대로**(걷기만으로 완전 회피) — `raidMode`
  가 꺼져 있으면 새 분기를 안 탄다, 기존 자가진단 무수정.
- **`js/fort.js`** — `startRaidDuel()`에 `timeSec: 75, raid: true` 추가, 결과의
  `p.parts`를 `raid.fight()` opts 로 넘김. 성채 수비대 쪽(`startFortDuel()`)은
  안 건드림.
- **`js/raid.js`** — 부위 파괴 수만큼 단사 ×4(`growth.addDust`, 이기든 지든 —
  "즉시" 자체는 회차가 끝나는 자리에서 한 번에 주지만 판정은 부서질 때마다
  이미 셌다), 3부위 전부 깨면 등용 확률 ×1.5.
- **`js/battle3d.js`** — `partbreak`(먼지+금빛, hold 80ms) · `justdodge`(금빛+
  바람, hit-stop 없음) · 기존 `heavy` dodged 분기에 `just` 를 얹어 걷기 회피보다
  또렷하게.
- **`js/audio.js`** — "타격음 3종 라운드로빈"(SAGA-DESIGN §3 표준 C) — 새 mp3
  없이(에셋 규칙) 기존 3자리 풀에 재생 배속 0.92/1.0/1.12 를 박아 코드로만
  세 갈래를 낸다. 실제 별도 음원을 원하면 나중에 `hit2.mp3`·`hit3.mp3` 를
  사람이 받아 얹고 `CLIPS`/`bank()` 를 손보면 된다 — 지금은 그 자리까지만.
- **UI** — `#rogue-hud`에 `raidMode`일 때만 부위 게이지(🛡️🗡️🐎, 깨지면 흐려짐)
  ·"회피" 버튼(저스트 창 안에서 금테 강조) 추가. 저스트 성공은 토스트
  "🌟 간발! 완전히 피했다"로도 알린다(PLAN의 80ms 플래시 대신 — 기존 토스트
  큐를 그대로 썼다, 실기 체감 후 더 짧은 연출이 필요하면 그때 손본다).

**검증** — 헤드리스 3회 `RESULT 495/496` 동일(남은 1건은 여전히 §7 목록에
있는 무관한 기존 실패 — "에셋 — 인물이 서로 다른 몸을 나눠 입는다"). 새 진단
6항목(75초 종료·부위 3 순서·저스트 기 보너스·걷기 절반 감쇄(토벌만)·단사
보상·타격음 배속 3종) 전부 통과, 기존 "강타 예고 때 원 밖이면 완전히 안
맞는다"(비토벌) 회귀 없음, "성과·접기는 duel.js와 같은 모양" 회귀 없음.
`bash tools/precheck.sh saga-web/saga-go` → PRECHECK OK. `sw.js`
`go-v5.32.0` → `go-v5.33.0`.

**남은 것** — 저스트 창 0.25s·걷기 감쇄 50%가 실기에서 체감상 맞는지는
`_admin.html` 손잡이(`rogueAction.justWindow`·`rogueAction.walkDodgeMul`)로
사람이 조정. Phase 1 은 이제 코드분 전부 완료 — 다음은 실기 확인이거나 Phase 2.

## 2026-09-18 — §10 열린 질문 Q4·Q7 확정, Q5·Q6 보류

PLAN.md §10 나머지 질문을 Q4 순서부터 처리(Q1~Q3 은 2026-09-17 에 이미 확정):

- Q4 패배 비용: 15% 상한 300 으로 시작, 어드민 손잡이로 0 까지(권장안 채택).
- Q5 비석 규모: 격자 200m·12% 로 시작하되 실제 체감 밀도는 실기 측정이 있어야
  판단 가능 — 보류.
- Q6 온라인 사관: 서버 비용·키 관리 지속 여부는 사용자의 지속 비용 판단 사항이라
  보류. 오프라인과 독립이라 언제든 뺄 수 있다는 점만 재확인.
- Q7 퓨전 인물 시기: 사가블로 검증 뒤 진행(확정). 루트 CLAUDE.md 의 "사가블로가
  퓨전 선행 판" 규칙을 그대로 따르고, GPS 권역이 잘 맞아도 순서를 안 바꾼다.

코드 변경 없음, PLAN.md §10 문구만 갱신.

## 2026-09-19 — 초상 3D↔2D 깜빡임 버그 수정(다섯 판 공통)

제보: "캐릭터 인물 사진들이 2D 스프라이트랑 겹쳐서 깜빡인다"(화면 전체 공통).

**원인**: `portrait3d.img()`가 **이미 3D 초상을 구워 캐시해 둔 인물이라도** 매번
캔버스 2D 그림(`sprite.portrait()`)으로 `<img>`를 새로 시작하고, 40ms 뒤
`sweep()`이 `src`를 3D 그림으로 갈아 끼우는 구조였다. 첫 굽기 때 한 번뿐인
지연이 아니라 **화면을 다시 열 때마다**(대화·교전·포획·서찰 등 반복해서 여는
자리) 이 40ms 동안 2D 그림이 잠깐 비쳤다 3D로 바뀌어, 반복해서 보면 깜빡이는
것처럼 보였다.

**고침**: `portrait3d.js` `img()`와 `ui.js`의 도감 상세(`dt-portrait`, 150×172)
두 자리 모두, 그리기 전에 `portrait3d.of(kind, ref, w, h)`로 캐시를 먼저
확인해 이미 구워져 있으면 처음부터 그 그림으로 시작한다(`data-p3-done="1"`도
같이 찍어 `sweep()`이 다시 손대지 않게). 아직 못 구웠으면 여태처럼 캔버스
그림으로 시작 — 화면은 여전히 한 번도 안 빈다.

같은 구조(`ui.js`의 `pt()`/`p3tag()`)가 saga-dungeon·saga-forest·saga-story
`js/ui.js`, saga-realm `js/ui-rtk.js`(`pt()`/`ptBig()`)에도 그대로 있어
**다섯 판 모두 같은 방식으로 고쳤다**. 다섯 파일 모두 `node -c` 통과.
실기(같은 인물을 반복해서 열어 깜빡임이 사라졌는지)는 미확인 — 사용자 확인 대기.

## 2026-09-19 (이어서) — PLAN §5 ① 봉수대 — Phase 2 착수(코드분 완료)

사가스토리 §5-4(관문 대장)를 먼저 끝낸 뒤 이 세션으로 넘어왔다(다른 판 이어가기).
Phase 0·1(안정화·손맛)은 이미 코드분 완료·실기 대기 상태라, 로드맵대로 **Phase 2
첫 후보** ①을 시작했다.

**CREATE `js/beacon.js`** — 27 봉수대(한국·일본·중국 REGIONS 27개 대표점)를
위경도 시드 해시(`h01`, world.js 의 `×2` 보정과 같은 요령)로 300~800m 밀어
고정한다. **늘 위경도로만 갖고 있다가 그때그때 `world.latLngToWorld()`로 지금
원점(origin) 기준 월드 좌표를 다시 구한다** — origin 은 실제 GPS 첫 수신 때
재설정되므로(world.js `useGeo`), 미리 월드 좌표로 굳히면 그 순간 다 어긋난다.

**world.js는 안 고쳤다**(PLAN 원문은 MODIFY 로 적었었다) — 근접 훅 대신
`ui.js`의 "가까운 것" 패널(`renderNear`)에 봉수대 카드를 하나 더 끼우고
`beacon:request` 이벤트로 `beacon.js` 자신의 `#encounter` 카드를 여는 길을
탔다(기존 `station:request`/`fort:request` 와 완전히 같은 결 — `station.js`
를 그대로 본떴다). 3초 홀드는 그 카드 안(diff 리렌더에 안 걸리는 자리)
`pointerdown/up` + `game.js` 메인 루프의 새 `beacon.tick(dt)` 로 채운다.

**반경 1.5km 노출**(48절 "가 보기 전까지 안 뜬다"의 예외)은 `world.stationsIn`
/`fortAt`(둘 다 이미 공개돼 있었다)을 봉수대 좌표 둘레 그리드로 훑어
`revealedNear(b)`가 낸다 — **불을 안 올린 권역은 빈 배열**이다. 사당(②)·
비석(⑤)은 아직 안 지어져 이 목록에 못 낀다 — 그 후보가 서면 한 줄씩 보탠다.

**`overworld.js` `project()`가 3번째 인자(`extra`)를 새로 받는다**(하위호환 —
기존 2-인자 호출은 그대로 돈다). 27개 봉수대를 범위(bounds) 계산에 넣으면
나라를 가로지르는 스팬에 발자취가 점 하나로 묻혀 버려서, **범위는 발자취·
지금 위치로만 잡고 그 위에 얹은 다음 화면 밖(±1 밖)이면 그린 쪽에서 지운다**
(project() 자체의 순수함수 계약·기존 진단은 안 건드렸다 — 새 필드 `pois`만
늘었다). `minimap.js`도 27개를 다 재긴 하지만 **6km 밖은 미리 거른다** —
`minimap.project()`는 거리와 무관하게 방향만 재는 함수라, 안 거르면 다른
나라의 것까지 늘 테두리 화살표로 뜬다(실측으로 확인하고 뺀 판단, station 재현
지역 문제와 결이 같다).

**`codex.js`에 `landmark`(명소) 갈래 추가** — 다른 다섯 갈래처럼 "목록을 새로
안 만들고 있는 것을 읽어 와 같이 센다": `beacon.list()`를 그대로 읽는다.
발견 보상은 소액(exp 20)만 — 큰 보상은 `beacon.light()`가 이미 준다(금
120·공적 40·丹 5).

**`asset3d.js`는 이번 세션에 손 안 댔다** — PLAN 의 "탑 모델(`Watchtower.glb`
팔레트 스왑)"은 새 3D 랜드마크 자산 레시피를 넣는 일인데, **같은 종류(역참
`tower_ruin.glb`)의 아이콘 굽기가 SAGA-HANDOFF 기준 여섯 번 넘게 실패한
전례**가 있어 이번엔 손 안 댔다. 지금은 근접 카드(🗼 이모지)·미니맵·오버월드
점만으로 기능한다 — 3D 랜드마크는 자산이 준비되면 따로 붙인다(열린 항목으로
등록).

`index.html`·`_test.html`·`sw.js` 세 곳에 `js/beacon.js` 스크립트 태그·캐시
목록을 추가(`js/station.js` 바로 뒤). `_test.html`에 §5① 진단 5개 추가
(권역마다 하나·해시 결정성 · 3초 홀드로 점화+보상 · 두 번째는 거절 · 반경
노출(48절 예외) · 값을 내는 함수는 세이브 불변). 5번째 진단(revealedNear가
실제로 항목을 낸다)은 노드로 해시를 직접 돌려 첫 봉수대가 결정적으로 50개를
낸다는 것까지 확인한 뒤 썼다(운에 기대는 진단이 아니다).

**`node -c` 전체 통과(beacon.js·ui.js·codex.js·game.js·overworld.js·
minimap.js), 인라인 테스트 스크립트 추출도 `node -c` 통과, `bash
tools/precheck.sh saga-web/saga-go` → PRECHECK OK — 다만 이번 세션은
서버·헤드리스 크롬을 띄우지 말라는 지시가 있어 실제로 돌려 RESULT 를 본 적은
없다.** **다음 세션에서 가장 먼저 헤드리스 3회 돌려 이 절 진단 5개가 실제로
통과하는지 확인할 것**(사가스토리 §5-1·§5-4 진단도 같은 이유로 아직
미확인이라 함께 돌린다). `sw.js` `go-v5.33.0` → `go-v5.34.0`.

**실기 확인 대기**(§7 추가): 3초 홀드 감촉(너무 길거나 짧은지), "가까운 것"
패널에 봉수대 카드가 야생 대상·역참 카드와 함께 있을 때 안 헷갈리는지, 미니맵
6km 컷오프가 실제로 체감상 자연스러운지, 오버월드에서 봉수대 이름표가 다른
발자취 점과 안 겹치는지.

## 2026-09-20 — 자체 몸짓 `js/anim-own.js` (VRM 몸이 Mixamo·UAL1 없이 움직인다, 다섯 판 공용 복사본)

사용자 "mixamo 가 깃에 배포가 불가하니 VRM 캐릭터에 애니메이션으로 재제작" → "자체 애니메이션을 제작해줘"
(범위: **사가웹만**, 이동 5종 + 전투 4종). (사가의숲에서 옮김 — 상세는 사가의숲 HANDOFF 같은 날 절)

**무엇**: `anim-own.js`(다섯 판 md5 동일 — 고치면 다섯 벌 함께) 가 VRM 휴머노이드(`J_Bip_*`) 몸짓 아홉을 **코드로 직접 짠다**:
Idle_Loop · Walk_Loop · Run_Loop · Jump · Land · Attack · Hit · Death · Dodge. 남의 데이터 없음(전부 수식·키).
자세는 뼈마다 (롤·피치·요)를 **정지 자세에서 뽑은 몸 축(위·앞·왼)** 기준 도(°)로 적고 뼈 로컬 회전으로 바꿔 30/60fps 로 굽는다.
발이 땅에 붙는 클립은 FK 로 발바닥 높이를 재서 엉덩이 높이를 자동으로 맞춘다(발이 뜨거나 박히지 않는다).
모든 클립이 같은 뼈 전부에 트랙을 갖는다(크로스페이드 때 앞 클립 자세가 안 남게).

**연결**(각 판 `asset3d.js`): `wantsOwnAnim(url)` — 경로에 `/people/anime/` 가 든 VRM 몸만, 손잡이 `world3d.ownAnim`(기본 1)이 0 이면 예전 길
(UAL1 retarget)로 되돌아간다. 자체 몸짓이면 UAL1(7MB)을 **받으러 가지도 않는다**. 몸이 조립된 장면에서 실제 뼈를 읽어 그 몸에 맞춰 굽고
(`parts.body.ownClips` 캐시), 못 만들면 그때 UAL1 을 받아 예전 길로 간다. `mapClips` 에 슬롯 `jump`·`land` 추가(다른 슬롯은 그대로).
`index.html`·`_test.html`·`_demo.html`·`sw.js` 에 스크립트 추가, sw VERSION 올림.

**검증(렌더 없이 FK 로 잰 것 — 눈으로는 못 봤다)**: 표준 VRoid 뼈대 + 아바타 4종 실제 GLB 뼈대 5개에서 — 아홉 클립 값 유한·단위 쿼터니언,
루프 셋 이음매 0, 걷기 보폭 0.6m·왼발 앞일 때 오른팔 앞, 접지 오차 <1cm, 공격 순간 오른손 0.58m 앞·예비 동작 머리 위, 쓰러진 뒤 머리 높이 ~0.2m·몸통 수평,
구르기 한 바퀴 뒤 서 있는 높이 복귀, 어느 관절도 땅 아래 4.5cm 넘게 안 박힘, 각속도 <3200°/s. 실제 three 뼈대에 `AnimationMixer` 로 돌린 왼발 위치가
FK 계산과 <1mm. 각 판 `_test.html` 에 항목 6개씩(`anim-own — …`). 시험은 **jsdom(node)** 으로 돌렸다(크롬·서버 안 띄움) — WebGL·캔버스는 스텁.
결과: 사가국지 156/156, 사가의숲 311/312, 사가고 505/507, 사가블로 353/355, 사가스토리 188/192 — **남은 실패는 전부 변경 전 HEAD 에서도 똑같이 난다**
(사가의숲 "깃발 그림"·사가고 "배우 조립·걸음"(jsdom 에 `Request` 없음)·사가블로 미니맵 둘·사가스토리 §5-1 궁수/§5-4 관문 대장 등 넷 —
**사가스토리 넷은 jsdom 한계인지 진짜 버그인지 아직 모른다**, 실크롬 `_test.html` 로 확인할 것).

**모른다(실기 몫)**: 실제 화면에서 자연스러운지(관절 각도·발 미끄러짐·구르기 궤적). 진폭은 `anim-own.js` 의 클립 함수(`walkPose` 등)에 숫자로 있다 —
보고 어색하면 그 숫자를 고치면 된다. 발 미끄러짐은 이동 속도와 보폭이 안 맞으면 생긴다(게임 쪽 걷기 속도에 맞춰 `Walk_Loop` 의 `dur`(1.0s)·`26`°를 조정).
**손 안 댄 것**: Mixamo 관련 옛 코드(`HERO_RECIPES_MIXAMO`·`world3d.mixamoReal`, 파일은 .gitignore)는 그대로 — 지워도 공개판 동작은 같다.
Godot·Unity 트랙은 이번 범위 밖. 새 인물 VRM 을 넣을 땐 경로에 `/people/anime/` 가 있어야 자체 몸짓 길을 탄다.

## 2026-09-20 — 인물 전원 VRoid 몸 + 인물별 색 변형 `js/vroid-variant.js` (다섯 판 공용 복사본, md5 동일)

사용자 "104명은 다 바꾸라고 했는데" → (a) 각자 새로 제작 + (b) 기존 몸 4개 배정·색 변형. **이 절은 (b)** — (a)(인물마다 다른 몸을 VRoid Studio 로 한 명씩 제작)는 코드로 못 하고 아직 시작 안 했다.
(인물 수는 104가 아니라 **105**다 — `DG.data.heroes.length`.)

**무엇**: `world3d.animeAvatar` 손잡이를 **기본 켜짐(1)** 으로 바꿨다(끄면 0). 그러면 인물이 `avatar_sample_a·b·c`·`avatar_custom_01` 넷 중 하나를
id 해시로 받는다(배정 코드는 이미 있었고 기본 꺼짐이었을 뿐). 넷만으로는 백여 명이 똑같아 보여서 `vroid-variant.js` 가 조립된 몸의 **머리(`_HAIR`)·옷(`_CLOTH`: 윗도리·아랫도리·신을 칸을 비켜 다르게)·눈동자(`EyeIris`)**
재질만 복제해 12×12×12 색 칸 중 인물 id 해시 칸으로 곱한다. 피부·얼굴·원본 재질은 안 바뀐다(재질을 복제하고 uuid+칸으로 캐시). `asset3d.js` 에 `applyVroid(model, rec, id)` 한 줄 배선.

**검증(렌더 없이)**: 진단 505/507 → 508/510(새 항목 3 — 인물 전원 칸 결정성·서로 다른 조합 ≥90%, 재질 분리·원본 불변·캐시·두 번 입혀도 원본 기준, 경로 판정·손잡이 기본 켜짐/끄기). 남은 실패 둘("배우 — 조립·걸음", jsdom 에 `Request` 없음)은 변경 전 HEAD 와 같다. jsdom(node)으로 돌렸다(크롬·서버 없음).
**모른다(실기 몫)**: ① **색이 실제로 어떻게 보이는지** — 색은 텍스처 × `material.color` 곱이라 원본이 어두우면(검은 머리 등) 색이 덜 산다. 팔레트(`vroid-variant.js` `palette(채도, 명도, 밝기배수)` 세 줄)를 보고 조정.
② 기본 켜짐이라 처음 인물이 뜰 때 VRoid 몸을 받는다(몸당 5~9MB, 네 개). ③ 인물 역할·성별과는 무관하게 해시로만 배정한다(데이터에 성별 칸이 없다) — 여성형 몸이 장군에 배정될 수 있다.
**손잡이**: `world3d.animeAvatar` 0 → 예전(QRPG 등)으로 즉시 복귀. `sw.js` VERSION 올림.

## 2026-09-20 — 사용자 실기 1차 보고(요점만, 다섯 판 공통·해당 판 표시) — 아직 착수 전

사용자가 실기기를 돌려 본 첫 보고. "이 작업 다 완료 후" 순서로 처리하라고 했고, 상세 결과는 전체 확인이 끝난 뒤 다시 준다고 했다. 원인은 **미확인**이다(아래 의심은 추정일 뿐).

1. **움직이는 캐릭터가 전부 반대로 이동한다**(다섯 판 공통 증상으로 보고됨). 의심 갈래: 같은 날 들어간 자체 몸짓 `js/anim-own.js`·VRoid 몸 배정 `vroid-variant.js`가 VRM 0.x/1.0 정면(+Z/-Z) 처리와 어긋났을 가능성. 확인은 한 판에서 이동 방향 벡터와 모델 `rotation.y` 를 대조하고, 다섯 벌 복사본이라 고치면 md5 로 함께.
2. **사가블로가 심하게 느리다** — 불러오기가 오래 걸리고 플레이 중에도 무겁다(사양 탄다고 함). 로딩(에셋 용량·동시 디코딩)과 프레임(그림자·소품 수·VRM 인물 수) 둘로 나눠서 봐야 한다. 그래픽 품질 3단 손잡이(§7-2)가 다른 판엔 있어도 이 판 기본값이 높은지부터 확인.
3. **사가국지 인물 사진이 뒷모습이다**(`portrait3d.js` 초상 카메라/모델 정면 문제 — 1번과 같은 뿌리일 수 있다).
4. **사가고에 자동 전투를 넣어 달라**(전투 화면도 이상하다고 함 — 어떻게 이상한지는 다음 보고에서 확인). 사가고 전투 표준·PLAN 을 먼저 볼 것.
5. **캐릭터 디자인을 더 늘리고 싶다** — VRoid Hub 수동 내려받기가 빠른지 물었다. 답: 손으로 받는 쪽이 맞다(로그인·약관 동의가 모델마다 있다). 단 **이 저장소는 공개(GitHub Pages)** 라 모델마다 "재배포 허용"을 확인해야 하고, 재배포 불가는 애초에 받지 않는다(`assets/ASSET_LICENSES.md` 규칙). 받은 파일 처리(`tools/glb-compress`·`vroid-variant.js` 몸 배정)는 코드 몫.

## 2026-09-20 (이어서) — VRM 정면 -Z 보정(다섯 판 공용) + 사가고 자동 전투

같은 날 실기 1차 보고(위 절)의 1·3·4 번을 처리. 사용자 "사가고 자동 전투 시작해, 캐릭터 방향이 이상했어".

**① 캐릭터 방향 — 원인을 찾았다.** 이 저장소의 VRM(`assets/models/people/anime/*.glb` 넷 — 샘플 A·B·C·custom_01)은 전부 **VRM 0.x 이고 정면이 -Z** 다. GLB 를 직접 파싱해 재 봤다(발끝이 발목보다 z -0.10~-0.12).
그런데 다섯 판의 배우는 `rotation.y = atan2(dx, dz)`·`lookAt`·"카메라는 +Z" 같이 **+Z 가 앞**이라는 가정으로 돌려 세운다(QRPG·MPFB 가 그랬다). VRM 만 그대로 세우면 **뒷걸음질(이동 반대)·마주 서야 할 때 등을 돌림(전투 무대)·초상이 뒷모습**이 된다 — 실기 보고 세 건(이동 반대·사가고 전투 방향·사가국지 초상 뒷모습)이 이 하나로 설명된다.
**고침**: `js/vroid-variant.js`(다섯 판 공용 복사본, md5 동일 `5edf315c…`)에 `faceFront(model, url)` 를 더했다 — VRM 경로(`/people/anime/`)의 몸을 감싼 그룹을 반 바퀴(`rotation.y = π`) 돌려 +Z 로 맞춘다(두 번 안 돌림·다른 몸은 그대로). 각 판 `asset3d.js` 의 `applyVroid` 가 색 변형 직전에 부른다(사가의숲은 `applyVroid` 가 주민에만 걸려 있어 `assemble()` 에서 따로 부른다). 뼈·몸짓은 안 건드린다 — `anim-own.js` 는 뼈 로컬 값만 읽어 바깥 회전에 안 흔들린다.
진단 항목 하나(다섯 판 각 `_test.html`, 「vroid-variant — faceFront」). `sw.js` 다섯 다 올림(go-v5.37.0·dungeon-v0.132.0·village-v0.79.0·side-v0.57.0·realm-v1.34.1).
**모른다(실기 몫)**: 화면을 직접 못 봤다(스크린샷 금지) — GLB 측정과 순수 함수 진단까지만이다. 이동 방향·초상·전투 무대가 실제로 바로 섰는지, 사가스토리 옆면 뷰(`rotation.y = ±π/2`)가 좌우로 맞는지, 사가의숲 NPC 가 바로 서는지는 사람이 봐야 한다. VRM 이 아닌 캐릭터(짐승 등)도 반대로 가는지는 이번엔 못 봤다 — 그렇다면 다른 원인이다.

**② 사가고 자동 전투**(`js/rogue-action.js`, 야생 조우·성채·토벌·적도 전부 이 엔진): 전투 HUD 에 **🤖 자동** 버튼. 켜면 사람이 화면에서 하는 입력만 대신 낸다 — 판정은 한 줄도 새로 안 만든다(§2 규칙).
- **판단**(`autoPlan(s, dist, react)`, 순수 함수): 멀면 다가가고(`in`)·사거리 안이면 서며(`hold`)·강타류 예고가 뜨면 **반응 시간(0.3초, 손잡이 `rogueAction.autoReact`) 뒤에** 반경 밖으로 물러선다(`out`). 필살은 기가 차면, 차지는 차고 헛치지 않을 거리(`MY_REACH×1.4`)일 때, 원거리는 쿨이 돌면 바로.
- **일부러 사람보다 약하게**: 반응이 늦어 예고가 짧은 돌진·긴 휩쓸기는 자주 맞고, 저스트 회피(토벌)는 안 한다. 물러설 때도 **걷는 속도**(달리기 안 함). 도망(물러나기)은 안 한다.
- **실행**: 이동은 화면 스틱과 같은 통로 `world.setStick()` 으로만, 필살·차지·원거리는 버튼이 부르는 `doUlt`·`doCharge`·`doRanged`(핸들러를 함수로 뽑았다). GPS 모드(`world.mode==='geo'`)는 몸을 코드로 못 움직이므로 스킬만 대신 내고 HUD 에 "직접 걸어 다가가세요"를 띄운다.
- **켜짐은 세이브**(`save.settings.autoBattle`, 칸 없으면 꺼짐)에 남아 다음 전투에도 이어진다. 끄거나 전투가 끝나면 스틱을 놓는다.
- **진단**: 511 → **514**(새 항목 3 — 판단표·화면 없이 굴린 자동 전투가 가만히 서 있는 것보다 3배 넘게 냄·세이브 칸). jsdom 기준 512/514, 실패 둘은 HEAD 에서도 나는 「배우 — Request is not defined」(jsdom 한계).
- **모른다(실기 몫)**: 반응 0.3초가 체감상 너무 잘 피하는지/너무 맞는지(예고 1.1초·0.825초·1.485초 대비), 자동이 손보다 얼마나 약한지, 폰 폭에서 버튼 다섯이 한 줄에 들어가는지(`.ra-pad`), 성채·토벌·적도에서 자동이 실제로 이기는지. 「화면도 이상하다」는 방향 문제였을 가능성이 커서 이번엔 HUD 배치는 안 건드렸다 — 그 말이 HUD 였다면 알려 달라.

## 2026-09-20 (이어서 2) — VRM 다이어트 `tools/glb-compress/vrm-slim.mjs` (실기 보고 2번 "사가블로 느림" 1차 처방, 다섯 판 공용)

같은 날 인물 전원이 VRoid 몸(기본 켜짐)이 된 뒤 사가블로가 무거워졌다는 보고. 화면을 못 보므로 **GLB 를 직접 재서** 원인을 좁혔다.
- **원인 둘(측정)**: ① 파일의 절반 이상이 얼굴 **표정 모프 타깃**(54~57개, 2.7~5.8MB) — 이 게임은 표정을 안 움직인다(코드 grep 0건, 초기 가중치 전부 0).
  ② **프리미티브 과다** — `sample_c` 는 재질 14개인데 프리미티브 135(머리카락 하나가 118조각), `a` 90·`b` 94. 프리미티브=드로우콜이고,
  `toon3d.outline()` 이 프리미티브마다 뒤집힌 헐 메시를 하나 더 얹으므로 한 명이 **한 패스에 270콜 넘게** 그렸다(그림자·SSAO 패스는 또 곱).
  정점 버퍼는 프리미티브끼리 공유라 삼각형 수(2.5~5만)는 문제가 아니었다.
- **처방**: 모프 타깃·`mesh.weights` 를 떼고, 한 메시 안에서 **같은 재질** 프리미티브의 인덱스만 이어 붙인다(정점 복제 없음). 뼈·스킨·재질·텍스처는 그대로.
  결과 — 용량 **25MB → 11MB**(a 5.19→2.25 · b 5.61→2.64 · c 4.97→2.11 · custom_01 9.42→3.59), 프리미티브 a 90→17 · b 94→19 · c 135→14, 캐릭터당 콜 약 8~10분의 1.
- **같은 모양인지 검증(스크립트로)**: 메시·재질별 삼각형 인덱스 목록이 원본과 **완전 일치**, 재질(알파 모드·양면·텍스처 이미지 해시·샘플러·`KHR_materials_unlit`) 의미 동일,
  노드·스킨·관절 수 동일. 차이는 `KHR_texture_transform` 항등 `{offset:[0,0],scale:[1,1]}` → `{}`(기본값) 뿐. gltf 검증기 결과는 원본과 같다(`SKIN_SKELETON_INVALID` 는 VRoid 원본에 원래 있음).
- **함정**: gltf-transform 은 `ALL_EXTENSIONS` 를 등록하지 않으면 `KHR_materials_unlit`·`KHR_texture_transform` 을 **말없이 떨군다**(경고만 뜬다) — 재질이 조명 받는 것으로 바뀐다. 도구에 등록해 두었다.
  `extensions.VRM`(메타·휴머노이드)은 도구가 다시 붙이고, 모프 결합(`blendShapeMaster.binds`)만 비웠다.
- 다섯 판 `assets/models/people/anime/` 넷을 같은 결과로 교체(md5 동일), 각 `ASSET_LICENSES.md` 끝에 가공 사실 한 절, `sw.js` 다섯 올림(go-v5.38.0·dungeon-v0.133.0·village-v0.80.0·side-v0.58.0·realm-v1.35.0).
  재실행: `cd tools/glb-compress && node vrm-slim.mjs <입력> <출력>` 후 다섯 곳에 복사. 이미 가공한 파일에 또 돌려도 결과는 같다(멱등).
- **모른다(실기 몫)**: 화면·프레임을 못 봤다. 로딩(용량)은 확실히 줄었지만, **프레임이 충분히 나아졌는지**는 실기기 확인 — 남은 후보는 한 화면의 VRoid 인물 수, 들판 소품·그림자(HIGH), 후처리(블룸·SSAO).
  여전히 무거우면 다음은 ⓐ 원거리 인물 외곽선 끄기 ⓑ 몸 4종 텍스처 512 유지 상태에서 메시 LOD ⓒ 사가블로 기본 품질 한 단 낮춤.

## 2026-09-20 (이어서 3) — 사가의숲 이동 방향 재수정: `faceFront` 를 안쪽 그룹으로 (다섯 판 공용, md5 동일)

실기 재보고 "사가의숲은 캐릭터 이동 방향이 아직 문제". **원인**: 위 (이어서) 절의 `faceFront` 는 모델 자신의 `rotation.y = π` 를 줬는데, 사가의숲은 `asset3d.build('hero')` 가 돌려준 모델을 그대로 `player.group`·NPC·주민 그룹으로 쓰고 매 프레임 `group.rotation.y = 걸음각` 을 **대입**한다(`village-view3d.js` `syncCamera`·주민·NPC 루프). π 가 그 대입에 지워져 숲에서만 VRM 이 여전히 뒤로 걸었다. (다른 판은 배우를 바깥 그룹으로 감싸서 살아남았을 가능성이 크다.)
**고침**: `js/vroid-variant.js` `faceFront` 가 모델의 자식들을 **안쪽 그룹(`vrmFront`)** 에 옮겨 담고 그 안에서 π 를 돌린다 — 바깥 `rotation.y` 는 게임이 마음대로 써도 된다. 모델이 메시 하나면(지금 VRM 은 전부 그룹) 옛 방식으로 되돌아간다. 뼈·몸짓·믹서는 그대로(믹서는 뼈를 이름으로 후손에서 찾는다). 진단(다섯 판 「vroid-variant — faceFront」)을 "걸음각 1.2 를 바깥에 줘도 표지가 (sin, cos) 를 본다"로 바꿨다. `sw.js` 다섯 올림.
**모른다(실기 몫)**: 화면을 못 봤다. 숲의 플레이어·NPC·주민이 걸음 방향으로 앞을 보는지, 사가스토리 옆면 뷰(`±π/2`)·사가고 전투 무대가 안쪽 그룹으로 바뀐 뒤에도 그대로 맞는지 확인이 필요하다. 반대로 걷는 게 **VRM 이 아닌 캐릭터(짐승 등)** 도 그렇다면 다른 원인이다.

## 2026-09-20 (이어서 4) — 2D 교체 Phase 0: 도감 초상이 3D 로 바뀌기 전 코드 스프라이트가 비치지 않게 (다섯 판, SAGA-DESIGN §11)

실기 보고 "도감 보면 로딩 전에 스프라이트가 보인다". 3D 초상이 구워지기 전 초기 그림이 `sprite.portrait()`(코드 그림)였다(`portrait3d.img`·각 ui 의 `p3src`).
- **고침**: 3D 로 **꼭** 갈아 끼워질 자리(`P3.willSwap` — 이름표를 되읽을 수 있고 `data.find` 가 찾으며 굽기를 포기하지 않았고 3D 가 켜져 있을 때)는 코드 그림 대신 **중립 자리표시**(`P3.holder(w,h)`, 투명 회색 액자+실루엣 SVG)로 시작한다(`data-p3-holder`). `p3src` 의 fallback 을 함수로 바꿔 자리표시일 땐 스프라이트를 아예 안 그린다(성능도 이득). 사가고는 `portrait3d.img()` 도 같은 판정.
- **함정 셋(고쳐 둠)**: ① 굽기가 **실패해도 `sweep` 이 안 불린다**(성공 때만) → 자리표시가 영영 남는다. `sweep` 이 자리표시가 남아 있는 동안 400ms 로 스스로 다시 본다. ② `ready()` 가 꺼져도(three 실패·손잡이 내림) `sweep` 이 자리표시를 옛 그림으로 되돌린다(`revert` — 정사각이면 `portrait`, 아니면 `portraitCard`). ③ `id` 에 `:` 가 있거나 `find` 가 못 찾는 참조·건물은 되읽을 수 없어 갈아 끼워지지 않으니 자리표시를 쓰지 않는다(옛 그림 그대로).
- **일부러 안 건드린 것**: 3D 와 무관한 코드 그림 자리(사가블로 `starter.js`·서명 무예 컷인·사가의숲 `admin.js`·사가고 `world3d` 빌보드 텍스처, 2D 뷰 `stamp`, 지도 건물·짐승) — Phase 1~3 몫. 못 쓰는 자리는 3D 가 없으니 코드 그림이 최종 그림이다.
- **알려진 체감 변화**: 도감을 열면 자리표시가 먼저 보이고 3D 가 한 명씩(굽는 줄이 하나씩) 채운다 — 스프라이트가 먼저 비치던 것이 사라진 대신 그 시간이 빈 액자로 보인다. **Phase 1(초상 미리 굽기 에셋)이 이 시간을 없앤다.**
- **진단 +1**(다섯 판): 자리표시 SVG 캐시·갈아 끼울 자리 판정(건물·`no:such`·빈 참조는 거짓)·굽기 포기 시 옛 그림 되돌림. 사가스토리 `_test.html` 에 `portrait3d.js` 가 빠져 있어(index 에는 있다) 넣었다. jsdom: 사가고 508/515(실패 19→7)·사가블로 359/361(4→2)·사가의숲 311/317·사가스토리 191/197 — **새 실패 없음**, 줄어든 것은 jsdom 에서 캔버스 스프라이트가 터지던 항목이 그림을 안 그리게 되어서다. `sw.js` 다섯 올림.
- **모른다(실기 몫)**: 화면을 못 봤다. 자리표시 모양(회색 실루엣이 어두운 도감 카드에서 읽히는지)·목록 스크롤 중 채워지는 속도·GPS 지도 위 카드에서 옛 그림 되돌림이 실제로 되는지.

## 2026-09-20 (이어서 5) — 2D 교체 Phase 1(초상 미리 굽기, 다섯 판) + Phase 2(사가스토리 사람) + 등급 체계(사가의숲·사가스토리·사가국지)

사용자 "1,2,3 해" — SAGA-DESIGN §11 Phase 1(헤드리스 크롬 허용, Q-2D-1 해소)·Phase 2 의 사가스토리·아이템·장비 등급 체계 셋 다.

**Phase 1 — 초상을 디스크 파일로**
- 도구 `saga-web/tools/bake-portraits/bake.mjs`(README 대신 머리말): 임시 정적 서버 + 헤드리스 크롬(swiftshader) + CDP. **게임 자신의 `DG.portrait3d.warm()`/`of()` 를 부려** 정사각 96px(→192)·카드 150×172(→300×344)를 webp(0.8)로 받는다 → `<판>/assets/portraits/{hero,pet}/<id>_s|c.webp` + `manifest.js`(목록, 부분 실행이면 합침). 옵션 `--kind --limit --only --tune=키:값 --gl=d3d11 --out --eval=파일.js`. 자기가 띄운 크롬 PID 만 끝낸다.
- 런타임: 다섯 판 `portrait3d.js` 에 `diskOf`/`diskOfKey`(정사각 ≤110px → _s, 150×172 꼴(±0.03) → _c, `portrait3d.on` 손잡이가 내려가면 끔). `of()` 가 목록에 있으면 파일 주소를 바로 주고(three·GLB 대기 없음) `warm()` 은 안 굽고 `sweep()` 이 이름표 <img> 를 파일로 갈아 끼운다. `index.html` 에 `assets/portraits/manifest.js` 스크립트, `sw.js` SHELL 에 manifest 추가 + VERSION 올림(다섯 판). **단독 빌드(`build/build-single.mjs`, 네 판)는 이 스크립트·태그를 뺀다**(assets 가 단독 파일에 안 들어가 깨진 그림이 뜨므로).
- 굽힌 것: 사가고 인물 105·펫 105, 사가블로 인물 135·펫 14(GLB 있는 것만), 사가의숲 인물 105·펫 105, 사가스토리 인물 105·펫 7(`supports()` 만), 사가국지 인물 233(나머지 27명은 못 구움, 펫은 안 굽는 판 — **사가국지는 반드시 `--kind=hero`**: `willSwap` 이 펫에도 참이라 펫을 시키면 하나마다 45초 타임아웃을 기다린다). 절차 생성 인물(`genchar`)·GLB 없는 펫은 파일이 없어 예전처럼 실시간 굽기. 용량 판당 1.5~3.2MB(총 약 13MB).
- 사가블로 `starter.js`·서명 컷인이 코드 그림(`sprite.portraitCard`)을 직접 부르던 두 곳은 구운 초상이 있으면 그것을 먼저 쓴다.
- **함정 셋(고쳐 둠)**: ① 사가블로는 조립을 `asset3d.tick()` 이 프레임당 하나씩 비워야 끝난다 — 굽기 페이지엔 게임 루프가 없어 `assetState` 가 영영 `shape` → 도구가 `tick()` 을 40ms 마다 돌린다. ② **펫 초상 외곽선 줄무늬**: 소프트웨어 렌더·실제 GPU(d3d11) 둘 다 동물 GLB 의 뒤집힌 헐 외곽선이 몸과 어긋나 검은 줄무늬가 번진다(사람은 정상) — 게임 실시간 굽기도 같은 코드라 **원래 있던 결함**으로 보이나 원인은 안 캤다. 펫은 `--tune=world3d.outline:0` 로 외곽선을 끄고 구웠다(깨끗함). ③ 사가스토리 펫·사가블로 펫은 굽기가 안 되는 종이 많아 실패마다 1.5초 기다린다(전체 굽기 5~6분).
- 진단: 다섯 판 `_test.html` 에 "디스크 초상" +1(가짜 목록으로 `of`·`warm`·`sweep` 검증). 사가고 516/516, 사가의숲 319/319 통과.
- **모른다(실기 몫)**: 화면을 못 봤다(구운 파일은 직접 열어 봄 — 사람 정면·펫 정상). 도감을 열자마자 뜨는지·인물 카드와 정사각 잘림·`data:` 가 아닌 파일 주소를 쓰는 깜빡임·서비스워커가 새 SHELL 을 받는지.

**Phase 2 — 사가스토리 옆면 뷰 사람(2D 폴백)**
- `sprite.js` `bake()` 사람 분기를 Kenney 열넷(사가의숲과 같은 `human_01~14.png`, md5 동일) + id 해시로. 그림이 안 실린 첫 프레임은 옛 `human()` 으로 채우고, 실린 뒤엔 `stamp()` 가 그 컷을 다시 굽는다(박제 방지). 에셋 그림엔 메이플풍 후처리(`storyize`)를 안 걸었다(윤곽이 이미 있음). `human()` 은 초상 되돌림 등이 아직 불러 남겼다. `assets/ASSET_LICENSES.md` 에 Kenney 절 추가. 사가블로 2D 는 Q-2D-2 대기.
- 진단 사가스토리 197/199 — **실패 2건(§5-1 궁수·§5-4 관문 대장)은 HEAD 사본에서도 그대로다(195/197)**, 이번 변경과 무관.

**등급 체계 — 사가고 외 세 판**(사용자 요청 2026-09-10, 각 판 구조부터 훑고 값에서 읽는 이름표로 좁혔다 — 값·세이브·판정 무관)
- 공통: 도감 `RARITY.name`(기본·상급·희귀·영웅·전설) — **사가고 `data.js` 만 갖고 있던 것을 네 판에 복사해 다섯 벌 md5 를 다시 맞췄다**(다섯 벌이 이미 어긋나 있었고 차이는 `RARITY.name` + 일본·중국 생성기 조회뿐, 그 모듈이 없는 판은 무동작).
- 사가스토리: 장비 요구 Lv 단 1·5·12·20 → 기본·상급·희귀·영웅, 고유는 전설(`gearData.grade`) — 낀 것·가방·상점 이름 옆 색 글씨. 도감 카드·태그·툴팁에 등급 이름.
- 사가의숲: 물건 값에서 — 가구·옷·바닥 <800 기본 … ≥4200 전설, 채집물 <60 기본 … ≥450 전설(`villageData.gradeOf`). 채집 판매·가구 상점·창고·옷·마감 목록 이름 옆 글씨. 도감 카드·태그·툴팁에 등급 이름.
- 사가국지: 보물 보너스 +6·+8·+9·+10 → 기본·상급·희귀·영웅(+12 이상 전설, 지금 표엔 없음) — 무장 보물 한 줄(`itemBadge`).
- 진단 +1씩(사가스토리·사가국지·사가의숲, 경계값 검증). 사가블로는 이미 상품→전설 체계라 손 안 댐.
- **모른다(실기 몫)**: 목록에서 이름 옆 색 글씨가 읽히는지·경계(가구 800·1600·2800·4200, 채집 60·120·250·450)가 체감에 맞는지 — 표에서 숫자만 고치면 된다.

## 2026-09-20 (이어서 6) — 2D 교체 Phase 2: 사가블로 2D 를 살림 (Q-2D-2 결정: "사가블로 2D 살려서 이어해")

사가스토리와 같은 길 — `saga-dungeon/js/sprite.js` `bake()` 사람 분기를 Kenney 열넷(`assets/sprites2d/human_01~14.png`, 사가의숲 것과 md5 동일, 새 폴더) + 인물 id 해시로. 4곳의 `stamp()`(`dungeon-view.js`: NPC·동행·주인공·인간형 적)가 다 이 길을 탄다. **이 판의 양식인 `diabloize`(어둡고 채도 낮게, 위쪽 테)는 그림에도 그대로 건다**(사가스토리는 후처리를 뺐던 것과 다르다). 그림이 안 실린 첫 프레임은 옛 `human()`, 실린 뒤 `stamp()` 가 한 번 다시 굽고 이후 캐시(프로브로 확인: 첫 컷 462픽셀 → 재구움 2218픽셀 → 셋째는 히트). `ASSET_LICENSES.md` 에 Kenney 절 추가, sw VERSION 올림. `human()`·`beast()` 는 남겼다(초상 되돌림·짐승 Phase 3).
- **모른다(실기 몫)**: 화면을 못 봤다(스크린샷 금지 규칙). 디아블로풍 어둠 속에서 밝은 Kenney 그림이 어울리는지·크기(H×1.2)가 예전 코드 그림과 키가 맞는지 — 어색하면 `diabloize` 세기나 `bake()` 의 그림 크기(`hdw`)만 손보면 된다.

## 2026-09-20 (이어서 7) — 2D 교체 Phase 3(일부): 사가블로·사가스토리 적 짐승을 3D 몸에서 구운 옆모습 걷기 시트로

- **도구**: `tools/bake-portraits/bake.mjs --sprites=monsters [--tune=world3d.outline:0]`(사가블로용). `data-enemy.js` 의 짐승 형 적 몸(`body`, 없으면 `beast`) 60종을 게임의 `asset3d.build()` 로 세워 **측면 직교 카메라**(-X 에서 +X, 앞(+Z)이 오른쪽)로 5컷 가로 시트(걷기 4 + 서기 1, 컷 128px, 발 아래·중심 가운데, 컷 사이 배율 동일)를 `assets/sprites2d/mon_<몸>.webp` 로 굽고 `mon-manifest.js`(`DG.monsterSprites.keys`)에 키를 적는다. 걷기 클립이 있는 몸(대부분)은 걷기 4컷이 실제 걷는 자세다. 외곽선은 끈다(펫과 같은 줄무늬 문제).
- **런타임(사가블로)**: `sprite.js` `bake()` 짐승 분기 — `monKeyOf(ref)`(manifest 에 있는 몸 && `ref.body` 또는 `tier` 있는 적 정의)이면 시트 컷을 그린다(걷는 위상 `pb>>1`, 서기 `pb===PHASES`→컷 4, 크기 `H×1.55`) + `diabloize` 유지. 못 쓰면(파일 없음·manifest 없는 단독 빌드·**도감 펫은 `tier` 가 없어 안 탐**) 옛 `beast()`. 그림이 안 실린 첫 프레임 컷은 실린 뒤 `stamp()` 가 한 번 다시 굽는다(사람 그림과 같은 장치, `e.img`). index.html·sw.js(SHELL·VERSION)·`build-single`(sprites2d 스크립트 제외) 연결. **`build-single.mjs` 네 개의 주석 글자가 지난 커밋에서 깨져 있던 것(인코딩 실수, 코드는 정상)을 이번에 바로잡았다.**
- **사가스토리**: 사가블로가 구운 시트 셋(`beast`=늑대·`beast_big`=코끼리·`beast_boar`=멧돼지, md5 동일)을 복사해 오고 manifest 를 손으로 씀. 몸 고르기는 이름으로(`코끼리`→big, `산짐승|멧돼지`→boar, 나머지 늑대) — 이 판 3D 뷰의 `beast_big` 은 소(`Cow.glb`)지만 2D 는 이름에 맞게 코끼리. 메이플풍 후처리(`storyize`)는 짐승 시트엔 그대로 건다(윤곽이 없어 어두운 배경에 묻힘 방지).
- **라이선스**: 시트는 각 원본 모델의 파생물 — Slime Enemy(Charlie)·Boar·Tiger·Elephant(Poly by Google)는 **CC-BY 표시 의무**가 그대로 따른다. 두 판 `ASSET_LICENSES.md` 에 절 추가.
- 진단: 사가블로 362/362, 사가스토리 197/199(같은 기존 실패 2건). 프로브로 확인: 사가블로 늑대·티렉스 첫 컷 코드 그림 → 시트 재구움 → 캐시 히트, 도감 펫은 코드 그림 그대로.
- **모른다(실기 몫)**: 화면을 못 봤다. 시트 크기·발 위치가 예전 코드 그림과 맞는지, 유령·해골처럼 정면/뒷모습이 섞여 보이는 몸(KayKit 해골 등은 측면이 어색)이 눈에 거슬리는지, 시트가 어두운 던전에서 `diabloize` 로 읽히는지. 어색한 몸만 manifest 에서 빼면 옛 코드 그림으로 돌아간다.
- **남은 Phase 3**: 사가고 지도 건물(`building()`)·깃발·에셋에 없는 배경 생물(`beast()`), 사가블로 건물·환경 그림. Phase 4(코드 그림 함수 삭제)는 호출부가 0 이 되면.

## 2026-09-20 (이어서 8) — 2D 교체 Phase 3(사가고): 배경 생물 다섯 종·역참 지도 마커를 코드 그림에서 에셋으로

- **배경 생물(`animal.js` 사슴·늑대·까치·잉어·소, id `an_*`)**: `sprite.js` `BG_BEAST_FILE` 표로 닮은 모델 하나를 못 박아 `assets/sprites2d/beast_<이름>.png`(이미 있던 것: Deer·Wolf·Pigeon·Koi·Cow)를 쓴다. `beastImgOf` 가 도감 펫(`pt_/pk_`)뿐 아니라 이 표의 `an_*` 도 받는다(내보냄 `DG.sprite.beastImgOf`). 새 굽기 없음.
- **그림이 안 실린 채 박제되던 것 고침**: 이 판 `stamp()` 는 첫 프레임에 그림이 아직 안 실렸으면 코드 그림 컷을 구워 캐시에 넣고 **그대로 둬** 그 크기·걸음 위상은 캐시가 밀릴 때까지 코드 그림이었다. 사가스토리와 같은 장치 — `bake()` 가 `img`(그림을 썼는가)를 돌려주고 `stamp()` 가 `!e.img && imgReady()` 면 다시 굽는다(사람·짐승 공통).
- **역참 지도 마커**: `tower_ruin.glb` 굽기가 끝내 안 돼(`tools/bake-icons/README.md` 알려진 흠) 코드 그림 `form:'stable'` 이 남아 있었다 → 옛 역참 모델 `Inn.glb`(CC0)를 `_bake_one.html` 로 구워 `assets/sprites2d/building_Inn.png`(96px) 로, `world.js` 역참 그리기가 `buildingImg('Inn')` 을 넘긴다. **`_bake_one.html` 에 `&fit=`(기본 1.6, 탑 기준)·`&px=` 쿼리를 더했다** — 넓은 건물은 기본값이면 지붕만 크게 잘려 나온다(Inn 은 `fit=0.7`). 그림이 안 실렸으면 옛 코드 그림으로 떨어진다(그대로).
- `sw.js` VERSION go-v5.42.0. 진단 `_test.html` 517/517(새 진단 1건: 다섯 종이 모두 `beast_` 그림 주소를 받는지·첫 프레임 굽기 횟수). `ASSET_LICENSES.md` Inn 줄에 파생 그림 언급.
- **깃발은 손댈 것이 없다**: `sprite.flag()` 는 `building()` 의 `barracks` 형만 부르는데 그 형은 본편 호출부가 없다("경영을 뺐다"). 지도의 성채 깃발은 `world.js` 가 직접 그린 삼각형이다 — 코드 그림 스프라이트가 아니라 지도 UI 도형이라 유지.
- **남은 것(Phase 4)**: `portrait()`·`portraitCard()`·`human()`·`beast()`·`building()` 이 아직 호출된다 — `portrait3d.js` 되돌림(347·434행), `ui.js` 1104·1305, `world3d.js` 1901(빌보드 되돌림)·2228, `_test/_demo/_admin`. 이걸 걷으려면 되돌림 그림을 뭘로 할지(디스크 초상 `assets/portraits`·Kenney `human_*`·`beast_*` 로 충분한가) 정해야 한다. 사가블로 건물·환경 그림도 남았다.
- **모른다(실기 몫)**: 화면을 못 봤다. 역참 그림이 지도에서 성채 그림과 같은 크기 감으로 읽히는지, 배경 생물이 사슴·늑대처럼 보이는지(까치는 비둘기 모델).

## 2026-09-21 (이어서 9) — 2D 교체 Phase 3(마무리): 사가블로·사가의숲·사가스토리·사가국지의 죽은 `building()`·`flag()` 삭제

- **무엇**: 네 판 `js/sprite.js` 에서 `building()`(한옥 기와 건물 그림)과 그것만 부르던 `flag()`·`ROOF` 표·지붕/기단/몸체 보조 함수(270줄)를 지웠다. 내보내기 `DG.sprite.building`, 초상 되돌림의 `kind === 'building'` 분기, 머리말 설명도 함께. 제거 원칙("안 쓰이게 되는 순간 지운다", SAGA-DESIGN §11-2)에 따른 것 — Phase 4 까지 안 미룬다.
- **왜 지워도 되나**: 네 판 모두 `building` 호출부가 `sprite.js` 안의 `portrait(kind='building')` 분기 하나뿐이었고, 그 분기를 부르는 곳이 없었다(`grep` 으로 js·_test·_admin·_demo 확인). `P3.willSwap('building', …)` 진단은 `portrait3d.js` 쪽이라 영향 없다. `flag()` 는 `building()` 의 barracks 형만 불렀다. 사가의숲 진단 "깃발" 은 마을 깃발(별개 코드)이다.
- **사가고는 그대로**: `world.js` 1702 가 `DG.sprite.building(…, {img: buildingImg('Inn')})` 로 아직 부른다(역참 마커가 에셋 그림이지만 진입점이 이 함수). Phase 4 에서 그 진입점을 얇은 함수로 바꾸며 함께 정리한다.
- **확인**: `node -c` 네 판 통과. jsdom 러너(스크래치패드; 서버·크롬 없이 _test.html 을 돌리는 방식)로 수정본과 HEAD 를 같은 러너로 돌려 결과가 **줄 단위로 동일** — 사가블로 360/362(실패 2건 HEAD 에서도 동일), 사가의숲 318/319(깃발 그림 1건 HEAD 동일), 사가스토리 195/199(HEAD 동일 4건), 사가국지 210/210. `git diff --numstat` 은 판마다 +2/-275(줄바꿈 뒤집힘 없음).
- **남은 것**: Phase 4(사가고 `portrait()`·`portraitCard()`·`human()`·`beast()` 되돌림 그림을 디스크 초상+Kenney/`beast_*` 로 갈지 — **사용자 결정 필요**), 실기 확인, VRoid 캐릭터 추가. 사가블로 "환경 그림"(타일 등)은 `sprite.js` 가 아니라 `dungeon-view.js` 가 캔버스 그라디언트·도형으로 직접 그린다(벽·바닥·문). `sprite.js` 밖이라 이번 삭제 대상이 아니고, 에셋 교체 여부는 사용자 결정 몫이다.

## 2026-09-21 (이어서 10) — PLAN §5 ② 사당(祠堂) 시련 — Phase 2 둘째(코드분 완료)

2D 교체 줄기(Phase 4)는 사용자 결정 대기라, 로드맵의 다음 미착수 후보 ②를 지었다.

**CREATE `js/shrine.js`** — 봉수대(`beacon.js`)를 본떴다. 권역 27 대표점마다 사당 셋(81) — 대표점에서 350~1400m 를 해시로 밀어 고정, 늘 lat/lng 로만 갖고 그때그때 `world.latLngToWorld()`(origin 이 GPS 첫 수신 때 바뀐다). **숨은 자리**: 보이는 때는 ① 120m 안 ② 그 권역 봉수대에 불을 올려 열린 반경 1.5km(`beacon.revealedNear()` 가 `shrine.within()` 을 이어 붙인다 — 봉수대 절 "사당은 한 줄 보탠다" 가 이것) ③ 한 번이라도 깬 자리. 30m 안이면 들어간다.
- **시련**: 파도 셋 = 늑대 무리(90) → 산적(120) → 사당 수호자(정찰병 170, 기세 ×1.6) — `event.FOES`·`foeVisual` 을 그대로 빌린다(새 적 칸 없음). 엔진은 `rogueAction.open(o)` 그대로(수정 없음), 파도 전환만 onDone 체인. **제한 3분은 파도별이 아니라 전체 예산**(다음 파도 `timeSec` = 남은 시간, 최소 5초; 예산을 넘기고 파도가 남으면 실패, 마지막 파도를 깼으면 통과).
- **보상**: 금 150·공적 60·丹 10·인장 조각 1 + **그 권역의 인물**(`genchar.hero(권역, ★3 60%/★4 40%, 해시)` — 같은 (사당, 횟수)면 같은 인물) 이 곁에 선다. 조각 셋은 자동으로 인장 하나(`bag.js` 새 물건 `shard`·`seal`) — 쓸 곳(⑦ 승급 3택)은 아직 없다.
- **비용**: 지면(시간 초과·기세 꺾임·둘째 파도 이후 이탈) 사료 2 를 잃고 그 사당 10분 봉쇄(`failAt`). 첫 파도에서 한 대도 못 치고 물러나면 시련이 없던 것으로(횟수·비용 없음). 하루 셋 — `save.daily.shrine`, 날이 바뀌면 `daily.js st()` 가 0 으로.
- **세이브**: `save.shrines{키:{clears,lastAt,failAt}}`(스펙의 `{clears,lastAt}` 에 `failAt` 하나 보탬) · `save.daily.shrine`. 없으면 초기값(읽기는 세이브를 안 만든다 — 살피기 진단이 잡는다).

**바꾼 곳**: `world.js` `spawnSpecial(rarity, ref)` 둘째 인자 · `daily.js` 롤오버에 `shrine=0` 한 줄 · `bag.js` 물건 둘 · `beacon.js` `revealedNear` 끝에 사당 · `ui.js` 근접 패널 사당 카드(`data-act="shrine"`, 500m 안·보이는 것만)와 목표판 "지금" 줄 · `minimap.js`·`overworld.js` 사당 점(보이는 것만) · `index.html`·`_test.html`·`sw.js`(`go-v5.43.0`) 스크립트 등록 · `_admin.html` 손잡이 `shrine.dailyMax`·`shrine.timeSec`. **`event.js`·`land.js`·`geo.js` 는 안 고쳤다**(PLAN 은 MODIFY 로 적었었다 — 봉수대와 같은 길이라 필요 없었다).

**일과 풀의 `shrine` 항목은 꺼 둔 채다**(`daily.js` `disabled:true`). 사당이 27 대표점 근처에만 있어 그 밖에 사는 사람은 못 채우는 일과가 사흘에 하루쯤 뜬다 — 켤지는 사용자 결정. 클리어 때 `daily.progress('shrine')` 는 이미 부른다(꺼져 있으면 아무 일도 없다). 켜려면 `disabled` 한 줄과 진단 "비석·사당은 안 뽑힌다" 를 고친다.

**확인**: `node -c` 통과(신규·수정 js 전부·인라인 진단). jsdom 러너로 수정본 **525/527**(세 번 동일), HEAD **515/517** — 새 진단 10건(자리 81 결정성 · 숨은 자리 · 하루 셋·롤오버 · 실패 비용·봉쇄·무피해 이탈 · 시간 예산 · 파도 사양 · 클리어 보상·인물·인장 · 살피기 세이브 불변 · 오버월드 노출 · 가짜 전투 엔진으로 입장→파도 셋→결과 카드·패배·무피해 이탈) 전부 통과, 실패 2건은 HEAD 와 같은 jsdom 한계(`배우` 두 건 `Request is not defined`). **실제 화면·진짜 rogue-action 연결은 못 봤다** — 판정 층과 가짜 엔진 체인만 굴렸다.

**실기 확인 대기**: 사당 입구 카드가 "가까운 것" 패널에 봉수대·역참 카드와 같이 떴을 때 안 헷갈리는지 · 파도가 끝나 다음 파도가 곧바로 열릴 때 `#rogue-hud` 가 깜빡이거나 안 열리는지(`rogue-action.finish()` 가 `cb` 를 부르기 전에 `cur` 를 비우므로 onDone 안에서 다시 `open` 해도 되게 읽었다) · 3분이 빠듯한지 · 미니맵·오버월드 사당 점이 봉수대 이름표와 안 겹치는지 · 인물이 곁에 선 뒤 조우 카드로 잘 이어지는지.

## 2026-09-21 (이어서 11) — PLAN §5 ⑦ 승급 3택 — Phase 3 첫째(코드분 완료)

②(사당) 커밋·푸시(008d7acc) 뒤 로드맵의 다음 후보. "가장 큰 구멍 ②(선택·실패 없음)"를 채우는 자리라 ⑤(비석) 대신 이쪽을 먼저 지었다.

**CREATE `js/perk.js`** — 특성 12(攻 4·守 4·補 4)와 카드 로직. 攻·守 는 무력·지력·통솔 배율 +5~8%, 補 는 동행 중일 때 경험치 +6·설득 +5·귀한 만남 +6·신령 만남 +8(%). 카드 셋은 갈래마다 하나 — (인물 id, 승급 단계) 해시라 **다시 굴릴 수 없다**. 인물당 셋까지(같은 특성 중복 불가), 거절하면 丹 10.
- **`hero.js`**: `perkMul(id, 스탯)` 를 `stats()`·`breakdown()` 성장 배율 층에 곱한다(특성이 없으면 1 — 기존 수치·진단 불변). `rankUp()` 끝에서 `perk.offer(id)` 로 카드를 `save.heroes[id].offer` 에 건다. `rankUpCheck()` 는 카드가 걸려 있으면 '특성 카드부터' 로 잠근다(안 그러면 앱을 닫는 사이 카드가 덮여 사라진다).
- **`core.js`**: `effect()` 소스 목록에 `perk.bonus` 한 줄(補 갈래, 파티에 있는 인물만). **`auto.js`**: 자동 승급 뒤 `perk.autoPick()` = 첫 카드.
- **UI(`ui.js`)**: PLAN 은 "승급 결과 카드 뒤 3택 카드"라 적었지만 인물 상세가 승급 때마다 통째로 다시 그려지므로 **상세 카드 안 능력치 아래**에 카드 셋을 세웠다(`perkBlock`, 카드=버튼 셋 + "물린다"). 가진 특성은 같은 자리에 칩으로. 상세를 나중에 열어도 걸린 카드가 그대로 보인다.
- **세이브**: `heroes[id].perks[]`·`offer[]`(둘 다 선택 필드 — 없으면 빈 값, 옛 세이브 그대로 읽힌다). `sw.js` `go-v5.44.0`.

**확인**: `node -c` 통과. jsdom 러너로 **535/537**(세 번 동일; 직전 525/527 에 새 진단 10건 — 서로 다른 축 · 100번 굴려 중복·재등장 0 · 같은 입력 같은 카드 · 승급→카드→잠김→고르면 열림 · 능력치에 곱으로만(기본치 불변) · 거절 丹 10 · 補 는 동행일 때만 core.effect · 셋 상한 · 자동은 첫 카드 · 살피기 세이브 불변), 실패 2건은 HEAD 와 같은 jsdom 한계. **화면(상세 카드 안 3택 버튼)은 못 봤다.**

**실기 확인 대기**: 상세 카드가 길어져 폰에서 스크롤이 답답하지 않은지 · 카드 세 장이 버튼으로 잘 눌리는지 · 승급 직후 카드가 바로 보이는지(승급 버튼 자리가 아니라 능력치 아래라 눈에 띄는지) · 補 갈래가 너무 약하거나 강하지 않은지(파티 다섯이 모두 補 셋이면 경험치 +90%).
**열린 것**: ⑥ 인연·⑤ 비석·⑧ 패배 비용은 그대로. 인장(`seal`)의 쓸 곳은 ⑦ 스펙에 없어 비어 있다.

## 2026-09-21 (이어서 12) — PLAN §5 ⑤ 비석(碑石) 순례 — Phase 3 둘째(코드분 완료)

②(사당)·⑦(승급 3택) 커밋·푸시(008d7acc·76f1c88→merge accf139c) 뒤 Phase 3 의 남은 후보. 다음은 ⑥ 인연·⑧ 패배 비용.

**CREATE `js/stela.js`** — 실제 위경도를 200m 격자로 나눠 칸마다 해시 12% 에 비석 하나(어디서나 같다, 밀도 3.19/km² 실측). 15m 안이면 `game.js` 루프의 `stela.tick` 이 저절로 발견 → 丹 3·공적 5(판정엔 안 닿는다). 권역(가장 가까운 27 대표점)마다 열 개 = 그 권역 ★4 인물 초대장.
- **자리**: 위도 행마다 경도 폭을 달리한다(`colStep(row)`) — 행 중앙 위도로 200m. 칸 안 자리는 가장자리 10% 를 피한 해시. 위경도로만 갖고 그때그때 `world.worldToLatLng` 로 견준다(origin 이 GPS 첫 수신 때 바뀐다).
- **초대장**: `letter.js` 에 등급 키 `st:<권역코드>`(`stelaGrade`, 3km·★4·genchar)를 얹었다. 가방(9)이 차면 못 받은 몫이 남고(`save.stelaeInv` = 받은 수, 몫 = 열 개마다 하나 − 받은 수) 12초마다 다시 시도. 봉을 뗄 때마다 다른 인물(`opened` 를 섞은 인덱스).
- **도감**: `codex.js` 새 갈래 `stele`(비문) — 27 권역 줄, 첫 비석에 도장·힌트 n/10.
- **지도(48절)**: 미니맵은 발견한 것만 600m 안(`foundNear`). 오버월드는 발견한 것 전부 + 봉수대에 불을 올린 반경의 **미발견을 흐릿하게**(`beacon.revealedNear` 끝에 `stela.revealedWithin` 이어 붙임 — 사당과 같은 자리).
- **일과**: `daily.js` 풀의 `stele` 항목을 켰다(격자가 어디서나 서서 못 채울 사람이 없다). 발견 때 `daily.progress('stele')`. 진단 "비석·사당은 안 뽑힌다" 는 사당만·비석은 60일 표본에 든다로 고쳤다.
- **세이브**: `save.stelae{격자키:at}`(상한 2000, 넘으면 기록 안 함) · `save.stelaeInv{권역:받은 수}`(스펙에 없던 칸 — 초대장 몫 계산). `sw.js` `go-v5.45.0`. 어드민 `stela.chance`.
- **안 한 것**: `world.js` 근접 훅(tick 으로 대신)·3D 비석 소품(`procgen`)·발견 연출 카드(토스트+도감 도장으로 대신).

**확인**: `node -c` 통과. jsdom 러너 **544/546**(세 번 동일; 직전 535/537 에 새 진단 9건 — 격자 해시 결정성 · 밀도 2~4(108개 창 평균 3.19) · 15m 발견·두 번 불가 · 일과 진행 · 열이면 초대장(가방 참/비움 · 3km 뒤 그 권역 ★4)· 도감 비문 · 미니맵 발견만·오버월드 흐릿하게 · 판정 불변·살피기 세이브 불변 · 상한 2000), 실패 2건은 HEAD 와 같은 jsdom 한계. **실제 GPS 로 걸어 본 적은 없다** — 시뮬레이션 좌표로만.

**실기 확인 대기**: 15m 반경이 GPS 오차(보통 5~20m)에 견주어 너무 빡빡하지 않은지 · 밀도(집 근처 3/km²)가 걷는 재미로 충분한지 · 발견 토스트가 다른 토스트에 묻히지 않는지 · 오버월드에 흐릿한 점 수십 개가 지저분하지 않은지 · 초대장 3km 가 적당한지.

## 2026-09-21 (이어서 13) — PLAN §5 ⑥ 인연(因緣) — Phase 4 첫째(코드분 완료)

⑤(비석) 커밋·푸시(2b24d0d→41d25579) 뒤 로드맵의 남은 후보. 이로써 PLAN §5 의 ①~⑦ 은 전부 코드분이 끝났고 ⑧(패배 비용)만 남는다(§10-Q4 벌칙 충돌 — 사용자 결정).

**CREATE `js/bond.js`·`js/data-bond.js`** — 파티가 함께 걸은 거리(2·6·15km)·함께 이긴 토벌(3·10·25승) 중 먼저 닿는 쪽으로 인연 0~3. **파티에 있을 때만** 쌓이고 오르는 순간 한 마디가 토스트·로그로 뜬다.
- **능력 +2%/등급**: `hero.perkMul()` 이 승급 특성 몫에 `bond.mulOf(id)` 를 곱한다(`stats`·`breakdown` 성장 배율 층 — 기본치 불변, 인연 0 이면 1).
- **결(結)**: **같은 세력** 둘이 파티면 `core.effect('kiPct')` +10(쌓이지 않음 — 다섯이 다 같아도 10). PLAN 의 "같은 faction/era" 에서 시대를 뺐다 — 시대는 4종뿐(삼국지 22·한국사 26·일본사 20·세계사 37)이라 파티 다섯이면 비둘기집 원리로 늘 켜진다. 세력은 56 곳(조선 12·촉 8·위 5·오 5…)이라 무작위 5인 파티에서 23%, 맞춰 편성하면 켜지는 진짜 선택이 된다. `rogue-action.create` 가 `kiMul` 로 만들 때 한 번 읽고 속공·저스트 회피의 기 충전에 곱한다(`tick` 은 그대로 순수). 인연 0 파티는 `kiMul=1` 이라 기존 진단·수치 불변.
- **대사**: PLAN 의 "8×105 새 문장"은 안 썼다 — 기질 4 × 단계 3 × 변형 3 = 36문장을 인물 id 해시로 배정(`{name}` 자리만, 실명·시대·시리즈 이름 없음, 회귀 진단). 인물별 고유 문장이 필요하면 `data-bond.js` 에 `BY_ID` 표를 얹으면 된다.
- **UI**: 인물 상세에 인연 칸(♥ 셋·+n%·다음까지 걸음/승·결이 맞는 동료) · 인연 3 이면 초상 테두리 금빛(`.dt-portrait.bond3`). **파티 목록·지도의 초상은 그대로.**
- **훅**: `game.js` `bond.tick()`(세션 `mark` 기준 — 세이브를 되돌리거나 거리가 줄면 기준점만 다시 잡는다) · `raid.js fight` 승리에서 `onRaidWin()`. `world.js` 는 안 고쳤다. `core.js` effect 소스에 한 줄. `sw.js` `go-v5.46.0`.
- **세이브**: `heroes[id].bond{walk,wins,lv}`(선택 칸). 동행하지 않은 인물은 기록조차 안 생긴다(진단).

**확인**: `node -c` 통과. jsdom 러너 **552/554**(두 번 동일; 직전 544/546 에 새 진단 8건 — 걸음으로 오름·상한 3 · 승으로 오름·먼저 닿는 쪽 · 파티 밖은 안 오름 · 결 +10%(쌓이지 않음, 실제 속공 기 9.00→9.90) · 능력 +2% 곱·기본치 불변 · 대사 105×3 실명 0·서로 다른 문장 30 · 알림 한 번씩 · 살피기 세이브 불변), 실패 2건은 HEAD 와 같은 jsdom 한계. **화면은 못 봤다.**

**실기 확인 대기**: 인물 상세가 길어졌다(특성 칸 + 인연 칸) — 폰에서 스크롤 · 인연 오를 때 토스트가 다른 토스트에 묻히지 않는지 · 15km 가 파티 하나에게 너무 먼지(걷는 사람은 하루 5~10km 라 인연 3 은 며칠) · 결이 너무 드물지 않은지(세력이 같은 쌍은 무작위 파티에서 23% — 세력을 맞춰 파티를 짜는 재미가 되는지).

## 2026-09-21 (이어서 14) — PLAN §5 ⑧ 패배 비용과 회수 — Phase 2 마지막(코드분 완료)

⑥(인연) 커밋·푸시(5f34f679) 뒤 마지막 남은 후보. 이로써 PLAN §5 ①~⑧ 코드분이 전부 끝났다(전부 실기 확인 대기). §10-Q4 는 PLAN 에 이미 "확정(2026-09-18, 15%·상한 300, 어드민에서 0 까지)"이라 그대로 지었다.

**CREATE `js/drop.js`** — 졌을 때 소지 금의 15%(상한 300)가 그 자리 위경도에 '떨어진 짐'으로 남는다. 10분 안에 **150m 안에서 넷(토벌·성채·도적전·사당) 중 무엇이든 이기면** 되찾고(반경 안 짐 전부), 아니면 흩어진다. 동시 3개 — 넷째가 생기면 가장 오래된 것이 먼저 흩어진다. 위경도로만 갖는다(origin 이 GPS 첫 수신 때 바뀐다 — stela.js 와 같은 요령).
- **손으로 진 판에만 붙는다** — `raid.fight`·`rogue.fight`·`fort.challenge` 는 `opts.live` 일 때만 `drop.lose()`, 인자 없는 즉시 판정(자동 순행·자가진단·`rogue-action` 없을 때의 폴백)은 잃지 않는다(자동이 사람 몰래 금을 흘리면 안 된다). **사당은 늘 손으로 치르므로** `settleFail` 에서 항상. 이긴 쪽의 회수(`drop.win()`)는 즉시 판정에서도 돈다. 금이 적어 15% 가 0 이면(금 6 미만) 잃을 것도 없다. 위치를 모르면(월드 없음) 벌칙도 없다.
- **카드**: 패배 카드(토벌·성채·도적전·사당)에 "🎒 🪙 n 을(를) 떨어뜨렸다 — 10분 안에 이 근처에서 이기면 되찾는다 / 도감·인물·인연·비석은 그대로입니다", 승리 카드에 "🎒 떨어뜨린 짐을 되찾았다 — 🪙 +n". 결과 객체에 `drop`(패배)·`recovered`(승리)로 실어 카드가 그린다.
- **미니맵**: 살아 있는 짐이 주황 점(`drop`, 테두리에도 붙어 방향을 알린다). 오버월드는 안 고쳤다.
- **손잡이(`_admin.html`)**: `drop.pct`(0 이면 벌칙 꺼짐)·`drop.cap`·`drop.minutes`·`drop.radiusM`. 모듈이 매 호출 읽으므로 새로고침 없이 듣는다.
- **세이브**: `save.drops[{lat, lng, gold, at}]`(선택 칸, 읽기는 세이브를 안 만든다). `game.js` 루프의 `drop.tick` 이 5초마다 흩어진 것을 걷고 로그 한 줄. `sw.js` `go-v5.47.0`.

**확인**: `node -c` 통과. jsdom 러너 **559/561**(세 번 한 줄도 안 다름; 직전 552/554 에 새 진단 7건 — 15%·상한 300·금 0/4 는 잃을 것 없음 · 근처 승리로 돌아옴(제자리·100m 안 됨·500m 밖 안 됨·두 번은 없음) · 10분(9분엔 있고 10분 1ms 엔 없음, `drop.minutes` 손잡이) · 동시 셋(첫째가 흩어짐)·`drop.cap`·`drop.pct=0` · 토벌: 즉시 판정 패배는 그대로·손으로 진 판만 짐·손으로 이기면 돌아옴 · 사당 실패→클리어 회수 · 카드 문구·미니맵 마커·살피기 세이브 불변), 실패 2건은 HEAD 와 같은 jsdom 한계(`배우` 두 건 `Request is not defined`). 기존 진단 하나("사당 — 클리어하면 확정 보상")가 앞 진단이 남긴 짐을 되찾아 금이 늘며 깨졌는데 **기능이 아니라 진단의 복원 목록에 `drops` 가 빠진 탓** — `shrineKeep` 에 넣어 고쳤다. **화면은 못 봤다.** 직접 진 판(rogue·fort 의 `live` 갈래)을 굴리는 진단은 없다 — 토벌과 사당만 굴렸다(성채·도적전은 같은 한 줄 훅).

**같은 대상 재도전은 못 한다는 함정**: 토벌은 `tried`, 도적전·성채는 10분 쿨다운, 사당은 10분 봉쇄라 **진 그 대상을 곧바로 다시 쳐서 되찾을 수는 없다**(쿨다운이 짐 수명과 같다). 되찾으려면 근처의 다른 것을 이겨야 한다 — 성채 곁이면 토벌↔성채 공략 짝이 되고, 그 밖엔 근처에 이길 것이 없을 수 있다. PLAN 문구 "2분 안에 회수 루프" 는 이 자리에서 성립하지 않을 수 있다. 실기에서 순수 벌칙처럼 느껴지면 ① 회수 반경 확대 ② 짐 위에 서 있기만 해도(승리 없이) 회수 ③ 쿨다운을 짐 수명보다 짧게, 중 고르는 게 사용자 결정.

**실기 확인 대기**: 패배 카드 한 줄이 눈에 띄는지 · 미니맵 짐 점이 다른 점과 헷갈리지 않는지 · 15%(상한 300)가 걷는 캐주얼 게임에 벌칙으로 과한지(어드민 `drop.pct`) · 150m 안에 이길 것이 있는지 · 토스트가 다른 토스트에 묻히지 않는지.
**열린 것**: 일과 풀 `shrine` 켤지(사용자 결정) · 2D Phase 4 코드 그림 제거 · 실기 확인 결과 반영 · VRoid 캐릭터 추가.

## 2026-09-22 — 진단 실패 정리: 저스트 회피 시험은 인연(⑥) 기 배율 때문에 세이브 상태를 탔다

jsdom 559/561 ×3(남은 둘은 `Request is not defined` — jsdom 한계). `토벌 — 저스트 회피` 시험이 기 +30 을 못박았는데 실제로는 ⑥ 인연 결의 `kiPct`(`rogue-action.js` `kiMul`)가 곱해져 33 이 됐다 — 시험을 `30 × (1 + core.effect('kiPct')/100)` 기대치로 고침(코드 변경 없음). ⑥ 커밋 뒤부터 있던 시험 결함이다.

## 2026-09-22 — 실기 확인: 체크리스트 19항목 전부 통과

사용자가 실기기로 공통 5(캐릭터 이동 방향·사가블로 로딩 프레임·사가국지 인물 뒷모습·사가고 자동전투·목표판 숨김)·국지5·스토리4·사가고3·숲1·블로1 를 확인, 전부 정상. 실기 확인 대기였던 항목들 해소. 남은 것: 사용자 결정 3건(사가고 ⑧ 재도전 완화·일과 풀 `shrine` 켤지·2D Phase 4 코드 그림 제거) + 그래픽 작업(국지 Phase 4 톤매핑·병종 기둥, 스토리 Phase 5, 숲 마감 불꽃 파티클·현대 목적지 에셋) + VRoid 캐릭터 추가.

## 2026-09-22 (이어서) — 2D Phase 4: portrait()·portraitCard()·human()·beast() 되돌림 코드 그림 삭제

실기 확인 19항목 전부 통과 뒤 남은 사용자 결정 3건 중 2D Phase 4 를 사용자가 "포함하여 완전 새로 설계"로 골랐다. 들여다보니 `human()`/`beast()`(약 1100줄, 전용 헬퍼 `storyFace`·`limb`·`hand`·`foot`·`headgear`·`weapon`·`leg2`·`eye` 포함)는 죽은 코드가 아니라 세 자리에서 실제로 쓰이고 있었다 — ① 도깨비(ogre) 펫이 `beast()` 안에서 `human()`을 몸통으로 그대로 썼다(전용 그림 없음) ② 지도 스탬프 `bake()`가 Kenney 그림이 아직 안 실린 첫 프레임만 이걸로 메웠다 ③ 초상 `portrait()`/`portraitCard()`가 3D(portrait3d)가 준비 안 됐거나 그 인물을 못 구웠을 때 최종 대체로 썼다.

조사해 보니 ①은 이미 해소돼 있었다 — `BEAST_FORM_FILES.ogre = ['Orc','Demon','BlueDemon']`가 2026-09-11에 구워져 `assets/sprites2d/beast_{Orc,Demon,BlueDemon}.png` 로 이미 존재한다(README·ASSET_LICENSES.md 기록). 배경 생물(`animal.js`, deer·wolf·magpie·carp·ox 다섯)도 `BG_BEAST_FILE` 로 전부 채워져 있었다(2026-09-20 Phase 3). 즉 `beastImgOf()`/`humanImg()`는 실제로 쓰는 모든 대상에 대해 **항상** 유효한 이미지를 돌려준다 — 절차적 그림이 진짜로 필요한 순간은 "그림 파일이 아직 로드 중인 찰나"뿐이었다(관련 옛 주석 "그림이 없거나(ogre 형태)"·"배경 생물은 절차적 그림 그대로 둔다"는 둘 다 2026-09-11/20 이후로 낡아 있었다 — 이번에 고쳤다).

**한 일**:
- `bake()`(지도 스탬프): 이미지 미로드 시 절차적 그림 대신 `loadingMark()`(작은 머리원+몸통타원 자리표시, 새 함수) — 로컬 PNG 라 대개 한두 프레임 안에 실리고, `stamp()`의 `imgReady` 검사가 곧바로 다시 굽는다.
- `portrait()`/`portraitCard()`: hero/pet 분기를 `humanImg(humanIndexOf(ref))`/`beastImgOf(ref)` 로 그리도록 바꿈(기존 `storyize()` 종이·선화 후처리는 그대로 걸린다 — 그림 종류 상관없이 도는 공통 필터라 안 건드렸다). 실리기 전엔 `loadingMark()`.
- `human()`·`beast()`·전용 헬퍼 8개 삭제(node 스크립트로 두 구간 251-898/1003-1444 줄 삭제, CRLF 유지). export 목록에서 `human`·`beast` 제거. `lookOf()`는 안 지웠다 — `_expansion/battle-view.js`·`_test.html`이 여전히 부른다(그림엔 더는 안 쓰이지만 다른 소비자가 있어 범위 밖으로 남김).
- 낡은 주석 셋 정리(파일 머리말, ogre "그림 없음", 배경 생물 "절차적 그림 그대로").
- `sw.js` VERSION `go-v5.47.0`→`go-v5.48.0`(precheck 경고).

**진단**: jsdom 559/561(변경 전과 동일 카운트, 남은 둘은 `Request is not defined` — jsdom 한계, 이 변경과 무관). `node -c` 통과. precheck 통과(Godot 문서 이번엔 안 막았다).

**실기 확인 필요**: 도감·상세 화면 초상이 Kenney 그림으로 잘 보이는지(전엔 절차적 인물 그림), 지도 위 걷는 사람·짐승 첫 등장 때 자리표시가 눈에 띄게 어색하지 않은지, 3D(portrait3d) 실패 시 대체 그림.

**남은 것**: 나머지 네 판(사가블로·사가의숲·사가스토리·사가국지)은 Phase 4 미착수(사용자가 사가고만 콕 집어 결정한 것 — 다른 판은 각자 상황이 달라 손 안 댐). VRoid 캐릭터 추가.

## 2026-09-23 — 넷째 권역 서역(region-xy·genchar-xy): 절차적 인물·펫

"사가웹 이어해" 안에서, SAGA-HANDOFF "절차적 인물·펫 셋째 나라 — 사용자가 정한다" 항목을 사용자가 이미 고른 방향(중국 서역·실크로드, 자동 메모리)대로 진행. **중국(구주)은 이미 셋째로 들어가 있었다**(2026-09-11, SAGA-HANDOFF 표기가 낡았다) — 그래서 서역은 넷째.

**한 일**: `js/region-xy.js` — 한대 서역 아홉 나라(돈황·누란·차사·구자·우전·소륵·오손·대완·강거), 대표 좌표는 오늘날 둔황·롭노르·투르판·쿠처·호탄·카슈가르·이닝·페르가나·사마르칸트. `js/genchar-xy.js` — 성은 서역 사람을 한문으로 적던 한 글자 성(안·강·석·사·미·하·조·목·필·백·위지·배·차·누·지), 이름은 소리 옮김 한자 둘. id `xy_`·`ptgenxy_`, era `'서역'`, 등급 능력치 기준은 세 나라와 같다. **막는 짝 `BLOCK` 19** — 전수 634 짝을 훑어 실존 인물·신격(달마·라마·마라·마야·제바·비마·바수)·겨레 이름(돌궐)·우스운 한국어 낱말(수달·제비·비밀·바다…)이 되는 것을 둘째 음절을 밀어 피한다(진단이 첫 출력 "배 달마"를 보고 발견).

**배선**: 권역을 합치는 네 곳(`beacon.js`·`shrine.js`·`stela.js`·`world.js` genRegionAt)에 `RX`, 생성기 고르는 세 곳(`world.js`·`shrine.js` gencharOf, `letter.js`), `data.js` bio/find 훅(**다섯 벌 같이, md5 동일 432552f4…**), `index.html`·`_test.html`. 봉수대 27→36·사당 81→108·비문 27→36 줄(진단·주석·칭호 문구는 실제 개수로). 이미 칭호를 받은 세이브는 그대로(회수 안 함). `sw.js` 선캐시 목록에 **중국 권역 두 파일도 빠져 있어** 서역 둘과 함께 넣었다, go-v5.52.0.

**진단**: 새 2(대표 도시 다섯 → 제 권역 + 넷 병합에서 서역·시안→중국·서울→한국, 코드 겹침 0 / 결정적·도감 105 그대로·세 나라와 id 교차 없음·data.find/bio·전 권역×등급 이름 모양·BLOCK 없음·능력치 범위), 기존 3개 숫자 갱신. **jsdom 561/563 ×3 동일**(남은 둘은 기존 `Request` 없음 한계). data.js 가 바뀐 나머지 판도 한 번씩: 사가스토리 221/222·사가의숲 355/356·사가블로 365/365(남은 건 기존 jsdom 한계).

**참고(안 고침)**: 네 생성기 모두 **이웃 번호**끼리는 해시 하위 비트만 달라 이름이 자주 겹친다(번호 0~99 → 서로 다른 이름 52~55). 게임은 번호를 난수(`Math.random()*1e6`)·해시로 뽑아 실제로는 안 드러난다 — 진단의 비교 대상만 먼 번호로 잡았다. 실기 확인: 서역 권역은 실제로 그 땅을 걸어야 나오니 사실상 헤드리스·어드민으로만 볼 수 있다.

## 2026-09-23 — "원신급": VRoid 인물 셀 셰이딩 + 원신식 얼굴 그림자 + 림

VRoid 인물이 unlit(`MeshBasicMaterial`) 그대로라 **명암 없이 평면**이었다(`delam()` 은 Standard/Physical 만 본다). `asset3d.js` GLB 받는 자리에서 다섯 벌 공용 `vroidVariant.shade()` 를 먼저 태워 툰 + 얼굴 그림자(머리뼈 기준 두 톤) + 림으로 바꾼다 — 외곽선은 그 뒤 `delam()` 이 예전처럼 두른다. `toon3d.js` 에 `applyRimLight`(밝기 비례, 손잡이 `world3d.rim`)·`cloneMat`. 하늘은 `sky3d.js` 가 이미 있어 안 건드림. 상세·원리는 `saga-forest/HANDOFF.md` 같은 날 "원신급 다시" 절, 공통 함정은 `SAGA-HANDOFF.md` 함정 표(clone 이 셰이더를 떨굼·스킨 메시 배율 외곽선).
진단 jsdom 556/565 ×2 동일(새 2: shade·림), 실패 9 는 HEAD 와 같은 목록(Request 없음·캔버스 스텁). `sw.js` go-v5.53.0. **실기 확인**: 인물이 빛을 받게 돼 전보다 어두워 보일 수 있다, 얼굴 경계·림 세기. 끄는 손잡이 `world3d.faceShade`·`world3d.rim`.

## 2026-09-23 (이어서) — 스크린샷으로 다듬기: VRoid 전용 외곽선·피부 색조·초상 Neutral 0.8·인물 초상 다시 굽기

헤드리스 스크린샷으로 확인하며 고쳤다. 판 외곽선이 VRoid 의 알파 컷 머리·옷과 얼굴 부품에서 검은 덩어리를 만들어 `vroid-variant.js`(다섯 벌) 전용 외곽선(알파 존중·색 외곽선·피부·옷·머리만)으로 바꾸고 VRoid 메시는 판 외곽선이 건너뛴다. 얼굴 그늘 살구빛·그림자 맵 안 받음, 피부 색조. `portrait3d.js` 톤매핑 ACES 1.3 → Neutral 0.8, 인물 초상 전부 다시 구움(`tools/bake-portraits --kind=hero`). `applyTint` 는 외곽선 재질을 건너뛰고 `cloneMat` 으로(사가스토리·사가국지는 VRoid 면 옷만 물들임 — 예전엔 몸 전체가 세력 색 단색이었다). 사가국지는 VRoid 레시피의 `anim: 몸 파일` 을 빼 초상·일기토 T자세를 고쳤고(자체 몸짓 길), 초상 `settle` 이 `actions` 를 먼저 보며, VRoid 엔 투구를 안 씌운다. 상세·스크린샷 경위는 `saga-forest/HANDOFF.md` 같은 날 "(이어서) 스크린샷 찍으면서 다듬어줘" 절.

## 2026-09-23 (이어서 2) — 머리카락 하이라이트(천사 고리)·흉상 초상, 법선 없는 GLB 예방

"원신급" 남은 셋 중 다섯 판 공통 둘. `vroid-variant.js`(다섯 벌, md5 동일) `hairShadeMaterial` — VRoid `_HAIR` 재질에 화면 기준 법선의 위쪽 성분이 일정 범위(0.34~0.48)인 띠 × 카메라를 마주 보는 만큼 밝기 비례 광택(텍스처 밝기로 가장자리를 끊는다), 손잡이 `world3d.hairHi`(0 끔). `portrait3d.camPlan` 사람 초상을 흉상으로(span 0.62→0.37, look 0.78→0.86, 손잡이 `portrait3d.bust` 0 이면 예전) — 인물 초상 전부 다시 구움(210장, 실패 0). 스크린샷(스크래치 확인 페이지·초상 모음)으로 맞췄다. 진단: 머리 하이라이트 1 추가, 557/566, 실패 목록은 직전과 같다(전부 기존 jsdom 한계). `sw.js` go-v5.55.0. `delam` 에 법선 없는 GLB 면 `computeVertexNormals()` 한 줄(예방 — 사가국지 킷배싱 탑이 이것으로 새까맸다, `MeshToonMaterial` 은 `flatShading` 을 안 받는다). 상세·경위는 `saga-realm/HANDOFF.md` 같은 날 "(이어서 2)" 절.

## 2026-09-23 (이어서 3) — 짐승 GLB 외곽선 검은 파편 고침: 모델 안 한 폭 + 매끈한 외곽선 법선

커밋 `c301bd6e`(메시지 "11") 로 먼저 올라갔고 세션 기록이 끊겨, 이 절은 다음 세션이 커밋 차이로 되짚어 적었다.

- **증상**: 펫 초상·짐승 배우 몸 둘레에 검은 파편·톱니(펫 초상 스크린샷). 2026-09-20 펫 초상·사가블로 몬스터 스프라이트를 `--tune=world3d.outline:0` 로 끄고 구운 이유(2026-09-20 "(이어서 5)" 절 "함정 ②")가 이것이다.
- **뿌리**: `toon3d.outline()` 이 부품마다 제 반지름 × 3%(최소 0.006)로 밀었는데, 동물 GLB 는 부품 반지름이 0.002~0.03(뼈·메시 배율 100)이라 최소 0.006 이 부품의 20~300% 였다. 게다가 로우폴리 GLB 는 면마다 꼭짓점이 갈려 법선대로 밀면 면끼리 벌어졌다. 사가국지 "지오메트리 단위 부풂"(시야 거리 비례로 고침)과는 **다른 뿌리**.
- **고침**: `asset3d.js` `delam` — traverse 가 끝난 뒤 모아서 **모델 안 가장 큰 부품 반지름 × 2%** 한 폭, 그 12% 미만 부품(눈·이빨·발굽)은 안 두름(사가스토리 방식). `toon3d.outline(mesh, width)` 이 폭 인자를 받고(없으면 예전 규칙 — `actor3d` 도형 조립 배우용), `smoothOutlineNormals()`(사가스토리에서 옮김)로 같은 자리 꼭짓점 법선 평균 `outlineNormal` 을 따로 실어 셰이더가 그걸로 민다(본 몸 음영은 그대로). r169 에 없는 `skinning` 재질 속성(경고만 내던 것) 뺌.
- **진단**: 외곽선 항목 1 추가 → 565/567(실패 둘은 기존 jsdom `Request` 한계). 이어 받은 세션이 이 항목을 고침 — 재질 캐시 키가 폭을 소수 넷째 자리로 반올림해 앞 항목이 만든 0.006022 재질을 이어 받아 정확 비교(1e-9)에서 떨어졌다 → 허용치 6e-5. `sw.js` go-v5.56.0.
- **남은 것**: 펫 초상은 아직 **외곽선 끈 채로 구운 그대로**다. 고쳐졌으니 외곽선 켜고 다시 구울지(`tools/bake-portraits/bake.mjs`, 헤드리스 크롬) 결정·실기 확인 전.

## 2026-09-23 (이어서 4) — 펫 초상 외곽선 켜고 다시 굽기: 종마다 제 모델·상자 맞춤 구도·외곽선 스키닝 순서

사용자 "알아서 이어해" → 위 절 남은 것(외곽선 끈 채 구운 펫 초상)부터. 굽기 결과를 스크래치에서 모음 그림으로 보며 맞췄다.

- **외곽선**: 외곽선 셰이더를 **스키닝 전**(`begin_vertex` 직후, `outlineNormal`)에 밀도록 옮김 — 폭은 지오메트리 반지름으로 재는데 스키닝 뒤(메시 지역 공간)에 밀어, 역바인드 행렬이 배율을 품은 양자화 GLB(Koi)에서 외곽선이 몸의 몇 배가 됐다(사가블로 잉어 초상이 검은 덩어리). 사가고·사가블로·사가스토리·사가의숲 `toon3d.js` 같은 수정, 사가국지는 뷰 공간 방식이라 무관.
- **종마다 제 모델**: 도감 펫 105종 중 64종이 `sprite.js` BEAST_FORM 에 없어 `quad` 로 떨어져 네발 열 벌 중 id 해시로 하나를 입었다(고래=사슴·흰동가리=소·호랑이=사슴·황소=여우). 사가블로가 2026-09-07 받아 둔 Quaternius CC0 를 복사(`models/animals/` 범·곰·판다·원숭이·멧돼지·부엉이·고양이·두루미 8, `models/animals_extra/` 11, `models/animals_extra2/` 38 — 2.8MB, sw 는 모델을 미리 받지 않음) → `asset3d.js` DEFAULTS `pet:pt_*` 77줄(이미 있던 여우·황소·공룡 등 이어 붙임). 신수 11·포켓몬 오마주 16·까치(사용자가 정한 비둘기)는 형태 표 그대로. BEAST_FORM 에 물고기 42·말 5·공룡 6 형태(여덟 형태 그대로 — 2D 도형·폴백도 맞게). 출처 `assets/ASSET_LICENSES.md` 새 절.
- **펫 구도**: `portrait3d.petFrame` — 자세 굴린 뒤 스킨 반영 몸 상자를 재 바라보는 높이(바닥 + 키 28%)·위 끝·가로가 다 들어오게 물러남(네발은 예전과 거의 같은 거리). 물고기 형태는 `petYaw` 로 긴 축이 옆(yaw 1.3)에 오게 — 네발 yaw 0.95 면 3/4 정면이라 얼굴·눈만 찍혔다. 긴 몸이 멀리 물러나 카메라 far(40)를 넘어 빈 카드가 되던 것 → far = max(40, dist × 3). 손잡이 `portrait3d.petFit`(0 이면 예전 고정 구도).
- 펫 초상 210장 다시 구움(외곽선 켬, 실패 0). 진단: "종마다 제 모델" 1 추가 → 566/568(실패 둘은 기존 jsdom `Request` 한계). `sw.js` go-v5.57.0.
- **남은 것**: 긴 몸(장룡·쥐가오리·황새치)은 카드에 작게 선다(상자 맞춤의 대가). 갑옷메기는 모델 자체가 거의 검다. 실기 확인 전.

## 2026-09-23 (이어서 5) — 지도 짐승 그림도 펫마다 제 모델, 옆 3/4 구도로 85장 다시 굽기

사용자 "이어해줘". (이어서 4)로 3D 초상은 종마다 제 모델이 됐는데 지도 스탬프(`sprite.beastImgOf`)는 여전히 형태 해시로 네발 열 벌 중 하나를 골라 초상(고래)과 지도(사슴)가 달랐다.

- `sprite.js` `PET_BEAST_FILE`(77줄, asset3d `pet:pt_*` 와 같은 모델) — 있으면 형태 해시보다 먼저. `_x2` 는 animals_extra2 의 같은 이름(Koi·Shark) 모델.
- 기존 아이콘은 `_bake_one.html` 기본 구도(정면·fit 1.6)라 얼굴만 크게 위아래가 잘려 있었다(사슴 다리) → `view=pet` 새 구도(쉬는 자세·긴 축 옆으로·스킨 반영 상자 투영으로 정사각 직교 프레임에 몸 전체·발 바닥, 물고기 yaw 1.4). meshopt 디코더도 붙임. 기존 28 + 새 57 = 85장 굽고(85/85) 모음 그림으로 확인.
- 러너를 `tools/bake-icons/bake-beasts.mjs` 로 저장소에 둠(키는 sprite.js 표에서 뽑음, 다시 구워 바이트 같음 확인). README·`ASSET_LICENSES.md` 새 절.
- 진단: "제 모델 펫은 지도 그림도 그 모델" 1 추가 → 567/569(실패 둘은 기존 jsdom `Request` 한계). `sw.js` go-v5.58.0.
- **남은 것**: 몸 전체가 들어오니 예전(얼굴 클로즈업)보다 지도에서 작게 보일 수 있다 — 특히 장룡·고래 같은 긴 몸. 실기 확인 전(작으면 `sprite.js` 짐승 그림 상자 `H * 1.5` 를 키울 자리).

## 2026-09-23 (이어서 6) — 역참 지도 마커 `tower_ruin` 굽기 풀림

SAGA-HANDOFF 열린 항목 "tower_ruin.glb(역참) 아이콘 굽기 — 여섯 번 넘게 실패". (이어서 5)의 러너 방식(잡마다 새로 연 페이지)으로 `_bake_one.html?key=tower_ruin&group=building&fit=1.1` 을 열자 첫 시도에 구워졌다(원인은 여전히 모름 — 예전 막힘이 간헐적이었다는 기록과 맞음, meshopt 는 아니고 webp 텍스처 모델). fit 0.7 은 작고 1.4 는 위가 잘려 1.1. `world.js` `drawStation` 이 `building_Inn.png`(옛 여관, 되돌림 자리로 남김) 대신 `building_tower_ruin.png` — 3D 역참과 같은 모델. README·`ASSET_LICENSES.md`·sprite.js 주석 갱신, SAGA-HANDOFF 열린 항목에서 지움. 진단 567/569 그대로. `sw.js` go-v5.59.0. 실기 확인 전(폐허 탑은 여관보다 어둡고 회색이라 지도에서 덜 띌 수 있다 — 위에 등롱·깃발은 그대로 뜬다).

## 2026-09-23 — 손으로 놓은 소품 층(`land.js` `deco`) — 맵 편집기 "3D 배치"용

- `land.js`: 땅 객체 `deco: []`(월드 미터 x·z, `t`·`h`·`rot`, 집·탑 `w`·`d`·`shade`) + `DECO_T` 10종 · `decoAt(tx,ty)`(칸 가운데 기준, `di` 번호 붙임) · `hasDeco` · 손잡이 `land.deco` · `validate()` 가 deco 도 본다.
- `world3d.js`: `propPlan` 끝에서 `decoAt` 를 얹는다(모델·LOD·등불·`houseRects` 벽 — 벽은 `world.js` 가 마을 칸만 잰다). `syncProps` 는 소품 있는 먼 풀밭·길 칸도 세운다.
- 배열이 비어 있어 화면은 그대로. 진단 "땅 — 손으로 놓은 소품(deco)…" 추가 → jsdom 569/569(세 번 동일). `sw.js` go-v5.58.0.
- 채우는 법: `saga-web/tools/map-editor/` "3D 배치"(README). 실기 확인 전.

## 2026-09-23 (이어서 7) — 마을 칸 밖 deco 집도 벽 · 들판의 안 보이는 집 없앰

- `world3d.houseRects(gx,gy)` 가 칸 종류를 안 보고 늘 `propPlan('town', …)` 을 뽑았다. 그래서
  ① `world.js` 는 마을 칸만 골라 불러 **마을 칸 밖에 놓은 deco 집·탑·우물·장터는 뚫고 지나갔고**,
  ② 칸을 안 가리고 9칸을 부르는 `duelSpotBlocked` 는 들판에서도 **안 보이는 해시 집**을 피해 교전 상대를 밀어냈다.
- 고침: `houseRects` 가 `DG.world.terrainAt` 로 칸을 보고, 마을이면 여태대로 마을 계획, 아니면 그 칸 `land.decoAt` 만.
  `world.js solidRectsNear` 의 마을 칸 거름은 뺐다(들판 칸은 deco 가 없으면 빈 배열이라 값이 싸다).
- 3D 배치 편집기(`tools/map-editor/scene.js`)의 "마을 칸이 아니라 부딪히지 않는다" 경고·회색 선을 걷었다(이제 늘 주황).
- 진단 "땅 — 마을 칸 밖에 놓은 deco 집도 벽이고, 들판엔 안 보이는 집이 없다" 추가: 이웃까지 마을 아닌 풀밭 칸의 벽 0 → 집 놓으면 1,
  스틱으로 동쪽으로 걸으면 집 앞(-4.8m)에서 멈춤. jsdom 563/571 세 번 동일 — 실패 8건은 HEAD 사본에서도 같은 목록(캔버스 흉내·`Request` 없음).
- deco 배열은 아직 비어 있어 게임 화면·판정은 그대로(교전 상대 자리만 들판에서 덜 밀린다). `sw.js` go-v5.61.0. 실기 확인 전.

## 2026-09-24 — 원신식 들판 전투(PLAN §5 ⑨) — 무리·편성 4 교체·원소 반응·방패

- 계기: 사용자 "사가고를 원신 스타일로 전투… 재미가 없네 그리고 맵형태도" → 추천안 둘(들판 전투·지역 지도)을 골랐다. 이번 절은 전투, 지도(⑩)는 다음.
- 새 `js/field-combat.js`: 160m 격자 해시 무리(보통 6꼴·정예 방패 3·우두머리), 편성 4(동행 앞 넷, 원소 id 해시), 기본 3타·스킬·폭발·회피·교체, 반응 셋·원소 방패, 전멸 30% 부활 + ⑧ 짐 떨굼. 판정 층은 순수 함수, 런타임은 `game.js` 루프 `tick(dt)`.
- `world3d.js`: 적을 도감 펫 모델 배우(`'fc'+uid`)로 세운다 · 교체한 사람이 지도 위 아바타(`dropActorIfNot` — actorOf 가 key 로만 캐시해서 옛 몸이 남던 것) · 동행 곁 배우는 활성 인물을 건너뜀 · `groundY` 노출 · `playAnim` 이 아무 배우 키나 받음.
- 예고 원·광역 원은 `three()`·`addFx` 로 땅에 붙인 링, 숫자·체력바는 카메라 투영 DOM(`#field-hud`). 싸울 때만 뜨고 그동안 독·근처 줄은 숨는다.
- 함정: 전멸시킨 한 대의 `wind` 끝 처리가 `downMember` 가 돌려 둔 `'return'` 을 `'recover'` 로 덮던 것 — 진단을 쓰다 잡았다(상태가 아직 `wind` 일 때만 덮는다).
- `sw.js` SHELL 에 `rogue-action.js` 가 빠져 있었다(오프라인이면 옛 카드 전투로 떨어짐) — `field-combat.js` 와 같이 넣음, go-v5.62.0.
- 자동 전투(🤖)·자동 순행 중에는 폭발·스킬·공격을 대신 누른다(자동 순행 중 무방비 전멸 → 금 떨굼 반복을 막으려고).
- 진단 15(판정 14 + 런타임 1, 세이브 백업·복원 try/finally) → jsdom 584/586 세 번 동일(남은 둘은 jsdom `Request` 한계, 전부터). **실기 확인 대기**: 손맛·난이도·HUD 배치(우상단 편성이 도구줄과 겹치는지)·적 모델 크기·예고 원이 언덕에 묻히는지.

## 2026-09-24 (이어서) — 원신식 지역 지도(PLAN §5 ⑩) — 바이옴·랜드마크·순간이동

- 새 `js/biome.js`: 1.2km 지터 격자 보로노이 지역. 원점 지역은 **고향 들녘**(옛 지형 비율 그대로), 그 밖은 푸른 벌판·대숲 골짜기·붉은 협곡·안개 늪·옛 성터 고원. 이름은 칸 해시로 앞말 + 바이옴(예: 흰구름 벌판).
- `world.js` `terrainAt` 노이즈 길의 문턱을 `bandFor(tx,ty)`(칸 캐시)로 — 손그림 땅·OSM 이 먼저 답하는 순서는 그대로. `nearTown` 도 같은 문턱. 랜드마크 34m 둘레는 들.
- `relief3d.js` — 자연 지형만 바이옴 기복 배수(협곡 1.9·늪 0.5). 처음엔 마을·길까지 곱해 "사람이 만든 자리는 평평하다" 진단이 깼다.
- 함정: 경계에서 가장 가까운 둘만 섞으면 세 지역이 만나는 자리에서 "둘째"가 바뀌어 문턱이 튄다 → 아홉 지역 거리 가중(smoothstep, 폭 260m).
- `field-combat.js` `campAt(…, bfn)` — 바이옴이 무리 꼴·정예·우두머리 확률을 고른다(인자 없으면 옛 그대로).
- `world3d.js` 랜드마크 배우(`asset3d` `landmark:<바이옴>` 키, heightMul 5) · `biome.js` 가 안개를 뚫는 빛기둥(`fog:false`) · `overworld.js` 순간이동 지점 단추(키보드 모드만) + 지도 표식 · 2D 탑 그림 · 지역 이름 띠 `#region-banner`.
- 진단 7 → jsdom 591/593 세 번 동일(남은 둘은 jsdom `Request` 한계). `sw.js` go-v5.63.0. **실기 확인 대기**.

## 2026-09-24 — 원신식 ⑪ 지역 수호자(PLAN §5 ⑪)

- 계기: 사용자 "사가웹 이어해" — 사가국지 §5-10 다섯 단계를 마친 뒤 SAGA-HANDOFF 열린 목록 첫 판(사가고 ⑪~⑬)으로. ⑨⑩ 실기 확인 결과는 아직 없다(몰아서 확인 — 그대로 진행).
- `field-combat.js` — FOES 에 수호자 다섯(`g_<바이옴>`, `guard:true`·`shields:[겉,속]`), `guardianAt(cell)`(순수, key 'g:<지역키>'·등급+1), 방패 겹 `breakShield`(겉 → 속 방패 차오름·0.8초 / 마지막 3초 / 한 겹 정예는 예전 2초), `populate(…, lfn)`(tick 이 `biome.landmarks` 를 넘긴다, 손잡이 `field.guards`), 되돌아가면 겹도 처음으로. 보상은 `clear` 중 kind 'guard' 갈래 — `save.field.guards` 에 적고 `respawnSweep` 은 'g:' 를 건너뛴다(영구).
- HUD — 이름 뒤 남은 겹 그림(🛡️🔥⚡), 겉이 깨질 때 떠오르는 글 "겉 방패 깨짐! ⚡ 속 방패" + 알림 "⚡ 방패 — 🔥 원소 동행으로 바꿔라".
- 진단 1 추가 → jsdom 592/594 두 번 동일(남은 둘은 jsdom `Request` 한계). `sw.js` go-v5.64.0. **실기 확인 대기**(크기·탑과 겹침·알림·난이도).

## 2026-09-24 (이어서) — 원신식 ⑫ 인물마다 다른 스킬 모양(PLAN §5 ⑫)

- 사용자 "이어해" — ⑪ 커밋(`7a4199b5`) 뒤.
- `field-combat.js` — `shapeOf(id)`(원소와 다른 해시 `id#shape`, '_me' 는 원형), 멤버에 `shape`. `skill()` 이 모양별로 갈린다: 찌르기(선분 거리 `segDist`)·돌진(`S.dash` 재사용·무적 0.3)·장판/소환은 `S.zones` 에 얹고 `stepZones` 가 틱마다 친다(놓자마자 첫 틱, 경계는 `z.t > 1e-9` — 안 그러면 5초 장판이 여섯 번 친다). 적이 없을 때 겨누는 쪽은 마지막 회피 방향(`S.lastDx/Dy`).
- 그림 — 선 모양은 길을 따라 원 넷, 장판은 틱마다 원, 소환은 정령 자리·맞은 자리 원(정령 몸은 아직 없다). 스킬 단추 "🔥 찌르기".
- 옛 원형 ×2.2 를 전제로 한 진단 셋(증발·폭발 기력)은 멤버 모양을 'circle' 로 박았다.
- 진단 1 추가 → jsdom 593/595 두 번 동일(둘은 jsdom `Request` 한계). `sw.js` go-v5.65.0. **실기 확인 대기**.
- 다음: ⑬ 지역 사명 사슬(지역마다 3단, 랜드마크로 이어짐).

## 2026-09-24 (이어서 2) — 원신식 ⑬ 지역 사명 사슬(PLAN §5 ⑬)

- ⑫ 커밋(`a882b145`) 뒤 바로.
- `biome.js` — `missionView(st)`(순수, 세 단·앞에서부터 차례), `missionState(key)`(save.regions·save.missions·save.field.guards 에서 읽음), `progressMission(key)`(넘어간 단 보상 한 번 — `paid`), 구독은 `tick` 첫 호출의 `subscribe()`(field:clear → 그 무리 자리의 지역 셈 +1, field:guard·region:found → 진행). 위쪽 가운데 `#region-mission` 한 줄.
- 진단 1 추가(세이브를 잠깐 비웠다 `finally` 로 되돌린다 — 메모리 "검증 스크립트 세이브 복원 순서" 대로) → jsdom 594/596 두 번 동일(둘은 `Request` 한계). `sw.js` go-v5.66.0. **실기 확인 대기**(한 줄 자리).
- 원신식 다음 후보 넷 중 셋(⑪⑫⑬) 끝. 남은 것: 지도 위 동행 모션(교체할 때 곁에서 걸어 나오기).

## 2026-09-24 (이어서 3) — 지도 위 동행 모션 ⑭ 교체 연출(PLAN §5 ⑭)

- 사용자 "이어해" — ⑬ 커밋(`4531c864`) 뒤.
- `world3d.js` — `lastLead`·`swapFx`. 선두가 바뀌면 `actors.me` 를 `actors.swapout` 으로 옮기고(몸을 다시 짓지 않는다) 0.6초 동안 옆뒤로 2.4m 걸어 물러나게, 0.9초 뒤엔 안 먹여서 `sweepActors` 가 떠오르며 흩어 지운다. 새 몸은 옆 1.6m 에서 0.35초 ease-out 으로 들어선다. 순수 함수 `swapMotion(ms)`.
- 진단 1 추가 → jsdom 595/597 두 번 동일(둘은 `Request` 한계). `sw.js` go-v5.67.0. **실기 확인 대기**.
- 이로써 §8 "원신식 다음(후보)" 넷(⑪⑫⑬⑭) 모두 코드분 끝.

## 2026-09-24 (이어서 4) — 고정 특색 지역 ⑮(PLAN §5 ⑮) — 해시 바이옴 대신 이름 있는 땅 열여섯

- 계기: 사용자 "사가고 플랜 추가 — 전체 지역을 랜덤이 아닌 사가블로처럼 각각 특색 있는 지역으로" + "모든 프로젝트 적용"(SAGA-DESIGN §12).
- `biome.js` — `cellAt` 이 `PICK`·`PREFIX` 해시 대신 `zoneOfCell(i,j)`(방위 여덟 × 고리 둘, 경계 흔들림은 칸 좌표 해시)로 바이옴·이름을 받는다. `cell.zone` 추가, 발견·사명·수호자 키는 칸 키 그대로라 세이브 호환.
- 이름 띠 — 칸이 바뀌면 "땅 · 칸 이름", 땅이 바뀌면 큰 띠(이모지·이름·한자·사연). `zone:enter`.
- 기존 지역 진단 9 그대로 통과(안쪽 여덟 땅이 다섯 바이옴을 다 가진다). 새 진단 3. jsdom 598/600 세 번 동일(2 는 늘 있던 `Request` 한계). `sw.js` go-v5.68.0. **실기 확인 대기**.
- 함정: `core.on` 은 떼는 함수가 없다 — 진단에서 듣기 깃발로 막았다.
