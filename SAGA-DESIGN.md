# SAGA 개편 설계 — 다섯 판·두 3D 트랙 공통 (2026-09-16)

**지위**: 일곱 `PLAN.md`(웹 다섯 + saga-godot + saga-unity)가 공통으로 가리키는 상위 문서다.
재미·그래픽·에셋·버그·문서 규칙의 **공통분모**만 여기 두고, 판별 세부는 각 PLAN 이 든다.
여기에 이력·날짜 세션 기록을 쓰지 않는다(그건 `SAGA-HANDOFF.md`·각 판 `HANDOFF.md`).

**읽는 법**: 40KB 안쪽으로 유지한다. 필요한 절만 `grep -n "^## "` 로 찾아 `sed -n` 으로 읽는다.
§9(열린 질문)는 사용자 결정이 나면 그 답을 해당 PLAN 으로 내려보내고 여기서 지운다.

목차: §1 진단 · §2 방향 · §3 재미 표준 8 · §4 참고 게임 카탈로그 · §5 판별 처방 · §6 그래픽 · §7 에셋 창조 · §8 버그 · §9 문서·토큰 · §10 열린 질문

---

## 1. 왜 재미가 없나 — 공통 진단

일곱 문서(1.3MB)를 목차·상태 절 중심으로 훑은 결과다. 판별 세부 근거는 각 PLAN §4 에 있다.

| # | 증상 | 근거(문서에서 확인) | 결과 |
|---|---|---|---|
| 1 | **폭은 넓고 루프는 얕다** | 인물 105·펫 64·몬스터 95·무예 80·성 58·문항 260. 반면 "지금 뭘 하면 되나"를 화면에 늘 보여 주는 목표판·세션 마무리가 어느 판에도 없다 | 콘텐츠는 많은데 5분 놀면 할 일이 안 보인다 |
| 2 | **손맛이 1차에 멈춤** | 사가블로만 hitstop·콤보(2026-09-10 "전투 손맛 1차"). 나머지는 타격 반응·소리·화면 흔들림 표준이 없다 | 때려도 맞은 느낌이 없다 |
| 3 | **선택에 무게가 없다** | 전직·스킬 갈래를 "더 추가해" 로 기계적으로 늘렸다(사가블로 갈래 8, 사가스토리 무예 80). 갈래 간 트레이드오프·희소성이 없다 | 뭘 골라도 같다 |
| 4 | **실패가 없다** | 어느 판도 죽음·패배의 비용과 회복 루프가 설계돼 있지 않다 | 긴장이 없다 |
| 5 | **발견 밀도가 낮다** | 오픈월드로 넓혔지만(사가블로 A안, 사가고 BOTW 절, 사가의숲 "크기에 비해 빈다") 30초마다 뭔가 나오는 배치 규칙이 없다 | 걷기만 한다 |
| 6 | **검증 안 된 기능이 쌓임** | 다섯 판 모두 "실기기 확인 대기" 목록이 세션마다 길어졌다 | 버그가 많다고 느낀다 |
| 7 | **그래픽 방향이 없다** | Kenney·Quaternius·KayKit·VRoid 가 한 화면에 섞인다. 아트 바이블(스타일·팔레트·스케일·빛 방향)이 없다 | 허접해 보인다 |
| 8 | **문서가 설계를 덮음** | PLAN 이 세션 기록으로 불어(사가블로 240KB) 설계 결정을 찾을 수 없다. 새 세션이 설계를 못 보니 같은 실수를 반복한다 | 토큰 낭비 + 방향 상실 |

**한 줄 결론**: 콘텐츠를 더 붓는 게 아니라 **루프(목표→행동→보상→선택)** 와 **감각(손맛·그래픽 통일)** 을 먼저 세운다.

## 2. 개편 방향 (제안 — §10 에서 사용자 확정)

1. **웹 다섯 판 = 기획 검증 샌드박스.** 사용자가 "여기서 접었다"고 했으니 기능 확장을 멈추고
   (a) 재미 표준 8(§3)을 가장 싸게 실험하는 자리, (b) 버그 스프린트(§8)로 안정화, (c) 그래픽은 §6.1 의 "저비용 통일" 까지만.
   여기서 검증된 루프를 3D 트랙이 가져간다. **웹에서 통하지 않은 시스템은 3D 로 옮기지 않는다.**
2. **3D 두 트랙은 병행 유지**(사용자 결정). 다만 §10-Q1 처럼 "완성판" 하나를 정하지 않으면 그래픽·에셋 투자가 둘로 갈려 둘 다 허접해진다.
3. **다섯 판 공통 뼈대 = 인물(HEROES 105).** 다섯 판이 같은 인물을 다른 장르로 쓴다는 것이 이 시리즈의 유일한 정체성이다.
   판마다 "이 인물이 이 판에서 무엇이 되나"(사가고=동료, 사가블로=서명 무예, 사가의숲=주민, 사가스토리=직업 스승, 사가국지=장수)를 §5 처방의 축으로 삼는다.
4. **퓨전 방향(2026-09-10, `SAGA-HANDOFF.md` "창작 방향")은 유지**하되, 새 시대 인물·건물을 넣기 전에 §3 루프를 먼저 세운다. 퓨전은 루프 위에 얹는 살이다.

## 3. 재미 표준 8 — 모든 판이 갖춘다

각 PLAN §5 의 후보는 이 8개 중 무엇을 채우는지 반드시 표기한다. 수치는 기본값이고 판마다 조정한다.

| # | 표준 | 규격(기본값) | 진단(`_test.html`/에디터 테스트) |
|---|---|---|---|
| A | **목표판** | 화면에 항상 3줄: 지금(≤30초)·이번 세션(≤10분)·이번 주. 완료 즉시 다음 줄이 채워진다 | 부팅 직후 목표 3줄이 비어 있지 않다 |
| B | **세션 마무리 카드** | 종료·귀환 시 카드 1장: 얻은 것·도감 진척·다음에 할 것 1개. 5초 안에 닫힌다 | 카드 데이터에 next 필드가 있다 |
| C | **손맛 표준** | 입력→반응 ≤100ms · hitstop 60~90ms(치명 120) · 화면 흔들림 4px/120ms · 피격 플래시 80ms · 숫자 팝 0.6s · 타격음 3종 라운드로빈 | 타격 이벤트가 5요소를 모두 발생시킨다 |
| D | **선택 3택** | 성장 선택은 3개 중 1개, 서로 다른 축(공격/방어/유틸). 같은 축 둘이 나오지 않는다. 거절 시 소량 보상 | 100회 굴려 같은 축 중복 0 |
| E | **발견 밀도** | 이동 30초(≈60m)마다 상호작용 1개 이상. 종류 6 이상(상자·조우·수집·비밀·이벤트·NPC) 주기적 재배치 | 맵 스캔 시 60m 격자 빈칸 ≤10% |
| F | **실패와 회복** | 죽음/패배 비용 = 진행의 10~20%(소지 재화·한 칸 후퇴). 회복 루프 ≤2분. 죽어도 남는 것(도감·인물·기억) 명시 | 사망→재도전까지 조작 3회 이하 |
| G | **성장 가시화** | 힘의 변화가 숫자 말고 **보이는 것**(외형·이펙트·지형 반응)으로 1단계마다 1개 | 등급별 시각 차이 스냅샷 존재 |
| H | **돌아올 이유** | 일일 1개(3분)·주간 1개(20분)·계절/월간 1개. 놓쳐도 벌칙 없음, 보상은 도감·외형 | 날짜 바꿔 부팅 시 일일 목표 갱신 |

## 4. 참고 게임 메커니즘 카탈로그

에셋·이름·데이터는 가져오지 않는다. **문법만** 가져온다(루트 CLAUDE.md). 각 항목은 어느 표준(§3)을 채우는지, 어느 판에 맞는지 적었다.

| 참고 게임 | 메커니즘 | §3 | 맞는 판 |
|---|---|---|---|
| 젤다 BOTW | 탑 오르면 지도 해제·랜드마크 시야 유도·신전(3분 방)·900 수집품(코로그) | A E G | 사가고·사가의숲 |
| 몬스터헌터 나우 | 75초 실시간 전투·부위 파괴·저스트 회피·소재→장비 트리 | C F G | 사가고·사가블로 |
| 포켓몬GO | 일일/주간 리서치·협동 레이드·커뮤니티 데이·알 부화(걸음) | A H | 사가고 |
| 원신 | 일일 위탁·탐험도 %·상자 등급·원소 반응(조합 시너지)·나선비경(주간 도전) | A E H D | 사가고·사가스토리 |
| 하데스 | 축복 3택 조합(빌드)·죽음이 서사를 진행·열기(난이도 계약)·관계도 | D F G | 사가블로·사가스토리 |
| 슬레이 더 스파이어 | 경로 선택 지도(전투/상점/휴식/이벤트)·유물 | D E | 사가블로 던전·사가스토리 미니던전 |
| 뱀파이어 서바이버 | 15~30분 폭발 성장·레벨업 3택·생존 타이머 | C D G | 사가블로 "난입" 모드 |
| 디아블로 2/4 | 어픽스 전설·룬워드·난이도 계단·나이트메어 티어·월드 보스 시간표 | G F H | 사가블로 |
| 패스 오브 엑자일 | 스킬+서포트 조합·맵 변형자 | D | 사가블로 |
| 스타듀밸리 | 하루/계절·관계 하트·번들(수집→세계 변화)·축제 | A H G | 사가의숲 |
| 동물의숲 | 박물관·일간 방문객·섬 평가·시간 실동기 | G H | 사가의숲 |
| 메이플스토리 | 주간 보스·파티 퀘스트·직업 정체성(고유 조작)·전직 이벤트·미니게임 | H D G | 사가스토리 |
| 할로우 나이트 / 데드셀 | 이동 손맛(대시·벽)·지도 조각·숏컷 해제 | C E | 사가스토리 |
| 코에이 삼국지 | 인물 관계·의리·일기토/설전·시작 시나리오 선택·역사 IF | D G | 사가국지 |
| 크루세이더 킹즈 3 | 인물 특성·야망·계승·왕조 서사(이벤트 체인) | A F | 사가국지 |
| 문명 | 승리 조건 다중·불가사의 경쟁·시대 전환 | A G | 사가국지 |
| 파이어 엠블렘 | 지형 전술·지원 대화·영구 이탈 | D F | 사가국지 전투 |
| 다크소울 | 재화 회수(죽은 자리)·보스 앞 안전지대·숏컷 | F | 사가블로·사가스토리 |
| 마리오 오디세이 | 발견 보상 30초 규칙·수집품 힌트 판매 | E | 사가고·사가의숲 |
| 로그라이트 공통 | 메타 진행(죽어도 남는 해제) | F G | 다섯 판 |

## 5. 판별 처방 — 한 줄씩 (세부는 각 PLAN §5)

각 PLAN §5 는 아래를 출발점으로 **6~8개 후보를 상세 설계**한다(§9.3 의 9필드 필수).

| 판 | 인물이 되는 것 | 핵심 루프 처방 | 우선 참고 |
|---|---|---|---|
| 사가고 | 동료(등용) | 탑→지도 해제→랜드마크 신전(3분)→인물 조우. 75초 실시간 토벌. 일일 리서치·걸음 알 | BOTW·몬헌 나우·포켓몬GO |
| 사가블로 | 서명 무예 | 던전 진입마다 축복 3택(인물 무예 조합)·죽음 비용·나이트메어 티어·월드 보스 시간표. 갈래 8 을 "조합 시너지" 로 재해석 | 하데스·D4·PoE |
| 사가의숲 | 주민(관계) | 하루 사이클에 번들(수집→마을 변화)·관계 하트·축제·택배 아르바이트(다른 마을 연결) | 스타듀·동물의숲·BOTW |
| 사가스토리 | 직업 스승 | 직업 정체성(직업별 고유 조작 1개)·주간 보스·경로 선택 미니던전·이동 손맛 | 메이플·하데스·할로우 나이트 |
| 사가국지 | 장수 | 인물 야망·관계 이벤트 체인·일기토/설전 미니게임·시작 시나리오 3종·승리 조건 다중 | CK3·코에이·문명 |

**3D 트랙(godot·unity)**: 위 처방을 새로 설계하지 않는다. 웹 PLAN §5 에서 **웹에서 검증된 것부터** 순서대로 옮기고, 엔진 고유 장점(물리·조명·애니 블렌딩)으로 손맛(C)·가시화(G)를 웹보다 한 단 올린다.

## 6. 그래픽 대책

### 6.0 아트 바이블(다섯 판·두 트랙 공통, 판별 팔레트만 다르다)
1. **트랙마다 한 스타일**: 웹·saga-godot 은 스타일라이즈드 로우폴리 + 3단 툰 램프 + 얇은 외곽선(0.8~1.2px). saga-unity 는 사용자 지시(2026-09-13, 그 PLAN 66-2)대로 **사실적 PBR** — 어느 쪽이든 한 화면에 두 스타일을 섞지 않는다(VRoid 하이폴을 툰 판에, Kenney 로우폴리를 PBR 판에 섞지 않는다).
2. **팔레트**: 바이옴당 24색(기본 8 + 명·암 2단). 모든 에셋은 임포트 시 팔레트에 스냅(§7.2 스크립트).
3. **스케일**: 사람 1.7m 기준, 문 2.2m, 층 3m. 에셋 임포트 시 자동 리스케일.
4. **빛**: 키 라이트 방향 고정(NW 45°·고도 55°), 림 라이트 1, 앰비언트는 스카이 색. 판별로 안 바꾼다.
5. **카메라**: 판별 1개(FOV·거리·기울기)로 고정. 줌 범위 2단. 흔들림은 §3-C 만.
6. **실루엣 우선**: 캐릭터·몬스터는 128px 축소에서도 구별돼야 한다. 안 되면 형태를 바꾼다.

### 6.1 웹(three.js r169) — "저비용 통일" 까지만
- 렌더 파이프라인 하나로: `post3d.js`·`ssao3d.js`(사가고) 를 다섯 판 공통 규격으로 정리 → 톤매핑 **`NeutralToneMapping`**(Khronos PBR Neutral, ACESFilmic 아님 — 사가고·사가블로·사가스토리 셋 다 ACES 로 걸었다가 "밤 화면이 망가진다"로 물러섰다, 손으로 맞춘 화면색에 ACES 를 걸면 대비가 뒤집힌다. 이 판들 `post3d.js` 머리글 참고)·노출 1.0·SSAO 반경 0.6·블룸 임계 0.9·비네트 0.25·안개(색=하늘 지평선색).
- 재질 통일: `MeshToonMaterial` + 그라디언트 맵 3단 + 외곽선(뒤집힌 헐, 스케일 1.03). 팔레트 스냅은 텍스처 로드 후 캔버스에서 1회.
- 지형: 트라이플레이너 타일(잔디·흙·돌) + 노이즈 블렌드, 초목은 `InstancedMesh` 로 300~2000개.
- 그림자: `PCFSoftShadowMap`, 캐스케이드 대신 카메라 추적 직교 라이트 1개(범위 60m).
- 폰: 픽셀 비율 상한 1.5, SSAO 끔, 외곽선 유지.
- 전부 기존 구조 안에서 재질·후처리만 갈아 끼우는 작업이다. 새 렌더러를 쓰지 않는다.

### 6.2 Godot 4.7
- `WorldEnvironment`: 톤맵 AgX(4.3+), SSAO on(반경 1.0·강도 2), SSIL 데스크톱만, Glow(HDR 임계 1.0·강도 0.6), Adjustments(대비 1.05·채도 1.1) + 색보정 LUT 1장(판별), Fog(높이·깊이) 색=하늘.
- 전역 조명: 모바일 목표라 SDFGI 대신 **LightmapGI 베이크**(정적) + `ReflectionProbe` 1개. Forward+ 는 데스크톱, Mobile 렌더러 프로파일 별도 유지.
- 캐릭터: 툰 셰이더 1개(`shading_mode: unshaded` + 램프 텍스처 샘플, 림), 외곽선 `next_pass` 뒤집힌 헐. VRoid 는 `MToon` 호환 셰이더로만 쓰고 §6.0-1 에 맞지 않으면 실루엣만 참고한다.
- 초목·소품: `MultiMeshInstance3D`, 풀은 바람 셰이더(정점 흔들림).
- 애니: `AnimationTree` 블렌드(이동 속도 1D 블렌드·공격 원샷), 루트 모션 대신 발 미끄러짐 보정 0.2.
- 지형: `Terrain3D` 애드온 대신 자체 heightmap 메시 + 트라이플레이너 셰이더(의존 최소).

### 6.3 Unity 6 (URP)
- `Volume` 프로파일 1개(판별 오버라이드): Tonemapping ACES, Bloom(임계 0.9·강도 0.5), Color Adjustments(대비 5·채도 10), Vignette 0.25, SSAO Renderer Feature(반경 0.5), Screen Space Shadows.
- 툰: Shader Graph 1개(램프 텍스처·림·팔레트 스냅 옵션) + `Render Objects` 외곽선 패스(뒤집힌 헐 레이어). Lit 셰이더 캐릭터를 남기지 않는다.
- 조명: Directional 1 + Adaptive Probe Volumes(6000.x) 또는 라이트맵 베이크, 리플렉션 프로브 1.
- VRoid: UniVRM + MToon10 으로만 임포트. §6.0-1 에 안 맞으면 캐릭터 기본은 저폴리 팩으로 하고 VRoid 는 초상·컷신에만.
- 지형: Terrain + Detail 인스턴싱 대신 자체 heightmap 메시(godot 과 동일 규격, 데이터 공유 가능 — 코드는 공유 안 함).
- `Assets/Art/*_candidates` 폴더는 결정 뒤 하나만 남기고 삭제(§10-Q3).

### 6.4 "허접해 보이는" 10가지와 처방
| 원인 | 처방 |
|---|---|
| 에셋 스타일 혼재 | §6.0-1, §7.2 팔레트 스냅 |
| 후처리 없음(평평한 색) | §6.1~6.3 볼륨/환경 1개 |
| 그림자 없거나 계단 | 소프트 섀도 + 접지 그림자(blob) 겸용 |
| 바닥 한 색 | 트라이플레이너 3타일 + 노이즈 + 데칼(길·얼룩) |
| 하늘·안개 없음 | 그라디언트 스카이 + 지평선 안개 색 일치 |
| 스케일 뒤죽박죽 | §6.0-3 임포트 자동 리스케일 |
| 애니 끊김 | 블렌드 0.15s·공격 원샷·idle 미세 흔들림 |
| 타격 반응 없음 | §3-C 5요소 |
| UI 폰트·패널 불일치 | 폰트 1(본문)+1(숫자), 9-slice 패널 1종, 8px 그리드 |
| 카메라 흔들림·클리핑 | 스프링암 + 충돌 당김, 근접 페이드 |

## 7. 에셋 창조 — 무엇이 가능한가(정직하게)

### 7.1 이 환경에서 확인된 것(2026-09-16)
| 도구 | 상태 | 쓸 수 있는 일 |
|---|---|---|
| Python 3.14 + Pillow 12 + numpy | 있음 | 텍스처·타일·스프라이트 시트·노멀맵·팔레트 스왑·아이콘 일괄 생성 |
| trimesh | 있음 | **GLB 절차 생성**(바위·나무·건물 조립·소품)·기존 GLB 병합·리스케일·정점색 팔레트 스냅 |
| Node 20 + three.js r169 | 있음 | 런타임 절차 메시·셰이더·파티클 |
| ffmpeg | 있음 | 절차 사운드(사가국지 방식) 변환·루프 처리 |
| Blender / ImageMagick / Godot CLI | **없음** | 설치하면 §7.3 확장 |
| GPU 이미지 생성 모델 | 미확인 | §7.4 |

세션(Claude)이 **직접 만들 수 있는 것**: 위 도구로 돌아가는 스크립트와 그 산출물(PNG·GLB·JSON), 셰이더·파티클 코드, SVG UI 아이콘, 절차 지형·식생 코드. **못 하는 것**: 손그림 품질의 2D 일러스트, 리깅된 인간형 애니 캐릭터를 처음부터.

### 7.2 변형 배가 파이프라인(가장 싸고 즉시 가능) — `tools/asset-forge/` 신설 제안
현재 웹 GLB 767개·godot `assets/`·unity `Assets/Art/` 가 있다. 새로 그리지 않고 **조합으로 수천 변형**을 만든다.
1. `palette.py` — 팔레트 JSON(바이옴 24색) → GLB 정점색/텍스처를 최근접 색으로 스냅. 같은 나무 1종 → 바이옴 5종.
2. `kitbash.py` — 부품 GLB(몸·머리·무기·장식) 조합표 → 캐릭터/건물 변형 생성(trimesh 병합, 스케일 §6.0-3).
3. `procgen.py` — 바위(노이즈 변형 구체)·나무(L-system 3단)·울타리·돌담·비석 등 소품을 씨앗으로 생성.
4. `tilegen.py` — 512px 트라이플레이너 타일(잔디·흙·돌·눈·모래·용암) + 노멀 + 러프니스, 판별 팔레트로.
5. `spritegen.py` — 아이콘·UI 9-slice·상태 아이콘을 SVG→PNG.
6. `sfxgen.py` — 타격·획득·UI 음 3종씩 라운드로빈(사가국지 절차 사운드 이식) → ogg.
출력은 각 판 `assets/generated/` 로, 원본 팩과 섞지 않는다. 씨앗 고정으로 재생성 가능(저장소에 결과물 대신 스크립트+씨앗만 둘 수도 있다 — §10-Q4).

### 7.3 사람이 설치하면 열리는 것(무료)
| 도구 | 용도 | 비고 |
|---|---|---|
| Blender 4.x | 킷배싱·리토폴로지·베이크·자동 리깅(Rigify)·Python 배치 | 헤드리스 `blender -b -P` 로 세션이 자동화 가능 |
| Mixamo | 인간형 자동 리깅 + 애니 2천 종 | Adobe 계정, 게임 사용 허용. 인물 105명 모션 통일에 가장 빠름 |
| VRoid Studio | 이미 사용. 인물 초상·컷신용 | §6.0-1 스타일과 충돌 — 툰 셰이더로만 |
| Material Maker | 절차 텍스처 노드 | MIT |
| Krita / GIMP | 2D 수정 | |
| Audacity | 사운드 | |

### 7.4 AI 생성(가능하나 조건부)
- **2D**(초상·아이콘·텍스처): Stable Diffusion 로컬(GPU 8GB+)·ControlNet 으로 실루엣 유지·LoRA 로 스타일 고정. 상용 서비스는 라이선스 확인 필수.
- **3D**(소품·건물): TRELLIS·Hunyuan3D-2(오픈)·Meshy/Tripo(서비스) — 소품·건물엔 쓸 만, 애니 캐릭터는 리깅 후처리(Mixamo) 필요. 폴리 수 정리(Blender decimate) 필수.
- **정책**: 원작 IP·실존 인물을 프롬프트에 쓰지 않는다(이름 정책과 같은 이유). 생성물도 §7.2 팔레트 스냅을 거쳐 스타일을 맞춘다.
- 이 PC 의 GPU 여부·모델 설치는 미확인(§10-Q5).

## 8. 버그 대책(웹)

1. **기능 동결 + 버그 스프린트**: 각 판 PLAN §6 로드맵 첫 Phase 를 "안정화" 로 둔다. 실기기 확인 대기 목록을 전부 닫기 전엔 새 시스템을 넣지 않는다.
2. **오류 수집**: `window.onerror`·`unhandledrejection` → localStorage 링버퍼 50건 → `_admin.html` "오류" 탭에서 보기·복사. 사용자 제보가 "이동하면 갈색" 같은 증상 말고 스택으로 온다.
3. **세이브 스키마 버전**: 모든 세이브에 `v` 필드, 마이그레이션 함수 체인, `_test.html` 에 "v(n-1) 세이브 로드" 항목. 사가국지 `aa4b8b8` 류 재발 방지.
4. **QA 프리셋**: `_admin.html` 프리셋으로 제보 상황을 1클릭 재현(이미 있는 기능을 제보마다 추가).
5. **커밋 전 자동**: `node -c` 전 파일 + `_test.html` RESULT 를 `tools/precheck.sh` 하나로. 훅은 커밋하지 않고 스크립트만 둔다.
6. **폰 성능 상한**: 픽셀 비율 1.5·SSAO 끔·인스턴스 수 상한을 설정 시트 "성능" 손잡이로. "갈색 화면" 류는 대개 포스트 패스 실패다 — 실패 시 후처리 자동 끔.

## 9. 문서·토큰 규칙 — 3층

**현재 방식 평가**: CLAUDE.md 압축과 "통째로 읽지 않는다" 규칙은 맞는 방향이지만 **절반**이다. 진짜 낭비는 (a) PLAN 에 세션 기록을 덧붙이는 습관과 (b) PROJECT_STATE 가 "짧게" 규칙을 두고도 600KB 로 자란 것이다. 읽는 쪽 규칙만으론 못 막는다 — **쓰는 쪽 규칙과 크기 상한**이 있어야 한다.

| 층 | 파일 | 내용 | 상한 | 쓰기 규칙 |
|---|---|---|---|---|
| 1 규칙 | `CLAUDE.md`(루트·폴더) | 불변 규칙·경로·금지 | 6KB | 이력·"이전엔" 문장 금지 |
| 2 설계 | `PLAN.md`, `SAGA-DESIGN.md` | 정체성·규칙·설계·로드맵·열린 질문 | PLAN 70KB(사가블로 90KB) | 날짜 세션 기록 금지. 완료된 Phase 는 한 줄로 접는다 |
| 3 이력 | `SAGA-HANDOFF.md`, 각 판 `HANDOFF.md`, 3D `docs/HISTORY.md` | 날짜별 세션 기록, 실기 확인 대기 | 없음(append-only) | grep 으로만 읽는다. 머리말에 그 규칙 |
| 상태 | 3D `docs/PROJECT_STATE.md`, 웹 `README.md` "현재" 절 | 지금 상태만 | 15KB | 세션이 끝나면 **덮어쓴다**, 덧붙이지 않는다 |

- 새 세션 시작 절차: 루트 CLAUDE.md(자동) → 해당 폴더 CLAUDE.md(자동) → PLAN 목차 grep → 상태 파일 → 필요 시 HANDOFF grep. 이 순서로 첫 턴 입력 30KB 이내.
- 세션 종료 절차: HANDOFF 에 날짜 항목 append → 상태 파일 덮어쓰기 → PLAN 은 **결정이 바뀌었을 때만** 수정.
- 크기 상한 검사: `wc -c` 를 `tools/precheck.sh` 에 넣어 넘으면 경고.

### 9.3 PLAN.md 새 틀(일곱 판 공통)
```
# <판> PLAN — 정본
0. 읽는 법        크기·목차 grep·이력은 HANDOFF·이 파일엔 날짜 기록 금지
1. 정체성         한 줄·원작 오마주·이 판만의 차별점·인물이 되는 것(§5)·플레이어 판타지
2. 반드시 지킬 것  이 판 고유 불변 규칙(루트 CLAUDE.md 중복은 링크만)
3. 현재 시스템 지도 표: 시스템 | 주 파일 | 상태(완료/부분/미검증) | 진단 항목
4. 재미 진단       §3 표준 8 각각 현재 상태(○△×)·근거(파일·수치)·가장 큰 구멍 3개
5. 게임성 확장     후보 6~8, 각각 9필드 — 참고 게임 / 채우는 표준(§3) / 왜 / 메커니즘 / 수치 /
                  UI·조작 / 세이브 스키마 변경·마이그레이션 / 진단 항목 / 범위(파일)·우선순위·불변규칙 충돌 확인
6. 그래픽·에셋     §6 중 이 판 적용분, 판별 팔레트 24색, §7.2 변형 계획, 교체 대상 목록
7. 버그·안정화     실기 확인 대기 목록(HANDOFF 에서 가져와 닫을 것만), §8 적용
8. 로드맵          Phase 표(우선순위 순·상태). Phase 0 = 안정화
9. 검증           진단 명령·씨앗·고정 축·헤드리스 금지 규칙 링크
10. 열린 질문      사용자 결정 필요
```
3D 트랙 PLAN 은 기존 장 번호를 유지한다(PROJECT_STATE·코드가 장 번호를 가리킨다). 0장(읽는 법)과 새 장(재미·그래픽·에셋·안정화)을 **끝에 추가**하고, 날짜 기록은 `docs/HISTORY.md` 로 옮긴다.

## 10. 열린 질문(사용자 결정)

- **Q1 완성판 트랙** — **결정(2026-09-21, 2026-09-20 "Godot 먼저" 결정을 뒤집음): Unity 먼저 집중.** URP Volume·APV·Cinemachine·Mixamo 직접 임포트가 이미 돌고 있어 사실적 완성판 투자로는 준비가 더 앞서 있다는 이유로 사용자가 우선순위를 바꿨다. Godot 병행은 유지(진행 중인 작업 존중), 그래픽·에셋 신규 투자 우선순위만 Unity 쪽부터.
- **Q2 웹 다섯 판 기능 동결**: §2-1 대로 웹은 안정화·루프 실험만 하고 새 대형 시스템은 3D 로만 갈 것인가.
- **Q3 캐릭터 기본 스타일 — 결정(2026-09-21): 트랙 간 불일치를 허용한다.** 웹·godot 은 저폴리 툰, unity 는 사실적 PBR(66-2 결정) — 하나로 맞추지 않는다. §6.0-1 은 이미 이 전제로 쓰여 있었다(사실상 추인). 한 트랙 안에서 둘을 섞는 것만 계속 금지.
- **Q4 생성 에셋 커밋 방식**: 산출물(PNG·GLB) 커밋 vs 스크립트+씨앗만 커밋(저장소 가벼움, 빌드 단계 필요).
- **Q5 AI 생성 도구**: 이 PC GPU 사양·유료 서비스 사용 의사.
- **Q6 계정 공용 업적**: 다섯 판이 같은 `account.js` 를 복사해 쓰므로(서버 없음, localStorage) 한 브라우저 안에서만 "다른 판 진척" 을 읽어 보상하는 교차 해제가 가능하다. 다섯 벌 복사 원칙은 안 깨진다(읽기만). 넣을 것인가.
- **Q7 사가블로 퓨전 선행**: 2026-09-10 "사가블로만 먼저" 결정과 이번 "다섯 판 전부 게임성 추가" 결정을 어떻게 겹칠 것인가(권장: 루프(§3)는 다섯 판 동시, 시대 퓨전 콘텐츠는 사가블로 선행 유지).

## 11. 2D 모드 — 코드로 그린 스프라이트를 실제 2D 에셋으로 (2026-09-20 사용자 확정, 계획 단계)

**목표(사용자)**: 2D 모드는 **없애지 않는다.** `js/sprite.js` 의 코드 그림(`human`·`beast`·`building`·`flag`·`storyize` 후처리)을 걷어 내고 실제 2D 에셋으로 바꾼다.
**계기**: 도감에서 3D 초상이 구워지기 전에 코드 그림이 먼저 보인다 — `portrait3d.img()`/`p3src()` 가 구워 둔 그림이 없으면 `S.portrait()`(코드 그림)로 시작했다가 `sweep()` 이 3D 로 갈아 끼우는 구조라서다(사가고·사가스토리·사가국지 ui 가 같은 요령).
원칙은 그대로 — **원작사의 실제 에셋 금지**, 라이선스는 CC0(또는 표시 의무를 `ASSET_LICENSES.md` 에 적은 CC-BY)만, 공개 저장소라 재배포 허용 확인 필수.

### 11-1. 현황 (2026-09-20 조사 — 파일·호출을 직접 셌다)

`sprite.js` 는 다섯 벌이 **이미 서로 다르다**(md5 다섯 종, 크기 85~102KB). 합치자는 제안은 하지 않는다(CLAUDE.md).

| 판 | `sprite.js` | `assets/sprites2d/` | 코드 그림이 아직 나오는 곳 |
|---|---|---|---|
| 사가고 | 101KB | 47개 — Kenney 사람 14·짐승 27(3D 모델 이름을 딴 파일)·건물 4·초상 얼굴 조각 | 도감·상세 초상의 **초기 그림**(`portrait`·`portraitCard`), 지도 `building()`, 에셋에 없는 배경 생물 `beast()`, 깃발 |
| 사가블로 | 92KB | **0개** | 2D 던전 뷰 `stamp()` 4곳(전부 코드로 그린 사람 + "디아블로2풍" 픽셀 후처리), 도감·상세 초상 초기 그림 |
| 사가의숲 | 95KB | 26개 — Kenney 사람 14·나무·타일 | 초상 초기 그림. (2D 마을 `stamp()` 4곳은 이미 Kenney 사람) |
| 사가스토리 | 92KB | 3개(타일뿐) | 옆면 뷰 `stamp()` 2곳(코드 그림 사람), 도감·상세 초상 초기 그림 |
| 사가국지 | 85KB | **0개** | 초상 초기 그림만(지도는 3D) |

### 11-2. 단계 (각 단계는 따로 커밋·실기 확인)

1. **Phase 0 — 코드 그림이 먼저 보이는 것 끊기(에셋 없이, 반나절)** — **코드분 완료(2026-09-20, 다섯 판, 실기 확인 대기)**. 구현·함정은 saga-go/HANDOFF.md 같은 날 「이어서 4」: 초기 `src` 를 코드 그림 대신 **중립 자리표시**(같은 크기의 빈 액자·실루엣 CSS)로. 구워 둔 그림이 있으면 그대로(현행). 다섯 판 `portrait3d.img`·`p3src`·`portraitCard` 호출부. 진단: 초기 `src` 가 `S.portrait` 의 데이터 URL 이 아닌지.
2. **Phase 1 — 초상을 디스크 에셋으로 미리 굽기**: 인물 105·펫·건물의 3D 초상을 오프라인으로 한 번 구워 `assets/portraits/<id>.webp` 로 둔다 → 도감을 열자마자 뜬다(3D 를 기다리지 않음). 도구는 `tools/bake-icons/`(건물 아이콘 굽기)의 방식을 잇는다. **헤드리스 크롬으로 굽는 일이라 사용자 승인이 필요하다**(세션 규칙: 크롬은 사용자가 요청할 때만). **완료(2026-09-20, 사용자 승인 Q-2D-1)** — 도구 `saga-web/tools/bake-portraits/bake.mjs`(게임 자신의 `portrait3d.warm()` 을 CDP 로 부려 정사각 192px·카드 300×344 webp 를 `<판>/assets/portraits/{hero,pet}/<id>_s|c.webp` 로 받고 `manifest.js` 에 목록을 적는다). 판마다 `portrait3d.js` 의 `of()`·`warm()`·`sweep()` 이 목록에 있으면 파일 주소를 바로 준다(굽기·three 없이). 단독 빌드(`build-single`)는 이 스크립트를 뺀다. 인물·펫은 다시 구울 일이 생기면 `node tools/bake-portraits/bake.mjs <판폴더>`.
3. **Phase 2 — 2D 지도·뷰의 사람**: 사가블로·사가스토리 `stamp()` 를 사가고·사가의숲과 같은 길(Kenney Roguelike Characters CC0, 인물 id 해시로 고정)로. 사가블로는 "2D 지향점 밖"이라 적혀 있었다(SAGA-HANDOFF) — **2D 를 살리는 것이므로 그 결정을 사용자에게 다시 확인**한다. **사가스토리만 완료(2026-09-20)** — `bake()` 의 사람 분기를 Kenney 열넷 + id 해시로(사가의숲과 같은 파일·같은 코드), 그림이 안 실린 첫 프레임의 코드 그림 컷은 실린 뒤 다시 굽는다. 코드 그림 `human()` 은 초상 되돌림·다른 자리가 아직 부르므로 남겼다. **사가블로도 완료(2026-09-20, 사용자가 "사가블로 2D 살려서" 로 Q-2D-2 결정)** — 같은 Kenney 열넷을 `diabloize` 후처리를 건 채로 `bake()` 에. 4곳의 `stamp()`(NPC·동행·주인공·인간형 적)가 모두 이 길을 탄다.
4. **Phase 3 — 짐승·건물·깃발**: 사가고 배경 생물·지도 건물·깃발의 남은 코드 그림을 에셋으로(3D 를 구운 2D 를 우선 검토 — 사가고 짐승 27종이 그 이름 붙이기를 따른다, 굽는 절차는 착수 때 확인). **일부 완료(2026-09-20)** — 사가블로·사가스토리 적 짐승은 3D 몸을 옆모습 걷기 시트(5컷)로 구워 씀(`bake-portraits --sprites=monsters`, 사가스토리는 사가블로 시트 셋 복사). 사가고 배경 생물 다섯 종·역참 지도 마커도 완료(2026-09-20, saga-go/HANDOFF.md 「이어서 8」) — 깃발은 코드 그림이 아니라 지도 UI 도형이라 남긴다. 사가블로·사가의숲·사가스토리·사가국지의 호출부 없는 `building()`·`flag()` 는 2026-09-21 에 삭제(saga-go/HANDOFF.md 「이어서 9」, 사가고 `building()` 은 `world.js` 가 부르므로 Phase 4 에서). 사가블로 벽·바닥·문은 `dungeon-view.js` 가 직접 그리는 절차 그림이라 이 단계 대상이 아니다.
5. **Phase 4 — 코드 그림 함수 삭제**: `human`·`beast`·`building`·`flag` 와 관련 캐시를 걷어 내고 진단(`_test.html`)이 그 함수를 안 부르는지 확인. `sprite.js` 는 에셋을 찍는 얇은 층(`stamp`·`portrait` 껍데기)만 남는다. `storyize()`(종이·선화 후처리)는 코드 그림이 아니라 **실제 그림 위에도 거는 공통 화풍 필터**라 대상이 아니다 — 끝까지 남긴다.
   **사가고 완료(2026-09-22)** — `human()`·`beast()`·전용 헬퍼(`storyFace`·`limb`·`hand`·`foot`·`headgear`·`weapon`·`leg2`·`eye`, 도합 약 1100줄) 삭제. `portrait()`·`portraitCard()`·지도 스탬프 `bake()` 는 전부 Kenney `human_*`/베이크한 `beast_*`(ogre 셋 `Orc`·`Demon`·`BlueDemon` 포함, 이미 구워져 있었다) 를 그린다. 그림이 아직 안 실린 첫 프레임만 작은 자리표시(`loadingMark`, 머리 원+몸통 타원)로 메꾸고 `stamp()` 의 `imgReady` 재굽기가 곧 실제 그림으로 바꾼다. `building()`(world.js 역참 마커 진입점)은 그대로 남음 — Phase 4 대상이 아니었다. jsdom 진단 559/561(변경 전과 동일, 남은 둘은 jsdom `Request` 한계).
   **사가블로 일부 완료(2026-09-22)** — 던전 뷰 `bake()`(사람·적 스탬프)와 `portrait()`/`portraitCard()`의 hero 갈래는 saga-go 와 같은 방식(Kenney `human_*`·`mon-manifest.js` 60종, 둘 다 data-enemy.js 를 늘 덮는다)으로 전환. **펫만 예외로 남았다** — 105종 중 원래 14종만 디스크 초상이 있고, REG 확장분 91종은 `bake-portraits --kind=pet` 재시도(210회)로도 **0회 성공**(3D 굽기 자체가 안 된다), 2D 대체 그림도 없다(saga-go 의 `BEAST_FORM_FILES` 같은 매핑이 없다) — `human()`/`beast()`/전용 헬퍼는 펫 초상 경로용으로 그대로 남겨 뒀다(사용자 결정). jsdom 362/362(변경 전과 동일).
   **사가의숲 완료(2026-09-22)** — 마을 지도 `stamp()` 4곳이 전부 `kind:'human'`뿐이라 `beast()` 갈래는 애초에 지도에서 안 탔다(배경 짐승 5종은 `village-view.js` 가 `sprite.js` 밖에서 따로 그린다). 펫 초상은 **디스크 초상 105종이 전부 구워져 있어** saga-go 와 같은 안전한 상황 — `human()`·`beast()`·전용 헬퍼 삭제, hero 갈래는 Kenney 그림, pet 갈래는 2D 대체 그림이 없어 자리표시(`loadingMark`, 3D 실패하는 드문 경우만 보임). jsdom 355/356(변경 전과 동일). 사가스토리·사가국지는 Phase 4 미착수(각 판 상황이 달라 필요할 때 따로 조사).

**제거 원칙(사용자, 2026-09-20)**: 코드 그림 함수·캐시·에셋이 **더 안 쓰이게 되는 순간 그 교체 커밋에서 같이 지운다**(Phase 4 까지 미루지 않는다). 호출부 0 은 `DG.sprite.*`·진단 포함 grep 으로 확인하고, 다른 호출부가 남아 있으면 남긴다.

### 11-3. 에셋 후보 (전부 **받기 전에 라이선스를 재확인** — 아래는 기억에 의한 후보다)

- **이미 쓰는 것**: Kenney Roguelike Characters(CC0, 사람 14) — 얼굴이 14가지뿐이라 105명에겐 모자란다. 단독으로 못 끝낸다.
- **3D 를 2D 로 굽기(권장)**: 저장소에 이미 VRoid 몸 4종 + 인물별 색 변형(`vroid-variant.js`)이 있다. 이걸 정면 초상·걷기 컷으로 구우면 **인물마다 다른 2D 그림**이 나오고 3D 와 얼굴이 같다(라이선스는 몸 파일의 것을 따르니 `ASSET_LICENSES.md` 재확인).
- **Kenney 계열 CC0**(Toon Characters·Tiny Dungeon·Animal Pack 등): 짐승·몬스터·소품 채움용 후보. 받으려면 사용자가 내려받는다(로그인 없이도 되는 것 위주).
- 피할 것: CC-BY-SA(공개 저장소 전체에 전염), 상업 금지, 원작 캐릭터 팬아트.

### 11-4. 사용자에게 받을 결정 (Phase 1 착수 전)

- ~~Q-2D-1~~ 초상 굽기를 헤드리스 크롬으로 해도 되는가 — **허용(2026-09-20)**.
- ~~Q-2D-2~~ 사가블로 2D 를 살릴 것인가 — **살린다(2026-09-20)**. 다섯 판 `SAGA-HANDOFF` 의 "사가블로는 2D 지향점 밖" 은 이걸로 뒤집힌 것.
- Q-2D-3 인물 그림 양식 — 3D 굽기(같은 얼굴) vs Kenney 류 픽셀 사람(가볍지만 얼굴 반복).
